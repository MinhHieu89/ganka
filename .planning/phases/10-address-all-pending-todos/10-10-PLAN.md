---
phase: 10-address-all-pending-todos
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/Pharmacy.Unit.Tests/Features/DrugCatalogImportHandlerTests.cs
  - backend/tests/Clinical.Unit.Tests/Features/OsdiNotificationServiceTests.cs
  - backend/tests/Shared.Unit.Tests/Features/UploadClinicLogoHandlerTests.cs
  - backend/tests/Clinical.Unit.Tests/Features/PrintBatchLabelsHandlerTests.cs
autonomous: true
requirements: []

must_haves:
  truths:
    - "ConfirmDrugCatalogImport tests cover: invalid enum values, negative MinStockLevel, empty Name"
    - "OsdiNotificationService test asserts LogWarning was called on SignalR failure"
    - "UploadClinicLogo tests assert settingsService was called to persist blob URL"
    - "PrintBatchLabelsHandler tests remove dead _visitRepository mock scaffolding"
    - "Test exists for prescription with empty items list in batch labels"
  artifacts: []
  key_links: []
---

<objective>
Fill test coverage gaps identified by code review across plans 10-02, 10-03, and 10-04.

Purpose: Address 6 test coverage gaps including missing edge case tests for the confirm import handler, missing assertion on warning log in OSDI notification, missing settings persistence test for logo upload, dead mock scaffolding, and missing empty-items test for batch labels. Note: Some of these tests will need to be updated after 10-08 fixes the underlying code.
Output: Comprehensive test coverage meeting the 80% threshold required by project guidelines.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task id="1" name="Fix pharmacy and shared test gaps" commit_prefix="test(10-10)">
**1. ConfirmDrugCatalogImport edge case tests** (`DrugCatalogImportHandlerTests.cs`):
Add tests for:
- Row with unrecognized Form/Route enum string — verify behavior (default enum value or error)
- Row with negative MinStockLevel — verify validation error or domain exception
- Row with empty Name — verify validation error
- Duplicate names within batch — verify handling
These tests should be written against the current behavior first, then updated after 10-08 adds validation.

**2. UploadClinicLogo settings persistence test** (`UploadClinicLogoHandlerTests.cs`):
- Add test asserting `_settingsService.Received(1).CreateOrUpdateAsync(...)` was called with the blob URL after successful upload
- Note: This test will fail until 10-08 implements the actual persistence. Write it as a failing test to document the expected behavior.

**3. Remove dead _visitRepository mock** (`PrintBatchLabelsHandlerTests.cs`):
- Remove unused `_visitRepository` field and its setup from tests
- Clean up any other dead test scaffolding
</task>

<task id="2" name="Fix clinical test gaps" commit_prefix="test(10-10)">
**4. OsdiNotificationService warning log assertion** (`OsdiNotificationServiceTests.cs`):
Update `NotifyOsdiSubmittedAsync_SignalRFailure_LogsWarningDoesNotThrow` to assert that `LogWarning` was actually called using NSubstitute's ILogger extension.

**5. Batch labels empty items test** (`PrintBatchLabelsHandlerTests.cs`):
Add test for prescription with zero items — verify behavior (either error result or empty PDF as accepted outcome). Document the expected behavior.
</task>

</tasks>
