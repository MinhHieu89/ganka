---
phase: 10-address-all-pending-todos
plan: 05
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - frontend/src/features/pharmacy/api/pharmacy-api.ts
  - frontend/src/features/pharmacy/hooks/pharmacy-queries.ts
  - frontend/src/features/pharmacy/components/DrugCatalogPage.tsx
  - frontend/src/features/pharmacy/components/ExcelImportDialog.tsx
  - frontend/src/features/pharmacy/components/OtcSalePage.tsx
autonomous: true
requirements: [TODO-05, TODO-11, TODO-12]

must_haves:
  truths:
    - "Drug catalog page uses server-side pagination with page/pageSize/search parameters"
    - "Drug catalog page shows Excel import button that opens preview dialog"
    - "Excel import dialog shows valid rows green, invalid rows red with per-cell errors"
    - "OTC sale form shows inline stock warning when quantity exceeds available stock"
    - "OTC sale submit button is disabled when any row exceeds available stock"
  artifacts:
    - path: "frontend/src/features/pharmacy/components/ExcelImportDialog.tsx"
      provides: "Excel import dialog with validation preview table"
      min_lines: 80
    - path: "frontend/src/features/pharmacy/components/DrugCatalogPage.tsx"
      provides: "Drug catalog page with server-side pagination"
    - path: "frontend/src/features/pharmacy/components/OtcSalePage.tsx"
      provides: "OTC sale form with inline stock validation"
  key_links:
    - from: "DrugCatalogPage.tsx"
      to: "/api/pharmacy/drugs"
      via: "paginated API call with page/pageSize/search"
      pattern: "page.*pageSize.*search"
    - from: "ExcelImportDialog.tsx"
      to: "/api/pharmacy/drugs/import/preview"
      via: "file upload for preview"
      pattern: "import/preview"
    - from: "OtcSalePage.tsx"
      to: "/api/pharmacy/drugs/{id}/available-stock"
      via: "stock check on drug selection and quantity change"
      pattern: "available-stock"
---

<objective>
Frontend implementation for pharmacy module enhancements: server-side pagination on drug catalog page, Excel import dialog with validation preview, and OTC sale inline stock validation.

Purpose: Connect the drug catalog page to the new paginated backend endpoint, add Excel import UI with green/red row preview, and add inline stock warnings to the OTC sale form.
Output: Updated DrugCatalogPage with pagination, new ExcelImportDialog, updated OtcSalePage with stock warnings.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-address-all-pending-todos/10-CONTEXT.md
@.planning/phases/10-address-all-pending-todos/10-RESEARCH.md
@.planning/phases/10-address-all-pending-todos/10-02-SUMMARY.md
@frontend/src/features/pharmacy/api/pharmacy-api.ts
@frontend/src/features/pharmacy/hooks/pharmacy-queries.ts
@frontend/src/features/pharmacy/components/DrugCatalogPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drug catalog server-side pagination + Excel import dialog</name>
  <files>
    frontend/src/features/pharmacy/api/pharmacy-api.ts,
    frontend/src/features/pharmacy/hooks/pharmacy-queries.ts,
    frontend/src/features/pharmacy/components/DrugCatalogPage.tsx,
    frontend/src/features/pharmacy/components/ExcelImportDialog.tsx
  </files>
  <action>
    1. Update `pharmacy-api.ts`:
       - Add `searchDrugCatalog(params: { page: number, pageSize: number, search?: string })` that calls GET /api/pharmacy/drugs with query params and returns `{ items, totalCount, page, pageSize, totalPages }`
       - Add `importDrugCatalogPreview(file: File)` that POSTs multipart to /api/pharmacy/drugs/import/preview
       - Add `confirmDrugCatalogImport(validRows: ValidDrugCatalogRow[])` that POSTs to /api/pharmacy/drugs/import/confirm
       - Add `getDrugCatalogTemplate()` that GETs /api/pharmacy/drugs/import/template and returns blob

    2. Update `pharmacy-queries.ts`:
       - Add `useSearchDrugCatalog(page, pageSize, search)` hook using TanStack Query with keepPreviousData
       - Add `useImportDrugCatalogPreview()` mutation hook
       - Add `useConfirmDrugCatalogImport()` mutation hook that invalidates drug catalog query on success

    3. Update `DrugCatalogPage.tsx`:
       - Replace client-side filtering with `useSearchDrugCatalog` hook
       - Add search input that debounces (300ms) and passes to the hook
       - Use TanStack Table `manualPagination: true` with page/pageSize state (follow PatientTable.tsx pattern)
       - Show pagination controls: page X of Y, previous/next buttons
       - Add "Import from Excel" button in the toolbar that opens ExcelImportDialog
       - Add "Download Template" button that downloads the Excel template

    4. Create `ExcelImportDialog.tsx`:
       - Dialog with file upload input (accept .xlsx, .xls)
       - On file select, call importDrugCatalogPreview mutation
       - Show preview table with columns matching template (Name, NameVi, GenericName, Form, Route, Strength, Unit, SellingPrice, MinStockLevel)
       - Valid rows: green background or green check icon
       - Invalid rows: red background with error message tooltip/text per cell that has errors
       - "Import Valid Rows" button calls confirmDrugCatalogImport with only valid rows
       - Show count: "X valid, Y invalid"
       - On success, close dialog and show toast
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Drug catalog page uses server-side pagination with search. Excel import dialog shows preview with valid/invalid rows. Template downloadable. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: OTC sale inline stock validation</name>
  <files>
    frontend/src/features/pharmacy/api/pharmacy-api.ts,
    frontend/src/features/pharmacy/hooks/pharmacy-queries.ts,
    frontend/src/features/pharmacy/components/OtcSalePage.tsx
  </files>
  <action>
    1. Add to `pharmacy-api.ts`:
       - `getDrugAvailableStock(drugCatalogItemId: string)` that calls GET /api/pharmacy/drugs/{id}/available-stock

    2. Add to `pharmacy-queries.ts`:
       - `useDrugAvailableStock(drugCatalogItemId: string | undefined)` hook that fetches available stock, enabled only when drugCatalogItemId is truthy

    3. Update `OtcSalePage.tsx` (or the OTC sale form component):
       - For each OTC sale line item, call `useDrugAvailableStock` when drug is selected
       - On drug selection AND quantity change, check if quantity > availableStock
       - Show inline warning under the row: "Only {N} in stock" (yellow) or "Out of stock" (red) when quantity exceeds available
       - Disable the form submit button if ANY row has quantity > availableStock
       - Use a per-row stock check pattern (one hook call per active line, not a single batch call)
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>OTC sale form shows inline stock warnings on drug selection and quantity change. Submit disabled when stock exceeded. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with zero errors
- Drug catalog page shows paginated results with search
- Excel import dialog shows valid/invalid rows with errors
- OTC sale form shows stock warnings inline
- Submit button disabled when stock exceeded
</verification>

<success_criteria>
Drug catalog page uses server-side pagination with debounced search. Excel import shows validation preview. OTC sale has inline stock validation with disabled submit. All TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/10-address-all-pending-todos/10-05-SUMMARY.md`
</output>
