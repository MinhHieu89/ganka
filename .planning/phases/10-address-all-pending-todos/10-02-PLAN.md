---
phase: 10-address-all-pending-todos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DrugCatalog/ImportDrugCatalogFromExcel.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DrugCatalog/ConfirmDrugCatalogImport.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DrugCatalog/GetDrugCatalogTemplate.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/GetDrugAvailableStock.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs
  - backend/tests/Pharmacy.Unit.Tests
autonomous: true
requirements: [TODO-05, TODO-11, TODO-12]

must_haves:
  truths:
    - "Drug catalog endpoint accepts page/pageSize/search params and returns paginated results with totalCount"
    - "Excel file upload parses drug catalog data and returns preview with valid/invalid rows and per-cell errors"
    - "Confirm import endpoint persists only valid rows from a previous preview"
    - "Available stock endpoint returns total quantity across all valid batches for a given drug"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs"
      provides: "Server-side paginated drug catalog search"
      contains: "pageSize"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DrugCatalog/ImportDrugCatalogFromExcel.cs"
      provides: "Excel import with preview validation"
      contains: "MiniExcel"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/GetDrugAvailableStock.cs"
      provides: "Available stock query for OTC validation"
  key_links:
    - from: "SearchDrugCatalog.cs"
      to: "IDrugCatalogItemRepository"
      via: "paginated repository method"
      pattern: "GetAllAsync.*page.*pageSize"
    - from: "ImportDrugCatalogFromExcel.cs"
      to: "MiniExcel"
      via: "Excel parsing"
      pattern: "MiniExcel\\.Query"
    - from: "GetDrugAvailableStock.cs"
      to: "IDrugBatchRepository"
      via: "FEFO batch query"
      pattern: "GetAvailableBatchesFEFOAsync"
---

<objective>
Backend TDD implementation for pharmacy module enhancements: server-side drug catalog pagination, Excel drug catalog import with preview, and OTC available stock check endpoint.

Purpose: Enable the drug catalog page to handle large datasets with server-side pagination/search, allow bulk drug catalog import via Excel with validation preview, and provide stock availability data for OTC sale inline warnings.
Output: 3 new handlers with unit tests, updated API endpoints.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-address-all-pending-todos/10-CONTEXT.md
@.planning/phases/10-address-all-pending-todos/10-RESEARCH.md
@backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs
@backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/ImportStockFromExcel.cs
@backend/src/Modules/Pharmacy/Pharmacy.Application/Features/CreateDrugCatalogItem.cs
@backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/CreateOtcSale.cs
@backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs

<interfaces>
From Pharmacy.Application.Features.StockImport.ImportStockFromExcel.cs (pattern reference):
- MiniExcel.Query<T>(stream, hasHeader: true) for parsing
- ExcelImportPreview(ValidLines, Errors) for two-phase import

From Pharmacy.Application.Features.SearchDrugCatalog.cs (to be updated):
- Current SearchDrugCatalog query — needs page/pageSize params added

From Pharmacy.Application (existing patterns):
- IDrugBatchRepository.GetAvailableBatchesFEFOAsync(drugCatalogItemId, ct) for batch queries
- IDrugCatalogItemRepository for catalog CRUD
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server-side drug catalog pagination + OTC stock check endpoint</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/GetDrugAvailableStock.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs,
    backend/tests/Pharmacy.Unit.Tests
  </files>
  <behavior>
    - SearchDrugCatalog with page=1, pageSize=10 returns first 10 items and totalCount
    - SearchDrugCatalog with search="amox" filters by name/nameVi containing the term
    - SearchDrugCatalog with page=2 returns second page of results
    - GetDrugAvailableStock returns sum of AvailableQuantity across all non-expired batches for a given drug
    - GetDrugAvailableStock returns 0 when no batches exist
  </behavior>
  <action>
    1. Update `SearchDrugCatalog.cs`:
       - Add `Page` (int, default 1), `PageSize` (int, default 20) to the query record
       - Update handler to use `Math.Clamp` for page/pageSize (same pattern as GetStockImports)
       - Add paginated repository method or update existing to accept page/pageSize/search
       - Return new response record: `SearchDrugCatalogResponse(Items, TotalCount, Page, PageSize, TotalPages)`
       - Also add `IDrugCatalogItemRepository.GetPaginatedAsync(page, pageSize, search, ct)` if not exists

    2. Create `GetDrugAvailableStock.cs`:
       - Query: `GetDrugAvailableStockQuery(Guid DrugCatalogItemId)`
       - Handler: call `IDrugBatchRepository.GetAvailableBatchesFEFOAsync(drugCatalogItemId, ct)` and return `Sum(b.AvailableQuantity)`
       - Response: simple int (total available quantity)

    3. Register both endpoints in `PharmacyApiEndpoints.cs`:
       - GET /api/pharmacy/drugs?page=1&pageSize=20&search=term (update existing)
       - GET /api/pharmacy/drugs/{id}/available-stock (new)

    4. Write unit tests following TDD: tests FIRST, then implementation.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet test tests/Pharmacy.Unit.Tests/ --no-build -v q 2>&1 | tail -5</automated>
  </verify>
  <done>SearchDrugCatalog returns paginated results with totalCount. GetDrugAvailableStock returns stock sum. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drug catalog Excel import with validation preview</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DrugCatalog/ImportDrugCatalogFromExcel.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DrugCatalog/ConfirmDrugCatalogImport.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DrugCatalog/GetDrugCatalogTemplate.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs,
    backend/tests/Pharmacy.Unit.Tests
  </files>
  <behavior>
    - ImportDrugCatalogFromExcel parses Excel and returns preview with valid rows (green) and invalid rows (red with errors)
    - Invalid row: missing required field (Name) shows error "Name is required" on that cell
    - Invalid row: negative SellingPrice shows error "SellingPrice must be positive"
    - ConfirmDrugCatalogImport creates DrugCatalogItems for all valid rows from preview
    - GetDrugCatalogTemplate returns a byte[] Excel template with expected column headers
  </behavior>
  <action>
    1. Create `ImportDrugCatalogFromExcel.cs`:
       - Command takes Stream (uploaded file)
       - Use `MiniExcel.Query<DrugCatalogExcelRow>(stream, hasHeader: true)` to parse
       - DrugCatalogExcelRow: Name, NameVi, GenericName, Form, Route, Strength, Unit, SellingPrice, MinStockLevel
       - Validate each row: Name required, SellingPrice >= 0, etc.
       - Return `DrugCatalogImportPreview(List<ValidDrugCatalogRow> ValidRows, List<DrugCatalogImportError> Errors)`
       - Each error has RowNumber, ColumnName, ErrorMessage
       - Follow ImportStockFromExcel.cs pattern exactly

    2. Create `ConfirmDrugCatalogImport.cs`:
       - Command takes `List<ValidDrugCatalogRow>` (the valid rows from preview)
       - Creates DrugCatalogItem entities via DrugCatalogItem.Create() for each row
       - Uses IDrugCatalogItemRepository and IUnitOfWork
       - Returns count of items created

    3. Create `GetDrugCatalogTemplate.cs`:
       - Returns Excel template bytes using MiniExcel.SaveAs with column headers
       - Columns match DrugCatalogExcelRow properties

    4. Register endpoints:
       - POST /api/pharmacy/drugs/import/preview (multipart file upload)
       - POST /api/pharmacy/drugs/import/confirm (JSON body with valid rows)
       - GET /api/pharmacy/drugs/import/template (download Excel template)

    5. Write unit tests following TDD: tests FIRST, then implementation.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet test tests/Pharmacy.Unit.Tests/ --no-build -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Excel import with preview validation works. Template download endpoint works. Confirm endpoint creates catalog items. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet test backend/tests/Pharmacy.Unit.Tests/ -v q` passes with all new tests
- `dotnet build backend/` compiles without errors
- Server-side pagination returns correct page/totalCount
- Excel import preview shows valid/invalid rows with per-cell errors
- Available stock endpoint returns correct batch sum
</verification>

<success_criteria>
3 new backend features implemented with TDD: server-side drug catalog pagination, Excel import with validation preview, and OTC available stock check. All unit tests pass with 80%+ coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/10-address-all-pending-todos/10-02-SUMMARY.md`
</output>
