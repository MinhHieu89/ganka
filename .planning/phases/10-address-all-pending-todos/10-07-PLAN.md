---
phase: 10-address-all-pending-todos
plan: 07
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03, 10-04, 10-05, 10-06]
files_modified: []
autonomous: false
requirements: [TODO-01, TODO-02, TODO-03, TODO-04, TODO-05, TODO-06, TODO-07, TODO-08, TODO-09, TODO-10, TODO-11, TODO-12, TODO-13]

must_haves:
  truths:
    - "All 13 todos are addressed and verifiable"
    - "Backend builds and all tests pass"
    - "Frontend compiles without TypeScript errors"
    - "All features work end-to-end when manually tested"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for all Phase 10 todos.

Purpose: Verify all 13 todo items are correctly implemented and working together. Run automated tests and have user verify key features visually.
Output: Verified Phase 10 completion.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-address-all-pending-todos/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification - build, tests, TypeScript</name>
  <files></files>
  <action>
    1. Run full backend build: `dotnet build backend/`
    2. Run full backend test suite: `dotnet test backend/ -v q`
    3. Verify 80%+ code coverage on new handlers
    4. Run frontend TypeScript check: `cd frontend && npx tsc --noEmit`
    5. Check for any console errors or warnings

    Report results with pass/fail for each check.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet test -v q 2>&1 | tail -10 && cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Backend builds clean, all tests pass, frontend TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 13 Phase 10 todo items: quick UX fixes (textarea auto-resize, OpticalPrescription defaultOpen, DrugCombobox auto-focus, patient name link), bug fixes (stock import search, OTC stock validation), and features (dry eye metric charts, OSDI answers display, realtime OSDI, drug catalog pagination, Excel import, batch labels, logo upload)</what-built>
  <how-to-verify>
    Start backend (port 5255) and frontend (port 3000), then verify:

    1. **AutoResizeTextarea**: Navigate to any page with a textarea (e.g., visit notes). Type multiple lines -- textarea should auto-expand height.

    2. **OpticalPrescriptionSection**: Open a visit detail page -- optical prescription section should be expanded by default.

    3. **DrugCombobox**: In a prescription form, open the drug combobox -- search input should auto-focus.

    4. **Patient name link**: On visit detail page, click patient name -- should open patient profile in new tab.

    5. **Drug catalog pagination**: Navigate to Pharmacy > Drug Catalog. Should show paginated table with search. Type a drug name -- should filter server-side.

    6. **Excel import**: On drug catalog page, click "Import from Excel". Upload a test Excel file. Should show preview with valid/invalid rows.

    7. **OTC stock validation**: Go to OTC sales. Add a drug and set quantity higher than available stock. Should see inline warning and submit button disabled.

    8. **Dry eye charts**: Open a patient with dry eye assessments > Dry Eye tab. Should see 5 separate charts (TBUT, Schirmer, etc.) with OD/OS lines. Test time range selector.

    9. **OSDI answers**: On a visit with OSDI score, click "View Details" next to OSDI score. Should expand showing 12 questions grouped by category.

    10. **Batch labels**: On a visit with drug prescription, find "Print All Labels" button. Click it -- should download/open a PDF with one label per drug.

    11. **Logo upload**: Go to Settings > Clinic Settings. Upload a logo image. Should preview and save.

    12. **Stock import search**: Go to stock import page. Drug search combobox should correctly filter drugs by name.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 13 todos verified both automatically and manually.
</verification>

<success_criteria>
All automated checks pass (build, tests, TypeScript). User confirms all 13 features work correctly via manual verification.
</success_criteria>

<output>
After completion, create `.planning/phases/10-address-all-pending-todos/10-07-SUMMARY.md`
</output>
