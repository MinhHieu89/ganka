---
phase: 10-address-all-pending-todos
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Shared/Shared.Application/Features/UploadClinicLogo.cs
  - backend/src/Shared/Shared.Infrastructure/Services/ClinicSettingsService.cs
  - backend/src/Modules/Clinical/Clinical.Infrastructure/Documents/BatchPharmacyLabelDocument.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/Prescriptions/PrintBatchLabels.cs
  - backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs
  - backend/src/Shared/Shared.Infrastructure/SharedApiEndpoints.cs
  - backend/tests/Shared.Unit.Tests
  - backend/tests/Clinical.Unit.Tests
  - frontend/src/features/pharmacy/components/StockImportPage.tsx
autonomous: true
requirements: [TODO-04, TODO-08, TODO-09]

must_haves:
  truths:
    - "Clinic logo can be uploaded via POST endpoint and stored in Azure Blob Storage"
    - "ClinicSettings.LogoBlobUrl is updated after successful upload"
    - "Batch label endpoint generates a single PDF with one label per prescribed drug"
    - "Stock import drug search combobox filtering works correctly"
  artifacts:
    - path: "backend/src/Shared/Shared.Application/Features/UploadClinicLogo.cs"
      provides: "Logo upload handler"
      contains: "IAzureBlobService"
    - path: "backend/src/Modules/Clinical/Clinical.Infrastructure/Documents/BatchPharmacyLabelDocument.cs"
      provides: "Multi-page QuestPDF document for batch labels"
      contains: "PharmacyLabel"
    - path: "backend/src/Modules/Clinical/Clinical.Application/Features/Prescriptions/PrintBatchLabels.cs"
      provides: "Handler that generates batch label PDF"
  key_links:
    - from: "UploadClinicLogo.cs"
      to: "IAzureBlobService"
      via: "blob upload"
      pattern: "UploadAsync.*clinic-logos"
    - from: "UploadClinicLogo.cs"
      to: "ClinicSettings.UpdateLogo"
      via: "updates logo URL"
      pattern: "UpdateLogo"
    - from: "BatchPharmacyLabelDocument.cs"
      to: "PharmacyLabelDocument.cs"
      via: "extends existing label pattern"
      pattern: "page\\.Size.*70.*35"
---

<objective>
Backend implementation for clinic logo upload, batch pharmacy label printing, and stock import search fix.

Purpose: Enable clinic logo upload to Azure Blob Storage for document headers, generate batch pharmacy labels in a single PDF for thermal printer output, and fix the broken drug search combobox in stock import.
Output: Logo upload endpoint, batch label PDF generator, stock import search fix.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-address-all-pending-todos/10-CONTEXT.md
@.planning/phases/10-address-all-pending-todos/10-RESEARCH.md
@backend/src/Shared/Shared.Application/Services/IAzureBlobService.cs
@backend/src/Shared/Shared.Infrastructure/Entities/ClinicSettings.cs
@backend/src/Shared/Shared.Infrastructure/Services/ClinicSettingsService.cs
@backend/src/Modules/Clinical/Clinical.Infrastructure/Documents/PharmacyLabelDocument.cs

<interfaces>
From IAzureBlobService.cs:
```csharp
Task<string> UploadAsync(string containerName, string blobName, Stream content, string contentType);
```

From ClinicSettings.cs:
```csharp
public void UpdateLogo(string? logoUrl) { LogoBlobUrl = logoUrl; SetUpdatedAt(); }
```

From PharmacyLabelDocument.cs (pattern reference):
- Uses QuestPDF with page.Size(70, 35, Unit.Millimetre) for thermal labels
- Contains patient name, drug name, dose, frequency, expiry
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clinic logo upload endpoint + batch pharmacy label PDF</name>
  <files>
    backend/src/Shared/Shared.Application/Features/UploadClinicLogo.cs,
    backend/src/Shared/Shared.Infrastructure/SharedApiEndpoints.cs,
    backend/src/Modules/Clinical/Clinical.Infrastructure/Documents/BatchPharmacyLabelDocument.cs,
    backend/src/Modules/Clinical/Clinical.Application/Features/Prescriptions/PrintBatchLabels.cs,
    backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs,
    backend/tests/Shared.Unit.Tests,
    backend/tests/Clinical.Unit.Tests
  </files>
  <behavior>
    - UploadClinicLogo accepts image file (jpg/png/webp), max 5MB, uploads to Azure Blob, updates ClinicSettings.LogoBlobUrl
    - UploadClinicLogo rejects files over 5MB with validation error
    - UploadClinicLogo rejects non-image MIME types
    - PrintBatchLabels generates a PDF with one label per drug in the prescription
    - PrintBatchLabels returns PDF bytes for a given prescriptionId
  </behavior>
  <action>
    1. Create `UploadClinicLogo.cs`:
       - Command: `UploadClinicLogoCommand(Stream FileStream, string ContentType, string FileName)`
       - Handler: validate file size (max 5MB) and content type (image/jpeg, image/png, image/webp)
       - Upload to Azure Blob: container "clinic-logos", blob name `{branchId}/{timestamp}_{fileName}`
       - Load ClinicSettings for current branch, call `UpdateLogo(blobUrl)`, SaveChangesAsync
       - Return the blob URL

    2. Register endpoint in `SharedApiEndpoints.cs` (or wherever settings endpoints live):
       - POST /api/settings/clinic/logo (multipart file upload)
       - Accept IFormFile, extract stream/contentType/fileName

    3. Create `BatchPharmacyLabelDocument.cs`:
       - Extends existing PharmacyLabelDocument pattern
       - Takes a list of prescription items (drug name, dose, frequency, patient name, etc.)
       - Generates multi-page QuestPDF document: each page = 70x35mm label for one drug
       - Uses same styling as PharmacyLabelDocument but in a loop creating multiple pages

    4. Create `PrintBatchLabels.cs`:
       - Query: `PrintBatchLabelsQuery(Guid PrescriptionId)`
       - Handler: load DrugPrescription with all PrescriptionItems
       - For each item, map to label data (drug name, dosage, frequency, patient name)
       - Generate PDF using BatchPharmacyLabelDocument
       - Return byte[] for download

    5. Register endpoint: GET /api/clinical/prescriptions/{prescriptionId}/labels/batch (returns PDF)

    6. Write unit tests for UploadClinicLogo (validation logic) and PrintBatchLabels (label generation).
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet build -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Logo upload validates and stores in Azure Blob. Batch label PDF generates multi-page document. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stock import drug search combobox filtering</name>
  <files>
    frontend/src/features/pharmacy/components/StockImportPage.tsx
  </files>
  <action>
    1. Find the stock import page and identify the drug search combobox component
    2. Debug the filtering issue — likely one of:
       - Combobox filtering by wrong field (id instead of name)
       - Filter function not applied or wrong comparison
       - Data not loaded before filter runs
    3. Fix the combobox so searching by drug name correctly filters the dropdown options
    4. Test locally by running the frontend and navigating to the stock import page
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Stock import drug search combobox correctly filters drugs by name when typing. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `dotnet build backend/` compiles without errors
- `dotnet test backend/tests/Clinical.Unit.Tests/ -v q` passes
- Logo upload endpoint validates file size and type
- Batch label PDF generates one page per drug
- Stock import search combobox filters correctly
</verification>

<success_criteria>
Clinic logo upload stores to Azure Blob and updates ClinicSettings. Batch pharmacy labels generate multi-page PDF for thermal printing. Stock import drug search is fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/10-address-all-pending-todos/10-04-SUMMARY.md`
</output>
