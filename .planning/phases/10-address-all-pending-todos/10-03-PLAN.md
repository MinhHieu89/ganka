---
phase: 10-address-all-pending-todos
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Clinical/Clinical.Application/Features/DryEye/GetDryEyeMetricHistory.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/Osdi/GetOsdiAnswers.cs
  - backend/src/Modules/Clinical/Clinical.Infrastructure/Hubs/OsdiHub.cs
  - backend/src/Modules/Clinical/Clinical.Infrastructure/Services/OsdiNotificationService.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/Osdi/NotifyOsdiSubmitted.cs
  - backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs
  - backend/src/Bootstrapper/Program.cs
  - backend/tests/Clinical.Unit.Tests
autonomous: true
requirements: [TODO-07, TODO-10, TODO-13]

must_haves:
  truths:
    - "Dry eye metric history endpoint returns time-series data per metric with OD/OS values and visit dates"
    - "Dry eye metric history supports time range filtering (3mo, 6mo, 1yr, all)"
    - "OSDI answers endpoint returns structured answers with question text per category"
    - "OsdiHub broadcasts OsdiSubmitted event to visit group subscribers"
  artifacts:
    - path: "backend/src/Modules/Clinical/Clinical.Application/Features/DryEye/GetDryEyeMetricHistory.cs"
      provides: "Per-metric time-series data with OD/OS and time range filter"
      contains: "MetricHistory"
    - path: "backend/src/Modules/Clinical/Clinical.Application/Features/Osdi/GetOsdiAnswers.cs"
      provides: "Structured OSDI answers with question text grouped by category"
      contains: "OsdiAnswer"
    - path: "backend/src/Modules/Clinical/Clinical.Infrastructure/Hubs/OsdiHub.cs"
      provides: "SignalR hub for realtime OSDI push"
      contains: "JoinVisit"
    - path: "backend/src/Modules/Clinical/Clinical.Infrastructure/Services/OsdiNotificationService.cs"
      provides: "Service that broadcasts to OsdiHub groups"
  key_links:
    - from: "GetDryEyeMetricHistory.cs"
      to: "IDryEyeAssessmentRepository"
      via: "joined query with visits for dates"
      pattern: "GetMetricHistoryAsync"
    - from: "OsdiHub.cs"
      to: "BillingHub.cs"
      via: "same SignalR hub pattern"
      pattern: "Groups\\.AddToGroupAsync"
    - from: "NotifyOsdiSubmitted.cs"
      to: "OsdiNotificationService"
      via: "event handler triggers SignalR push"
      pattern: "NotifyAsync"
---

<objective>
Backend TDD implementation for clinical module enhancements: dry eye metric trend history endpoint, OSDI answers structured display endpoint, and realtime OSDI push via SignalR hub.

Purpose: Enable the frontend to display per-metric dry eye trend charts with time range filtering, show individual OSDI question answers grouped by category, and receive realtime OSDI score updates when patients submit via the public QR page.
Output: 3 new handlers with unit tests, OsdiHub for SignalR, notification service, updated API endpoints.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-address-all-pending-todos/10-CONTEXT.md
@.planning/phases/10-address-all-pending-todos/10-RESEARCH.md
@backend/src/Modules/Clinical/Clinical.Domain/Entities/DryEyeAssessment.cs
@backend/src/Modules/Clinical/Clinical.Contracts/Dtos/DryEyeAssessmentDto.cs
@backend/src/Modules/Billing/Billing.Infrastructure/Hubs/BillingHub.cs
@frontend/src/features/billing/hooks/use-billing-hub.ts

<interfaces>
From BillingHub.cs (pattern to follow):
```csharp
[Authorize]
public class BillingHub : Hub
{
    public async Task JoinVisit(string visitId) =>
        await Groups.AddToGroupAsync(Context.ConnectionId, $"visit-{visitId}");
    public async Task LeaveVisit(string visitId) =>
        await Groups.RemoveFromGroupAsync(Context.ConnectionId, $"visit-{visitId}");
}
```

From DryEyeAssessment.cs:
- Has Tbut, Schirmer, MeibomianGrading, TearMeniscus, StainingScore per eye (OD/OS)
- Linked to Visit via VisitId

From OsdiSubmission (existing):
- AnswersJson stores JSON array of 12 answers (0-4 or null)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dry eye metric history endpoint with time range filtering</name>
  <files>
    backend/src/Modules/Clinical/Clinical.Application/Features/DryEye/GetDryEyeMetricHistory.cs,
    backend/src/Modules/Clinical/Clinical.Contracts/Dtos/DryEyeMetricHistoryDto.cs,
    backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs,
    backend/tests/Clinical.Unit.Tests
  </files>
  <behavior>
    - GetDryEyeMetricHistory returns time-series data points per metric (TBUT, Schirmer, Meibomian, TearMeniscus, Staining)
    - Each data point has visitDate, odValue, osValue
    - TimeRange "3m" filters to last 3 months, "6m" to 6 months, "1y" to 1 year, "all" returns everything
    - Returns empty arrays when patient has no dry eye assessments
    - Data is ordered by visitDate ascending
  </behavior>
  <action>
    1. Create `DryEyeMetricHistoryDto.cs` in Contracts:
       - `DryEyeMetricHistoryResponse(List<MetricTimeSeries> Metrics)` where MetricTimeSeries has `string MetricName` and `List<MetricDataPoint> DataPoints`
       - `MetricDataPoint(DateTime VisitDate, decimal? OdValue, decimal? OsValue)`
       - Metrics: "TBUT", "Schirmer", "MeibomianGrading", "TearMeniscus", "StainingScore"

    2. Create `GetDryEyeMetricHistory.cs`:
       - Query: `GetDryEyeMetricHistoryQuery(Guid PatientId, string TimeRange)` where TimeRange is "3m"|"6m"|"1y"|"all"
       - Handler: join DryEyeAssessments with Visits in a SINGLE query (avoid N+1) to get visit dates
       - Apply time range filter at SQL level: `Where(v => v.CreatedAt >= cutoffDate)` based on TimeRange
       - Map each assessment to 5 MetricTimeSeries entries with OD/OS values
       - Order by visitDate ASC

    3. Add repository method `GetMetricHistoryAsync(Guid patientId, DateTime? since, CancellationToken ct)` that returns assessments joined with visit dates in a single query

    4. Register endpoint: GET /api/clinical/patients/{patientId}/dry-eye/metric-history?timeRange=all

    5. Write unit tests following TDD.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet test tests/Clinical.Unit.Tests/ --no-build -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Dry eye metric history returns per-metric time-series with OD/OS values and time range filtering. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OSDI answers endpoint + OsdiHub SignalR for realtime push</name>
  <files>
    backend/src/Modules/Clinical/Clinical.Application/Features/Osdi/GetOsdiAnswers.cs,
    backend/src/Modules/Clinical/Clinical.Contracts/Dtos/OsdiAnswersDto.cs,
    backend/src/Modules/Clinical/Clinical.Infrastructure/Hubs/OsdiHub.cs,
    backend/src/Modules/Clinical/Clinical.Infrastructure/Services/OsdiNotificationService.cs,
    backend/src/Modules/Clinical/Clinical.Application/Features/Osdi/NotifyOsdiSubmitted.cs,
    backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs,
    backend/src/Bootstrapper/Program.cs,
    backend/tests/Clinical.Unit.Tests
  </files>
  <behavior>
    - GetOsdiAnswers returns 12 structured answers with question text (En/Vi) and individual scores
    - Answers are grouped by category: Vision (Q1-Q5), Eye Symptoms (Q6-Q9), Environmental Triggers (Q10-Q12)
    - Returns null/empty when no OSDI submission exists for the visit
    - OsdiHub allows clients to join/leave visit groups
    - NotifyOsdiSubmitted handler broadcasts OSDI score to visit group via OsdiHub
  </behavior>
  <action>
    1. Create `OsdiAnswersDto.cs` in Contracts:
       - `OsdiAnswersResponse(List<OsdiAnswerGroup> Groups, decimal TotalScore, string Severity)`
       - `OsdiAnswerGroup(string Category, List<OsdiQuestionAnswer> Questions)`
       - `OsdiQuestionAnswer(int QuestionNumber, string TextEn, string TextVi, int? Score)`

    2. Create `GetOsdiAnswers.cs`:
       - Query: `GetOsdiAnswersQuery(Guid VisitId)`
       - Handler: load OsdiSubmission by VisitId, parse AnswersJson (JSON array of 12 ints/nulls)
       - Map answers to the 12 OSDI questions with text from OsdiQuestionDto definitions
       - Group by category: Vision (1-5), Eye Symptoms (6-9), Environmental Triggers (10-12)
       - Return TotalScore and Severity classification

    3. Create `OsdiHub.cs` following BillingHub pattern exactly:
       - `[Authorize] public class OsdiHub : Hub`
       - `JoinVisit(string visitId)` -> Groups.AddToGroupAsync
       - `LeaveVisit(string visitId)` -> Groups.RemoveFromGroupAsync

    4. Create `OsdiNotificationService.cs`:
       - `IOsdiNotificationService` interface with `NotifyOsdiSubmittedAsync(Guid visitId, decimal score, string severity)`
       - Implementation uses `IHubContext<OsdiHub>` to send to visit group

    5. Create `NotifyOsdiSubmitted.cs` (or hook into existing OSDI submission handler):
       - After OSDI is submitted, call `IOsdiNotificationService.NotifyOsdiSubmittedAsync`
       - Fire-and-forget pattern (log warnings, never throw) matching BillingHub pattern

    6. Register in `Program.cs`: `app.MapHub<OsdiHub>("/hubs/osdi")`

    7. Register endpoint: GET /api/clinical/visits/{visitId}/osdi-answers

    8. Write unit tests following TDD.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet test tests/Clinical.Unit.Tests/ --no-build -v q 2>&1 | tail -5</automated>
  </verify>
  <done>OSDI answers endpoint returns structured answers with categories. OsdiHub registered and broadcasts events. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet test backend/tests/Clinical.Unit.Tests/ -v q` passes with all new tests
- `dotnet build backend/` compiles without errors
- Dry eye metric history returns 5 metric time series with OD/OS values
- OSDI answers returns grouped questions with individual scores
- OsdiHub mapped at /hubs/osdi in Program.cs
</verification>

<success_criteria>
3 new backend features implemented with TDD: dry eye metric history with time range filtering, OSDI answers with categorized display, and OsdiHub for realtime OSDI push. All unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-address-all-pending-todos/10-03-SUMMARY.md`
</output>
