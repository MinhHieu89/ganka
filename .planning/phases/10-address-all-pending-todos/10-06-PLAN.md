---
phase: 10-address-all-pending-todos
plan: 06
type: execute
wave: 2
depends_on: [10-03, 10-04]
files_modified:
  - frontend/src/features/patient/components/DryEyeMetricCharts.tsx
  - frontend/src/features/patient/components/PatientDryEyeTab.tsx
  - frontend/src/features/patient/api/patient-api.ts
  - frontend/src/features/patient/hooks/patient-queries.ts
  - frontend/src/features/clinical/components/OsdiAnswersSection.tsx
  - frontend/src/features/clinical/components/VisitDetailPage.tsx
  - frontend/src/features/clinical/hooks/use-osdi-hub.ts
  - frontend/src/features/clinical/api/clinical-api.ts
  - frontend/src/features/clinical/hooks/clinical-queries.ts
  - frontend/src/features/clinical/components/DrugPrescriptionSection.tsx
  - frontend/src/features/settings/components/ClinicSettingsPage.tsx
autonomous: true
requirements: [TODO-07, TODO-08, TODO-09, TODO-10, TODO-13]

must_haves:
  truths:
    - "Patient dry eye tab shows separate trend charts per metric (TBUT, Schirmer, Meibomian, TearMeniscus, Staining)"
    - "Each dry eye chart shows OD/OS lines with time range selector"
    - "Visit detail page shows expandable OSDI answers section grouped by category"
    - "OSDI score updates in realtime when patient submits via QR page"
    - "Prescription section shows Print All Labels button that downloads batch PDF"
    - "Clinic settings page has logo upload section"
  artifacts:
    - path: "frontend/src/features/patient/components/DryEyeMetricCharts.tsx"
      provides: "5 stacked metric trend charts with recharts"
      min_lines: 80
    - path: "frontend/src/features/clinical/components/OsdiAnswersSection.tsx"
      provides: "Expandable OSDI answers display grouped by category"
      min_lines: 40
    - path: "frontend/src/features/clinical/hooks/use-osdi-hub.ts"
      provides: "SignalR hook for realtime OSDI updates"
      min_lines: 30
  key_links:
    - from: "DryEyeMetricCharts.tsx"
      to: "/api/clinical/patients/{patientId}/dry-eye/metric-history"
      via: "API call with timeRange param"
      pattern: "metric-history"
    - from: "DryEyeMetricCharts.tsx"
      to: "PatientDryEyeTab.tsx"
      via: "rendered inside dry eye tab container"
      pattern: "DryEyeMetricCharts"
    - from: "OsdiAnswersSection.tsx"
      to: "/api/clinical/visits/{visitId}/osdi-answers"
      via: "API call"
      pattern: "osdi-answers"
    - from: "use-osdi-hub.ts"
      to: "/hubs/osdi"
      via: "SignalR connection"
      pattern: "HubConnectionBuilder.*hubs/osdi"
---

<objective>
Frontend implementation for clinical module enhancements: dry eye metric trend charts, OSDI answers expandable display, realtime OSDI via SignalR, batch label print button, and clinic logo upload UI.

Purpose: Display per-metric dry eye trends with OD/OS differentiation, show individual OSDI answers grouped by category, enable realtime OSDI score updates, add batch label printing, and clinic logo upload.
Output: DryEyeMetricCharts component, OsdiAnswersSection component, useOsdiHub hook, batch label button, logo upload UI.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-address-all-pending-todos/10-CONTEXT.md
@.planning/phases/10-address-all-pending-todos/10-RESEARCH.md
@.planning/phases/10-address-all-pending-todos/10-03-SUMMARY.md
@.planning/phases/10-address-all-pending-todos/10-04-SUMMARY.md
@frontend/src/features/patient/components/OsdiTrendChart.tsx
@frontend/src/features/patient/components/PatientDryEyeTab.tsx
@frontend/src/features/billing/hooks/use-billing-hub.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry eye metric trend charts + OSDI answers section</name>
  <files>
    frontend/src/features/patient/components/DryEyeMetricCharts.tsx,
    frontend/src/features/patient/components/PatientDryEyeTab.tsx,
    frontend/src/features/patient/api/patient-api.ts,
    frontend/src/features/patient/hooks/patient-queries.ts,
    frontend/src/features/clinical/components/OsdiAnswersSection.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    frontend/src/features/clinical/hooks/clinical-queries.ts,
    frontend/src/features/clinical/components/VisitDetailPage.tsx
  </files>
  <action>
    1. Add API functions and hooks:
       - `patient-api.ts`: `getDryEyeMetricHistory(patientId, timeRange)` -> GET /api/clinical/patients/{patientId}/dry-eye/metric-history?timeRange={range}
       - `patient-queries.ts`: `useDryEyeMetricHistory(patientId, timeRange)` hook
       - `clinical-api.ts`: `getOsdiAnswers(visitId)` -> GET /api/clinical/visits/{visitId}/osdi-answers
       - `clinical-queries.ts`: `useOsdiAnswers(visitId)` hook

    2. Create `DryEyeMetricCharts.tsx`:
       - Props: `patientId: string`
       - Time range selector (buttons or Select): "3 months", "6 months", "1 year", "All" mapping to "3m"/"6m"/"1y"/"all"
       - Default to "all"
       - For each of the 5 metrics (TBUT, Schirmer, Meibomian Grading, Tear Meniscus, Staining Score):
         - Render a recharts `LineChart` with two `Line` components (OD in `hsl(var(--chart-1))`, OS in `hsl(var(--chart-2))`)
         - XAxis: visitDate formatted with date-fns
         - YAxis: metric value
         - Tooltip showing date, OD value, OS value
         - Chart title: metric name
       - Stack charts vertically, each ~200px height
       - Follow existing OsdiTrendChart.tsx styling patterns
       - Show "No data" message when no assessments exist

    3. Integrate `DryEyeMetricCharts` into `PatientDryEyeTab.tsx` — import and render the component below or alongside the existing OSDI trend chart in the dry eye tab container

    4. Create `OsdiAnswersSection.tsx`:
       - Props: `visitId: string`
       - "View Details" button next to existing OSDI score display
       - On click, expand a Collapsible section showing all 12 questions
       - Group by category: "Vision" (Q1-5), "Eye Symptoms" (Q6-9), "Environmental Triggers" (Q10-12)
       - Each question: question text (Vietnamese primary, English fallback), score (0-4 or "N/A")
       - Category header with average score for that group
       - Use shadcn Card or simple styled div for each group

    5. Integrate `OsdiAnswersSection` into visit detail page near the OSDI score display
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>5 dry eye metric charts render with OD/OS lines and time range selector. DryEyeMetricCharts is rendered inside PatientDryEyeTab.tsx. OSDI answers section expands to show categorized questions with scores. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Realtime OSDI hub + batch label button + logo upload UI</name>
  <files>
    frontend/src/features/clinical/hooks/use-osdi-hub.ts,
    frontend/src/features/clinical/components/VisitDetailPage.tsx,
    frontend/src/features/clinical/components/DrugPrescriptionSection.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    frontend/src/features/settings/components/ClinicSettingsPage.tsx,
    frontend/src/features/settings/api/settings-api.ts
  </files>
  <action>
    1. Create `use-osdi-hub.ts` following `use-billing-hub.ts` pattern:
       - Connect to /hubs/osdi via HubConnectionBuilder
       - On mount with visitId: invoke "JoinVisit" on hub
       - On unmount: invoke "LeaveVisit"
       - On "OsdiSubmitted" event: invalidate visit query cache via queryClient
       - Handle reconnection: re-invoke "JoinVisit" in onreconnected handler
       - Return `{ connectionStatus }` (connecting/connected/disconnected)

    2. Wire `useOsdiHub` into visit detail page:
       - Call `useOsdiHub(visitId)` in VisitDetailPage component
       - When "OsdiSubmitted" fires, the React Query cache invalidation will auto-refresh OSDI score display

    3. Add "Print All Labels" button to DrugPrescriptionSection:
       - Add API function: `printBatchLabels(prescriptionId)` -> GET /api/clinical/prescriptions/{prescriptionId}/labels/batch (returns PDF blob)
       - Button: "Print All Labels" (or Vietnamese "In tat ca nhan") next to existing print button
       - On click: fetch PDF blob, open in new window or trigger download
       - Only show when prescription has items

    4. Add logo upload to ClinicSettingsPage:
       - Add API function: `uploadClinicLogo(file: File)` -> POST /api/settings/clinic/logo (multipart)
       - Add a "Logo" section to the clinic settings page
       - Show current logo if LogoBlobUrl exists (img tag)
       - File input (accept image/jpeg, image/png, image/webp)
       - Upload button that calls the API, shows toast on success
       - Preview the selected image before upload
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>useOsdiHub hook connected and invalidates cache on OSDI submission. Print All Labels button generates batch PDF. Logo upload works on settings page. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with zero errors
- 5 dry eye metric charts render with OD/OS lines
- Time range selector filters chart data
- DryEyeMetricCharts integrated into PatientDryEyeTab.tsx
- OSDI answers section expands with categorized questions
- useOsdiHub connects to /hubs/osdi and invalidates cache
- Print All Labels button downloads batch PDF
- Logo upload section on clinic settings page
</verification>

<success_criteria>
All 5 frontend clinical features implemented: dry eye metric charts with OD/OS and time range, OSDI answers expandable display, realtime OSDI via SignalR, batch label print button, and clinic logo upload UI. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/10-address-all-pending-todos/10-06-SUMMARY.md`
</output>
