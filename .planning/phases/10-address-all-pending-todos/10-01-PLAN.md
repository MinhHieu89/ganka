---
phase: 10-address-all-pending-todos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/shared/components/AutoResizeTextarea.tsx
  - frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx
  - frontend/src/features/clinical/components/DrugCombobox.tsx
  - frontend/src/features/clinical/components/VisitDetailPage.tsx
autonomous: true
requirements: [TODO-01, TODO-02, TODO-03, TODO-06, TODO-09]

must_haves:
  truths:
    - "All textareas across the app auto-resize to fit content"
    - "Optical prescription section is expanded by default on visit detail page"
    - "DrugCombobox auto-focuses the search input when opened"
    - "Patient name on visit detail page is a clickable link opening patient profile in a new tab"
  artifacts:
    - path: "frontend/src/shared/components/AutoResizeTextarea.tsx"
      provides: "Auto-resizing textarea wrapper component"
      min_lines: 20
    - path: "frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx"
      provides: "Optical prescription section with defaultOpen"
    - path: "frontend/src/features/clinical/components/DrugCombobox.tsx"
      provides: "Drug combobox with auto-focus on open"
  key_links:
    - from: "frontend/src/shared/components/AutoResizeTextarea.tsx"
      to: "frontend/src/shared/components/Textarea.tsx"
      via: "extends Textarea component"
      pattern: "import.*Textarea"
---

<objective>
Quick UX fixes and AutoResizeTextarea global replacement across the application.

Purpose: Address 4 quick UX improvements (defaultOpen, auto-focus, patient link, textarea auto-resize) that require only frontend changes and no backend work. These are independent of all other Phase 10 plans.
Output: AutoResizeTextarea wrapper component; updated OpticalPrescriptionSection, DrugCombobox, and VisitDetailPage.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-address-all-pending-todos/10-CONTEXT.md
@.planning/phases/10-address-all-pending-todos/10-RESEARCH.md
@frontend/src/shared/components/Textarea.tsx
@frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx
@frontend/src/features/clinical/components/DrugCombobox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AutoResizeTextarea wrapper and apply quick UX fixes</name>
  <files>
    frontend/src/shared/components/AutoResizeTextarea.tsx,
    frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx,
    frontend/src/features/clinical/components/DrugCombobox.tsx
  </files>
  <action>
    1. Create `frontend/src/shared/components/AutoResizeTextarea.tsx`:
       - Import and wrap the existing `Textarea` component from `@/shared/components/Textarea`
       - Use forwardRef to preserve ref forwarding compatibility
       - On mount and on every value/onChange, set `el.style.height = "auto"` then `el.style.height = el.scrollHeight + "px"` via a useCallback
       - Use a merged ref pattern (innerRef + forwarded ref) as shown in RESEARCH.md code example
       - Keep the same prop interface as Textarea so it's a drop-in replacement
       - Add `overflow: hidden` style to prevent scrollbar flash during resize

    2. Update `OpticalPrescriptionSection.tsx`:
       - Find the Collapsible or accordion wrapper and set `defaultOpen={true}` (or equivalent prop)
       - Use Claude's discretion on the exact prop name based on whichever collapsible pattern is used

    3. Update `DrugCombobox.tsx`:
       - Add auto-focus to the search input when the combobox popover opens
       - Use a ref + useEffect or onOpenChange callback to focus the input element when the popover becomes visible
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AutoResizeTextarea component created, OpticalPrescriptionSection defaults open, DrugCombobox auto-focuses on open. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all Textarea imports with AutoResizeTextarea and add patient name link</name>
  <files>
    frontend/src/features/clinical/components/VisitDetailPage.tsx
  </files>
  <action>
    1. Find all files that import `Textarea` from `@/shared/components/Textarea` using grep:
       `grep -rn "from.*shared/components/Textarea" frontend/src/`
       For each file found, replace the import to use `AutoResizeTextarea` from `@/shared/components/AutoResizeTextarea`.
       Replace `<Textarea` with `<AutoResizeTextarea` in each file's JSX.
       Verify each replacement preserves existing props (value, onChange, className, placeholder, etc.)

    2. In `VisitDetailPage.tsx` (or wherever the patient name is displayed on the visit detail page):
       - Find where patient name is rendered
       - Wrap it in a `<a href="/patients/{patientId}" target="_blank" rel="noopener noreferrer">` or use the router's Link component with target="_blank"
       - Style it as a clickable link (underline on hover, cursor pointer)

    3. Run TypeScript type check to verify no regressions from the global Textarea replacement.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>All Textarea usages replaced with AutoResizeTextarea across the app. Patient name on visit detail page opens patient profile in new tab. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- TypeScript compilation passes with zero errors
- AutoResizeTextarea component exists and extends Textarea
- All former Textarea imports now use AutoResizeTextarea
- OpticalPrescriptionSection renders expanded by default
- DrugCombobox search input receives focus when opened
- Patient name on visit detail page is a clickable link with target="_blank"
</verification>

<success_criteria>
All 4 quick UX improvements are implemented: auto-resize textareas, defaultOpen optical prescription, DrugCombobox auto-focus, patient name link. TypeScript compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/10-address-all-pending-todos/10-01-SUMMARY.md`
</output>
