---
phase: 09-treatment-protocols
plan: 30
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Clinical/Clinical.Domain/Entities/OsdiSubmission.cs
  - backend/src/Modules/Clinical/Clinical.Infrastructure/Configurations/OsdiSubmissionConfiguration.cs
  - backend/src/Modules/Clinical/Clinical.Contracts/IntegrationEvents/CreateOsdiTokenForTreatmentCommand.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/CreateOsdiTokenForTreatmentHandler.cs
  - backend/src/Modules/Treatment/Treatment.Application/Treatment.Application.csproj
  - backend/src/Modules/Treatment/Treatment.Application/Features/RegisterOsdiToken.cs
  - backend/src/Modules/Treatment/Treatment.Application/Interfaces/IOsdiTokenStore.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Services/InMemoryOsdiTokenStore.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/IoC.cs
  - frontend/src/features/treatment/components/SessionOsdiCapture.tsx
  - frontend/src/features/treatment/api/treatment-api.ts
autonomous: true
gap_closure: true
requirements:
  - TRT-06
must_haves:
  truths:
    - "OSDI patient self-fill QR link opens working questionnaire page"
    - "OSDI direct input shows full 12-question questionnaire with auto-calculated score"
    - "SessionOsdiCapture displays Vietnamese labels when VN locale is active"
  artifacts:
    - path: "backend/src/Modules/Clinical/Clinical.Contracts/IntegrationEvents/CreateOsdiTokenForTreatmentCommand.cs"
      provides: "Cross-module command for Treatment to create DB-backed OSDI token"
    - path: "frontend/src/features/treatment/components/SessionOsdiCapture.tsx"
      provides: "OSDI capture with OsdiQuestionnaire component and i18n"
  key_links:
    - from: "SessionOsdiCapture.tsx"
      to: "OsdiQuestionnaire.tsx"
      via: "import from clinical module"
      pattern: "import.*OsdiQuestionnaire.*from.*clinical"
    - from: "RegisterOsdiTokenHandler"
      to: "CreateOsdiTokenForTreatmentCommand"
      via: "IMessageBus.InvokeAsync"
      pattern: "bus\\.InvokeAsync.*CreateOsdiTokenForTreatment"
---

<objective>
Fix two OSDI issues in treatment session recording: (1) patient self-fill QR link shows error because token is stored in-memory instead of Clinical module's DB, and (2) direct input shows a single number field instead of the full 12-question OsdiQuestionnaire component. Also add i18n to SessionOsdiCapture.

Purpose: UAT Test 7 reported both issues as major severity. The self-fill link is completely broken and the direct input defeats the purpose of structured OSDI assessment.
Output: Working OSDI self-fill flow with DB-backed tokens and full questionnaire for inline scoring.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/osdi-treatment-session-issues.md

@backend/src/Modules/Clinical/Clinical.Domain/Entities/OsdiSubmission.cs
@backend/src/Modules/Clinical/Clinical.Infrastructure/Configurations/OsdiSubmissionConfiguration.cs
@backend/src/Modules/Clinical/Clinical.Application/Features/GenerateOsdiLink.cs
@backend/src/Modules/Clinical/Clinical.Application/Interfaces/IOsdiSubmissionRepository.cs
@backend/src/Modules/Treatment/Treatment.Application/Features/RegisterOsdiToken.cs
@backend/src/Modules/Treatment/Treatment.Application/Interfaces/IOsdiTokenStore.cs
@backend/src/Modules/Treatment/Treatment.Infrastructure/Services/InMemoryOsdiTokenStore.cs
@frontend/src/features/treatment/components/SessionOsdiCapture.tsx
@frontend/src/features/clinical/components/OsdiQuestionnaire.tsx
@frontend/src/features/clinical/api/clinical-api.ts

<interfaces>
From frontend/src/features/clinical/components/OsdiQuestionnaire.tsx:
```typescript
// 12-question OSDI questionnaire with auto-score calculation
// Props: onSubmit callback receives { answers, score, severity }
// Already has bilingual support via useTranslation
export function OsdiQuestionnaire({ onSubmit, onCancel, initialAnswers })
```

From backend/src/Modules/Clinical/Clinical.Domain/Entities/OsdiSubmission.cs:
```csharp
public class OsdiSubmission : Entity {
    public Guid VisitId { get; private set; }  // Currently non-nullable - needs to be nullable
    public string? PublicToken { get; private set; }
    public DateTime? TokenExpiresAt { get; private set; }
    public static OsdiSubmission CreateWithToken(Guid visitId, string token)  // Needs visitId-free variant
}
```

From backend/src/Modules/Clinical/Clinical.Application/Interfaces/IOsdiSubmissionRepository.cs:
```csharp
public interface IOsdiSubmissionRepository {
    Task AddAsync(OsdiSubmission submission, CancellationToken ct = default);
    Task<OsdiSubmission?> GetByTokenAsync(string token, CancellationToken ct = default);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend - DB-backed OSDI token for treatment sessions</name>
  <files>
    backend/src/Modules/Clinical/Clinical.Domain/Entities/OsdiSubmission.cs,
    backend/src/Modules/Clinical/Clinical.Infrastructure/Configurations/OsdiSubmissionConfiguration.cs,
    backend/src/Modules/Clinical/Clinical.Contracts/IntegrationEvents/CreateOsdiTokenForTreatmentCommand.cs,
    backend/src/Modules/Clinical/Clinical.Application/Features/CreateOsdiTokenForTreatmentHandler.cs,
    backend/src/Modules/Treatment/Treatment.Application/Treatment.Application.csproj,
    backend/src/Modules/Treatment/Treatment.Application/Features/RegisterOsdiToken.cs,
    backend/src/Modules/Treatment/Treatment.Application/Interfaces/IOsdiTokenStore.cs,
    backend/src/Modules/Treatment/Treatment.Infrastructure/Services/InMemoryOsdiTokenStore.cs,
    backend/src/Modules/Treatment/Treatment.Infrastructure/IoC.cs
  </files>
  <behavior>
    - Test 1: CreateOsdiTokenForTreatmentHandler creates OsdiSubmission with null VisitId and valid PublicToken
    - Test 2: RegisterOsdiTokenHandler invokes CreateOsdiTokenForTreatmentCommand via message bus and returns token+URL
    - Test 3: GetOsdiByToken (existing Clinical handler) finds token created by treatment flow
  </behavior>
  <action>
    1. In OsdiSubmission.cs: Make VisitId nullable (`public Guid? VisitId`). Add factory method `CreateWithTokenForTreatment(string token)` that creates an OsdiSubmission with VisitId=null, SubmittedBy="patient", PublicToken=token, TokenExpiresAt=24h.

    2. In OsdiSubmissionConfiguration.cs: Change `builder.Property(o => o.VisitId).IsRequired()` to `builder.Property(o => o.VisitId).IsRequired(false)`.

    3. Create `Clinical.Contracts/IntegrationEvents/CreateOsdiTokenForTreatmentCommand.cs`: A record `CreateOsdiTokenForTreatmentCommand(string Token)` and response `CreateOsdiTokenForTreatmentResponse(string Token, string Url, DateTime ExpiresAt)`.

    4. Create `Clinical.Application/Features/CreateOsdiTokenForTreatmentHandler.cs`: A Wolverine handler that receives CreateOsdiTokenForTreatmentCommand, calls `OsdiSubmission.CreateWithTokenForTreatment(command.Token)`, saves via IOsdiSubmissionRepository, returns response with url `/osdi/{token}`.

    5. In Treatment.Application.csproj: Add ProjectReference to `Clinical.Contracts.csproj`.

    6. Modify RegisterOsdiTokenHandler: Replace in-memory IOsdiTokenStore usage with `IMessageBus.InvokeAsync<CreateOsdiTokenForTreatmentResponse>(new CreateOsdiTokenForTreatmentCommand(command.Token))`. Return the response token+URL. Remove IOsdiTokenStore dependency.

    7. Delete IOsdiTokenStore.cs interface and InMemoryOsdiTokenStore.cs implementation. Remove the IOsdiTokenStore registration from Treatment.Infrastructure IoC.cs.

    8. Create and run EF Core migration for nullable VisitId: `dotnet ef migrations add MakeOsdiVisitIdNullable --project backend/src/Modules/Clinical/Clinical.Infrastructure --startup-project backend/src/Bootstrapper --output-dir Migrations`. Then `dotnet ef database update --project backend/src/Modules/Clinical/Clinical.Infrastructure --startup-project backend/src/Bootstrapper`.

    Note: The existing `GenerateOsdiLink.cs` and `CreateWithToken(Guid visitId, string token)` remain unchanged - they work correctly for the Clinical module's visit-based flow.
  </action>
  <verify>
    <automated>cd backend && dotnet build --no-restore 2>&1 | tail -5 && dotnet test src/Modules/Treatment/Treatment.Unit.Tests --no-build --filter "Osdi" -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>RegisterOsdiToken creates a DB-backed OsdiSubmission record. InMemoryOsdiTokenStore is deleted. Public OSDI page at /osdi/{token} can find tokens created by treatment flow.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend - Replace OSDI number input with OsdiQuestionnaire + add i18n</name>
  <files>
    frontend/src/features/treatment/components/SessionOsdiCapture.tsx,
    frontend/src/features/treatment/api/treatment-api.ts,
    frontend/public/locales/en/treatment.json,
    frontend/public/locales/vi/treatment.json
  </files>
  <action>
    1. In SessionOsdiCapture.tsx:
       - Add `import { useTranslation } from "react-i18next"` and `const { t } = useTranslation("treatment")`
       - Replace all hardcoded strings with t() calls using existing keys under `osdi.*` namespace. Add missing keys for tab labels, QR instructions, severity guide text.

    2. Replace the inline tab content: Remove the single `<Input type="number">` for OSDI Score. Instead, import `OsdiQuestionnaire` from `@/features/clinical/components/OsdiQuestionnaire`. Render it with `onSubmit` callback that:
       - Receives `{ answers, score, severity }` from OsdiQuestionnaire
       - Calls `onOsdiScoreChange(score)` to set the score on the parent form
       - Also store severity via a new `onOsdiSeverityChange` prop (or use the existing `osdiSeverity` prop pattern)

    3. Update the SessionOsdiCapture props interface: The component currently takes `osdiScore` and `onOsdiScoreChange`. Add `onOsdiAnswersChange?: (answers: (number | null)[]) => void` so the parent can capture the full answers array if needed. The OsdiQuestionnaire component handles all 12 questions and auto-calculates the score.

    4. For the self-fill tab: Update `useRegisterOsdiToken` to expect the backend to return `{ token, url, expiresAt }` (the response now comes from the DB-backed flow). Use the returned URL directly instead of constructing it client-side. The `handleGenerateQr` should use the URL from the response: `setQrUrl(baseUrl + response.url)`.

    5. Add missing translation keys to both en/treatment.json and vi/treatment.json:
       - `osdi.inlineTab`: "Enter Directly" / "Nhap truc tiep"
       - `osdi.selfFillTab`: "Patient Self-Fill" / "Benh nhan tu dien"
       - `osdi.generateQrDesc`: "Generate QR code for patient to self-fill OSDI questionnaire" / Vietnamese equivalent
       - `osdi.scanQrInstructions`: "Patient scans QR code or opens link to fill OSDI form" / Vietnamese equivalent
       - `osdi.generatingQr`: "Generating QR code..." / "Dang tao ma QR..."
       - `osdi.linkCopied`: "Link copied" / "Link da duoc sao chep"
       - `osdi.qrError`: "Could not generate QR. Please try again." / Vietnamese equivalent
       Note: Use proper Vietnamese diacritics in vi/treatment.json.

    6. Remove the duplicated SEVERITY_CONFIG from SessionOsdiCapture.tsx since OsdiQuestionnaire handles severity display internally.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Inline OSDI tab shows full 12-question OsdiQuestionnaire with auto-score. Self-fill tab uses DB-backed URL from backend response. All text uses t() with translation keys. No hardcoded strings remain.</done>
</task>

</tasks>

<verification>
1. Backend builds without errors: `cd backend && dotnet build`
2. Frontend compiles without errors: `cd frontend && npx tsc --noEmit`
3. Backend starts successfully: Treatment module registers, OSDI endpoint works
4. Self-fill flow: RegisterOsdiToken creates DB record -> public page finds token
5. Direct input: OsdiQuestionnaire renders 12 questions in SessionOsdiCapture
</verification>

<success_criteria>
- OSDI self-fill QR link opens working questionnaire (token stored in DB, not in-memory)
- OSDI direct input shows 12 OSDI questions with auto-calculated score (not single number input)
- InMemoryOsdiTokenStore is deleted
- SessionOsdiCapture uses useTranslation with t() calls for all strings
- All translation keys exist in both en/treatment.json and vi/treatment.json
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-30-SUMMARY.md`
</output>
