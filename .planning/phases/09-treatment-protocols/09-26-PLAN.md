---
phase: 09-treatment-protocols
plan: 26
type: execute
wave: 10
depends_on: [09-21, 09-22]
files_modified:
  - frontend/src/features/treatment/components/PatientTreatmentsTab.tsx
  - frontend/src/app/routes/_authenticated/patients/$patientId.tsx
autonomous: true
requirements: [TRT-02, TRT-06]

must_haves:
  truths:
    - "Patient profile has a Treatments tab showing all packages"
    - "Tab displays active, completed, cancelled packages with progress"
    - "User can navigate to package detail from patient profile"
  artifacts:
    - path: "frontend/src/features/treatment/components/PatientTreatmentsTab.tsx"
      provides: "Treatments tab for patient profile"
      contains: "PatientTreatmentsTab"
    - path: "frontend/src/app/routes/_authenticated/patients/$patientId.tsx"
      provides: "Updated patient profile with Treatments tab"
      contains: "Treatments"
  key_links:
    - from: "PatientTreatmentsTab"
      to: "usePatientTreatments"
      via: "Query by patientId"
      pattern: "usePatientTreatments"
    - from: "Patient profile"
      to: "PatientTreatmentsTab"
      via: "Tab panel in patient detail"
      pattern: "PatientTreatmentsTab"
---

<objective>
Create patient profile Treatments tab and integrate with patient detail page.

Purpose: Clinicians need to see all treatment history for a patient from their profile. This tab shows active, completed, and cancelled packages with progress, enabling quick navigation to package details and new package creation from the patient context.
Output: PatientTreatmentsTab component + patient profile integration
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference: Patient profile page with existing tabs (Overview, Visits, Allergies, etc.)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PatientTreatmentsTab</name>
  <files>frontend/src/features/treatment/components/PatientTreatmentsTab.tsx</files>
  <action>
    PatientTreatmentsTab.tsx:
    - Props: patientId (string)
    - Uses usePatientTreatments(patientId) hook
    - "Create Treatment Package" button at top (opens TreatmentPackageForm with patientId pre-set)
    - Groups packages by status:
      - Active section (top, most prominent)
      - Completed section
      - Cancelled/Switched section (collapsed by default)
    - Each package displayed as a card:
      - Treatment type badge
      - Template name
      - Progress bar: sessions completed / total
      - Status badge
      - Last session date
      - Next due date (if active)
      - "View Details" link to /treatments/{packageId}
    - Empty state: "No treatment packages for this patient" with create button
    - Loading state: Skeleton cards
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>PatientTreatmentsTab renders patient packages grouped by status with progress</done>
</task>

<task type="auto">
  <name>Task 2: Add Treatments tab to patient profile page</name>
  <files>frontend/src/app/routes/_authenticated/patients/$patientId.tsx</files>
  <action>
    Update the patient detail page to add a "Treatments" tab:

    1. Import PatientTreatmentsTab from treatment feature
    2. Add a new tab to the existing Tabs component:
       - Tab trigger: "Treatments" with i18n key treatment.tab (or "Treatments" in both languages)
       - Tab content: <PatientTreatmentsTab patientId={patientId} />
    3. Place the tab after existing clinical tabs (e.g., after Visits, Dry Eye, Prescriptions)
    4. The tab should only be visible to users with Treatment.View permission (if permission guard pattern is used on tabs)

    IMPORTANT: Only modify the relevant section of the file. Do not refactor existing tab code.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Patient profile shows Treatments tab with patient's treatment packages</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- Patient profile has a Treatments tab visible after existing tabs
- Tab shows all packages with progress and status grouping
- Create button enables package creation from patient context
- Navigation to package detail works from cards
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-26-SUMMARY.md`
</output>
