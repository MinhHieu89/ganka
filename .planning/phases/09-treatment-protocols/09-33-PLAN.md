---
phase: 09-treatment-protocols
plan: 33
type: execute
wave: 2
depends_on: [30, 31]
files_modified:
  - frontend/src/features/treatment/components/OsdiTrendChart.tsx
  - frontend/src/features/treatment/components/VersionHistoryDialog.tsx
  - frontend/src/features/treatment/components/ConsumableSelector.tsx
  - frontend/src/features/treatment/components/DueSoonSection.tsx
  - frontend/src/features/treatment/components/ModifyPackageDialog.tsx
  - frontend/src/features/treatment/components/SwitchTreatmentDialog.tsx
  - frontend/src/features/treatment/components/PatientTreatmentsTab.tsx
  - frontend/src/features/treatment/components/TreatmentsPage.tsx
  - frontend/public/locales/en/treatment.json
  - frontend/public/locales/vi/treatment.json
autonomous: true
gap_closure: true
requirements:
  - TRT-11
must_haves:
  truths:
    - "All remaining treatment components show Vietnamese labels when VN locale is active"
    - "VersionHistoryDialog uses proper Vietnamese diacritics instead of unaccented Vietnamese"
  artifacts:
    - path: "frontend/src/features/treatment/components/OsdiTrendChart.tsx"
      provides: "i18n-wired OSDI chart"
      contains: "useTranslation"
    - path: "frontend/src/features/treatment/components/VersionHistoryDialog.tsx"
      provides: "i18n-wired version history with proper diacritics"
      contains: "useTranslation"
  key_links:
    - from: "all 8 components"
      to: "treatment.json"
      via: "useTranslation('treatment')"
      pattern: "useTranslation.*treatment"
---

<objective>
Add i18n wiring to the remaining 8 treatment components that are missing it: OsdiTrendChart, VersionHistoryDialog, ConsumableSelector, DueSoonSection, ModifyPackageDialog, SwitchTreatmentDialog, PatientTreatmentsTab, and TreatmentsPage. Fix VersionHistoryDialog's hardcoded Vietnamese without diacritics.

Purpose: Complete i18n coverage for the entire treatment module. These components have fewer hardcoded strings than the batch 1 components but still need wiring.
Output: All treatment module components use t() calls. Zero hardcoded strings remain.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/debug/treatment-vn-localization-missing.md

@frontend/public/locales/en/treatment.json
@frontend/public/locales/vi/treatment.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n for OsdiTrendChart, VersionHistoryDialog, ConsumableSelector, DueSoonSection</name>
  <files>
    frontend/src/features/treatment/components/OsdiTrendChart.tsx,
    frontend/src/features/treatment/components/VersionHistoryDialog.tsx,
    frontend/src/features/treatment/components/ConsumableSelector.tsx,
    frontend/src/features/treatment/components/DueSoonSection.tsx,
    frontend/public/locales/en/treatment.json,
    frontend/public/locales/vi/treatment.json
  </files>
  <action>
    1. OsdiTrendChart.tsx (~163 lines):
       - Add useTranslation("treatment")
       - "OSDI Score Trend" -> add key `osdiChart.title`
       - "No OSDI scores recorded yet" -> add key `osdiChart.noData`
       - "More sessions needed for trend chart" -> add key `osdiChart.needMore`
       - "Session" (axis label) -> add key `osdiChart.sessionAxis`
       - Severity labels "Normal", "Mild", "Moderate", "Severe" -> add keys `osdiChart.normal`, `osdiChart.mild`, `osdiChart.moderate`, `osdiChart.severe`

    2. VersionHistoryDialog.tsx (~135 lines):
       - Add useTranslation("treatment")
       - CRITICAL: This component has hardcoded Vietnamese WITHOUT diacritics. Replace ALL with t() calls:
         - "Lich su thay doi" -> t("versionHistory") (key already exists, EN: "Version History")
         - "Dang tai..." -> add key `history.loading`
         - "Chua co thay doi nao duoc ghi nhan" -> add key `history.noChanges`
         - "Phien ban" -> add key `history.version`
         - "Ly do:" -> add key `history.reason`
         - "Noi dung thay doi:" -> add key `history.changes`
         - "Chi tiet ky thuat" -> add key `history.technicalDetails`
         - "Truoc" -> add key `history.before`
         - "Sau" -> add key `history.after`

    3. ConsumableSelector.tsx (~169 lines):
       - Add useTranslation("treatment")
       - "Stock:" -> add key `consumable.stock`
       - Any other hardcoded labels (search placeholder, etc.)

    4. DueSoonSection.tsx (~179 lines):
       - Add useTranslation("treatment")
       - Use existing keys: t("dueSoon"), t("noDueSoon")
       - Any additional hardcoded strings like "Show more", "Show less" -> add keys `dueSoon.showMore`, `dueSoon.showLess`
       - Patient name, treatment type badges: t("treatmentType.IPL") etc.

    5. Add all missing keys to BOTH en/treatment.json and vi/treatment.json with proper Vietnamese diacritics.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>OsdiTrendChart, VersionHistoryDialog, ConsumableSelector, DueSoonSection all use useTranslation with t() calls. VersionHistoryDialog no longer has unaccented Vietnamese. All translation keys in both locale files.</done>
</task>

<task type="auto">
  <name>Task 2: i18n for ModifyPackageDialog, SwitchTreatmentDialog, PatientTreatmentsTab, TreatmentsPage</name>
  <files>
    frontend/src/features/treatment/components/ModifyPackageDialog.tsx,
    frontend/src/features/treatment/components/SwitchTreatmentDialog.tsx,
    frontend/src/features/treatment/components/PatientTreatmentsTab.tsx,
    frontend/src/features/treatment/components/TreatmentsPage.tsx,
    frontend/public/locales/en/treatment.json,
    frontend/public/locales/vi/treatment.json
  </files>
  <action>
    1. ModifyPackageDialog.tsx (~265 lines):
       - Add useTranslation("treatment")
       - Dialog title: t("modifyPackage")
       - Use existing keys: t("modify.changeReason"), t("modify.currentValues")
       - Field labels: t("fields.sessionCount"), t("fields.minInterval"), t("fields.parameters")
       - Button labels: add keys `modify.submit`, `modify.cancel`
       - Toast messages: add keys `modify.success`, `modify.error`

    2. SwitchTreatmentDialog.tsx (~266 lines):
       - Add useTranslation("treatment")
       - Dialog title: t("switchTreatment")
       - Use existing keys: t("switch.currentWillBeSwitched"), t("switch.newWillBeCreated", { remaining }), t("switch.selectNewTemplate")
       - Button labels: add keys `switch.confirm`, `switch.cancel`
       - Toast messages: add keys `switch.success`, `switch.error`

    3. PatientTreatmentsTab.tsx (~313 lines):
       - Add useTranslation("treatment")
       - Tab label: t("tab")
       - Use existing keys: t("noPackages"), t("createFirst"), t("createPackage")
       - Status grouping headers: add keys `patientTab.active`, `patientTab.completed`, `patientTab.other`
       - Treatment type and status badges: t("treatmentType.*"), t("status.*")

    4. TreatmentsPage.tsx (~375 lines):
       - Add useTranslation("treatment")
       - Page title: t("title")
       - Use existing keys: t("createPackage")
       - DataTable column headers: add keys `list.patient`, `list.type`, `list.status`, `list.progress`, `list.pricing`, `list.lastSession`, `list.nextDue`
       - Filter labels, search placeholder, pagination text
       - Any remaining hardcoded strings

    5. Add all missing keys to BOTH en/treatment.json and vi/treatment.json. Verify complete coverage by doing a final search for any remaining hardcoded strings across ALL treatment components.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>ModifyPackageDialog, SwitchTreatmentDialog, PatientTreatmentsTab, TreatmentsPage all use useTranslation with t() calls. All treatment module components now have i18n wiring. Zero hardcoded user-visible strings across entire treatment module.</done>
</task>

</tasks>

<verification>
1. Frontend compiles: `cd frontend && npx tsc --noEmit`
2. All 8 components import useTranslation and use t() calls
3. Grep confirms no hardcoded English patterns remain in treatment components: `grep -rn '"[A-Z][a-z]' frontend/src/features/treatment/components/ --include="*.tsx" | grep -v "import\|//\|console\|className\|variant\|type=\|key=\|id=\|name=\|icon"` should return minimal/zero results
4. VersionHistoryDialog has proper Vietnamese diacritics (no "Lich su" or "Truoc" patterns)
</verification>

<success_criteria>
- All 8 remaining components use useTranslation("treatment")
- VersionHistoryDialog uses t() calls instead of hardcoded Vietnamese without diacritics
- Combined with plan 32, all 14 treatment components (minus the 3 already done) now have full i18n
- Translation keys in both en and vi locale files are complete and consistent
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-33-SUMMARY.md`
</output>
