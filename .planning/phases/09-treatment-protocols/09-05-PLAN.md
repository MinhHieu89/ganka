---
phase: 09-treatment-protocols
plan: 05
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - backend/src/Modules/Treatment/Treatment.Domain/Entities/ProtocolVersion.cs
  - backend/src/Modules/Treatment/Treatment.Domain/Entities/CancellationRequest.cs
autonomous: true
requirements: [TRT-07, TRT-09]

must_haves:
  truths:
    - "Protocol modifications create version snapshots with before/after state"
    - "Cancellation requests capture requester, reason, deduction percentage, and approval status"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Domain/Entities/ProtocolVersion.cs"
      provides: "Version history entity for mid-course modifications"
      contains: "class ProtocolVersion"
    - path: "backend/src/Modules/Treatment/Treatment.Domain/Entities/CancellationRequest.cs"
      provides: "Cancellation request entity with approval workflow"
      contains: "class CancellationRequest"
  key_links:
    - from: "TreatmentPackage.Modify()"
      to: "ProtocolVersion"
      via: "Snapshot creation before applying changes"
      pattern: "_versions\\.Add"
    - from: "TreatmentPackage.RequestCancellation()"
      to: "CancellationRequest"
      via: "Creates cancellation request"
      pattern: "_cancellationRequest"
---

<objective>
Create ProtocolVersion and CancellationRequest child entities for TreatmentPackage.

Purpose: ProtocolVersion captures snapshots of package state before each modification (TRT-07), enabling a full audit trail of mid-course changes. CancellationRequest models the manager-approved cancellation workflow (TRT-09) with configurable refund deduction.
Output: Two entity files in Treatment.Domain/Entities/
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference pattern: Billing.Domain/Entities/Refund.cs (approval workflow with Requested/Approved/Rejected)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProtocolVersion and CancellationRequest entities</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Domain/Entities/ProtocolVersion.cs
    backend/src/Modules/Treatment/Treatment.Domain/Entities/CancellationRequest.cs
  </files>
  <action>
    1. ProtocolVersion.cs:
       - namespace Treatment.Domain.Entities
       - Child entity of TreatmentPackage
       - Properties (all private set):
         - Id (Guid)
         - TreatmentPackageId (Guid) -- FK
         - VersionNumber (int) -- sequential within package (1, 2, 3...)
         - PreviousJson (string) -- JSON snapshot of previous state
         - CurrentJson (string) -- JSON snapshot of new state
         - ChangeDescription (string) -- human-readable description, e.g., "Session count changed from 4 to 6"
         - ChangedById (Guid) -- doctor who made the change
         - Reason (string) -- reason for modification
         - CreatedAt (DateTime)
       - Private parameterless constructor for EF Core
       - Static Create factory method

    2. CancellationRequest.cs:
       - namespace Treatment.Domain.Entities
       - Child entity of TreatmentPackage (one-to-one optional)
       - Properties (all private set):
         - Id (Guid)
         - TreatmentPackageId (Guid) -- FK
         - RequestedById (Guid)
         - RequestedAt (DateTime)
         - Reason (string)
         - DeductionPercent (decimal) -- default from protocol template, overridable by manager (10-20%)
         - RefundAmount (decimal) -- calculated at approval time
         - Status (CancellationStatus enum -- add inline or separate: Requested = 0, Approved = 1, Rejected = 2)
         - ApprovedById (Guid?) -- manager who approved/rejected
         - ApprovedAt (DateTime?)
         - RejectionReason (string?)
       - Private parameterless constructor for EF Core
       - Static Create factory method: sets Status to Requested, RequestedAt = DateTime.UtcNow
       - Approve(managerId, deductionPercent, refundAmount) method: sets Status to Approved
       - Reject(managerId, reason) method: sets Status to Rejected

    Add CancellationStatus as a nested enum in CancellationRequest or as a separate file in Enums/. Choose what fits -- if separate, add it to Treatment.Domain/Enums/CancellationStatus.cs (5th file is fine within 6 limit).
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Domain/Treatment.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>ProtocolVersion and CancellationRequest entities compile with factory methods and approval workflow</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Treatment/Treatment.Domain/Treatment.Domain.csproj --no-restore -v q
</verification>

<success_criteria>
- ProtocolVersion stores both previous and current state snapshots plus change description
- CancellationRequest has Approve/Reject methods with status transitions
- CancellationRequest captures deduction percentage and calculated refund amount
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-05-SUMMARY.md`
</output>
