---
phase: 09-treatment-protocols
plan: 23
type: execute
wave: 9
depends_on: [09-22]
files_modified:
  - frontend/src/features/treatment/components/TreatmentSessionForm.tsx
  - frontend/src/features/treatment/components/ConsumableSelector.tsx
  - frontend/src/features/treatment/components/SessionOsdiCapture.tsx
autonomous: true
requirements: [TRT-03, TRT-05, TRT-11]

must_haves:
  truths:
    - "Doctor/technician can record session with device parameters, OSDI, notes, consumables"
    - "Session form shows type-specific parameter fields"
    - "Consumable selector allows searching and adding consumables with quantities"
    - "OSDI can be captured inline or via QR link for patient self-fill"
  artifacts:
    - path: "frontend/src/features/treatment/components/TreatmentSessionForm.tsx"
      provides: "Session recording dialog form"
      contains: "TreatmentSessionForm"
    - path: "frontend/src/features/treatment/components/ConsumableSelector.tsx"
      provides: "Searchable consumable selection with quantities"
      contains: "ConsumableSelector"
    - path: "frontend/src/features/treatment/components/SessionOsdiCapture.tsx"
      provides: "Inline OSDI recording and QR link"
      contains: "SessionOsdiCapture"
  key_links:
    - from: "TreatmentSessionForm"
      to: "useRecordSession"
      via: "Mutation on form submit"
      pattern: "useRecordSession"
    - from: "ConsumableSelector"
      to: "Consumables API"
      via: "Search consumable items"
      pattern: "consumable"
---

<objective>
Create session recording form with consumable selector and OSDI capture.

Purpose: Session recording is the core clinical action in this module. The form captures device parameters (type-specific), OSDI score, clinical observations, and consumables used. The OSDI capture supports both inline entry and patient self-fill via QR code.
Output: Session form + consumable selector + OSDI capture components
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference:
- frontend/src/features/clinical/ (OsdiSection for QR pattern)
- frontend/src/features/consumables/ (consumable list/API pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TreatmentSessionForm dialog</name>
  <files>frontend/src/features/treatment/components/TreatmentSessionForm.tsx</files>
  <action>
    TreatmentSessionForm.tsx:
    - shadcn Dialog with multi-section form
    - Props: open, onOpenChange, packageId, treatmentType, defaultParametersJson
    - React Hook Form + Zod schema

    Sections:
    1. Device Parameters (rendered based on treatmentType prop):
       - IPL: energy (number, J/cm2), pulseCount (number), spotSize (text), treatmentZones (multi-select or checkboxes)
       - LLLT: wavelength (number, nm), power (number, mW), duration (number, min), treatmentArea (text)
       - Lid Care: procedureSteps (checklist with checkboxes), productsUsed (multi-input), duration (number, min)
       - Default values from defaultParametersJson (parsed from template)
       - Parameters serialized to JSON on submit

    2. OSDI Section:
       - SessionOsdiCapture component (inline score entry + QR option)

    3. Clinical Notes:
       - Optional textarea for freeform observations

    4. Consumables:
       - ConsumableSelector component for adding items with quantities

    5. Optional VisitId link:
       - If session is linked to a clinical visit, select from patient's recent visits

    6. Interval warning display:
       - If the response includes an IntervalWarning, show a yellow alert with days since last session vs minimum
       - Option to provide IntervalOverrideReason if booking too early

    Submit: uses useRecordSession(packageId) mutation
    On success: close dialog, show toast, if warning present show it
    onError: handleServerValidationError
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Session form renders type-specific parameters with OSDI, consumables, and interval warning</done>
</task>

<task type="auto">
  <name>Task 2: Create ConsumableSelector and SessionOsdiCapture</name>
  <files>
    frontend/src/features/treatment/components/ConsumableSelector.tsx
    frontend/src/features/treatment/components/SessionOsdiCapture.tsx
  </files>
  <action>
    1. ConsumableSelector.tsx:
       - Searchable combobox for consumable items (fetch from /api/pharmacy/consumables via existing API)
       - Selected items displayed as a list below the selector
       - Each selected item has: name (read-only), quantity input (number, min 1)
       - Add button to add selected consumable to list
       - Remove button per item
       - Props: value (array of {consumableItemId, consumableName, quantity}), onChange
       - Controlled component pattern (parent manages state)
       - Use shadcn Command/Combobox pattern (same as ICD-10 selector)

    2. SessionOsdiCapture.tsx:
       - Two modes: "Inline" and "Patient Self-Fill"
       - Inline mode:
         - Single number input for OSDI score (0-100)
         - Auto-calculates severity on input (Normal/Mild/Moderate/Severe)
         - Displays severity badge with color
       - Patient Self-Fill mode:
         - "Generate QR Link" button
         - Shows QR code (using qrcode.react, already installed) pointing to public OSDI page
         - The QR link includes a treatment session token for OSDI to be linked back
         - Note: this reuses the public OSDI page pattern from Phase 4
       - Toggle between modes via shadcn Tabs
       - Props: osdiScore, onOsdiScoreChange, osdiSeverity
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>ConsumableSelector enables searchable multi-select, SessionOsdiCapture supports inline and QR modes</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- Session form captures all required data per the research spec
- Type-specific parameters rendered correctly for IPL/LLLT/LidCare
- Consumable selector supports search, add, quantity, remove
- OSDI capture supports both inline entry and QR self-fill
- Interval warning displayed when applicable
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-23-SUMMARY.md`
</output>
