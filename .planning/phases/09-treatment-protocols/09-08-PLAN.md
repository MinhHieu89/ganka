---
phase: 09-treatment-protocols
plan: 08
type: execute
wave: 3
depends_on: [09-03, 09-04, 09-05]
files_modified:
  - backend/src/Modules/Treatment/Treatment.Infrastructure/TreatmentDbContext.cs
  - backend/src/Modules/Treatment/Treatment.Application/Interfaces/ITreatmentProtocolRepository.cs
  - backend/src/Modules/Treatment/Treatment.Application/Interfaces/ITreatmentPackageRepository.cs
  - backend/src/Modules/Treatment/Treatment.Application/Interfaces/IUnitOfWork.cs
autonomous: true
requirements: [TRT-01, TRT-02]

must_haves:
  truths:
    - "TreatmentDbContext has DbSets for all entities and applies configurations"
    - "Repository interfaces define data access contracts for aggregate roots"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Infrastructure/TreatmentDbContext.cs"
      provides: "Updated DbContext with all DbSets and configuration application"
      contains: "DbSet<TreatmentProtocol>"
    - path: "backend/src/Modules/Treatment/Treatment.Application/Interfaces/ITreatmentProtocolRepository.cs"
      provides: "Repository interface for protocol templates"
      contains: "interface ITreatmentProtocolRepository"
    - path: "backend/src/Modules/Treatment/Treatment.Application/Interfaces/ITreatmentPackageRepository.cs"
      provides: "Repository interface for treatment packages"
      contains: "interface ITreatmentPackageRepository"
    - path: "backend/src/Modules/Treatment/Treatment.Application/Interfaces/IUnitOfWork.cs"
      provides: "Unit of work interface"
      contains: "interface IUnitOfWork"
  key_links:
    - from: "TreatmentDbContext"
      to: "Configurations/"
      via: "ApplyConfigurationsFromAssembly"
      pattern: "ApplyConfigurationsFromAssembly"
---

<objective>
Update TreatmentDbContext with DbSets and create repository interfaces.

Purpose: The DbContext needs DbSets for all entities and must apply the EF Core configurations from plan 07. Repository interfaces define the data access contracts that handlers depend on (Application layer, no infrastructure dependency).
Output: Updated DbContext + 3 repository interfaces
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference patterns:
- Optical.Infrastructure/OpticalDbContext.cs (DbSets + ApplyConfigurationsFromAssembly)
- Optical.Application/Interfaces/IFrameRepository.cs (repository interface pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TreatmentDbContext with DbSets</name>
  <files>backend/src/Modules/Treatment/Treatment.Infrastructure/TreatmentDbContext.cs</files>
  <action>
    Update the existing TreatmentDbContext.cs:

    - Add using statements for Treatment.Domain.Entities
    - Add DbSet properties:
      - DbSet of TreatmentProtocol (TreatmentProtocols)
      - DbSet of TreatmentPackage (TreatmentPackages)
      - DbSet of TreatmentSession (TreatmentSessions)
      - DbSet of SessionConsumable (SessionConsumables)
      - DbSet of ProtocolVersion (ProtocolVersions)
      - DbSet of CancellationRequest (CancellationRequests)
    - In OnModelCreating:
      - Keep existing schema and Guid.NewGuid() override
      - Add: modelBuilder.ApplyConfigurationsFromAssembly(typeof(TreatmentDbContext).Assembly);
      - This auto-discovers all IEntityTypeConfiguration implementations

    Follow OpticalDbContext pattern exactly.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>TreatmentDbContext compiles with all 6 DbSets and applies configurations from assembly</done>
</task>

<task type="auto">
  <name>Task 2: Create repository interfaces</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Application/Interfaces/ITreatmentProtocolRepository.cs
    backend/src/Modules/Treatment/Treatment.Application/Interfaces/ITreatmentPackageRepository.cs
    backend/src/Modules/Treatment/Treatment.Application/Interfaces/IUnitOfWork.cs
  </files>
  <action>
    Create Treatment.Application/Interfaces/ directory and:

    1. ITreatmentProtocolRepository.cs:
       - namespace Treatment.Application.Interfaces
       - Methods:
         - GetByIdAsync(Guid id, CancellationToken ct)
         - GetAllAsync(bool includeInactive, CancellationToken ct)
         - GetByTypeAsync(TreatmentType type, CancellationToken ct)
         - AddAsync(TreatmentProtocol protocol, CancellationToken ct)

    2. ITreatmentPackageRepository.cs:
       - namespace Treatment.Application.Interfaces
       - Methods:
         - GetByIdAsync(Guid id, CancellationToken ct) -- includes Sessions, Versions, CancellationRequest
         - GetByPatientIdAsync(Guid patientId, CancellationToken ct)
         - GetActivePackagesAsync(CancellationToken ct) -- all active across patients
         - GetDueSoonAsync(CancellationToken ct) -- packages where last session + minInterval <= now
         - GetPendingCancellationsAsync(CancellationToken ct) -- status = PendingCancellation
         - AddAsync(TreatmentPackage package, CancellationToken ct)

    3. IUnitOfWork.cs:
       - namespace Treatment.Application.Interfaces
       - Method: SaveChangesAsync(CancellationToken ct)

    Follow the established IFrameRepository / IUnitOfWork pattern from Optical module.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Application/Treatment.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>All 3 repository interfaces compile with correct method signatures</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q && dotnet build backend/src/Modules/Treatment/Treatment.Application/Treatment.Application.csproj --no-restore -v q
</verification>

<success_criteria>
- TreatmentDbContext has 6 DbSets and applies configurations from assembly
- Repository interfaces are in Application layer (no Infrastructure dependency)
- ITreatmentPackageRepository.GetByIdAsync includes child entities (Sessions, Versions)
- ITreatmentPackageRepository.GetDueSoonAsync enables "Due Soon" feature
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-08-SUMMARY.md`
</output>
