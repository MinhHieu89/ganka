---
phase: 09-treatment-protocols
plan: 31
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/treatment/components/TreatmentParameterFields.tsx
  - frontend/src/features/treatment/components/TreatmentPackageForm.tsx
  - frontend/src/features/treatment/components/ProtocolTemplateForm.tsx
  - frontend/public/locales/en/treatment.json
  - frontend/public/locales/vi/treatment.json
autonomous: true
gap_closure: true
requirements:
  - TRT-02
must_haves:
  truths:
    - "Treatment package creation form shows structured input fields for device parameters instead of raw JSON textarea"
    - "Structured fields match treatment type: IPL shows energy/pulse/spot/zones, LLLT shows wavelength/power/duration/area, LidCare shows steps/products/duration"
    - "TreatmentPackageForm displays Vietnamese labels when VN locale is active"
  artifacts:
    - path: "frontend/src/features/treatment/components/TreatmentParameterFields.tsx"
      provides: "Shared structured parameter input component for IPL/LLLT/LidCare"
      exports: ["TreatmentParameterFields", "buildParametersJson", "parseParametersJson"]
  key_links:
    - from: "TreatmentPackageForm.tsx"
      to: "TreatmentParameterFields.tsx"
      via: "import and render"
      pattern: "import.*TreatmentParameterFields"
    - from: "ProtocolTemplateForm.tsx"
      to: "TreatmentParameterFields.tsx"
      via: "import and render"
      pattern: "import.*TreatmentParameterFields"
---

<objective>
Extract shared TreatmentParameterFields component from ProtocolTemplateForm and replace the raw JSON textarea in TreatmentPackageForm with structured input fields. Also add i18n to TreatmentPackageForm.

Purpose: UAT Test 4 reported raw JSON textarea as major severity. Treatment parameters should be user-friendly structured fields matching each treatment type, not raw JSON editing.
Output: Shared component reused by both ProtocolTemplateForm and TreatmentPackageForm with proper i18n.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/debug/package-form-raw-json-params.md

@frontend/src/features/treatment/components/ProtocolTemplateForm.tsx
@frontend/src/features/treatment/components/TreatmentPackageForm.tsx

<interfaces>
From ProtocolTemplateForm.tsx (lines 99-197) - helpers to extract:
```typescript
interface IplParams { energy?: number; pulseCount?: number; spotSize?: string; treatmentZones?: string[] }
interface LlltParams { wavelength?: number; power?: number; duration?: number; treatmentArea?: string }
interface LidCareParams { procedureSteps?: string[]; products?: string[]; duration?: number }

function buildParametersJson(treatmentType: string, values: FormValues): string | null
function parseParametersJson(treatmentType: string, json: string | null): Partial<FormValues>
```

From ProtocolTemplateForm.tsx (lines 581-861) - UI to extract:
```typescript
// Conditional rendering based on treatmentType:
// IPL: Energy (J/cm2), Pulse Count, Spot Size, Treatment Zones (dynamic list)
// LLLT: Wavelength (nm), Power (mW), Duration (min), Treatment Area
// LidCare: Duration (min), Procedure Steps (dynamic list), Products (dynamic list)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared TreatmentParameterFields component</name>
  <files>
    frontend/src/features/treatment/components/TreatmentParameterFields.tsx,
    frontend/src/features/treatment/components/ProtocolTemplateForm.tsx
  </files>
  <action>
    1. Create TreatmentParameterFields.tsx with:
       - Export the IplParams, LlltParams, LidCareParams interfaces
       - Export buildParametersJson(treatmentType, fieldValues) and parseParametersJson(treatmentType, json) helper functions extracted from ProtocolTemplateForm.tsx lines 121-197
       - Export a TreatmentParameterFields component that takes props: `{ treatmentType: string, values: Record<string, unknown>, onChange: (field: string, value: unknown) => void, disabled?: boolean }`
       - The component renders treatment-type-specific fields conditionally (same UI as ProtocolTemplateForm lines 581-861):
         - IPL: Energy (number), Pulse Count (number), Spot Size (text), Treatment Zones (dynamic string list with Add/Remove)
         - LLLT: Wavelength (number), Power (number), Duration (number), Treatment Area (text)
         - LidCare: Duration (number), Procedure Steps (dynamic string list), Products (dynamic string list)
       - Use useTranslation("treatment") with keys under ipl.*, lllt.*, lidCare.* namespaces (already exist in translation files)
       - Use shadcn/ui Input, Label, Button components

    2. Refactor ProtocolTemplateForm.tsx:
       - Remove the inline buildParametersJson, parseParametersJson helpers and param interfaces (lines ~99-197)
       - Remove the inline treatment-type-specific field rendering (lines ~581-861)
       - Import { TreatmentParameterFields, buildParametersJson, parseParametersJson } from ./TreatmentParameterFields
       - Wire the shared component into the form with the same field state management
       - Verify all existing functionality is preserved (create/edit template, validation)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>TreatmentParameterFields.tsx exists with exported component and helpers. ProtocolTemplateForm uses the shared component. No duplicate parameter field code.</done>
</task>

<task type="auto">
  <name>Task 2: Replace JSON textarea in TreatmentPackageForm + add i18n</name>
  <files>
    frontend/src/features/treatment/components/TreatmentPackageForm.tsx,
    frontend/public/locales/en/treatment.json,
    frontend/public/locales/vi/treatment.json
  </files>
  <action>
    1. In TreatmentPackageForm.tsx:
       - Add `import { useTranslation } from "react-i18next"` and `const { t } = useTranslation("treatment")`
       - Import { TreatmentParameterFields, buildParametersJson, parseParametersJson } from ./TreatmentParameterFields
       - Replace all hardcoded strings with t() calls. Add missing keys as needed.

    2. Replace the raw JSON textarea (lines ~501-522):
       - Remove the AutoResizeTextarea for parametersJson
       - Add local state for structured parameter fields (e.g., `const [paramFields, setParamFields] = useState({})`)
       - When a template is selected (handleTemplateSelect), parse the template's defaultParametersJson using parseParametersJson(selectedTemplate.treatmentType, json) into structured field values
       - Render `<TreatmentParameterFields treatmentType={selectedTemplate?.treatmentType} values={paramFields} onChange={handleParamFieldChange} />`
       - On form submit, call buildParametersJson(treatmentType, paramFields) to convert back to JSON string for the API payload

    3. Update the form schema: The parametersJson field in the zod schema can stay as z.string().nullable().optional() since the API still expects a JSON string. The conversion happens in the submit handler.

    4. Add missing translation keys to both en/treatment.json and vi/treatment.json for TreatmentPackageForm labels:
       - `packageForm.title`: "Create Treatment Package" / "Tao goi dieu tri"
       - `packageForm.editTitle`: "Edit Treatment Package" / "Sua goi dieu tri"
       - `packageForm.selectTemplate`: "Select Protocol Template" / "Chon phac do mau"
       - `packageForm.selectPatient`: "Select Patient" / "Chon benh nhan"
       - `packageForm.searchPatient`: "Search patient..." / "Tim benh nhan..."
       - `packageForm.overrideSessions`: "Override Session Count" / "Ghi de so buoi"
       - `packageForm.overridePricing`: "Override Pricing Mode" / "Ghi de che do gia"
       - `packageForm.overrideParams`: "Override Parameters" / "Ghi de tham so"
       - `packageForm.templateDefaults`: "Template Defaults" / "Mac dinh tu phac do"
       - `packageForm.submit`: "Create Package" / "Tao goi"
       - `packageForm.cancel`: "Cancel" / "Huy"
       Note: Use proper Vietnamese diacritics in vi file.

    5. Remove the AutoResizeTextarea import if no longer used in this file.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>TreatmentPackageForm shows structured input fields for treatment parameters. No raw JSON textarea. All labels use t() with translation keys. Vietnamese locale shows correct text.</done>
</task>

</tasks>

<verification>
1. Frontend compiles: `cd frontend && npx tsc --noEmit`
2. ProtocolTemplateForm still renders correctly (uses shared component)
3. TreatmentPackageForm shows structured fields when template is selected
4. Both forms handle all 3 treatment types (IPL, LLLT, LidCare)
5. No hardcoded English strings in TreatmentPackageForm
</verification>

<success_criteria>
- TreatmentParameterFields.tsx exports shared component + helpers
- ProtocolTemplateForm uses shared component (no duplicate field code)
- TreatmentPackageForm shows structured fields instead of JSON textarea
- TreatmentPackageForm uses useTranslation for all labels
- Translation keys exist in both en and vi files
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-31-SUMMARY.md`
</output>
