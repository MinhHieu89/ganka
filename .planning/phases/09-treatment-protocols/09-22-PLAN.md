---
phase: 09-treatment-protocols
plan: 22
type: execute
wave: 8
depends_on: [09-19]
files_modified:
  - frontend/src/features/treatment/components/TreatmentPackageDetail.tsx
  - frontend/src/features/treatment/components/TreatmentSessionCard.tsx
  - frontend/src/features/treatment/components/OsdiTrendChart.tsx
  - frontend/src/app/routes/_authenticated/treatments/$packageId.tsx
autonomous: true
requirements: [TRT-02, TRT-03, TRT-04]

must_haves:
  truths:
    - "Package detail page shows progress, all sessions, and OSDI trend"
    - "Each session displayed as a card with parameters, OSDI, consumables"
    - "OSDI trend chart visualizes score progression across sessions"
  artifacts:
    - path: "frontend/src/features/treatment/components/TreatmentPackageDetail.tsx"
      provides: "Package detail page with session grid and actions"
      contains: "TreatmentPackageDetail"
    - path: "frontend/src/features/treatment/components/TreatmentSessionCard.tsx"
      provides: "Individual session card component"
      contains: "TreatmentSessionCard"
    - path: "frontend/src/features/treatment/components/OsdiTrendChart.tsx"
      provides: "OSDI score trend chart using Recharts"
      contains: "OsdiTrendChart"
    - path: "frontend/src/app/routes/_authenticated/treatments/$packageId.tsx"
      provides: "Route file for /treatments/:packageId"
      contains: "createFileRoute"
  key_links:
    - from: "TreatmentPackageDetail"
      to: "useTreatmentPackage"
      via: "Data fetching by packageId"
      pattern: "useTreatmentPackage"
---

<objective>
Create the treatment package detail page with session cards and OSDI trend chart.

Purpose: This is the primary clinical view for a treatment course -- showing progress, all recorded sessions with their parameters and OSDI scores, and a trend chart visualizing treatment effectiveness over time. This enables the doctor to assess patient response and adjust treatment.
Output: Package detail component + session card + OSDI chart + route
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference:
- frontend/src/features/clinical/ (visit detail page pattern)
- Recharts usage for OSDI trend (from Phase 4 dry eye)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TreatmentPackageDetail and TreatmentSessionCard</name>
  <files>
    frontend/src/features/treatment/components/TreatmentPackageDetail.tsx
    frontend/src/features/treatment/components/TreatmentSessionCard.tsx
  </files>
  <action>
    1. TreatmentPackageDetail.tsx:
       - Uses useTreatmentPackage(packageId) hook
       - Header section:
         - Patient name (clickable link to patient profile)
         - Treatment type badge
         - Status badge
         - Progress: "3/5 sessions completed" with visual progress bar
         - Pricing info: package price or session price
         - Created date, last session date
       - Action buttons (conditional on status):
         - "Record Session" (Active only) -> opens TreatmentSessionForm dialog
         - "Modify" (Active/Paused) -> opens ModifyPackageDialog
         - "Pause/Resume" toggle (Active/Paused)
         - "Switch Type" (Active/Paused) -> opens SwitchTreatmentDialog
         - "Request Cancellation" (Active/Paused) -> opens cancel dialog
         - "View History" -> opens VersionHistoryDialog
       - OSDI trend chart (OsdiTrendChart component)
       - Sessions grid: responsive grid (2 cols lg, 1 col mobile) of TreatmentSessionCard
       - Cancellation info (if status PendingCancellation/Cancelled): shows request details

    2. TreatmentSessionCard.tsx:
       - shadcn Card component
       - Header: "Session #N" + status badge + date
       - Body:
         - Device parameters: parsed from ParametersJson and displayed based on treatment type
           (IPL: Energy, Pulses, Spot Size, Zones | LLLT: Wavelength, Power, Duration, Area | Lid Care: Steps, Products, Duration)
         - OSDI score with severity badge (color-coded: Normal=green, Mild=yellow, Moderate=orange, Severe=red)
         - Clinical notes (if any)
         - Performed by name
         - Consumables used (list with quantities)
         - Interval override reason (if any, shown as warning)
       - Card border color based on OSDI severity for quick visual scanning
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Package detail renders with progress, action buttons, and session cards</done>
</task>

<task type="auto">
  <name>Task 2: Create OsdiTrendChart and route</name>
  <files>
    frontend/src/features/treatment/components/OsdiTrendChart.tsx
    frontend/src/app/routes/_authenticated/treatments/$packageId.tsx
  </files>
  <action>
    1. OsdiTrendChart.tsx:
       - Uses Recharts LineChart (already available from Phase 4)
       - Props: sessions (TreatmentSessionDto[])
       - X-axis: Session number (1, 2, 3...) or date
       - Y-axis: OSDI score (0-100)
       - Data points: each session's OSDI score
       - Color-coded zones: Normal (0-12 green), Mild (13-22 yellow), Moderate (23-32 orange), Severe (33-100 red)
       - Reference lines or areas showing severity thresholds
       - Tooltip shows session date, score, severity
       - Handle case where only 1 or 0 data points (show message instead of empty chart)
       - Responsive width

    2. treatments/$packageId.tsx:
       - createFileRoute for /treatments/$packageId
       - Reads packageId from route params
       - Renders page title "Treatment Package" + TreatmentPackageDetail component
       - Breadcrumb: Treatments > Package Detail
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>OSDI chart renders with severity zones, route navigable at /treatments/:id</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- Package detail shows all treatment info with progress visualization
- Session cards display type-specific parameters parsed from JSON
- OSDI trend chart shows score progression with severity color zones
- Route navigable at /treatments/:packageId
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-22-SUMMARY.md`
</output>
