---
phase: 09-treatment-protocols
plan: 20
type: execute
wave: 8
depends_on: [09-19]
files_modified:
  - frontend/src/features/treatment/components/ProtocolTemplateList.tsx
  - frontend/src/features/treatment/components/ProtocolTemplateForm.tsx
  - frontend/src/app/routes/_authenticated/treatments.templates.tsx
autonomous: true
requirements: [TRT-01, TRT-10]

must_haves:
  truths:
    - "Admin/doctor can view list of protocol templates in a DataTable"
    - "Admin/doctor can create and edit protocol templates via dialog form"
    - "Form captures treatment type, session count, pricing, intervals, default parameters, and deduction percentage"
  artifacts:
    - path: "frontend/src/features/treatment/components/ProtocolTemplateList.tsx"
      provides: "DataTable listing protocol templates"
      contains: "ProtocolTemplateList"
    - path: "frontend/src/features/treatment/components/ProtocolTemplateForm.tsx"
      provides: "Create/edit dialog for protocol templates"
      contains: "ProtocolTemplateForm"
    - path: "frontend/src/app/routes/_authenticated/treatments.templates.tsx"
      provides: "Route file for /treatments/templates"
      contains: "createFileRoute"
  key_links:
    - from: "ProtocolTemplateForm"
      to: "useCreateProtocolTemplate"
      via: "Mutation hook on form submit"
      pattern: "useCreateProtocolTemplate"
---

<objective>
Create protocol template management page with DataTable and form dialog.

Purpose: Doctors need to manage global protocol templates (e.g., "Standard IPL 4-session") that serve as starting points for patient treatment packages. This page provides CRUD for templates with treatment-type-specific default parameters.
Output: Template list component + form dialog + route file
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference patterns:
- frontend/src/features/pharmacy/ (drug catalog page pattern)
- frontend/src/features/optical/ (frame catalog page with DataTable + form dialog)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProtocolTemplateList DataTable</name>
  <files>frontend/src/features/treatment/components/ProtocolTemplateList.tsx</files>
  <action>
    Create frontend/src/features/treatment/components/ directory.

    ProtocolTemplateList.tsx:
    - Import DataTable from shared/components, useProtocolTemplates hook
    - Column definitions:
      - Name (text, sortable)
      - Treatment Type (badge with color: IPL=blue, LLLT=purple, LidCare=green)
      - Sessions (default count)
      - Pricing (PerSession/PerPackage with price)
      - Interval (min-max days)
      - Deduction % (cancellation deduction)
      - Active (boolean badge)
      - Actions (edit button)
    - "Create Template" button opens ProtocolTemplateForm dialog
    - Row click opens edit dialog with pre-populated form
    - Loading/error states using shadcn Skeleton/Alert

    Use shadcn/ui Badge for treatment type and status. Use DataTable for the table.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>ProtocolTemplateList renders DataTable with columns and create button</done>
</task>

<task type="auto">
  <name>Task 2: Create ProtocolTemplateForm dialog and route</name>
  <files>
    frontend/src/features/treatment/components/ProtocolTemplateForm.tsx
    frontend/src/app/routes/_authenticated/treatments.templates.tsx
  </files>
  <action>
    1. ProtocolTemplateForm.tsx:
       - shadcn Dialog with form inside
       - React Hook Form + Zod validation:
         - name: string, required, max 200
         - treatmentType: select (IPL, LLLT, LidCare)
         - defaultSessionCount: number, 1-6
         - pricingMode: select (PerSession, PerPackage)
         - defaultPackagePrice: number, >= 0
         - defaultSessionPrice: number, >= 0
         - minIntervalDays: number, >= 1
         - maxIntervalDays: number, >= minIntervalDays
         - cancellationDeductionPercent: number, 10-20
         - description: optional textarea
       - Conditional parameters section based on treatmentType:
         - IPL: energy (number), pulse count (number), spot size (text), treatment zones (multi-input)
         - LLLT: wavelength (number), power (number), duration (number), treatment area (text)
         - Lid Care: procedure steps (multi-input checklist), products (multi-input), duration (number)
       - Parameters serialized to JSON string for defaultParametersJson
       - Uses useCreateProtocolTemplate or useUpdateProtocolTemplate based on mode (create/edit)
       - Props: open, onOpenChange, initialData? (for edit mode)
       - onError: handleServerValidationError + toast

    2. treatments.templates.tsx:
       - createFileRoute for /treatments/templates
       - Renders page header "Protocol Templates" + ProtocolTemplateList component
       - i18n key: treatment.protocolTemplates
       - Breadcrumb: Treatments > Protocol Templates

    Use shadcn Select, Input, Textarea, Dialog, Button components.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>ProtocolTemplateForm handles create/edit with type-specific parameters, route navigable</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- DataTable shows all protocol templates with proper columns
- Form validates session count 1-6, deduction 10-20%
- Treatment-type-specific parameters rendered conditionally
- Route exists at /treatments/templates
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-20-SUMMARY.md`
</output>
