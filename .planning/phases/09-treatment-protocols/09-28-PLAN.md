---
phase: 09-treatment-protocols
plan: 28
type: execute
wave: 11
depends_on: [09-23, 09-24, 09-25, 09-26, 09-27]
files_modified: []
autonomous: false
requirements: [TRT-01, TRT-02, TRT-03, TRT-04, TRT-05, TRT-06, TRT-07, TRT-08, TRT-09, TRT-10, TRT-11]

must_haves:
  truths:
    - "All 11 TRT requirements verified working end-to-end"
    - "Backend tests pass with 80%+ coverage"
    - "Frontend compiles without errors"
    - "Full workflow is functional via browser"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for the Treatment Protocols phase.

Purpose: Verify all 11 TRT requirements are working by running the full test suite, checking API responses, and having the user perform browser-based functional verification of the complete treatment workflow.
Output: Verification pass/fail for each requirement
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite and verify</name>
  <files></files>
  <action>
    Run all backend tests and verify:
    1. dotnet test backend/Ganka28.slnx -v q -- all tests pass
    2. dotnet test backend/tests/Treatment.Unit.Tests -v q --collect:"XPlat Code Coverage" -- verify 80%+ coverage
    3. Start backend: cd backend/src/Bootstrapper && dotnet run --urls "http://localhost:5255"
    4. Test key API endpoints:
       - GET /api/treatments/protocols -> 200 with template list
       - POST /api/treatments/packages -> 201 with new package
       - POST /api/treatments/packages/{id}/sessions -> 201 with session
       - GET /api/treatments/packages/due-soon -> 200
       - GET /api/treatments/cancellations/pending -> 200
    5. Verify frontend builds: cd frontend && npm run build
  </action>
  <verify>
    <automated>dotnet test backend/Ganka28.slnx -v q && cd frontend && npx tsc --noEmit --pretty</automated>
  </verify>
  <done>All backend tests pass, API endpoints respond, frontend compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Treatment Protocols module with:
    - Protocol template management (CRUD)
    - Treatment package creation from templates
    - Session recording with OSDI, device parameters, consumables
    - Mid-course modification with version history
    - Treatment type switching (close + create)
    - Pause/resume
    - Cancellation with manager approval
    - Due Soon alerts
    - Patient profile Treatments tab
    - OSDI trend chart
    - Consumable auto-deduction
    - Sidebar navigation with 3 subitems
    - Bilingual UI (EN/VI)
  </what-built>
  <how-to-verify>
    Backend running at http://localhost:5255, Frontend at http://localhost:3000
    Login: Admin@ganka28.com / Admin@123456

    TRT-01: Create protocol template flow:
    1. Navigate to /treatments/templates
    2. Click "Create Template", fill in IPL template with 4 sessions, per-package pricing
    3. Verify template appears in list

    TRT-01 + TRT-02: Create package flow:
    4. Navigate to /treatments, click "Create Package"
    5. Select template, select a patient, create
    6. Verify package appears with 0/4 sessions

    TRT-03 + TRT-04 + TRT-05: Session recording:
    7. Open package detail, click "Record Session"
    8. Fill in device parameters, enter OSDI score
    9. Record 4 sessions total -- verify auto-completion after 4th session
    10. On one session, try recording before min interval -- verify warning shows

    TRT-06: Concurrent packages:
    11. Create another package (different type) for same patient
    12. Verify both appear on patient profile Treatments tab

    TRT-07: Mid-course modification:
    13. Create a new package, modify session count, verify version history shows change

    TRT-08: Treatment type switch:
    14. Click "Switch Type" on an active package, select new template
    15. Verify old package shows "Switched", new package created with remaining sessions

    TRT-09: Cancellation:
    16. Request cancellation on a package (provide reason)
    17. Navigate to /treatments/approvals, approve with manager PIN
    18. Verify package status changes to "Cancelled"

    TRT-10: Permission:
    19. Verify only Doctor role users can create/modify templates and packages

    TRT-11: Consumables:
    20. When recording a session, add consumables
    21. Verify consumable stock deducted (check /consumables page)

    Additional checks:
    22. Verify sidebar shows Treatments with 3 subitems
    23. Verify OSDI trend chart shows on package detail (after recording sessions with OSDI)
    24. Switch language to Vietnamese -- verify all labels translated
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
dotnet test backend/Ganka28.slnx -v q
cd frontend && npm run build
</verification>

<success_criteria>
- All 11 TRT requirements verified working end-to-end
- Backend test suite passes with 80%+ coverage on Treatment handlers
- Frontend builds and renders without errors
- User approves the full workflow in browser
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-28-SUMMARY.md`
</output>
