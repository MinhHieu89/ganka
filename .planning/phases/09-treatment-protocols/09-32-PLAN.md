---
phase: 09-treatment-protocols
plan: 32
type: execute
wave: 2
depends_on: [31]
files_modified:
  - frontend/src/features/treatment/components/ProtocolTemplateForm.tsx
  - frontend/src/features/treatment/components/TreatmentPackageDetail.tsx
  - frontend/src/features/treatment/components/TreatmentSessionForm.tsx
  - frontend/src/features/treatment/components/TreatmentSessionCard.tsx
  - frontend/public/locales/en/treatment.json
  - frontend/public/locales/vi/treatment.json
autonomous: true
gap_closure: true
requirements:
  - TRT-11
must_haves:
  truths:
    - "ProtocolTemplateForm shows Vietnamese labels when VN locale is active"
    - "TreatmentPackageDetail shows Vietnamese labels when VN locale is active"
    - "TreatmentSessionForm shows Vietnamese device parameter labels when VN locale is active"
    - "TreatmentSessionCard shows Vietnamese labels when VN locale is active"
  artifacts:
    - path: "frontend/src/features/treatment/components/ProtocolTemplateForm.tsx"
      provides: "i18n-wired protocol template form"
      contains: "useTranslation"
    - path: "frontend/src/features/treatment/components/TreatmentPackageDetail.tsx"
      provides: "i18n-wired package detail page"
      contains: "useTranslation"
    - path: "frontend/src/features/treatment/components/TreatmentSessionForm.tsx"
      provides: "i18n-wired session recording form"
      contains: "useTranslation"
    - path: "frontend/src/features/treatment/components/TreatmentSessionCard.tsx"
      provides: "i18n-wired session card"
      contains: "useTranslation"
  key_links:
    - from: "ProtocolTemplateForm.tsx"
      to: "treatment.json"
      via: "useTranslation('treatment')"
      pattern: "useTranslation.*treatment"
---

<objective>
Add i18n wiring to the 4 largest treatment components that are missing it: ProtocolTemplateForm, TreatmentPackageDetail, TreatmentSessionForm, and TreatmentSessionCard. These are the components flagged in UAT Tests 3, 6, and 7.

Purpose: UAT found all labels hardcoded in English. Translation keys already exist in vi/treatment.json but components don't use useTranslation.
Output: All 4 components use t() calls for every user-visible string.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/debug/treatment-vn-localization-missing.md

@frontend/public/locales/en/treatment.json
@frontend/public/locales/vi/treatment.json

<interfaces>
Existing translation key structure (from en/treatment.json):
```json
{
  "fields": { "name", "treatmentType", "sessionCount", "pricingMode", ... },
  "ipl": { "energy", "pulseCount", "spotSize", "treatmentZones" },
  "lllt": { "wavelength", "power", "duration", "treatmentArea" },
  "lidCare": { "procedureSteps", "productsUsed", "duration" },
  "status": { "Active", "Paused", "PendingCancellation", "Cancelled", "Switched", "Completed" },
  "treatmentType": { "IPL", "LLLT", "LidCare" },
  "pricingMode": { "PerSession", "PerPackage" }
}
```

Pattern from already-i18n'd component (CancellationApprovalQueue.tsx):
```typescript
import { useTranslation } from "react-i18next"
const { t } = useTranslation("treatment")
// Usage: t("approvalQueue.pageTitle"), t("status.Active"), etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n for ProtocolTemplateForm and TreatmentPackageDetail</name>
  <files>
    frontend/src/features/treatment/components/ProtocolTemplateForm.tsx,
    frontend/src/features/treatment/components/TreatmentPackageDetail.tsx,
    frontend/public/locales/en/treatment.json,
    frontend/public/locales/vi/treatment.json
  </files>
  <action>
    **IMPORTANT:** Plan 09-31 may also modify ProtocolTemplateForm.tsx (extracting parameter fields). If 09-31 runs first, this task operates on the refactored version. If this task runs first, it operates on the original. Either way, the i18n changes are the same: add useTranslation and replace hardcoded strings with t() calls. The parameter field labels will be handled by TreatmentParameterFields once extracted, so focus on the non-parameter labels in this component.

    1. ProtocolTemplateForm.tsx (~882 lines):
       - Add `import { useTranslation } from "react-i18next"` and `const { t } = useTranslation("treatment")`
       - Replace all hardcoded strings with t() calls. Map to existing keys where possible:
         - "Name" -> t("fields.name")
         - "Treatment Type" -> t("fields.treatmentType")
         - "Default Session Count" -> t("fields.sessionCount")
         - "Pricing Mode" -> t("fields.pricingMode")
         - "Package Price (VND)" -> t("fields.packagePrice")
         - "Session Price (VND)" -> t("fields.sessionPrice")
         - "Min Interval (days)" -> t("fields.minInterval")
         - "Max Interval (days)" -> t("fields.maxInterval")
         - "Cancellation Deduction %" -> t("fields.deductionPercent")
         - "Description" -> t("fields.description")
         - Dialog titles: t("createTemplate"), t("editTemplate")
         - Select options: t("pricingMode.PerSession"), t("pricingMode.PerPackage"), t("treatmentType.IPL"), etc.
         - Button text: add keys `templateForm.cancel`, `templateForm.create`, `templateForm.update`
         - Toast messages: add keys `templateForm.createSuccess`, `templateForm.updateSuccess`, `templateForm.createError`, `templateForm.updateError`
         - Validation messages: add keys or use existing patterns

    2. TreatmentPackageDetail.tsx (~423 lines):
       - Add `import { useTranslation } from "react-i18next"` and `const { t } = useTranslation("treatment")`
       - Replace hardcoded strings:
         - "Sessions Progress" -> t("fields.progress")
         - "Package price:" -> t("fields.packagePrice")
         - "Per session:" -> t("fields.sessionPrice")
         - "Created:" -> add key `detail.created`
         - "Last session:" -> t("fields.lastSession")
         - "Next due:" -> t("fields.nextDue")
         - "Patient:" -> add key `detail.patient`
         - Button labels: t("recordSession"), t("modifyPackage"), t("switchTreatment"), t("requestCancellation"), t("versionHistory")
         - "Pause"/"Resume" -> add keys `detail.pause`, `detail.resume`
         - "Sessions" section header -> t("sessionHistory")
         - "No sessions recorded yet" -> add key `detail.noSessions`
         - "Treatment package not found" -> add key `detail.notFound`
         - "Back" -> add key `detail.back`
         - Status badges: t("status.Active"), t("status.Paused"), etc.
         - Treatment type badges: t("treatmentType.IPL"), etc.

    3. Add all missing translation keys to BOTH en/treatment.json and vi/treatment.json. Use proper Vietnamese diacritics in vi file. Group new keys under `templateForm.*` and `detail.*` namespaces.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>ProtocolTemplateForm and TreatmentPackageDetail use useTranslation("treatment") with t() calls for all user-visible strings. No hardcoded English remains. Translation keys exist in both locale files.</done>
</task>

<task type="auto">
  <name>Task 2: i18n for TreatmentSessionForm and TreatmentSessionCard</name>
  <files>
    frontend/src/features/treatment/components/TreatmentSessionForm.tsx,
    frontend/src/features/treatment/components/TreatmentSessionCard.tsx,
    frontend/public/locales/en/treatment.json,
    frontend/public/locales/vi/treatment.json
  </files>
  <action>
    1. TreatmentSessionForm.tsx (~819 lines):
       - Add `import { useTranslation } from "react-i18next"` and `const { t } = useTranslation("treatment")`
       - The main form and its inline sub-components (IplParameterFields, LlltParameterFields, LidCareParameterFields) all need i18n
       - Device parameter labels: use existing keys t("ipl.energy"), t("ipl.pulseCount"), t("ipl.spotSize"), t("ipl.treatmentZones"), t("lllt.wavelength"), t("lllt.power"), t("lllt.duration"), t("lllt.treatmentArea"), t("lidCare.procedureSteps"), t("lidCare.productsUsed"), t("lidCare.duration")
       - Form section labels: t("fields.parameters"), t("fields.clinicalNotes"), t("fields.osdiScore"), t("fields.consumables")
       - Button/action labels: add keys `sessionForm.submit`, `sessionForm.cancel`, `sessionForm.addZone`, `sessionForm.addStep`, `sessionForm.addProduct`
       - Interval warning: use existing key t("intervalWarning", { days, minDays })
       - Toast messages: add keys `sessionForm.success`, `sessionForm.error`
       - Pass t function or use useTranslation within sub-components if they are separate function components

    2. TreatmentSessionCard.tsx (~306 lines):
       - Add `import { useTranslation } from "react-i18next"` and `const { t } = useTranslation("treatment")`
       - Replace hardcoded strings:
         - "Session #" -> add key `sessionCard.sessionNumber` with interpolation `t("sessionCard.sessionNumber", { number })`
         - Device param labels: t("ipl.energy"), t("ipl.pulseCount"), etc. (reuse same keys)
         - "Consumables:" -> t("fields.consumables")
         - "Interval override:" -> add key `sessionCard.intervalOverride`
         - Severity labels: use existing severity config with translation
         - "Energy:", "Pulses:", "Spot Size:", "Zones:", etc. -> use ipl/lllt/lidCare namespace keys

    3. Add all missing translation keys to BOTH locale files:
       - `sessionForm.*` namespace for form-specific labels
       - `sessionCard.*` namespace for card-specific labels
       Use proper Vietnamese diacritics in vi file.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>TreatmentSessionForm (including inline sub-components) and TreatmentSessionCard use useTranslation("treatment") with t() calls. No hardcoded English/Vietnamese remains. All device parameter labels use shared ipl/lllt/lidCare keys.</done>
</task>

</tasks>

<verification>
1. Frontend compiles: `cd frontend && npx tsc --noEmit`
2. All 4 components import useTranslation and use t() calls
3. No hardcoded English strings remain in these components (grep for common hardcoded patterns)
4. Translation keys exist in both en/treatment.json and vi/treatment.json
</verification>

<success_criteria>
- ProtocolTemplateForm, TreatmentPackageDetail, TreatmentSessionForm, TreatmentSessionCard all use useTranslation("treatment")
- Zero hardcoded user-visible strings in these 4 components
- All new translation keys added to both en and vi locale files with proper Vietnamese diacritics
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-32-SUMMARY.md`
</output>
