---
phase: 09-treatment-protocols
plan: 24
type: execute
wave: 9
depends_on: [09-22]
files_modified:
  - frontend/src/features/treatment/components/ModifyPackageDialog.tsx
  - frontend/src/features/treatment/components/VersionHistoryDialog.tsx
  - frontend/src/features/treatment/components/SwitchTreatmentDialog.tsx
autonomous: true
requirements: [TRT-07, TRT-08]

must_haves:
  truths:
    - "Doctor can modify package mid-course with reason"
    - "Doctor can view version history of all modifications"
    - "Doctor can switch treatment type with reason"
  artifacts:
    - path: "frontend/src/features/treatment/components/ModifyPackageDialog.tsx"
      provides: "Mid-course modification dialog"
      contains: "ModifyPackageDialog"
    - path: "frontend/src/features/treatment/components/VersionHistoryDialog.tsx"
      provides: "Version history viewer dialog"
      contains: "VersionHistoryDialog"
    - path: "frontend/src/features/treatment/components/SwitchTreatmentDialog.tsx"
      provides: "Treatment type switch dialog"
      contains: "SwitchTreatmentDialog"
  key_links:
    - from: "ModifyPackageDialog"
      to: "useModifyPackage"
      via: "Mutation on submit"
      pattern: "useModifyPackage"
---

<objective>
Create modification, version history, and type switching dialogs.

Purpose: Mid-course changes are essential for clinical flexibility -- doctors adjust sessions, parameters, and even treatment types based on patient response. These dialogs provide structured interfaces for modifications with mandatory reasons, version history viewing, and treatment type switching (close old + create new).
Output: 3 dialog components
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModifyPackageDialog and VersionHistoryDialog</name>
  <files>
    frontend/src/features/treatment/components/ModifyPackageDialog.tsx
    frontend/src/features/treatment/components/VersionHistoryDialog.tsx
  </files>
  <action>
    1. ModifyPackageDialog.tsx:
       - shadcn Dialog with form
       - Props: open, onOpenChange, package (TreatmentPackageDto)
       - Form fields (all optional except reason):
         - totalSessions: number input (pre-filled with current, 1-6)
         - parametersJson: editable parameters (type-specific fields based on package.treatmentType)
         - minIntervalDays: number input (pre-filled)
         - reason: required textarea -- why the modification
       - Shows current values for comparison
       - Uses useModifyPackage(package.id) mutation
       - On success: close, toast, refresh package detail
       - onError: handleServerValidationError

    2. VersionHistoryDialog.tsx:
       - shadcn Dialog (read-only viewer)
       - Props: open, onOpenChange, versions (from package.versions or separate fetch)
       - Displays chronological list of modifications:
         - Version number
         - Date of change
         - Changed by (name)
         - Reason
         - Change description (human-readable diff)
         - Expandable: previous vs current JSON diff (optional, for technical users)
       - If no versions: "No modifications have been made"
       - Use shadcn Accordion or simple card list
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Modify dialog edits package fields with reason, history dialog shows version trail</done>
</task>

<task type="auto">
  <name>Task 2: Create SwitchTreatmentDialog</name>
  <files>frontend/src/features/treatment/components/SwitchTreatmentDialog.tsx</files>
  <action>
    SwitchTreatmentDialog.tsx:
    - shadcn Dialog with form
    - Props: open, onOpenChange, currentPackage (TreatmentPackageDto)
    - Shows current treatment summary: type, sessions completed/remaining, status
    - Form fields:
      - newProtocolTemplateId: select from protocol templates (uses useProtocolTemplates hook)
        - Filtered to exclude current treatment type (can't switch to same type)
      - reason: required textarea
    - Preview section: shows what will happen
      - "Current package (IPL) will be marked as 'Switched'"
      - "New package ({selected template name}) will be created with {remaining} sessions"
    - Uses useSwitchTreatmentType mutation
    - On success: close dialog, navigate to new package detail, toast
    - onError: handleServerValidationError
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Switch dialog shows preview and creates new package from selected template</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- ModifyPackageDialog edits package with required reason (TRT-07)
- VersionHistoryDialog shows full modification trail
- SwitchTreatmentDialog previews the close+create outcome (TRT-08)
- All dialogs use established shadcn Dialog pattern
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-24-SUMMARY.md`
</output>
