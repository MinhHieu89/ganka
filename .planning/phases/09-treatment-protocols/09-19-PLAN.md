---
phase: 09-treatment-protocols
plan: 19
type: execute
wave: 7
depends_on: [09-17]
files_modified:
  - frontend/src/features/treatment/api/treatment-api.ts
  - frontend/src/features/treatment/api/treatment-queries.ts
  - frontend/src/features/treatment/api/treatment-types.ts
autonomous: true
requirements: [TRT-01, TRT-02]

must_haves:
  truths:
    - "TypeScript types match backend DTOs"
    - "React Query hooks exist for all treatment API operations"
    - "Query key factories follow established pattern"
  artifacts:
    - path: "frontend/src/features/treatment/api/treatment-types.ts"
      provides: "TypeScript types for treatment DTOs"
      contains: "TreatmentPackageDto"
    - path: "frontend/src/features/treatment/api/treatment-queries.ts"
      provides: "Query key factories"
      contains: "treatmentKeys"
    - path: "frontend/src/features/treatment/api/treatment-api.ts"
      provides: "API functions and React Query hooks"
      contains: "useActiveTreatments"
  key_links:
    - from: "treatment-api.ts"
      to: "/api/treatments"
      via: "api.GET/POST fetch calls"
      pattern: "api\\.(GET|POST|PUT)"
---

<objective>
Create the frontend API layer for treatments: types, query keys, and React Query hooks.

Purpose: This is the data access foundation for all treatment frontend components. Types mirror backend DTOs, query key factories enable cache management, and hooks wrap all API calls with proper error handling and cache invalidation.
Output: 3 API layer files in frontend/src/features/treatment/api/
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference patterns:
- frontend/src/features/billing/ (billing-api.ts, billing types pattern)
- frontend/src/features/optical/ (optical-api.ts, query key factory pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create treatment TypeScript types</name>
  <files>frontend/src/features/treatment/api/treatment-types.ts</files>
  <action>
    Create frontend/src/features/treatment/ and frontend/src/features/treatment/api/ directories.

    Create treatment-types.ts with TypeScript interfaces/types matching backend DTOs:

    - TreatmentType = "IPL" | "LLLT" | "LidCare"
    - PackageStatus = "Active" | "Paused" | "PendingCancellation" | "Cancelled" | "Switched" | "Completed"
    - SessionStatus = "Scheduled" | "InProgress" | "Completed" | "Cancelled"
    - PricingMode = "PerSession" | "PerPackage"

    - TreatmentProtocolDto: id, name, treatmentType, defaultSessionCount, pricingMode, defaultPackagePrice, defaultSessionPrice, minIntervalDays, maxIntervalDays, defaultParametersJson, cancellationDeductionPercent, isActive, description, createdAt

    - TreatmentPackageDto: id, protocolTemplateId, protocolTemplateName, patientId, patientName, treatmentType, status, totalSessions, sessionsCompleted, sessionsRemaining, pricingMode, packagePrice, sessionPrice, minIntervalDays, parametersJson, visitId, createdAt, lastSessionDate, nextDueDate, sessions (TreatmentSessionDto[]), cancellationRequest (CancellationRequestDto | null)

    - TreatmentSessionDto: id, sessionNumber, status, parametersJson, osdiScore, osdiSeverity, clinicalNotes, performedById, performedByName, visitId, scheduledAt, completedAt, createdAt, intervalOverrideReason, consumables (SessionConsumableDto[])

    - SessionConsumableDto: id, consumableItemId, consumableName, quantity

    - CancellationRequestDto: id, requestedById, requestedByName, requestedAt, reason, deductionPercent, refundAmount, status, approvedById, approvedByName, approvedAt, rejectionReason

    - IntervalWarning: daysSinceLast, minIntervalDays

    - RecordSessionResponse: session (TreatmentSessionDto), warning (IntervalWarning | null)

    - Command types for mutations: CreateProtocolTemplateCommand, UpdateProtocolTemplateCommand, CreateTreatmentPackageCommand, RecordTreatmentSessionCommand, ModifyTreatmentPackageCommand, SwitchTreatmentTypeCommand, RequestCancellationCommand, ApproveCancellationCommand, RejectCancellationCommand, PauseTreatmentPackageCommand
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>All treatment types defined and TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create query key factories and React Query hooks</name>
  <files>
    frontend/src/features/treatment/api/treatment-queries.ts
    frontend/src/features/treatment/api/treatment-api.ts
  </files>
  <action>
    1. treatment-queries.ts:
       - Export treatmentKeys object following established pattern:
         - all: ["treatments"]
         - templates: () => [...all, "templates"]
         - template: (id) => [...templates(), id]
         - packages: () => [...all, "packages"]
         - package: (id) => [...packages(), id]
         - patientPackages: (patientId) => [...all, "patient", patientId]
         - dueSoon: () => [...all, "due-soon"]
         - sessions: (packageId) => [...all, "sessions", packageId]
         - pendingCancellations: () => [...all, "pending-cancellations"]

    2. treatment-api.ts:
       - Import api from @/shared/lib/api-client
       - Import types from treatment-types.ts
       - Import treatmentKeys from treatment-queries.ts
       - Import useQuery, useMutation, useQueryClient from @tanstack/react-query
       - Import toast from sonner

       Query hooks:
       - useProtocolTemplates(type?: TreatmentType)
       - useProtocolTemplate(id: string)
       - useActiveTreatments()
       - useTreatmentPackage(id: string)
       - usePatientTreatments(patientId: string)
       - useDueSoonSessions()
       - useTreatmentSessions(packageId: string)
       - usePendingCancellations()

       Mutation hooks (all with onError toast.error):
       - useCreateProtocolTemplate()
       - useUpdateProtocolTemplate()
       - useCreateTreatmentPackage()
       - useRecordSession(packageId: string)
       - useModifyPackage(packageId: string)
       - useSwitchTreatmentType()
       - usePausePackage()
       - useRequestCancellation()
       - useApproveCancellation()
       - useRejectCancellation()

       Each mutation invalidates relevant queries on success.
       Follow the exact pattern from billing-api.ts/consumables-api.ts.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>All React Query hooks compile with proper cache invalidation and error handling</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- Types match backend DTOs exactly
- Query key factory covers all treatment queries
- All mutation hooks include onError with toast
- Cache invalidation on mutations follows established patterns
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-19-SUMMARY.md`
</output>
