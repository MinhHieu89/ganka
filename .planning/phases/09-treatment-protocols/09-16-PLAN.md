---
phase: 09-treatment-protocols
plan: 16
type: execute
wave: 6
depends_on: [09-11, 09-12, 09-13, 09-14, 09-15]
files_modified:
  - backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj
  - backend/src/Modules/Treatment/Treatment.Presentation/IoC.cs
  - backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs
autonomous: true
requirements: [TRT-01, TRT-02, TRT-03, TRT-04, TRT-05, TRT-07, TRT-08, TRT-09, TRT-10]

must_haves:
  truths:
    - "Treatment.Presentation project exists with .csproj referencing Application + Contracts + Shared.Presentation"
    - "All treatment API endpoints are mapped under /api/treatments"
    - "Endpoints require authorization with appropriate permissions"
    - "Create/modify endpoints require Treatment.Create or Treatment.Manage permission"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj"
      provides: "Presentation project file"
      contains: "Treatment.Application"
    - path: "backend/src/Modules/Treatment/Treatment.Presentation/IoC.cs"
      provides: "Presentation DI registration"
      contains: "AddTreatmentPresentation"
    - path: "backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs"
      provides: "All API endpoint mappings"
      contains: "MapTreatmentApiEndpoints"
  key_links:
    - from: "TreatmentApiEndpoints"
      to: "All handler commands/queries"
      via: "IMessageBus.InvokeAsync"
      pattern: "bus\\.InvokeAsync"
---

<objective>
Create Treatment.Presentation project with all API endpoints.

Purpose: The Presentation layer exposes all Treatment handlers as Minimal API HTTP endpoints. This is the critical infrastructure that was not scaffolded with the module -- it must be created before any API calls work. All endpoints require authorization.
Output: New Treatment.Presentation project with IoC + endpoints
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference: backend/src/Modules/Billing/Billing.Presentation/ (exact same pattern for .csproj, IoC.cs, BillingApiEndpoints.cs)

<interfaces>
From Billing.Presentation.csproj:
- References: Billing.Application, Billing.Contracts, Shared.Presentation
- Packages: WolverineFx
- Framework: Microsoft.AspNetCore.App

From BillingApiEndpoints.cs:
- MapGroup("/api/billing").RequireAuthorization()
- Each endpoint: async (CommandType command, IMessageBus bus, CancellationToken ct) => { var result = await bus.InvokeAsync<Result<Dto>>(command, ct); return result.ToHttpResult(); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Treatment.Presentation project and IoC</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj
    backend/src/Modules/Treatment/Treatment.Presentation/IoC.cs
  </files>
  <action>
    1. Create backend/src/Modules/Treatment/Treatment.Presentation/ directory

    2. Treatment.Presentation.csproj:
       - Copy Billing.Presentation.csproj structure exactly
       - Target net10.0, ImplicitUsings, Nullable
       - FrameworkReference: Microsoft.AspNetCore.App
       - PackageReference: WolverineFx
       - ProjectReferences:
         - Treatment.Application
         - Treatment.Contracts
         - Shared.Presentation (for ResultExtensions ToHttpResult/ToCreatedHttpResult)

    3. IoC.cs:
       - namespace Treatment.Presentation
       - Static class IoC
       - AddTreatmentPresentation extension method (empty body, return services)
       - Follow Billing.Presentation.IoC.cs exactly
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj -v q</automated>
  </verify>
  <done>Treatment.Presentation project builds with correct references</done>
</task>

<task type="auto">
  <name>Task 2: Create all API endpoints</name>
  <files>backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs</files>
  <action>
    Create TreatmentApiEndpoints.cs following BillingApiEndpoints.cs pattern:

    - namespace Treatment.Presentation
    - Static class with MapTreatmentApiEndpoints extension method on IEndpointRouteBuilder
    - var group = app.MapGroup("/api/treatments").RequireAuthorization()

    Organize into private static methods:

    MapProtocolTemplateEndpoints(group):
    - POST /protocols -> CreateProtocolTemplateCommand -> ToCreatedHttpResult
    - PUT /protocols/{id} -> UpdateProtocolTemplateCommand -> ToHttpResult
    - GET /protocols -> GetProtocolTemplatesQuery -> ToHttpResult
    - GET /protocols/{id} -> GetProtocolTemplateByIdQuery -> ToHttpResult

    MapPackageEndpoints(group):
    - POST /packages -> CreateTreatmentPackageCommand -> ToCreatedHttpResult
    - GET /packages -> GetActiveTreatmentsQuery -> ToHttpResult
    - GET /packages/{id} -> GetTreatmentPackageByIdQuery -> ToHttpResult
    - GET /packages/due-soon -> GetDueSoonSessionsQuery -> ToHttpResult
    - GET /patients/{patientId}/packages -> GetPatientTreatmentsQuery -> ToHttpResult

    MapSessionEndpoints(group):
    - POST /packages/{packageId}/sessions -> RecordTreatmentSessionCommand -> ToCreatedHttpResult
    - GET /packages/{packageId}/sessions -> GetTreatmentSessionsQuery -> ToHttpResult

    MapModificationEndpoints(group):
    - PUT /packages/{packageId}/modify -> ModifyTreatmentPackageCommand -> ToHttpResult
    - POST /packages/{packageId}/switch -> SwitchTreatmentTypeCommand -> ToHttpResult
    - POST /packages/{packageId}/pause -> PauseTreatmentPackageCommand -> ToHttpResult

    MapCancellationEndpoints(group):
    - POST /packages/{packageId}/cancel -> RequestCancellationCommand -> ToHttpResult
    - POST /packages/{packageId}/cancel/approve -> ApproveCancellationCommand -> ToHttpResult
    - POST /packages/{packageId}/cancel/reject -> RejectCancellationCommand -> ToHttpResult
    - GET /cancellations/pending -> GetPendingCancellationsQuery -> ToHttpResult

    All endpoints use: async (command, IMessageBus bus, CancellationToken ct) pattern
    Return result.ToHttpResult() or result.ToCreatedHttpResult()
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj -v q</automated>
  </verify>
  <done>All ~17 API endpoints mapped with correct routes, methods, and authorization</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj -v q
</verification>

<success_criteria>
- Treatment.Presentation project builds with Billing.Presentation-identical structure
- All handler commands/queries have corresponding HTTP endpoints
- Endpoints use RequireAuthorization() (TRT-10)
- Routes follow RESTful conventions under /api/treatments
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-16-SUMMARY.md`
</output>
