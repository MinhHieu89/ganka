---
phase: 09-treatment-protocols
plan: 17
type: execute
wave: 7
depends_on: [09-16]
files_modified:
  - backend/src/Bootstrapper/Program.cs
  - backend/src/Bootstrapper/Bootstrapper.csproj
  - backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj
autonomous: true
requirements: [TRT-01]

must_haves:
  truths:
    - "Treatment module is fully wired into Bootstrapper"
    - "API endpoints respond to HTTP requests"
    - "EF Core migration exists for Treatment schema"
  artifacts:
    - path: "backend/src/Bootstrapper/Program.cs"
      provides: "Bootstrapper with Treatment DI and endpoint mapping"
      contains: "AddTreatmentApplication"
    - path: "backend/src/Bootstrapper/Bootstrapper.csproj"
      provides: "Solution reference to Treatment.Presentation"
      contains: "Treatment.Presentation"
  key_links:
    - from: "Program.cs"
      to: "Treatment.Presentation"
      via: "AddTreatmentPresentation + MapTreatmentApiEndpoints"
      pattern: "MapTreatmentApiEndpoints"
    - from: "Program.cs"
      to: "Treatment.Infrastructure"
      via: "AddTreatmentInfrastructure (DbContext + repos)"
      pattern: "AddTreatmentInfrastructure"
---

<objective>
Wire Treatment module into Bootstrapper and create EF Core migration.

Purpose: Without Bootstrapper wiring, the Treatment module endpoints will never be registered and the database tables won't exist. This plan connects all Treatment DI registrations and creates the EF Core migration for the treatment schema.
Output: Updated Bootstrapper Program.cs + .csproj + migration
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference: Look at how Optical.Presentation was wired into Bootstrapper Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Treatment module into Bootstrapper</name>
  <files>
    backend/src/Bootstrapper/Program.cs
    backend/src/Bootstrapper/Bootstrapper.csproj
  </files>
  <action>
    1. Update Bootstrapper.csproj:
       - Add ProjectReference to Treatment.Presentation.csproj
       - Path: ..\..\src\Modules\Treatment\Treatment.Presentation\Treatment.Presentation.csproj

    2. Update Program.cs:
       - Add using statements:
         - using Treatment.Application
         - using Treatment.Infrastructure
         - using Treatment.Presentation
       - In the services registration section (near other AddXxxApplication/Infrastructure/Presentation calls):
         - builder.Services.AddTreatmentApplication();
         - builder.Services.AddTreatmentInfrastructure();
         - builder.Services.AddTreatmentPresentation();
       - In the endpoint mapping section (near other MapXxxApiEndpoints calls):
         - app.MapTreatmentApiEndpoints();

    Follow the exact same pattern used for Billing, Optical, and other modules.
  </action>
  <verify>
    <automated>dotnet build backend/src/Bootstrapper/Bootstrapper.csproj -v q</automated>
  </verify>
  <done>Bootstrapper builds with Treatment module fully wired</done>
</task>

<task type="auto">
  <name>Task 2: Create EF Core migration</name>
  <files>backend/src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj</files>
  <action>
    Run EF Core migration for the Treatment module:

    1. Navigate to the Bootstrapper project directory
    2. Run: dotnet ef migrations add InitialTreatment --project ../Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --context TreatmentDbContext --output-dir Migrations
    3. Run: dotnet ef database update --project ../Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --context TreatmentDbContext

    If the ef tools command doesn't find the DbContext, ensure the Bootstrapper project references Treatment.Infrastructure (via Treatment.Presentation -> Treatment.Application -> Treatment.Domain, and Treatment.Infrastructure is registered in IoC).

    Alternative approach if needed: Use the Bootstrapper as startup project:
    cd backend/src/Bootstrapper
    dotnet ef migrations add InitialTreatment --context TreatmentDbContext --project ../Modules/Treatment/Treatment.Infrastructure
    dotnet ef database update --context TreatmentDbContext --project ../Modules/Treatment/Treatment.Infrastructure

    Note: The migration files themselves are auto-generated and don't count toward the 6-file limit since they're build artifacts.
  </action>
  <verify>
    <automated>dotnet build backend/src/Bootstrapper/Bootstrapper.csproj -v q && dotnet test backend/tests/Treatment.Unit.Tests -v q --no-build</automated>
  </verify>
  <done>Migration created and applied, Treatment tables exist in database</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Bootstrapper/Bootstrapper.csproj -v q
dotnet test backend/Ganka28.slnx -v q --no-build
</verification>

<success_criteria>
- Bootstrapper builds with Treatment module wired
- Treatment tables exist in database (treatment schema)
- Full test suite passes (no regressions)
- API endpoints are reachable (verified by subsequent frontend plans)
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-17-SUMMARY.md`
</output>
