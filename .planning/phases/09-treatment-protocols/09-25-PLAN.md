---
phase: 09-treatment-protocols
plan: 25
type: execute
wave: 9
depends_on: [09-22]
files_modified:
  - frontend/src/features/treatment/components/CancellationApprovalQueue.tsx
  - frontend/src/features/treatment/components/CancellationRequestDialog.tsx
  - frontend/src/app/routes/_authenticated/treatments.approvals.tsx
autonomous: true
requirements: [TRT-09, TRT-10]

must_haves:
  truths:
    - "Manager can view all pending cancellation requests"
    - "Manager can approve/reject cancellation with PIN verification"
    - "Doctor/staff can request cancellation with reason"
    - "Approval queue is a dedicated page"
  artifacts:
    - path: "frontend/src/features/treatment/components/CancellationApprovalQueue.tsx"
      provides: "Manager approval queue with approve/reject actions"
      contains: "CancellationApprovalQueue"
    - path: "frontend/src/features/treatment/components/CancellationRequestDialog.tsx"
      provides: "Cancel request dialog and approve/reject dialogs"
      contains: "CancellationRequestDialog"
    - path: "frontend/src/app/routes/_authenticated/treatments.approvals.tsx"
      provides: "Route file for /treatments/approvals"
      contains: "createFileRoute"
  key_links:
    - from: "CancellationApprovalQueue"
      to: "usePendingCancellations"
      via: "Data fetching for pending requests"
      pattern: "usePendingCancellations"
    - from: "CancellationRequestDialog"
      to: "useApproveCancellation"
      via: "Manager approval mutation"
      pattern: "useApproveCancellation"
---

<objective>
Create cancellation request dialog and manager approval queue page.

Purpose: Treatment cancellation follows a multi-step approval workflow matching the Billing refund pattern. Staff requests cancellation, it appears in the manager's approval queue, and the manager approves (with PIN and deduction percentage) or rejects (with reason). This ensures financial accountability.
Output: Approval queue + request/approve/reject dialogs + route
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference: frontend/src/features/billing/ (RefundDialog, ApprovalPinDialog pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CancellationApprovalQueue and route</name>
  <files>
    frontend/src/features/treatment/components/CancellationApprovalQueue.tsx
    frontend/src/app/routes/_authenticated/treatments.approvals.tsx
  </files>
  <action>
    1. CancellationApprovalQueue.tsx:
       - Uses usePendingCancellations() hook
       - DataTable with columns:
         - Patient Name
         - Treatment Type (badge)
         - Sessions Completed / Total
         - Requested By
         - Request Date
         - Reason
         - Default Deduction %
         - Actions (Approve, Reject buttons)
       - Approve button opens approve dialog (inline or separate component):
         - Manager PIN input (password field)
         - Deduction percentage: number input (10-20, pre-filled from package default)
         - Calculated refund amount (displayed after deduction, computed client-side)
         - Uses useApproveCancellation mutation
       - Reject button opens reject dialog:
         - Rejection reason (required textarea)
         - Uses useRejectCancellation mutation
       - Loading/empty states

    2. treatments.approvals.tsx:
       - createFileRoute for /treatments/approvals
       - Renders page title "Cancellation Approvals" + CancellationApprovalQueue
       - Breadcrumb: Treatments > Approvals
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Approval queue shows pending cancellations with approve/reject actions</done>
</task>

<task type="auto">
  <name>Task 2: Create CancellationRequestDialog</name>
  <files>frontend/src/features/treatment/components/CancellationRequestDialog.tsx</files>
  <action>
    CancellationRequestDialog.tsx:
    - shadcn Dialog form
    - Props: open, onOpenChange, package (TreatmentPackageDto)
    - Shows package summary: patient, type, progress, pricing
    - Shows estimated refund calculation:
      - Default deduction from template (e.g., "15%")
      - Estimated refund based on remaining sessions and pricing mode
    - Form fields:
      - reason: required textarea
    - Warning: "This will submit a cancellation request requiring manager approval"
    - Uses useRequestCancellation mutation
    - On success: close, toast "Cancellation request submitted"
    - On error: handleServerValidationError
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Cancellation request dialog shows refund estimate and submits request</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- Approval queue displays all pending cancellations in DataTable (TRT-09)
- Manager approve dialog includes PIN input and deduction percentage
- Client-side refund calculation preview
- Rejection requires reason
- Route at /treatments/approvals
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-25-SUMMARY.md`
</output>
