---
phase: 09-treatment-protocols
plan: 21
type: execute
wave: 8
depends_on: [09-19]
files_modified:
  - frontend/src/features/treatment/components/TreatmentsPage.tsx
  - frontend/src/features/treatment/components/DueSoonSection.tsx
  - frontend/src/features/treatment/components/TreatmentPackageForm.tsx
  - frontend/src/app/routes/_authenticated/treatments/index.tsx
autonomous: true
requirements: [TRT-01, TRT-02, TRT-05, TRT-06]

must_haves:
  truths:
    - "Treatments page shows DataTable of all active treatment packages"
    - "Due Soon section at top surfaces packages eligible for next session"
    - "Doctor can create new treatment package from template for a patient"
  artifacts:
    - path: "frontend/src/features/treatment/components/TreatmentsPage.tsx"
      provides: "Main treatments list page with DataTable"
      contains: "TreatmentsPage"
    - path: "frontend/src/features/treatment/components/DueSoonSection.tsx"
      provides: "Due Soon alerts section"
      contains: "DueSoonSection"
    - path: "frontend/src/features/treatment/components/TreatmentPackageForm.tsx"
      provides: "Create package dialog from template"
      contains: "TreatmentPackageForm"
    - path: "frontend/src/app/routes/_authenticated/treatments/index.tsx"
      provides: "Route file for /treatments"
      contains: "createFileRoute"
  key_links:
    - from: "TreatmentsPage"
      to: "useActiveTreatments"
      via: "Data fetching for DataTable"
      pattern: "useActiveTreatments"
    - from: "DueSoonSection"
      to: "useDueSoonSessions"
      via: "Data fetching for due soon list"
      pattern: "useDueSoonSessions"
---

<objective>
Create the main treatments page with DataTable, Due Soon section, and package creation form.

Purpose: This is the staff landing page for treatment management -- showing all active packages across patients with a "Due Soon" section at the top for proactive scheduling. Doctors can create new treatment packages from templates.
Output: Treatments page + Due Soon section + Package form + route
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference: frontend/src/features/billing/ (cashier dashboard pattern with tables)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TreatmentsPage and DueSoonSection</name>
  <files>
    frontend/src/features/treatment/components/TreatmentsPage.tsx
    frontend/src/features/treatment/components/DueSoonSection.tsx
  </files>
  <action>
    1. TreatmentsPage.tsx:
       - Uses useActiveTreatments() hook
       - "Create Package" button opens TreatmentPackageForm dialog
       - Renders DueSoonSection at top
       - DataTable below with columns:
         - Patient Name (clickable, navigates to /patients/{id})
         - Treatment Type (color-coded badge)
         - Status (color-coded badge: Active=green, Paused=yellow, PendingCancellation=orange, Completed=blue, Cancelled=red, Switched=gray)
         - Progress (e.g., "3/5 sessions" with progress bar or text)
         - Pricing (formatted VND amount)
         - Last Session (relative date)
         - Next Due (date or "Overdue" badge if past due)
         - Actions (view detail link)
       - Row click navigates to /treatments/{packageId} detail page
       - Filters: Treatment Type, Status
       - Loading/error states

    2. DueSoonSection.tsx:
       - Uses useDueSoonSessions() hook
       - Card component at top of page
       - Shows list of packages with upcoming due sessions
       - Each item: Patient name, treatment type, days since last session, "Record Session" button
       - If empty: subtle "No sessions due" message
       - Collapsed by default if more than 5 items (show first 5, expand button)
       - Use shadcn Alert or Card with subtle background color
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>TreatmentsPage renders DataTable with all columns, DueSoonSection shows due packages</done>
</task>

<task type="auto">
  <name>Task 2: Create TreatmentPackageForm and route</name>
  <files>
    frontend/src/features/treatment/components/TreatmentPackageForm.tsx
    frontend/src/app/routes/_authenticated/treatments/index.tsx
  </files>
  <action>
    1. TreatmentPackageForm.tsx:
       - shadcn Dialog form
       - Step 1: Select protocol template (uses useProtocolTemplates hook, rendered as Select or searchable combobox)
       - On template selection: pre-populate form with template defaults
       - Step 2: Customize (all optional, defaults from template):
         - Patient selector (combobox searching by name/ID -- reuse existing patient search pattern)
         - Total sessions: number, 1-6
         - Pricing mode: select
         - Package price: number
         - Session price: number
         - Min interval days: number
         - Parameters: pre-filled from template, editable
         - Optional VisitId link (if opened from visit context)
       - Uses useCreateTreatmentPackage mutation
       - On success: invalidates queries, closes dialog, toast success
       - onError: handleServerValidationError

    2. treatments/index.tsx:
       - Create frontend/src/app/routes/_authenticated/treatments/ directory
       - createFileRoute for /treatments
       - Renders page title "Treatments" + TreatmentsPage component
       - Breadcrumb: Treatments
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>TreatmentPackageForm creates packages from templates, route at /treatments works</done>
</task>

</tasks>

<verification>
cd frontend && npx tsc --noEmit --pretty
</verification>

<success_criteria>
- DataTable shows all active packages with progress and status
- Due Soon section surfaces overdue/eligible packages
- Package form pre-populates from template with override capability
- Route navigable at /treatments
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-21-SUMMARY.md`
</output>
