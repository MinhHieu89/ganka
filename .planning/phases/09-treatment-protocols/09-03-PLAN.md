---
phase: 09-treatment-protocols
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - backend/src/Modules/Treatment/Treatment.Domain/Entities/TreatmentProtocol.cs
  - backend/src/Modules/Treatment/Treatment.Domain/Events/TreatmentSessionCompletedEvent.cs
  - backend/src/Modules/Treatment/Treatment.Domain/Events/TreatmentPackageCompletedEvent.cs
autonomous: true
requirements: [TRT-01, TRT-05, TRT-09]

must_haves:
  truths:
    - "Protocol templates can be created with treatment type, session count, pricing, and default parameters"
    - "Protocol templates store configurable min interval days and cancellation deduction percentage"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Domain/Entities/TreatmentProtocol.cs"
      provides: "TreatmentProtocol aggregate root for global templates"
      contains: "class TreatmentProtocol"
    - path: "backend/src/Modules/Treatment/Treatment.Domain/Events/TreatmentSessionCompletedEvent.cs"
      provides: "Cross-module domain event for consumable deduction"
      contains: "record TreatmentSessionCompletedEvent"
    - path: "backend/src/Modules/Treatment/Treatment.Domain/Events/TreatmentPackageCompletedEvent.cs"
      provides: "Internal domain event for auto-completion"
      contains: "record TreatmentPackageCompletedEvent"
  key_links: []
---

<objective>
Create the TreatmentProtocol aggregate root entity and domain events.

Purpose: TreatmentProtocol is the global template that doctors configure (e.g., "Standard IPL 4-session"). When creating a patient's treatment package, the template provides defaults. Domain events enable cross-module communication for consumable deduction and auto-completion notifications.
Output: TreatmentProtocol entity + 2 domain event records
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference patterns:
- Optical.Domain/Entities/Frame.cs (aggregate root with factory method, IAuditable, BranchId)
- Billing.Domain/Events/ (domain event records implementing IDomainEvent)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TreatmentProtocol aggregate root</name>
  <files>backend/src/Modules/Treatment/Treatment.Domain/Entities/TreatmentProtocol.cs</files>
  <action>
    Create Treatment.Domain/Entities/ directory and TreatmentProtocol.cs:

    - namespace Treatment.Domain.Entities
    - Inherits from AggregateRoot, implements IAuditable
    - Properties (all private set):
      - Name (string, max 200) -- template name, e.g., "Standard IPL 4-session"
      - TreatmentType (TreatmentType enum)
      - DefaultSessionCount (int, 1-6)
      - PricingMode (PricingMode enum)
      - DefaultPackagePrice (decimal)
      - DefaultSessionPrice (decimal)
      - MinIntervalDays (int) -- default min interval between sessions
      - MaxIntervalDays (int) -- max recommended interval
      - DefaultParametersJson (string) -- JSON of typed value object (IplParameters/LlltParameters/LidCareParameters)
      - CancellationDeductionPercent (decimal, default 15, range 10-20) -- per TRT-09
      - IsActive (bool, default true) -- soft delete
      - Description (string?, optional notes)
      - IAuditable: CreatedAt, UpdatedAt, CreatedById, UpdatedById
      - BranchId (BranchId value object)

    - Private parameterless constructor for EF Core
    - Static factory method: Create(name, treatmentType, defaultSessionCount, pricingMode, packagePrice, sessionPrice, minIntervalDays, maxIntervalDays, parametersJson, cancellationDeductionPercent, createdById, branchId)
      - Validates session count 1-6, deduction percent 10-20, prices >= 0
      - Sets Id = Guid.NewGuid(), CreatedAt = DateTime.UtcNow
    - Update method for modifying template properties
    - Deactivate/Activate methods for soft delete

    Follow Frame.cs pattern for AggregateRoot base class usage.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Domain/Treatment.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>TreatmentProtocol entity compiles with factory method, all properties, and IAuditable</done>
</task>

<task type="auto">
  <name>Task 2: Create domain events</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Domain/Events/TreatmentSessionCompletedEvent.cs
    backend/src/Modules/Treatment/Treatment.Domain/Events/TreatmentPackageCompletedEvent.cs
  </files>
  <action>
    Create Treatment.Domain/Events/ directory and two event records:

    1. TreatmentSessionCompletedEvent.cs:
       - namespace Treatment.Domain.Events
       - sealed record implementing IDomainEvent (from Shared.Domain)
       - Properties: PackageId (Guid), SessionId (Guid), PatientId (Guid), TreatmentType (TreatmentType), Consumables (List of ConsumableUsage)
       - Include nested record: ConsumableUsage(Guid ConsumableItemId, int Quantity)
       - Auto-generated EventId (Guid.NewGuid()) and OccurredAt (DateTime.UtcNow)

    2. TreatmentPackageCompletedEvent.cs:
       - namespace Treatment.Domain.Events
       - sealed record implementing IDomainEvent
       - Properties: PackageId (Guid), PatientId (Guid), TreatmentType (TreatmentType)
       - Auto-generated EventId and OccurredAt

    Follow established IDomainEvent pattern from Billing/Optical modules.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Domain/Treatment.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>Both domain events compile and implement IDomainEvent</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Treatment/Treatment.Domain/Treatment.Domain.csproj --no-restore -v q
</verification>

<success_criteria>
- TreatmentProtocol has factory method with validation, all specified properties
- Domain events implement IDomainEvent with correct payload
- TreatmentSessionCompletedEvent includes ConsumableUsage list for cross-module deduction
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-03-SUMMARY.md`
</output>
