---
phase: 09-treatment-protocols
plan: 09
type: execute
wave: 3
depends_on: [09-03, 09-04, 09-05, 09-06]
files_modified:
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/TreatmentProtocolRepository.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/TreatmentPackageRepository.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/UnitOfWork.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/IoC.cs
autonomous: true
requirements: [TRT-01, TRT-02, TRT-06]

must_haves:
  truths:
    - "Repository implementations use TreatmentDbContext for data access"
    - "GetByIdAsync includes child entities via Include"
    - "GetDueSoonAsync calculates due-soon from last session date + min interval"
    - "IoC registers repositories and UnitOfWork for DI"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/TreatmentProtocolRepository.cs"
      provides: "Protocol repository implementation"
      contains: "class TreatmentProtocolRepository"
    - path: "backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/TreatmentPackageRepository.cs"
      provides: "Package repository with eager loading"
      contains: "class TreatmentPackageRepository"
    - path: "backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/UnitOfWork.cs"
      provides: "UnitOfWork wrapping TreatmentDbContext.SaveChangesAsync"
      contains: "class UnitOfWork"
    - path: "backend/src/Modules/Treatment/Treatment.Infrastructure/IoC.cs"
      provides: "Infrastructure DI registration"
      contains: "AddTreatmentInfrastructure"
  key_links:
    - from: "TreatmentPackageRepository"
      to: "TreatmentDbContext"
      via: "EF Core queries with Include for child entities"
      pattern: "Include.*Sessions.*Consumables"
---

<objective>
Create repository implementations and Infrastructure IoC registration.

Purpose: Repository implementations translate the data access contracts (interfaces from plan 08) into EF Core queries against TreatmentDbContext. The IoC class registers all infrastructure services for dependency injection.
Output: 3 repository implementations + Infrastructure IoC.cs
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-treatment-protocols/09-RESEARCH.md

Reference patterns:
- Optical.Infrastructure/Repositories/FrameRepository.cs
- Optical.Infrastructure/Repositories/GlassesOrderRepository.cs (Include patterns for aggregate children)
- Optical.Infrastructure/IoC.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create repository implementations</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/TreatmentProtocolRepository.cs
    backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/TreatmentPackageRepository.cs
    backend/src/Modules/Treatment/Treatment.Infrastructure/Repositories/UnitOfWork.cs
  </files>
  <action>
    Create Treatment.Infrastructure/Repositories/ directory and:

    1. TreatmentProtocolRepository.cs:
       - Implements ITreatmentProtocolRepository
       - Constructor takes TreatmentDbContext
       - GetByIdAsync: simple FirstOrDefaultAsync by Id
       - GetAllAsync: filter by IsActive unless includeInactive=true, OrderBy Name
       - GetByTypeAsync: filter by TreatmentType + IsActive, OrderBy Name
       - AddAsync: _context.TreatmentProtocols.Add

    2. TreatmentPackageRepository.cs:
       - Implements ITreatmentPackageRepository
       - Constructor takes TreatmentDbContext
       - GetByIdAsync: Include(Sessions).ThenInclude(Consumables), Include(Versions), Include(CancellationRequest)
         Use AsSplitQuery() to avoid cartesian explosion with multiple child collections
       - GetByPatientIdAsync: filter by PatientId, Include Sessions, OrderByDescending CreatedAt
       - GetActivePackagesAsync: filter Status == Active, Include Sessions, OrderBy PatientName
       - GetDueSoonAsync: query packages where Status == Active, compute from last completed session's CompletedAt + MinIntervalDays <= DateTime.UtcNow. Return packages where the interval has passed (session is due). This requires loading sessions to compute. Use a client-side filter after loading active packages with sessions:
         - Load all active packages with their sessions
         - Filter to those where either (a) no sessions completed yet, or (b) last session CompletedAt + MinIntervalDays <= now
       - GetPendingCancellationsAsync: filter Status == PendingCancellation, Include CancellationRequest
       - AddAsync: _context.TreatmentPackages.Add

    3. UnitOfWork.cs:
       - Implements IUnitOfWork
       - Constructor takes TreatmentDbContext
       - SaveChangesAsync delegates to _context.SaveChangesAsync
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>All 3 repository implementations compile with correct Include patterns and query logic</done>
</task>

<task type="auto">
  <name>Task 2: Create Infrastructure IoC registration</name>
  <files>backend/src/Modules/Treatment/Treatment.Infrastructure/IoC.cs</files>
  <action>
    Create IoC.cs:
    - namespace Treatment.Infrastructure
    - Static class IoC with AddTreatmentInfrastructure extension method on IServiceCollection
    - Register:
      - ITreatmentProtocolRepository -> TreatmentProtocolRepository (Scoped)
      - ITreatmentPackageRepository -> TreatmentPackageRepository (Scoped)
      - IUnitOfWork -> UnitOfWork (Scoped)
    - Return services

    Follow Optical.Infrastructure/IoC.cs pattern.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>IoC.cs registers all repository implementations and UnitOfWork</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q
</verification>

<success_criteria>
- All repositories use correct Include patterns for eager loading
- GetDueSoonAsync correctly identifies packages with overdue sessions
- IoC registers all services as Scoped
- AsSplitQuery used on GetByIdAsync to prevent cartesian explosion
</success_criteria>

<output>
After completion, create `.planning/phases/09-treatment-protocols/09-09-SUMMARY.md`
</output>
