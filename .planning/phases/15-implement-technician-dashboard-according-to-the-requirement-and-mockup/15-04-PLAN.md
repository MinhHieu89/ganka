---
phase: 15-implement-technician-dashboard
plan: 04
type: execute
wave: 3
depends_on: [15-02, 15-03]
files_modified:
  - frontend/src/features/technician/components/TechnicianDashboard.tsx
  - frontend/src/features/technician/components/TechnicianKpiCards.tsx
  - frontend/src/features/technician/components/TechnicianBanner.tsx
  - frontend/src/features/technician/components/TechnicianToolbar.tsx
  - frontend/src/features/technician/components/TechnicianQueueTable.tsx
  - frontend/src/features/technician/components/TechnicianActionMenu.tsx
  - frontend/src/features/technician/components/TechnicianStatusBadge.tsx
  - frontend/src/features/technician/components/RedFlagDialog.tsx
  - frontend/src/features/technician/components/ReturnToQueueDialog.tsx
  - frontend/src/features/technician/components/PausePatientDialog.tsx
  - frontend/src/app/routes/_authenticated/dashboard.tsx
autonomous: true
requirements: [TECH-09, TECH-10, TECH-11, TECH-12]

must_haves:
  truths:
    - "Technician sees 4 KPI cards with correct colors and polling at 30s"
    - "Technician sees patient queue table with 9 columns, FIFO sort, in-progress pinned to top, completed dimmed"
    - "Technician can filter by status using filter pills with counts"
    - "Technician sees in-progress banner when measuring a patient"
    - "Action menu shows correct actions per patient status"
    - "Red flag dialog requires reason selection with validation"
    - "Return-to-queue dialog shows confirmation message"
    - "Dashboard route renders TechnicianDashboard for Technician role"
  artifacts:
    - path: "frontend/src/features/technician/components/TechnicianDashboard.tsx"
      provides: "Main dashboard container orchestrating all sub-components"
      contains: "TechnicianDashboard"
    - path: "frontend/src/features/technician/components/TechnicianQueueTable.tsx"
      provides: "Patient queue table with TanStack Table"
      contains: "TechnicianQueueTable"
    - path: "frontend/src/features/technician/components/TechnicianActionMenu.tsx"
      provides: "Status-based action dropdown menu"
      contains: "TechnicianActionMenu"
    - path: "frontend/src/features/technician/components/RedFlagDialog.tsx"
      provides: "Red flag confirmation with reason selection"
      contains: "RedFlagDialog"
  key_links:
    - from: "frontend/src/app/routes/_authenticated/dashboard.tsx"
      to: "TechnicianDashboard"
      via: "Role check isTechnician -> render component"
      pattern: 'roles.*includes.*Technician'
    - from: "TechnicianDashboard.tsx"
      to: "technician-api.ts"
      via: "useTechnicianDashboard and useTechnicianKpi hooks"
      pattern: "useTechnicianDashboard|useTechnicianKpi"
    - from: "TechnicianActionMenu.tsx"
      to: "technician-api.ts"
      via: "useAcceptOrder, useCompleteOrder, useReturnToQueue, useRedFlagOrder mutations"
      pattern: "useAcceptOrder|useCompleteOrder"
---

<objective>
Build the complete Technician Dashboard UI: KPI cards, in-progress banner, toolbar with filter pills and search, patient queue table with TanStack Table, status-based action menus, and all confirmation dialogs. Wire into the dashboard route with role-based rendering.

Purpose: Delivers the full interactive dashboard that technicians use to manage their pre-exam patient queue. This is the primary user-facing deliverable of Phase 15.
Output: Fully functional dashboard UI with all interactions, filtering, polling, and toast notifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-implement-technician-dashboard-according-to-the-requirement-and-mockup/15-CONTEXT.md
@.planning/phases/15-implement-technician-dashboard-according-to-the-requirement-and-mockup/15-UI-SPEC.md
@.planning/phases/15-implement-technician-dashboard-according-to-the-requirement-and-mockup/15-02-SUMMARY.md
@.planning/phases/15-implement-technician-dashboard-according-to-the-requirement-and-mockup/15-03-SUMMARY.md

<interfaces>
<!-- Types from Plan 03 -->
From frontend/src/features/technician/types/technician.types.ts:
```typescript
export type TechnicianStatus = "waiting" | "in_progress" | "red_flag" | "completed"
export type TechnicianVisitType = "new" | "follow_up" | "additional"
export interface TechnicianDashboardRow {
  orderId: string; visitId: string; patientId: string; patientName: string;
  patientCode: string | null; birthYear: number | null; checkinTime: string;
  waitMinutes: number; reason: string | null; visitType: TechnicianVisitType;
  status: TechnicianStatus; technicianName: string | null;
  redFlagReason: string | null; isRedFlag: boolean;
}
export interface TechnicianKpi { waiting: number; inProgress: number; completed: number; redFlag: number; }
export interface TechnicianDashboardFilters { status?: TechnicianStatus; search?: string; page: number; pageSize: number; }
```

<!-- API hooks from Plan 03 -->
From frontend/src/features/technician/api/technician-api.ts:
```typescript
export function useTechnicianDashboard(filters: TechnicianDashboardFilters): UseQueryResult<TechnicianDashboardRow[]>
export function useTechnicianKpi(): UseQueryResult<TechnicianKpi>
export function useAcceptOrder(): UseMutationResult
export function useCompleteOrder(): UseMutationResult
export function useReturnToQueue(): UseMutationResult
export function useRedFlagOrder(): UseMutationResult<void, Error, { orderId: string; reason: string }>
```

<!-- Receptionist components to follow as pattern -->
From frontend/src/features/receptionist/components/ReceptionistDashboard.tsx: (container pattern)
From frontend/src/features/receptionist/components/KpiCards.tsx: (KPI card grid)
From frontend/src/features/receptionist/components/StatusFilterPills.tsx: (filter pills)
From frontend/src/features/receptionist/components/PatientQueueTable.tsx: (TanStack Table)
From frontend/src/features/receptionist/components/RowActionMenu.tsx: (action dropdown)

<!-- Dashboard route pattern -->
From frontend/src/app/routes/_authenticated/dashboard.tsx:
```typescript
const isReceptionist = user?.roles?.includes("Receptionist") ?? false
if (isReceptionist) return <ReceptionistDashboard />
// ADD: Technician check BEFORE default dashboard
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KPI cards, banner, toolbar, status badge components</name>
  <files>
    frontend/src/features/technician/components/TechnicianKpiCards.tsx,
    frontend/src/features/technician/components/TechnicianBanner.tsx,
    frontend/src/features/technician/components/TechnicianToolbar.tsx,
    frontend/src/features/technician/components/TechnicianStatusBadge.tsx
  </files>
  <read_first>
    frontend/src/features/receptionist/components/KpiCards.tsx,
    frontend/src/features/receptionist/components/StatusFilterPills.tsx,
    frontend/src/features/receptionist/components/StatusBadge.tsx,
    frontend/src/features/technician/types/technician.types.ts,
    frontend/src/features/technician/api/technician-api.ts,
    frontend/src/shared/i18n/locales/vi/technician.json,
    frontend/src/styles/globals.css,
    docs/dev/technician/technician-dashboard.md
  </read_first>
  <action>
    **All components use CSS variables defined in Plan 03 and i18n keys from `technician` namespace.**

    1. Create `TechnicianKpiCards.tsx`:
       - Props: `kpi: TechnicianKpi | undefined`, `isLoading: boolean`
       - 4 cards in a grid: `grid grid-cols-4 gap-3` (12px gap = gap-3)
       - Each card uses shadcn `Card` component
       - Card structure:
         - Label: 12px regular text (use `text-xs`), translated from `t("kpi.waiting")` etc.
         - Value: 24px bold text (use `text-2xl font-bold`), colored per KPI:
           - Waiting: `color: var(--tech-kpi-waiting)` (#BA7517 amber)
           - In Progress: `color: var(--tech-kpi-in-progress)` (#185FA5 blue)
           - Completed: `color: var(--tech-kpi-completed)` (#0F6E56 teal)
           - Red Flag: `color: var(--tech-kpi-red-flag)` (#A32D2D red)
         - Sub-text: 12px regular hint text (use `text-xs text-muted-foreground`), translated from `t("kpi.waitingSub")` etc.
       - Icons from Tabler: `IconClock` (waiting), `IconEye` (in progress), `IconCircleCheck` (completed), `IconAlertTriangle` (red flag)
       - Loading state: 4 `Skeleton` rectangles matching card dimensions
       - Padding: `p-4` (16px)

    2. Create `TechnicianBanner.tsx`:
       - Props: `currentPatient: TechnicianDashboardRow | null`, `onContinue: () => void`
       - Visible only when `currentPatient` is not null (technician has a patient in "in_progress" status)
       - Container: `bg-[var(--tech-banner-bg)]` (#E6F1FB), border `0.5px solid var(--tech-banner-border)`, border-radius `rounded-lg` (8px)
       - Content: Left side shows label "Dang thuc hien Pre-Exam" (12px bold, color `var(--tech-banner-text)`), patient name (14px), birth year, chief complaint, check-in time
       - Right side: "Tiep tuc do" button with `bg-[var(--tech-action-primary)]` (#534AB7), white text, onClick calls `onContinue`
       - Hidden during loading (no skeleton per UI spec)

    3. Create `TechnicianToolbar.tsx`:
       - Props: `activeFilter: TechnicianStatus | undefined`, `onFilterChange: (status?: TechnicianStatus) => void`, `search: string`, `onSearchChange: (search: string) => void`, `counts: Record<string, number>`
       - Left: Filter pills using shadcn `ToggleGroup` (single mode):
         - "Tat ca" (all), "Cho kham" (waiting), "Dang do" (in_progress), "Hoan tat" (completed), "Red flag" (red_flag)
         - Each pill shows count in parentheses: "Cho kham (3)"
         - Active: black bg (#000), white text
         - Inactive: transparent bg, border light gray, foreground text
       - Right: Search input using shadcn `Input`, placeholder from i18n `t("search.placeholder")`, debounce 300ms using `setTimeout`/`clearTimeout` pattern
       - Layout: `flex items-center justify-between gap-4`

    4. Create `TechnicianStatusBadge.tsx`:
       - Props: `status: TechnicianStatus`
       - Uses shadcn `Badge` component with custom inline styles for colors:
         - waiting: `bg: var(--tech-status-waiting-bg)`, `color: var(--tech-status-waiting-text)`
         - in_progress: `bg: var(--tech-status-in-progress-bg)`, `color: var(--tech-status-in-progress-text)`
         - red_flag: `bg: var(--tech-status-red-flag-bg)`, `color: var(--tech-status-red-flag-text)`
         - completed: `bg: var(--tech-status-completed-bg)`, `color: var(--tech-status-completed-text)`
       - Text from i18n: `t("status.waiting")`, etc.
       - Add `role="status"` for accessibility
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TechnicianKpiCards.tsx contains 4 Card components with `var(--tech-kpi-waiting)`, `var(--tech-kpi-in-progress)`, `var(--tech-kpi-completed)`, `var(--tech-kpi-red-flag)`
    - TechnicianBanner.tsx contains `var(--tech-banner-bg)` and `var(--tech-action-primary)` and conditional rendering based on currentPatient
    - TechnicianToolbar.tsx contains `ToggleGroup` and `Input` with debounce logic
    - TechnicianStatusBadge.tsx contains `var(--tech-status-waiting-bg)` and uses shadcn `Badge`
    - All components import from `@/features/technician/types/technician.types`
    - All components use `useTranslation("technician")` for text
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>KPI cards, in-progress banner, toolbar with filter pills and search, and status badge components all created matching UI spec exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Queue table, action menu, dialogs, and dashboard container with route wiring</name>
  <files>
    frontend/src/features/technician/components/TechnicianQueueTable.tsx,
    frontend/src/features/technician/components/TechnicianActionMenu.tsx,
    frontend/src/features/technician/components/RedFlagDialog.tsx,
    frontend/src/features/technician/components/ReturnToQueueDialog.tsx,
    frontend/src/features/technician/components/PausePatientDialog.tsx,
    frontend/src/features/technician/components/TechnicianDashboard.tsx,
    frontend/src/app/routes/_authenticated/dashboard.tsx
  </files>
  <read_first>
    frontend/src/features/receptionist/components/PatientQueueTable.tsx,
    frontend/src/features/receptionist/components/RowActionMenu.tsx,
    frontend/src/features/receptionist/components/ReceptionistDashboard.tsx,
    frontend/src/app/routes/_authenticated/dashboard.tsx,
    frontend/src/features/technician/types/technician.types.ts,
    frontend/src/features/technician/api/technician-api.ts,
    frontend/src/shared/i18n/locales/vi/technician.json,
    docs/dev/technician/technician-dashboard.md
  </read_first>
  <action>
    1. Create `TechnicianQueueTable.tsx`:
       - Props: `rows: TechnicianDashboardRow[]`, `isLoading: boolean`, `onAction: (action, row) => void`
       - Use TanStack Table with column definitions for 9 columns per UI spec:
         - `#` (32px): Row index, `accessorFn: (_, index) => index + 1`
         - `Ho ten` (22%): PatientName. If red flag: text color `var(--tech-action-destructive)` (#A32D2D). Font weight 400 regular.
         - `Sinh` (46px): BirthYear (4 digits)
         - `Check-in` (60px): Format `checkinTime` as HH:mm (use `new Date(row.checkinTime).toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit' })`)
         - `Cho` (54px): WaitMinutes + "p" suffix. Color: `var(--tech-wait-default)` (#BA7517) normally, `var(--tech-wait-urgent)` (#A32D2D) if >= 25 minutes
         - `Ly do kham` (auto): Reason with text-ellipsis. If red flag: text red, show RedFlagReason
         - `Loai` (54px): VisitType pill. "Moi"/"Tai kham"/"Do bo sung" as small bordered pill (text-xs, border, rounded-full, px-2 py-0.5)
         - `Trang thai` (82px): `<TechnicianStatusBadge status={row.status} />`
         - Actions (40px): `<TechnicianActionMenu row={row} onAction={onAction} />`
       - Row sorting: Data should already come sorted from backend (in_progress pinned, then FIFO). No client-side re-sort needed.
       - Row styling:
         - "in_progress" row: visually pinned (already first from backend sort)
         - "completed" rows: `opacity: 0.55`
         - Add `className` conditionally: `row.status === "completed" ? "opacity-55" : ""`
       - Loading: 5 Skeleton rows
       - Client-side wait time update: Use `setInterval` every 60s per D-17. Increment `waitMinutes` by 1 every 60s without refetching (update local state).
       - Empty state: Show empty message from i18n when rows.length === 0

    2. Create `TechnicianActionMenu.tsx`:
       - Props: `row: TechnicianDashboardRow`, `onAction: (action: string, row: TechnicianDashboardRow) => void`
       - Uses shadcn `DropdownMenu` with vertical 3-dot trigger (`IconDotsVertical`)
       - Actions per status (per UI spec):
         - `waiting`: "Nhan BN" (text `var(--tech-action-primary)`, font-bold), separator, "Xem ket qua"
         - `in_progress`: "Tiep tuc do" (text `var(--tech-action-primary)`, font-bold), "Hoan tat chuyen BS" (normal + check icon), "Tra lai hang doi" (normal), separator, "Chuyen BS ngay" (text `var(--tech-action-destructive)`, destructive), separator, "Xem ket qua"
         - `completed`: "Xem ket qua" only
         - `red_flag`: "Xem ket qua" only
       - Each item calls `onAction(actionName, row)`

    3. Create `RedFlagDialog.tsx`:
       - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `patientName: string`, `onConfirm: (reason: string) => void`, `isLoading: boolean`
       - Uses shadcn `AlertDialog`
       - Content: Dropdown/select for reason (4 options from i18n `dialog.redFlagReasons`):
         - "Dau mat nhieu", "Giam thi luc dot ngot", "Trieu chung lech 1 ben ro", "Khac"
       - When "Khac" selected: show text input for custom reason
       - Validation with React Hook Form + Zod: reason required; if "other" selected, custom text required
       - Confirm: "Chuyen BS ngay" button (destructive red), calls `onConfirm(selectedReason)`
       - Cancel: "Huy" button

    4. Create `ReturnToQueueDialog.tsx`:
       - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `patientName: string`, `onConfirm: () => void`, `isLoading: boolean`
       - Uses shadcn `AlertDialog`
       - Message: "Tra BN {ten} ve hang doi? Du lieu da nhap se duoc giu nguyen." (from i18n with {{name}} interpolation)
       - Confirm: "Xac nhan", Cancel: "Huy"

    5. Create `PausePatientDialog.tsx`:
       - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `currentPatientName: string`, `onConfirm: () => void`, `isLoading: boolean`
       - Uses shadcn `AlertDialog`
       - Message: "Ban dang do BN {ten}. Muon tam dung va nhan BN moi?" (from i18n)
       - Confirm: "Tam dung & nhan BN moi", Cancel: "Huy"

    6. Create `TechnicianDashboard.tsx`:
       - Main container component that orchestrates all sub-components
       - State: `filter` (TechnicianStatus | undefined), `search` (string), `page` (number), `pageSize` (number)
       - Uses hooks: `useTechnicianDashboard(filters)`, `useTechnicianKpi()`
       - Derive `currentPatient`: find row where `status === "in_progress"` (current technician's patient)
       - Compute filter counts from data or KPI
       - Dialog state management: `redFlagOpen`, `returnOpen`, `pauseOpen`, `selectedRow`
       - Action handler function:
         - "accept": If `currentPatient` exists -> show PausePatientDialog. If not -> call `acceptOrder.mutate(row.orderId)` then navigate to `/pre-exam`
         - "continue": Navigate to `/pre-exam`
         - "complete": Call `completeOrder.mutate(row.orderId)`, show success toast via Sonner
         - "returnToQueue": Open ReturnToQueueDialog
         - "redFlag": Open RedFlagDialog
         - "viewResults": Open slide-over panel (Plan 05)
       - Error handling: On accept mutation error, check for already-accepted error, show toast "BN da duoc nhan boi {ten}"
       - Toast notifications via Sonner: success/error/info messages from i18n
       - Layout top-to-bottom: Page title (h1, 22px bold) -> KPI cards -> Banner (conditional) -> Toolbar -> Table
       - Spacing: 24px gaps between sections (`space-y-6`)

    7. Update `dashboard.tsx` to add Technician role check:
       Add BEFORE the existing `isReceptionist` check (or after, order is: check Technician first since both might have Clinical.View):
       ```typescript
       import { TechnicianDashboard } from "@/features/technician/components/TechnicianDashboard"

       // In DashboardPage function, AFTER existing user extraction:
       const isTechnician = user?.roles?.includes("Technician") ?? false
       if (isTechnician) return <TechnicianDashboard />

       // Keep existing receptionist check below
       const isReceptionist = user?.roles?.includes("Receptionist") ?? false
       if (isReceptionist) return <ReceptionistDashboard />
       ```
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TechnicianQueueTable.tsx contains TanStack Table column definitions for all 9 columns (index, name, birthYear, checkin, wait, reason, type, status, actions)
    - TechnicianQueueTable.tsx contains `opacity-55` for completed rows and `var(--tech-wait-urgent)` for wait >= 25min
    - TechnicianActionMenu.tsx contains `DropdownMenu` with conditional items based on `row.status`
    - RedFlagDialog.tsx contains `AlertDialog` with 4 reason options and Zod validation
    - ReturnToQueueDialog.tsx contains `AlertDialog` with interpolated patient name
    - PausePatientDialog.tsx contains `AlertDialog` for pause confirmation
    - TechnicianDashboard.tsx contains `useTechnicianDashboard`, `useTechnicianKpi`, toast notifications via Sonner, and orchestrates all sub-components
    - dashboard.tsx contains `user?.roles?.includes("Technician")` and imports `TechnicianDashboard`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Complete dashboard UI with table, action menus, all dialogs, and route wiring. Technician role users see their dashboard at /dashboard.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Dashboard route renders TechnicianDashboard for Technician role
- All 11 component files exist in `frontend/src/features/technician/components/`
- All components use i18n for text and CSS variables for colors
</verification>

<success_criteria>
- All dashboard UI components implemented per UI spec and mockup
- Filter pills work with counts, search debounces at 300ms
- Action menus show correct options per status
- Dialogs use proper shadcn components with validation
- Route wiring renders correct dashboard per role
- Wait time updates client-side every 60s
- Completed rows dimmed, in-progress pinned to top
</success_criteria>

<output>
After completion, create `.planning/phases/15-implement-technician-dashboard-according-to-the-requirement-and-mockup/15-04-SUMMARY.md`
</output>
