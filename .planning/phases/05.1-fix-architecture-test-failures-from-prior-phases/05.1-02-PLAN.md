---
phase: 05.1-fix-architecture-test-failures-from-prior-phases
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs
  - backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs
  - backend/src/Modules/Patient/Patient.Application/Features/ValidatePatientFields.cs
  - backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs
autonomous: true
requirements: [ARCH-04, ARCH-05]

must_haves:
  truths:
    - "PatientFieldValidationResult and MissingFieldInfo records live in Patient.Contracts.Dtos namespace"
    - "Patient.Presentation no longer has using Patient.Domain.Services"
    - "Patient.Presentation no longer has using Patient.Domain.Enums"
    - "PatientFieldValidator in Domain still returns PatientFieldValidationResult correctly"
    - "ValidatePatientFields handler compiles with new namespace"
  artifacts:
    - path: "backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs"
      provides: "PatientFieldValidationResult and MissingFieldInfo records"
      contains: "namespace Patient.Contracts.Dtos"
    - path: "backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs"
      provides: "Validator without inline record definitions"
      contains: "using Patient.Contracts.Dtos"
  key_links:
    - from: "backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs"
      to: "backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs"
      via: "using Patient.Contracts.Dtos"
      pattern: "using Patient\\.Contracts\\.Dtos"
    - from: "backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs"
      to: "backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs"
      via: "using Patient.Contracts.Dtos (already present)"
      pattern: "using Patient\\.Contracts\\.Dtos"
---

<objective>
Move PatientFieldValidationResult and MissingFieldInfo records from Patient.Domain.Services to Patient.Contracts.Dtos, and clean up Patient.Presentation using directives to remove Domain layer dependencies.

Purpose: Fix the `Presentation_Should_Not_Depend_On_Domain_Directly` arch test failure (PatientApiEndpoints references Patient.Domain.Services) and ensure DTO-like result types live in Contracts where they belong.
Output: PatientFieldValidationResult.cs in Contracts, cleaned PatientFieldValidator.cs, cleaned PatientApiEndpoints.cs
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-fix-architecture-test-failures-from-prior-phases/05.1-CONTEXT.md

<interfaces>
<!-- Current PatientFieldValidator.cs defines these records inline: -->
```csharp
// Patient.Domain.Services.PatientFieldValidator.cs (lines 47-56):
public record PatientFieldValidationResult(
    bool IsValid,
    List<MissingFieldInfo> MissingFields);

public record MissingFieldInfo(
    string FieldName,
    string RequiredForContext);
```

<!-- Current PatientApiEndpoints.cs using directives: -->
```csharp
using Patient.Domain.Enums;    // line 6 -- REMOVE (unused after analysis)
using Patient.Domain.Services;  // line 7 -- REMOVE (PatientFieldValidationResult moves to Contracts)
```

<!-- ValidatePatientFields.cs current imports: -->
```csharp
using Patient.Domain.Enums;    // for FieldRequirementContext (Domain enum, stays)
using Patient.Domain.Services;  // for PatientFieldValidationResult -- CHANGE to Patient.Contracts.Dtos
```

<!-- Patient.Domain.csproj needs to reference Patient.Contracts for the using. Check if it already does. If not, add a project reference. IMPORTANT: Domain referencing Contracts is acceptable per this project's architecture (Domain -> Contracts dependency is allowed since Contracts is a lightweight DTO project). However, verify this doesn't violate any arch test. If it does, keep PatientFieldValidationResult in Domain.Services but move it to a separate file in Domain. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move PatientFieldValidationResult to Contracts and clean Presentation imports</name>
  <files>
    backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs,
    backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs,
    backend/src/Modules/Patient/Patient.Application/Features/ValidatePatientFields.cs,
    backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs
  </files>
  <action>
    **IMPORTANT: Before making changes, check if Patient.Domain.csproj already references Patient.Contracts:**
    - If NO: Adding `using Patient.Contracts.Dtos` in Domain would require a project reference, which could create a circular dependency (Contracts already references Domain). In this case, do NOT move the records to Contracts. Instead, create a NEW file `Patient.Domain/Services/PatientFieldValidationResult.cs` and move just the record definitions there (keeping them in Domain.Services namespace). Then the Presentation fix becomes: remove `using Patient.Domain.Services` and add `using Patient.Domain.Services` only if needed -- wait, that doesn't fix the arch test.

    **REVISED APPROACH (handles circular dependency correctly):**

    The arch test `Presentation_Should_Not_Depend_On_Domain_Directly` flags Presentation -> Domain references. The fix per user decision is to move PatientFieldValidationResult to Patient.Contracts. But Domain.Services.PatientFieldValidator returns this type, creating a Domain -> Contracts dependency.

    Check Patient.Domain.csproj for existing Contracts reference:
    - If it exists: proceed with move to Contracts.Dtos
    - If it does NOT exist: Patient.Domain cannot reference Patient.Contracts (would create circular: Domain -> Contracts -> Domain). In this case:
      a. Keep PatientFieldValidationResult and MissingFieldInfo in Patient.Domain but move to a NEW namespace `Patient.Domain.ValueObjects` (same pattern as FieldChange fix in Plan 01)
      b. This avoids the `Presentation_Should_Not_Depend_On_Domain_Directly` test failure IF the test only checks specific namespaces. Check the arch test rule.
      c. If the arch test checks ANY Patient.Domain namespace reference from Presentation, then the correct fix is: PatientApiEndpoints already uses `bus.InvokeAsync<Result<PatientFieldValidationResult>>` -- the generic type parameter requires the type. Move the result type to Contracts, and have Domain.Services.PatientFieldValidator return it by having Patient.Domain reference Patient.Contracts. BUT check circular dependency first.

    **Most likely path (based on research):** Patient.Contracts references Patient.Domain (confirmed in csproj). So Domain -> Contracts would be circular. The correct approach per the user's decision is:
    1. Move records to Patient.Contracts.Dtos
    2. Remove Patient.Domain -> Patient.Contracts reference (it doesn't exist, so no issue)
    3. Add Patient.Contracts -> remove Patient.Domain reference (this is the existing reference being removed in Plan 03)
    4. Since PatientFieldValidator in Domain needs to return PatientFieldValidationResult, and the type moves to Contracts, Domain would need a Contracts reference. This IS circular.

    **FINAL APPROACH:**
    - Since Patient.Contracts currently references Patient.Domain (for enums), and we're removing that reference in Plan 03, the circular dependency issue resolves itself. HOWEVER, Domain referencing Contracts is an architecture violation itself (Domain should be independent).
    - **Best solution:** Define the records in BOTH places temporarily? No, that's messy.
    - **Actual best solution:** The PatientFieldValidator in Domain returns the result type. The Application handler uses the result type. The Presentation endpoint uses the result type via bus.InvokeAsync generic parameter. The fix is:
      a. Create PatientFieldValidationResult in Patient.Contracts.Dtos
      b. In Patient.Domain.Services.PatientFieldValidator: define a SEPARATE internal result type that the validator returns
      c. In Patient.Application.Features.ValidatePatientFields: map from Domain result to Contracts result

    **SIMPLEST CORRECT SOLUTION (per user decision + avoiding circular deps):**
    1. Create `Patient.Contracts.Dtos.PatientFieldValidationResult.cs` with both records
    2. KEEP the same records in `Patient.Domain.Services.PatientFieldValidator.cs` but make them internal/private to the validator -- NO, they're return types.
    3. Actually: Remove records from PatientFieldValidator.cs. Add `Patient.Contracts` project reference to `Patient.Domain.csproj`. This creates Domain -> Contracts direction. After Plan 03 removes the Contracts -> Domain reference, there's no cycle. Domain -> Contracts is unusual but acceptable for a shared DTO type.

    **BUT**: Domain -> Contracts violates clean architecture (Domain should have NO outward dependencies). The arch tests likely enforce this.

    **TRULY FINAL APPROACH:** Keep PatientFieldValidationResult in Patient.Domain but in a different namespace that doesn't trigger the Presentation arch test. Check what the arch test actually tests -- it likely checks that Presentation doesn't reference `*.Domain` assemblies, not specific namespaces. So moving within Domain won't help.

    **The user's decision says:** "Move PatientFieldValidationResult out of Patient.Domain.Services to Patient.Contracts". Follow this exactly. The circular dependency concern: Patient.Contracts CURRENTLY references Patient.Domain for enums. Plan 03 removes that reference. Once both Plan 02 and Plan 03 complete, there's no cycle. During Plan 02 execution (before Plan 03), Patient.Domain will reference Patient.Contracts AND Patient.Contracts references Patient.Domain = temporary cycle. The .NET build system handles circular project references as an error. So Plan 02 and Plan 03 must be coordinated.

    **EXECUTION ORDER SOLUTION:** Do both changes atomically:
    1. Remove the records from PatientFieldValidator.cs
    2. Create PatientFieldValidationResult.cs in Patient.Contracts.Dtos
    3. Do NOT add Patient.Contracts reference to Patient.Domain.csproj
    4. Instead, have PatientFieldValidator return a simple tuple or custom type that stays in Domain
    5. Map to PatientFieldValidationResult in the Application handler

    **CONCRETE IMPLEMENTATION:**

    Step 1: Create `backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs`:
    ```csharp
    namespace Patient.Contracts.Dtos;

    public record PatientFieldValidationResult(
        bool IsValid,
        List<MissingFieldInfo> MissingFields);

    public record MissingFieldInfo(
        string FieldName,
        string RequiredForContext);
    ```

    Step 2: In `Patient.Domain.Services.PatientFieldValidator.cs`:
    - Remove PatientFieldValidationResult and MissingFieldInfo record definitions
    - Change return type to `(bool IsValid, List<(string FieldName, string RequiredForContext)> MissingFields)`
    - Update internal logic to return the tuple

    Step 3: In `Patient.Application.Features.ValidatePatientFields.cs`:
    - Remove `using Patient.Domain.Services` (no longer needed for result type)
    - Add mapping from Domain tuple to Contracts.Dtos.PatientFieldValidationResult
    - Keep `using Patient.Domain.Services` only if PatientFieldValidator class reference needs it, or use fully-qualified name

    Step 4: In `Patient.Presentation.PatientApiEndpoints.cs`:
    - Remove `using Patient.Domain.Services;` (line 7)
    - Remove `using Patient.Domain.Enums;` (line 6) -- verify no other code in this file uses Domain.Enums types. The `GetPatientListQuery` uses `Gender?` but that type comes through Application layer; Presentation doesn't need the explicit using since it passes the query object, not constructing it with Gender directly. Verify by attempting build.
    - `using Patient.Contracts.Dtos;` is already present (line 5) and covers PatientFieldValidationResult
  </action>
  <verify>
    <automated>cd /d/projects/ganka && dotnet build backend/src/Modules/Patient/ --no-restore -v quiet && dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore --filter "Presentation_Should_Not_Depend_On_Domain_Directly" -v normal</automated>
  </verify>
  <done>
    - PatientFieldValidationResult and MissingFieldInfo in Patient.Contracts.Dtos
    - PatientFieldValidator returns tuple, Application handler maps to Contracts DTO
    - PatientApiEndpoints has no using Patient.Domain.* directives
    - Arch test `Presentation_Should_Not_Depend_On_Domain_Directly` passes for Patient module
    - Solution builds cleanly
  </done>
</task>

</tasks>

<verification>
- `dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore --filter "Presentation_Should_Not_Depend_On_Domain_Directly"` passes
- `dotnet build backend/ --no-restore -v quiet` succeeds
</verification>

<success_criteria>
- PatientFieldValidationResult lives in Patient.Contracts.Dtos namespace
- Patient.Presentation.PatientApiEndpoints has zero `using Patient.Domain.*` directives
- PatientFieldValidator still validates correctly (tuple return mapped in Application layer)
- No circular project references introduced
- Architecture test passes
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-fix-architecture-test-failures-from-prior-phases/05.1-02-SUMMARY.md`
</output>
