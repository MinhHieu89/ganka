---
phase: 05.1-fix-architecture-test-failures-from-prior-phases
plan: 07
type: execute
wave: 4
depends_on: [01, 02, 05, 06]
files_modified: []
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04, ARCH-05]

must_haves:
  truths:
    - "All 55 architecture tests pass (50 previously passing + 5 fixed)"
    - "Full solution builds with zero errors and zero warnings related to the changes"
    - "All existing unit tests still pass"
    - "No circular project references exist"
  artifacts: []
  key_links: []
---

<objective>
Run comprehensive verification of all architecture test fixes across the entire solution.

Purpose: Confirm all 5 arch test failures are fixed, no regressions introduced, and the full solution builds and tests cleanly. This is the phase gate.
Output: Verified passing test suite, phase completion confirmation
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-fix-architecture-test-failures-from-prior-phases/05.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full architecture test verification</name>
  <files></files>
  <action>
    Run the complete architecture test suite and verify all 5 previously failing tests now pass:

    1. Run all architecture tests:
    ```bash
    dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v normal
    ```

    2. Verify each specific fixed test passes:
    - `Contracts_Should_Be_Independent` (was failing for Patient.Contracts -> Patient.Domain)
    - `Contracts_Should_Not_Reference_Module_Internals` (was failing for Patient.Contracts -> Patient.Domain.Enums)
    - `Application_Should_Not_Depend_On_Infrastructure` (was failing for Clinical.Application -> Shared.Infrastructure)
    - `All_Domain_Entities_Should_Have_Private_Setters` (was failing for FieldChange record in Entities namespace)
    - `Presentation_Should_Not_Depend_On_Domain_Directly` (was failing for Patient.Presentation -> Patient.Domain.Services)

    3. Run the full backend test suite to check for regressions:
    ```bash
    dotnet test backend/ --no-restore -v normal
    ```

    4. If ANY test fails:
    - Diagnose the root cause
    - Fix it inline (minor fixes only)
    - If the fix requires changes beyond this plan's scope, document and report

    5. Verify no circular references by building the full solution:
    ```bash
    dotnet build backend/ --no-restore -v quiet
    ```

    6. Spot-check that the Pharmacy module's existing int-normalization pattern still passes arch tests (it should, since we didn't touch Pharmacy.Contracts)
  </action>
  <verify>
    <automated>cd /d/projects/ganka && dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v normal && dotnet test backend/ --no-restore -v normal</automated>
  </verify>
  <done>
    - All 55 architecture tests pass (0 failures)
    - All unit tests pass
    - Full solution builds cleanly
    - No circular project references
    - Phase 05.1 objective achieved: all 5 arch test failures fixed
  </done>
</task>

</tasks>

<verification>
- `dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v normal` shows all tests passed
- `dotnet test backend/ --no-restore -v normal` shows all tests passed
- `dotnet build backend/ --no-restore -v quiet` succeeds with no errors
</verification>

<success_criteria>
- 55/55 architecture tests pass
- All existing unit tests pass (zero regressions)
- Full solution builds cleanly
- 5 specific test failures from CONTEXT.md are confirmed fixed
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-fix-architecture-test-failures-from-prior-phases/05.1-07-SUMMARY.md`
</output>
