---
phase: 07-billing-finance
plan: 24
type: execute
wave: 12
depends_on: ["07-22", "07-23"]
files_modified: []
autonomous: false

requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
  - FIN-06
  - FIN-07
  - FIN-08
  - FIN-09
  - FIN-10

must_haves:
  truths:
    - "Full billing workflow verifiable end-to-end"
    - "All backend tests pass"
    - "Frontend compiles without errors"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for the complete billing and finance module.

Purpose: Verify all FIN requirements work together in a running system before marking the phase complete.
Output: Verified billing module
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification suite</name>
  <files></files>
  <action>
    Run the following automated checks:

    1. Full backend build: dotnet build backend/Ganka28.slnx -v q
    2. All billing unit tests: dotnet test backend/tests/Billing.Unit.Tests -v q
    3. Full test suite: dotnet test backend/Ganka28.slnx -v q
    4. Frontend type check: cd frontend && npx tsc --noEmit
    5. Verify billing API endpoints accessible:
       - Start backend on port 5255
       - curl GET /api/billing/shifts/current (should return 401 without auth)
       - curl GET /api/billing/shifts/templates (should return 401)

    Fix any compilation or test failures before proceeding to human verification.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Billing.Unit.Tests -v q && cd D:/projects/ganka/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>All automated checks pass -- backend builds, tests green, frontend compiles</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete billing and finance module: invoices with department grouping, multi-method payments, manager-approved discounts, refund processing, shift management with cash reconciliation, e-invoice export, and all printable documents.</what-built>
  <how-to-verify>
    Start backend (port 5255) and frontend (port 3000). Login with Admin@ganka28.com / Admin@123456.

    1. **Shift Management** (FIN-10):
       - Navigate to Billing > Shifts
       - Open a new shift with Morning template and 1,000,000 VND opening balance
       - Verify shift status shows as Open

    2. **Invoice Flow** (FIN-01, FIN-02):
       - Create a visit for a patient (or use existing)
       - Navigate to Billing dashboard
       - Find/create an invoice for the visit
       - Verify line items are grouped by department (Kham benh, Duoc pham, etc.)
       - Verify VND amounts formatted correctly (dot separator, no decimals)

    3. **Payment** (FIN-03):
       - Click "Thu tien" (Collect Payment)
       - Try cash payment -- verify amount and shift totals update
       - Try adding a second payment with different method (bank transfer)
       - Verify split payment across methods works

    4. **Discount** (FIN-07):
       - Click "Giam gia" (Apply Discount) on a draft invoice
       - Try percentage discount (10%) -- verify calculated amount
       - Enter manager PIN to approve
       - Verify invoice total recalculates

    5. **Finalize + E-Invoice** (FIN-04):
       - When invoice is fully paid, click "Hoan tat" (Finalize)
       - Click "Xuat hoa don dien tu" (Export E-Invoice)
       - Download PDF -- verify it has all required Vietnamese fields
       - Download JSON -- verify structure suitable for MISA import

    6. **Print** (PRT-03):
       - Click "In hoa don" -- verify PDF opens with department grouping
       - Click "In phieu thu" -- verify receipt PDF

    7. **Shift Close** (FIN-10):
       - Navigate to Shifts page
       - Click "Dong ca" (Close Shift)
       - Enter cash count, verify discrepancy calculation
       - Close shift, verify report shows revenue by method

    8. **Bilingual** (UI-01):
       - Switch language to English
       - Verify all billing UI text translates correctly
       - Switch back to Vietnamese
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All automated tests pass. Human verification of full billing workflow confirms FIN-01 through FIN-10.
</verification>

<success_criteria>
- Full billing workflow works end-to-end
- All 10 FIN requirements verifiable
- PRT-03 (invoice printing) working
- Bilingual support confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-24-SUMMARY.md`
</output>
