---
phase: 07-billing-finance
plan: 18
type: execute
wave: 8
depends_on: ["07-17"]
files_modified:
  - frontend/src/app/routes/_authenticated/billing/index.tsx
  - frontend/src/app/routes/_authenticated/billing/invoices.$invoiceId.tsx
  - frontend/src/features/billing/components/InvoiceLineItemsTable.tsx
  - frontend/src/features/billing/components/InvoiceView.tsx
autonomous: true
requirements:
  - FIN-01
  - FIN-02

must_haves:
  truths:
    - "Cashier dashboard shows pending invoices and current shift status"
    - "Invoice detail page displays line items grouped by department"
    - "VND amounts displayed with proper Vietnamese formatting"
  artifacts:
    - path: "frontend/src/app/routes/_authenticated/billing/index.tsx"
      provides: "Cashier dashboard with pending invoices list and shift status"
      contains: "function BillingDashboard"
    - path: "frontend/src/features/billing/components/InvoiceView.tsx"
      provides: "Invoice detail view with line items, payments, discounts, balance"
      contains: "InvoiceView"
  key_links:
    - from: "billing/index.tsx"
      to: "billing-api.ts"
      via: "useVisitInvoice, useCurrentShift hooks"
      pattern: "useVisitInvoice|useCurrentShift"
---

<objective>
Create the cashier billing dashboard and invoice detail page with line items grouped by department and VND formatting.

Purpose: Primary UI for cashiers to view pending invoices, see accumulated charges, and navigate to payment/checkout flow.
Output: 2 route pages + 2 components
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-17-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InvoiceView and InvoiceLineItemsTable components</name>
  <files>
    frontend/src/features/billing/components/InvoiceView.tsx,
    frontend/src/features/billing/components/InvoiceLineItemsTable.tsx
  </files>
  <action>
    Create features/billing/components/ directory.

    1. **InvoiceLineItemsTable.tsx**: Component that renders invoice line items grouped by department:
       - Props: lineItems (InvoiceLineItemDto[])
       - Group items by Department field (0=Medical, 1=Pharmacy, 2=Optical, 3=Treatment)
       - Render section headers: "KHAM BENH" (Medical), "DUOC PHAM" (Pharmacy), "KINH" (Optical), "DIEU TRI" (Treatment)
       - Only show sections that have items
       - Use DataTable or simple table with columns: #, Description (Vietnamese if available, else English), Qty, Unit Price, Total
       - VND formatting using formatVND for all price columns
       - Subtotal per section and grand total at bottom
       - Use shadcn/ui Table component

    2. **InvoiceView.tsx**: Main invoice detail component:
       - Props: invoiceId (string)
       - Uses useInvoice(invoiceId) hook
       - Layout:
         - Header: Invoice number (HD-2026-XXXXX), Status badge (Draft=yellow, Finalized=green, Voided=red)
         - Patient info: Name
         - InvoiceLineItemsTable with grouped items
         - Summary section: SubTotal, Discount (if any), Total Amount, Paid Amount, Balance Due
         - Balance Due highlighted in red when > 0, green when fully paid
         - Payments list: method icon + amount + reference + date for each payment
         - Discounts list: type + value + status (Pending/Approved/Rejected)
         - Action buttons area (for payment, discount, finalize -- implemented in later plans)
       - Use shadcn/ui Card, Badge, Separator components
       - Loading skeleton while data fetches
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>InvoiceView and InvoiceLineItemsTable components compile with department grouping</done>
</task>

<task type="auto">
  <name>Task 2: Create billing route pages</name>
  <files>
    frontend/src/app/routes/_authenticated/billing/index.tsx,
    frontend/src/app/routes/_authenticated/billing/invoices.$invoiceId.tsx
  </files>
  <action>
    Create the billing route directory: app/routes/_authenticated/billing/

    1. **billing/index.tsx**: Cashier dashboard page:
       - TanStack Router route: createFileRoute("/_authenticated/billing/")
       - Layout: Two-column on desktop (invoices list left, shift status right), single column on mobile
       - Left panel: "Hoa don cho xu ly" (Pending invoices) -- list of Draft invoices
         - Use DataTable or Card list with InvoiceSummaryDto
         - Show: Invoice#, Patient Name, Total Amount, Created time
         - Click navigates to invoice detail
         - Empty state: "Khong co hoa don nao dang cho" (No pending invoices)
       - Right panel: Current shift status card
         - If no open shift: "Chua mo ca" with "Mo ca" (Open shift) button
         - If open shift: Show CashierName, OpenedAt, TotalRevenue, TransactionCount
         - "Dong ca" (Close shift) button
       - Uses useCurrentShift() and a pending invoices query
       - Page title via i18n: "Thanh toan" (Billing)

    2. **billing/invoices.$invoiceId.tsx**: Invoice detail page:
       - TanStack Router route with $invoiceId param
       - Renders InvoiceView component
       - Breadcrumb: Billing > Invoice #HD-2026-XXXXX
       - Back button to billing dashboard
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Billing dashboard and invoice detail routes render with proper data</done>
</task>

</tasks>

<verification>
cd D:/projects/ganka/frontend && npx tsc --noEmit
</verification>

<success_criteria>
- Cashier dashboard shows pending invoices and current shift status
- Invoice detail displays line items grouped by Vietnamese department names
- VND amounts formatted correctly throughout
- Route navigation works between dashboard and invoice detail
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-18-SUMMARY.md`
</output>
