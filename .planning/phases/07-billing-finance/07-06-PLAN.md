---
phase: 07-billing-finance
plan: 06
type: execute
wave: 2
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - backend/src/Modules/Billing/Billing.Contracts/Dtos/InvoiceDto.cs
  - backend/src/Modules/Billing/Billing.Contracts/Dtos/PaymentDto.cs
  - backend/src/Modules/Billing/Billing.Contracts/Dtos/CashierShiftDto.cs
  - backend/src/Modules/Billing/Billing.Contracts/Dtos/EInvoiceExportDto.cs
  - backend/src/Modules/Billing/Billing.Contracts/Queries/GetVisitInvoiceQuery.cs
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-10

must_haves:
  truths:
    - "All DTOs use int for enum values (Contracts has no Domain reference)"
    - "InvoiceDto includes line items grouped by department for display"
    - "EInvoiceExportDto contains all fields required by Vietnamese e-invoice regulations"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Contracts/Dtos/InvoiceDto.cs"
      provides: "InvoiceDto, InvoiceLineItemDto, DiscountDto, RefundDto sealed records"
      contains: "sealed record InvoiceDto"
    - path: "backend/src/Modules/Billing/Billing.Contracts/Dtos/EInvoiceExportDto.cs"
      provides: "E-invoice export DTO with all Vietnamese tax law required fields"
      contains: "sealed record EInvoiceExportDto"
  key_links:
    - from: "Billing.Contracts"
      to: "Billing.Application"
      via: "Handler return types and cross-module queries"
      pattern: "InvoiceDto|PaymentDto|CashierShiftDto"
---

<objective>
Create all Billing.Contracts DTOs and cross-module query records. These DTOs define the API contract for invoice, payment, shift, and e-invoice export data.

Purpose: Define the data transfer contracts that Application handlers return and Presentation endpoints expose. Contracts project has no Domain reference -- enums are represented as int.
Output: All Billing DTOs and query records in Billing.Contracts
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Invoice, Payment, and Shift DTOs</name>
  <files>
    backend/src/Modules/Billing/Billing.Contracts/Dtos/InvoiceDto.cs,
    backend/src/Modules/Billing/Billing.Contracts/Dtos/PaymentDto.cs,
    backend/src/Modules/Billing/Billing.Contracts/Dtos/CashierShiftDto.cs
  </files>
  <action>
    Create Dtos/ directory in Billing.Contracts.

    1. **InvoiceDto.cs** (multiple records in one file for cohesion, matching Phase 5 pattern):
       - InvoiceDto: sealed record with Id (Guid), InvoiceNumber (string), VisitId (Guid?), PatientId (Guid), PatientName (string), Status (int), SubTotal (decimal), DiscountTotal (decimal), TotalAmount (decimal), PaidAmount (decimal), BalanceDue (decimal), CashierShiftId (Guid?), FinalizedById (Guid?), FinalizedAt (DateTime?), CreatedAt (DateTime), LineItems (List of InvoiceLineItemDto), Payments (List of PaymentDto), Discounts (List of DiscountDto), Refunds (List of RefundDto)
       - InvoiceLineItemDto: sealed record with Id (Guid), Description (string), DescriptionVi (string?), UnitPrice (decimal), Quantity (int), LineTotal (decimal), Department (int), SourceId (Guid?), SourceType (string?)
       - DiscountDto: sealed record with Id (Guid), InvoiceLineItemId (Guid?), Type (int), Value (decimal), CalculatedAmount (decimal), Reason (string), ApprovalStatus (int), RequestedById (Guid), RequestedAt (DateTime), ApprovedById (Guid?), ApprovedAt (DateTime?)
       - RefundDto: sealed record with Id (Guid), InvoiceLineItemId (Guid?), Amount (decimal), Reason (string), Status (int), RequestedById (Guid), RequestedAt (DateTime), ApprovedById (Guid?), ApprovedAt (DateTime?), ProcessedById (Guid?), ProcessedAt (DateTime?)
       - InvoiceSummaryDto: sealed record with Id (Guid), InvoiceNumber (string), PatientName (string), Status (int), TotalAmount (decimal), PaidAmount (decimal), BalanceDue (decimal), CreatedAt (DateTime) -- lightweight for list views

    2. **PaymentDto.cs**:
       - PaymentDto: sealed record with Id (Guid), InvoiceId (Guid), Method (int), Amount (decimal), Status (int), ReferenceNumber (string?), CardLast4 (string?), CardType (string?), Notes (string?), RecordedById (Guid), RecordedAt (DateTime), CashierShiftId (Guid?), TreatmentPackageId (Guid?), IsSplitPayment (bool), SplitSequence (int?)

    3. **CashierShiftDto.cs** (multiple records in one file):
       - CashierShiftDto: sealed record with Id (Guid), CashierId (Guid), CashierName (string), ShiftTemplateId (Guid?), Status (int), OpenedAt (DateTime), ClosedAt (DateTime?), OpeningBalance (decimal), ExpectedCashAmount (decimal), CashReceived (decimal), CashRefunds (decimal), ActualCashCount (decimal?), Discrepancy (decimal?), ManagerNote (string?), TotalRevenue (decimal), TransactionCount (int)
       - ShiftReportDto: sealed record with ShiftId (Guid), CashierName (string), OpenedAt (DateTime), ClosedAt (DateTime?), RevenueByMethod (Dictionary of string to decimal), TransactionCount (int), OpeningBalance (decimal), CashReceived (decimal), CashRefunds (decimal), ExpectedCash (decimal), ActualCash (decimal?), Discrepancy (decimal?), ManagerNote (string?)
       - ShiftTemplateDto: sealed record with Id (Guid), Name (string), NameVi (string?), DefaultStartTime (string), DefaultEndTime (string), IsActive (bool)

    All records use int for Domain enums (Contracts has no Domain reference -- follows established pattern from Phase 5).
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Contracts/Billing.Contracts.csproj --no-restore -v q</automated>
  </verify>
  <done>All DTOs compile with int-based enums and proper nullable fields</done>
</task>

<task type="auto">
  <name>Task 2: Create E-Invoice export DTO and cross-module queries</name>
  <files>
    backend/src/Modules/Billing/Billing.Contracts/Dtos/EInvoiceExportDto.cs,
    backend/src/Modules/Billing/Billing.Contracts/Queries/GetVisitInvoiceQuery.cs
  </files>
  <action>
    1. **EInvoiceExportDto.cs**: Sealed record with all Vietnamese e-invoice required fields per Decree 123/2020 and Circular 32/2025:
       - string InvoiceTemplateName (e.g., "HOA DON GIA TRI GIA TANG")
       - string InvoiceTemplateSymbol (Ky hieu mau so hoa don, e.g., "1C26TBB")
       - string InvoiceSymbol (Ky hieu hoa don, e.g., "AA/26E")
       - string InvoiceNumber (So hoa don)
       - DateTime DateOfIssue (Ngay, thang, nam)
       - string SellerName (Ten nguoi ban)
       - string SellerTaxCode (Ma so thue nguoi ban)
       - string SellerAddress (Dia chi nguoi ban)
       - string BuyerName (Ten nguoi mua)
       - string? BuyerTaxCode (Ma so thue nguoi mua -- conditional, required if buyer is business)
       - string? BuyerAddress
       - List of EInvoiceLineItemDto Items
       - decimal PreTaxTotal (Tong tien chua thue)
       - decimal TaxRate (Thue suat -- 8% default, configurable)
       - decimal TaxAmount (Tien thue)
       - decimal TotalAmount (Tong tien thanh toan)
       - string Currency ("VND")
       - string PaymentMethod (Hinh thuc thanh toan)
       - EInvoiceLineItemDto: sealed record with string Description, string? Unit (Don vi tinh), int Quantity, decimal UnitPrice, decimal Amount

    2. **Queries/GetVisitInvoiceQuery.cs**: Create Queries/ directory.
       - GetVisitInvoiceQuery: sealed record with Guid VisitId -- used by other modules to get invoice for a visit
       - GetPendingInvoicesQuery: sealed record with Guid? CashierShiftId -- get unpaid invoices for current shift
       - Also add cross-module query types for charge collection:
         - GetVisitChargesQuery: sealed record with Guid VisitId -- Billing uses this to ask Clinical/Pharmacy/Optical for charges
         - VisitChargeDto: sealed record with string Description, string? DescriptionVi, decimal UnitPrice, int Quantity, int Department, Guid SourceId, string SourceType
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Contracts/Billing.Contracts.csproj --no-restore -v q</automated>
  </verify>
  <done>E-invoice DTO has all Vietnamese tax law required fields. Cross-module query records defined for charge collection.</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Contracts/Billing.Contracts.csproj --no-restore -v q
</verification>

<success_criteria>
- All DTOs use int for enums (no Domain reference)
- EInvoiceExportDto contains all mandatory Vietnamese e-invoice fields
- Cross-module GetVisitChargesQuery and VisitChargeDto defined for charge collection
- All files compile
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-06-SUMMARY.md`
</output>
