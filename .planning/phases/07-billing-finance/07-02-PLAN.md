---
phase: 07-billing-finance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Billing/Billing.Domain/Entities/Payment.cs
  - backend/src/Modules/Billing/Billing.Domain/Entities/Discount.cs
  - backend/src/Modules/Billing/Billing.Domain/Entities/Refund.cs
  - backend/src/Modules/Billing/Billing.Domain/Enums/PaymentMethod.cs
  - backend/src/Modules/Billing/Billing.Domain/Enums/PaymentStatus.cs
autonomous: true
requirements:
  - FIN-03
  - FIN-05
  - FIN-07
  - FIN-08

must_haves:
  truths:
    - "Payment entity supports cash, bank transfer, QR, and card methods"
    - "Discount entity has approval workflow with pending/approved/rejected status"
    - "Refund entity requires manager/owner approval before processing"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Domain/Entities/Payment.cs"
      provides: "Payment entity with method, amount, status, reference number, card details"
      contains: "class Payment : Entity, IAuditable"
    - path: "backend/src/Modules/Billing/Billing.Domain/Entities/Discount.cs"
      provides: "Discount entity with percentage/fixed amount, approval status, manager approval"
      contains: "class Discount : Entity, IAuditable"
    - path: "backend/src/Modules/Billing/Billing.Domain/Entities/Refund.cs"
      provides: "Refund entity with approval workflow and audit trail"
      contains: "class Refund : Entity, IAuditable"
  key_links:
    - from: "Payment.cs"
      to: "Invoice.cs"
      via: "InvoiceId foreign key"
      pattern: "Guid InvoiceId"
---

<objective>
Create Payment, Discount, and Refund domain entities with their supporting enums. These entities handle multi-method payment collection, manager-approved discounts, and refund processing with audit trail.

Purpose: Complete the billing domain model for payment processing, discount approval, and refund workflow.
Output: 3 domain entities + 2 enums in Billing.Domain
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-billing-finance/07-CONTEXT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md

<interfaces>
From Shared.Domain:
```csharp
public abstract class Entity { public Guid Id; public BranchId BranchId; ... }
public interface IAuditable { }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Payment enums</name>
  <files>
    backend/src/Modules/Billing/Billing.Domain/Enums/PaymentMethod.cs,
    backend/src/Modules/Billing/Billing.Domain/Enums/PaymentStatus.cs
  </files>
  <action>
    Create two enum files in Billing.Domain/Enums/:

    1. **PaymentMethod.cs**: Cash = 0, BankTransfer = 1, QrVnPay = 2, QrMomo = 3, QrZaloPay = 4, CardVisa = 5, CardMastercard = 6. Namespace: Billing.Domain.Enums.

    2. **PaymentStatus.cs**: Pending = 0, Confirmed = 1, Refunded = 2. Namespace: Billing.Domain.Enums.

    Also create these additional enums in the same Enums directory:
    - **DiscountType.cs**: Percentage = 0, FixedAmount = 1
    - **ApprovalStatus.cs**: Pending = 0, Approved = 1, Rejected = 2
    - **RefundStatus.cs**: Requested = 0, Approved = 1, Processed = 2, Rejected = 3

    Note: DiscountType, ApprovalStatus, and RefundStatus can be in their own files or combined -- keep them as separate files for clarity. These 3 extra files are small enums that fit within the task scope.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Domain/Billing.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>All 5 enum files compile</done>
</task>

<task type="auto">
  <name>Task 2: Create Payment, Discount, and Refund entities</name>
  <files>
    backend/src/Modules/Billing/Billing.Domain/Entities/Payment.cs,
    backend/src/Modules/Billing/Billing.Domain/Entities/Discount.cs,
    backend/src/Modules/Billing/Billing.Domain/Entities/Refund.cs
  </files>
  <action>
    Create three entity files in Billing.Domain/Entities/:

    1. **Payment.cs**: Entity implementing IAuditable with:
       - Guid InvoiceId, PaymentMethod Method, decimal Amount, PaymentStatus Status (default Pending)
       - string? ReferenceNumber (bank transfer ref, QR transaction ID)
       - string? CardLast4 (for card payments, last 4 digits)
       - string? CardType (Visa, MC as string -- simpler than enum for display)
       - string? Notes
       - Guid RecordedById (cashier who recorded)
       - DateTime RecordedAt
       - Guid? CashierShiftId (links to shift for reconciliation)
       - Guid? TreatmentPackageId (nullable -- for FIN-05 treatment package payments)
       - bool IsSplitPayment (marks 50/50 split payment -- FIN-05)
       - int? SplitSequence (1 = first half, 2 = second half)
       - Private constructor, static Create factory
       - Confirm() method sets Status to Confirmed
       - MarkRefunded() sets Status to Refunded
       - All private setters

    2. **Discount.cs**: Entity implementing IAuditable with:
       - Guid InvoiceId
       - Guid? InvoiceLineItemId (nullable -- applies to specific line item or entire invoice)
       - DiscountType Type (Percentage or FixedAmount)
       - decimal Value (percentage value 0-100 or fixed amount in VND)
       - decimal CalculatedAmount (actual VND amount deducted)
       - string Reason
       - ApprovalStatus ApprovalStatus (default Pending)
       - Guid RequestedById
       - DateTime RequestedAt
       - Guid? ApprovedById
       - DateTime? ApprovedAt
       - string? RejectionReason
       - Private constructor, static Create factory
       - Approve(Guid managerId) and Reject(Guid managerId, string reason) methods
       - CalculateAmount(decimal baseAmount) method -- computes CalculatedAmount from Type and Value

    3. **Refund.cs**: Entity implementing IAuditable with:
       - Guid InvoiceId
       - Guid? InvoiceLineItemId (nullable for partial refunds -- per discretion recommendation)
       - decimal Amount
       - string Reason
       - RefundStatus Status (default Requested)
       - Guid RequestedById
       - DateTime RequestedAt
       - Guid? ApprovedById
       - DateTime? ApprovedAt
       - Guid? ProcessedById
       - DateTime? ProcessedAt
       - string? RejectionReason
       - string? Notes
       - Private constructor, static Create factory
       - Approve(Guid managerId), Reject(Guid managerId, string reason), Process(Guid userId) methods
       - All state transitions validate current status

    All entities use private setters, factory methods, and follow established Entity base pattern.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Domain/Billing.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>Payment, Discount, and Refund entities compile with all domain methods and IAuditable marker</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Domain/Billing.Domain.csproj --no-restore -v q
</verification>

<success_criteria>
- Payment.cs supports all payment methods (cash, bank transfer, QR variants, card)
- Discount.cs has approval workflow (Pending -> Approved/Rejected) with percentage and fixed amount support
- Refund.cs has multi-step approval (Requested -> Approved -> Processed / Rejected) with full audit fields
- All entities implement IAuditable for automatic audit trail via AuditInterceptor
- All files compile
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-02-SUMMARY.md`
</output>
