---
phase: 07-billing-finance
plan: 21
type: execute
wave: 9
depends_on: ["07-18"]
files_modified:
  - frontend/src/app/routes/_authenticated/billing/shifts.tsx
  - frontend/src/features/billing/components/ShiftOpenDialog.tsx
  - frontend/src/features/billing/components/ShiftCloseDialog.tsx
  - frontend/src/features/billing/components/ShiftReportView.tsx
autonomous: true
requirements:
  - FIN-10

must_haves:
  truths:
    - "Cashier can open a shift with opening balance"
    - "Cashier can close a shift with physical cash count and discrepancy display"
    - "Shift report shows revenue by payment method and cash reconciliation"
  artifacts:
    - path: "frontend/src/app/routes/_authenticated/billing/shifts.tsx"
      provides: "Shift management page with history table"
      contains: "ShiftsPage"
    - path: "frontend/src/features/billing/components/ShiftCloseDialog.tsx"
      provides: "Close shift dialog with cash count and discrepancy"
      contains: "ShiftCloseDialog"
  key_links:
    - from: "shifts.tsx"
      to: "shift-api.ts"
      via: "useCurrentShift, useOpenShift, useCloseShift hooks"
      pattern: "useCurrentShift|useOpenShift|useCloseShift"
---

<objective>
Create shift management UI: shift history page, open/close dialogs, and shift report view.

Purpose: Enable cashier shift lifecycle management with cash reconciliation and printable shift reports.
Output: 1 route page + 3 components
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ShiftOpenDialog and ShiftCloseDialog</name>
  <files>
    frontend/src/features/billing/components/ShiftOpenDialog.tsx,
    frontend/src/features/billing/components/ShiftCloseDialog.tsx
  </files>
  <action>
    1. **ShiftOpenDialog.tsx**: Dialog to open a new shift:
       - Props: onSuccess callback, lastClosingBalance (decimal? -- suggested opening balance)
       - Fields:
         - Shift template selector: Select dropdown with ShiftTemplateDto options (Morning/Afternoon). Optional -- can open without template.
         - Opening balance: VND input, default to lastClosingBalance if available
       - Submit calls useOpenShift mutation
       - On success: toast "Da mo ca" (Shift opened), close dialog
       - Validation: opening balance >= 0
       - Use shadcn/ui Dialog, Select, Input, Field+Controller

    2. **ShiftCloseDialog.tsx**: Dialog to close current shift:
       - Props: shiftId (string), expectedCash (decimal), onSuccess callback
       - Shows expected cash amount prominently: "So tien mat du kien: {formatVND(expectedCash)}"
       - Fields:
         - Actual cash count: VND input (required)
         - Manager note: Textarea (optional, shown when discrepancy exists)
       - Live discrepancy calculation: actualCashCount - expectedCash
         - Positive discrepancy: green "Thua {formatVND(amount)}" (Surplus)
         - Negative discrepancy: red "Thieu {formatVND(amount)}" (Deficit)
         - Zero: green "Khop" (Matches)
       - When discrepancy is non-zero, manager note field becomes recommended (yellow highlight)
       - Confirm with AlertDialog: "Xac nhan dong ca? Hanh dong nay khong the hoan tac."
       - Submit calls useCloseShift mutation
       - On success: toast "Da dong ca" (Shift closed), close dialog
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>ShiftOpen and ShiftClose dialogs compile with cash reconciliation UI</done>
</task>

<task type="auto">
  <name>Task 2: Create ShiftReportView and shifts route page</name>
  <files>
    frontend/src/features/billing/components/ShiftReportView.tsx,
    frontend/src/app/routes/_authenticated/billing/shifts.tsx
  </files>
  <action>
    1. **ShiftReportView.tsx**: Shift report display component:
       - Props: shiftId (string)
       - Uses useShiftReport(shiftId) hook
       - Layout:
         - Header: "Bao cao ca lam viec" (Shift Report), Cashier name, date range
         - Revenue by payment method table: Method | Count | Amount (using DataTable or simple table)
         - Total row with grand total
         - Cash reconciliation card:
           - Opening balance
           - + Cash received
           - - Cash refunds
           - = Expected cash
           - Physical count (actual)
           - Discrepancy (color-coded: green=match, red=deficit, blue=surplus)
           - Manager note (if any)
         - Print button: calls getShiftReportPdf, opens in new tab
       - All amounts formatted with formatVND

    2. **billing/shifts.tsx**: Shift management page:
       - Route: createFileRoute("/_authenticated/billing/shifts")
       - Current shift status card at top:
         - If no open shift: ShiftOpenDialog trigger button
         - If open shift: Shift details + ShiftCloseDialog trigger button
       - Shift history table below:
         - Columns: Date, Cashier, Template, Opened, Closed, Revenue, Status
         - Click row expands ShiftReportView inline or navigates to detail
         - DataTable with pagination
         - Filter by date range
       - Uses useCurrentShift(), shift history query
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Shifts page shows current shift status, history table, and shift report detail</done>
</task>

</tasks>

<verification>
cd D:/projects/ganka/frontend && npx tsc --noEmit
</verification>

<success_criteria>
- Open shift with template selection and opening balance
- Close shift with live discrepancy calculation
- Shift report shows revenue by method and cash reconciliation
- Print shift report as PDF
- Shift history with date filtering
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-21-SUMMARY.md`
</output>
