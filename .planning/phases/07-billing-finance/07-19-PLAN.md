---
phase: 07-billing-finance
plan: 19
type: execute
wave: 9
depends_on: ["07-18"]
files_modified:
  - frontend/src/features/billing/components/PaymentForm.tsx
  - frontend/src/features/billing/components/PaymentMethodSelector.tsx
  - frontend/src/features/billing/components/InvoiceView.tsx
autonomous: true
requirements:
  - FIN-03
  - FIN-05

must_haves:
  truths:
    - "Cashier can select from all payment methods (cash, bank transfer, QR variants, card)"
    - "Payment form validates amount against balance due"
    - "Split payment for treatment packages is supported"
  artifacts:
    - path: "frontend/src/features/billing/components/PaymentForm.tsx"
      provides: "Payment recording form with method selection, amount, reference fields"
      contains: "PaymentForm"
    - path: "frontend/src/features/billing/components/PaymentMethodSelector.tsx"
      provides: "Payment method radio/select with method-specific fields"
      contains: "PaymentMethodSelector"
  key_links:
    - from: "PaymentForm.tsx"
      to: "billing-api.ts"
      via: "useRecordPayment mutation"
      pattern: "useRecordPayment"
---

<objective>
Create payment recording form with multi-method support, method-specific fields (card details, QR reference), and treatment package split payment.

Purpose: Enable cashiers to collect payments against invoices via any supported method with proper validation.
Output: PaymentForm, PaymentMethodSelector, updated InvoiceView
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PaymentMethodSelector and PaymentForm</name>
  <files>
    frontend/src/features/billing/components/PaymentMethodSelector.tsx,
    frontend/src/features/billing/components/PaymentForm.tsx
  </files>
  <action>
    1. **PaymentMethodSelector.tsx**: Component for selecting payment method:
       - Props: value (number), onChange (method: number) => void
       - Render RadioGroup or ToggleGroup with icons for each method:
         - Cash (icon: IconCash or IconBanknote)
         - Bank Transfer (icon: IconBuildingBank)
         - QR VNPay (icon: IconQrcode with "VNPay" label)
         - QR MoMo (icon: IconQrcode with "MoMo" label)
         - QR ZaloPay (icon: IconQrcode with "ZaloPay" label)
         - Card Visa (icon: IconCreditCard with "Visa")
         - Card MC (icon: IconCreditCard with "MC")
       - Grid layout: 4 columns on desktop, 2 on mobile
       - Selected method highlighted with primary color
       - Use shadcn/ui ToggleGroup or custom radio card pattern

    2. **PaymentForm.tsx**: Dialog-based payment form:
       - Props: invoiceId (string), balanceDue (decimal), onSuccess callback
       - Uses React Hook Form + Zod validation
       - Fields:
         - PaymentMethodSelector (required)
         - Amount input (number, default: balanceDue, max: balanceDue)
         - Reference Number (shown for BankTransfer, QR methods -- required for those)
         - Card Last 4 (shown for Card methods -- required, exactly 4 digits)
         - Card Type (shown for Card -- auto-detected from method selection)
         - Notes (optional textarea)
         - Treatment package fields (shown conditionally):
           - TreatmentPackageId (if applicable)
           - IsSplitPayment toggle
           - SplitSequence selector (1st payment / 2nd payment)
       - Submit calls useRecordPayment mutation
       - On success: close dialog, show toast "Thanh toan thanh cong" (Payment successful)
       - Format amount display with formatVND
       - Show remaining balance after this payment
       - Use shadcn/ui Dialog, Input, Textarea, Button, Field+Controller pattern
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>PaymentForm and PaymentMethodSelector compile with all payment method support</done>
</task>

<task type="auto">
  <name>Task 2: Integrate PaymentForm into InvoiceView</name>
  <files>frontend/src/features/billing/components/InvoiceView.tsx</files>
  <action>
    Update InvoiceView to add payment functionality:

    1. Add "Thu tien" (Collect Payment) button in the action area -- only visible when BalanceDue > 0 and Status is Draft
    2. Button opens PaymentForm dialog
    3. Add "In hoa don" (Print Invoice) button -- calls getInvoicePdf, opens in new tab (using native fetch + blob URL pattern from Phase 5)
    4. Add "In phieu thu" (Print Receipt) button -- visible when at least one payment exists
    5. Add "Hoan tat" (Finalize) button -- only visible when invoice is fully paid (BalanceDue <= 0) and Status is Draft
       - Finalize button calls useFinalizeInvoice mutation
       - Confirm with AlertDialog: "Xac nhan hoan tat hoa don? Hoa don se khong the chinh sua sau khi hoan tat."
    6. Update payment list to show method name using PAYMENT_METHOD_MAP

    Print button pattern (following Phase 5):
    ```typescript
    const handlePrint = async () => {
      const response = await fetch(`/api/billing/print/${invoiceId}/invoice`, { credentials: 'include' })
      const blob = await response.blob()
      const url = URL.createObjectURL(blob)
      window.open(url, '_blank')
      setTimeout(() => URL.revokeObjectURL(url), 30000)
    }
    ```
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>InvoiceView has payment, print, and finalize buttons integrated</done>
</task>

</tasks>

<verification>
cd D:/projects/ganka/frontend && npx tsc --noEmit
</verification>

<success_criteria>
- All 7 payment methods selectable with visual indicators
- Payment form validates amount, shows method-specific fields
- InvoiceView has payment collection, print, and finalize buttons
- Treatment package split payment fields available
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-19-SUMMARY.md`
</output>
