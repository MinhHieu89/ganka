---
phase: 07-billing-finance
plan: 10
type: tdd
wave: 4
depends_on: ["07-07", "07-08"]
files_modified:
  - backend/tests/Billing.Unit.Tests/Features/PaymentHandlerTests.cs
  - backend/src/Modules/Billing/Billing.Application/Features/RecordPayment.cs
  - backend/src/Modules/Billing/Billing.Application/Features/GetPaymentsByInvoice.cs
autonomous: true
requirements:
  - FIN-03
  - FIN-05
  - FIN-06

must_haves:
  truths:
    - "Payment can be recorded for any supported method (cash, bank transfer, QR, card)"
    - "Split payment marks treatment package payments as 50/50"
    - "Payment recording updates invoice PaidAmount"
  artifacts:
    - path: "backend/tests/Billing.Unit.Tests/Features/PaymentHandlerTests.cs"
      provides: "Unit tests for payment recording including split payments"
      contains: "PaymentHandlerTests"
    - path: "backend/src/Modules/Billing/Billing.Application/Features/RecordPayment.cs"
      provides: "RecordPayment command + handler supporting all payment methods"
      contains: "RecordPaymentCommand"
  key_links:
    - from: "RecordPayment.cs"
      to: "Invoice.cs"
      via: "RecordPayment domain method on invoice"
      pattern: "invoice.RecordPayment"
---

<objective>
TDD implementation of payment handlers: RecordPayment with support for all payment methods, multi-method split, and treatment package 50/50 tracking.

Purpose: Enable cashiers to record payments against invoices using cash, bank transfer, QR, or card with manual confirmation and split payment support.
Output: Payment handler tests + 2 feature handlers
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-09-SUMMARY.md
</context>

<feature>
  <name>Payment Handlers</name>
  <files>
    backend/tests/Billing.Unit.Tests/Features/PaymentHandlerTests.cs,
    backend/src/Modules/Billing/Billing.Application/Features/RecordPayment.cs,
    backend/src/Modules/Billing/Billing.Application/Features/GetPaymentsByInvoice.cs
  </files>
  <behavior>
    RecordPayment:
    - Given valid InvoiceId, amount, method=Cash -> creates confirmed payment, updates PaidAmount on invoice, updates shift CashReceived
    - Given method=BankTransfer with ReferenceNumber -> creates payment with reference
    - Given method=QrVnPay/QrMomo/QrZaloPay -> creates payment with reference, updates shift NonCashRevenue
    - Given method=CardVisa with CardLast4 -> creates payment with card details
    - Given voided invoice -> returns error
    - Given amount exceeding BalanceDue -> returns error "Payment exceeds balance due"
    - Given TreatmentPackageId with IsSplitPayment=true, SplitSequence=1 -> records first 50% payment
    - Payment updates CashierShift totals (CashReceived for cash, TotalRevenue for all)

    GetPaymentsByInvoice:
    - Given valid InvoiceId -> returns list of PaymentDto
  </behavior>
  <implementation>
    Write tests first, then implement handlers.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing payment handler tests</name>
  <files>backend/tests/Billing.Unit.Tests/Features/PaymentHandlerTests.cs</files>
  <behavior>
    - RecordPayment_CashPayment_CreatesConfirmedPaymentAndUpdatesTotals
    - RecordPayment_BankTransfer_RecordsReferenceNumber
    - RecordPayment_QrPayment_RecordsMethodAndReference
    - RecordPayment_CardPayment_RecordsCardDetails
    - RecordPayment_VoidedInvoice_ReturnsError
    - RecordPayment_ExceedsBalance_ReturnsError
    - RecordPayment_TreatmentSplitPayment_MarksAsSplitWithSequence
  </behavior>
  <action>
    Write PaymentHandlerTests with NSubstitute mocks for IInvoiceRepository, IPaymentRepository, ICashierShiftRepository, IUnitOfWork, ICurrentUser.

    Test each payment method creates the correct Payment entity with proper fields. Verify invoice.RecordPayment is called and shift totals are updated.

    Tests MUST fail (RED phase).
  </action>
  <verify>
    <automated>dotnet test backend/tests/Billing.Unit.Tests --filter "PaymentHandler" --no-build -v q 2>&1 | head -10</automated>
  </verify>
  <done>Payment tests compile but fail (RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement payment handlers to pass tests</name>
  <files>
    backend/src/Modules/Billing/Billing.Application/Features/RecordPayment.cs,
    backend/src/Modules/Billing/Billing.Application/Features/GetPaymentsByInvoice.cs
  </files>
  <action>
    1. **RecordPayment.cs**:
       - RecordPaymentCommand: sealed record with Guid InvoiceId, int Method (PaymentMethod as int), decimal Amount, string? ReferenceNumber, string? CardLast4, string? CardType, string? Notes, Guid? TreatmentPackageId, bool IsSplitPayment, int? SplitSequence
       - Validator: InvoiceId NotEmpty, Amount > 0, Method in valid range, CardLast4 length == 4 when Method is Card, ReferenceNumber required for BankTransfer/QR methods
       - Handler:
         a. Load invoice (error if not found or voided)
         b. Validate Amount <= BalanceDue
         c. Create Payment entity via factory
         d. Call invoice.RecordPayment(payment)
         e. Load current shift via ICashierShiftRepository.GetCurrentOpenAsync
         f. If cash: shift.AddCashReceived(amount), else: shift.AddNonCashRevenue(amount)
         g. Set payment.CashierShiftId
         h. Save all changes
         i. Return Result<PaymentDto>

    2. **GetPaymentsByInvoice.cs**:
       - GetPaymentsByInvoiceQuery: sealed record(Guid InvoiceId)
       - Handler: Query payments by invoice, map to PaymentDto list, return
  </action>
  <verify>
    <automated>dotnet test backend/tests/Billing.Unit.Tests --filter "PaymentHandler" -v q</automated>
  </verify>
  <done>All payment handler tests pass (GREEN). All payment methods supported with shift integration.</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Billing.Unit.Tests --filter "Payment" -v q
</verification>

<success_criteria>
- All 7+ payment tests pass
- Cash, bank transfer, QR (3 variants), card (2 variants) all supported
- Treatment package split payment tracked with TreatmentPackageId + SplitSequence
- Shift totals updated per payment type
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-10-SUMMARY.md`
</output>
