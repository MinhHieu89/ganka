---
phase: 07-billing-finance
plan: 20
type: execute
wave: 9
depends_on: ["07-18"]
files_modified:
  - frontend/src/features/billing/components/DiscountDialog.tsx
  - frontend/src/features/billing/components/RefundDialog.tsx
  - frontend/src/features/billing/components/ApprovalPinDialog.tsx
  - frontend/src/features/billing/components/EInvoiceExportButton.tsx
autonomous: true
requirements:
  - FIN-04
  - FIN-07
  - FIN-08

must_haves:
  truths:
    - "Discount dialog supports both percentage and fixed-amount discounts"
    - "Manager PIN dialog required for discount approval and refund approval"
    - "E-invoice export provides PDF, JSON, and XML download options"
  artifacts:
    - path: "frontend/src/features/billing/components/DiscountDialog.tsx"
      provides: "Discount form with type toggle, amount, reason, and manager approval"
      contains: "DiscountDialog"
    - path: "frontend/src/features/billing/components/ApprovalPinDialog.tsx"
      provides: "Manager PIN entry dialog for discount/refund approval"
      contains: "ApprovalPinDialog"
  key_links:
    - from: "DiscountDialog.tsx"
      to: "ApprovalPinDialog.tsx"
      via: "Opens PIN dialog after discount applied"
      pattern: "ApprovalPinDialog"
---

<objective>
Create discount, refund, manager PIN approval, and e-invoice export UI components.

Purpose: Enable discount application with manager oversight, refund requests with approval workflow, and e-invoice export for MISA.
Output: 4 dialog/button components
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-18-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ApprovalPinDialog and DiscountDialog</name>
  <files>
    frontend/src/features/billing/components/ApprovalPinDialog.tsx,
    frontend/src/features/billing/components/DiscountDialog.tsx
  </files>
  <action>
    1. **ApprovalPinDialog.tsx**: Reusable manager PIN entry dialog:
       - Props: open (boolean), onOpenChange, onApprove (pin: string) => void, title (string), description (string)
       - Uses shadcn/ui Dialog
       - PIN input: 4-6 digit numeric input, password type for masking
       - "Xac nhan" (Confirm) button calls onApprove with PIN value
       - "Huy" (Cancel) button closes
       - Error display if PIN validation fails (backend returns error)
       - Use React Hook Form for simple form management
       - PIN input auto-focuses on dialog open

    2. **DiscountDialog.tsx**: Discount application dialog:
       - Props: invoiceId (string), subTotal (decimal), lineItems (for per-item discount), onSuccess callback
       - React Hook Form + Zod validation
       - Fields:
         - Discount scope: Radio "Toan bo hoa don" (Full invoice) / "Theo dich vu" (Per line item)
         - Line item selector (shown when per-item selected): Select dropdown with line item descriptions
         - Discount type: ToggleGroup "%" (Percentage) / "VND" (Fixed Amount)
         - Value input: Number field. When percentage: shows "%" suffix, validates 0-100. When fixed: shows VND suffix, validates > 0 and <= base amount.
         - Live preview: Shows calculated discount amount in VND
         - Reason: Required text input (e.g., "Giam gia khach hang than thiet")
       - Submit flow:
         a. Call useApplyDiscount to create pending discount
         b. Immediately open ApprovalPinDialog for manager PIN
         c. On PIN entry, call useApproveDiscount with PIN
         d. On success: toast "Giam gia da duoc ap dung" (Discount applied), close dialog
         e. On PIN failure: show error, allow retry
       - Use shadcn/ui Dialog, RadioGroup, ToggleGroup, Input, Field+Controller
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>ApprovalPinDialog and DiscountDialog compile with percentage/fixed toggle and manager PIN flow</done>
</task>

<task type="auto">
  <name>Task 2: Create RefundDialog and EInvoiceExportButton</name>
  <files>
    frontend/src/features/billing/components/RefundDialog.tsx,
    frontend/src/features/billing/components/EInvoiceExportButton.tsx
  </files>
  <action>
    1. **RefundDialog.tsx**: Refund request dialog:
       - Props: invoiceId (string), totalAmount (decimal), lineItems (for partial refund), onSuccess callback
       - Fields:
         - Refund scope: Radio "Toan bo" (Full) / "Mot phan" (Partial per line item)
         - Line item selector (partial mode): Select dropdown
         - Amount: VND input (defaults to full amount or line item amount)
         - Reason: Required text input
       - Submit flow:
         a. Call useRequestRefund to create refund request
         b. Open ApprovalPinDialog for manager/owner PIN
         c. On PIN entry, call useApproveRefund
         d. On approval, call useProcessRefund (automatic -- refund is processed)
         e. Success toast: "Hoan tien da duoc xu ly" (Refund processed)
       - Note for Finalized invoices only -- Draft invoices should not show refund option
       - Use shadcn/ui Dialog, RadioGroup, Input, Field+Controller

    2. **EInvoiceExportButton.tsx**: E-invoice export dropdown:
       - Props: invoiceId (string)
       - Renders a Button with DropdownMenu:
         - "Xuat hoa don dien tu" (Export e-invoice) label
         - Menu items:
           a. "PDF" -- calls getEInvoicePdf, opens in new tab
           b. "JSON (MISA)" -- calls exportEInvoiceJson, triggers file download
           c. "XML (MISA)" -- calls exportEInvoiceXml, triggers file download
       - File download pattern: fetch response, create blob URL, trigger download via temporary <a> element
       - Only visible on Finalized invoices
       - Use shadcn/ui Button, DropdownMenu
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>RefundDialog has approval workflow. EInvoiceExportButton supports PDF, JSON, XML downloads.</done>
</task>

</tasks>

<verification>
cd D:/projects/ganka/frontend && npx tsc --noEmit
</verification>

<success_criteria>
- Discount supports percentage and fixed amount with live VND preview
- Manager PIN dialog is reusable for both discount and refund approval
- Refund supports full and partial with approval workflow
- E-invoice export offers PDF, JSON, XML for MISA
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-20-SUMMARY.md`
</output>
