---
phase: 07-billing-finance
plan: 14
type: execute
wave: 5
depends_on: ["07-09", "07-10", "07-11", "07-12"]
files_modified:
  - backend/src/Bootstrapper/Program.cs
  - backend/Ganka28.slnx
  - backend/src/Modules/Billing/Billing.Domain/Entities/Invoice.cs
autonomous: true
requirements:
  - FIN-01
  - FIN-09

must_haves:
  truths:
    - "Billing module registered in Bootstrapper with DbContext, IoC, and endpoint mapping"
    - "Billing projects added to solution file"
    - "Invoice aggregate updated with Payment and Discount collection references"
    - "Database migration created for billing schema"
  artifacts:
    - path: "backend/src/Bootstrapper/Program.cs"
      provides: "Billing module registration with DbContext, IoC, Wolverine discovery, endpoint mapping"
      contains: "AddBillingInfrastructure"
    - path: "backend/Ganka28.slnx"
      provides: "Solution file with Billing.Presentation and Billing.Unit.Tests projects"
  key_links:
    - from: "Program.cs"
      to: "Billing.Infrastructure/IoC.cs"
      via: "Extension method call"
      pattern: "AddBillingInfrastructure|AddBillingApplication"
---

<objective>
Wire the Billing module into the Bootstrapper, add all projects to the solution file, update Invoice aggregate with Payment/Discount collections, and create the database migration.

Purpose: Complete backend integration -- Billing module becomes functional in the running application.
Output: Updated Bootstrapper, solution file, Invoice entity, and EF Core migration
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Invoice entity and wire Bootstrapper</name>
  <files>
    backend/src/Modules/Billing/Billing.Domain/Entities/Invoice.cs,
    backend/src/Bootstrapper/Program.cs
  </files>
  <action>
    1. **Invoice.cs update**: Now that Payment and Discount entities exist (from Plan 02), add the remaining backing fields and navigation properties to Invoice:
       - private readonly List&lt;Payment&gt; _payments = [];
       - private readonly List&lt;Discount&gt; _discounts = [];
       - public IReadOnlyList&lt;Payment&gt; Payments => _payments.AsReadOnly();
       - public IReadOnlyList&lt;Discount&gt; Discounts => _discounts.AsReadOnly();
       - Update RecordPayment method to add payment to _payments and recalculate PaidAmount
       - Add ApplyDiscount method that adds discount to _discounts
       - Update RecalculateTotals to include DiscountTotal from approved discounts

    2. **Program.cs update**: Add Billing module registration following established pattern:
       - Add BillingDbContext registration using the ConfigureDbContext helper (with AuditInterceptor, "billing" schema)
       - Call services.AddBillingApplication()
       - Call services.AddBillingInfrastructure()
       - Add Billing.Presentation endpoint mapping: app.MapBillingApiEndpoints()
       - Add Billing.Application.Marker to Wolverine handler discovery assembly list
       - Add Billing.Presentation project reference to Bootstrapper.csproj

    Follow the exact pattern used for Pharmacy and Clinical module registration in Program.cs.
  </action>
  <verify>
    <automated>dotnet build backend/src/Bootstrapper/Bootstrapper.csproj --no-restore -v q</automated>
  </verify>
  <done>Bootstrapper compiles with Billing module fully wired</done>
</task>

<task type="auto">
  <name>Task 2: Add projects to solution and create migration</name>
  <files>backend/Ganka28.slnx</files>
  <action>
    1. Add Billing.Presentation and Billing.Unit.Tests to the solution file:
       - dotnet sln backend/Ganka28.slnx add backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj
       - dotnet sln backend/Ganka28.slnx add backend/tests/Billing.Unit.Tests/Billing.Unit.Tests.csproj

    2. Create EF Core migration for the billing schema:
       - Run: dotnet ef migrations add InitialBilling --project backend/src/Modules/Billing/Billing.Infrastructure --startup-project backend/src/Bootstrapper --output-dir Migrations -- --context BillingDbContext
       - This creates the migration for all 7 billing tables: Invoices, InvoiceLineItems, Payments, Discounts, Refunds, CashierShifts, ShiftTemplates

    3. Apply the migration to update the database:
       - Run: dotnet ef database update --project backend/src/Modules/Billing/Billing.Infrastructure --startup-project backend/src/Bootstrapper -- --context BillingDbContext

    4. Build the full solution to verify everything compiles:
       - dotnet build backend/Ganka28.slnx --no-restore -v q
  </action>
  <verify>
    <automated>dotnet build backend/Ganka28.slnx -v q</automated>
  </verify>
  <done>Full solution compiles, billing migration created and applied, all projects in solution</done>
</task>

</tasks>

<verification>
dotnet build backend/Ganka28.slnx -v q && dotnet test backend/tests/Billing.Unit.Tests -v q
</verification>

<success_criteria>
- Bootstrapper has Billing module registered (DbContext, IoC, endpoints, Wolverine)
- All Billing projects in solution file
- Invoice entity has Payment and Discount navigation properties
- Migration created for billing schema
- Full solution builds and billing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-14-SUMMARY.md`
</output>
