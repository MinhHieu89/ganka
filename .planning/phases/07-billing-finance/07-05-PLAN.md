---
phase: 07-billing-finance
plan: 05
type: execute
wave: 2
depends_on: ["07-03"]
files_modified:
  - backend/src/Modules/Billing/Billing.Infrastructure/Configurations/CashierShiftConfiguration.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/Configurations/ShiftTemplateConfiguration.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/BillingDbContext.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/Seeding/ShiftTemplateSeeder.cs
autonomous: true
requirements:
  - FIN-10

must_haves:
  truths:
    - "CashierShift config enforces one open shift per branch via filtered unique index"
    - "BillingDbContext registers all 7 entity DbSets and applies configurations"
    - "ShiftTemplateSeeder seeds Morning and Afternoon templates"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/BillingDbContext.cs"
      provides: "Updated BillingDbContext with all entity DbSets"
      contains: "DbSet<Invoice>"
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/Seeding/ShiftTemplateSeeder.cs"
      provides: "IHostedService seeder for Morning/Afternoon shift templates"
      contains: "class ShiftTemplateSeeder"
  key_links:
    - from: "BillingDbContext.cs"
      to: "Configurations/*.cs"
      via: "ApplyConfigurationsFromAssembly"
      pattern: "ApplyConfigurationsFromAssembly"
---

<objective>
Create CashierShift and ShiftTemplate EF Core configurations, update BillingDbContext with all entity DbSets, and create shift template seeder.

Purpose: Complete the EF Core infrastructure for all billing entities and provide default shift templates matching clinic hours.
Output: 2 configurations, updated DbContext, 1 seeder
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CashierShift and ShiftTemplate EF Core configurations</name>
  <files>
    backend/src/Modules/Billing/Billing.Infrastructure/Configurations/CashierShiftConfiguration.cs,
    backend/src/Modules/Billing/Billing.Infrastructure/Configurations/ShiftTemplateConfiguration.cs
  </files>
  <action>
    1. **CashierShiftConfiguration.cs**: IEntityTypeConfiguration of CashierShift:
       - ToTable("CashierShifts")
       - CashierName: HasMaxLength(200), IsRequired
       - All decimal fields (OpeningBalance, ExpectedCashAmount, CashReceived, CashRefunds, ActualCashCount, Discrepancy, TotalRevenue): HasPrecision(18, 0)
       - ManagerNote: HasMaxLength(1000)
       - Status stored as int
       - Filtered unique index: (BranchId, Status) WHERE Status = 0 (Open) -- enforces max one open shift per branch
       - Index on CashierId
       - HasQueryFilter(!IsDeleted)

    2. **ShiftTemplateConfiguration.cs**: IEntityTypeConfiguration of ShiftTemplate:
       - ToTable("ShiftTemplates")
       - Name: HasMaxLength(100), IsRequired
       - NameVi: HasMaxLength(100)
       - DefaultStartTime and DefaultEndTime as TimeOnly
       - Index on BranchId
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>CashierShift and ShiftTemplate configurations compile with filtered unique index for single open shift</done>
</task>

<task type="auto">
  <name>Task 2: Update BillingDbContext and create ShiftTemplateSeeder</name>
  <files>
    backend/src/Modules/Billing/Billing.Infrastructure/BillingDbContext.cs,
    backend/src/Modules/Billing/Billing.Infrastructure/Seeding/ShiftTemplateSeeder.cs
  </files>
  <action>
    1. **BillingDbContext.cs**: Update the existing scaffolded DbContext:
       - Add DbSet properties for all 7 entities: Invoice, InvoiceLineItem, Payment, Discount, Refund, CashierShift, ShiftTemplate
       - In OnModelCreating, keep modelBuilder.HasDefaultSchema("billing")
       - Add modelBuilder.ApplyConfigurationsFromAssembly(typeof(BillingDbContext).Assembly) to auto-discover all configurations
       - Add project references to Billing.Domain in Billing.Infrastructure.csproj if not already present

    2. **ShiftTemplateSeeder.cs**: Create Seeding/ directory. IHostedService pattern (matching AllergyCatalogSeeder from Phase 2):
       - Constructor injects IServiceScopeFactory
       - In StartAsync, create scope, get BillingDbContext
       - Check if ShiftTemplates table has any data (skip if seeded)
       - Seed two templates:
         a. Morning (Ca sang): DefaultStartTime 08:00, DefaultEndTime 12:00 -- matches Sat-Sun hours
         b. Afternoon (Ca chieu): DefaultStartTime 13:00, DefaultEndTime 20:00 -- matches Tue-Fri hours
       - Use BranchId 00000000-0000-0000-0000-000000000001 (matching existing seeder pattern)
       - SaveChangesAsync
       - StopAsync is no-op
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>BillingDbContext has all 7 DbSets, applies configurations from assembly. ShiftTemplateSeeder seeds Morning/Afternoon templates.</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q
</verification>

<success_criteria>
- BillingDbContext has DbSets for all 7 billing entities
- CashierShift config has filtered unique index preventing multiple open shifts per branch
- ShiftTemplateSeeder creates Morning (08:00-12:00) and Afternoon (13:00-20:00) templates
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-05-SUMMARY.md`
</output>
