---
phase: 07-billing-finance
plan: 26
type: execute
wave: 5
depends_on: ["07-11"]
files_modified:
  - backend/src/Modules/Billing/Billing.Application/Features/RejectDiscount.cs
  - backend/src/Modules/Billing/Billing.Application/Features/ApproveRefund.cs
  - backend/src/Modules/Billing/Billing.Application/Features/ProcessRefund.cs
autonomous: true
requirements:
  - FIN-07
  - FIN-08

must_haves:
  truths:
    - "Manager can reject discounts with a reason"
    - "Refunds follow Requested -> Approved -> Processed lifecycle"
    - "Processing a refund creates negative payment and updates shift CashRefunds"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Application/Features/RejectDiscount.cs"
      provides: "RejectDiscount command handler with rejection reason"
      contains: "RejectDiscountCommand"
    - path: "backend/src/Modules/Billing/Billing.Application/Features/ApproveRefund.cs"
      provides: "ApproveRefund command handler with manager PIN verification"
      contains: "ApproveRefundCommand"
    - path: "backend/src/Modules/Billing/Billing.Application/Features/ProcessRefund.cs"
      provides: "ProcessRefund command handler creating refund payment"
      contains: "ProcessRefundCommand"
  key_links:
    - from: "ApproveRefund.cs"
      to: "Auth module"
      via: "Manager PIN verification (cross-module)"
      pattern: "VerifyManagerPin|IMessageBus"
    - from: "ProcessRefund.cs"
      to: "CashierShift.cs"
      via: "Shift cash refunds update"
      pattern: "shift.AddCashRefund"
---

<objective>
Create supplementary discount and refund handlers that were identified during Plan 11 but separated to respect the 5-file constraint.

Purpose: Complete the discount rejection and refund approval/processing lifecycle. These handlers are needed by Plan 13 (API endpoints) which references RejectDiscount, ApproveRefund, and ProcessRefund endpoints.
Output: 3 feature handler files in Billing.Application/Features/
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-11-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RejectDiscount handler</name>
  <files>
    backend/src/Modules/Billing/Billing.Application/Features/RejectDiscount.cs
  </files>
  <action>
    Create RejectDiscount.cs following the vertical slice pattern:

    - RejectDiscountCommand: sealed record(Guid InvoiceId, Guid DiscountId, string RejectionReason, string ManagerPin)
    - Validator: DiscountId NotEmpty, RejectionReason NotEmpty
    - Handler:
      a. Load invoice via IInvoiceRepository
      b. Find discount by DiscountId in invoice.Discounts
      c. Verify manager PIN (same approach as ApproveDiscount in Plan 11 -- IMessageBus query to Auth module)
      d. Call discount.Reject(managerId, rejectionReason) domain method
      e. Save via IUnitOfWork
      f. Return Result
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>RejectDiscount handler compiles with manager PIN verification and rejection reason</done>
</task>

<task type="auto">
  <name>Task 2: Create ApproveRefund and ProcessRefund handlers</name>
  <files>
    backend/src/Modules/Billing/Billing.Application/Features/ApproveRefund.cs,
    backend/src/Modules/Billing/Billing.Application/Features/ProcessRefund.cs
  </files>
  <action>
    1. **ApproveRefund.cs**:
       - ApproveRefundCommand: sealed record(Guid InvoiceId, Guid RefundId, string ManagerPin)
       - Validator: RefundId NotEmpty, ManagerPin NotEmpty
       - Handler:
         a. Load invoice, find refund by RefundId
         b. Verify manager PIN via IMessageBus query to Auth module
         c. Call refund.Approve(managerId) domain method -- validates status is Requested
         d. Save via IUnitOfWork
         e. Return Result

    2. **ProcessRefund.cs**:
       - ProcessRefundCommand: sealed record(Guid InvoiceId, Guid RefundId, int RefundMethod, string? Notes)
       - Validator: RefundId NotEmpty, RefundMethod valid enum range
       - Handler:
         a. Load invoice, find refund by RefundId
         b. Validate refund status is Approved
         c. Call refund.Process(userId) domain method
         d. If refund method is Cash: load current shift via ICashierShiftRepository.GetCurrentOpenAsync, call shift.AddCashRefund(refund.Amount)
         e. Mark original payment(s) as Refunded if applicable (call payment.MarkRefunded())
         f. Save all changes
         g. Return Result<RefundDto>
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>ApproveRefund and ProcessRefund handlers compile. Full refund lifecycle: Requested -> Approved -> Processed with shift cash refund tracking.</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj --no-restore -v q
</verification>

<success_criteria>
- RejectDiscount handler works with manager PIN verification and rejection reason
- ApproveRefund validates Requested status and manager PIN
- ProcessRefund validates Approved status, updates shift CashRefunds for cash refunds
- All 3 handlers compile and follow vertical slice pattern
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-26-SUMMARY.md`
</output>
