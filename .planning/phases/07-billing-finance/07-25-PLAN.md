---
phase: 07-billing-finance
plan: 25
type: execute
wave: 5
depends_on: ["07-09", "07-12"]
files_modified:
  - backend/src/Modules/Billing/Billing.Application/Features/RemoveInvoiceLineItem.cs
  - backend/src/Modules/Billing/Billing.Application/Features/GetInvoiceById.cs
  - backend/src/Modules/Billing/Billing.Application/Features/GetInvoicesByVisit.cs
  - backend/src/Modules/Billing/Billing.Application/Features/GetShiftTemplates.cs
autonomous: true
requirements:
  - FIN-01
  - FIN-10

must_haves:
  truths:
    - "Invoice line items can be removed from draft invoices"
    - "Invoice can be queried by ID or by VisitId"
    - "Shift templates can be listed for shift opening"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Application/Features/RemoveInvoiceLineItem.cs"
      provides: "RemoveInvoiceLineItem command handler"
      contains: "RemoveInvoiceLineItemCommand"
    - path: "backend/src/Modules/Billing/Billing.Application/Features/GetInvoiceById.cs"
      provides: "GetInvoiceById query handler returning InvoiceDto"
      contains: "GetInvoiceByIdQuery"
    - path: "backend/src/Modules/Billing/Billing.Application/Features/GetInvoicesByVisit.cs"
      provides: "GetInvoicesByVisit query handler"
      contains: "GetInvoicesByVisitQuery"
    - path: "backend/src/Modules/Billing/Billing.Application/Features/GetShiftTemplates.cs"
      provides: "GetShiftTemplates query handler"
      contains: "GetShiftTemplatesQuery"
  key_links:
    - from: "RemoveInvoiceLineItem.cs"
      to: "Invoice.cs"
      via: "RemoveLineItem domain method"
      pattern: "invoice.RemoveLineItem"
    - from: "GetInvoiceById.cs"
      to: "IInvoiceRepository"
      via: "Repository query"
      pattern: "GetByIdAsync"
---

<objective>
Create supplementary invoice and shift query/command handlers that were identified during Plan 09 and Plan 12 but separated to respect the 5-file constraint.

Purpose: Complete the invoice CRUD surface (remove line item, get by ID, get by visit) and shift templates query. These are needed by Plan 13 (API endpoints) which references all these handlers.
Output: 4 feature handler files in Billing.Application/Features/
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-09-SUMMARY.md
@.planning/phases/07-billing-finance/07-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create supplementary invoice handlers</name>
  <files>
    backend/src/Modules/Billing/Billing.Application/Features/RemoveInvoiceLineItem.cs,
    backend/src/Modules/Billing/Billing.Application/Features/GetInvoiceById.cs,
    backend/src/Modules/Billing/Billing.Application/Features/GetInvoicesByVisit.cs
  </files>
  <action>
    Create three handler files following the established vertical slice pattern:

    1. **RemoveInvoiceLineItem.cs**:
       - RemoveInvoiceLineItemCommand: sealed record(Guid InvoiceId, Guid LineItemId)
       - Validator: InvoiceId NotEmpty, LineItemId NotEmpty
       - Handler: Load invoice via IInvoiceRepository, call invoice.RemoveLineItem(lineItemId) domain method (available from Plan 01), save via IUnitOfWork. Return Result<InvoiceDto>.

    2. **GetInvoiceById.cs**:
       - GetInvoiceByIdQuery: sealed record(Guid InvoiceId)
       - Handler: Load invoice with all child collections (LineItems, Payments, Discounts) via IInvoiceRepository.GetByIdWithChildrenAsync. Map to InvoiceDto with all fields including line items grouped by department, payment summary, discount summary. Return Result<InvoiceDto>.
       - InvoiceDto should include: Id, InvoiceNumber, PatientId, PatientName, VisitId, Status, SubTotal, DiscountTotal, TotalAmount, PaidAmount, BalanceDue, IsFullyPaid, LineItems (list of InvoiceLineItemDto), Payments (list of PaymentDto), Discounts (list of DiscountDto), FinalizedAt, FinalizedById.

    3. **GetInvoicesByVisit.cs**:
       - GetInvoicesByVisitQuery: sealed record(Guid VisitId)
       - Handler: Query invoices by VisitId via IInvoiceRepository.GetByVisitIdAsync. Map to list of InvoiceDto (lightweight, without full children -- just summary fields). Return Result<List<InvoiceSummaryDto>>.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>All 3 supplementary invoice handlers compile and follow vertical slice pattern</done>
</task>

<task type="auto">
  <name>Task 2: Create GetShiftTemplates handler</name>
  <files>
    backend/src/Modules/Billing/Billing.Application/Features/GetShiftTemplates.cs
  </files>
  <action>
    Create GetShiftTemplates.cs query handler:

    - GetShiftTemplatesQuery: sealed record() (no params, uses ICurrentUser for BranchId)
    - Handler: Query all active ShiftTemplate entities via IShiftTemplateRepository (or ICashierShiftRepository if templates are accessed there). Map to list of ShiftTemplateDto with fields: Id, Name, StartTime, EndTime, IsActive.
    - Return Result<List<ShiftTemplateDto>>.
    - This handler is called by the /api/billing/shifts/templates endpoint (defined in Plan 13).
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>GetShiftTemplates handler compiles and returns active shift templates</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj --no-restore -v q
</verification>

<success_criteria>
- RemoveInvoiceLineItem removes line items from draft invoices and recalculates totals
- GetInvoiceById returns full invoice with all child collections
- GetInvoicesByVisit returns invoice summaries for a visit
- GetShiftTemplates returns active shift templates
- All handlers compile
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-25-SUMMARY.md`
</output>
