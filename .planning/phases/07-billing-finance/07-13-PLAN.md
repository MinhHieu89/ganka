---
phase: 07-billing-finance
plan: 13
type: execute
wave: 5
depends_on: ["07-09", "07-10", "07-11", "07-12"]
files_modified:
  - backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj
  - backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs
  - backend/src/Modules/Billing/Billing.Presentation/IoC.cs
autonomous: true
requirements:
  - FIN-01
  - FIN-03
  - FIN-07
  - FIN-08
  - FIN-10

must_haves:
  truths:
    - "All billing API endpoints are defined with proper authorization"
    - "Invoice, payment, discount, refund, and shift endpoints all mapped"
    - "Presentation IoC registers endpoint mapping"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs"
      provides: "All billing Minimal API endpoints grouped under /api/billing"
      contains: "MapBillingApiEndpoints"
    - path: "backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj"
      provides: "Presentation project with correct references"
      contains: "Billing.Application"
  key_links:
    - from: "BillingApiEndpoints.cs"
      to: "Application/Features/*.cs"
      via: "Wolverine bus.InvokeAsync"
      pattern: "bus.InvokeAsync"
---

<objective>
Create Billing.Presentation project with all Minimal API endpoints for invoices, payments, discounts, refunds, and shifts.

Purpose: Expose billing functionality via HTTP API endpoints with proper authorization, following established Minimal API patterns.
Output: Presentation project with endpoints for all billing features
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-09-SUMMARY.md
@.planning/phases/07-billing-finance/07-10-SUMMARY.md
@.planning/phases/07-billing-finance/07-11-SUMMARY.md
@.planning/phases/07-billing-finance/07-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Billing.Presentation project</name>
  <files>
    backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj,
    backend/src/Modules/Billing/Billing.Presentation/IoC.cs
  </files>
  <action>
    1. Create the Billing.Presentation directory and .csproj:
       - TargetFramework net10.0
       - FrameworkReference Microsoft.AspNetCore.App (for Minimal API)
       - ProjectReference to Billing.Application, Billing.Contracts
       - ProjectReference to Shared.Presentation (for ResultExtensions ToHttpResult/ToCreatedHttpResult)
       - PackageReference to WolverineFx (for IMessageBus)

    2. **IoC.cs**: Static class PresentationIoC with AddBillingPresentation extension method:
       - Currently empty (no presentation-specific services)
       - Add MapBillingApiEndpoints as separate extension method on IEndpointRouteBuilder
       - Return services for chaining
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj --no-restore -v q</automated>
  </verify>
  <done>Presentation project compiles with correct references</done>
</task>

<task type="auto">
  <name>Task 2: Create all billing API endpoints</name>
  <files>backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs</files>
  <action>
    Create BillingApiEndpoints.cs with MapBillingApiEndpoints extension method on IEndpointRouteBuilder. Follow the established pattern from PharmacyApiEndpoints.cs and ClinicalApiEndpoints.cs.

    Define endpoint groups:

    **Invoice Group** (/api/billing/invoices):
    - POST / -> CreateInvoice (RequireAuthorization with Billing permissions)
    - GET /{invoiceId} -> GetInvoiceById
    - GET /by-visit/{visitId} -> GetInvoicesByVisit
    - POST /{invoiceId}/line-items -> AddInvoiceLineItem
    - DELETE /{invoiceId}/line-items/{lineItemId} -> RemoveInvoiceLineItem
    - POST /{invoiceId}/finalize -> FinalizeInvoice

    **Payment Group** (/api/billing/payments):
    - POST / -> RecordPayment
    - GET /by-invoice/{invoiceId} -> GetPaymentsByInvoice

    **Discount Group** (/api/billing/discounts):
    - POST / -> ApplyDiscount
    - POST /{discountId}/approve -> ApproveDiscount
    - POST /{discountId}/reject -> RejectDiscount

    **Refund Group** (/api/billing/refunds):
    - POST / -> RequestRefund
    - POST /{refundId}/approve -> ApproveRefund
    - POST /{refundId}/process -> ProcessRefund

    **Shift Group** (/api/billing/shifts):
    - POST /open -> OpenShift
    - POST /close -> CloseShift
    - GET /current -> GetCurrentShift
    - GET /{shiftId}/report -> GetShiftReport
    - GET /templates -> GetShiftTemplates

    Each endpoint:
    - Uses bus.InvokeAsync to dispatch commands/queries
    - Returns appropriate HTTP status via ToHttpResult/ToCreatedHttpResult
    - Has RequireAuthorization() with appropriate billing permissions
    - Uses [AsParameters] for GET query parameters

    Use MapGroup for prefix consolidation. Group-level RequireAuthorization on all billing routes.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj --no-restore -v q</automated>
  </verify>
  <done>All billing endpoints defined with proper authorization, HTTP methods, and route patterns</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj --no-restore -v q
</verification>

<success_criteria>
- All 16+ endpoints defined covering invoices, payments, discounts, refunds, shifts
- Proper RequireAuthorization on all routes
- MapGroup used for route prefix consolidation
- bus.InvokeAsync pattern for command/query dispatch
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-13-SUMMARY.md`
</output>
