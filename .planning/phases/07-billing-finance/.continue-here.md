---
phase: 07-billing-finance
task: UAT re-test
total_tasks: 35
status: in_progress
last_updated: 2026-03-13T04:05:36.413Z
---

<current_state>
Running UAT re-test for Phase 7 (Billing & Finance). Tests 1-3 passed, Test 4 (Create Invoice) skipped because there's no UI to create invoices from billing dashboard — per FIN-01, invoices should be created per visit. Tests 5-9 skipped (depend on invoice creation). Test 10 (Open Shift) passed after fixing two bugs. Test 11 (Duplicate Shift) passed via API. Test 12+ requires an invoice with line items — invoice creation via API returns 201 but AddLineItem returns 500 (needs debugging).
</current_state>

<completed_work>

- Tests 1-3: Cold Start, Sidebar Nav, Dashboard Layout — all PASS
- Test 4: Create Invoice — SKIPPED (no UI, per FIN-01 invoices are per-visit)
- Tests 5-9: Invoice Detail, Add/Remove Line Items, Finalize, Empty Finalize — SKIPPED (depend on T4)
- Test 10: Open Cashier Shift — PASS (after fixing 2 bugs below)
- Test 11: Cannot Open Duplicate Shift — PASS (tested via API, 409 returned)

**Bugs fixed during UAT:**
1. **Shift template localization**: Seeder had "Ca sang"/"Ca chieu" without diacritics → fixed to "Ca sáng"/"Ca chiều" in seeder + updated DB records directly
2. **Open Shift 404**: Frontend POST to `/api/billing/shifts` but backend endpoint is `/api/billing/shifts/open` → fixed frontend shift-api.ts
3. **Close Shift URL mismatch**: Frontend sends to `/shifts/{id}/close` but backend has `/shifts/close` → fixed frontend shift-api.ts

**Files modified:**
- `backend/src/Modules/Billing/Billing.Infrastructure/Seeding/ShiftTemplateSeeder.cs` — Vietnamese diacritics fix
- `frontend/src/features/billing/api/shift-api.ts` — open/close shift endpoint URL fixes
</completed_work>

<remaining_work>

- Test 12-18: Payment tests (Cash, Bank Transfer, QR, Card, Split, Validation) — BLOCKED by AddLineItem 500 error
- Test 19-22: Discount tests (Percentage, Fixed, Approve, Reject)
- Test 23-24: Refund tests (Request, Approve/Process)
- Test 25-26: Close Shift, Shift Report
- Test 27: Shift History
- Test 28-33: Print/Export tests (Invoice PDF, Receipt PDF, E-Invoice PDF/JSON/XML, Shift Report PDF)
- Test 34-35: VND formatting, VND-to-words
- Debug and fix AddLineItem 500 error before continuing payment tests
</remaining_work>

<decisions_made>

- Skipped Test 4 (Create Invoice from UI) — per FIN-01, invoices are "single consolidated invoice per visit", so the creation flow belongs in clinical workflow, not billing dashboard. User agreed to address in separate phase.
- Reverted the CreateInvoiceDialog component that was partially built — keeping billing dashboard clean
- Using API calls to create test invoices for payment testing since no UI exists
</decisions_made>

<blockers>

- **AddLineItem returns 500**: POST to `/api/billing/invoices/{id}/line-items` returns Internal Server Error. Invoice creation works (201). The 500 is likely a Wolverine message bus issue or EF Core concurrency problem (known ValueGeneratedNever issue from previous UAT). Backend console output wasn't captured — need to restart with visible logging to debug.
- **ToCreatedHttpResult wrapping**: Known gap from previous UAT — response wraps DTO in `{"id": {...dto...}}` instead of returning DTO directly. Affects all billing POST endpoints.
</blockers>

<context>
This is a UAT re-test of Phase 7 Billing & Finance. Previous UAT (completed 2026-03-07) had 43 tests, 40 passed, 3 skipped. This re-test has 35 focused tests. The billing module is mostly functional at the API level but has frontend-to-backend URL mismatches and missing UI flows (invoice creation). The AddLineItem 500 is the critical blocker — without it, payment/discount/refund tests can't proceed since they need invoices with line items and non-zero totals.

UAT file: `.planning/phases/07-billing-finance/07-UAT.md`
</context>

<next_action>
1. Restart backend with visible console logging to capture the AddLineItem 500 error stacktrace
2. Debug and fix the AddLineItem issue (likely EF Core related — check RowVersion/concurrency or Wolverine handler routing)
3. Once fixed, create invoice + add line items via API, then continue UAT from Test 12 (Record Cash Payment)
</next_action>
