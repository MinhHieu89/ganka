---
phase: 07-billing-finance
plan: 08
type: execute
wave: 3
depends_on: ["07-05", "07-06"]
files_modified:
  - backend/src/Modules/Billing/Billing.Infrastructure/Repositories/CashierShiftRepository.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/UnitOfWork.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/IoC.cs
  - backend/src/Modules/Billing/Billing.Application/IoC.cs
  - backend/src/Modules/Billing/Billing.Application/Interfaces/IBillingDocumentService.cs
autonomous: true
requirements:
  - FIN-04
  - FIN-10

must_haves:
  truths:
    - "CashierShift repository can find current open shift by branch"
    - "UnitOfWork wraps BillingDbContext SaveChanges"
    - "IoC registers all repositories, UnitOfWork, and seeder"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/Repositories/CashierShiftRepository.cs"
      provides: "CashierShift repository with GetCurrentOpenAsync"
      contains: "class CashierShiftRepository"
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/IoC.cs"
      provides: "Infrastructure DI registration for repositories, UnitOfWork, seeder"
      contains: "static class InfrastructureIoC"
    - path: "backend/src/Modules/Billing/Billing.Application/IoC.cs"
      provides: "Application DI registration for validators"
      contains: "static class ApplicationIoC"
  key_links:
    - from: "IoC.cs (Infrastructure)"
      to: "Repositories/*.cs"
      via: "AddScoped registration"
      pattern: "AddScoped<IInvoiceRepository, InvoiceRepository>"
---

<objective>
Create CashierShift repository implementation, UnitOfWork, IoC registration for both Application and Infrastructure layers, and the IBillingDocumentService interface.

Purpose: Complete the data access layer and wire up dependency injection for all billing infrastructure.
Output: Repository, UnitOfWork, 2 IoC files, document service interface
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-05-SUMMARY.md
@.planning/phases/07-billing-finance/07-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CashierShift repository and UnitOfWork</name>
  <files>
    backend/src/Modules/Billing/Billing.Infrastructure/Repositories/CashierShiftRepository.cs,
    backend/src/Modules/Billing/Billing.Infrastructure/UnitOfWork.cs,
    backend/src/Modules/Billing/Billing.Application/Interfaces/IBillingDocumentService.cs
  </files>
  <action>
    1. **CashierShiftRepository.cs**: Implements ICashierShiftRepository:
       - Constructor injects BillingDbContext
       - GetByIdAsync: FirstOrDefaultAsync by Id
       - GetCurrentOpenAsync: FirstOrDefaultAsync where BranchId matches AND Status == ShiftStatus.Open
       - GetByDateRangeAsync: Where OpenedAt between from and to, OrderByDescending OpenedAt, ToListAsync
       - GetLastClosedAsync: Where BranchId matches AND Status == ShiftStatus.Closed, OrderByDescending ClosedAt, FirstOrDefaultAsync
       - Add and Update standard pattern

    2. **UnitOfWork.cs**: Implements IUnitOfWork (from Billing.Application.Interfaces):
       - Constructor injects BillingDbContext
       - SaveChangesAsync delegates to _context.SaveChangesAsync
       - Follow established pattern from Pharmacy.Infrastructure.UnitOfWork

    3. **IBillingDocumentService.cs**: Interface in Billing.Application/Interfaces:
       - GenerateInvoicePdfAsync(Guid invoiceId, CancellationToken ct) -> byte[]
       - GenerateReceiptPdfAsync(Guid invoiceId, CancellationToken ct) -> byte[]
       - GenerateEInvoicePdfAsync(Guid invoiceId, CancellationToken ct) -> byte[]
       - GenerateShiftReportPdfAsync(Guid shiftId, CancellationToken ct) -> byte[]
       - ExportEInvoiceJsonAsync(Guid invoiceId, CancellationToken ct) -> string
       - ExportEInvoiceXmlAsync(Guid invoiceId, CancellationToken ct) -> string

       This interface will be implemented in Plan 15-16 when QuestPDF documents are created.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>CashierShift repository, UnitOfWork, and IBillingDocumentService interface compile</done>
</task>

<task type="auto">
  <name>Task 2: Create IoC registration for Application and Infrastructure layers</name>
  <files>
    backend/src/Modules/Billing/Billing.Application/IoC.cs,
    backend/src/Modules/Billing/Billing.Infrastructure/IoC.cs
  </files>
  <action>
    Follow the established IoC.cs pattern from Phase 01.1-08.

    1. **Billing.Application/IoC.cs**: Static class ApplicationIoC with AddBillingApplication(this IServiceCollection services) extension method:
       - Register FluentValidation validators from assembly: services.AddValidatorsFromAssembly(typeof(Marker).Assembly)
       - Add FluentValidation.DependencyInjectionExtensions to Billing.Application.csproj if not present
       - Return services for chaining

    2. **Billing.Infrastructure/IoC.cs**: Static class InfrastructureIoC with AddBillingInfrastructure(this IServiceCollection services) extension method:
       - AddScoped IInvoiceRepository, InvoiceRepository
       - AddScoped IPaymentRepository, PaymentRepository
       - AddScoped ICashierShiftRepository, CashierShiftRepository
       - AddScoped IUnitOfWork (Billing.Application.Interfaces.IUnitOfWork), UnitOfWork
       - AddHostedService ShiftTemplateSeeder
       - Return services for chaining

    Note: BillingDbContext registration stays in Bootstrapper Program.cs (cross-module AuditInterceptor dependency). IoC only registers module-internal services.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q && dotnet build backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>Both IoC files compile with all repository and service registrations</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q
</verification>

<success_criteria>
- CashierShiftRepository.GetCurrentOpenAsync finds active shift by branch
- UnitOfWork wraps BillingDbContext
- Infrastructure IoC registers all 3 repos, UnitOfWork, seeder
- Application IoC registers FluentValidation
- IBillingDocumentService interface defined for future implementation
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-08-SUMMARY.md`
</output>
