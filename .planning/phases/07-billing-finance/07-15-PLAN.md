---
phase: 07-billing-finance
plan: 15
type: execute
wave: 6
depends_on: ["07-14"]
files_modified:
  - backend/src/Modules/Billing/Billing.Infrastructure/Documents/InvoiceDocument.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/Documents/ReceiptDocument.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/Documents/EInvoiceDocument.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/Documents/ShiftReportDocument.cs
autonomous: true
requirements:
  - FIN-01
  - FIN-04

must_haves:
  truths:
    - "Invoice PDF groups line items by department (Kham benh, Duoc pham, Kinh, Dieu tri)"
    - "E-invoice PDF includes all Vietnamese tax law required fields"
    - "Shift report PDF shows revenue by payment method and cash reconciliation"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/Documents/InvoiceDocument.cs"
      provides: "QuestPDF A4 invoice with department grouping and VND formatting"
      contains: "class InvoiceDocument : IDocument"
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/Documents/EInvoiceDocument.cs"
      provides: "QuestPDF e-invoice with all Decree 123/2020 required fields"
      contains: "class EInvoiceDocument : IDocument"
  key_links:
    - from: "InvoiceDocument.cs"
      to: "ClinicHeaderComponent"
      via: "Reuse existing clinic header from Phase 5"
      pattern: "ClinicHeaderComponent"
---

<objective>
Create QuestPDF documents for invoice, receipt, e-invoice, and shift report. Reuse the existing ClinicHeaderComponent and Noto Sans Vietnamese font from Phase 5.

Purpose: Generate all printable billing documents with proper Vietnamese formatting, department grouping, and e-invoice compliance (FIN-04, PRT-03).
Output: 4 QuestPDF document classes
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-billing-finance/07-RESEARCH.md
@.planning/phases/07-billing-finance/07-14-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Invoice and Receipt QuestPDF documents</name>
  <files>
    backend/src/Modules/Billing/Billing.Infrastructure/Documents/InvoiceDocument.cs,
    backend/src/Modules/Billing/Billing.Infrastructure/Documents/ReceiptDocument.cs
  </files>
  <action>
    Create Documents/ directory in Billing.Infrastructure. Add QuestPDF package reference to Billing.Infrastructure.csproj. Add project reference to Shared.Infrastructure for ClinicHeaderComponent and FontManager access.

    1. **InvoiceDocument.cs**: IDocument implementation following DrugPrescriptionDocument pattern from Phase 5:
       - Constructor takes InvoiceData (custom data class with invoice + line items + payments) and ClinicHeaderData
       - Page size: A4, margins 15mm
       - Font: Noto Sans (already configured in FontManager from Phase 5)
       - Header: ClinicHeaderComponent (reuse from Phase 5)
       - Title: "HOA DON BAN HANG" centered, bold, 14pt
       - Patient info: Name, patient code
       - Invoice info: Invoice number, date
       - Line items table grouped by department:
         - Section header per department: "KHAM BENH", "DUOC PHAM", "KINH", "DIEU TRI"
         - Columns: STT, Description (Vietnamese primary), Quantity, Unit Price, Total
         - VND formatting: dot thousands separator, no decimals (e.g., 1.500.000)
       - Subtotal, Discount (if any), Total in bold
       - Payment summary: method + amount per payment
       - Footer: Cashier name, date, signature line

    2. **ReceiptDocument.cs**: Compact receipt (A5 or thermal receipt style):
       - Simpler version of invoice -- just totals and payment confirmation
       - Clinic header (compact)
       - "PHIEU THU" (Payment receipt) title
       - Patient name, invoice number
       - Total amount, payment method(s), amount paid
       - Cashier signature line
       - Smaller margins (10mm)

    Create a shared InvoiceData class (or use InvoiceDto directly) for data passing to documents.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>Invoice and Receipt QuestPDF documents compile with department grouping and VND formatting</done>
</task>

<task type="auto">
  <name>Task 2: Create E-Invoice and Shift Report QuestPDF documents</name>
  <files>
    backend/src/Modules/Billing/Billing.Infrastructure/Documents/EInvoiceDocument.cs,
    backend/src/Modules/Billing/Billing.Infrastructure/Documents/ShiftReportDocument.cs
  </files>
  <action>
    1. **EInvoiceDocument.cs**: Vietnamese e-invoice (hoa don dien tu) per Decree 123/2020:
       - Page size: A4, margins 15mm
       - Title: "HOA DON GIA TRI GIA TANG" or "HOA DON BAN HANG" (configurable)
       - Invoice template symbol (Ky hieu mau so hoa don) and invoice symbol (Ky hieu hoa don)
       - Seller section: Name, tax code, address (from clinic settings)
       - Buyer section: Name, tax code (if business), address
       - Items table: STT, Description, Unit (Don vi tinh), Quantity, Unit Price (Don gia), Amount (Thanh tien)
       - Tax section: Pre-tax total (Cong tien hang), Tax rate (Thue suat, e.g., 8%), Tax amount (Tien thue GTGT), Total (Tong tien thanh toan)
       - Amount in words (Vietnamese): "Tong so tien (bang chu): ..." -- implement a VND-to-words converter
       - Payment method text
       - Footer: Buyer signature, Seller signature columns

    2. **ShiftReportDocument.cs**: Shift report PDF:
       - Page size: A4
       - Title: "BAO CAO CA LAM VIEC" (Shift Report)
       - Shift info: Cashier name, template name, opened at, closed at
       - Revenue table: Payment method | Transaction count | Amount (grouped by method)
       - Summary: Total revenue, total transactions
       - Cash reconciliation section:
         - Opening balance
         - Cash received
         - Cash refunds
         - Expected cash (= opening + received - refunds)
         - Actual cash count
         - Discrepancy (highlighted if non-zero)
         - Manager note (if discrepancy)
       - Signature lines: Cashier, Manager
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>E-invoice document has all Vietnamese tax law required fields. Shift report shows cash reconciliation.</done>
</task>

</tasks>

<verification>
dotnet build backend/src/Modules/Billing/Billing.Infrastructure/Billing.Infrastructure.csproj --no-restore -v q
</verification>

<success_criteria>
- Invoice PDF groups line items by department with Vietnamese headers
- E-invoice includes all mandatory fields per Decree 123/2020
- VND formatted with dot thousands separator, no decimal places
- Shift report shows revenue by method and cash discrepancy
- All documents reuse ClinicHeaderComponent and Noto Sans from Phase 5
</success_criteria>

<output>
After completion, create `.planning/phases/07-billing-finance/07-15-SUMMARY.md`
</output>
