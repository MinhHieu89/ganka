---
phase: 01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing
plan: 06
type: execute
wave: 4
depends_on:
  - 01.2-02
  - 01.2-05
files_modified:
  - frontend/src/shared/components/AppShell.tsx
  - frontend/src/shared/components/AppSidebar.tsx
  - frontend/src/shared/components/SiteHeader.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - AUTH-04

must_haves:
  truths:
    - "SidebarProvider sets CSS variables --sidebar-width and --header-height matching dashboard-01 reference"
    - "AppSidebar uses variant='inset' and collapsible='offcanvas' (not collapsible='icon')"
    - "Dashboard has inset visual treatment: sidebar on muted background, main content in SidebarInset with border and rounded corners"
    - "SiteHeader uses h-(--header-height) CSS variable instead of fixed h-12"
    - "Content wrapper uses @container/main for container query support"
    - "All existing functionality preserved: sidebar collapse, active routes, admin conditional rendering, user dropdown, language toggle, session warning"
  artifacts:
    - path: "frontend/src/shared/components/AppShell.tsx"
      provides: "Dashboard shell with SidebarProvider CSS variables, SidebarInset content wrapper with @container/main"
      contains: "--sidebar-width"
    - path: "frontend/src/shared/components/AppSidebar.tsx"
      provides: "Sidebar with variant='inset' and collapsible='offcanvas'"
      contains: "variant=\"inset\""
    - path: "frontend/src/shared/components/SiteHeader.tsx"
      provides: "Header using h-(--header-height) CSS variable with transition classes"
      contains: "h-(--header-height)"
  key_links:
    - from: "frontend/src/shared/components/AppShell.tsx"
      to: "frontend/src/shared/components/AppSidebar.tsx"
      via: "SidebarProvider wraps AppSidebar with CSS custom properties"
      pattern: "--sidebar-width.*calc"
    - from: "frontend/src/shared/components/AppShell.tsx"
      to: "frontend/src/shared/components/SiteHeader.tsx"
      via: "SidebarInset wraps SiteHeader and content area"
      pattern: "SidebarInset"
    - from: "frontend/src/shared/components/SiteHeader.tsx"
      to: "CSS variable --header-height"
      via: "h-(--header-height) class references CSS variable set by SidebarProvider"
      pattern: "h-\\(--header-height\\)"
---

<objective>
Update AppShell, AppSidebar, and SiteHeader to match the shadcn dashboard-01 reference layout exactly. The current implementation uses `collapsible="icon"` with no variant and fixed `h-12` header height. The dashboard-01 reference requires `variant="inset"` with `collapsible="offcanvas"`, CSS custom properties for sidebar width and header height, and container query support.

Purpose: The user locked the decision that the dashboard layout must match dashboard-01 exactly. Plan 02 established the basic structure but used different sidebar props and header sizing. This plan corrects those specifics to achieve exact dashboard-01 fidelity -- the "card floating beside sidebar" inset visual treatment.

Output: Dashboard shell with exact dashboard-01 visual treatment: inset sidebar on muted background, bordered/rounded main content area, CSS variable-driven sizing, container query wrapper.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-CONTEXT.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AppShell with SidebarProvider CSS variables and content wrapper</name>
  <files>
    frontend/src/shared/components/AppShell.tsx
  </files>
  <action>
    Update AppShell.tsx to match the dashboard-01 reference page.tsx structure exactly:

    1. Add `style` prop to SidebarProvider with CSS custom properties:
    ```tsx
    <SidebarProvider
      style={{
        "--sidebar-width": "calc(var(--spacing) * 72)",
        "--header-height": "calc(var(--spacing) * 12)",
      } as React.CSSProperties}
    >
    ```

    2. Pass `variant="inset"` to AppSidebar:
    ```tsx
    <AppSidebar variant="inset" />
    ```
    This requires updating AppSidebar to accept and forward the `variant` prop (done in Task 2).

    3. Replace the current `<main>` wrapper with dashboard-01's content structure:
    ```tsx
    <SidebarInset>
      <SiteHeader />
      <div className="flex flex-1 flex-col">
        <div className="@container/main flex flex-1 flex-col gap-2">
          <div className="flex-1 p-4 md:p-6">
            <Outlet />
          </div>
        </div>
      </div>
    </SidebarInset>
    ```
    Key changes from current code:
    - Remove `<main>` tag, use `<div>` wrappers instead (dashboard-01 pattern)
    - Add `@container/main` class for container query support on the content area
    - Add `flex flex-1 flex-col` wrapper for proper flex layout
    - Keep `p-4 md:p-6` padding on innermost content div

    4. Keep SessionWarningModal rendering exactly as-is (outside SidebarInset, inside SidebarProvider).

    The `SidebarInset` component (from shadcn sidebar) automatically provides the bordered, rounded-corner, inset visual treatment when used with `variant="inset"` sidebar.

    Do NOT change any imports beyond what's needed. Do NOT modify session warning logic.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>AppShell.tsx has SidebarProvider with style prop setting --sidebar-width and --header-height. Content area has @container/main class.</manual>
  </verify>
  <done>SidebarProvider sets --sidebar-width and --header-height CSS variables. Content wrapper uses @container/main. AppSidebar receives variant="inset" prop. Dashboard-01 reference structure is matched.</done>
</task>

<task type="auto">
  <name>Task 2: Update AppSidebar and SiteHeader to match dashboard-01 props and sizing</name>
  <files>
    frontend/src/shared/components/AppSidebar.tsx
    frontend/src/shared/components/SiteHeader.tsx
  </files>
  <action>
    **AppSidebar.tsx changes:**

    1. Update the component to accept and forward `variant` prop from parent:
    ```tsx
    import type { ComponentProps } from "react"

    export function AppSidebar(props: ComponentProps<typeof Sidebar>) {
    ```
    Or more specifically, accept the props that Sidebar accepts and spread them:
    ```tsx
    export function AppSidebar({ ...sidebarProps }: ComponentProps<typeof Sidebar>) {
      // ... existing code ...
      return (
        <Sidebar collapsible="offcanvas" {...sidebarProps}>
    ```

    2. Change `collapsible="icon"` to `collapsible="offcanvas"` on the `<Sidebar>` component.
    The `variant` prop ("inset") will come from the parent via `{...sidebarProps}` spread, so don't hardcode it -- let the parent pass it through.

    Current code: `<Sidebar collapsible="icon">`
    New code: `<Sidebar collapsible="offcanvas" {...sidebarProps}>`

    3. Keep ALL existing sidebar content unchanged -- nav groups, placeholder items, admin conditional rendering, NavUser in footer, SidebarRail, branding header. Do NOT restructure navigation.

    **SiteHeader.tsx changes:**

    1. Update the header className to use CSS variable height and transition classes matching dashboard-01:

    Current: `className="flex h-12 shrink-0 items-center border-b bg-background px-4 gap-2"`

    New: `className="flex h-(--header-height) shrink-0 items-center gap-2 border-b transition-[width,height] ease-linear group-has-data-[collapsible=icon]/sidebar-wrapper:h-(--header-height)"`

    Key changes:
    - `h-12` becomes `h-(--header-height)` -- uses the CSS variable set by SidebarProvider
    - Add `transition-[width,height] ease-linear` for smooth collapse transitions
    - Add `group-has-data-[collapsible=icon]/sidebar-wrapper:h-(--header-height)` for responsive behavior when sidebar collapses
    - Remove `bg-background` (SidebarInset handles the background)
    - Keep `px-4` for horizontal padding

    2. Keep ALL existing SiteHeader content unchanged -- breadcrumbs, language toggle, user dropdown, route parsing logic.

    **IMPORTANT:** Use Tailwind v4 parentheses syntax for CSS custom properties: `h-(--header-height)`, NOT `h-[var(--header-height)]`. The project uses Tailwind v4 and the recent commit 3800c65 specifically fixed this syntax.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>AppSidebar uses collapsible="offcanvas" and accepts variant prop. SiteHeader uses h-(--header-height) instead of h-12. Sidebar shows inset visual treatment (muted background, bordered content area).</manual>
  </verify>
  <done>AppSidebar uses collapsible="offcanvas" and accepts variant="inset" from parent. SiteHeader uses h-(--header-height) CSS variable with transition classes. Dashboard has inset visual treatment matching dashboard-01 reference.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in frontend/
2. `npm run build` succeeds
3. AppShell.tsx contains `--sidebar-width` and `--header-height` in SidebarProvider style prop
4. AppSidebar.tsx has `collapsible="offcanvas"` (not "icon")
5. AppSidebar.tsx accepts variant prop (receives "inset" from parent)
6. SiteHeader.tsx uses `h-(--header-height)` (not `h-12`)
7. Content area has `@container/main` class
8. All existing functionality preserved: breadcrumbs, language toggle, user dropdown, session warning, admin conditional sidebar, active route detection
</verification>

<success_criteria>
- Dashboard layout matches dashboard-01 reference: inset sidebar with muted background, bordered/rounded content area with gaps
- SidebarProvider sets CSS custom properties for sidebar width and header height
- Sidebar uses offcanvas collapsible behavior and inset variant
- Header height is driven by CSS variable, not hardcoded
- Container query support via @container/main on content wrapper
- No regressions in any existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-06-SUMMARY.md`
</output>
