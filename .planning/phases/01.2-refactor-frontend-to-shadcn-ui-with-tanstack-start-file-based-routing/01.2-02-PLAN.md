---
phase: 01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 01.2-01b
files_modified:
  - frontend/src/shared/components/AppShell.tsx
  - frontend/src/shared/components/AppSidebar.tsx
  - frontend/src/shared/components/SiteHeader.tsx
  - frontend/src/shared/components/NavUser.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - AUTH-04

must_haves:
  truths:
    - "Dashboard layout uses SidebarProvider + SidebarInset with collapsible sidebar"
    - "Sticky header shows breadcrumbs, language toggle, and user dropdown menu"
    - "Sidebar displays placeholder navigation items for future phases as disabled/coming-soon"
    - "TopBar.tsx is removed -- replaced by SiteHeader.tsx"
    - "All existing functionality preserved: sidebar collapse, active route highlighting, admin section conditional rendering, user dropdown with logout, language toggle, session warning modal"
  artifacts:
    - path: "frontend/src/shared/components/SiteHeader.tsx"
      provides: "Dashboard header with breadcrumbs, language toggle, user dropdown"
    - path: "frontend/src/shared/components/NavUser.tsx"
      provides: "User dropdown menu component for sidebar footer or header"
    - path: "frontend/src/shared/components/AppSidebar.tsx"
      provides: "Dashboard-01 sidebar with nav groups and placeholder items"
    - path: "frontend/src/shared/components/AppShell.tsx"
      provides: "Dashboard shell with SidebarProvider + SidebarInset"
  key_links:
    - from: "frontend/src/shared/components/AppShell.tsx"
      to: "frontend/src/shared/components/AppSidebar.tsx"
      via: "renders AppSidebar inside SidebarProvider"
      pattern: "<AppSidebar"
    - from: "frontend/src/shared/components/AppShell.tsx"
      to: "frontend/src/shared/components/SiteHeader.tsx"
      via: "renders SiteHeader inside SidebarInset"
      pattern: "<SiteHeader"
    - from: "frontend/src/shared/components/SiteHeader.tsx"
      to: "frontend/src/shared/components/LanguageToggle.tsx"
      via: "renders language toggle in header"
      pattern: "<LanguageToggle"
    - from: "frontend/src/shared/components/AppSidebar.tsx"
      to: "frontend/src/shared/components/NavUser.tsx"
      via: "renders NavUser in SidebarFooter (dashboard-01 convention) or SiteHeader renders NavUser in header"
      pattern: "<NavUser"
---

<objective>
Replace the current dashboard layout (AppShell + TopBar + AppSidebar) with the dashboard-01 pattern adapted for TanStack Start. Create SiteHeader with breadcrumbs, adapt sidebar with placeholder navigation items for future phases.

Purpose: The user locked the decision to replace AppShell entirely with dashboard-01's layout. This plan implements the sidebar + header pattern from dashboard-01, preserving all existing functionality (session warning, language toggle, user dropdown, permission-based sidebar, active route detection).

Output: New dashboard shell matching dashboard-01 pattern, TopBar replaced by SiteHeader, sidebar enhanced with future-phase placeholder items.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-RESEARCH.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-01b-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SiteHeader and NavUser components, replace TopBar</name>
  <files>
    frontend/src/shared/components/SiteHeader.tsx
    frontend/src/shared/components/NavUser.tsx
  </files>
  <action>
    Create SiteHeader.tsx adapted from dashboard-01's site-header pattern. Structure:
    - Sticky header with `flex h-12 shrink-0 items-center border-b bg-background px-4 gap-2`
    - Left side: `SidebarTrigger` (from Sidebar wrapper) + vertical `Separator` + `Breadcrumb`
    - Right side (flex-1 spacer then): `LanguageToggle` + user dropdown
    - Breadcrumb: Use `useRouterState()` from TanStack Router to read `location.pathname`, split into segments, map to i18n labels. Start with simple current-page breadcrumb (e.g., "Dashboard", "Admin > Users"). Use `useTranslation("common")` for sidebar.* i18n keys.
    - Import all UI components from wrappers (Sidebar, Separator, Breadcrumb, etc.), never from ui/ directly.

    Create NavUser.tsx as a standalone user dropdown component (extracted from current TopBar logic):
    - Takes no props (reads user from authStore, logout from useAuth)
    - Renders avatar fallback (user initials or icon) + dropdown menu with user email label + logout action
    - Use Avatar wrapper component for user avatar display
    - Use DropdownMenu wrapper for the menu

    Delete TopBar.tsx after SiteHeader.tsx is working and imported in AppShell.

    Use @tabler/icons-react for all icons (project convention): IconUser, IconLogout, IconChevronsUpDown for trigger.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>SiteHeader renders with breadcrumbs, language toggle, and user dropdown. TopBar.tsx is deleted.</manual>
  </verify>
  <done>SiteHeader.tsx replaces TopBar.tsx. Breadcrumb shows current page. Language toggle and user dropdown are in the header. NavUser.tsx extracts user dropdown logic.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild AppSidebar with dashboard-01 pattern and placeholder nav items</name>
  <files>
    frontend/src/shared/components/AppSidebar.tsx
    frontend/src/shared/components/AppShell.tsx
  </files>
  <action>
    Rebuild AppSidebar.tsx following dashboard-01's sidebar structure:
    - Keep `Sidebar collapsible="icon"` with `SidebarRail`
    - SidebarHeader: Ganka28 branding (existing "G" logo block + app name) -- keep current pattern
    - SidebarContent with nav groups:
      - **Main group** (no label): Dashboard (active, links to /dashboard)
      - **Clinic group** (label: "Clinic"): Placeholder items for future phases, derived from roadmap:
        - Patients (Phase 2) -- disabled, tooltip "Coming soon"
        - Appointments (Phase 2) -- disabled, tooltip "Coming soon"
        - Clinical (Phase 3) -- disabled, tooltip "Coming soon"
        - Imaging (Phase 4) -- disabled, tooltip "Coming soon"
        - Prescriptions (Phase 5) -- disabled, tooltip "Coming soon"
      - **Operations group** (label: "Operations"): Placeholder items:
        - Pharmacy (Phase 6) -- disabled, tooltip "Coming soon"
        - Billing (Phase 7) -- disabled, tooltip "Coming soon"
        - Optical (Phase 8) -- disabled, tooltip "Coming soon"
        - Treatments (Phase 9) -- disabled, tooltip "Coming soon"
      - **Admin group** (label: t("sidebar.admin"), conditional on hasAdminAccess): Users, Roles, Audit Logs (existing active items)
    - SidebarFooter: Optionally move NavUser here (dashboard-01 pattern has user in footer) -- or keep in header. Claude's discretion per dashboard-01 convention.
    - Disabled items: Use `SidebarMenuButton` with `disabled` attribute and opacity-50 styling. Use appropriate @tabler/icons-react icons for each:
      - Patients: IconUsers, Appointments: IconCalendar, Clinical: IconStethoscope, Imaging: IconCamera, Prescriptions: IconPrescription or IconPill
      - Pharmacy: IconMedicineSyrup, Billing: IconReceipt, Optical: IconGlasses, Treatments: IconHeartbeat
    - Keep active route detection: `currentPath === item.to || currentPath.startsWith(item.to + "/")`
    - Keep admin section conditional rendering based on Auth.Manage/Auth.View permissions
    - All imports from wrappers, not ui/

    Update AppShell.tsx:
    - Import SiteHeader instead of TopBar
    - Keep SessionWarningModal rendering
    - Keep SidebarProvider + SidebarInset structure (already matches dashboard-01)
    - Add `className="flex-1 p-4 md:p-6"` on main element (already present)
    - Remove TopBar import

    Use i18n keys for all new sidebar labels. Add new keys to both vi.json and en.json translation files:
    - sidebar.clinic, sidebar.operations, sidebar.patients, sidebar.appointments, sidebar.clinical, sidebar.imaging, sidebar.prescriptions, sidebar.pharmacy, sidebar.billing, sidebar.optical, sidebar.treatments, sidebar.comingSoon
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Sidebar shows grouped navigation with disabled placeholder items. Admin section still conditional. Dashboard still accessible.</manual>
  </verify>
  <done>AppSidebar uses dashboard-01 pattern with grouped navigation. Placeholder items for future phases shown as disabled with appropriate icons. AppShell uses SiteHeader. TopBar.tsx deleted. All i18n keys added.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in frontend/
2. TopBar.tsx no longer exists
3. SiteHeader.tsx exists with breadcrumbs, LanguageToggle, and user dropdown
4. AppSidebar.tsx shows placeholder nav items for Phases 2-9 as disabled
5. Admin section in sidebar still conditionally rendered based on permissions
6. Session warning modal still works (rendered in AppShell)
</verification>

<success_criteria>
- Dashboard shell matches dashboard-01 layout pattern (sidebar + sticky header + inset content)
- All existing functionality preserved (collapse, active routes, admin conditional, logout, language toggle, session warning)
- Placeholder navigation items for future phases are visible but disabled
- All translations present in both vi.json and en.json
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-02-SUMMARY.md`
</output>
