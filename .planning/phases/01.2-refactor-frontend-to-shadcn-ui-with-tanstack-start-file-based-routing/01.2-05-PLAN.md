---
phase: 01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing
plan: 05
type: execute
wave: 3
depends_on:
  - 01.2-02
  - 01.2-03
  - 01.2-04
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - AUTH-01
  - AUTH-04
  - AUD-01

must_haves:
  truths:
    - "All shadcn/ui patterns are applied consistently across the entire frontend"
    - "No direct ui/ imports exist outside wrapper files"
    - "Dashboard layout, login page, forms, tables, and session warning all use new patterns"
    - "All existing functionality works end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 01.2 refactoring -- dashboard layout, login page, wrapper pattern, forms, tables, and session warning modal -- all work correctly together.

Purpose: End-to-end verification that all locked user decisions are implemented and no existing functionality was lost during the refactoring.

Output: Verification results confirming all shadcn/ui patterns are applied and working.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-RESEARCH.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-02-SUMMARY.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-03-SUMMARY.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <action>
    Run all automated verification checks:

    1. TypeScript compilation:
    ```bash
    cd frontend && npx tsc --noEmit
    ```

    2. Wrapper pattern enforcement -- no direct ui/ imports outside wrapper files:
    ```bash
    grep -rn "from \"@/shared/components/ui/" frontend/src/ --include="*.tsx" --include="*.ts" | grep -v "src/shared/components/ui/" | grep -v "src/shared/components/[A-Z][a-zA-Z]*\.tsx"
    ```
    Must return empty.

    3. No old form patterns remain:
    ```bash
    # No register() in form components (should use Controller)
    grep -rn "\.register(" frontend/src/features/ --include="*.tsx"
    # No inline error <p> elements
    grep -rn "text-sm text-destructive" frontend/src/features/ --include="*.tsx"
    ```
    Both should return empty.

    4. No Dialog used for non-dismissible modals:
    ```bash
    grep -rn "hideCloseButton" frontend/src/ --include="*.tsx"
    grep -rn "onEscapeKeyDown" frontend/src/ --include="*.tsx"
    ```
    Both should return empty.

    5. DataTable is used by all tables:
    ```bash
    grep -rn "import.*DataTable" frontend/src/features/ --include="*.tsx"
    ```
    Should show imports in UserTable, RoleTable, AuditLogTable.

    6. Build check:
    ```bash
    cd frontend && npm run build 2>&1 | tail -5
    ```

    7. Verify wrapper count matches ui/ count:
    ```bash
    ls frontend/src/shared/components/ui/*.tsx | wc -l
    ls frontend/src/shared/components/[A-Z]*.tsx | wc -l
    ```
    Wrapper count should match ui/ count (excluding AppShell, AppSidebar, SiteHeader, NavUser, LanguageToggle, DataTable which are layout/feature components, not wrappers).

    Report all results.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>TypeScript compiles. Build succeeds. No direct ui/ imports. No old form patterns. No Dialog workarounds. DataTable used by all tables.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete shadcn/ui refactoring</name>
  <action>Human verifies all visual and functional aspects of the refactoring.</action>
  <what-built>Complete shadcn/ui refactoring: dashboard-01 layout with sidebar and sticky header, login-04 two-column login page, wrapper component pattern, Field+Controller forms, DataTable for tables, AlertDialog for session warning</what-built>
  <how-to-verify>
    1. Start the frontend dev server: `cd frontend && npm run dev`
    2. Visit the login page:
       - [ ] Two-column layout visible (form left, branding right on desktop)
       - [ ] Language toggle in top-right of form panel
       - [ ] Username field (not email)
       - [ ] No social login buttons
       - [ ] Form validation shows accessible error messages
    3. Log in with valid credentials:
       - [ ] Dashboard loads with sidebar + sticky header
       - [ ] Sidebar shows Ganka28 branding in header
       - [ ] Sidebar shows Dashboard (active), placeholder items (disabled), Admin section (if admin)
       - [ ] Sticky header has: sidebar toggle, breadcrumb, language toggle, user dropdown
       - [ ] Sidebar collapses to icon-only mode
    4. Navigate to Admin > Users:
       - [ ] User table renders with data (using DataTable)
       - [ ] Create user dialog opens with Field+Controller form
       - [ ] Validation errors display properly
    5. Navigate to Admin > Roles:
       - [ ] Role table renders, clicking row shows permission matrix
       - [ ] Create role dialog uses Field+Controller form
    6. Navigate to Admin > Audit Logs:
       - [ ] Audit log table with expandable rows
       - [ ] Filters work (date, action type, user)
       - [ ] Pagination works
    7. Wait for session timeout warning (or set short timeout):
       - [ ] AlertDialog appears (not regular Dialog)
       - [ ] Cannot dismiss with ESC or click outside
       - [ ] Countdown timer visible
       - [ ] Extend and Logout buttons work
    8. Switch language (VI/EN):
       - [ ] All new sidebar labels translate correctly
       - [ ] Login page translates
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All automated checks pass. Visual verification confirms all shadcn/ui patterns applied correctly.
</verification>

<success_criteria>
- All automated checks pass (TypeScript, build, grep checks)
- Human verification confirms visual correctness and functional completeness
- No regression in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-05-SUMMARY.md`
</output>
