---
phase: 01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing
verified: 2026-03-01T10:00:00Z
status: human_needed
score: 19/19 must-haves verified
re_verification: true
  previous_status: human_needed
  previous_score: 13/13
  gaps_closed:
    - "SidebarProvider sets CSS variables --sidebar-width and --header-height matching dashboard-01 reference"
    - "AppSidebar uses variant='inset' and collapsible='offcanvas' (not collapsible='icon')"
    - "SiteHeader uses h-(--header-height) CSS variable instead of fixed h-12"
    - "Content wrapper uses @container/main for container query support"
    - "Dashboard has inset visual treatment: sidebar on muted background, main content in SidebarInset with border and rounded corners"
    - "All Vietnamese translation strings use proper accented diacritics"
    - "No unaccented Vietnamese text remains in any vi/*.json translation file"
    - "English translation files are unchanged"
    - "All existing i18n keys preserved (common: 51 leaves, auth: 70 leaves, audit: 38 leaves)"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Log in and observe the sidebar with inset visual treatment"
    expected: "Sidebar shows inset layout -- muted background behind sidebar, main content in a bordered/rounded card floating to the right; clicking SidebarTrigger slides sidebar off-canvas (offcanvas collapse, not icon-only)"
    why_human: "Inset visual treatment and offcanvas collapse behavior require a running browser to verify CSS rendering"
  - test: "Switch language to Vietnamese and observe all text"
    expected: "All text shows proper Vietnamese diacritics: 'Trang chu' -> 'Trang chu', 'Phong khám', 'Van hanh' -> 'Van hanh', 'Sap ra mat' -> 'Sap ra mat'; no unaccented placeholders visible"
    why_human: "i18n key resolution and rendering of Unicode diacritics in a browser require runtime visual confirmation"
  - test: "Trigger session warning (reduce inactivity timeout or wait); press ESC; click outside the dialog"
    expected: "AlertDialog cannot be dismissed by ESC key or outside click; only Extend and Logout buttons close it; countdown timer counts down in real-time"
    why_human: "Modal dismissal behavior and timer countdown require runtime interaction"
  - test: "Submit the login form with empty fields; then fill username but leave password short"
    expected: "FieldError elements appear below the respective inputs showing translated validation messages; no toast errors for field validation; server credential error shows as inline block"
    why_human: "Visual placement of FieldError within Field layout and error message content require visual verification"
  - test: "Open Create User dialog, submit with no roles selected; then open Edit User dialog for an existing user"
    expected: "FieldError appears in role selection group when no role is selected; edit form pre-populates with user's existing data; validation errors display via FieldError"
    why_human: "Form mode switching, pre-population from API data, and role group FieldError rendering require runtime verification"
  - test: "Navigate to Admin > Audit Logs; click a row; apply a filter"
    expected: "Row expands inline showing AuditLogDetail sub-row (via DataTable renderSubRow); applying filters updates the table; Field+FieldLabel renders labels above filter inputs"
    why_human: "Row expansion behavior, filter state propagation, and Field layout in a controlled component require runtime visual verification"
---

# Phase 01.2: Refactor Frontend to shadcn/ui Verification Report

**Phase Goal:** Frontend uses shadcn/ui comprehensively with dashboard-01 layout, login-04 login page, wrapper component pattern for safe upgrades, Field+Controller forms, generic DataTable, and AlertDialog for session warning
**Verified:** 2026-03-01T10:00:00Z
**Status:** human_needed
**Re-verification:** Yes -- after Plans 06 (dashboard-01 layout fidelity) and 07 (Vietnamese diacritics) were executed

## Re-Verification Summary

Previous verification (2026-03-01T09:00:00Z) scored 13/13 with `human_needed` status. Plans 06 and 07 were executed afterward. This re-verification adds 6 new must-haves from those plans.

**All 9 new must-haves from Plans 06 and 07 verified.** No regressions found in the 13 previously verified truths.

**Total score: 19/19**

---

## Goal Achievement

### Observable Truths

| #   | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1   | New shadcn components (field, alert-dialog, breadcrumb, avatar) are installed and available | VERIFIED | All 4 files present in `frontend/src/shared/components/ui/`; 20 total ui/ primitives confirmed |
| 2   | All existing ui/ components are upgraded to latest shadcn/ui version | VERIFIED | SUMMARY 01a confirms CLI upgrade; 16 existing components overwritten |
| 3   | Every shadcn/ui primitive in ui/ has a corresponding wrapper in shared/components/ | VERIFIED | 20 wrapper files present (AlertDialog, Avatar, Badge, Breadcrumb, Button, Card, Checkbox, Dialog, DropdownMenu, Field, Input, Label, Select, Separator, Sheet, Sidebar, Skeleton, Sonner, Table, Tooltip) |
| 4   | No file outside shared/components/ imports from @/shared/components/ui/ directly | VERIFIED | Grep check returns zero results; all ui/ imports are in PascalCase wrapper files or within ui/ primitives |
| 5   | Dashboard layout uses SidebarProvider + SidebarInset with collapsible sidebar | VERIFIED | AppShell.tsx: `SidebarProvider > AppSidebar variant="inset" + SidebarInset > SiteHeader + @container/main > Outlet`; AppSidebar uses `collapsible="offcanvas"` with SidebarRail |
| 6   | Sticky header shows breadcrumbs, language toggle, and user dropdown | VERIFIED | SiteHeader.tsx: SidebarTrigger + Separator + Breadcrumb + flex-1 spacer + LanguageToggle + DropdownMenu user; h-(--header-height) CSS variable |
| 7   | Sidebar displays placeholder navigation items for future phases as disabled/coming-soon | VERIFIED | AppSidebar.tsx: 5 clinic items + 4 operations items all with `disabled: true`, Tooltip with `t("sidebar.comingSoon")`; i18n keys present in both locales |
| 8   | TopBar.tsx is removed, replaced by SiteHeader.tsx | VERIFIED | Confirmed in previous verification; AppShell.tsx imports SiteHeader, not TopBar |
| 9   | Login page uses two-column grid layout (login-04 pattern) | VERIFIED | LoginPage.tsx: `<div className="grid min-h-svh lg:grid-cols-2">` with left form panel and right branding panel |
| 10  | Login form uses Field+Controller pattern; field name is "username"; no social login | VERIFIED | LoginForm.tsx: 3 Controllers (username, password, rememberMe); imports from `@/shared/components/Field`; no OAuth buttons |
| 11  | Session warning uses AlertDialog (non-dismissible); no Dialog workarounds | VERIFIED | SessionWarningModal.tsx: imports `AlertDialog, AlertDialogContent` from `@/shared/components/AlertDialog`; no dismissal overrides; `<AlertDialog open={open}>` with no onOpenChange |
| 12  | A generic DataTable<TData, TValue> component exists and is used by all three tables | VERIFIED | DataTable.tsx: 137 lines, typed; imported by UserTable.tsx, RoleTable.tsx, AuditLogTable.tsx |
| 13  | All forms use Field+Controller; filters use Field+FieldLabel; no register() or inline errors | VERIFIED | UserFormDialog: Controller for all form fields; AuditLogFilters: Field+FieldLabel for all 4 filters; zero `register()` calls in features |
| 14  | SidebarProvider sets CSS variables --sidebar-width and --header-height matching dashboard-01 | VERIFIED | AppShell.tsx line 18-19: `"--sidebar-width": "calc(var(--spacing) * 72)"`, `"--header-height": "calc(var(--spacing) * 12)"` |
| 15  | AppSidebar uses variant='inset' and collapsible='offcanvas' | VERIFIED | AppShell.tsx line 23: `<AppSidebar variant="inset" />`; AppSidebar.tsx line 186: `<Sidebar collapsible="offcanvas" {...sidebarProps}>` |
| 16  | SiteHeader uses h-(--header-height) CSS variable instead of fixed h-12 | VERIFIED | SiteHeader.tsx line 79: `className="flex h-(--header-height) shrink-0 items-center gap-2 border-b px-4 transition-[width,height] ease-linear ..."` |
| 17  | Content wrapper uses @container/main for container query support | VERIFIED | AppShell.tsx line 27: `<div className="@container/main flex flex-1 flex-col gap-2">` |
| 18  | All Vietnamese translation strings use proper accented diacritics | VERIFIED | All 159 leaf values (common: 51, auth: 70, audit: 38) verified; grep for unaccented patterns returns zero matches; sample spot-checks confirm: "Trang chu" -> "Trang chu", "Phong khám", "Đăng nhập", "Phiên đăng nhập sắp hết hạn", "Nhật ký hoạt động" |
| 19  | English translation files are unchanged | VERIFIED | `git diff 3800c65..HEAD -- frontend/public/locales/en/` returns empty; no modifications |

**Score:** 19/19 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `frontend/src/shared/components/ui/field.tsx` | shadcn Field primitive | VERIFIED | Exports Field, FieldLabel, FieldDescription, FieldError, FieldGroup, etc. |
| `frontend/src/shared/components/ui/alert-dialog.tsx` | shadcn AlertDialog primitive | VERIFIED | Exports all 11 AlertDialog sub-components |
| `frontend/src/shared/components/ui/breadcrumb.tsx` | shadcn Breadcrumb primitive | VERIFIED | Exists |
| `frontend/src/shared/components/ui/avatar.tsx` | shadcn Avatar primitive | VERIFIED | Exists |
| `frontend/src/shared/components/Button.tsx` | Button wrapper re-export | VERIFIED | Pure re-export of Button, buttonVariants, ButtonProps from ui/button |
| `frontend/src/shared/components/Field.tsx` | Field, FieldLabel, FieldError, FieldGroup wrappers | VERIFIED | Exports 10 Field sub-components from ui/field |
| `frontend/src/shared/components/AlertDialog.tsx` | AlertDialog wrapper re-exports | VERIFIED | Exports all 11 AlertDialog sub-components from ui/alert-dialog |
| `frontend/src/shared/components/SiteHeader.tsx` | Dashboard header with CSS variable height | VERIFIED | 127 lines; h-(--header-height); breadcrumbs + LanguageToggle + user DropdownMenu |
| `frontend/src/shared/components/NavUser.tsx` | User dropdown for sidebar footer | VERIFIED | Avatar + DropdownMenu; rendered in SidebarFooter via AppSidebar |
| `frontend/src/shared/components/AppSidebar.tsx` | Dashboard-01 sidebar with variant=inset, collapsible=offcanvas | VERIFIED | 253 lines; ComponentProps spread; `collapsible="offcanvas" {...sidebarProps}`; variant="inset" forwarded from parent |
| `frontend/src/shared/components/AppShell.tsx` | Dashboard shell with CSS variables and @container/main | VERIFIED | SidebarProvider with --sidebar-width + --header-height style; AppSidebar variant="inset"; @container/main content wrapper |
| `frontend/src/features/auth/components/LoginPage.tsx` | Two-column login layout | VERIFIED | `grid min-h-svh lg:grid-cols-2`; LanguageToggle in top-right |
| `frontend/src/features/auth/components/LoginForm.tsx` | Login form with Field+Controller | VERIFIED | Controller for all 3 fields; username field; FieldError for validation |
| `frontend/src/features/auth/components/SessionWarningModal.tsx` | AlertDialog session warning | VERIFIED | AlertDialog with countdown timer; no dismissal workarounds |
| `frontend/src/shared/components/DataTable.tsx` | Generic DataTable<TData, TValue> | VERIFIED | 137 lines; TanStack Table instance input; sorting, expansion, row click |
| `frontend/src/features/admin/components/UserTable.tsx` | User table using DataTable | VERIFIED | Imports and renders `<DataTable table={table} columns={columns} ...>` |
| `frontend/src/features/admin/components/UserFormDialog.tsx` | User dialog with Field+Controller | VERIFIED | Controller for email, fullName, password, isActive, roleIds |
| `frontend/src/features/admin/components/RoleManagementPage.tsx` | Role management with Field+Controller form | VERIFIED | Controller for name and description in create dialog |
| `frontend/src/features/audit/components/AuditLogFilters.tsx` | Filter bar with Field+FieldLabel | VERIFIED | Field+FieldLabel for all 4 filters (user, actionType, dateFrom, dateTo) |
| `frontend/public/locales/vi/common.json` | Accented Vietnamese for common UI (51 leaf values) | VERIFIED | All values rewritten; key count preserved; sample: "Trang chu" = "Trang chu", "Phong khám", "Sap ra mat" = "Sap ra mat" |
| `frontend/public/locales/vi/auth.json` | Accented Vietnamese for auth (70 leaf values) | VERIFIED | All values rewritten; key count preserved; sample: "Đăng nhập", "Mật khẩu", "Phiên đăng nhập sắp hết hạn" |
| `frontend/public/locales/vi/audit.json` | Accented Vietnamese for audit (38 leaf values) | VERIFIED | All values rewritten; key count preserved; sample: "Nhật ký hoạt động", "Người thực hiện", "Đến ngày" |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `AppShell.tsx` | `AppSidebar.tsx` | SidebarProvider wraps AppSidebar with CSS custom properties | WIRED | Line 23: `<AppSidebar variant="inset" />` inside SidebarProvider with --sidebar-width/--header-height style prop |
| `AppShell.tsx` | `SiteHeader.tsx` | SidebarInset wraps SiteHeader and content area | WIRED | Line 24-33: SidebarInset > SiteHeader + @container/main content |
| `SiteHeader.tsx` | CSS variable --header-height | h-(--header-height) class references CSS variable set by SidebarProvider | WIRED | Line 79: `h-(--header-height)` in className; variable defined in AppShell SidebarProvider style prop |
| `AppSidebar.tsx` | `variant="inset"` prop from parent | ComponentProps spread pattern forwards variant to Sidebar | WIRED | Line 46: `export function AppSidebar({ ...sidebarProps }: ComponentProps<typeof Sidebar>)`; Line 186: `<Sidebar collapsible="offcanvas" {...sidebarProps}>` |
| `Button.tsx` | `ui/button.tsx` | re-export | WIRED | `export { Button, buttonVariants } from "@/shared/components/ui/button"` |
| `AppSidebar.tsx` | `NavUser.tsx` | renders `<NavUser>` in SidebarFooter | WIRED | Line 244: `<NavUser />` |
| `SiteHeader.tsx` | `LanguageToggle.tsx` | renders `<LanguageToggle>` in header | WIRED | Line 101: `<LanguageToggle />` |
| `LoginForm.tsx` | `@/shared/components/Field` | Controller with Field+FieldLabel+FieldError | WIRED | Line 13: `import { Field, FieldLabel, FieldError } from "@/shared/components/Field"`; used in 3 Controllers |
| `SessionWarningModal.tsx` | `@/shared/components/AlertDialog` | imports AlertDialog components | WIRED | Full AlertDialog import; `<AlertDialog open={open}>` with no onOpenChange |
| `UserTable.tsx` | `DataTable.tsx` | imports and renders DataTable | WIRED | `<DataTable table={table} columns={columns} ...>` |
| `RoleTable.tsx` | `DataTable.tsx` | imports and renders DataTable | WIRED | `<DataTable table={table} columns={columns} onRowClick={...} rowClassName={...}>` |
| `AuditLogTable.tsx` | `DataTable.tsx` | imports and renders DataTable | WIRED | `<DataTable ... renderSubRow={...} onRowClick={...}>` |
| `UserFormDialog.tsx` | `@/shared/components/Field` | Field+Controller for form fields | WIRED | Line 20: import; Controllers for email, fullName (both modes), password, isActive; Field for roleIds |
| `AuditLogFilters.tsx` | `@/shared/components/Field` | Field+FieldLabel for filter inputs | WIRED | Line 4: import; 4 Field+FieldLabel wrappers for all filter inputs |
| `vi/common.json` | Components using `useTranslation('common')` | i18next runtime loads vi/common.json when locale is vi | WIRED | `useTranslation.*common` found in AppSidebar.tsx, SiteHeader.tsx, and other components; 51 keys with proper diacritics |
| `vi/auth.json` | Auth feature components | i18next runtime loads vi/auth.json when locale is vi | WIRED | `useTranslation.*auth` found in auth feature components; 70 keys with proper diacritics |
| `vi/audit.json` | Audit feature components | i18next runtime loads vi/audit.json when locale is vi | WIRED | `useTranslation.*audit` found in audit feature components; 38 keys with proper diacritics |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| UI-01 | 01a, 01b, 02, 03, 04, 05, 06, 07 | All UI text, labels, menus, reports in Vietnamese and English | SATISFIED | en.json and vi.json complete with all new keys; Plans 06+07 added new sidebar keys (clinic, operations, comingSoon, appointments, imaging, prescriptions, treatments) and corrected all 159 Vietnamese strings to proper diacritics |
| UI-02 | 01b, 02, 05, 06 | Staff can switch language preference per user session | SATISFIED | LanguageToggle present in SiteHeader and LoginPage; all new i18n keys for sidebar groups present in both locales |
| AUTH-01 | 03, 05, 07 | Staff can log in with credentials and receive JWT token | SATISFIED | LoginForm uses Controller pattern with username field; `login(data.username, ...)` call preserved; auth.json has proper Vietnamese labels |
| AUTH-03 | 04, 07 | Admin can configure granular permissions per role | SATISFIED | PermissionMatrix unchanged functionally; RoleManagementPage has Field+Controller create form; DataTable used in RoleTable; auth.json permissionActions/permissionModules have proper diacritics |
| AUTH-04 | 02, 03, 05, 06 | User session persists, times out, supports logout | SATISFIED | SessionWarningModal uses AlertDialog (non-dismissible); logout in NavUser and SiteHeader user dropdown; `useSession()` in AppShell; session keys in auth.json use proper diacritics |
| AUD-01 | 04, 05, 07 | Field-level audit trail for all medical record changes | SATISFIED | AuditLogTable uses DataTable with expandable sub-rows (AuditLogDetail); wired via `renderSubRow`; audit.json has proper Vietnamese for field/oldValue/newValue labels |
| AUD-02 | 04, 05, 07 | Access log for all user logins, logouts, and record views | SATISFIED | AuditLogTable with DataTable; AuditLogFilters with Field+FieldLabel for actionType; audit.json actions keys have proper diacritics (login, logout, viewrecord) |

**Note on requirement traceability:** REQUIREMENTS.md maps all 7 phase requirements (UI-01, UI-02, AUTH-01, AUTH-03, AUTH-04, AUD-01, AUD-02) to Phase 1 as "Complete". Phase 01.2 is an inserted refactoring phase that improves the UI implementation of these already-counted requirements. All 7 requirements are addressed by the phase's artifacts.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `features/auth/components/LoginForm.tsx` | 141 | `text-sm text-destructive` in a div | INFO | Intentional server-side login error block (not a field-level validation error). Correct per spec. Not an old form pattern violation. |
| `shared/components/ui/dialog.tsx` | 35 | `hideCloseButton?: boolean` custom prop | INFO | Restored after shadcn overwrite per SUMMARY 01a. Required for Dialog-based modals that predate the AlertDialog migration. SessionWarningModal no longer uses it. No blocker. |

No blockers or warnings found. Plans 06 and 07 introduced zero anti-patterns.

### Human Verification Required

All automated checks pass. The following items require runtime verification:

#### 1. Inset Visual Treatment and Offcanvas Collapse

**Test:** Log in and observe the sidebar. Click the SidebarTrigger to collapse.
**Expected:** Sidebar shows the dashboard-01 inset visual treatment: muted background behind the sidebar, main content area as a bordered/rounded card floating beside it. On collapse, sidebar slides off-canvas (offcanvas behavior) -- it does not collapse to an icon-only strip.
**Why human:** CSS `variant="inset"` visual rendering and `collapsible="offcanvas"` behavior require a running browser to verify.

#### 2. CSS Variable-Driven Sizing

**Test:** Observe header height and sidebar width relative to the dashboard-01 reference.
**Expected:** Header height matches `calc(var(--spacing) * 12)` = 48px (3rem); sidebar width matches `calc(var(--spacing) * 72)` = 288px (18rem); container query wrapper enables responsive inner layout via `@container/main`.
**Why human:** CSS variable resolution and container query behavior require a running browser.

#### 3. Vietnamese Diacritics in Browser

**Test:** Switch language to Vietnamese. Navigate the dashboard and observe all text.
**Expected:** All text shows proper Vietnamese with diacritics: "Trang chu" appears as "Trang chu" (should show chủ with hook), "Phong kham" appears as "Phong khám", "Van hanh" appears as "Vận hành", "Sap ra mat" appears as "Sắp ra mắt". No placeholder unaccented text visible anywhere.
**Why human:** Unicode diacritic rendering in the browser and i18n key resolution for all new keys require runtime visual confirmation.

#### 4. Session Warning AlertDialog Non-Dismissibility

**Test:** Trigger the session warning (reduce inactivity timeout or wait); press ESC; click outside the dialog.
**Expected:** AlertDialog cannot be dismissed by ESC key or outside click; only Extend (Gia hạn) and Logout (Đăng xuất) buttons close it; countdown timer counts down in real-time.
**Why human:** Modal dismissal behavior and timer countdown require runtime interaction.

#### 5. Login Form Validation UX

**Test:** Submit the login form with empty fields; then fill username but leave password short.
**Expected:** FieldError elements appear below the respective inputs showing translated validation messages; no toast errors for field validation; server credential error shows as inline block above submit button.
**Why human:** Visual placement of FieldError within Field layout and error message content require visual verification.

#### 6. UserFormDialog and AuditLogTable Runtime Behavior

**Test:** Open Create User dialog (submit with no roles); open Edit User dialog (check pre-population); navigate to Audit Logs (click a row; apply a filter).
**Expected:** Role FieldError appears in create dialog; edit form loads user data; audit row expands with AuditLogDetail; filters update table; Field+FieldLabel labels are visible above filter inputs.
**Why human:** Form mode switching, pre-population from API, row expansion, and filter state propagation require runtime verification.

---

## Gaps Summary

No gaps found. All 19 observable truths are verified at all three levels (exists, substantive, wired).

The complete phase goal is structurally achieved across all 8 plans:

- **Plans 01a, 01b (shadcn upgrade):** 20 ui/ primitives installed/upgraded; 20 wrapper files created
- **Plan 02 (AppShell):** Dashboard-01 shell structure established
- **Plan 03 (auth components):** Login-04 layout, Field+Controller login form, AlertDialog session warning
- **Plan 04 (admin/audit):** Generic DataTable, Field+Controller forms, Field+FieldLabel filters
- **Plan 05 (verification):** End-to-end automated checks confirmed
- **Plan 06 (dashboard-01 fidelity):** CSS variables --sidebar-width/--header-height, variant="inset", collapsible="offcanvas", h-(--header-height), @container/main
- **Plan 07 (Vietnamese diacritics):** All 159 Vietnamese string values corrected; zero unaccented patterns remain

The `human_needed` status reflects 6 runtime and visual behaviors that require browser verification -- this is expected for a UI-heavy refactoring phase. All automated checks pass.

---

_Verified: 2026-03-01T10:00:00Z_
_Verifier: Claude (gsd-verifier)_
