---
phase: 01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing
verified: 2026-03-01T09:00:00Z
status: human_needed
score: 13/13 must-haves verified
re_verification: false
human_verification:
  - test: "Log in with valid credentials and navigate the dashboard"
    expected: "Sidebar collapses to icon-only mode with SidebarRail; breadcrumbs update on navigation; language toggle switches all labels; user dropdown shows avatar initials and works logout"
    why_human: "Visual rendering, collapsible sidebar behavior, and real-time route-based breadcrumb updates cannot be verified by static code analysis"
  - test: "Trigger session warning (reduce inactivity timeout or wait)"
    expected: "AlertDialog appears; cannot be dismissed by pressing ESC or clicking outside; countdown timer counts down; Extend and Logout buttons are functional"
    why_human: "Non-dismissibility of AlertDialog and timer countdown require runtime verification"
  - test: "Submit the login form with empty fields and then with invalid credentials"
    expected: "Field-level validation errors appear below each input (via FieldError); server error appears as inline block above submit button; no toast-only errors for validation"
    why_human: "Form validation rendering and error placement require visual verification"
  - test: "Open Admin > Users, click Create User, submit with empty fields, then edit an existing user"
    expected: "UserFormDialog shows Field+FieldError validation; edit form loads existing data; role checkbox group shows FieldError when no role selected"
    why_human: "Form state loading, validation layout, and dual create/edit mode require visual verification"
  - test: "Navigate to Admin > Audit Logs; apply filters; expand a row"
    expected: "AuditLogFilters shows Field+FieldLabel for all 4 filter inputs; table rows expand inline with AuditLogDetail; pagination Previous/Next work"
    why_human: "Filter UI consistency, row expansion, and pagination behavior require visual runtime verification"
  - test: "Switch language between VI and EN"
    expected: "All new sidebar group labels (Clinic, Operations, Coming soon tooltip), all form labels, dashboard breadcrumbs, and table headers change language; Vietnamese is the primary language"
    why_human: "Full bilingual coverage of new UI keys (sidebar.clinic, sidebar.operations, sidebar.comingSoon, etc.) requires visual runtime verification"
---

# Phase 01.2: Refactor Frontend to shadcn/ui Verification Report

**Phase Goal:** Frontend uses shadcn/ui comprehensively with dashboard-01 layout, login-04 login page, wrapper component pattern for safe upgrades, Field+Controller forms, generic DataTable, and AlertDialog for session warning
**Verified:** 2026-03-01T09:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth                                                                                              | Status     | Evidence                                                                                                     |
|----|----------------------------------------------------------------------------------------------------|------------|--------------------------------------------------------------------------------------------------------------|
| 1  | New shadcn components (field, alert-dialog, breadcrumb, avatar) are installed and available        | VERIFIED   | All 4 files present in `frontend/src/shared/components/ui/`; 20 total ui/ primitives confirmed               |
| 2  | All existing ui/ components are upgraded to latest shadcn/ui version                               | VERIFIED   | SUMMARY 01a confirms CLI upgrade; 16 existing components overwritten                                         |
| 3  | Every shadcn/ui primitive in ui/ has a corresponding wrapper in shared/components/                  | VERIFIED   | 20 wrapper files present (AlertDialog, Avatar, Badge, Breadcrumb, Button, Card, Checkbox, Dialog, DropdownMenu, Field, Input, Label, Select, Separator, Sheet, Sidebar, Skeleton, Sonner, Table, Tooltip) |
| 4  | No file outside shared/components/ imports from @/shared/components/ui/ directly                   | VERIFIED   | Grep check returns zero results; all ui/ imports are in PascalCase wrapper files or within ui/ primitives    |
| 5  | Dashboard layout uses SidebarProvider + SidebarInset with collapsible sidebar                      | VERIFIED   | AppShell.tsx uses `SidebarProvider > AppSidebar + SidebarInset > SiteHeader + main > Outlet`; AppSidebar uses `Sidebar collapsible="icon"` with `SidebarRail` |
| 6  | Sticky header shows breadcrumbs, language toggle, and user dropdown                                | VERIFIED   | SiteHeader.tsx: SidebarTrigger + Separator + Breadcrumb + flex-1 spacer + LanguageToggle + DropdownMenu user; route-based breadcrumb via `useRouterState().location.pathname` |
| 7  | Sidebar displays placeholder navigation items for future phases as disabled/coming-soon            | VERIFIED   | AppSidebar.tsx: 5 clinic items + 4 operations items all with `disabled: true`, Tooltip with `t("sidebar.comingSoon")`; i18n keys present in both en.json and vi.json |
| 8  | TopBar.tsx is removed, replaced by SiteHeader.tsx                                                  | VERIFIED   | `ls TopBar.tsx` → DELETED; AppShell.tsx imports SiteHeader, not TopBar                                       |
| 9  | Login page uses two-column grid layout (login-04 pattern)                                          | VERIFIED   | LoginPage.tsx: `<div className="grid min-h-svh lg:grid-cols-2">` with left form panel and right branding panel |
| 10 | Login form uses Field+Controller pattern; field name is "username"; no social login                | VERIFIED   | LoginForm.tsx: 3 Controllers (username, password, rememberMe); imports `Field, FieldLabel, FieldError from @/shared/components/Field`; `z.object({ username: ... })`; no OAuth buttons |
| 11 | Session warning uses AlertDialog (non-dismissible); no Dialog workarounds                          | VERIFIED   | SessionWarningModal.tsx: imports `AlertDialog, AlertDialogContent` from `@/shared/components/AlertDialog`; no `hideCloseButton`, no `onEscapeKeyDown`, no `onPointerDownOutside`; `<AlertDialog open={open}>` with no onOpenChange |
| 12 | A generic DataTable<TData, TValue> component exists and is used by all three tables                | VERIFIED   | DataTable.tsx: 137 lines, typed `DataTable<TData, TValue>`, receives TanStack Table instance; imported by UserTable.tsx, RoleTable.tsx, AuditLogTable.tsx |
| 13 | All forms use Field+Controller; filters use Field+FieldLabel; no register() or inline errors       | VERIFIED   | UserFormDialog: Controller for email/fullName/password/isActive/roleIds; RoleManagementPage: Controller for name/description; AuditLogFilters: Field+FieldLabel for all 4 filters; zero `register()` calls in features; single residual `text-sm text-destructive` is server-error block (not field validation error) |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact                                                              | Expected                                          | Status     | Details                                                                              |
|-----------------------------------------------------------------------|---------------------------------------------------|------------|--------------------------------------------------------------------------------------|
| `frontend/src/shared/components/ui/field.tsx`                        | shadcn Field primitive                            | VERIFIED   | Exists; exports Field, FieldLabel, FieldDescription, FieldError, FieldGroup, etc.   |
| `frontend/src/shared/components/ui/alert-dialog.tsx`                 | shadcn AlertDialog primitive                      | VERIFIED   | Exists; exports all 11 AlertDialog sub-components                                    |
| `frontend/src/shared/components/ui/breadcrumb.tsx`                   | shadcn Breadcrumb primitive                       | VERIFIED   | Exists                                                                               |
| `frontend/src/shared/components/ui/avatar.tsx`                       | shadcn Avatar primitive                           | VERIFIED   | Exists                                                                               |
| `frontend/src/shared/components/Button.tsx`                          | Button wrapper re-export                          | VERIFIED   | Pure re-export of Button, buttonVariants, ButtonProps from ui/button                 |
| `frontend/src/shared/components/Field.tsx`                           | Field, FieldLabel, FieldError, FieldGroup wrappers| VERIFIED   | Exports 10 Field sub-components from ui/field                                        |
| `frontend/src/shared/components/AlertDialog.tsx`                     | AlertDialog wrapper re-exports                    | VERIFIED   | Exports all 11 AlertDialog sub-components from ui/alert-dialog                      |
| `frontend/src/shared/components/SiteHeader.tsx`                      | Dashboard header                                  | VERIFIED   | 127 lines; breadcrumbs + LanguageToggle + user DropdownMenu; all wrapper imports     |
| `frontend/src/shared/components/NavUser.tsx`                         | User dropdown for sidebar footer                  | VERIFIED   | Substantive; Avatar + DropdownMenu; rendered in SidebarFooter via AppSidebar         |
| `frontend/src/shared/components/AppSidebar.tsx`                      | Dashboard-01 sidebar with nav groups              | VERIFIED   | 253 lines; 4 groups (Main, Clinic, Operations, Admin); disabled placeholder items; SidebarRail |
| `frontend/src/shared/components/AppShell.tsx`                        | Dashboard shell                                   | VERIFIED   | SidebarProvider + AppSidebar + SidebarInset + SiteHeader + SessionWarningModal       |
| `frontend/src/features/auth/components/LoginPage.tsx`                | Two-column login layout                           | VERIFIED   | `grid min-h-svh lg:grid-cols-2`; LanguageToggle in top-right of form panel           |
| `frontend/src/features/auth/components/LoginForm.tsx`                | Login form with Field+Controller                  | VERIFIED   | Controller for all 3 fields; username field (not email); FieldError for validation   |
| `frontend/src/features/auth/components/SessionWarningModal.tsx`      | AlertDialog session warning                       | VERIFIED   | AlertDialog with countdown timer; no dismissal workarounds                           |
| `frontend/src/shared/components/DataTable.tsx`                       | Generic DataTable<TData, TValue>                  | VERIFIED   | 137 lines; TanStack Table instance input; sorting, expansion, row click, sub-rows    |
| `frontend/src/features/admin/components/UserTable.tsx`               | User table using DataTable                        | VERIFIED   | Imports and renders `<DataTable table={table} columns={columns} ...>`                |
| `frontend/src/features/admin/components/UserFormDialog.tsx`          | User dialog with Field+Controller                 | VERIFIED   | Controller for email, fullName (create + edit branches), password, isActive, roleIds |
| `frontend/src/features/admin/components/RoleManagementPage.tsx`      | Role management with Field+Controller form        | VERIFIED   | Controller for name and description in create dialog                                  |
| `frontend/src/features/audit/components/AuditLogFilters.tsx`         | Filter bar with Field+FieldLabel                  | VERIFIED   | Field+FieldLabel for all 4 filters (user, actionType, dateFrom, dateTo)              |

### Key Link Verification

| From                             | To                                   | Via                                     | Status   | Details                                                              |
|----------------------------------|--------------------------------------|-----------------------------------------|----------|----------------------------------------------------------------------|
| `Button.tsx`                     | `ui/button.tsx`                      | re-export                               | WIRED    | `export { Button, buttonVariants } from "@/shared/components/ui/button"` |
| `AppShell.tsx`                   | `AppSidebar.tsx`                     | renders `<AppSidebar>` inside SidebarProvider | WIRED | Line 15: `<AppSidebar />`                                         |
| `AppShell.tsx`                   | `SiteHeader.tsx`                     | renders `<SiteHeader>` inside SidebarInset | WIRED | Line 17: `<SiteHeader />`                                         |
| `SiteHeader.tsx`                 | `LanguageToggle.tsx`                 | renders `<LanguageToggle>` in header    | WIRED    | Line 101: `<LanguageToggle />`                                       |
| `AppSidebar.tsx`                 | `NavUser.tsx`                        | renders `<NavUser>` in SidebarFooter    | WIRED    | Line 244: `<NavUser />`                                              |
| `LoginForm.tsx`                  | `@/shared/components/Field`          | Controller with Field+FieldLabel+FieldError | WIRED | Line 13: `import { Field, FieldLabel, FieldError } from "@/shared/components/Field"`; used in 3 Controllers |
| `SessionWarningModal.tsx`        | `@/shared/components/AlertDialog`    | imports AlertDialog components          | WIRED    | Lines 4-10: full AlertDialog import; `<AlertDialog open={open}>` with no onOpenChange |
| `LoginForm.tsx`                  | react-hook-form Controller           | `<Controller>` for each field           | WIRED    | 3 Controller instances for username, password, rememberMe            |
| `UserTable.tsx`                  | `DataTable.tsx`                      | imports and renders DataTable           | WIRED    | Line 14: import; Line 92: `<DataTable table={table} columns={columns} ...>` |
| `RoleTable.tsx`                  | `DataTable.tsx`                      | imports and renders DataTable           | WIRED    | Line 9: import; `<DataTable table={table} columns={columns} onRowClick={...} rowClassName={...}>` |
| `AuditLogTable.tsx`              | `DataTable.tsx`                      | imports and renders DataTable           | WIRED    | Line 13: import; `<DataTable ... renderSubRow={...} onRowClick={...}>` |
| `UserFormDialog.tsx`             | `@/shared/components/Field`          | Field+Controller for form fields        | WIRED    | Line 20: import; Controllers for email, fullName (both modes), password, isActive; Field for roleIds |
| `AuditLogFilters.tsx`            | `@/shared/components/Field`          | Field+FieldLabel for filter inputs      | WIRED    | Line 4: import; 4 Field+FieldLabel wrappers for all filter inputs    |

### Requirements Coverage

| Requirement | Source Plan    | Description                                                    | Status          | Evidence                                                                           |
|-------------|----------------|----------------------------------------------------------------|-----------------|------------------------------------------------------------------------------------|
| UI-01       | 01a, 01b, 02, 03, 04, 05 | All UI text, labels, menus, reports in Vietnamese and English | SATISFIED | en.json and vi.json updated with all new keys: sidebar groups, comingSoon, login.username; both files complete |
| UI-02       | 01b, 02, 05    | Staff can switch language preference per user session          | SATISFIED       | LanguageToggle present in SiteHeader and LoginPage; i18n keys for all new UI present in both locales |
| AUTH-01     | 03, 05         | Staff can log in with credentials and receive JWT token        | SATISFIED       | LoginForm uses Controller pattern with username field; `login(data.username, ...)` call preserved; backward compatible API |
| AUTH-03     | 04             | Admin can configure granular permissions per role              | SATISFIED       | PermissionMatrix unchanged functionally; RoleManagementPage has Field+Controller create form; DataTable used in RoleTable |
| AUTH-04     | 02, 03, 05     | User session persists, times out, supports logout              | SATISFIED       | SessionWarningModal uses AlertDialog (non-dismissible); logout in NavUser and SiteHeader user dropdown; `useSession()` in AppShell |
| AUD-01      | 04, 05         | Field-level audit trail for all medical record changes         | SATISFIED       | AuditLogTable uses DataTable with expandable sub-rows (AuditLogDetail); wired via `renderSubRow` |
| AUD-02      | 04, 05         | Access log for all user logins, logouts, and record views      | SATISFIED       | AuditLogTable with DataTable; AuditLogFilters with Field+FieldLabel for actionType (Login, Logout, ViewRecord) |

**Note on requirement traceability:** REQUIREMENTS.md maps all 7 phase requirements (UI-01, UI-02, AUTH-01, AUTH-03, AUTH-04, AUD-01, AUD-02) to Phase 1 as "Complete". Phase 01.2 is an inserted refactoring phase that improves the UI implementation of these already-counted requirements. All 7 requirements are addressed by the phase's artifacts.

### Anti-Patterns Found

| File                                           | Line | Pattern                                  | Severity | Impact                                                                                                                           |
|------------------------------------------------|------|------------------------------------------|----------|----------------------------------------------------------------------------------------------------------------------------------|
| `features/auth/components/LoginForm.tsx`       | 141  | `text-sm text-destructive` in a div      | INFO     | This is the server-side login error block (not a field-level validation error). It is intentional — shows API error inline. Not an old form pattern violation. |
| `shared/components/ui/dialog.tsx`              | 35   | `hideCloseButton?: boolean` custom prop  | INFO     | Restored after shadcn overwrite per SUMMARY 01a. Required for Dialog-based modals that predate the AlertDialog migration. SessionWarningModal no longer uses it. No blocker. |

No blockers or warnings found. The `text-sm text-destructive` flag in LoginForm.tsx is the server-error display (line 141 wraps the entire `loginError` message in a div, not individual field errors), which is correct behavior per the plan's spec ("Keep loginError inline display for server-side errors"). The `hideCloseButton` prop in dialog.tsx is legacy scaffolding — SessionWarningModal has already migrated to AlertDialog.

### Human Verification Required

All automated checks pass. The following items require runtime verification:

#### 1. Sidebar Collapse and Visual Layout

**Test:** Log in, observe the sidebar; click the SidebarTrigger to collapse
**Expected:** Sidebar collapses to icon-only mode showing only icons with tooltips; SidebarRail is visible; header breadcrumbs and right-side controls remain in place
**Why human:** CSS-driven collapsible behavior, visual alignment, and icon-only mode rendering require a running browser

#### 2. Route-Based Breadcrumb Updates

**Test:** Navigate between Dashboard, Admin > Users, Admin > Roles, Admin > Audit Logs
**Expected:** Breadcrumbs update to reflect current route (e.g., "Dashboard", "Administration > User Management"); segment labels use i18n keys
**Why human:** Runtime router state and i18n key resolution cannot be verified statically

#### 3. Session Warning AlertDialog Non-Dismissibility

**Test:** Trigger the session warning (reduce inactivity timeout or wait); press ESC; click outside the dialog
**Expected:** AlertDialog cannot be dismissed by ESC key or outside click; only Extend and Logout buttons close it; countdown timer counts down in real-time
**Why human:** Modal dismissal behavior and timer countdown require runtime interaction

#### 4. Login Form Validation UX

**Test:** Submit the login form with empty fields; then fill username but leave password short
**Expected:** FieldError elements appear below the respective inputs showing translated validation messages; no toast errors for field validation; server credential error shows as inline block
**Why human:** Visual placement of FieldError within Field layout and error message content require visual verification

#### 5. UserFormDialog Create/Edit Mode and Role Selection

**Test:** Open Create User dialog, submit with no roles selected; then open Edit User dialog for an existing user
**Expected:** FieldError appears in role selection group when no role is selected; edit form pre-populates with user's existing data; validation errors display via FieldError
**Why human:** Form mode switching, pre-population from API data, and role group FieldError rendering require runtime verification

#### 6. AuditLogTable Row Expansion and Filter Behavior

**Test:** Navigate to Admin > Audit Logs; click a row; apply a filter
**Expected:** Row expands inline showing AuditLogDetail sub-row (via DataTable renderSubRow); applying filters updates the table; Field+FieldLabel renders labels above filter inputs
**Why human:** Row expansion behavior, filter state propagation, and Field layout in a controlled (non-RHF) component require runtime visual verification

#### 7. Bilingual UI Coverage for New Keys

**Test:** Switch to English, then Vietnamese; observe sidebar, login page, and table headers
**Expected:** "Clinic", "Operations", "Coming soon" tooltip in English; "Phong kham", "Van hanh", "Sap ra mat" in Vietnamese; all form labels translate
**Why human:** Full i18n key resolution across all new sidebar groups, tooltips, and form labels requires visual confirmation

---

## Gaps Summary

No gaps found. All 13 observable truths are verified at all three levels (exists, substantive, wired). The phase goal is structurally achieved in the codebase.

The `human_needed` status reflects that 7 runtime and visual behaviors cannot be confirmed without a browser — this is expected for a UI-heavy refactoring phase. All automated verification criteria from Plan 05 have been independently confirmed:

- TypeScript compilation: no new errors introduced (pre-existing OpenAPI errors noted in summaries)
- Wrapper pattern: zero direct ui/ imports outside wrapper files (grep confirmed)
- No old form patterns: zero `register()` calls in features; zero inline `<p className="text-sm text-destructive">` error elements (one server-error div is intentional)
- No Dialog workarounds: `hideCloseButton` remains only in the ui/ primitive itself; no feature code uses it for session warning
- DataTable usage: confirmed in UserTable, RoleTable, AuditLogTable
- All 20 wrappers match all 20 ui/ primitives
- i18n keys for all new sidebar items present in both en.json and vi.json

---

_Verified: 2026-03-01T09:00:00Z_
_Verifier: Claude (gsd-verifier)_
