---
phase: 01-foundation-infrastructure
plan: 07
type: execute
wave: 4
depends_on:
  - 01-05
  - 01-06
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUD-01
  - AUD-02
  - AUD-03
  - AUD-04
  - UI-01
  - UI-02
  - ARC-01
  - ARC-02
  - ARC-03
  - ARC-04
  - ARC-05
  - ARC-06

must_haves:
  truths:
    - "Staff member can log in, see dashboard, and navigate the app"
    - "Admin can manage users and roles with permission matrix"
    - "Audit logs are visible and filterable in the admin UI"
    - "Language toggle switches all UI text between Vietnamese and English"
    - "Session timeout warning appears and extend/logout work correctly"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of Phase 1: Foundation & Infrastructure.

Purpose: Verify that all Phase 1 requirements work together as an integrated system before marking the phase complete. This covers the full user journey: login -> dashboard -> admin pages -> audit logs -> language switching -> session management.

Output: Human-verified confirmation that Phase 1 meets all success criteria.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-05-SUMMARY.md
@.planning/phases/01-foundation-infrastructure/01-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify complete Phase 1 end-to-end</name>
  <files>No files modified -- verification only</files>
  <action>Human verification checkpoint. No automated action -- user performs manual testing of the complete Phase 1 system.</action>
  <what-built>
Complete Phase 1 Foundation & Infrastructure:
- .NET 10 modular monolith backend with 9 module scaffolds (4 layers each)
- JWT authentication with refresh token rotation and role-based access control
- 8 system roles with granular configurable permissions
- EF Core field-level audit logging on all IAuditable entities
- Access logging for all HTTP requests and login attempts
- ICD-10 ophthalmology codes seeded
- TanStack Start SPA frontend with shadcn/ui (Stone+Green, no border radius)
- Bilingual UI (Vietnamese/English) with i18next
- Collapsible sidebar + top bar app shell
- Login page (split layout), session timeout warning modal
- User management and role/permission administration pages
- Audit log viewer with filters and CSV export
- NetArchTest architecture rules enforcing module boundaries
- Azure Blob Storage service interface
- ACL adapter, template engine, and data export interfaces
  </what-built>
  <how-to-verify>
**Prerequisites:** Start both backend (`cd backend && dotnet run --project src/Bootstrapper`) and frontend (`cd frontend && npm run dev`)

**1. Login Flow (AUTH-01, AUTH-04)**
   1. Navigate to http://localhost:3000
   2. Verify redirect to /login page with split layout (branding left, form right)
   3. Try invalid credentials -- should show error message
   4. Log in with admin@ganka28.com and the configured admin password
   5. Verify redirect to dashboard

**2. Language Switching (UI-01, UI-02)**
   6. Click language toggle in top bar
   7. Verify ALL visible text switches between Vietnamese and English
   8. Toggle back -- verify text reverts

**3. User Management (AUTH-02, AUTH-03)**
   9. Navigate to Admin > Users
   10. Verify user table shows the admin user
   11. Click "Add User" -- create a test user with Doctor role
   12. Verify new user appears in table with Doctor role badge

**4. Role/Permission Management (AUTH-03)**
   13. Navigate to Admin > Roles
   14. Verify 8 system roles are listed
   15. Click on "Doctor" role
   16. Verify permission matrix shows checkboxes grouped by module
   17. Toggle a permission checkbox and save -- verify it persists on page refresh

**5. Audit Logs (AUTH-05, AUD-01, AUD-02, AUD-03)**
   18. Navigate to Admin > Audit Logs
   19. Verify login attempts are logged (your login from step 4)
   20. Verify user creation is logged (the test user from step 11)
   21. Filter by action type "Created" -- verify results narrow
   22. Expand a row -- verify field-level change details are shown
   23. Click "Export CSV" -- verify file downloads

**6. Session Timeout (AUTH-04)**
   24. Wait for session warning modal to appear (or reduce timeout for testing)
   25. Verify countdown timer is shown
   26. Click "Extend" -- verify session is refreshed
   27. Click "Logout" -- verify redirect to login page

**7. Architecture Tests**
   28. Run `cd backend && dotnet test tests/Ganka28.ArchitectureTests/` -- verify all pass

**8. Backend API (spot check)**
   29. Open Swagger UI (http://localhost:5000/swagger)
   30. Verify auth endpoints are listed
   31. Verify audit endpoints are listed
  </how-to-verify>
  <verify>Human verifies all Phase 1 requirements by following the verification steps above</verify>
  <done>User types "approved" confirming all Phase 1 success criteria are met</done>
  <resume-signal>Type "approved" to mark Phase 1 complete, or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
All 5 success criteria from the ROADMAP are verified:
1. Staff member can log in with email/password, session persists, times out, supports logout
2. Admin can assign roles and configure granular permissions per role
3. All login attempts and record access are logged immutably
4. User can switch between Vietnamese and English UI
5. System has schema-per-module database, Azure Blob configured, all module DbContexts scaffolded with BranchId
</verification>

<success_criteria>
Human verifies the complete Phase 1 end-to-end flow works correctly. All 17 requirements (AUTH-01 through AUTH-05, AUD-01 through AUD-04, UI-01, UI-02, ARC-01 through ARC-06) are met.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-07-SUMMARY.md`
</output>
