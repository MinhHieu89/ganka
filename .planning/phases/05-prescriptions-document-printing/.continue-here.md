---
phase: 05-prescriptions-document-printing
task: UAT test 22/29
total_tasks: 29
status: in_progress
last_updated: 2026-03-10T10:43:34.427Z
---

<current_state>
Running UAT (verify-work) for Phase 05. Currently at Test 22 (Print Optical Prescription PDF).
The backend has uncommitted changes that add PatientCode to all document PDFs. The backend server is running (process 10828) so `dotnet build` fails with file lock errors — need to stop server first to rebuild.
</current_state>

<completed_work>

- Tests 1-19: All pass (15 passed directly, 4 fixed during UAT)
- Test 20 (Upload Clinic Logo): Skipped — backend endpoint missing, captured as todo
- Test 21 (Print Drug Rx PDF): Pass

**Bugs fixed during UAT:**
1. DrugCatalogPage not routed — created /pharmacy/drug-catalog route + sidebar link
2. Drug prescription dosage duplicate line + "thời gian" label bug — removed duplicate frequency/duration, added durationUnit translation key
3. Drug prescription doseAmount not round-tripping on edit — added to PrescriptionItemInput
4. Delete prescription lacking confirmation — added AlertDialog
5. Prescription notes (Loi Dan) not saving — stale closure bug, fixed with notesRef
6. Optical Rx cancel/save buttons missing localization — changed actions.* to buttons.*
7. Optical Rx section not expanding on "Write Optical Rx" click — added controlled open state to VisitSection
8. Near Rx not auto-expanding when editing with existing values — added defaultOpen based on data

**Todos captured:**
1. Server side pagination for /pharmacy page
2. Import drugs from Excel in drug catalog page
3. Auto focus search field when opening Add Drug form
4. Print all pharmacy labels at once
5. Implement clinic logo upload endpoint
6. Auto expand Optical Prescription section when data exists

**Currently in progress (uncommitted):**
- Added PatientCode to all document data records (DrugPrescriptionData, OpticalPrescriptionData, ReferralLetterData, ConsentFormData)
- Updated PatientBasicInfo SQL query to include PatientCode
- Updated all 4 PDF document templates to show "Mã BN: {code}" next to patient name
</completed_work>

<remaining_work>

- Test 22: Print Optical Prescription PDF — user reported missing PatientCode (fix in progress, uncommitted)
- Test 23: Print Pharmacy Label (70x35mm)
- Test 24: Print Referral Letter PDF (A4)
- Test 25: Print Consent Form PDF (A4)
- Test 26: Vietnamese Diacritics in Documents
- Test 27: Clinic Settings Reflected in Documents
- Test 28: Vietnamese UI Labels
- Test 29: Drug Form and Route Enum Labels
- Final: Commit UAT file, commit all fixes
</remaining_work>

<decisions_made>

- Used notesRef (useRef) instead of restructuring callbacks to fix stale closure in prescription notes
- Added PatientCode to ALL document types at once (not just optical) since it was missing everywhere
- Used "Mã BN:" as the Vietnamese label for patient code in documents
- VisitSection now supports controlled open/onOpenChange props for programmatic expansion
</decisions_made>

<blockers>
- Backend server running on port 5255 (process 10828) prevents dotnet build — need to stop server before rebuilding to test PatientCode fix
- Unit tests in Clinical.Unit.Tests may need updating for the new PatientCode parameter in data records
</blockers>

<context>
The UAT session is going well — most features work. The main pattern of issues is UI wiring (components built but not connected to routes/pages) and small UX bugs (stale closures, missing translations, missing confirmation dialogs). The PatientCode fix is the last backend change — after that, remaining tests are mostly printing/display verification. The UAT file at .planning/phases/05-prescriptions-document-printing/05-UAT.md tracks all results but needs updating with tests 20-22 results.
</context>

<next_action>
1. Stop the backend server (kill process 10828 or stop from terminal)
2. Build backend: `cd backend && dotnet build`
3. Fix any unit test compilation errors from PatientCode addition
4. Restart backend server
5. Resume UAT from Test 22 — verify PatientCode shows in Optical Rx PDF
6. Continue through Tests 23-29
7. Commit all fixes and update UAT file
</next_action>
