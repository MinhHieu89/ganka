---
phase: 05-prescriptions-document-printing
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Shared/Shared.Infrastructure/Entities/ClinicSettings.cs
  - backend/src/Shared/Shared.Application/Interfaces/IClinicSettingsService.cs
  - backend/src/Shared/Shared.Infrastructure/Services/ClinicSettingsService.cs
  - backend/src/Shared/Shared.Infrastructure/Configurations/ClinicSettingsConfiguration.cs
autonomous: true
requirements:
  - PRT-01
  - PRT-02
  - PRT-04
  - PRT-05
  - PRT-06
must_haves:
  truths:
    - "ClinicSettings entity stores configurable clinic header info (logo, name, address, phone, fax, license number, tagline)"
    - "IClinicSettingsService interface available for document generation to retrieve clinic header data"
    - "ClinicSettings supports multi-branch via BranchId"
  artifacts:
    - path: "backend/src/Shared/Shared.Infrastructure/Entities/ClinicSettings.cs"
      provides: "Clinic settings entity with all header fields"
      contains: "class ClinicSettings"
    - path: "backend/src/Shared/Shared.Application/Interfaces/IClinicSettingsService.cs"
      provides: "Interface for clinic settings retrieval"
      contains: "interface IClinicSettingsService"
    - path: "backend/src/Shared/Shared.Infrastructure/Services/ClinicSettingsService.cs"
      provides: "Implementation loading from database"
      contains: "class ClinicSettingsService"
  key_links:
    - from: "ClinicSettingsService.cs"
      to: "ClinicSettings entity"
      via: "DbContext query"
      pattern: "ClinicSettings"
---

<objective>
Create the configurable clinic settings infrastructure for document headers.

Purpose: All printed documents (drug Rx, optical Rx, referral letters, consent forms, pharmacy labels) pull clinic branding from ClinicSettings. This entity stores logo URL, clinic name, address, phone, fax, license number, and tagline. Configurable by admin, supports multi-branch via BranchId.

Output: ClinicSettings entity, IClinicSettingsService interface, implementation, EF config
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-prescriptions-document-printing/05-CONTEXT.md
@.planning/phases/05-prescriptions-document-printing/05-RESEARCH.md

@backend/src/Shared/Shared.Infrastructure/Shared.Infrastructure.csproj
@backend/src/Shared/Shared.Application/Shared.Application.csproj

<interfaces>
From Shared.Domain:
```csharp
public abstract class Entity { public Guid Id; }
public abstract class AggregateRoot : Entity { public BranchId BranchId; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClinicSettings entity and EF Core configuration</name>
  <files>
    backend/src/Shared/Shared.Infrastructure/Entities/ClinicSettings.cs,
    backend/src/Shared/Shared.Infrastructure/Configurations/ClinicSettingsConfiguration.cs
  </files>
  <action>
**ClinicSettings.cs** (Shared.Infrastructure/Entities/):
- Inherits from `AggregateRoot` (has BranchId for multi-branch support)
- Properties:
  - `string ClinicName` (English name)
  - `string? ClinicNameVi` (Vietnamese name)
  - `string Address`
  - `string? Phone`
  - `string? Fax`
  - `string? LicenseNumber` (So GPHN / Giay phep hoat dong)
  - `string? Tagline` (e.g., "Cham soc mat chuyen sau")
  - `string? LogoBlobUrl` (Azure Blob URL for clinic logo image)
  - `string? Email`
  - `string? Website`
- Private constructor for EF Core
- Static `Create(...)` factory method
- `Update(...)` method for admin editing
- `UpdateLogo(string logoUrl)` method

**ClinicSettingsConfiguration.cs** (Shared.Infrastructure/Configurations/):
- Table: "ClinicSettings" in shared schema (or dbo if Shared.Infrastructure uses default)
- ClinicName: required, max length 200
- ClinicNameVi: max length 200
- Address: required, max length 500
- Phone/Fax/Email/Website: max length 100
- LicenseNumber: max length 100
- Tagline: max length 300
- LogoBlobUrl: max length 500
- BranchId configured

Note: Check which schema Shared.Infrastructure uses. If no schema, use default. If it needs to be in a specific schema, check existing SharedInfrastructure DbContext patterns.
  </action>
  <verify>
    <automated>dotnet build backend/src/Shared/Shared.Infrastructure/Shared.Infrastructure.csproj</automated>
  </verify>
  <done>ClinicSettings entity with all header fields and EF configuration compile. Multi-branch support via BranchId.</done>
</task>

<task type="auto">
  <name>Task 2: Create IClinicSettingsService interface and implementation</name>
  <files>
    backend/src/Shared/Shared.Application/Interfaces/IClinicSettingsService.cs,
    backend/src/Shared/Shared.Infrastructure/Services/ClinicSettingsService.cs
  </files>
  <action>
**IClinicSettingsService.cs** (Shared.Application/Interfaces/):
```csharp
public interface IClinicSettingsService
{
    Task<ClinicSettingsDto?> GetCurrentAsync(CancellationToken ct);
    Task<Result<Guid>> CreateOrUpdateAsync(UpdateClinicSettingsCommand command, CancellationToken ct);
}
```

Define `ClinicSettingsDto` as a sealed record in Shared.Contracts (or inline in the interface file if Shared.Contracts doesn't exist as a separate project). Fields: Id, ClinicName, ClinicNameVi, Address, Phone, Fax, LicenseNumber, Tagline, LogoBlobUrl, Email, Website.

Define `UpdateClinicSettingsCommand` record with same fields.

**ClinicSettingsService.cs** (Shared.Infrastructure/Services/):
- Inject the appropriate DbContext (determine which DbContext Shared.Infrastructure uses -- likely needs a reference to a DbContext that includes ClinicSettings)
- `GetCurrentAsync`: query first ClinicSettings record (or by BranchId), project to ClinicSettingsDto
- `CreateOrUpdateAsync`: upsert pattern -- if exists update, if not create

Register in Shared.Infrastructure IoC.cs (AddSharedInfrastructure extension method).

Note: Determine the DbContext approach. Options:
1. Add ClinicSettings to an existing DbContext (e.g., AuthDbContext which handles system settings)
2. Create a minimal SharedDbContext
3. Use ReferenceDbContext if appropriate

Prefer adding to an existing context to avoid proliferation. Check if AuthDbContext or a shared context makes sense. If uncertain, add a new DbSet to the ReferenceDbContext since clinic settings are reference data.
  </action>
  <verify>
    <automated>dotnet build backend/src/Shared/Shared.Infrastructure/Shared.Infrastructure.csproj && dotnet build backend/src/Shared/Shared.Application/Shared.Application.csproj</automated>
  </verify>
  <done>IClinicSettingsService interface and implementation exist. Service registered in DI. ClinicSettingsDto available for document generation.</done>
</task>

</tasks>

<verification>
- `dotnet build backend/src/Shared/Shared.Infrastructure/Shared.Infrastructure.csproj` passes
- `dotnet build backend/src/Shared/Shared.Application/Shared.Application.csproj` passes
- ClinicSettings entity has all fields from CONTEXT.md (logo, name, address, phone, fax, license number, tagline)
- Service registered in IoC
</verification>

<success_criteria>
Clinic settings infrastructure complete. All document generators can retrieve clinic header data via IClinicSettingsService. Admin can configure clinic branding.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prescriptions-document-printing/05-09-SUMMARY.md`
</output>
