---
phase: 05-prescriptions-document-printing
plan: 18
type: execute
wave: 6
depends_on: ["05-13"]
files_modified:
  - frontend/src/features/admin/components/ClinicSettingsPage.tsx
  - frontend/src/features/admin/api/clinic-settings-api.ts
  - frontend/public/locales/vi/pharmacy.json
  - frontend/src/app/routes/_authenticated/admin/clinic-settings.tsx
  - frontend/src/shared/components/AppSidebar.tsx
autonomous: true
requirements:
  - PRT-01
  - PRT-02
  - PRT-04
  - PRT-05
must_haves:
  truths:
    - "Admin can configure clinic header (name, address, phone, fax, license number, tagline, logo)"
    - "Clinic settings saved to backend and used by all printed documents"
    - "Vietnamese pharmacy translations exist with proper diacritics"
    - "Route file exists for /admin/clinic-settings"
    - "Sidebar navigation includes Pharmacy catalog and Clinic Settings links"
  artifacts:
    - path: "frontend/src/features/admin/components/ClinicSettingsPage.tsx"
      provides: "Admin page for clinic header configuration"
      contains: "ClinicSettingsPage"
    - path: "frontend/src/features/admin/api/clinic-settings-api.ts"
      provides: "API hooks for clinic settings CRUD"
      contains: "useClinicSettings|useUpdateClinicSettings"
    - path: "frontend/src/app/routes/_authenticated/admin/clinic-settings.tsx"
      provides: "Route file for clinic settings page"
      contains: "ClinicSettingsPage"
    - path: "frontend/src/shared/components/AppSidebar.tsx"
      provides: "Updated sidebar with Pharmacy and Clinic Settings nav links"
      contains: "pharmacy|clinic-settings"
  key_links:
    - from: "ClinicSettingsPage.tsx"
      to: "clinic-settings-api.ts"
      via: "TanStack Query hooks"
      pattern: "useClinicSettings|useUpdateClinicSettings"
    - from: "AppSidebar.tsx"
      to: "pharmacy/index.tsx"
      via: "Navigation link"
      pattern: "/pharmacy"
---

<objective>
Create admin clinic settings page, Vietnamese pharmacy translations, route file, and update sidebar navigation.

Purpose: The configurable clinic header (locked decision) requires an admin UI for setting logo, clinic name, address, phone, fax, license number, and tagline. All printed documents pull from these settings. Also completes the Vietnamese pharmacy translations and adds navigation links for Pharmacy and Clinic Settings to the sidebar.

Output: ClinicSettingsPage, clinic settings API hooks, vi/pharmacy.json, route file, updated sidebar
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-prescriptions-document-printing/05-CONTEXT.md
@.planning/phases/05-prescriptions-document-printing/05-09-SUMMARY.md
@.planning/phases/05-prescriptions-document-printing/05-13-SUMMARY.md

@frontend/src/shared/components/AppSidebar.tsx
@frontend/src/app/routes/_authenticated/admin/users.tsx

<interfaces>
Backend API for clinic settings (from 05-09):
```
GET /api/settings/clinic -> ClinicSettingsDto
PUT /api/settings/clinic -> void
```
(Exact endpoint path TBD -- may be under /api/admin/settings or /api/settings)

ClinicSettingsDto: { id, clinicName, clinicNameVi, address, phone, fax, licenseNumber, tagline, logoBlobUrl, email, website }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create clinic settings API hooks and admin page</name>
  <files>
    frontend/src/features/admin/api/clinic-settings-api.ts,
    frontend/src/features/admin/components/ClinicSettingsPage.tsx,
    frontend/src/app/routes/_authenticated/admin/clinic-settings.tsx
  </files>
  <action>
**clinic-settings-api.ts**: TanStack Query hooks:
- `clinicSettingsKeys`: `{ all: ['clinic-settings'] }`
- `useClinicSettings()`: useQuery for GET clinic settings
- `useUpdateClinicSettings()`: useMutation for PUT, invalidates cache on success

**ClinicSettingsPage.tsx**: Admin page for clinic header configuration:
- Card layout with form
- Fields (React Hook Form + Zod):
  - Clinic Name (EN) - required
  - Clinic Name (VI) - optional
  - Address - required
  - Phone
  - Fax
  - Email
  - Website
  - License Number (So GPHN)
  - Tagline
  - Logo upload (file input -> upload to blob storage -> save URL)
- Logo preview if exists
- Save button with loading state
- Success/error toast on save

For logo upload: use the existing file upload pattern (native fetch + FormData, like patient photo upload). Upload to Azure Blob Storage via a new endpoint or reuse existing blob upload infrastructure.

**admin/clinic-settings.tsx**: Route file following file-based routing pattern:
- Reference existing admin route files (e.g., admin/users.tsx) for pattern
- Import and render ClinicSettingsPage
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ClinicSettingsPage renders form for all clinic header fields including logo upload. Settings saved via API. Route file created.</done>
</task>

<task type="auto">
  <name>Task 2: Create Vietnamese pharmacy translations and update sidebar</name>
  <files>
    frontend/public/locales/vi/pharmacy.json,
    frontend/src/shared/components/AppSidebar.tsx
  </files>
  <action>
**pharmacy.json (VI)**: Vietnamese translations with proper diacritics:
```json
{
  "catalog": {
    "title": "Danh muc thuoc",
    "addDrug": "Them thuoc",
    "editDrug": "Sua thuoc",
    "search": "Tim kiem thuoc...",
    "name": "Ten thuoc",
    "nameVi": "Ten tieng Viet",
    "genericName": "Hoat chat",
    "form": "Dang bao che",
    "strength": "Ham luong",
    "route": "Duong dung",
    "unit": "Don vi",
    "defaultDosage": "Lieu dung mac dinh",
    "active": "Hoat dong",
    "inactive": "Ngung su dung",
    "empty": "Chua co thuoc trong danh muc"
  }
}
```

CRITICAL: All Vietnamese text MUST use proper diacritics. The above is a simplified representation. The executor must ensure every Vietnamese string has correct accent marks.

**AppSidebar.tsx** -- add navigation links:
- Add "Pharmacy" section/group with:
  - "Drug Catalog" / "Danh muc thuoc" link -> /pharmacy
- Add to admin section:
  - "Clinic Settings" / "Cai dat phong kham" link -> /admin/clinic-settings
- Follow existing sidebar navigation pattern (icons, i18n labels, permission checks)
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Vietnamese pharmacy translations created with proper diacritics. Sidebar updated with Pharmacy catalog and Clinic Settings navigation links.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- ClinicSettingsPage renders all fields
- Vietnamese pharmacy translations have proper diacritics
- Settings page accessible from admin section via sidebar
- Pharmacy catalog accessible via sidebar
</verification>

<success_criteria>
Admin can configure clinic header branding for all printed documents. Vietnamese pharmacy translations complete with proper diacritics. Sidebar navigation updated with new module links.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prescriptions-document-printing/05-18-SUMMARY.md`
</output>
