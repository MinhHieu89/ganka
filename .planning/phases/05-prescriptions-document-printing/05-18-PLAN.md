---
phase: 05-prescriptions-document-printing
plan: 18
type: execute
wave: 7
depends_on: ["05-13"]
files_modified:
  - frontend/src/features/admin/components/ClinicSettingsPage.tsx
  - frontend/src/features/admin/api/clinic-settings-api.ts
  - frontend/public/locales/vi/pharmacy.json
autonomous: true
requirements:
  - PRT-01
  - PRT-02
  - PRT-04
  - PRT-05
must_haves:
  truths:
    - "Admin can configure clinic header (name, address, phone, fax, license number, tagline, logo)"
    - "Clinic settings saved to backend and used by all printed documents"
    - "Vietnamese pharmacy translations exist with proper diacritics"
  artifacts:
    - path: "frontend/src/features/admin/components/ClinicSettingsPage.tsx"
      provides: "Admin page for clinic header configuration"
      contains: "ClinicSettingsPage"
    - path: "frontend/src/features/admin/api/clinic-settings-api.ts"
      provides: "API hooks for clinic settings CRUD"
      contains: "useClinicSettings|useUpdateClinicSettings"
  key_links:
    - from: "ClinicSettingsPage.tsx"
      to: "clinic-settings-api.ts"
      via: "TanStack Query hooks"
      pattern: "useClinicSettings|useUpdateClinicSettings"
---

<objective>
Create admin clinic settings page and Vietnamese pharmacy translations.

Purpose: The configurable clinic header (locked decision) requires an admin UI for setting logo, clinic name, address, phone, fax, license number, and tagline. All printed documents pull from these settings. Also completes the Vietnamese pharmacy translations.

Output: ClinicSettingsPage, clinic settings API hooks, vi/pharmacy.json
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-prescriptions-document-printing/05-CONTEXT.md
@.planning/phases/05-prescriptions-document-printing/05-09-SUMMARY.md
@.planning/phases/05-prescriptions-document-printing/05-13-SUMMARY.md

<interfaces>
Backend API for clinic settings (from 05-09):
```
GET /api/settings/clinic -> ClinicSettingsDto
PUT /api/settings/clinic -> void
```
(Exact endpoint path TBD -- may be under /api/admin/settings or /api/settings)

ClinicSettingsDto: { id, clinicName, clinicNameVi, address, phone, fax, licenseNumber, tagline, logoBlobUrl, email, website }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create clinic settings API hooks and admin page</name>
  <files>
    frontend/src/features/admin/api/clinic-settings-api.ts,
    frontend/src/features/admin/components/ClinicSettingsPage.tsx
  </files>
  <action>
**clinic-settings-api.ts**: TanStack Query hooks:
- `clinicSettingsKeys`: `{ all: ['clinic-settings'] }`
- `useClinicSettings()`: useQuery for GET clinic settings
- `useUpdateClinicSettings()`: useMutation for PUT, invalidates cache on success

**ClinicSettingsPage.tsx**: Admin page for clinic header configuration:
- Card layout with form
- Fields (React Hook Form + Zod):
  - Clinic Name (EN) - required
  - Clinic Name (VI) - optional
  - Address - required
  - Phone
  - Fax
  - Email
  - Website
  - License Number (So GPHN)
  - Tagline
  - Logo upload (file input -> upload to blob storage -> save URL)
- Logo preview if exists
- Save button with loading state
- Success/error toast on save

For logo upload: use the existing file upload pattern (native fetch + FormData, like patient photo upload). Upload to Azure Blob Storage via a new endpoint or reuse existing blob upload infrastructure.

Route: add page to admin section (e.g., `/admin/settings` or appropriate admin route).

Update sidebar to include a link to Clinic Settings under admin section.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>ClinicSettingsPage renders form for all clinic header fields including logo upload. Settings saved via API.</done>
</task>

<task type="auto">
  <name>Task 2: Create Vietnamese pharmacy translations</name>
  <files>
    frontend/public/locales/vi/pharmacy.json
  </files>
  <action>
**pharmacy.json (VI)**: Vietnamese translations with proper diacritics:
```json
{
  "catalog": {
    "title": "Danh muc thuoc",
    "addDrug": "Them thuoc",
    "editDrug": "Sua thuoc",
    "search": "Tim kiem thuoc...",
    "name": "Ten thuoc",
    "nameVi": "Ten tieng Viet",
    "genericName": "Hoat chat",
    "form": "Dang bao che",
    "strength": "Ham luong",
    "route": "Duong dung",
    "unit": "Don vi",
    "defaultDosage": "Lieu dung mac dinh",
    "active": "Hoat dong",
    "inactive": "Ngung su dung",
    "empty": "Chua co thuoc trong danh muc"
  }
}
```

CRITICAL: All Vietnamese text MUST use proper diacritics. The above is a simplified representation. Actual file must have:
- "Danh muc thuoc" with proper diacritics
- "Them thuoc" with proper diacritics
- etc.

The executor must ensure every Vietnamese string has correct accent marks (dau sac, dau huyen, dau hoi, dau nga, dau nang, etc.).
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Vietnamese pharmacy translations created with proper diacritics for all catalog management labels.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- ClinicSettingsPage renders all fields
- Vietnamese pharmacy translations have proper diacritics
- Settings page accessible from admin section
</verification>

<success_criteria>
Admin can configure clinic header branding for all printed documents. Vietnamese pharmacy translations complete with proper diacritics.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prescriptions-document-printing/05-18-SUMMARY.md`
</output>
