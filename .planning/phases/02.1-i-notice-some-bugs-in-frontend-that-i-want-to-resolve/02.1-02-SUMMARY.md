---
phase: 02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve
plan: 02
subsystem: auth
tags: [http-only-cookie, silent-refresh, credentials-include, session-persistence, tanstack-router]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: HTTP-only cookie refresh token storage on backend login/refresh/logout endpoints
provides:
  - Frontend auth flow using HTTP-only cookies (no refresh token in JavaScript)
  - Silent refresh on page load via async beforeLoad route guard
  - credentials:include on all API requests for cross-origin cookie transmission
  - Standalone silentRefresh function for non-React contexts
affects: [02.1-03, 02.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "credentials:include on openapi-fetch createClient for cross-origin cookie transmission"
    - "silentRefresh standalone function (not a hook) for TanStack Router beforeLoad"
    - "Async beforeLoad with silent refresh attempt before login redirect"
    - "Cookie-based token refresh: no body sent, browser sends cookie automatically"

key-files:
  created: []
  modified:
    - frontend/src/shared/lib/api-client.ts
    - frontend/src/features/auth/api/auth-api.ts
    - frontend/src/features/auth/hooks/useAuth.ts
    - frontend/src/app/routes/_authenticated.tsx

key-decisions:
  - "silentRefresh as standalone async function (not hook) because beforeLoad runs outside React component context"
  - "No changes to authStore.ts -- already correct (in-memory only, no refreshToken field, no persist middleware)"

patterns-established:
  - "Silent refresh pattern: async beforeLoad checks in-memory auth first, falls back to cookie-based silentRefresh, redirects to login only if both fail"
  - "Cookie auth pattern: remove token from types/bodies, add credentials:include, let browser handle cookie transmission"

requirements-completed: [AUTH-04]

# Metrics
duration: 3min
completed: 2026-03-02
---

# Phase 02.1 Plan 02: Frontend Cookie-Based Auth Flow Summary

**Frontend auth refactored to HTTP-only cookies: credentials:include on API client, refresh token removed from JavaScript, silent refresh in async beforeLoad for session persistence across page refresh**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-02T07:42:39Z
- **Completed:** 2026-03-02T07:45:44Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- API client sends credentials:include for cross-origin cookie transmission to backend
- LoginResponse no longer contains refreshToken field (backend sends it as HTTP-only cookie)
- RefreshTokenRequest type removed -- refresh endpoint reads cookie, not request body
- useAuth hook cleaned of all refreshTokenRef usage -- scheduleRefresh/login/logout/refresh updated
- _authenticated.tsx route guard performs async silent refresh on page load before redirecting to login
- Standalone silentRefresh function exported for use outside React component context

## Task Commits

Each task was committed atomically:

1. **Task 1: Update API client and auth-api for cookie-based auth** - `dcfac2b` (feat)
2. **Task 2: Refactor useAuth hook and implement silent refresh in route guard** - `4fb2268` (feat)

## Files Created/Modified
- `frontend/src/shared/lib/api-client.ts` - Added credentials:"include" to createClient for cookie transmission
- `frontend/src/features/auth/api/auth-api.ts` - Removed refreshToken from types, updated refresh to send no body, added silentRefresh function
- `frontend/src/features/auth/hooks/useAuth.ts` - Removed refreshTokenRef, updated all callbacks for cookie-based flow
- `frontend/src/app/routes/_authenticated.tsx` - Async beforeLoad with silentRefresh attempt before login redirect

## Decisions Made
- silentRefresh implemented as standalone async function (not a React hook) because TanStack Router's beforeLoad runs outside component context where hooks are not available
- authStore.ts confirmed already correct -- no refreshToken field, no persist middleware, purely in-memory state

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Full cookie-based auth flow operational (backend + frontend)
- Session persistence across page refresh working via silent refresh
- Ready for Plan 03 (UI/theme fixes) and Plan 04 (patient list/profile audit)

## Self-Check: PASSED

All files exist. All commits verified.

---
*Phase: 02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve*
*Completed: 2026-03-02*
