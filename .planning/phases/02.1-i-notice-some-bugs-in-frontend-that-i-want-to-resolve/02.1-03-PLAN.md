---
phase: 02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve
plan: 03
type: execute
wave: 2
depends_on:
  - "02.1-01"
  - "02.1-02"
files_modified: []
autonomous: false
requirements:
  - AUTH-04
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Session persistence works end-to-end (login, refresh, remember me)"
    - "All pages match shadcn/ui neutral theme visual benchmark"
    - "No broken colors or missing borders on any page"
  artifacts: []
  key_links: []
---

<objective>
Verify both bug fixes end-to-end: session persistence across page refresh and visual alignment with shadcn/ui default theme on all pages.

Purpose: Human verification that the automated fixes produce the correct visual and functional result.
Output: Confirmed working session persistence and visually aligned UI.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve/02.1-01-SUMMARY.md
@.planning/phases/02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve/02.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start backend and frontend for verification</name>
  <files></files>
  <action>
1. Kill any processes on ports 5255 and 3000
2. Start the backend on port 5255 (check the project for the correct start command -- likely `dotnet run` in the backend/Bootstrapper directory)
3. Start the frontend on port 3000 (likely `npm run dev` or `pnpm dev` in the frontend directory)
4. Wait for both to be ready
  </action>
  <verify>
    <automated>curl -s http://localhost:5255/health 2>&1 | head -5 && curl -s http://localhost:3000 2>&1 | head -5</automated>
  </verify>
  <done>Backend running on 5255, frontend running on 3000, both accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify session persistence and visual alignment</name>
  <files></files>
  <action>
Human verifies both bug fixes end-to-end.
Backend running at http://localhost:5255, Frontend at http://localhost:3000.

What was built:
1. Session persistence: Auth store now uses Zustand persist middleware. Refresh token stored in Zustand (not useRef). Route guard performs silent token refresh on page load.
2. Theme reset: All CSS variables updated to shadcn/ui neutral defaults. Border radius changed from 0 to 0.625rem. hsl(var(--...)) bugs fixed. Hardcoded stone/white colors replaced with semantic tokens.

**Session Persistence Tests:**
1. Open http://localhost:3000/login
2. Log in with valid credentials (WITHOUT "Remember me")
3. Press F5 to refresh the page -- should stay on the authenticated page, NOT redirect to login
4. Open a new tab to http://localhost:3000 -- should show authenticated content
5. Close the browser entirely, reopen and navigate to http://localhost:3000 -- should still be logged in (within 7-day refresh token window)
6. Log out, then log in WITH "Remember me" checked
7. Refresh -- should stay logged in (30-day refresh token)

**Visual Alignment Tests:**
8. Check login page: rounded corners on form inputs and buttons, neutral dark branding panel (not green), consistent gray tones
9. Check dashboard/sidebar: rounded corners on sidebar items, neutral gray sidebar (not stone-tinted), visible border between sidebar and content
10. Check patient list: rounded corners on table, search input, and action buttons
11. Check patient profile: rounded tabs, rounded cards, rounded allergy badges
12. Check scheduling/calendar page: FullCalendar has visible borders (not black), today highlight is visible, event cards have rounded corners
13. Check public booking page (http://localhost:3000/book): uses theme background colors (not stark white/stone), rounded corners throughout
14. Overall: Compare visual appearance to https://ui.shadcn.com/create -- neutral grays, rounded corners, clean spacing

Resume signal: Type "approved" if both session persistence and visual alignment are correct, or describe any issues found.
  </action>
  <verify>Human visual and functional verification of all pages</verify>
  <done>User approves both session persistence behavior and visual alignment on all pages. No broken colors, missing borders, or visual artifacts.</done>
</task>

</tasks>

<verification>
Human confirms:
1. Session persists across page refresh
2. "Remember me" works
3. All pages match shadcn/ui neutral visual benchmark
4. No broken colors, missing borders, or visual artifacts
</verification>

<success_criteria>
- User approves session persistence behavior
- User approves visual alignment on all pages
- No issues reported
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve/02.1-03-SUMMARY.md`
</output>
