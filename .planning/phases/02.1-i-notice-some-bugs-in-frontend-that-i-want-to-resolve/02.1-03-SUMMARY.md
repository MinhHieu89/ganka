---
phase: 02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve
plan: 03
subsystem: ui
tags: [shadcn-ui, css-variables, oklch, tailwindcss, theming, fullcalendar]

# Dependency graph
requires:
  - phase: 01.2
    provides: shadcn/ui component library with CSS variable theming
provides:
  - shadcn/ui neutral default CSS variables in globals.css
  - Correct oklch var() references across all components (no hsl wrappers)
  - Semantic color tokens replacing all hardcoded stone-* classes
  - Rounded corners via --radius 0.625rem
affects: [02.1-04, all-frontend-phases]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "color-mix(in oklch, ...) for opacity variants with oklch CSS variables"
    - "calc(var(--radius) - Npx) for FullCalendar border radius inheritance"

key-files:
  created: []
  modified:
    - frontend/src/styles/globals.css
    - frontend/src/features/scheduling/components/AppointmentCalendar.tsx
    - frontend/src/shared/components/ui/sidebar.tsx
    - frontend/src/features/booking/components/PublicBookingPage.tsx
    - frontend/src/app/routes/book/status.tsx
    - frontend/src/features/audit/components/AuditLogTable.tsx

key-decisions:
  - "Used color-mix(in oklch, ...) instead of rgba/hsla for opacity variants -- compatible with oklch CSS variables"
  - "FullCalendar buttons use calc(var(--radius) - 2px), events use calc(var(--radius) - 4px) for size-appropriate rounding"
  - "bg-white in LoginPage branding panel kept as-is -- intentional design for overlay on primary background"

patterns-established:
  - "var(--token) direct reference for oklch CSS variables -- never wrap in hsl()"
  - "Semantic color tokens (bg-muted, bg-card, text-muted-foreground) instead of hardcoded color classes"

requirements-completed: [UI-01, UI-02]

# Metrics
duration: 3min
completed: 2026-03-02
---

# Phase 02.1 Plan 03: Theme Reset Summary

**Reset all frontend pages to shadcn/ui neutral default palette with 0.625rem border radius and fixed oklch variable references**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-02T07:19:55Z
- **Completed:** 2026-03-02T07:22:59Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Replaced entire Stone+Green color palette with shadcn/ui neutral defaults in both :root and .dark blocks
- Restored rounded corners across all components via --radius: 0.625rem
- Fixed 14 hsl(var(--...)) instances in FullCalendar plus 2 in sidebar outline variant
- Replaced all hardcoded stone-* color classes with semantic tokens across 3 components
- Used color-mix(in oklch) for opacity variants in FullCalendar CSS (oklch-compatible replacement for hsl/a opacity)

## Task Commits

Each task was committed atomically:

1. **Task 1: Reset CSS variables to shadcn/ui neutral defaults** - `b97e4ca` (feat)
2. **Task 2: Fix hsl(oklch) bugs and replace hardcoded color classes** - `d7eddf3` (fix)

## Files Created/Modified
- `frontend/src/styles/globals.css` - Reset all :root and .dark CSS variables to shadcn/ui neutral defaults, --radius: 0.625rem
- `frontend/src/features/scheduling/components/AppointmentCalendar.tsx` - Fixed 14 hsl(var()) to var(), removed border-radius: 0, added calc(var(--radius)) rounding
- `frontend/src/shared/components/ui/sidebar.tsx` - Fixed hsl(var(--sidebar-border)) and hsl(var(--sidebar-accent)) in outline variant
- `frontend/src/features/booking/components/PublicBookingPage.tsx` - Replaced bg-stone-50 with bg-muted, bg-white with bg-card
- `frontend/src/app/routes/book/status.tsx` - Replaced bg-stone-50 with bg-muted, bg-white with bg-card
- `frontend/src/features/audit/components/AuditLogTable.tsx` - Replaced stone-* badge colors for Logout action with semantic tokens

## Decisions Made
- Used `color-mix(in oklch, var(--accent) 15%, transparent)` for FullCalendar opacity variants instead of hsl/a syntax -- this is the correct approach for oklch CSS variables
- FullCalendar buttons get `calc(var(--radius) - 2px)` and events get `calc(var(--radius) - 4px)` for size-proportionate rounding
- Kept `bg-white` in LoginPage branding panel -- those are intentional overlay effects on the primary background, not theme-inconsistent hardcodes
- Added `rounded-lg` to card containers in PublicBookingPage and book/status pages so they inherit the new border radius

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All pages now use shadcn/ui neutral theme with rounded corners
- Zero hsl(var()) wrappers and zero stone-* hardcoded classes remain
- Ready for Plan 04 (full page-by-page visual audit)

## Self-Check: PASSED

- All 6 modified files exist on disk
- Commit b97e4ca (Task 1) verified in git log
- Commit d7eddf3 (Task 2) verified in git log
- SUMMARY.md created at expected path

---
*Phase: 02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve*
*Completed: 2026-03-02*
