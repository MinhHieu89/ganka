---
phase: 02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve
plan: 04
type: execute
wave: 3
depends_on:
  - 02.1-02
  - 02.1-03
files_modified: []
autonomous: false
requirements:
  - AUTH-04
  - UI-01
  - UI-02

must_haves:
  truths:
    - "User can log in, refresh the browser, and remain authenticated"
    - "All pages use neutral shadcn/ui theme with rounded corners"
    - "No visual artifacts from old theme (green accents, zero radius, broken colors)"
    - "Logout works and clears session"
    - "Token refresh happens silently without user intervention"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 02.1 changes work correctly end-to-end.

Purpose: Confirm that session persistence (HTTP-only cookie), auth flow, and visual theme reset all function correctly together. This is the final gate before marking Phase 02.1 complete.

Output: Verified working system with persistent sessions and neutral shadcn/ui theme.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve/02.1-01-SUMMARY.md
@.planning/phases/02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve/02.1-02-SUMMARY.md
@.planning/phases/02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve/02.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <action>
    Run all automated verification:

    1. **Backend tests:** All auth tests pass (unit + integration):
       ```bash
       cd backend && dotnet test tests/Auth.Unit.Tests/ --verbosity normal
       cd backend && dotnet test tests/Auth.Integration.Tests/ --verbosity normal
       ```

    2. **Frontend TypeScript:** No compilation errors:
       ```bash
       cd frontend && npx tsc --noEmit
       ```

    3. **No hsl(var()) remnants:**
       ```bash
       grep -r "hsl(var(--" frontend/src/ --include="*.tsx" --include="*.ts" --include="*.css"
       ```
       Expected: zero results.

    4. **No hardcoded stone-* classes:**
       ```bash
       grep -r "bg-stone-\|text-stone-\|border-stone-" frontend/src/ --include="*.tsx" --include="*.ts"
       ```
       Expected: zero results.

    5. **No refreshToken in frontend types/code (except type definitions in node_modules):**
       ```bash
       grep -r "refreshToken\|refresh_token" frontend/src/ --include="*.tsx" --include="*.ts"
       ```
       Expected: zero results (refresh token is invisible to frontend JS).

    6. **CSS variables check:** Verify --radius is 0.625rem:
       ```bash
       grep "radius" frontend/src/styles/globals.css
       ```

    7. **Start backend and frontend:** Kill any processes on ports 5255 and 3000, then start both:
       ```bash
       cd backend/src/Bootstrapper && dotnet run --urls "http://localhost:5255" &
       cd frontend && npm run dev &
       ```
       Both should start without errors.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Auth.Unit.Tests/ --verbosity minimal && dotnet test tests/Auth.Integration.Tests/ --verbosity minimal && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - All backend tests pass
    - Frontend TypeScript compiles
    - No hsl(var()) remnants
    - No hardcoded stone-* classes
    - No refreshToken in frontend code
    - Both servers start successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of session persistence and visual theme</name>
  <files></files>
  <action>
    Present the following verification checklist to the user. Backend should be running on port 5255 and frontend on port 3000.

    **What was built:**
    1. Session persistence: HTTP-only cookie refresh token. Login -> refresh browser -> still logged in. Silent refresh restores access token from cookie on page load.
    2. Theme reset: All pages use shadcn/ui neutral default theme with rounded corners. No green accents, no zero-radius corners, no broken hsl(oklch) colors.

    **Session Persistence (AUTH-04) verification steps:**
    1. Open http://localhost:3000/login
    2. Log in with valid credentials (do NOT check "Remember me")
    3. After login, open browser DevTools > Application > Cookies. Verify a refresh_token cookie exists with HttpOnly: true, Path: /api/auth, no Max-Age (session cookie)
    4. Refresh the browser page (F5). Should remain on authenticated page, NOT redirect to login. Network tab should show POST to /api/auth/refresh returning 200.
    5. Click Logout. Verify cookie is cleared.
    6. Refresh page. Should redirect to login.
    7. Log in WITH "Remember me" checked. Cookie should have Max-Age ~2592000 (30 days).
    8. Close browser tab, reopen http://localhost:3000. Should be auto-logged-in.

    **Visual Theme (UI-01, UI-02) verification steps:**
    9. Login page: shadcn/ui neutral aesthetic, rounded corners.
    10. Dashboard/sidebar: neutral colors, rounded components, no green accents.
    11. Patient list: rounded corners, neutral palette.
    12. Patient profile: tabs, forms, allergy alerts themed correctly.
    13. Scheduling calendar: correct colors (not black/broken), rounded corners.
    14. Public booking page (http://localhost:3000/book): consistent neutral styling.
    15. Language toggle: Vietnamese and English both display correctly.
  </action>
  <verify>
    <manual>User confirms all 15 verification steps pass by typing "approved" or describing issues.</manual>
  </verify>
  <done>
    - User approves session persistence behavior
    - User approves visual theme across all pages
    - Phase 02.1 is complete
  </done>
</task>

</tasks>

<verification>
All automated checks pass and human verification confirms:
- Session persists across browser refresh
- Cookie has correct flags (HttpOnly, SameSite, Path)
- Remember me produces persistent cookie, non-remember-me produces session cookie
- All pages use neutral shadcn/ui theme
- All components have rounded corners
- No visual artifacts from old theme
</verification>

<success_criteria>
- Human approves session persistence behavior
- Human approves visual theme across all pages
- All automated tests pass
- Phase 02.1 is complete
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-i-notice-some-bugs-in-frontend-that-i-want-to-resolve/02.1-04-SUMMARY.md`
</output>
