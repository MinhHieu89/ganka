---
phase: 11-granular-permission-enforcement
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - backend/tests/Ganka28.ArchitectureTests/PermissionEnforcementTests.cs
autonomous: true
requirements: [AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Architecture test exists that checks all *ApiEndpoints.cs files for RequirePermissions calls"
    - "Test FAILS (RED) because most endpoint files do not yet have RequirePermissions calls"
    - "Test correctly excludes PublicBookingEndpoints.cs and PublicOsdiEndpoints.cs"
    - "Test correctly excludes TreatmentApiEndpoints.cs (already guarded reference)"
  artifacts:
    - path: "backend/tests/Ganka28.ArchitectureTests/PermissionEnforcementTests.cs"
      provides: "Architecture test validating permission enforcement coverage"
      contains: "RequirePermissions"
  key_links:
    - from: "PermissionEnforcementTests.cs"
      to: "All *ApiEndpoints.cs files"
      via: "File-based source scanning to verify RequirePermissions presence"
      pattern: "RequirePermissions"
---

<objective>
Create the RED-phase architecture test that validates all API endpoint files have RequirePermissions calls. This test MUST FAIL initially because Wave 1 plans (11-01) have not yet added the permission calls. Wave 1 implementation will turn this test GREEN.

Purpose: Satisfy TDD strict requirement from CLAUDE.md -- failing tests must exist BEFORE implementation. This test also serves as permanent regression protection against future unguarded endpoints.

Output: PermissionEnforcementTests.cs with failing tests (RED phase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-granular-permission-enforcement/11-CONTEXT.md
@.planning/phases/11-granular-permission-enforcement/11-RESEARCH.md

<interfaces>
<!-- Existing architecture test project -->
From backend/tests/Ganka28.ArchitectureTests/Ganka28.ArchitectureTests.csproj:
<!-- Uses xUnit + FluentAssertions -->
<!-- Existing tests: DependencyDirectionTests.cs, ModuleBoundaryTests.cs, SharedKernelTests.cs -->

From backend/src/Shared/Shared.Presentation/EndpointAuthorizationExtensions.cs:
```csharp
public static TBuilder RequirePermissions<TBuilder>(this TBuilder builder, params string[] permissions)
    where TBuilder : IEndpointConventionBuilder
// OR-logic: user needs at least ONE of the specified permissions
```

<!-- Reference implementation that IS already guarded -->
From backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs:
```csharp
group.MapPost("/protocols", handler).RequirePermissions(Permissions.Treatment.Create);
group.MapGet("/protocols", handler).RequirePermissions(Permissions.Treatment.View);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing architecture tests for permission enforcement (RED phase)</name>
  <files>
    backend/tests/Ganka28.ArchitectureTests/PermissionEnforcementTests.cs
  </files>
  <read_first>
    backend/tests/Ganka28.ArchitectureTests/Ganka28.ArchitectureTests.csproj,
    backend/tests/Ganka28.ArchitectureTests/DependencyDirectionTests.cs,
    backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs,
    backend/src/Shared/Shared.Presentation/EndpointAuthorizationExtensions.cs
  </read_first>
  <behavior>
    - Test 1 (AllNonPublicEndpointFiles_MustContain_RequirePermissionsCalls): Scans all *ApiEndpoints.cs source files under backend/src/. Asserts each file (except PublicBookingEndpoints.cs, PublicOsdiEndpoints.cs) contains at least one `RequirePermissions(Permissions.` call. EXPECTED TO FAIL (RED) because Patient, Clinical, Scheduling, Pharmacy, Optical, Billing, Auth, Audit, Settings endpoint files do not yet have RequirePermissions calls.
    - Test 2 (NoEndpointFile_ShouldUse_StringLiteralPermissions): Asserts no endpoint file contains `RequirePermissions("` (string literal instead of Permissions.* constant). EXPECTED TO PASS even now since no files use string literals yet.
  </behavior>
  <action>
    Create `PermissionEnforcementTests.cs` in the architecture tests project.

    **CRITICAL TDD REQUIREMENT:** After creating the test file, run the tests. Test 1 MUST FAIL (RED). This is the expected state -- the test documents what Wave 1 must achieve. Do NOT fix the failures. Commit the failing test.

    Implementation approach:
    1. Use `Directory.GetFiles()` to recursively find all `*ApiEndpoints.cs` files under `backend/src/Modules/` and `backend/src/Shared/`.
    2. Define an exclusion list: `PublicBookingEndpoints.cs`, `PublicOsdiEndpoints.cs`.
    3. For each non-excluded file, read its content and assert it contains `RequirePermissions(Permissions.`.
    4. For Test 2, scan all endpoint files and assert none contain `RequirePermissions("` (double-quote after opening paren).

    Use the same test project conventions as existing tests (xUnit [Fact], FluentAssertions). Use relative path resolution from the test assembly location to find `backend/src/` directory (navigate up from bin/Debug to the repo root).

    **Commit message:** `test(11-00): add failing permission enforcement architecture tests (RED)`
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v q --filter "PermissionEnforcement" 2>&1 || echo "EXPECTED: Test 1 should FAIL (RED phase). Test 2 may pass."</automated>
  </verify>
  <done>PermissionEnforcementTests.cs exists with 2 test methods. Test 1 (AllNonPublicEndpointFiles_MustContain_RequirePermissionsCalls) FAILS because endpoint files lack RequirePermissions calls. Test 2 (NoEndpointFile_ShouldUse_StringLiteralPermissions) passes. This is the correct RED state for TDD.</done>
</task>

</tasks>

<verification>
- `dotnet build backend/tests/Ganka28.ArchitectureTests/` compiles successfully
- `dotnet test --filter "PermissionEnforcement"` shows Test 1 FAILING (RED) and Test 2 passing
- PermissionEnforcementTests.cs excludes PublicBookingEndpoints.cs and PublicOsdiEndpoints.cs
- PermissionEnforcementTests.cs uses `Permissions.` constant pattern check, not hardcoded strings
</verification>

<success_criteria>
Architecture test file exists, compiles, and correctly FAILS on Test 1 (RED phase). This establishes the TDD contract that Wave 1 plans must satisfy.
</success_criteria>

<output>
After completion, create `.planning/phases/11-granular-permission-enforcement/11-00-SUMMARY.md`
</output>
