---
phase: 11-granular-permission-enforcement
plan: 01
type: execute
wave: 1
depends_on: [11-00]
files_modified:
  - backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs
  - backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs
  - backend/src/Modules/Scheduling/Scheduling.Presentation/SchedulingApiEndpoints.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Presentation/DispensingApiEndpoints.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Presentation/ConsumablesApiEndpoints.cs
  - backend/src/Modules/Optical/Optical.Presentation/OpticalApiEndpoints.cs
  - backend/src/Modules/Optical/Optical.Presentation/WarrantyApiEndpoints.cs
  - backend/src/Modules/Optical/Optical.Presentation/StocktakingApiEndpoints.cs
  - backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs
  - backend/src/Modules/Auth/Auth.Presentation/AuthApiEndpoints.cs
  - backend/src/Modules/Audit/Audit.Presentation/AuditApiEndpoints.cs
  - backend/src/Shared/Shared.Presentation/SettingsApiEndpoints.cs
autonomous: true
requirements: [AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Every module API endpoint has a granular RequirePermissions() call matching its CRUD action"
    - "A user without the required permission receives 403 Forbidden on protected endpoints"
    - "Public/unauthenticated endpoints (login, refresh, public booking, OSDI public) remain unguarded"
    - "Dashboard stats endpoint remains accessible to all authenticated users"
    - "Architecture test from 11-00 now passes (GREEN phase)"
  artifacts:
    - path: "backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs"
      provides: "Patient module permission enforcement"
      contains: "RequirePermissions(Permissions.Patient"
    - path: "backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs"
      provides: "Clinical module permission enforcement"
      contains: "RequirePermissions(Permissions.Clinical"
    - path: "backend/src/Modules/Scheduling/Scheduling.Presentation/SchedulingApiEndpoints.cs"
      provides: "Scheduling module permission enforcement"
      contains: "RequirePermissions(Permissions.Scheduling"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs"
      provides: "Pharmacy module permission enforcement"
      contains: "RequirePermissions(Permissions.Pharmacy"
    - path: "backend/src/Modules/Auth/Auth.Presentation/AuthApiEndpoints.cs"
      provides: "Auth admin endpoint permission enforcement"
      contains: "RequirePermissions(Permissions.Auth"
    - path: "backend/src/Modules/Audit/Audit.Presentation/AuditApiEndpoints.cs"
      provides: "Audit endpoint permission enforcement"
      contains: "RequirePermissions(Permissions.Audit"
    - path: "backend/src/Shared/Shared.Presentation/SettingsApiEndpoints.cs"
      provides: "Settings endpoint permission enforcement"
      contains: "RequirePermissions(Permissions.Settings"
  key_links:
    - from: "Each *ApiEndpoints.cs"
      to: "Shared.Domain.Permissions"
      via: "using Shared.Domain; + RequirePermissions(Permissions.Module.Action)"
      pattern: "RequirePermissions\\(Permissions\\."
    - from: "Each *ApiEndpoints.cs"
      to: "Shared.Presentation.EndpointAuthorizationExtensions"
      via: "using Shared.Presentation; + .RequirePermissions() extension method"
      pattern: "using Shared\\.Presentation;"
---

<objective>
Add granular permission enforcement to ALL backend API endpoints using the existing `RequirePermissions()` extension method and `Permissions` static class constants. This is the GREEN phase -- making the failing architecture test from 11-00 pass.

Purpose: Close AUTH-04/AUTH-05 gaps -- currently endpoints only check JWT authentication (RequireAuthorization) but do not enforce module-level CRUD permissions. After this plan, every endpoint verifies the caller has the specific permission (e.g., `Patient.View`, `Billing.Create`).

Output: All 13 backend API endpoint files updated with per-endpoint `RequirePermissions()` calls. Architecture test passes (GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-granular-permission-enforcement/11-CONTEXT.md
@.planning/phases/11-granular-permission-enforcement/11-RESEARCH.md
@.planning/phases/11-granular-permission-enforcement/11-00-SUMMARY.md

<interfaces>
<!-- Permission constants - use these exact strings -->
From backend/src/Shared/Shared.Domain/Permissions.cs:
```csharp
public static class Permissions
{
    public const string ClaimType = "permissions";
    public static class Auth { View, Create, Update, Delete, Export, Manage }
    public static class Patient { View, Create, Update, Delete, Export, Manage }
    public static class Clinical { View, Create, Update, Delete, Export, Manage }
    public static class Scheduling { View, Create, Update, Delete, Export, Manage }
    public static class Pharmacy { View, Create, Update, Delete, Export, Manage }
    public static class Optical { View, Create, Update, Delete, Export, Manage }
    public static class Billing { View, Create, Update, Delete, Export, Manage }
    public static class Treatment { View, Create, Update, Delete, Export, Manage }
    public static class Audit { View, Create, Update, Delete, Export, Manage }
    public static class Settings { View, Create, Update, Delete, Export, Manage }
}
```

From backend/src/Shared/Shared.Presentation/EndpointAuthorizationExtensions.cs:
```csharp
public static TBuilder RequirePermissions<TBuilder>(this TBuilder builder, params string[] permissions)
    where TBuilder : IEndpointConventionBuilder
// OR-logic: user needs at least ONE of the specified permissions
```

<!-- Reference implementation -->
From backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs:
```csharp
group.MapPost("/protocols", handler).RequirePermissions(Permissions.Treatment.Create);
group.MapGet("/protocols", handler).RequirePermissions(Permissions.Treatment.View);
group.MapPut("/protocols/{id:guid}", handler).RequirePermissions(Permissions.Treatment.Update);
group.MapDelete("/protocols/{id}", handler).RequirePermissions(Permissions.Treatment.Delete);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RequirePermissions to Patient, Clinical, Scheduling, and Auth/Audit/Settings endpoints</name>
  <files>
    backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs,
    backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs,
    backend/src/Modules/Scheduling/Scheduling.Presentation/SchedulingApiEndpoints.cs,
    backend/src/Modules/Auth/Auth.Presentation/AuthApiEndpoints.cs,
    backend/src/Modules/Audit/Audit.Presentation/AuditApiEndpoints.cs,
    backend/src/Shared/Shared.Presentation/SettingsApiEndpoints.cs
  </files>
  <read_first>
    backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs,
    backend/src/Shared/Shared.Domain/Permissions.cs,
    backend/src/Shared/Shared.Presentation/EndpointAuthorizationExtensions.cs,
    backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs,
    backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs,
    backend/src/Modules/Scheduling/Scheduling.Presentation/SchedulingApiEndpoints.cs,
    backend/src/Modules/Auth/Auth.Presentation/AuthApiEndpoints.cs,
    backend/src/Modules/Audit/Audit.Presentation/AuditApiEndpoints.cs,
    backend/src/Shared/Shared.Presentation/SettingsApiEndpoints.cs
  </read_first>
  <action>
    Add `.RequirePermissions(Permissions.Module.Action)` to every endpoint in each file following the Treatment reference pattern (per D-01, D-02, D-03). Ensure `using Shared.Domain;` and `using Shared.Presentation;` imports are present in each file.

    **Permission mapping per module (per D-01 module-level CRUD mapping):**

    **PatientApiEndpoints.cs** (per D-03):
    - POST endpoints (create patient) -> `.RequirePermissions(Permissions.Patient.Create)`
    - GET endpoints (get patient, list, search, recent, field-validation) -> `.RequirePermissions(Permissions.Patient.View)`
    - PUT endpoints (update patient) -> `.RequirePermissions(Permissions.Patient.Update)`
    - POST deactivate/reactivate -> `.RequirePermissions(Permissions.Patient.Update)`
    - POST allergies, POST photo -> `.RequirePermissions(Permissions.Patient.Update)`
    - DELETE allergies -> `.RequirePermissions(Permissions.Patient.Update)`
    - GET /api/dashboard/stats -> DO NOT ADD RequirePermissions (leave with just RequireAuthorization -- dashboard accessible to all authenticated users, per research pitfall 3)

    **ClinicalApiEndpoints.cs** (per D-03):
    - POST create visit -> `.RequirePermissions(Permissions.Clinical.Create)`
    - GET endpoints (visit details, active visits, ICD-10, OSDI history, comparisons, images) -> `.RequirePermissions(Permissions.Clinical.View)`
    - PUT/POST data endpoints (sign-off, cancel, amend, stage, notes, refraction, diagnoses, dry-eye, images, prescriptions) -> `.RequirePermissions(Permissions.Clinical.Update)`
    - DELETE endpoints -> `.RequirePermissions(Permissions.Clinical.Delete)`
    - Print endpoints -> `.RequirePermissions(Permissions.Clinical.View)`
    - DO NOT modify PublicOsdiEndpoints.cs -- it is intentionally public (per research pitfall 2)

    **SchedulingApiEndpoints.cs** (per D-03):
    - POST create appointment -> `.RequirePermissions(Permissions.Scheduling.Create)`
    - GET endpoints (list, calendar, slots) -> `.RequirePermissions(Permissions.Scheduling.View)`
    - PUT cancel/reschedule -> `.RequirePermissions(Permissions.Scheduling.Update)`
    - POST approve/reject self-booking -> `.RequirePermissions(Permissions.Scheduling.Update)`
    - DO NOT modify PublicBookingEndpoints.cs -- it is intentionally public (per research pitfall 2)

    **AuthApiEndpoints.cs** (per D-04):
    - Admin user management group only (NOT /api/auth/* login/refresh/logout/me/language which are unauthenticated or basic auth):
    - GET /api/admin/users -> `.RequirePermissions(Permissions.Auth.View)`
    - POST /api/admin/users -> `.RequirePermissions(Permissions.Auth.Create)`
    - PUT /api/admin/users/{id} -> `.RequirePermissions(Permissions.Auth.Update)`
    - PUT /api/admin/users/{id}/roles -> `.RequirePermissions(Permissions.Auth.Update)`
    - GET /api/admin/roles -> `.RequirePermissions(Permissions.Auth.View)`
    - POST /api/admin/roles -> `.RequirePermissions(Permissions.Auth.Create)`
    - PUT /api/admin/roles/{id}/permissions -> `.RequirePermissions(Permissions.Auth.Update)`
    - GET /api/admin/permissions -> `.RequirePermissions(Permissions.Auth.View)`

    **AuditApiEndpoints.cs** (per D-04):
    - GET /api/admin/audit-logs -> `.RequirePermissions(Permissions.Audit.View)`
    - GET /api/admin/audit-logs/export -> `.RequirePermissions(Permissions.Audit.Export)` (per research pitfall 7, export uses Audit.Export not Audit.View)
    - GET /api/admin/access-logs -> `.RequirePermissions(Permissions.Audit.View)`

    **SettingsApiEndpoints.cs** (per D-04):
    - GET /api/settings/clinic -> `.RequirePermissions(Permissions.Settings.View)`
    - PUT /api/settings/clinic -> `.RequirePermissions(Permissions.Settings.Update)`
    - POST /api/settings/clinic/logo -> `.RequirePermissions(Permissions.Settings.Update)`

    **IMPORTANT per D-02:** Do NOT auto-expand `.Manage` to cover individual actions. Use explicit permission matching only. The seeder already assigns all individual permissions per role.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Modules/Patient/Patient.Presentation/ --no-restore -v q && dotnet build backend/src/Modules/Clinical/Clinical.Presentation/ --no-restore -v q && dotnet build backend/src/Modules/Scheduling/Scheduling.Presentation/ --no-restore -v q && dotnet build backend/src/Modules/Auth/Auth.Presentation/ --no-restore -v q && dotnet build backend/src/Modules/Audit/Audit.Presentation/ --no-restore -v q && dotnet build backend/src/Shared/Shared.Presentation/ --no-restore -v q</automated>
  </verify>
  <done>All Patient, Clinical, Scheduling, Auth, Audit, and Settings endpoint files have per-endpoint RequirePermissions() calls. Public endpoints untouched. All projects compile.</done>
</task>

<task type="auto">
  <name>Task 2: Add RequirePermissions to Pharmacy, Optical, and Billing endpoints</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/DispensingApiEndpoints.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/ConsumablesApiEndpoints.cs,
    backend/src/Modules/Optical/Optical.Presentation/OpticalApiEndpoints.cs,
    backend/src/Modules/Optical/Optical.Presentation/WarrantyApiEndpoints.cs,
    backend/src/Modules/Optical/Optical.Presentation/StocktakingApiEndpoints.cs,
    backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs
  </files>
  <read_first>
    backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs,
    backend/src/Shared/Shared.Domain/Permissions.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/DispensingApiEndpoints.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/ConsumablesApiEndpoints.cs,
    backend/src/Modules/Optical/Optical.Presentation/OpticalApiEndpoints.cs,
    backend/src/Modules/Optical/Optical.Presentation/WarrantyApiEndpoints.cs,
    backend/src/Modules/Optical/Optical.Presentation/StocktakingApiEndpoints.cs,
    backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs
  </read_first>
  <action>
    Add `.RequirePermissions(Permissions.Module.Action)` to every endpoint in each file following the Treatment reference pattern (per D-01, D-02, D-03). Ensure `using Shared.Domain;` and `using Shared.Presentation;` imports are present.

    **PharmacyApiEndpoints.cs** (per D-03):
    - GET endpoints (drugs, suppliers, stock-imports, batches, alerts, search) -> `.RequirePermissions(Permissions.Pharmacy.View)`
    - POST create endpoints (suppliers, stock-imports, drugs) -> `.RequirePermissions(Permissions.Pharmacy.Create)`
    - PUT update endpoints -> `.RequirePermissions(Permissions.Pharmacy.Update)`
    - Drug catalog import (preview + confirm) -> `.RequirePermissions(Permissions.Pharmacy.Create)`

    **DispensingApiEndpoints.cs** (per D-03):
    - GET endpoints (dispensing history, pending, queue) -> `.RequirePermissions(Permissions.Pharmacy.View)`
    - POST dispense -> `.RequirePermissions(Permissions.Pharmacy.Create)`
    - POST OTC sale -> `.RequirePermissions(Permissions.Pharmacy.Create)`

    **ConsumablesApiEndpoints.cs** (per D-03, per research pitfall 4 -- uses Pharmacy permissions):
    - GET endpoints -> `.RequirePermissions(Permissions.Pharmacy.View)`
    - POST create endpoints -> `.RequirePermissions(Permissions.Pharmacy.Create)`
    - PUT update endpoints -> `.RequirePermissions(Permissions.Pharmacy.Update)`

    **OpticalApiEndpoints.cs** (per D-03):
    - GET endpoints (frames, lenses, orders, combos, search) -> `.RequirePermissions(Permissions.Optical.View)`
    - POST create endpoints (frames, lenses, orders, combos) -> `.RequirePermissions(Permissions.Optical.Create)`
    - PUT update endpoints (frame, lens, order status) -> `.RequirePermissions(Permissions.Optical.Update)`
    - DELETE endpoints -> `.RequirePermissions(Permissions.Optical.Delete)`
    - Export endpoints -> `.RequirePermissions(Permissions.Optical.Export)`

    **WarrantyApiEndpoints.cs** (per D-03):
    - GET endpoints -> `.RequirePermissions(Permissions.Optical.View)`
    - POST create warranty claim -> `.RequirePermissions(Permissions.Optical.Create)`
    - PUT update/upload -> `.RequirePermissions(Permissions.Optical.Update)`
    - POST approve (if already guarded with Optical.Manage, leave as-is)

    **StocktakingApiEndpoints.cs** (per D-03):
    - GET endpoints -> `.RequirePermissions(Permissions.Optical.View)`
    - POST create stocktaking session -> `.RequirePermissions(Permissions.Optical.Create)`
    - PUT update count -> `.RequirePermissions(Permissions.Optical.Update)`
    - POST complete (if already guarded with Optical.Manage, leave as-is)

    **BillingApiEndpoints.cs** (per D-03):
    - GET endpoints (invoices, payments, shifts, revenue) -> `.RequirePermissions(Permissions.Billing.View)`
    - POST create invoice/payment -> `.RequirePermissions(Permissions.Billing.Create)`
    - POST discount/refund request -> `.RequirePermissions(Permissions.Billing.Create)`
    - POST discount/refund approve -> `.RequirePermissions(Permissions.Billing.Manage)`
    - POST shift open/close -> `.RequirePermissions(Permissions.Billing.Create)`
    - Service catalog endpoints (if already guarded with Billing.Manage, leave as-is)
    - Print/export endpoints -> `.RequirePermissions(Permissions.Billing.View)` or `.RequirePermissions(Permissions.Billing.Export)` for CSV exports
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Modules/Pharmacy/Pharmacy.Presentation/ --no-restore -v q && dotnet build backend/src/Modules/Optical/Optical.Presentation/ --no-restore -v q && dotnet build backend/src/Modules/Billing/Billing.Presentation/ --no-restore -v q</automated>
  </verify>
  <done>All Pharmacy (including Dispensing and Consumables), Optical (including Warranty and Stocktaking), and Billing endpoint files have per-endpoint RequirePermissions() calls. All projects compile.</done>
</task>

<task type="auto">
  <name>Task 3: Verify architecture test passes (GREEN phase)</name>
  <files>none</files>
  <action>
    Run the architecture test from 11-00 to confirm all endpoint files now have RequirePermissions calls. The test that was RED in Wave 0 must now be GREEN after Tasks 1 and 2.

    Run: `dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v q --filter "PermissionEnforcement"`

    Both tests must pass:
    1. AllNonPublicEndpointFiles_MustContain_RequirePermissionsCalls -- GREEN (all files now have calls)
    2. NoEndpointFile_ShouldUse_StringLiteralPermissions -- GREEN (no string literals used)

    If any test fails, identify which endpoint file is missing RequirePermissions calls and fix it before proceeding.

    **Commit message:** `feat(11-01): add RequirePermissions to all backend endpoints (GREEN)`
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v q --filter "PermissionEnforcement"</automated>
  </verify>
  <done>Architecture test passes (GREEN). All non-public endpoint files have RequirePermissions calls using Permissions.* constants. TDD red-green cycle complete for backend.</done>
</task>

</tasks>

<verification>
- `dotnet build backend/ --no-restore -v q` compiles without errors
- `dotnet test --filter "PermissionEnforcement"` passes (GREEN -- was RED in 11-00)
- Every `*ApiEndpoints.cs` file (except Treatment which is already done) contains at least one `RequirePermissions(Permissions.` call
- PublicBookingEndpoints.cs and PublicOsdiEndpoints.cs contain zero `RequirePermissions` calls
- grep across all endpoint files confirms no endpoint is left with only `RequireAuthorization()` and no `RequirePermissions()` (except dashboard stats and auth flow endpoints)
</verification>

<success_criteria>
All 13 backend API endpoint files have granular per-endpoint permission enforcement. Architecture test from 11-00 now passes (GREEN). The backend compiles. Public endpoints remain unguarded. Auth flow endpoints (login/refresh/logout/me) remain with basic RequireAuthorization only.
</success_criteria>

<output>
After completion, create `.planning/phases/11-granular-permission-enforcement/11-01-SUMMARY.md`
</output>
