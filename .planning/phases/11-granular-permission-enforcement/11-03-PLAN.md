---
phase: 11-granular-permission-enforcement
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - backend/tests/Ganka28.ArchitectureTests/PermissionEnforcementTests.cs
autonomous: false
requirements: [AUTH-04, AUTH-05]

must_haves:
  truths:
    - "An architecture test verifies all endpoint groups (except public ones) have RequirePermissions calls"
    - "Full backend solution builds and existing tests pass"
    - "A user with restricted role sees only permitted sidebar items and gets 403/redirect on forbidden resources"
  artifacts:
    - path: "backend/tests/Ganka28.ArchitectureTests/PermissionEnforcementTests.cs"
      provides: "Architecture test validating permission enforcement coverage"
      contains: "RequirePermissions"
  key_links:
    - from: "PermissionEnforcementTests.cs"
      to: "All *ApiEndpoints.cs files"
      via: "Reflection or source scanning to verify RequirePermissions presence"
      pattern: "RequirePermissions"
---

<objective>
Validate the complete permission enforcement sweep with an architecture test and human verification.

Purpose: Ensure no endpoints were missed during the sweep and that the system works end-to-end for restricted roles. The architecture test provides automated regression protection against future unguarded endpoints.

Output: Architecture test file, full build verification, human-verified end-to-end permission flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-granular-permission-enforcement/11-CONTEXT.md
@.planning/phases/11-granular-permission-enforcement/11-RESEARCH.md
@.planning/phases/11-granular-permission-enforcement/11-01-SUMMARY.md
@.planning/phases/11-granular-permission-enforcement/11-02-SUMMARY.md

<interfaces>
From backend/tests/Ganka28.ArchitectureTests/:
<!-- Existing architecture test project -- add new test class here -->
<!-- Uses xUnit + FluentAssertions -->
<!-- Tests verify structural rules across the codebase -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create architecture test verifying permission enforcement coverage</name>
  <files>
    backend/tests/Ganka28.ArchitectureTests/PermissionEnforcementTests.cs
  </files>
  <read_first>
    backend/tests/Ganka28.ArchitectureTests/Ganka28.ArchitectureTests.csproj,
    backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs,
    backend/src/Shared/Shared.Presentation/EndpointAuthorizationExtensions.cs
  </read_first>
  <behavior>
    - Test 1: All *ApiEndpoints.cs files (except PublicBookingEndpoints.cs and PublicOsdiEndpoints.cs) must contain at least one call to `RequirePermissions(`
    - Test 2: No *ApiEndpoints.cs file uses string literals for permissions -- all must use `Permissions.` constants (compile-time safety)
  </behavior>
  <action>
    Create `PermissionEnforcementTests.cs` in the architecture tests project with xUnit tests that scan the source files of all `*ApiEndpoints.cs` to verify:

    1. **Every non-public endpoint file has RequirePermissions calls**: Read the source files of all `*ApiEndpoints.cs` classes. Verify each contains at least one `RequirePermissions(Permissions.` string. Exclude `PublicBookingEndpoints.cs` and `PublicOsdiEndpoints.cs`.

    2. **No raw string permission literals**: Verify that no endpoint file contains `RequirePermissions("` (string literal) -- they should all use `RequirePermissions(Permissions.Module.Action)` constants.

    The test can use file-based scanning (read .cs files from the source tree) since the permission calls are compile-time chained methods, not runtime-discoverable. Use `Directory.GetFiles()` with recursive search for `*ApiEndpoints.cs` in the `backend/src/` directory.

    After creating the test, run it to verify it passes (GREEN). Then run the full test suite to ensure nothing is broken.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v q --filter "PermissionEnforcement"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/Ganka28.ArchitectureTests/PermissionEnforcementTests.cs` exists
    - File contains `[Fact]` test attribute
    - File contains assertions checking for `RequirePermissions` in endpoint files
    - File excludes `PublicBookingEndpoints` and `PublicOsdiEndpoints` from the check
    - Test passes when run with `dotnet test --filter "PermissionEnforcement"`
    - Full backend build passes: `dotnet build backend/ --no-restore -v q`
  </acceptance_criteria>
  <done>Architecture test validates all non-public endpoint files have RequirePermissions calls. Test passes. Full solution builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end permission enforcement</name>
  <files>none</files>
  <action>
    Start backend (port 5255) and frontend (port 3000) for human verification. The executor should ensure both servers are running before presenting the checkpoint to the user.
  </action>
  <what-built>
    Complete granular permission enforcement across all backend API endpoints and frontend routes:
    - 13 backend API endpoint files now have per-endpoint RequirePermissions() calls
    - ~27 frontend routes now have beforeLoad permission guards
    - Sidebar navigation filters items by user permission
    - Architecture test validates no endpoint is left unguarded
  </what-built>
  <how-to-verify>
    The executor will start backend (port 5255) and frontend (port 3000) before this checkpoint.

    1. **Login as Admin (Admin@ganka28.com / Admin@123456)**: Verify all sidebar items are visible, all routes accessible
    2. **Create a test user with limited role** (e.g., Cashier role -- should only have Billing permissions):
       - Go to Admin > Users, create a test user
       - Assign only the Cashier role
    3. **Login as the restricted user**: Verify:
       - Sidebar shows only Dashboard + Billing items (and any other modules the Cashier role has permissions for)
       - Patient, Clinical, Pharmacy, Optical, Treatment, Admin items are HIDDEN from sidebar
       - Direct URL navigation to `/patients` redirects to `/dashboard` with toast error "You do not have permission to access this page"
       - Direct URL navigation to `/admin/users` redirects to `/dashboard` with toast error
    4. **API verification**: Open browser dev tools Network tab:
       - Navigate to a restricted API endpoint directly (e.g., fetch `/api/patients`) -- should return 403 Forbidden
       - Navigate to an allowed endpoint (e.g., fetch `/api/billing/invoices`) -- should return 200
    5. **Treatment routes still work**: Login as Admin, verify treatment routes (already guarded) still function correctly
  </how-to-verify>
  <verify>Human confirms permission enforcement works end-to-end</verify>
  <done>User confirms: admin sees all items, restricted role sees filtered sidebar, direct URL to forbidden route redirects with toast, API returns 403 for unauthorized requests</done>
  <resume-signal>Type "approved" if permission enforcement works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Architecture test passes: `dotnet test --filter "PermissionEnforcement"` exits 0
- Full backend builds: `dotnet build backend/ --no-restore` exits 0
- Frontend TypeScript compiles: `npx tsc --noEmit` exits 0
- Human verification confirms restricted roles see filtered sidebar and get 403/redirect on forbidden resources
</verification>

<success_criteria>
Architecture test validates permission enforcement coverage. Full solution builds. Human confirms end-to-end permission enforcement works for restricted roles (sidebar filtering + route guards + API 403 responses).
</success_criteria>

<output>
After completion, create `.planning/phases/11-granular-permission-enforcement/11-03-SUMMARY.md`
</output>
