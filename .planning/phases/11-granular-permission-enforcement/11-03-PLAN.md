---
phase: 11-granular-permission-enforcement
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified: []
autonomous: false
requirements: [AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Architecture test passes as regression validation after all implementation"
    - "Full backend solution builds and all existing tests pass"
    - "A user with restricted role sees only permitted sidebar items and gets 403/redirect on forbidden resources"
  artifacts: []
  key_links:
    - from: "PermissionEnforcementTests.cs"
      to: "All *ApiEndpoints.cs files"
      via: "Regression test confirming no endpoints lost RequirePermissions during implementation"
      pattern: "RequirePermissions"
---

<objective>
Final validation: re-run architecture test as regression, run full test suite, and human-verify end-to-end permission enforcement.

Purpose: Confirm that all implementation from Wave 1 (plans 11-01 and 11-02) is correct. The architecture test was created in 11-00 (RED), turned GREEN in 11-01. This plan re-runs it as regression validation and adds human verification of the complete flow.

Output: Confirmed passing test suite, human-verified permission enforcement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-granular-permission-enforcement/11-CONTEXT.md
@.planning/phases/11-granular-permission-enforcement/11-RESEARCH.md
@.planning/phases/11-granular-permission-enforcement/11-01-SUMMARY.md
@.planning/phases/11-granular-permission-enforcement/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression validation -- re-run architecture test and full test suite</name>
  <files>none</files>
  <action>
    Run the architecture test created in 11-00 as a regression check to confirm all endpoint files still have RequirePermissions calls after the full implementation sweep.

    1. Run architecture test: `dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v q --filter "PermissionEnforcement"`
       - Both tests must pass (AllNonPublicEndpointFiles_MustContain_RequirePermissionsCalls and NoEndpointFile_ShouldUse_StringLiteralPermissions)

    2. Run full backend build: `dotnet build backend/ --no-restore -v q`
       - Must compile without errors

    3. Run frontend TypeScript check: `cd frontend && npx tsc --noEmit`
       - Must compile without errors

    4. Run full backend test suite: `dotnet test backend/ --no-restore -v q`
       - All existing tests must pass (architecture + unit + integration)

    If any failures, investigate and fix before proceeding to human verification.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet test backend/tests/Ganka28.ArchitectureTests/ --no-restore -v q --filter "PermissionEnforcement" && dotnet build backend/ --no-restore -v q && cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>Architecture test passes as regression. Full backend builds and all tests pass. Frontend TypeScript compiles. No regressions introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end permission enforcement</name>
  <files>none</files>
  <action>
    Start backend (port 5255) and frontend (port 3000) for human verification. The executor should ensure both servers are running before presenting the checkpoint to the user.
  </action>
  <what-built>
    Complete granular permission enforcement across all backend API endpoints and frontend routes:
    - 13 backend API endpoint files now have per-endpoint RequirePermissions() calls
    - ~27 frontend routes now have beforeLoad permission guards
    - Sidebar navigation filters items by user permission
    - Architecture test validates no endpoint is left unguarded
  </what-built>
  <how-to-verify>
    The executor will start backend (port 5255) and frontend (port 3000) before this checkpoint.

    1. **Login as Admin (Admin@ganka28.com / Admin@123456)**: Verify all sidebar items are visible, all routes accessible
    2. **Create a test user with limited role** (e.g., Cashier role -- should only have Billing permissions):
       - Go to Admin > Users, create a test user
       - Assign only the Cashier role
    3. **Login as the restricted user**: Verify:
       - Sidebar shows only Dashboard + Billing items (and any other modules the Cashier role has permissions for)
       - Patient, Clinical, Pharmacy, Optical, Treatment, Admin items are HIDDEN from sidebar
       - Direct URL navigation to `/patients` redirects to `/dashboard` with toast error "You do not have permission to access this page"
       - Direct URL navigation to `/admin/users` redirects to `/dashboard` with toast error
    4. **API verification**: Open browser dev tools Network tab:
       - Navigate to a restricted API endpoint directly (e.g., fetch `/api/patients`) -- should return 403 Forbidden
       - Navigate to an allowed endpoint (e.g., fetch `/api/billing/invoices`) -- should return 200
    5. **Treatment routes still work**: Login as Admin, verify treatment routes (already guarded) still function correctly
  </how-to-verify>
  <verify>Human confirms permission enforcement works end-to-end</verify>
  <done>User confirms: admin sees all items, restricted role sees filtered sidebar, direct URL to forbidden route redirects with toast, API returns 403 for unauthorized requests</done>
  <resume-signal>Type "approved" if permission enforcement works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Architecture test passes: `dotnet test --filter "PermissionEnforcement"` exits 0
- Full backend builds: `dotnet build backend/ --no-restore` exits 0
- Frontend TypeScript compiles: `npx tsc --noEmit` exits 0
- Human verification confirms restricted roles see filtered sidebar and get 403/redirect on forbidden resources
</verification>

<success_criteria>
Architecture test validates permission enforcement coverage as regression. Full solution builds. Human confirms end-to-end permission enforcement works for restricted roles (sidebar filtering + route guards + API 403 responses).
</success_criteria>

<output>
After completion, create `.planning/phases/11-granular-permission-enforcement/11-03-SUMMARY.md`
</output>
