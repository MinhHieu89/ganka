---
phase: 04-dry-eye-template-medical-imaging
plan: 04
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - frontend/src/features/clinical/components/DryEyeSection.tsx
  - frontend/src/features/clinical/components/DryEyeForm.tsx
  - frontend/src/features/clinical/components/OsdiSection.tsx
  - frontend/src/features/clinical/components/OsdiQuestionnaire.tsx
  - frontend/src/features/clinical/components/VisitDetailPage.tsx
  - frontend/src/features/clinical/api/clinical-api.ts
  - frontend/src/features/patient/components/PatientDryEyeTab.tsx
  - frontend/src/features/patient/components/OsdiTrendChart.tsx
  - frontend/src/features/patient/components/DryEyeComparisonPanel.tsx
  - frontend/src/features/patient/components/PatientProfilePage.tsx
  - frontend/public/locales/en/clinical.json
  - frontend/public/locales/vi/clinical.json
autonomous: true
requirements: [DRY-01, DRY-02, DRY-03, DRY-04]

must_haves:
  truths:
    - "Doctor can view and edit dry eye metrics in OD/OS side-by-side grid within visit detail page"
    - "Auto-save on blur with 500ms debounce, consistent with Refraction pattern"
    - "OSDI severity badge shows color-coded classification (green/yellow/orange/red)"
    - "OSDI trend chart shows chronological line chart with severity background bands"
    - "Doctor can compare dry eye metrics between two visits side-by-side with delta indicators"
    - "Form is read-only when visit is signed off"
  artifacts:
    - path: "frontend/src/features/clinical/components/DryEyeForm.tsx"
      provides: "OD/OS grid form for dry eye metrics"
      min_lines: 80
    - path: "frontend/src/features/patient/components/OsdiTrendChart.tsx"
      provides: "recharts line chart with severity bands"
      min_lines: 40
    - path: "frontend/src/features/patient/components/DryEyeComparisonPanel.tsx"
      provides: "Side-by-side visit comparison with deltas"
      min_lines: 60
  key_links:
    - from: "DryEyeForm.tsx"
      to: "/api/clinical/{visitId}/dry-eye"
      via: "mutation with debounced auto-save on blur"
      pattern: "useUpdateDryEye|dry-eye"
    - from: "OsdiTrendChart.tsx"
      to: "/api/clinical/osdi-history/{patientId}"
      via: "TanStack Query hook"
      pattern: "useOsdiHistory|osdi-history"
    - from: "PatientDryEyeTab.tsx"
      to: "OsdiTrendChart + DryEyeComparisonPanel"
      via: "Tab content composition"
      pattern: "OsdiTrendChart|DryEyeComparisonPanel"
---

<objective>
Build the frontend Dry Eye assessment form, OSDI sections, trend chart, and cross-visit comparison UI. Install recharts. Add Dry Eye tab to patient profile page.

Purpose: Complete the user-facing dry eye assessment workflow (DRY-01 through DRY-04).
Output: DryEyeForm (OD/OS grid), OsdiSection (severity badge), OsdiQuestionnaire (12 questions), OsdiTrendChart (recharts), DryEyeComparisonPanel, PatientDryEyeTab.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dry-eye-template-medical-imaging/04-CONTEXT.md
@.planning/phases/04-dry-eye-template-medical-imaging/04-RESEARCH.md
@.planning/phases/04-dry-eye-template-medical-imaging/04-01-SUMMARY.md
@.planning/phases/04-dry-eye-template-medical-imaging/04-02-SUMMARY.md

<interfaces>
<!-- Existing frontend patterns to follow -->

From frontend/src/features/clinical/components/RefractionForm.tsx (pattern to mirror):
```typescript
// Uses grid-cols-[80px_1fr_1fr] for OD/OS layout
// React Hook Form + zodResolver with optionalNumber transform
// Debounced auto-save on blur via useUpdateRefraction mutation
// Read-only when visit.status === 1 (Signed)
const REFRACTION_FIELDS = [
  { key: "Sph", label: "sph", min: -30, max: 30, step: 0.25, unit: "D" },
  // ...
]
```

From frontend/src/features/clinical/components/VisitSection.tsx:
```typescript
// Collapsible card wrapper with defaultOpen prop and headerExtra slot
interface VisitSectionProps {
  title: string
  defaultOpen?: boolean
  headerExtra?: React.ReactNode
  children: React.ReactNode
}
```

From frontend/src/features/clinical/components/VisitDetailPage.tsx:
```typescript
// Renders sections: PatientInfoSection, RefractionSection, ExaminationNotesSection,
// DiagnosisSection, SignOffSection, VisitAmendmentHistory
// Import DryEyeSection and OsdiSection here
```

From frontend/src/features/patient/components/PatientProfilePage.tsx:
```typescript
// Tabs: Overview, Allergies, Appointments
// Add "Dry Eye" tab here
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/shared/components/Tabs"
```

API hooks pattern from clinical-api.ts:
```typescript
export function useUpdateRefraction() {
  return useMutation({ mutationFn: ..., onSuccess: invalidate visit query })
}
export function useVisitById(visitId: string) {
  return useQuery({ queryKey: ["clinical", "visit", visitId], queryFn: ... })
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DryEyeForm, OsdiSection, OsdiQuestionnaire + VisitDetailPage integration</name>
  <files>
    frontend/src/features/clinical/components/DryEyeSection.tsx,
    frontend/src/features/clinical/components/DryEyeForm.tsx,
    frontend/src/features/clinical/components/OsdiSection.tsx,
    frontend/src/features/clinical/components/OsdiQuestionnaire.tsx,
    frontend/src/features/clinical/components/VisitDetailPage.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    frontend/public/locales/en/clinical.json,
    frontend/public/locales/vi/clinical.json
  </files>
  <action>
    **Install recharts:**
    ```bash
    cd frontend && npm install recharts
    ```

    **Extend clinical-api.ts** with new hooks:
    - `useUpdateDryEye(visitId)` -- mutation for PUT /api/clinical/{visitId}/dry-eye
    - `useDryEyeByVisitId(visitId)` -- extracted from visit detail data (or separate query)
    - `useOsdiHistory(patientId)` -- query for GET /api/clinical/osdi-history/{patientId}
    - `useDryEyeComparison(patientId, visitId1, visitId2)` -- query for GET /api/clinical/dry-eye-comparison
    - `useGenerateOsdiLink(visitId)` -- mutation for POST /api/clinical/{visitId}/osdi-link

    **Create DryEyeForm.tsx** (mirror RefractionForm.tsx exactly):
    - OD/OS side-by-side grid using `grid-cols-[80px_1fr_1fr]`
    - Fields: TBUT (s, 0-30, step 1), Schirmer (mm, 0-35, step 1), Meibomian Grading (0-3, step 1), Tear Meniscus (mm, 0-2, step 0.1), Staining (Oxford 0-5, step 1)
    - React Hook Form + zodResolver with optionalNumber transform
    - Debounced auto-save on blur (500ms) using useUpdateDryEye mutation
    - Read-only when visit status is Signed (status === 1)
    - Show toast on save success/error (sonner)

    **Create OsdiSection.tsx:**
    - Displays OSDI score with severity badge (color-coded: Normal=green, Mild=yellow, Moderate=orange, Severe=red)
    - Uses SEVERITY_CONFIG pattern from research
    - Shows "Fill OSDI Questionnaire" button that expands OsdiQuestionnaire inline
    - Shows "Generate Patient Link" button that calls useGenerateOsdiLink and displays QR code (qrcode.react -- install it too)
    - When score exists: large score number + severity badge + severity label

    **Create OsdiQuestionnaire.tsx:**
    - Inline collapsible section (not modal) with all 12 OSDI questions
    - Each question has: Vietnamese text + English text, 5 radio options (0-4: None/Some/Half/Most/All of the time)
    - Questions 6-12 have additional "N/A" option
    - Auto-calculates score client-side as user fills in answers
    - Shows live score + severity preview
    - "Submit" button saves via backend API
    - Store 12 questions in a constant array with both vi and en text

    **Create DryEyeSection.tsx:**
    - VisitSection wrapper with title "Dry Eye Assessment" (translated)
    - Contains DryEyeForm + OsdiSection
    - defaultOpen={true}

    **Update VisitDetailPage.tsx:**
    - Import and render `<DryEyeSection>` between RefractionSection and DiagnosisSection
    - Pass visitId and visit data (status for read-only check, patientId for OSDI link)

    **Update i18n files** (en/clinical.json and vi/clinical.json):
    - Add all dry eye field labels, OSDI questions, severity labels, button text
    - Vietnamese translations must use proper diacritics
    - OSDI questions: use the validated Vietnamese OSDI translation
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>DryEyeForm renders OD/OS grid with auto-save on blur. OsdiSection shows color-coded severity badge. OsdiQuestionnaire shows 12 bilingual questions with live score preview. DryEyeSection integrated into VisitDetailPage. All i18n keys added in both languages with proper Vietnamese diacritics. Frontend builds with 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: OSDI trend chart + Dry Eye comparison in patient profile</name>
  <files>
    frontend/src/features/patient/components/PatientDryEyeTab.tsx,
    frontend/src/features/patient/components/OsdiTrendChart.tsx,
    frontend/src/features/patient/components/DryEyeComparisonPanel.tsx,
    frontend/src/features/patient/components/PatientProfilePage.tsx
  </files>
  <action>
    **Create OsdiTrendChart.tsx** using recharts:
    - ResponsiveContainer with 100% width, 300px height
    - LineChart with XAxis (visitDate), YAxis (domain 0-100)
    - 4 ReferenceArea bands for severity zones (green 0-12, yellow 12-22, orange 22-32, red 32-100) with fillOpacity 0.3
    - Single Line for OSDI score with primary color, strokeWidth 2, dot markers
    - Tooltip showing date, score, and severity label
    - Use useOsdiHistory(patientId) hook for data
    - Empty state: Card with message "No OSDI data recorded yet" (translated)
    - Date formatting: Vietnamese dd/MM/yyyy format using date-fns

    **Create DryEyeComparisonPanel.tsx:**
    - Two visit selectors (Select dropdowns) at the top
    - Load patient's visits that have dry eye assessments
    - Side-by-side card layout showing both visits' metrics
    - For each metric (TBUT, Schirmer, Meibomian Grading, Tear Meniscus, Staining): show OD and OS values for both visits
    - Delta indicator: green arrow up for improvement, red arrow down for deterioration
    - Improvement direction: TBUT (higher=better), Schirmer (higher=better), Meibomian Grading (lower=better), Tear Meniscus (higher=better), Staining (lower=better)
    - Use useDryEyeComparison hook, enabled only when both visits selected
    - Empty state when fewer than 2 visits available

    **Create PatientDryEyeTab.tsx:**
    - Container tab component rendering:
      1. OsdiTrendChart at the top
      2. Separator
      3. DryEyeComparisonPanel below

    **Update PatientProfilePage.tsx:**
    - Add "Dry Eye" tab (TabsTrigger + TabsContent) after Appointments tab
    - Import PatientDryEyeTab
    - Pass patientId prop
    - Use translated label for tab trigger
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>OSDI trend chart renders with severity bands and chronological data points. Comparison panel shows two visits side-by-side with delta arrows. PatientDryEyeTab added to patient profile. Frontend builds with 0 errors.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds with 0 errors
- DryEyeForm uses same OD/OS grid pattern as RefractionForm
- Auto-save on blur with 500ms debounce (not explicit save button)
- OSDI severity badge colors: green (Normal), yellow (Mild), orange (Moderate), red (Severe)
- OsdiTrendChart shows severity background bands
- Comparison panel has delta indicators with correct improvement direction
- All text in both English and Vietnamese with proper diacritics
</verification>

<success_criteria>
- DryEyeForm mirrors RefractionForm pattern (OD/OS grid, auto-save, read-only when signed)
- 12 OSDI questions displayed with Vietnamese text
- recharts LineChart with severity bands in OsdiTrendChart
- Delta indicators show improvement/deterioration in comparison panel
- New "Dry Eye" tab in patient profile page
- Frontend compiles and builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/04-dry-eye-template-medical-imaging/04-04-SUMMARY.md`
</output>
