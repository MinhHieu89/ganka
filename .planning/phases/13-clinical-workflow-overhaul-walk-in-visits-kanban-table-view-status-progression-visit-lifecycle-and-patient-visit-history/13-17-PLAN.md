---
phase: 13-clinical-workflow-overhaul
plan: 17
type: execute
wave: 6
depends_on: ["13-15", "13-16"]
files_modified:
  - frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx
  - frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx
  - frontend/src/features/clinical/components/stage-views/VisitCompleteBanner.tsx
  - frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
autonomous: true
requirements: [CLN-03, CLN-04]

must_haves:
  truths:
    - "Stage 9 shows read-only glasses Rx and frame/lens selection as reference card"
    - "Stage 9 has quality checklist with all items required before forward"
    - "Stage 9 forward button `Kinh san sang, tra cho benh nhan >` disabled until all checklist items ticked"
    - "Stage 10 has 3-item handoff checklist with inline prescription values for quick verification"
    - "Stage 10 checklist items: prescription verified, frame correct, patient confirmed fit"
    - "Stage 10 forward button `Hoan tat — da tra kinh` disabled until all 3 items checked"
    - "Stage 10 completion: visit record closed, closing summary banner shown"
    - "Stage 10 bottom bar: `In phieu bao hanh` and `Dong ho so` buttons"
    - "Visit complete banner shows total collected, visit duration, and date"
  artifacts:
    - path: "frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx"
      provides: "Optical lab quality checklist view"
      contains: "Kính sẵn sàng"
    - path: "frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx"
      provides: "Glasses return handoff checklist view"
      contains: "Hoàn tất — đã trả kính"
  key_links:
    - from: "Stage9OpticalLabView"
      to: "useCompleteOpticalLab hook"
      via: "Forward button"
      pattern: "useCompleteOpticalLab"
    - from: "Stage10ReturnGlassesView"
      to: "useCompleteHandoff hook"
      via: "Complete handoff button"
      pattern: "useCompleteHandoff"
---

<objective>
Implement Stage 9 (Optical Lab — lens grinding quality checklist), Stage 10 (Return Glasses — handoff checklist), and visit completion banner.

Purpose: These are the final stages of the glasses track. Stage 9 ensures quality before handoff. Stage 10 verifies the glasses match the prescription and the patient is satisfied. Completing Stage 10 closes the glasses track.

Output: Optical lab view, handoff view, and visit completion banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ophthalmology-clinic-workflow-spec.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-16-SUMMARY.md
@frontend/src/features/clinical/components/StageDetailShell.tsx
@frontend/src/features/clinical/components/StageBottomBar.tsx
@frontend/src/features/clinical/api/clinical-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 9 Optical Lab quality checklist view</name>
  <files>
    frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx,
    frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/StageDetailShell.tsx,
    frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **Stage9OpticalLabView.tsx:**
    Uses StageDetailShell.

    **Reference card (read-only):**
    - Full glasses prescription: SPH, CYL, AXIS, PD per eye
    - Frame/lens selection from Stage 7b: lens type, frame code
    - Card styled as read-only info card (`bg-muted/30 border`)

    **Quality checklist:**
    - Same interaction pattern as Stage 7a (checkbox rows)
    - Checklist items (configurable, but default set):
      - `Kiểm tra thông số tròng kính` (Verify lens parameters)
      - `Kiểm tra độ chính xác tâm quang học` (Check optical center accuracy)
      - `Kiểm tra gọng kính và lắp ráp` (Check frame and assembly)
      - `Kiểm tra chống xước và lớp phủ` (Check anti-scratch and coating)
      - `Vệ sinh kính trước khi giao` (Clean glasses before delivery)
    - Each row: checkbox on right, checking turns row green
    - All items must be ticked — no partial completion

    **Bottom bar:**
    - `Kính sẵn sàng, trả cho bệnh nhân ›` — disabled until all checklist items ticked
    - On click: calls useCompleteOpticalLab mutation
    - After completion: Stage 10 (ReturnGlasses) activates

    Update route file: Stage 10 (OpticalLab, value 10) renders Stage9OpticalLabView.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx exists
    - grep "Kính sẵn sàng, trả cho bệnh nhân" frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx matches
    - grep "Kiểm tra thông số tròng kính" frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx matches
    - grep "useCompleteOpticalLab" frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx matches
    - TypeScript compiles
  </acceptance_criteria>
  <done>Stage 9 has quality checklist with all-checked validation and reference card. Vietnamese labels match.</done>
</task>

<task type="auto">
  <name>Task 2: Stage 10 Return Glasses handoff checklist and visit completion</name>
  <files>
    frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx,
    frontend/src/features/clinical/components/stage-views/VisitCompleteBanner.tsx,
    frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/StageDetailShell.tsx,
    frontend/src/features/clinical/components/stage-views/Stage9OpticalLabView.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **Stage10ReturnGlassesView.tsx:**
    Uses StageDetailShell.

    **Handoff checklist (3 items, all required):**
    1. `Xác minh đơn kính đúng (OD [SPH] / OS [SPH])` — shows actual prescription values inline for quick cross-check (e.g., `"Đúng công suất: OD −2.50 / OS −1.75"`)
    2. `Gọng kính đúng mã, không trầy xước` — `"Gọng kính: [frame code] — không trầy xước"`
    3. `Bệnh nhân đã thử kính và xác nhận vừa` — `"Bệnh nhân xác nhận vừa vặn"`

    Each checklist item: checkbox on right, row turns green when checked.
    All 3 must be ticked — no partial handoff.

    **Bottom bar:**
    - `Hoàn tất — đã trả kính ✓` — disabled until all 3 checked
    - On click: calls useCompleteHandoff mutation with { prescriptionVerified: true, frameCorrect: true, patientConfirmedFit: true }
    - This completes the glasses track. If drug track also complete (or NotApplicable), visit is fully complete.

    **Post-completion:**
    - Show VisitCompleteBanner
    - Bottom bar: `In phiếu bảo hành` (print warranty card) + `Đóng hồ sơ` (close record — primary button, navigates back to kanban)

    **VisitCompleteBanner.tsx:**
    Closing summary banner shown after visit is fully complete.
    - Green background with checkmark icon
    - Total amount collected across all payment stages (visit + glasses)
    - Total visit duration (from reception to completion)
    - Date
    - `"Lượt khám hoàn tất"` heading

    Update route file:
    - Stage 11 (ReturnGlasses, value 11) renders Stage10ReturnGlassesView
    - Stage 99 (Done) shows a read-only summary or redirects to kanban
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx exists
    - File frontend/src/features/clinical/components/stage-views/VisitCompleteBanner.tsx exists
    - grep "Hoàn tất — đã trả kính" frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx matches
    - grep "In phiếu bảo hành" frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx matches
    - grep "Đóng hồ sơ" frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx matches
    - grep "Lượt khám hoàn tất" frontend/src/features/clinical/components/stage-views/VisitCompleteBanner.tsx matches
    - grep "useCompleteHandoff" frontend/src/features/clinical/components/stage-views/Stage10ReturnGlassesView.tsx matches
    - TypeScript compiles
  </acceptance_criteria>
  <done>Stage 10 has 3-item handoff checklist with inline prescription values. Visit completion banner shows totals. Print warranty and close record buttons work.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no errors
- Stage 9 quality checklist requires all items
- Stage 10 handoff checklist shows inline prescription values for verification
- Visit completion banner shows total payment and duration
- All Vietnamese labels match spec
</verification>

<success_criteria>
Final stages of glasses track implemented. Visit completion works correctly when both tracks done. All route mappings in place for all 12 stages.
</success_criteria>

<output>
After completion, create `.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-17-SUMMARY.md`
</output>
