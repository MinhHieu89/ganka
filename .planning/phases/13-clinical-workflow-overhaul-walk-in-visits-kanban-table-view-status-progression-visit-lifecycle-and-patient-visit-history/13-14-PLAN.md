---
phase: 13-clinical-workflow-overhaul
plan: 14
type: execute
wave: 4
depends_on: ["13-11", "13-12"]
files_modified:
  - frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx
  - frontend/src/features/clinical/components/stage-views/SignOffConfirmModal.tsx
  - frontend/src/features/clinical/components/stage-views/PostSigningLockedView.tsx
  - frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
autonomous: true
requirements: [CLN-03]

must_haves:
  truths:
    - "Stage 5 has drug prescription section with typeahead drug search, multiple rows, quantity + instructions"
    - "Stage 5 has glasses prescription section with 3 tabs: Kinh don trong / Kinh luy tien / Kinh ap trong"
    - "Glasses prescription fields are pre-filled from Stage 2 refraction with teal border"
    - "Sign-off button `Ky duyet luot kham` disabled until at least one prescription exists"
    - "Sign-off confirmation modal shows summary: ICD-10 count, drug count, glasses Rx type, image count, notes status, doctor + timestamp"
    - "Modal has `Kiem tra lai` (return) and `Xac nhan ky duyet` (lock) buttons"
    - "Post-signing: form dimmed to 75% opacity, green locked banner, print-only bottom bar"
    - "Post-signing bottom bar: `In giay chuyen vien` and `In phieu dong y` buttons, plus `Dang cho thu ngan...` hint"
    - "Auto-transition: after signing, visit moves to Cashier stage automatically"
  artifacts:
    - path: "frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx"
      provides: "Complete prescription view with drug + glasses Rx"
      contains: "Ký duyệt lượt khám"
    - path: "frontend/src/features/clinical/components/stage-views/SignOffConfirmModal.tsx"
      provides: "Sign-off confirmation modal with summary"
      contains: "Xác nhận ký duyệt"
    - path: "frontend/src/features/clinical/components/stage-views/PostSigningLockedView.tsx"
      provides: "Post-signing locked read-only state"
      contains: "locked"
  key_links:
    - from: "Stage5PrescriptionView"
      to: "useSignOffVisit hook"
      via: "SignOffConfirmModal confirm button"
      pattern: "useSignOffVisit"
---

<objective>
Implement Stage 5 (Prescription) with drug and glasses prescriptions, sign-off confirmation modal, and post-signing locked state.

Purpose: Stage 5 is where the doctor finalizes prescriptions and signs off the visit. The sign-off triggers auto-transition to Cashier. This is the critical lock point after which visit records become immutable.

Output: Stage 5 view with drug Rx, glasses Rx (pre-filled from refraction), sign-off modal, and locked state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ophthalmology-clinic-workflow-spec.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-12-SUMMARY.md
@frontend/src/features/clinical/components/DrugPrescriptionForm.tsx
@frontend/src/features/clinical/components/OpticalPrescriptionForm.tsx
@frontend/src/features/clinical/components/SignOffSection.tsx
@frontend/src/features/clinical/components/StageDetailShell.tsx
@frontend/src/features/clinical/api/clinical-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 5 Prescription view with drug Rx, glasses Rx, and sign-off flow</name>
  <files>
    frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx,
    frontend/src/features/clinical/components/stage-views/SignOffConfirmModal.tsx,
    frontend/src/features/clinical/components/stage-views/PostSigningLockedView.tsx,
    frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/DrugPrescriptionForm.tsx,
    frontend/src/features/clinical/components/DrugPrescriptionSection.tsx,
    frontend/src/features/clinical/components/OpticalPrescriptionForm.tsx,
    frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx,
    frontend/src/features/clinical/components/SignOffSection.tsx,
    frontend/src/features/clinical/components/StageDetailShell.tsx,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **Stage5PrescriptionView.tsx:**
    Uses StageDetailShell. Two main sections: drug prescription and glasses prescription.

    **Patient info card (top):**
    - Patient name, doctor, and all confirmed ICD-10 diagnosis tags inline
    - Teal tags = pre-imaging diagnoses; amber tags with `new` badge = added after imaging review (Stage 4b)

    **Drug prescription section:**
    - Reuse existing DrugPrescriptionForm component (drug typeahead, quantity, instructions)
    - Column headers: Drug name / Quantity / Instructions / (remove button)
    - Each row has optional sub-row note (e.g., `"Nhỏ sau khi tháo kính áp tròng"`)
    - `+ Thêm thuốc` dashed button adds new row
    - Multiple drugs allowed, no maximum

    **Glasses prescription section:**
    - Three prescription type tabs: `Kính đơn tròng` / `Kính lũy tiến` / `Kính áp tròng`
    - Two rows: OD and OS. Columns: SPH, CYL, AXIS, ADD
    - Hint text above table: `"Tự động điền từ kết quả đo — có thể chỉnh sửa"`
    - Pre-filled fields from Stage 2 refraction results have **teal border** (`border-teal-400`) and are editable
    - Fields not applicable to selected type are disabled with `—` value
    - PD and additional notes in two-column row below

    **Validation states:**
    - Error: `"Cần ít nhất một đơn thuốc hoặc đơn kính trước khi ký duyệt."`
    - Success: `"Sẵn sàng ký — đơn thuốc (X thuốc) và đơn kính đã hoàn tất."`

    **Bottom bar:**
    - `Lưu nháp` — always enabled
    - `Ký duyệt lượt khám` — primary button, disabled until at least one prescription (drug OR glasses) exists. On click: opens SignOffConfirmModal.

    **SignOffConfirmModal.tsx:**
    Summary modal triggered by `Ký duyệt lượt khám` button. Shows:
    - Number of ICD-10 codes (e.g., `"3 mã ICD-10"`)
    - Number of medications (e.g., `"2 thuốc"`)
    - Glasses prescription type (e.g., `"Kính đơn tròng"`) or `"Không có"` if none
    - Number of uploaded medical images
    - Presence of doctor's notes (Yes/No)
    - Doctor name and timestamp

    Two action buttons:
    - `Kiểm tra lại` — closes modal, returns to form, no data lost
    - `Xác nhận ký duyệt` — black primary button, calls useSignOffVisit mutation. On success, shows PostSigningLockedView.

    **PostSigningLockedView.tsx:**
    Displayed after successful sign-off (when visit.status === Signed and currentStage === Cashier or later).
    - Form opacity reduced to ~75% (`opacity-75`) to signal read-only
    - Each section title gains ` — đã khóa` suffix in muted text
    - Green banner at top: lock icon + `"Hồ sơ đã khóa · Ký lúc [time] · [doctor] · Bệnh nhân đã chuyển thu ngân"`
    - Bottom bar replaced with print buttons: `In giấy chuyển viện` / `In phiếu đồng ý`
    - Right side hint: `"Đang chờ thu ngân..."` in muted text

    Update route file: Stage 5 (Prescription, stage value 5) renders Stage5PrescriptionView. If visit is already signed, render PostSigningLockedView instead.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx exists
    - File frontend/src/features/clinical/components/stage-views/SignOffConfirmModal.tsx exists
    - File frontend/src/features/clinical/components/stage-views/PostSigningLockedView.tsx exists
    - grep "Ký duyệt lượt khám" frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx matches
    - grep "Kiểm tra lại" frontend/src/features/clinical/components/stage-views/SignOffConfirmModal.tsx matches
    - grep "Xác nhận ký duyệt" frontend/src/features/clinical/components/stage-views/SignOffConfirmModal.tsx matches
    - grep "Kính đơn tròng" frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx matches
    - grep "Kính lũy tiến" frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx matches
    - grep "Tự động điền từ kết quả đo" frontend/src/features/clinical/components/stage-views/Stage5PrescriptionView.tsx matches
    - grep "đã khóa" frontend/src/features/clinical/components/stage-views/PostSigningLockedView.tsx matches
    - grep "In giấy chuyển viện" frontend/src/features/clinical/components/stage-views/PostSigningLockedView.tsx matches
    - grep "opacity-75\|opacity.*75" frontend/src/features/clinical/components/stage-views/PostSigningLockedView.tsx matches
    - TypeScript compiles
  </acceptance_criteria>
  <done>Stage 5 fully implements prescription entry, sign-off confirmation modal, and post-signing locked state. All Vietnamese labels match spec. Auto-transition to Cashier works via existing SignOffVisit backend handler.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no errors
- Stage 5 has drug Rx and glasses Rx sections
- Glasses Rx pre-fills from refraction with teal borders
- Sign-off modal shows correct summary
- Post-signing state has dimmed form, green banner, print buttons
</verification>

<success_criteria>
Stage 5 prescription view is complete with sign-off confirmation and locked state. Vietnamese labels match spec exactly. Sign-off triggers auto-advance to Cashier via backend.
</success_criteria>

<output>
After completion, create `.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-14-SUMMARY.md`
</output>
