---
phase: 13-clinical-workflow-overhaul
plan: 11
type: execute
wave: 3
depends_on: ["13-10"]
files_modified:
  - frontend/src/features/clinical/api/clinical-api.ts
  - frontend/src/features/clinical/components/WorkflowDashboard.tsx
  - frontend/src/features/clinical/components/KanbanColumn.tsx
  - frontend/src/features/clinical/components/PatientCard.tsx
  - frontend/src/features/clinical/components/WorkflowTableView.tsx
  - frontend/src/features/clinical/components/StageReversalDialog.tsx
  - frontend/src/locales/en/clinical.json
  - frontend/src/locales/vi/clinical.json
autonomous: true
requirements: [CLN-03, CLN-04]

must_haves:
  truths:
    - "Kanban board has columns for all 11 stages plus Done (NO CashierGlasses — single payment at Cashier), with Vietnamese column headers from spec"
    - "Conditional column visibility: Pharmacy/OpticalCenter/OpticalLab/ReturnGlasses columns hide when no patients need them"
    - "Card anatomy: patient name (bold 14px), assigned doctor (muted), appointment time (right-aligned), elapsed wait timer with amber status dot"
    - "Forward shortcut button only on Stage 1 (Reception) cards with label `Chuyen tiep >`"
    - "All other stage cards show `Nhan de xem chi tiet ->` muted hint, entire card clickable to open detail view"
    - "Stage pills on cards: green `Da ky duyet` for signed, green `Da thanh toan` for paid, amber `Da bo qua` for skipped stage 2"
    - "Frontend API types and hooks updated for all new endpoints"
  artifacts:
    - path: "frontend/src/features/clinical/components/WorkflowDashboard.tsx"
      provides: "Kanban board with conditional columns and new stage map"
      contains: "OpticalCenter"
    - path: "frontend/src/features/clinical/api/clinical-api.ts"
      provides: "API hooks for all new workflow endpoints"
      contains: "useSkipRefraction"
  key_links:
    - from: "WorkflowDashboard.tsx"
      to: "KanbanColumn visibility"
      via: "Conditional rendering based on patient presence"
      pattern: "visibleColumns"
    - from: "PatientCard.tsx"
      to: "Stage detail views"
      via: "Card click navigation"
      pattern: "onClick.*navigate"
---

<objective>
Redesign the frontend kanban board for the new stage model: update column definitions, implement conditional column visibility, redesign card anatomy per spec, and create all API hooks for new endpoints.

Purpose: The frontend must reflect the new 12-stage branching workflow with proper card states, conditional columns, and API integration for all new backend endpoints.

Output: Updated kanban board, card component, table view, and API layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@ophthalmology-clinic-workflow-spec.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-10-SUMMARY.md
@frontend/src/features/clinical/components/WorkflowDashboard.tsx
@frontend/src/features/clinical/components/KanbanColumn.tsx
@frontend/src/features/clinical/components/PatientCard.tsx
@frontend/src/features/clinical/api/clinical-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API types and hooks for all new workflow endpoints</name>
  <files>
    frontend/src/features/clinical/api/clinical-api.ts,
    frontend/src/locales/en/clinical.json,
    frontend/src/locales/vi/clinical.json
  </files>
  <read_first>
    frontend/src/features/clinical/api/clinical-api.ts,
    frontend/src/locales/en/clinical.json,
    frontend/src/locales/vi/clinical.json,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    Update ActiveVisitDto interface to add new fields:
    ```typescript
    drugTrackStatus: number  // 0=NotApplicable, 1=Pending, 2=InProgress, 3=Completed
    glassesTrackStatus: number
    imagingRequested: boolean
    refractionSkipped: boolean
    ```

    Add new command/response types:
    - SkipRefractionCommand: { reason: number, freeTextNote?: string }
    - RequestImagingCommand: { note?: string, services: string[] }
    - ConfirmVisitPaymentCommand: { amount: number, paymentMethod: number, amountReceived: number, splitGlasses: boolean }
    - DispensePharmacyCommand: { dispensedItems: { drugName: string, quantity: number, isDispensed: boolean }[], note?: string }
    - ConfirmOpticalOrderCommand: { lensType: string, frameCode: string, lensCost: number, frameCost: number, totalPrice: number }
    - ConfirmGlassesPaymentCommand: { amount: number, paymentMethod: number, amountReceived: number }
    - CompleteOpticalLabCommand: { qualityChecklist: string[] }
    - CompleteHandoffCommand: { prescriptionVerified: boolean, frameCorrect: boolean, patientConfirmedFit: boolean }

    Add mutation hooks (follow existing useAdvanceStage pattern with invalidation of ["clinical", "active-visits"]):
    - useSkipRefraction(visitId)
    - useUndoRefractionSkip(visitId)
    - useRequestImaging(visitId)
    - useCompleteImaging(visitId)
    - useConfirmVisitPayment(visitId)
    - useDispensePharmacy(visitId)
    - useConfirmOpticalOrder(visitId)
    - useConfirmGlassesPayment(visitId)
    - useCompleteOpticalLab(visitId)
    - useCompleteHandoff(visitId)

    Add isReversalAllowed updates for new stage names (update the ALLOWED_REVERSALS map if exported from this file).

    Update i18n translations for new stage names. Vietnamese labels per spec:
    - workflow.stages.reception: "Tiep nhan" (existing)
    - workflow.stages.refractionVa: "Do khuc xa / Thi luc"
    - workflow.stages.doctorExam: "Bac si kham"
    - workflow.stages.imaging: "Chan doan hinh anh"
    - workflow.stages.doctorReviewsResults: "Bac si doc KQ"
    - workflow.stages.prescription: "Ke don"
    - workflow.stages.cashier: "Thu ngan"
    - workflow.stages.pharmacy: "Nha thuoc"
    - workflow.stages.opticalCenter: "Trung tam kinh"
    - workflow.stages.cashierGlasses: "Thu ngan (kinh)"
    - workflow.stages.opticalLab: "Ky thuat mai lap"
    - workflow.stages.returnGlasses: "Tra kinh"
    - workflow.done: "Hoan tat"

    NOTE: Use proper Vietnamese diacritics in the vi translations. The values above are simplified - use the exact Vietnamese from the spec column headers (e.g., "Tiếp nhận", "Đo khúc xạ / Thị lực", "Bác sĩ khám", "Chẩn đoán hình ảnh", "Bác sĩ đọc KQ", "Kê đơn", "Thu ngân", "Nhà thuốc", "Trung tâm kính", "Thu ngân (kính)", "Kỹ thuật mài lắp", "Trả kính", "Hoàn tất").
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "useSkipRefraction" frontend/src/features/clinical/api/clinical-api.ts matches
    - grep "useConfirmVisitPayment" frontend/src/features/clinical/api/clinical-api.ts matches
    - grep "useCompleteHandoff" frontend/src/features/clinical/api/clinical-api.ts matches
    - grep "drugTrackStatus" frontend/src/features/clinical/api/clinical-api.ts matches
    - grep "Trung tâm kính" frontend/src/locales/vi/clinical.json matches
    - grep "Nhà thuốc" frontend/src/locales/vi/clinical.json matches
    - TypeScript compiles without errors in clinical-api.ts
  </acceptance_criteria>
  <done>All API types and hooks ready for new workflow. Vietnamese translations include all 12 stage labels with proper diacritics.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign kanban board columns, card anatomy, and conditional visibility</name>
  <files>
    frontend/src/features/clinical/components/WorkflowDashboard.tsx,
    frontend/src/features/clinical/components/KanbanColumn.tsx,
    frontend/src/features/clinical/components/PatientCard.tsx,
    frontend/src/features/clinical/components/WorkflowTableView.tsx,
    frontend/src/features/clinical/components/StageReversalDialog.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/WorkflowDashboard.tsx,
    frontend/src/features/clinical/components/KanbanColumn.tsx,
    frontend/src/features/clinical/components/PatientCard.tsx,
    frontend/src/features/clinical/components/WorkflowTableView.tsx,
    frontend/src/features/clinical/components/StageReversalDialog.tsx,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **WorkflowDashboard.tsx:**
    Replace KANBAN_COLUMNS with new 13-column array (12 stages + Done):
    ```
    { id: "reception", titleKey: "workflow.stages.reception", stages: [0], colorAccent: "stone", alwaysVisible: true },
    { id: "refraction-va", titleKey: "workflow.stages.refractionVa", stages: [1], colorAccent: "blue", alwaysVisible: true },
    { id: "doctor-exam", titleKey: "workflow.stages.doctorExam", stages: [2], colorAccent: "emerald", alwaysVisible: true },
    { id: "imaging", titleKey: "workflow.stages.imaging", stages: [3], colorAccent: "cyan", alwaysVisible: true },
    { id: "doctor-reviews", titleKey: "workflow.stages.doctorReviewsResults", stages: [4], colorAccent: "teal", alwaysVisible: true },
    { id: "prescription", titleKey: "workflow.stages.prescription", stages: [5], colorAccent: "amber", alwaysVisible: true },
    { id: "cashier", titleKey: "workflow.stages.cashier", stages: [6], colorAccent: "orange", alwaysVisible: true },
    { id: "pharmacy", titleKey: "workflow.stages.pharmacy", stages: [7], colorAccent: "violet", alwaysVisible: false },
    { id: "optical-center", titleKey: "workflow.stages.opticalCenter", stages: [8], colorAccent: "pink", alwaysVisible: false },
    { id: "cashier-glasses", titleKey: "workflow.stages.cashierGlasses", stages: [9], colorAccent: "orange", alwaysVisible: false },
    { id: "optical-lab", titleKey: "workflow.stages.opticalLab", stages: [10], colorAccent: "indigo", alwaysVisible: false },
    { id: "return-glasses", titleKey: "workflow.stages.returnGlasses", stages: [11], colorAccent: "emerald", alwaysVisible: false },
    { id: "done", titleKey: "workflow.done", stages: [], colorAccent: "muted", alwaysVisible: true },
    ```

    Add `alwaysVisible` field to ColumnDef interface.

    Implement conditional column visibility:
    - Columns with `alwaysVisible: false` are hidden unless at least one visit has that stage in `currentStage` or has the corresponding track active.
    - Pharmacy column visible if any visit has `drugTrackStatus !== 0` (NotApplicable).
    - OpticalCenter/CashierGlasses/OpticalLab/ReturnGlasses columns visible if any visit has `glassesTrackStatus !== 0`.
    - Compute `visibleColumns` as a useMemo filtering KANBAN_COLUMNS.

    Update `getColumnForStage` for new stage numbers (0-11, 99 for Done).
    Update `handleDragEnd` for new stages.
    Update the Done column logic: visits where `isCompleted === true` go to Done.

    **PatientCard.tsx:**
    Redesign card anatomy per spec:
    - Patient name: bold 14px (existing but verify font size)
    - Assigned doctor: secondary text, muted color
    - Appointment time: right-aligned secondary text
    - Elapsed wait timer: live counter with amber status dot (round 6px dot using `bg-amber-400 rounded-full w-1.5 h-1.5 animate-pulse`)
    - Stage pill badges:
      - If visit.refractionSkipped: amber badge `Đã bỏ qua`
      - If visit.status === 1 (Signed): green badge `Đã ký duyệt`
      - If visit has payment (check currentStage > Cashier): green badge `Đã thanh toán`
    - Forward shortcut: ONLY on stage 0 (Reception). Button label: `Chuyển tiếp ›`. All other stages: muted hint text `Nhấn để xem chi tiết →`
    - Entire card clickable (navigate to detail view) for all stages except Reception

    Remove the advance button from PatientCard for all stages except Reception (stage 0). The `onAdvance` prop should only be called for stage 0.

    **KanbanColumn.tsx:**
    Add accent colors for new columns: pink, indigo. No structural changes needed.

    **WorkflowTableView.tsx:**
    Update stage labels and stage filter options for new 12-stage model. Update sorting to handle new stages.

    **StageReversalDialog.tsx:**
    Update allowed reversals map for new stage names if it has a local copy.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "alwaysVisible" frontend/src/features/clinical/components/WorkflowDashboard.tsx matches
    - grep "visibleColumns" frontend/src/features/clinical/components/WorkflowDashboard.tsx matches
    - grep "optical-center" frontend/src/features/clinical/components/WorkflowDashboard.tsx matches
    - grep "optical-lab" frontend/src/features/clinical/components/WorkflowDashboard.tsx matches
    - grep "Chuyển tiếp" frontend/src/features/clinical/components/PatientCard.tsx matches
    - grep "Nhấn để xem chi tiết" frontend/src/features/clinical/components/PatientCard.tsx matches
    - grep "Đã ký duyệt" frontend/src/features/clinical/components/PatientCard.tsx matches
    - grep "Đã bỏ qua" frontend/src/features/clinical/components/PatientCard.tsx matches
    - grep "animate-pulse" frontend/src/features/clinical/components/PatientCard.tsx matches (amber dot)
    - grep "pink" frontend/src/features/clinical/components/KanbanColumn.tsx matches
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Kanban board shows all 12 stages with conditional visibility. Card anatomy matches spec. Forward shortcut only on Reception. Stage pills shown correctly.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no errors
- Kanban has 13 column definitions (12 stages + Done)
- Conditional columns only appear when visits exist in those stages
- Card shows elapsed timer with amber dot, stage pills, forward only on Reception
</verification>

<success_criteria>
Kanban board visually matches spec requirements. Conditional columns work. Card anatomy correct. All API hooks ready for stage detail views.
</success_criteria>

<output>
After completion, create `.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-11-SUMMARY.md`
</output>
