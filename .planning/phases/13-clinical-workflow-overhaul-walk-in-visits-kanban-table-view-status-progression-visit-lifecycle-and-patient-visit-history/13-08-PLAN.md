---
phase: 13-clinical-workflow-overhaul
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/clinical/components/VisitHistoryTab.tsx
  - frontend/src/features/clinical/components/VisitTimeline.tsx
  - frontend/src/features/clinical/components/VisitTimelineCard.tsx
  - frontend/src/features/clinical/components/VisitHistoryDetail.tsx
autonomous: false
gap_closure: true
requirements: [CLN-03]

must_haves:
  truths:
    - "Visit History tab has a polished, professional UI with proper visual hierarchy"
    - "Timeline cards show visit date, doctor, diagnosis, and status with clear visual treatment"
    - "Selected visit detail panel has clear section headers, spacing, and visual separation"
    - "Layout is responsive with proper scrolling behavior"
  artifacts:
    - path: "frontend/src/features/clinical/components/VisitHistoryTab.tsx"
      provides: "Redesigned 2-column layout container"
      min_lines: 30
    - path: "frontend/src/features/clinical/components/VisitTimeline.tsx"
      provides: "Redesigned timeline with visual timeline connector"
      min_lines: 40
    - path: "frontend/src/features/clinical/components/VisitTimelineCard.tsx"
      provides: "Redesigned timeline card with visual polish"
      min_lines: 40
    - path: "frontend/src/features/clinical/components/VisitHistoryDetail.tsx"
      provides: "Redesigned detail panel with section cards"
      min_lines: 80
  key_links:
    - from: "VisitHistoryTab.tsx"
      to: "VisitTimeline.tsx"
      via: "visits prop and onSelectVisit callback"
      pattern: "<VisitTimeline"
    - from: "VisitHistoryTab.tsx"
      to: "VisitHistoryDetail.tsx"
      via: "selectedVisitId prop"
      pattern: "<VisitHistoryDetail"
---

<objective>
Redesign the Patient Visit History tab UI to have polished, professional styling. The current implementation is functional but visually basic. Apply proper visual hierarchy, spacing, card treatments, timeline connector line, section headers, and consistent styling with the rest of the application.

Purpose: User reported the Visit History tab design as "too ugly" during UAT. Needs redesign for a polished clinical look.
Output: 4 redesigned React components with improved visual treatment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-CONTEXT.md

<interfaces>
<!-- Current component interfaces — maintain these contracts -->

From VisitHistoryTab.tsx:
```typescript
interface VisitHistoryTabProps {
  patientId: string
}
```

From VisitTimeline.tsx:
```typescript
interface VisitTimelineProps {
  visits: PatientVisitHistoryDto[] | undefined
  isLoading: boolean
  selectedVisitId: string | null
  onSelectVisit: (visitId: string) => void
}
```

From VisitTimelineCard.tsx:
```typescript
interface VisitTimelineCardProps {
  visit: PatientVisitHistoryDto
  isSelected: boolean
  onClick: () => void
}
```

From VisitHistoryDetail.tsx:
```typescript
interface VisitHistoryDetailProps {
  visitId: string | null
}
```

From clinical-api.ts PatientVisitHistoryDto:
```typescript
export interface PatientVisitHistoryDto {
  visitId: string
  visitDate: string
  doctorName: string
  status: number // 0=Draft, 1=Signed, 2=Amended, 3=Cancelled
  primaryDiagnosisText: string | null
  diagnosisCount: number
}
```

Sections used in VisitHistoryDetail (all accept `disabled` prop for read-only):
- RefractionSection, DryEyeSection, DiagnosisSection
- DrugPrescriptionSection, OpticalPrescriptionSection, ExaminationNotesSection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Visit History timeline and detail panel</name>
  <files>
    frontend/src/features/clinical/components/VisitHistoryTab.tsx,
    frontend/src/features/clinical/components/VisitTimeline.tsx,
    frontend/src/features/clinical/components/VisitTimelineCard.tsx,
    frontend/src/features/clinical/components/VisitHistoryDetail.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/VisitHistoryTab.tsx,
    frontend/src/features/clinical/components/VisitTimeline.tsx,
    frontend/src/features/clinical/components/VisitTimelineCard.tsx,
    frontend/src/features/clinical/components/VisitHistoryDetail.tsx,
    frontend/src/features/clinical/components/VisitDetailPage.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    frontend/src/features/patient/components/PatientProfilePage.tsx
  </read_first>
  <action>
    Redesign all 4 Visit History components for a polished clinical UI. Use shadcn/ui components (Card, Badge, ScrollArea, Separator) and Tailwind CSS. Maintain all existing component interfaces (props) and data hooks unchanged.

    **VisitHistoryTab.tsx** redesign:
    - Add a header section with "Visit History" title and visit count badge (e.g., "12 visits")
    - Use a subtle border or `Separator` between the timeline and detail panels
    - Add a vertical divider line between left and right panels instead of just `gap-4`
    - Keep the 300px left / flex-1 right layout per D-14
    - Add an empty state when patient has no visits (icon + message)
    - Use `min-h-[600px]` instead of `calc(100vh - 280px)` for more predictable height

    **VisitTimeline.tsx** redesign:
    - Add a visual timeline connector: a vertical line (2px, muted color) running down the left side of the timeline with small circle dots at each visit node
    - Each visit card sits to the right of the timeline line with a small horizontal connector
    - Use relative positioning: the vertical line is `absolute left-3 top-0 bottom-0 w-0.5 bg-border`
    - Each card has a circle dot: `absolute left-1.5 w-3 h-3 rounded-full bg-primary border-2 border-background` (or `bg-muted` when not selected, `bg-primary` when selected)
    - Add subtle entrance animation with `transition-all duration-200`
    - Improve loading skeleton to match the timeline layout (dots + card shapes)
    - Keep ScrollArea wrapper, role="listbox", and aria-label

    **VisitTimelineCard.tsx** redesign:
    - Replace the plain Card with a more structured layout:
      - Top row: formatted date (e.g., "25 Mar 2026" using locale-aware formatting) on the left, status Badge on the right
      - Middle row: doctor name with a small user/stethoscope icon (use Tabler icon `IconStethoscope` or `IconUser`)
      - Bottom row: primary diagnosis text (truncated) with a subtle medical icon, or diagnosis count if no primary diagnosis
    - Selected state: `ring-2 ring-primary bg-primary/5` (add subtle background tint)
    - Hover state: `hover:bg-muted/50 hover:shadow-sm`
    - Add padding `p-3` and proper spacing `space-y-1.5`
    - Ensure text sizes are consistent: date = `text-sm font-medium`, doctor = `text-xs text-muted-foreground`, diagnosis = `text-xs`

    **VisitHistoryDetail.tsx** redesign:
    - Add a proper header card at the top with:
      - Visit date formatted nicely (e.g., "Thursday, March 25, 2026")
      - Doctor name with icon
      - Status badge
      - Visit duration or time if available
    - Wrap each clinical section (Refraction, DryEye, Diagnosis, etc.) in a Card with:
      - A section header with an appropriate icon and title
      - Subtle border and padding
      - `mb-4` spacing between section cards
    - Use `Separator` between the header card and the sections
    - Improve the empty/loading states with better visual treatment
    - Improve the signed-at footer: use a Card with a checkmark icon and formatted date/time
    - Keep all section components and their props unchanged (RefractionSection, DryEyeSection, etc. with `disabled` prop)

    Use existing Tabler icons from `@tabler/icons-react` that are already installed: `IconCalendar`, `IconUser`, `IconStethoscope`, `IconEye`, `IconClipboardHeart`, `IconPill`, `IconGlasses`, `IconNotes`, `IconCheck`.

    IMPORTANT: Do not change the component interfaces (props). Do not change the data fetching hooks. Only change the JSX/rendering and Tailwind classes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - VisitTimeline.tsx contains `bg-border` or `bg-muted` (timeline connector line)
    - VisitTimeline.tsx contains `rounded-full` (timeline dot)
    - VisitTimelineCard.tsx contains `hover:bg-muted` or `hover:shadow`
    - VisitTimelineCard.tsx contains `bg-primary/5` or similar selected background tint
    - VisitHistoryDetail.tsx contains at least 3 icon imports from `@tabler/icons-react`
    - VisitHistoryDetail.tsx wraps sections in Card components (contains multiple Card usages)
    - All 4 files maintain their existing interface/props signatures unchanged
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Visit History tab has polished visual design with timeline connector, styled cards, section grouping with icons, and proper visual hierarchy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Visit History tab redesign</name>
  <files>frontend/src/features/clinical/components/VisitHistoryTab.tsx</files>
  <action>
    Start frontend at port 3000 and backend at port 5255. Present the redesigned Visit History tab for human approval.

    What was built: Redesigned Visit History tab with polished UI including timeline connector line with dots, styled timeline cards with hover/selected states, detail panel with section cards and icons, proper visual hierarchy and spacing.

    How to verify:
    1. Start frontend (port 3000) and backend (port 5255)
    2. Log in with Admin@ganka28.com / Admin@123456
    3. Navigate to a patient profile who has multiple past visits
    4. Click the "Visit History" tab
    5. Verify the left panel has a vertical timeline line with circle dots at each visit
    6. Verify timeline cards show date, doctor with icon, diagnosis, and status badge
    7. Verify hovering a card shows subtle highlight
    8. Verify selected card has primary ring and subtle background tint
    9. Verify the right detail panel has a proper header card with date, doctor, and status
    10. Verify each clinical section (Refraction, Diagnosis, etc.) is wrapped in a card with an icon header
    11. Click different visits on the timeline and verify the detail panel updates
    12. Verify the overall look is polished and professional

    Resume signal: Type "approved" if the design meets expectations, or describe what needs to change.
  </action>
  <verify>Human approval of Visit History tab visual design</verify>
  <done>User has approved the Visit History tab redesign as meeting their design expectations.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — no TypeScript errors
2. Visual inspection of Visit History tab in browser
3. Component interfaces unchanged (grep for interface signatures)
</verification>

<success_criteria>
- Visit History tab has polished, professional UI approved by user
- Timeline has visual connector line with circle dots
- Timeline cards have proper hover/selected states
- Detail panel sections are wrapped in cards with icon headers
- All component props/interfaces unchanged
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-08-SUMMARY.md`
</output>
