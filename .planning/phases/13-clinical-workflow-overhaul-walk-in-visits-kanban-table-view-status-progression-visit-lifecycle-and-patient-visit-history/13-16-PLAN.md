---
phase: 13-clinical-workflow-overhaul
plan: 16
type: execute
wave: 5
depends_on: ["13-13", "13-14"]
files_modified:
  - frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx
  - frontend/src/features/clinical/components/stage-views/Stage7bOpticalCenterView.tsx
  - frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
autonomous: true
requirements: [CLN-03]

must_haves:
  truths:
    - "Stage 7a shows medication checklist with checkbox per drug row"
    - "Stage 7a forward button `Da phat du thuoc` disabled until all rows checked"
    - "Stage 7a completion shows green Done banner with pharmacist name and print drug label button"
    - "Stage 7b shows read-only glasses Rx from Stage 5"
    - "Stage 7b has lens type dropdown and frame code input; forward disabled until both filled"
    - "Stage 7b shows price breakdown: lens cost x2 + frame cost = total"
    - "Stage 7b confirms and advances to Cashier(6) for single combined payment (NO separate glasses payment stage)"
  artifacts:
    - path: "frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx"
      provides: "Pharmacy dispensing view with medication checklist"
      contains: "Đã phát đủ thuốc"
    - path: "frontend/src/features/clinical/components/stage-views/Stage7bOpticalCenterView.tsx"
      provides: "Optical center frame consultation view"
      contains: "Xác nhận đơn, thu tiền kính"
  key_links:
    - from: "Stage7aPharmacyView"
      to: "useDispensePharmacy hook"
      via: "Complete dispensing button"
      pattern: "useDispensePharmacy"
    - from: "Stage7bOpticalCenterView"
      to: "useConfirmOpticalOrder hook"
      via: "Confirm order button"
      pattern: "useConfirmOpticalOrder"
---

<objective>
Implement Stage 7a (Pharmacy dispensing), Stage 7b (Optical Center frame consultation), and Stage 8 (Cashier glasses payment). These are the parallel downstream stages after visit payment.

Purpose: After Stage 6 payment, the drug track (7a) and glasses track (7b->8->9->10) run independently. This plan implements the first three downstream stages.

Output: Pharmacy checklist view, optical center frame/lens selection view, glasses payment view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ophthalmology-clinic-workflow-spec.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-14-SUMMARY.md
@frontend/src/features/clinical/components/StageDetailShell.tsx
@frontend/src/features/clinical/components/StageBottomBar.tsx
@frontend/src/features/clinical/components/stage-views/PaymentMethodSelector.tsx
@frontend/src/features/clinical/api/clinical-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 7a Pharmacy dispensing view with medication checklist</name>
  <files>
    frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx,
    frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/DrugPrescriptionSection.tsx,
    frontend/src/features/clinical/components/StageDetailShell.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **Stage7aPharmacyView.tsx:**
    Uses StageDetailShell. Shows the drug track dispensing interface.

    **Medication checklist:**
    - Each medication from the signed prescription renders as a row:
      - Drug name (bold), quantity, dosage instruction
      - Square checkbox on right side
    - Checking a row: row background turns green (`bg-green-50`), checkbox shows checkmark icon
    - Unchecking allowed (toggle behavior)
    - State managed locally, submitted on confirm

    **Optional note field:**
    - Free-text textarea at bottom for dispensing instructions (e.g., storage conditions, timing with meals)
    - Not required

    **Bottom bar:**
    - `Đã phát đủ thuốc ✓` — primary button, disabled until ALL medication rows are checked. No partial completion.
    - On click: calls useDispensePharmacy mutation with all items marked as dispensed + optional note

    **Post-completion state:**
    - Green `"Hoàn tất"` banner: pharmacist name + timestamp
    - Bottom bar changes to: `In nhãn thuốc` (print drug label) button only
    - Checklist remains visible but disabled (read-only with all green rows)

    Update route file: case 7 (Pharmacy enum=7, spec Stage 7a) → render Stage7aPharmacyView.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx exists
    - grep "Đã phát đủ thuốc" frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx matches
    - grep "In nhãn thuốc" frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx matches
    - grep "bg-green-50" frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx matches
    - grep "useDispensePharmacy" frontend/src/features/clinical/components/stage-views/Stage7aPharmacyView.tsx matches
    - TypeScript compiles
  </acceptance_criteria>
  <done>Stage 7a pharmacy view has medication checklist with per-row checkboxes, all-checked validation, post-completion state with print button.</done>
</task>

<task type="auto">
  <name>Task 2: Stage 7b Optical Center view (frame/lens selection → routes to Cashier)</name>
  <files>
    frontend/src/features/clinical/components/stage-views/Stage7bOpticalCenterView.tsx,
    frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **Stage7bOpticalCenterView.tsx:**
    Uses StageDetailShell. This view is shown BEFORE Cashier — the patient selects frame/lens here,
    then goes to Cashier to pay for everything in one invoice.

    **Glasses prescription card (read-only):**
    - Compact card showing: SPH, CYL, AXIS per eye, PD, doctor's coating note
    - Read-only — not editable at this stage

    **Frame and lens selection:**
    - Lens type dropdown (e.g., "Essilor Crizal 1.60", "Hoya HD 1.60", "Zeiss BlueGuard 1.67")
    - Frame code free-text input (mã gọng)
    - Once both filled, price breakdown appears:
      - Lens cost (x2): [price] ₫
      - Frame cost: [price] ₫
      - Total: [price] ₫
    - Use Intl.NumberFormat('vi-VN') for currency formatting

    **Bottom bar:**
    - `Xác nhận đơn kính ›` — disabled until both lens type AND frame code are filled
    - On click: calls useConfirmOpticalOrder mutation with lens type, frame code, costs, total
    - On success: backend advances visit to Cashier(6) — patient pays for everything there

    NOTE: NO Stage8GlassesPaymentView. Per original requirement (CONFIRMATION_2.md Q1),
    the clinic uses ONE combined invoice at Cashier. OpticalCenter happens before Cashier
    so the glasses price is known at checkout.

    Update route file:
    - case 8 (OpticalCenter enum=8) → render Stage7bOpticalCenterView
    - NO case 9 for CashierGlasses (removed)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/features/clinical/components/stage-views/Stage7bOpticalCenterView.tsx exists
    - grep "Xác nhận đơn kính" frontend/src/features/clinical/components/stage-views/Stage7bOpticalCenterView.tsx matches
    - grep "useConfirmOpticalOrder" frontend/src/features/clinical/components/stage-views/Stage7bOpticalCenterView.tsx matches
    - NO file Stage8GlassesPaymentView.tsx exists
    - TypeScript compiles
  </acceptance_criteria>
  <done>Stage 7b has frame/lens selection with price breakdown. Confirms and routes to Cashier for single combined payment. No separate glasses payment stage.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no errors
- Stage 7a: checklist requires all items checked
- Stage 7b: lens + frame required for forward
- Stage 8: waiting state when 7b incomplete, payment flow when ready
</verification>

<success_criteria>
All three downstream stages (7a, 7b, 8) implemented with correct validation, Vietnamese labels, and API integration.
</success_criteria>

<output>
After completion, create `.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-16-SUMMARY.md`
</output>
