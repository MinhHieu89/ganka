---
phase: 13-clinical-workflow-overhaul
plan: 15
type: execute
wave: 5
depends_on: ["13-13", "13-14"]
files_modified:
  - frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx
  - frontend/src/features/clinical/components/stage-views/PaymentMethodSelector.tsx
  - frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx
  - frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
autonomous: true
requirements: [CLN-03, CLN-04]

must_haves:
  truths:
    - "Stage 6 shows auto-generated invoice with line items from previous stages"
    - "Glasses payment split toggle `Tach rieng tien kinh` defaults ON when glasses Rx exists"
    - "Payment method selector: cash/card/transfer in 3-column grid with blue selected state"
    - "Amount received input with real-time change calculation"
    - "Post-payment success view shows next steps tiles based on what was prescribed"
    - "Auto-routing: drugs -> Pharmacy queue, glasses -> Optical Center queue, both -> parallel, neither -> complete"
    - "Print buttons: `In hoa don` and `Xuat bien lai`"
  artifacts:
    - path: "frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx"
      provides: "Cashier payment view with invoice and payment flow"
      contains: "Thu tiền"
    - path: "frontend/src/features/clinical/components/stage-views/PaymentMethodSelector.tsx"
      provides: "3-column payment method selector"
      contains: "cash.*card.*transfer"
    - path: "frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx"
      provides: "Post-payment success with next steps tiles"
      contains: "Nhà thuốc"
  key_links:
    - from: "Stage6CashierView"
      to: "useConfirmVisitPayment hook"
      via: "Confirm payment button"
      pattern: "useConfirmVisitPayment"
    - from: "PostPaymentSuccessView"
      to: "Parallel track tiles"
      via: "drugTrackStatus/glassesTrackStatus"
      pattern: "Đang chờ"
---

<objective>
Implement Stage 6 (Cashier — Visit Payment) with invoice generation, payment method selection, change calculation, glasses split toggle, and post-payment auto-routing.

Purpose: Stage 6 is the payment gateway that branches into parallel tracks. The cashier collects payment and the system auto-routes to Pharmacy and/or Optical Center based on prescriptions.

Output: Complete cashier view with invoice, payment flow, and post-payment success view with next steps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@ophthalmology-clinic-workflow-spec.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-14-SUMMARY.md
@frontend/src/features/clinical/components/StageDetailShell.tsx
@frontend/src/features/clinical/components/StageBottomBar.tsx
@frontend/src/features/clinical/api/clinical-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 6 Cashier view with invoice, payment flow, and glasses split toggle</name>
  <files>
    frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx,
    frontend/src/features/clinical/components/stage-views/PaymentMethodSelector.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/StageDetailShell.tsx,
    frontend/src/features/clinical/components/StageBottomBar.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **Stage6CashierView.tsx:**
    Two-phase view: Invoice phase and Payment phase.

    **Invoice phase (initial state):**
    - Line items grouped into sections:
      - `Dịch vụ khám` (Examination services) — from Stage 1 service selection
      - `Dịch vụ CĐHA` (Imaging services) — from Stage 4a checklist (if any)
      - `Thuốc` (Medications) — from Stage 5 drug prescription
    - Each line item: name, sub-note (quantity or eye scope), quantity multiplier, price
    - Subtotal, discount line (if applicable), total at bottom
    - Elapsed wait time since sign-off shown in amber if > 15 min

    **Glasses payment split toggle:**
    - Position: bottom of invoice card, below total
    - Label: `"Tách riêng tiền kính"` with sub-label `"Kính sẽ thanh toán riêng sau khi chọn gọng"`
    - Toggle defaults ON when glasses prescription exists
    - When ON: split detail shows `"Thanh toán ngay: [amount]"` (green badge) and `"Kính: Tính sau"` (amber badge)
    - When OFF: glasses price included in total (if known)
    - Use shadcn Switch component

    **Bottom bar (invoice phase):**
    - `Thu tiền ›` primary button — transitions to payment phase

    **Payment phase:**
    - PaymentMethodSelector component (see below)
    - Amount due shown prominently (text-2xl font-bold)
    - `"Số tiền nhận"` input: large font (text-lg), number input
    - Real-time change calculation below:
      - If amount < due: `"Còn thiếu: [X] ₫"` in red
      - If amount >= due: `"Tiền thừa: [X] ₫"` in green
    - `Xác nhận đã thu tiền` button — calls useConfirmVisitPayment mutation. Disabled if amount received < amount due.

    **PaymentMethodSelector.tsx:**
    Reusable 3-column grid selector. Props: `value`, `onChange`.
    - Three buttons: `Tiền mặt` (cash) / `Thẻ` (card) / `Chuyển khoản` (transfer)
    - Selected state: blue tinted border and background (`border-blue-500 bg-blue-50`)
    - Unselected: default border, white background
    - Use shadcn Button with custom styling, not a radio group

    Format currency with Vietnamese dong: `new Intl.NumberFormat('vi-VN').format(amount) + ' ₫'`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx exists
    - File frontend/src/features/clinical/components/stage-views/PaymentMethodSelector.tsx exists
    - grep "Thu tiền" frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx matches
    - grep "Xác nhận đã thu tiền" frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx matches
    - grep "Tách riêng tiền kính" frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx matches
    - grep "Tiền mặt" frontend/src/features/clinical/components/stage-views/PaymentMethodSelector.tsx matches
    - grep "Chuyển khoản" frontend/src/features/clinical/components/stage-views/PaymentMethodSelector.tsx matches
    - grep "Còn thiếu\|Tiền thừa" frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx matches
    - TypeScript compiles
  </acceptance_criteria>
  <done>Stage 6 invoice view, payment method selector, amount input with change calculation, and glasses split toggle all implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Post-payment success view with auto-routing next steps and route update</name>
  <files>
    frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx,
    frontend/src/app/routes/_authenticated/clinical/visit.$visitId.stage.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/stage-views/Stage6CashierView.tsx,
    frontend/src/features/clinical/api/clinical-api.ts,
    ophthalmology-clinic-workflow-spec.md
  </read_first>
  <action>
    **PostPaymentSuccessView.tsx:**
    Shown after successful payment confirmation.

    **Success banner:**
    - Green background (`bg-green-50 border-green-300`)
    - Checkmark icon + payment amount, method, change given, time, cashier name
    - Full width, prominent

    **Next steps card section:**
    Tiles shown depend on what was prescribed (read from visit's drugTrackStatus/glassesTrackStatus):
    - If drugTrackStatus !== NotApplicable: `Nhà thuốc` tile with green `Đang chờ` badge
    - If glassesTrackStatus !== NotApplicable: `Trung tâm kính` tile with green `Đang chờ` badge
    - If both NotApplicable: show visit complete message instead of tiles: `"Lượt khám hoàn tất — không có thuốc hoặc kính cần xử lý."`

    Each tile: shadcn Card with icon, stage name, and `Đang chờ` badge (`bg-green-100 text-green-700`).
    Below tiles: note `"Tiền kính sẽ thanh toán riêng."` (if glasses track active)

    **Bottom bar:**
    - Print buttons on left: `In hóa đơn` / `Xuất biên lai`
    - Primary button on right: `Tiếp nhận bệnh nhân mới` — navigates back to kanban dashboard

    Update route file: Stage 6 (Cashier, value 6) renders Stage6CashierView. After payment confirmed (can detect via visit having a payment or currentStage > 6), show PostPaymentSuccessView.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx exists
    - grep "Nhà thuốc" frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx matches
    - grep "Trung tâm kính" frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx matches
    - grep "Đang chờ" frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx matches
    - grep "In hóa đơn" frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx matches
    - grep "Xuất biên lai" frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx matches
    - grep "Tiếp nhận bệnh nhân mới" frontend/src/features/clinical/components/stage-views/PostPaymentSuccessView.tsx matches
    - TypeScript compiles
  </acceptance_criteria>
  <done>Post-payment view shows correct next steps tiles with auto-routing. Print buttons and new patient button present. Route correctly switches between payment and success views.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no errors
- Stage 6 has complete invoice -> payment -> success flow
- Auto-routing shows correct tiles based on prescribed tracks
- Vietnamese labels match spec
</verification>

<success_criteria>
Stage 6 payment flow is complete from invoice through payment to post-payment success with auto-routing next steps. Currency formatting uses VND.
</success_criteria>

<output>
After completion, create `.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-15-SUMMARY.md`
</output>
