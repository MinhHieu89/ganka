---
phase: 13-clinical-workflow-overhaul
plan: 05
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - frontend/src/features/clinical/components/PatientCard.tsx
  - frontend/src/features/clinical/components/VisitDetailPage.tsx
  - frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx
  - frontend/src/features/clinical/components/OsdiSection.tsx
  - frontend/src/locales/en/clinical.json
  - frontend/src/locales/vi/clinical.json
autonomous: true
requirements:
  - CLN-03
  - CLN-04

must_haves:
  truths:
    - "Patient name in kanban cards links to patient profile page"
    - "Patient name in visit detail page header links to patient profile page"
    - "OSDI questionnaire answers are visible in the visit detail page"
    - "Optical Prescription section auto-expands when data exists"
    - "OSDI score updates in real-time when questionnaire is completed"
  artifacts:
    - path: "frontend/src/features/clinical/components/PatientCard.tsx"
      provides: "Patient name as clickable link to /patients/$patientId"
      contains: "Link"
    - path: "frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx"
      provides: "Auto-expand behavior when data exists"
      contains: "defaultOpen"
  key_links:
    - from: "PatientCard.tsx"
      to: "/patients/$patientId"
      via: "Link component with stopPropagation"
      pattern: "patients/"
    - from: "VisitDetailPage.tsx"
      to: "/patients/$patientId"
      via: "Link component on patient name"
      pattern: "patients/"
---

<objective>
Implement folded todos: patient name links (D-17), OSDI answers display (D-18), optical prescription auto-expand (D-19), and realtime OSDI score updates (D-20).

Purpose: Address 4 pending improvement items that were folded into this phase. These are small, independent changes to existing components that improve the clinical workflow UX.

Output: Updated PatientCard, VisitDetailPage, OpticalPrescriptionSection, and OsdiSection components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-CONTEXT.md
@.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-UI-SPEC.md

<interfaces>
From frontend/src/features/clinical/components/PatientCard.tsx:
- Receives ActiveVisitDto with patientId and patientName
- Uses useDraggable from @dnd-kit/core
- PointerSensor with distance: 8 constraint separates click from drag

From frontend/src/features/clinical/components/VisitDetailPage.tsx:
- Renders patient name in header area
- Contains all clinical sections
- Uses useVisitById hook for visit data

From frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx:
- Collapsible section component
- Currently collapsed by default

From frontend/src/features/clinical/components/OsdiSection.tsx:
- Already includes OsdiAnswersSection component (line ~145)
- Already has collapsible toggle for answers
- uses useOsdiHub hook for SignalR-based real-time updates

From frontend/src/features/clinical/hooks/use-osdi-hub.ts:
- SignalR hub connection for OSDI questionnaire updates
- Invalidates visit query on OSDI completion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patient name links (D-17) + OSDI answers verification (D-18)</name>
  <files>
    frontend/src/features/clinical/components/PatientCard.tsx,
    frontend/src/features/clinical/components/VisitDetailPage.tsx,
    frontend/src/features/clinical/components/OsdiSection.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/PatientCard.tsx,
    frontend/src/features/clinical/components/VisitDetailPage.tsx,
    frontend/src/features/clinical/components/OsdiSection.tsx,
    frontend/src/features/clinical/components/OsdiAnswersSection.tsx
  </read_first>
  <action>
    **D-17: Patient name link in PatientCard.tsx:**
    1. Import `Link` from `@tanstack/react-router`
    2. Find the patient name text (likely `visit.patientName` in JSX)
    3. Wrap it in a Link:
    ```typescript
    <Link
      to="/patients/$patientId"
      params={{ patientId: visit.patientId }}
      className="text-primary hover:underline cursor-pointer"
      onClick={(e: React.MouseEvent) => e.stopPropagation()}
    >
      {visit.patientName}
    </Link>
    ```
    The `e.stopPropagation()` prevents the card's click handler from firing when clicking the name link.
    The existing PointerSensor with `activationConstraint: { distance: 8 }` means a simple click (< 8px movement) fires the click event, not drag. So the Link click will navigate correctly.

    **D-17: Patient name link in VisitDetailPage.tsx:**
    1. Import `Link` from `@tanstack/react-router`
    2. Find the patient name in the header/title area
    3. Wrap it in a Link to `/patients/${visit.patientId}`:
    ```typescript
    <Link
      to="/patients/$patientId"
      params={{ patientId: visit.patientId }}
      className="text-primary hover:underline"
    >
      {visit.patientName}
    </Link>
    ```

    **D-18: OSDI answers verification:**
    Read `OsdiSection.tsx` to verify if OSDI questionnaire answers are already visible.
    Per RESEARCH.md: "OsdiAnswersSection component already exists and is already included in OsdiSection.tsx (line 145). The existing component already has a collapsible toggle."

    If D-18 is already implemented:
    - Verify OsdiAnswersSection is rendered within OsdiSection
    - Verify the collapsible toggle works
    - Document as "already complete" in summary

    If D-18 needs work:
    - Ensure OsdiAnswersSection is imported and rendered in OsdiSection
    - Add a collapsible wrapper (Collapsible from shadcn) defaulting to collapsed
    - The answers should appear below the OSDI score display
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PatientCard.tsx contains `import { Link }` or `import { Link` from tanstack/react-router
    - PatientCard.tsx contains `to="/patients/$patientId"` or equivalent route
    - PatientCard.tsx contains `stopPropagation` on the patient name link
    - VisitDetailPage.tsx contains `Link` wrapping patient name with route to patients page
    - OsdiSection.tsx contains `OsdiAnswersSection` component (either already present or newly added)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Patient name in kanban cards and visit detail page links to patient profile. OSDI answers are visible in visit detail page.</done>
</task>

<task type="auto">
  <name>Task 2: Optical Rx auto-expand (D-19) + realtime OSDI (D-20) verification</name>
  <files>
    frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx,
    frontend/src/features/clinical/components/VisitDetailPage.tsx
  </files>
  <read_first>
    frontend/src/features/clinical/components/OpticalPrescriptionSection.tsx,
    frontend/src/features/clinical/components/VisitDetailPage.tsx,
    frontend/src/features/clinical/hooks/use-osdi-hub.ts
  </read_first>
  <action>
    **D-19: Optical Prescription auto-expand:**
    1. Read OpticalPrescriptionSection.tsx to understand how the collapsible is implemented
    2. The section likely uses shadcn Collapsible or a custom expand/collapse mechanism
    3. Add a `defaultOpen` prop or check if data exists to set initial open state:

    Option A -- If section uses Collapsible:
    ```typescript
    interface OpticalPrescriptionSectionProps {
      // existing props...
      defaultOpen?: boolean
    }
    ```
    Pass `defaultOpen={true}` from VisitDetailPage when optical prescription data exists:
    ```typescript
    <OpticalPrescriptionSection
      visitId={visit.id}
      prescriptions={visit.opticalPrescriptions}
      defaultOpen={visit.opticalPrescriptions && visit.opticalPrescriptions.length > 0}
    />
    ```

    Option B -- If section uses useState for open state:
    ```typescript
    const [isOpen, setIsOpen] = useState(prescriptions && prescriptions.length > 0)
    ```
    Change the initial state from `false` to `prescriptions && prescriptions.length > 0`.

    Option C -- If section uses an accordion pattern:
    Set the initial value to include the optical section when data exists.

    Read the component first to determine which approach applies.

    **D-20: Realtime OSDI score update verification:**
    Per RESEARCH.md: "`useOsdiHub` hook already exists and is used in `VisitDetailPage.tsx` for SignalR-based updates."

    1. Read `use-osdi-hub.ts` to verify it invalidates the visit query when OSDI is completed
    2. Read VisitDetailPage.tsx to verify it calls `useOsdiHub`
    3. If the hook already invalidates `clinicalKeys.visit(visitId)`, the score will update automatically when the query refetches

    If D-20 is already implemented:
    - Verify the SignalR hub invalidates the correct query key
    - Document as "already complete" in summary

    If D-20 needs work:
    - Ensure the hub callback calls `queryClient.invalidateQueries({ queryKey: clinicalKeys.visit(visitId) })`
    - The OSDI score in the visit detail page will re-render with updated data after invalidation
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - OpticalPrescriptionSection.tsx contains logic to auto-expand when data exists (either `defaultOpen` prop, initial useState based on data, or equivalent)
    - The auto-expand condition checks whether optical prescription data is non-empty
    - VisitDetailPage.tsx passes auto-expand prop/data to OpticalPrescriptionSection (if using prop approach)
    - use-osdi-hub.ts or VisitDetailPage.tsx contains query invalidation for OSDI updates (verify existing or add)
    - `npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Optical prescription section auto-expands when data exists. OSDI score updates in real-time via SignalR query invalidation.</done>
</task>

</tasks>

<verification>
```bash
cd frontend && npx tsc --noEmit
```
Frontend compiles. All 4 folded todos addressed.
</verification>

<success_criteria>
- Patient name in kanban cards is a clickable link to patient profile per D-17
- Patient name in visit detail header is a clickable link per D-17
- Click on patient name link does not trigger card drag (stopPropagation) per D-17
- OSDI answers visible in visit detail page per D-18
- Optical prescription section auto-expands when data exists per D-19
- OSDI score updates in real-time when questionnaire is completed per D-20
</success_criteria>

<output>
After completion, create `.planning/phases/13-clinical-workflow-overhaul-walk-in-visits-kanban-table-view-status-progression-visit-lifecycle-and-patient-visit-history/13-05-SUMMARY.md`
</output>
