---
phase: 08-optical-center
plan: 28
type: execute
wave: 11
depends_on: [08-25]
files_modified:
  - frontend/src/features/optical/components/LensCatalogPage.tsx
  - frontend/src/features/optical/components/LensCatalogTable.tsx
  - frontend/src/features/optical/components/LensFormDialog.tsx
  - frontend/src/app/routes/_authenticated/optical/lenses.tsx
autonomous: true
requirements: [OPT-02]

must_haves:
  truths:
    - "Staff can view lens catalog with stock entries per power combination"
    - "Staff can create/edit lens catalog items and adjust per-power stock"
  artifacts:
    - path: "frontend/src/features/optical/components/LensCatalogPage.tsx"
      provides: "Lens catalog page with table and stock management"
    - path: "frontend/src/features/optical/components/LensCatalogTable.tsx"
      provides: "DataTable for lens catalog items with expandable stock entries"
    - path: "frontend/src/features/optical/components/LensFormDialog.tsx"
      provides: "Dialog for creating/editing lens catalog items + stock adjustment"
    - path: "frontend/src/app/routes/_authenticated/optical/lenses.tsx"
      provides: "TanStack Router route file"
  key_links:
    - from: "LensCatalogPage.tsx"
      to: "optical-queries.ts"
      via: "useLensCatalog hook"
      pattern: "useLensCatalog"
---

<objective>
Create the Lens Catalog frontend page with data table, form dialog, stock management, and route.

Purpose: Enable staff to manage lens catalog (brand, type, material, coatings, pricing) and per-power stock entries (SPH/CYL/ADD with quantity tracking). Shows low stock alerts inline.
Output: 3 component files + 1 route file
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LensCatalogTable and LensFormDialog</name>
  <files>
    frontend/src/features/optical/components/LensCatalogTable.tsx,
    frontend/src/features/optical/components/LensFormDialog.tsx
  </files>
  <action>
    1. LensCatalogTable.tsx:
       - DataTable with columns: Brand, Name, Lens Type, Material, Coatings (badges), Selling Price, Cost Price, Total Stock (sum of stock entries), Status (Active badge)
       - Expandable rows showing stock entries table: SPH, CYL, ADD, Quantity, Min Stock, Status badge (low stock warning)
       - Low stock entries highlighted in red/warning color
       - Actions column: Edit, Add Stock, View Stock Entries
       - Use shadcn/ui Collapsible or Accordion for expandable rows

    2. LensFormDialog.tsx:
       - Dialog with two modes: "Catalog Item" (create/edit) and "Stock Adjustment" (per-power)
       - Catalog Item mode: Brand (text), Name (text), Lens Type (select: single_vision/bifocal/progressive/reading), Material (select), Available Coatings (multi-select checkboxes using flags), Selling Price, Cost Price, Preferred Supplier (select)
       - Stock Adjustment mode: SPH (number, step 0.25, range -20 to +20), CYL (number, step 0.25, range -10 to 0), ADD (number, step 0.25, range 0 to +4, optional), Quantity Change (number, positive to add, negative to remove)
       - React Hook Form + Zod validation
       - Calls appropriate mutation on submit
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>LensCatalogTable shows items with expandable stock entries. LensFormDialog handles both catalog and stock modes.</done>
</task>

<task type="auto">
  <name>Task 2: Create LensCatalogPage and route file</name>
  <files>
    frontend/src/features/optical/components/LensCatalogPage.tsx,
    frontend/src/app/routes/_authenticated/optical/lenses.tsx
  </files>
  <action>
    1. LensCatalogPage.tsx:
       - Page header: "Lens Catalog" with "Add Lens" button
       - Low stock alert banner at top if any lens stock entries below min level (uses useLowLensStockAlerts hook)
       - LensCatalogTable as main content
       - Uses useLensCatalog hook for data
       - Uses shadcn/ui Card, Button, Alert components

    2. lenses.tsx route file:
       - createFileRoute('/_authenticated/optical/lenses')
       - component: LensCatalogPage
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Lens catalog page accessible at /optical/lenses with low stock alerts</done>
</task>

</tasks>

<verification>
All 4 files compile. Route file correctly exports the page component.
</verification>

<success_criteria>
- Lens catalog shows items with expandable stock entries per power
- Stock adjustment form uses optical power step (0.25)
- Low stock alert banner visible when stock below threshold
- Route accessible at /optical/lenses
- Frontend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-28-SUMMARY.md`
</output>
