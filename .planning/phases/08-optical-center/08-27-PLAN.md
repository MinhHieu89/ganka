---
phase: 08-optical-center
plan: 27
type: execute
wave: 11
depends_on: [08-25]
files_modified:
  - frontend/src/features/optical/components/FrameCatalogTable.tsx
  - frontend/src/features/optical/components/FrameFormDialog.tsx
  - frontend/src/features/optical/components/FrameCatalogPage.tsx
  - frontend/src/app/routes/_authenticated/optical/frames.tsx
autonomous: true
requirements: [OPT-01]

must_haves:
  truths:
    - "Staff can view frame inventory in a searchable, filterable data table"
    - "Staff can create and edit frames via a dialog with all catalog fields"
    - "Frame catalog page is accessible via authenticated route"
  artifacts:
    - path: "frontend/src/features/optical/components/FrameCatalogTable.tsx"
      provides: "DataTable for frames with search, filter by material/type/gender"
    - path: "frontend/src/features/optical/components/FrameFormDialog.tsx"
      provides: "Dialog form for creating/editing frames with validation"
    - path: "frontend/src/features/optical/components/FrameCatalogPage.tsx"
      provides: "Page component composing table + form + barcode display"
    - path: "frontend/src/app/routes/_authenticated/optical/frames.tsx"
      provides: "TanStack Router route file"
  key_links:
    - from: "FrameCatalogPage.tsx"
      to: "optical-queries.ts"
      via: "useFrames, useCreateFrame hooks"
      pattern: "useFrames|useCreateFrame"
---

<objective>
Create the Frame Catalog frontend page with data table, form dialog, and route.

Purpose: Enable staff to manage frame inventory with a searchable/filterable table, create/edit forms with all catalog attributes (brand, model, color, size, material, type, gender, pricing, barcode).
Output: 3 component files + 1 route file
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FrameCatalogTable and FrameFormDialog</name>
  <files>
    frontend/src/features/optical/components/FrameCatalogTable.tsx,
    frontend/src/features/optical/components/FrameFormDialog.tsx
  </files>
  <action>
    1. FrameCatalogTable.tsx:
       - Use the project's generic DataTable component (TanStack Table pattern)
       - Columns: Brand, Model, Color, Size (LensWidth-BridgeWidth-TempleLength format), Material, Type, Gender, Selling Price (VND formatted), Stock Qty, Barcode (with BarcodeDisplay if present), Actions (Edit, Generate Barcode)
       - Search bar for text search (brand, model, color, barcode)
       - Filter dropdowns: Material (Metal/Plastic/Titanium), Frame Type (FullRim/SemiRimless/Rimless), Gender (M/F/Unisex)
       - Pagination controls
       - Stock quantity color coding: red if <= MinStockLevel, green otherwise
       - Use established DataTable patterns from pharmacy

    2. FrameFormDialog.tsx:
       - shadcn/ui Dialog with form inside
       - React Hook Form + Zod validation (zodResolver pattern)
       - Fields: Brand (text), Model (text), Color (text), Lens Width (number, 40-65), Bridge Width (number, 12-24), Temple Length (number, 120-155), Material (select), Frame Type (select), Gender (select), Selling Price (number), Cost Price (number), Barcode (optional text, 13 digits)
       - Mode: create (empty) or edit (pre-populated from frame data)
       - onSubmit calls useCreateFrame or useUpdateFrame mutation
       - Validation matches backend: brand/model required, size ranges enforced
       - Close dialog on successful mutation
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>FrameCatalogTable shows frames with filters. FrameFormDialog validates all fields.</done>
</task>

<task type="auto">
  <name>Task 2: Create FrameCatalogPage and route file</name>
  <files>
    frontend/src/features/optical/components/FrameCatalogPage.tsx,
    frontend/src/app/routes/_authenticated/optical/frames.tsx
  </files>
  <action>
    1. FrameCatalogPage.tsx:
       - Page header: "Frame Catalog" with "Add Frame" button (opens FrameFormDialog in create mode)
       - BarcodeScannerInput at top for quick barcode lookup
       - FrameCatalogTable as main content
       - Uses useSearchFrames hook with debounced search term
       - Generate Barcode button on each row (calls useGenerateBarcode mutation)
       - Show BarcodeDisplay inline in table for frames with barcodes
       - Uses shadcn/ui Card, Button, Badge components

    2. frames.tsx route file:
       - createFileRoute('/_authenticated/optical/frames')
       - component: FrameCatalogPage
       - Follow established route file pattern from pharmacy/index.tsx
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Frame catalog page accessible at /optical/frames with search, filter, barcode scanner</done>
</task>

</tasks>

<verification>
All 4 files compile. Route file correctly exports the page component.
</verification>

<success_criteria>
- DataTable shows all frame attributes with size in standard optical format
- Search and filter work with Material/Type/Gender dropdowns
- Form validates optical-specific size ranges (40-65, 12-24, 120-155)
- Barcode scanner input enables quick frame lookup
- Route accessible at /optical/frames
- Frontend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-27-SUMMARY.md`
</output>
