---
phase: 08-optical-center
plan: 17
type: tdd
wave: 7
depends_on: [08-13]
files_modified:
  - backend/src/Modules/Optical/Optical.Application/Features/Lenses/CreateLensCatalogItem.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Lenses/UpdateLensCatalogItem.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Lenses/GetLensCatalog.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Lenses/AdjustLensStock.cs
autonomous: true
requirements: [OPT-02]

must_haves:
  truths:
    - "Staff can manage lens catalog items with stock entries per power combination"
    - "Staff can adjust lens stock per power entry"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Lenses/CreateLensCatalogItem.cs"
      provides: "Handler for creating lens catalog item"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Lenses/UpdateLensCatalogItem.cs"
      provides: "Handler for updating lens catalog item"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Lenses/GetLensCatalog.cs"
      provides: "Handler for listing lens catalog"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Lenses/AdjustLensStock.cs"
      provides: "Handler for adjusting per-power lens stock with low-stock alert"
  key_links:
    - from: "AdjustLensStock.cs"
      to: "ILensCatalogRepository"
      via: "GetStockEntryAsync for power lookup"
      pattern: "GetStockEntryAsync"
---

<objective>
Implement Lens catalog handlers with TDD: CreateLensCatalogItem, UpdateLensCatalogItem, GetLensCatalog, AdjustLensStock.

Purpose: Enable lens catalog management with per-piece stock tracking for specific power combinations and low-stock alerts.
Output: 4 handler files + corresponding tests
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<feature>
  <name>Lens Catalog Handlers</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/CreateLensCatalogItem.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/UpdateLensCatalogItem.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/GetLensCatalog.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/AdjustLensStock.cs
  </files>
  <behavior>
    CreateLensCatalogItem:
    - Valid command creates lens catalog item and returns Guid
    - Brand/Name required, LensType in allowed values, prices > 0

    UpdateLensCatalogItem:
    - Updates all fields on existing item
    - Returns NotFound for non-existent id

    GetLensCatalog:
    - Returns all lens catalog items with stock entries
    - Supports includeInactive filter

    AdjustLensStock:
    - Adds or removes stock for a specific power combo (sph, cyl, add)
    - Creates new LensStockEntry if none exists for that power
    - Throws if adjustment would result in negative stock
  </behavior>
  <implementation>
    Follow vertical slice pattern. Write tests first in LensHandlerTests.cs, then implement.
    AdjustLensStock: find catalog item, find or create stock entry for power combo, adjust stock.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CreateLensCatalogItem and UpdateLensCatalogItem (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/CreateLensCatalogItem.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/UpdateLensCatalogItem.cs
  </files>
  <behavior>
    - Create validates Brand, Name, LensType, SellingPrice > 0, CostPrice > 0
    - Update finds item, validates, updates
  </behavior>
  <action>
    Write tests in Optical.Unit.Tests/Features/LensHandlerTests.cs first (RED), then implement (GREEN).

    CreateLensCatalogItem: Command(Brand, Name, LensType, Material (int), AvailableCoatings (int), SellingPrice, CostPrice, SupplierId?). Validator: Brand/Name NotEmpty MaxLength, LensType in ["single_vision","bifocal","progressive","reading"], prices > 0. Handler: validate, create via factory, save, return id.

    UpdateLensCatalogItem: Command(Id, Brand, Name, LensType, Material, Coatings, SellingPrice, CostPrice, SupplierId?). Handler: get by id, return NotFound if null, update, save.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~LensHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>CreateLensCatalogItem and UpdateLensCatalogItem pass all tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GetLensCatalog and AdjustLensStock (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/GetLensCatalog.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Lenses/AdjustLensStock.cs
  </files>
  <behavior>
    - GetLensCatalog returns all items with stock entries mapped to DTOs
    - AdjustLensStock finds or creates stock entry, adjusts quantity
  </behavior>
  <action>
    Write tests first (RED), implement (GREEN).

    GetLensCatalog: Query(bool IncludeInactive). Handler: get all from repository, map to LensCatalogItemDto (including StockEntries), return list.

    AdjustLensStock: Command(Guid LensCatalogItemId, decimal Sph, decimal Cyl, decimal? Add, int QuantityChange). Handler: get catalog item by id, find stock entry for power combo (sph/cyl/add). If not found and QuantityChange > 0, create new entry. If not found and QuantityChange <= 0, return error. Adjust stock. Save. Map and return LensStockEntryDto.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~LensHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>GetLensCatalog returns items with stock entries. AdjustLensStock creates or updates stock entries.</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~LensHandler" passes all tests.
</verification>

<success_criteria>
- 4 handler files following vertical slice pattern
- AdjustLensStock creates stock entry if none exists for power combo
- AdjustLensStock prevents negative stock
- All handler tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-17-SUMMARY.md`
</output>
