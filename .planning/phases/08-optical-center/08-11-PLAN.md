---
phase: 08-optical-center
plan: 11
type: execute
wave: 5
depends_on: [08-09, 08-10]
files_modified:
  - backend/src/Modules/Optical/Optical.Infrastructure/OpticalDbContext.cs
  - backend/src/Modules/Optical/Optical.Application/Interfaces/IFrameRepository.cs
  - backend/src/Modules/Optical/Optical.Application/Interfaces/ILensCatalogRepository.cs
  - backend/src/Modules/Optical/Optical.Application/Interfaces/IUnitOfWork.cs
autonomous: true
requirements: [OPT-01, OPT-02]

must_haves:
  truths:
    - "OpticalDbContext registers all entity configurations and DbSets"
    - "Repository interfaces define data access contracts for handlers"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/OpticalDbContext.cs"
      provides: "Updated DbContext with all DbSets and configuration application"
    - path: "backend/src/Modules/Optical/Optical.Application/Interfaces/IFrameRepository.cs"
      provides: "Frame repository interface with search, barcode lookup"
    - path: "backend/src/Modules/Optical/Optical.Application/Interfaces/ILensCatalogRepository.cs"
      provides: "Lens catalog repository interface with stock entry management"
    - path: "backend/src/Modules/Optical/Optical.Application/Interfaces/IUnitOfWork.cs"
      provides: "Unit of work interface for Optical module"
  key_links:
    - from: "OpticalDbContext.cs"
      to: "all Configuration files"
      via: "ApplyConfigurationsFromAssembly"
      pattern: "ApplyConfigurationsFromAssembly"
---

<objective>
Update OpticalDbContext with all entity DbSets and create repository interfaces for Frame, LensCatalog, and UnitOfWork.

Purpose: OpticalDbContext must register all entities for EF Core migration generation. Repository interfaces define the data access contracts that handlers depend on.
Output: Updated OpticalDbContext.cs + 3 interface files
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Pharmacy.Application.Interfaces.ISupplierRepository (pattern):
```csharp
public interface ISupplierRepository {
    Task<Supplier?> GetByIdAsync(Guid id, CancellationToken ct = default);
    Task<List<Supplier>> GetAllAsync(bool includeInactive, CancellationToken ct = default);
    void Add(Supplier supplier);
}
```
From Pharmacy.Application.Interfaces.IUnitOfWork:
```csharp
public interface IUnitOfWork { Task<int> SaveChangesAsync(CancellationToken ct = default); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OpticalDbContext with all DbSets</name>
  <files>backend/src/Modules/Optical/Optical.Infrastructure/OpticalDbContext.cs</files>
  <action>
    Update the existing OpticalDbContext to add:

    DbSets:
    - DbSet<Frame> Frames
    - DbSet<LensCatalogItem> LensCatalogItems
    - DbSet<LensStockEntry> LensStockEntries
    - DbSet<LensOrder> LensOrders
    - DbSet<GlassesOrder> GlassesOrders
    - DbSet<GlassesOrderItem> GlassesOrderItems
    - DbSet<ComboPackage> ComboPackages
    - DbSet<WarrantyClaim> WarrantyClaims
    - DbSet<StocktakingSession> StocktakingSessions
    - DbSet<StocktakingItem> StocktakingItems

    OnModelCreating:
    - Keep modelBuilder.HasDefaultSchema("optical")
    - Add modelBuilder.ApplyConfigurationsFromAssembly(typeof(OpticalDbContext).Assembly) to auto-discover all IEntityTypeConfiguration implementations

    Add using statements for Optical.Domain.Entities namespace.
    Follow PharmacyDbContext pattern exactly.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Infrastructure/Optical.Infrastructure.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>OpticalDbContext has all 10 DbSets and applies configurations from assembly</done>
</task>

<task type="auto">
  <name>Task 2: Create repository interfaces</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Interfaces/IFrameRepository.cs,
    backend/src/Modules/Optical/Optical.Application/Interfaces/ILensCatalogRepository.cs,
    backend/src/Modules/Optical/Optical.Application/Interfaces/IUnitOfWork.cs
  </files>
  <action>
    Create in namespace Optical.Application.Interfaces:

    1. IFrameRepository.cs:
       - Task<Frame?> GetByIdAsync(Guid id, CancellationToken ct)
       - Task<Frame?> GetByBarcodeAsync(string barcode, CancellationToken ct)
       - Task<List<Frame>> GetAllAsync(bool includeInactive, CancellationToken ct)
       - Task<List<Frame>> SearchAsync(string? searchTerm, int? material, int? frameType, int? gender, int page, int pageSize, CancellationToken ct)
       - Task<int> GetTotalCountAsync(string? searchTerm, int? material, int? frameType, int? gender, CancellationToken ct)
       - Task<long> GetNextSequenceNumberAsync(CancellationToken ct) -- for EAN-13 barcode generation
       - Task<bool> IsBarcodeUniqueAsync(string barcode, Guid? excludeId, CancellationToken ct)
       - void Add(Frame frame)

    2. ILensCatalogRepository.cs:
       - Task<LensCatalogItem?> GetByIdAsync(Guid id, CancellationToken ct) -- includes StockEntries
       - Task<List<LensCatalogItem>> GetAllAsync(bool includeInactive, CancellationToken ct)
       - Task<LensStockEntry?> GetStockEntryAsync(Guid catalogItemId, decimal sph, decimal cyl, decimal? add, CancellationToken ct)
       - void Add(LensCatalogItem item)

    3. IUnitOfWork.cs:
       - Task<int> SaveChangesAsync(CancellationToken ct = default)

    Follow Pharmacy.Application.Interfaces pattern exactly.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Application/Optical.Application.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Repository interfaces define Frame search/barcode lookup, Lens catalog with stock entries, UnitOfWork pattern</done>
</task>

</tasks>

<verification>
OpticalDbContext updated + 3 interface files created. Both projects build.
</verification>

<success_criteria>
- OpticalDbContext has 10 DbSets and applies configurations from assembly
- IFrameRepository has barcode lookup and search with filtering
- ILensCatalogRepository includes stock entry queries
- Both Optical.Infrastructure and Optical.Application compile
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-11-SUMMARY.md`
</output>
