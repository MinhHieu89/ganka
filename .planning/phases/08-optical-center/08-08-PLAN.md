---
phase: 08-optical-center
plan: 08
type: execute
wave: 3
depends_on: [08-03, 08-04, 08-05, 08-06]
files_modified:
  - backend/src/Modules/Optical/Optical.Contracts/Dtos/WarrantyClaimDto.cs
  - backend/src/Modules/Optical/Optical.Contracts/Dtos/StocktakingReportDto.cs
  - backend/src/Modules/Optical/Optical.Contracts/Queries/GetOpticalChargesQuery.cs
autonomous: true
requirements: [OPT-07, OPT-08, OPT-09]

must_haves:
  truths:
    - "Warranty and stocktaking DTOs are defined for API serialization"
    - "Cross-module query exists for Billing to collect optical charges"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Contracts/Dtos/WarrantyClaimDto.cs"
      provides: "Warranty claim DTO with approval status and document URLs"
    - path: "backend/src/Modules/Optical/Optical.Contracts/Dtos/StocktakingReportDto.cs"
      provides: "Stocktaking report DTO with session, items, discrepancies"
    - path: "backend/src/Modules/Optical/Optical.Contracts/Queries/GetOpticalChargesQuery.cs"
      provides: "Cross-module query for Billing integration + prescription history query"
  key_links:
    - from: "Optical.Contracts"
      to: "Billing.Contracts"
      via: "GetOpticalChargesQuery responds with VisitChargeDto"
      pattern: "VisitChargeDto"
---

<objective>
Create remaining Contracts DTOs (warranty, stocktaking) and cross-module query records.

Purpose: Complete the Contracts layer with warranty claim DTOs, stocktaking report DTOs, and cross-module queries for Billing and Clinical module integration.
Output: 3 files in Optical.Contracts/
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Billing.Contracts.Queries.GetVisitInvoiceQuery:
```csharp
public sealed record GetVisitInvoiceQuery(Guid VisitId);
public sealed record GetVisitChargesQuery(Guid VisitId);
public sealed record VisitChargeDto(string Description, string? DescriptionVi, decimal UnitPrice, int Quantity, int Department, Guid SourceId, string SourceType);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WarrantyClaim and StocktakingReport DTOs</name>
  <files>
    backend/src/Modules/Optical/Optical.Contracts/Dtos/WarrantyClaimDto.cs,
    backend/src/Modules/Optical/Optical.Contracts/Dtos/StocktakingReportDto.cs
  </files>
  <action>
    1. WarrantyClaimDto.cs in namespace Optical.Contracts.Dtos:
       - public sealed record WarrantyClaimDto(Guid Id, Guid GlassesOrderId, string? PatientName, DateTime ClaimDate, int Resolution, int ApprovalStatus, string AssessmentNotes, decimal? DiscountAmount, Guid? ApprovedById, string? ApprovedByName, DateTime? ApprovedAt, List<string> DocumentUrls, bool RequiresApproval, DateTime CreatedAt)
       - Resolution and ApprovalStatus as int (int-serialized enums)

    2. StocktakingReportDto.cs in namespace Optical.Contracts.Dtos:
       - public sealed record StocktakingSessionDto(Guid Id, string Name, int Status, Guid StartedById, string? StartedByName, DateTime CreatedAt, DateTime? CompletedAt, int TotalItemsScanned, int DiscrepancyCount, string? Notes)
       - public sealed record StocktakingItemDto(Guid Id, Guid StocktakingSessionId, string Barcode, Guid? FrameId, string? FrameName, int PhysicalCount, int SystemCount, int Discrepancy)
       - public sealed record DiscrepancyReportDto(Guid SessionId, string SessionName, DateTime? CompletedAt, int TotalScanned, int TotalDiscrepancies, int OverCount, int UnderCount, int MissingFromSystem, List<StocktakingItemDto> Items)

    Follow Billing.Contracts.Dtos pattern.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Contracts/Optical.Contracts.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Warranty and stocktaking DTOs defined with all fields for API responses</done>
</task>

<task type="auto">
  <name>Task 2: Create cross-module query records</name>
  <files>backend/src/Modules/Optical/Optical.Contracts/Queries/GetOpticalChargesQuery.cs</files>
  <action>
    Create query records in Optical.Contracts/Queries/ namespace Optical.Contracts.Queries:

    1. GetOpticalChargesQuery -- cross-module: Billing queries Optical for charges per visit
       - This responds to Billing.Contracts.GetVisitChargesQuery with optical line items
       - Not a separate query record -- Optical.Application will handle GetVisitChargesQuery and return VisitChargeDto list
       - Instead, create this file with these queries that Optical needs from OTHER modules:

    Records:
    - public sealed record GetPatientOpticalPrescriptionsQuery(Guid PatientId) -- queries Clinical for patient's optical Rx history (OPT-08)
    - public sealed record OpticalPrescriptionHistoryDto(Guid Id, Guid VisitId, DateTime VisitDate, decimal? SphOd, decimal? CylOd, decimal? AxisOd, decimal? AddOd, decimal? SphOs, decimal? CylOs, decimal? AxisOs, decimal? AddOs, decimal? Pd, string? Notes) -- returned by Clinical handler
    - public sealed record GetOpticalSuppliersQuery() -- queries Pharmacy for suppliers with optical type tag

    These allow cross-module communication without direct project references.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Contracts/Optical.Contracts.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Cross-module queries defined for Clinical prescription history and Pharmacy supplier data</done>
</task>

</tasks>

<verification>
All 3 files exist and Optical.Contracts builds successfully.
</verification>

<success_criteria>
- WarrantyClaimDto includes RequiresApproval boolean
- StocktakingReportDto includes DiscrepancyReportDto for summary reports
- Cross-module queries enable Billing, Clinical, and Pharmacy integration
- Optical.Contracts compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-08-SUMMARY.md`
</output>
