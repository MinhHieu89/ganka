---
phase: 08-optical-center
plan: 32
type: execute
wave: 11
depends_on: [08-25]
files_modified:
  - frontend/src/features/optical/components/WarrantyClaimsPage.tsx
  - frontend/src/features/optical/components/WarrantyClaimForm.tsx
  - frontend/src/features/optical/components/WarrantyDocumentUpload.tsx
  - frontend/src/app/routes/_authenticated/optical/warranty.tsx
autonomous: true
requirements: [OPT-07]

must_haves:
  truths:
    - "Staff can file warranty claims with resolution type and supporting documents"
    - "Manager approval workflow visible for Replace resolution"
    - "Supporting documents uploaded to Azure Blob"
  artifacts:
    - path: "frontend/src/features/optical/components/WarrantyClaimsPage.tsx"
      provides: "Warranty claims list with approval status filter"
    - path: "frontend/src/features/optical/components/WarrantyClaimForm.tsx"
      provides: "Form for filing warranty claims"
    - path: "frontend/src/features/optical/components/WarrantyDocumentUpload.tsx"
      provides: "Document upload component for warranty evidence"
    - path: "frontend/src/app/routes/_authenticated/optical/warranty.tsx"
      provides: "TanStack Router route file"
  key_links:
    - from: "WarrantyClaimForm.tsx"
      to: "optical-queries.ts"
      via: "useCreateWarrantyClaim mutation"
      pattern: "useCreateWarrantyClaim"
---

<objective>
Create the Warranty Claims frontend: list page, claim form, document upload, and route.

Purpose: Enable staff to file warranty claims (12 months from delivery), select resolution type (Replace/Repair/Discount), upload supporting photos/documents, and view manager approval status for replacement claims.
Output: 3 component files + 1 route file
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WarrantyClaimsPage and WarrantyClaimForm</name>
  <files>
    frontend/src/features/optical/components/WarrantyClaimsPage.tsx,
    frontend/src/features/optical/components/WarrantyClaimForm.tsx
  </files>
  <action>
    1. WarrantyClaimsPage.tsx:
       - Page header: "Warranty Claims" with "File Claim" button
       - Filter tabs: All, Pending Approval, Approved, Rejected
       - DataTable columns: Claim Date, Patient, Order #, Resolution (Replace/Repair/Discount badge), Approval Status (Pending=yellow, Approved=green, Rejected=red badge), Documents (count), Actions
       - Actions: View details (expand), Approve/Reject (for managers on Replace claims)
       - Approve/Reject buttons trigger confirmation dialog then call useApproveWarrantyClaim mutation
       - Pagination
       - Uses useWarrantyClaims hook with approval status filter

    2. WarrantyClaimForm.tsx:
       - Dialog form for filing a new warranty claim
       - Fields: Glasses Order (searchable select - show patient name, order date), Resolution Type (radio: Replace/Repair/Discount), Assessment Notes (textarea, required), Discount Amount (number, shown only when Resolution is Discount)
       - Info panel showing: order delivery date, warranty expiry date, days remaining
       - Warning if order is not under warranty (disable submit)
       - For Replace: show "Requires manager approval" notice
       - React Hook Form + Zod validation
       - Calls useCreateWarrantyClaim mutation on submit
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Claims page shows approval workflow. Form validates warranty period and shows approval notice for Replace.</done>
</task>

<task type="auto">
  <name>Task 2: Create WarrantyDocumentUpload and route</name>
  <files>
    frontend/src/features/optical/components/WarrantyDocumentUpload.tsx,
    frontend/src/app/routes/_authenticated/optical/warranty.tsx
  </files>
  <action>
    1. WarrantyDocumentUpload.tsx:
       Props: { claimId: string, existingDocuments: string[] }
       - File upload area (drag-and-drop or click to browse) using shadcn/ui Input type="file"
       - Accept images (jpg, png) and documents (pdf)
       - Max file size: 10MB
       - Preview thumbnails for uploaded images
       - List of existing documents with links to view (opens Azure Blob URL in new tab)
       - Upload button calls useUploadWarrantyDocument mutation (multipart/form-data)
       - Show upload progress indicator
       - Multiple file support (upload one at a time)

    2. warranty.tsx route file:
       - createFileRoute('/_authenticated/optical/warranty')
       - component: WarrantyClaimsPage
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Document upload supports images/PDFs with preview. Route at /optical/warranty.</done>
</task>

</tasks>

<verification>
All 4 files compile. Route accessible at /optical/warranty.
</verification>

<success_criteria>
- Claims list filterable by approval status
- Replace claims show "Requires Approval" and manager can approve/reject
- Warranty period validated before claim submission
- Documents uploadable with preview
- Route accessible at /optical/warranty
- Frontend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-32-SUMMARY.md`
</output>
