---
phase: 08-optical-center
plan: 20
type: tdd
wave: 7
depends_on: [08-14]
files_modified:
  - backend/src/Modules/Optical/Optical.Application/Features/Warranty/GetWarrantyClaims.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/StartStocktakingSession.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/RecordStocktakingItem.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/CompleteStocktaking.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/GetDiscrepancyReport.cs
autonomous: true
requirements: [OPT-07, OPT-09]

must_haves:
  truths:
    - "Staff can start a stocktaking session, scan barcodes, record physical counts"
    - "System produces discrepancy report comparing physical vs system inventory"
    - "Duplicate barcode scans update existing entry (upsert)"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Warranty/GetWarrantyClaims.cs"
      provides: "Handler for listing warranty claims with filters"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/StartStocktakingSession.cs"
      provides: "Handler for starting a new stocktaking session"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/RecordStocktakingItem.cs"
      provides: "Handler for recording barcode scan with physical count"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/CompleteStocktaking.cs"
      provides: "Handler for completing stocktaking session"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/GetDiscrepancyReport.cs"
      provides: "Handler for generating discrepancy report"
  key_links:
    - from: "RecordStocktakingItem.cs"
      to: "IFrameRepository"
      via: "GetByBarcodeAsync to resolve frame from barcode"
      pattern: "GetByBarcodeAsync"
---

<objective>
Implement GetWarrantyClaims and all Stocktaking handlers with TDD.

Purpose: Complete warranty query handler and enable barcode-based stocktaking workflow: start session, scan items, record counts, generate discrepancy report.
Output: 5 handler files + corresponding tests
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<feature>
  <name>Warranty Query + Stocktaking Handlers</name>
  <files>5 handler files</files>
  <behavior>
    GetWarrantyClaims: Paginated list with approval status filter
    StartStocktakingSession: Creates new session, fails if one already in progress
    RecordStocktakingItem: Scans barcode, looks up frame, records physical count. Upserts if barcode already scanned.
    CompleteStocktaking: Marks session complete
    GetDiscrepancyReport: Returns summary with over/under/missing counts
  </behavior>
  <implementation>
    RecordStocktakingItem: look up frame by barcode via IFrameRepository.GetByBarcodeAsync. Get system count from frame.StockQuantity. Call session.RecordItem (upsert). Save.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GetWarrantyClaims and StartStocktakingSession (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Warranty/GetWarrantyClaims.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/StartStocktakingSession.cs
  </files>
  <behavior>
    - GetWarrantyClaims: paginated with approval status filter
    - StartStocktakingSession: creates session, fails if InProgress session exists
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    GetWarrantyClaims: Query(int? ApprovalStatusFilter, int Page, int PageSize). Handler: call repository, map to WarrantyClaimDto list with total count.

    StartStocktakingSession: Command(string Name). Handler: check if current InProgress session exists via repository.GetCurrentSessionAsync. If exists, return error "A stocktaking session is already in progress." Create new session, save, return id.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~WarrantyHandler|StocktakingHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>GetWarrantyClaims returns paginated results. StartStocktaking prevents concurrent sessions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RecordStocktakingItem, CompleteStocktaking, GetDiscrepancyReport (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/RecordStocktakingItem.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/CompleteStocktaking.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Stocktaking/GetDiscrepancyReport.cs
  </files>
  <behavior>
    - RecordStocktakingItem: scans barcode, resolves frame, records count, upserts
    - CompleteStocktaking: marks complete, fails if not InProgress
    - GetDiscrepancyReport: summary with item-level discrepancies
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    RecordStocktakingItem: Command(Guid SessionId, string Barcode, int PhysicalCount). Handler: get session by id (must be InProgress). Look up frame by barcode via IFrameRepository.GetByBarcodeAsync. System count = frame?.StockQuantity ?? 0. Call session.RecordItem(barcode, physicalCount, systemCount) -- this upserts. If frame found, set FrameId and FrameName. Save. Return StocktakingItemDto.

    CompleteStocktaking: Command(Guid SessionId). Handler: get session, call session.Complete(), save.

    GetDiscrepancyReport: Query(Guid SessionId). Handler: get session with items. Build DiscrepancyReportDto with: TotalScanned, TotalDiscrepancies (items where Physical != System), OverCount (Physical > System items count), UnderCount (Physical < System items count), MissingFromSystem (items where FrameId is null -- barcode not in system). Return dto.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~StocktakingHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>Stocktaking workflow complete: scan, record, complete, report. Upsert prevents duplicates.</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~StocktakingHandler" passes all tests.
</verification>

<success_criteria>
- StartStocktaking prevents concurrent sessions
- RecordStocktakingItem resolves frame from barcode and uses upsert
- GetDiscrepancyReport categorizes over/under/missing discrepancies
- All handler tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-20-SUMMARY.md`
</output>
