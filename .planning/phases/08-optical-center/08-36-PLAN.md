---
phase: 08-optical-center
plan: 36
type: execute
wave: 14
depends_on: [08-35]
autonomous: false
files_modified:
  - .planning/phases/08-optical-center/08-36-verification-report.md
requirements: [OPT-01, OPT-02, OPT-03, OPT-04, OPT-05, OPT-06, OPT-07, OPT-08, OPT-09]

must_haves:
  truths:
    - "All optical module backend tests pass"
    - "Frontend compiles and all pages render"
    - "API endpoints respond correctly"
    - "User can verify complete optical workflow"
  artifacts:
    - path: ".planning/phases/08-optical-center/08-36-verification-report.md"
      provides: "Automated verification results report"
  key_links: []
---

<objective>
End-to-end verification checkpoint for the Optical Center module.

Purpose: Verify all OPT requirements are working: frame/lens inventory, glasses orders with payment gate, combo pricing, warranty claims, prescription history, stocktaking.
Output: Verification report + human verification of complete optical workflow
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification and produce report</name>
  <files>.planning/phases/08-optical-center/08-36-verification-report.md</files>
  <action>
    Run all automated checks and write results to a verification report file:

    1. Backend unit tests:
       ```bash
       dotnet test backend/tests/Optical.Unit.Tests --no-build -v q
       ```
       Verify all tests pass with >= 80% code coverage per CLAUDE.md requirement.

    2. Full backend build:
       ```bash
       dotnet build backend/Ganka28.sln -v q
       ```

    3. Frontend type check:
       ```bash
       cd frontend && npx tsc --noEmit
       ```

    4. Start backend and frontend:
       ```bash
       # Kill processes on ports 5255 and 3000 if in use
       # Start backend on port 5255
       cd backend/src/Bootstrapper && dotnet run --urls "http://localhost:5255" &
       # Start frontend on port 3000
       cd frontend && npm run dev -- --port 3000 &
       ```

    5. API smoke tests (curl):
       - GET http://localhost:5255/api/optical/frames -> 200
       - GET http://localhost:5255/api/optical/lenses -> 200
       - GET http://localhost:5255/api/optical/orders -> 200
       - GET http://localhost:5255/api/optical/combos -> 200
       - GET http://localhost:5255/api/optical/warranty -> 200
       - GET http://localhost:5255/api/optical/stocktaking -> 200

    Write all results (pass/fail, output snippets) to `.planning/phases/08-optical-center/08-36-verification-report.md` with sections for each check. Include timestamps and coverage percentage.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>All backend tests pass, solution builds, frontend compiles, API endpoints respond 200. Report written.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Optical Center module: frame/lens inventory with barcodes, glasses order lifecycle with payment gate, combo pricing, warranty claims, prescription history, barcode-based stocktaking</what-built>
  <how-to-verify>
    1. Open http://localhost:3000 and log in (Admin@ganka28.com / Admin@123456)
    2. Navigate to Optical Center > Frame Catalog
       - Verify table loads (empty or with seed data)
       - Create a new frame with all fields
       - Generate barcode for the frame
       - Verify barcode displays correctly
    3. Navigate to Lens Catalog
       - Create a lens catalog item
       - Add stock entry for a specific power
       - Verify low stock alert if applicable
    4. Navigate to Glasses Orders
       - Create a new order (may need existing patient + visit + optical Rx)
       - Verify order appears with "Ordered" status
       - Try advancing to "Processing" without payment -> should show error
    5. Navigate to Combo Packages
       - Create a preset combo
       - Verify savings display
    6. Navigate to Warranty Claims
       - File a claim for Replace resolution
       - Verify "Requires Approval" indicator
    7. Navigate to Stocktaking
       - Start a new session
       - Scan a barcode (type in manually if no USB scanner)
       - Complete session
       - View discrepancy report
    8. Navigate to patient profile > Optical Rx History tab
       - Verify prescription history timeline renders
       - If prescriptions exist, verify comparison works
    9. Verify sidebar shows all sub-items under Optical Center
    10. Verify Vietnamese translations load when switching language
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
All automated checks pass. Human verifies complete optical workflow.
</verification>

<success_criteria>
- All OPT-01 through OPT-09 requirements verifiable
- Backend tests >= 80% coverage
- All API endpoints return correct responses
- Frontend pages render and are interactive
- Prescription history tab accessible from patient profile
- Vietnamese translations display correctly
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-36-SUMMARY.md`
</output>
