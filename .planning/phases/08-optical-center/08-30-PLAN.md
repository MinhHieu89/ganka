---
phase: 08-optical-center
plan: 30
type: execute
wave: 12
depends_on: [08-29]
files_modified:
  - frontend/src/features/optical/components/GlassesOrderDetailPage.tsx
  - frontend/src/features/optical/components/OverdueOrderAlert.tsx
  - frontend/src/app/routes/_authenticated/optical/orders.$orderId.tsx
autonomous: true
requirements: [OPT-03, OPT-04]

must_haves:
  truths:
    - "Staff can view full order detail with items, status timeline, payment status"
    - "Staff can advance order status from the detail page"
    - "Payment gate is visible: 'Pay before processing' message when unpaid"
  artifacts:
    - path: "frontend/src/features/optical/components/GlassesOrderDetailPage.tsx"
      provides: "Order detail with status advancement, items, payment gate UI"
    - path: "frontend/src/features/optical/components/OverdueOrderAlert.tsx"
      provides: "Reusable overdue order alert component"
    - path: "frontend/src/app/routes/_authenticated/optical/orders.$orderId.tsx"
      provides: "Dynamic route for order detail"
  key_links:
    - from: "GlassesOrderDetailPage.tsx"
      to: "optical-queries.ts"
      via: "useGlassesOrderById, useUpdateOrderStatus"
      pattern: "useGlassesOrderById|useUpdateOrderStatus"
---

<objective>
Create the Glasses Order detail page with status advancement and payment gate UI.

Purpose: Enable staff to view full order details and advance order status. Critical: shows payment gate -- staff cannot advance to Processing without confirmed payment (OPT-04).
Output: 2 component files + 1 route file
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GlassesOrderDetailPage and OverdueOrderAlert</name>
  <files>
    frontend/src/features/optical/components/GlassesOrderDetailPage.tsx,
    frontend/src/features/optical/components/OverdueOrderAlert.tsx
  </files>
  <action>
    1. GlassesOrderDetailPage.tsx:
       - Uses useGlassesOrderById(orderId) hook
       - Header: Patient name, Order ID, OrderStatusBadge
       - Status timeline: horizontal stepper showing Ordered -> Processing -> Received -> Ready -> Delivered with current step highlighted
       - "Advance Status" button: shows next status transition. Disabled with warning message if:
         - Current is Ordered and IsPaymentConfirmed is false: show "Payment must be completed before processing" alert
       - When clicked, calls useUpdateOrderStatus mutation
       - Order details card: Processing Type, Estimated Delivery, Created Date, Delivered Date
       - Items table: Description, Unit Price, Quantity, Line Total
       - If IsOverdue, show OverdueOrderAlert at top
       - If IsUnderWarranty, show green "Under Warranty" badge with expiry date
       - Notes section

    2. OverdueOrderAlert.tsx:
       Props: { estimatedDate: string, orderCount?: number }
       - shadcn/ui Alert with destructive variant
       - Shows "This order is overdue" with estimated date
       - Optional: "X orders are overdue" for list view
       - Uses AlertTriangle icon from tabler icons
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Order detail shows status timeline, payment gate UI, overdue alert, warranty status</done>
</task>

<task type="auto">
  <name>Task 2: Create order detail route</name>
  <files>frontend/src/app/routes/_authenticated/optical/orders.$orderId.tsx</files>
  <action>
    Create dynamic route file:
    - createFileRoute('/_authenticated/optical/orders/$orderId')
    - component: GlassesOrderDetailPage
    - Use Route.useParams() to get orderId parameter
    - Follow established pattern from visits/$visitId.tsx or patients/$patientId.tsx
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Dynamic route at /optical/orders/:orderId renders order detail page</done>
</task>

</tasks>

<verification>
All 3 files compile. Route accessible at /optical/orders/:orderId.
</verification>

<success_criteria>
- Status timeline shows full order lifecycle visually
- Payment gate: "Advance to Processing" disabled when unpaid with clear message
- Overdue alert shown for orders past estimated delivery
- Warranty status visible with expiry date
- Route works with dynamic orderId parameter
- Frontend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-30-SUMMARY.md`
</output>
