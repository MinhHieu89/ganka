---
phase: 08-optical-center
plan: 21
type: tdd
wave: 7
depends_on: [08-14, 08-15]
files_modified:
  - backend/src/Modules/Optical/Optical.Application/Features/Prescriptions/GetPatientPrescriptionHistory.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Prescriptions/GetPrescriptionComparison.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Alerts/GetLowLensStockAlerts.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Warranty/UploadWarrantyDocument.cs
autonomous: true
requirements: [OPT-08, OPT-02, OPT-07]

must_haves:
  truths:
    - "System shows lens prescription history per patient with year-over-year comparison"
    - "Staff can upload warranty claim supporting documents to Azure Blob"
    - "System alerts on low lens stock levels"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Prescriptions/GetPatientPrescriptionHistory.cs"
      provides: "Handler for prescription history via cross-module query"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Prescriptions/GetPrescriptionComparison.cs"
      provides: "Handler for year-over-year prescription comparison"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Alerts/GetLowLensStockAlerts.cs"
      provides: "Handler for low lens stock alerts"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Warranty/UploadWarrantyDocument.cs"
      provides: "Handler for uploading warranty supporting documents"
  key_links:
    - from: "GetPatientPrescriptionHistory.cs"
      to: "Clinical.Contracts"
      via: "IMessageBus cross-module query"
      pattern: "GetPatientOpticalPrescriptionsQuery"
---

<objective>
Implement prescription history, lens stock alerts, and warranty document upload handlers with TDD.

Purpose: Enable lens prescription history viewing (OPT-08), low stock alerting, and warranty document management with Azure Blob.
Output: 4 handler files + corresponding tests
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Optical.Contracts.Queries:
```csharp
public sealed record GetPatientOpticalPrescriptionsQuery(Guid PatientId);
public sealed record OpticalPrescriptionHistoryDto(Guid Id, Guid VisitId, DateTime VisitDate, decimal? SphOd, decimal? CylOd, decimal? AxisOd, decimal? AddOd, decimal? SphOs, decimal? CylOs, decimal? AxisOs, decimal? AddOs, decimal? Pd, string? Notes);
```
From Shared.Infrastructure:
```csharp
public interface IAzureBlobService { Task<string> UploadAsync(Stream stream, string fileName, string containerName, CancellationToken ct); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement prescription history and comparison handlers (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Prescriptions/GetPatientPrescriptionHistory.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Prescriptions/GetPrescriptionComparison.cs
  </files>
  <behavior>
    - GetPatientPrescriptionHistory: queries Clinical module for optical Rx history
    - GetPrescriptionComparison: compares two prescriptions side-by-side with change indicators
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    GetPatientPrescriptionHistory: Query(Guid PatientId). Handler: call bus.InvokeAsync<List<OpticalPrescriptionHistoryDto>?>(new GetPatientOpticalPrescriptionsQuery(patientId)). Return list ordered by VisitDate desc. If null/empty, return empty list.

    GetPrescriptionComparison: Query(Guid PatientId, Guid PrescriptionId1, Guid PrescriptionId2). Handler: get history, find both prescriptions, create comparison DTO showing: old values, new values, and change direction (improved/worsened/same) for each field (Sph, Cyl, Axis, Add per eye). Return PrescriptionComparisonDto.

    Create PrescriptionComparisonDto record: (OpticalPrescriptionHistoryDto Older, OpticalPrescriptionHistoryDto Newer, List<FieldChange> Changes). FieldChange record: (string FieldName, string? OldValue, string? NewValue, string Direction).

    For tests, mock IMessageBus to return sample prescription data. Test comparison correctly identifies older vs newer by date.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~PrescriptionHistory" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>Prescription history retrieves cross-module data. Comparison identifies changes per field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GetLowLensStockAlerts and UploadWarrantyDocument (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Alerts/GetLowLensStockAlerts.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Warranty/UploadWarrantyDocument.cs
  </files>
  <behavior>
    - GetLowLensStockAlerts: returns stock entries below min stock level
    - UploadWarrantyDocument: uploads file to Azure Blob, adds URL to warranty claim
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    GetLowLensStockAlerts: Query(). Handler: get all lens catalog items with stock entries. Filter entries where Quantity <= MinStockLevel. Return list of alert DTOs (CatalogItemName, Sph, Cyl, Add, CurrentStock, MinStockLevel).

    UploadWarrantyDocument: Command(Guid ClaimId, Stream FileStream, string FileName). Handler: get claim by id. Upload to Azure Blob via IAzureBlobService.UploadAsync(stream, fileName, "warranty-documents", ct). Add returned URL to claim.AddDocumentUrl(url). Save. Return URL.

    For UploadWarrantyDocument test, mock IAzureBlobService to return a URL. Verify claim.DocumentUrls contains the URL after handler.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~Alert|Warranty" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>Low stock alerts filter entries below threshold. Warranty documents uploaded to Azure Blob.</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Optical.Unit.Tests passes all prescription, alert, and warranty upload tests.
</verification>

<success_criteria>
- Prescription history uses cross-module query to Clinical
- Comparison identifies changes per eye per field
- Low stock alerts filter below MinStockLevel
- Warranty documents stored in Azure Blob
- All handler tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-21-SUMMARY.md`
</output>
