---
phase: 08-optical-center
plan: 15
type: tdd
wave: 7
depends_on: [08-13]
files_modified:
  - backend/tests/Optical.Unit.Tests/Optical.Unit.Tests.csproj
  - backend/tests/Optical.Unit.Tests/Domain/FrameTests.cs
  - backend/tests/Optical.Unit.Tests/Domain/GlassesOrderTests.cs
  - backend/tests/Optical.Unit.Tests/Domain/Ean13GeneratorTests.cs
autonomous: true
requirements: [OPT-01, OPT-03]

must_haves:
  truths:
    - "Test project exists with references to Optical.Domain and Optical.Application"
    - "Domain entity tests validate Frame creation, stock adjustment, and barcode validation"
    - "GlassesOrder tests validate status transitions and invalid transition rejection"
    - "EAN-13 tests validate generation and check digit calculation"
  artifacts:
    - path: "backend/tests/Optical.Unit.Tests/Optical.Unit.Tests.csproj"
      provides: "Test project with xUnit, FluentAssertions, NSubstitute references"
    - path: "backend/tests/Optical.Unit.Tests/Domain/FrameTests.cs"
      provides: "Frame entity domain tests"
    - path: "backend/tests/Optical.Unit.Tests/Domain/GlassesOrderTests.cs"
      provides: "GlassesOrder lifecycle domain tests"
    - path: "backend/tests/Optical.Unit.Tests/Domain/Ean13GeneratorTests.cs"
      provides: "EAN-13 barcode generation/validation tests"
  key_links:
    - from: "Optical.Unit.Tests.csproj"
      to: "Optical.Domain"
      via: "ProjectReference"
      pattern: "Optical\\.Domain"
---

<objective>
Create Optical.Unit.Tests project and domain entity tests for Frame, GlassesOrder, and EAN-13 generator.

Purpose: Establish the test project scaffold and write domain-level tests that validate entity behavior before handler tests. TDD: write tests first (RED), verify they compile and fail, then verify domain entities make them pass (GREEN).
Output: Test project + 3 domain test files
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test project scaffold</name>
  <files>backend/tests/Optical.Unit.Tests/Optical.Unit.Tests.csproj</files>
  <behavior>
    - Project references Optical.Domain, Optical.Application
    - Package references: xUnit, xunit.runner.visualstudio, FluentAssertions, NSubstitute, Microsoft.NET.Test.Sdk, coverlet.collector
    - dotnet test discovers the project
  </behavior>
  <action>
    Create Optical.Unit.Tests.csproj following the exact pattern from other test projects (e.g., Pharmacy.Unit.Tests or Billing.Unit.Tests). Include:
    - TargetFramework net10.0
    - IsPackable false
    - PackageReferences for xUnit, FluentAssertions 8.x, NSubstitute, Microsoft.NET.Test.Sdk, coverlet.collector
    - ProjectReferences to Optical.Domain and Optical.Application

    Also add to backend/Ganka28.sln if not already there (use dotnet sln add).
  </action>
  <verify>
    <automated>dotnet build backend/tests/Optical.Unit.Tests/Optical.Unit.Tests.csproj -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Test project builds and is registered in solution file</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write domain entity tests (RED then GREEN)</name>
  <files>
    backend/tests/Optical.Unit.Tests/Domain/FrameTests.cs,
    backend/tests/Optical.Unit.Tests/Domain/GlassesOrderTests.cs,
    backend/tests/Optical.Unit.Tests/Domain/Ean13GeneratorTests.cs
  </files>
  <behavior>
    FrameTests:
    - Create_ShouldSetAllProperties
    - Create_ShouldSetBranchId
    - AdjustStock_ShouldIncreaseQuantity
    - AdjustStock_ShouldDecreaseQuantity
    - AdjustStock_NegativeResult_ShouldThrow
    - Update_ShouldModifyFields

    GlassesOrderTests:
    - TransitionTo_OrderedToProcessing_ShouldSucceed
    - TransitionTo_ProcessingToReceived_ShouldSucceed
    - TransitionTo_ReceivedToReady_ShouldSucceed
    - TransitionTo_ReadyToDelivered_ShouldSucceed_AndSetDeliveredAt
    - TransitionTo_InvalidTransition_ShouldThrow (e.g., Ordered->Delivered)
    - IsUnderWarranty_WithinTwelveMonths_ShouldReturnTrue
    - IsUnderWarranty_AfterTwelveMonths_ShouldReturnFalse
    - IsOverdue_PastEstimatedDate_ShouldReturnTrue

    Ean13GeneratorTests:
    - Generate_ShouldReturn13Digits
    - Generate_ShouldHaveCorrectCheckDigit
    - IsValid_ValidBarcode_ShouldReturnTrue
    - IsValid_InvalidCheckDigit_ShouldReturnFalse
    - IsValid_WrongLength_ShouldReturnFalse
    - CalculateCheckDigit_KnownValue_ShouldMatch (use known EAN-13: "5901234123457" -> check digit 7)
  </behavior>
  <action>
    Write all tests first (RED) using xUnit + FluentAssertions. Then run tests -- they should pass since domain entities were created in Plans 03-06. If any tests fail, fix the domain entities to make them pass (GREEN).

    Use [Fact] for single cases, [Theory] with [InlineData] for parameterized tests (e.g., invalid transitions).

    For GlassesOrderTests, use the factory method to create an order, then test transitions.
    For Ean13GeneratorTests, use known EAN-13 barcodes to verify check digit calculation.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~Domain" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>All domain tests pass: Frame CRUD, GlassesOrder lifecycle, EAN-13 validation</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Optical.Unit.Tests --no-build -v q passes all domain tests.
</verification>

<success_criteria>
- Test project builds and is in solution
- 20+ domain tests cover Frame, GlassesOrder, EAN-13 behavior
- All tests pass (GREEN)
- Invalid state transitions throw exceptions
- EAN-13 check digit verified against known values
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-15-SUMMARY.md`
</output>
