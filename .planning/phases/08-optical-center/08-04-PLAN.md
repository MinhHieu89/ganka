---
phase: 08-optical-center
plan: 04
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - backend/src/Modules/Optical/Optical.Domain/Entities/LensCatalogItem.cs
  - backend/src/Modules/Optical/Optical.Domain/Entities/LensStockEntry.cs
  - backend/src/Modules/Optical/Optical.Domain/Entities/LensOrder.cs
autonomous: true
requirements: [OPT-02]

must_haves:
  truths:
    - "Lens catalog supports hybrid model: bulk stock for common powers + custom orders per prescription"
    - "Per-piece quantity tracking for stocked lenses with low-stock alerts"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Domain/Entities/LensCatalogItem.cs"
      provides: "AggregateRoot for lens types with brand, type, material, coating, price"
      min_lines: 60
    - path: "backend/src/Modules/Optical/Optical.Domain/Entities/LensStockEntry.cs"
      provides: "Entity for specific power combinations in stock (child of LensCatalogItem)"
    - path: "backend/src/Modules/Optical/Optical.Domain/Entities/LensOrder.cs"
      provides: "Entity for custom lens orders to suppliers per patient prescription"
  key_links:
    - from: "LensCatalogItem.cs"
      to: "LensStockEntry.cs"
      via: "one-to-many child collection"
      pattern: "List<LensStockEntry>"
---

<objective>
Create lens catalog entities: LensCatalogItem (aggregate root), LensStockEntry (stocked power entries), and LensOrder (custom supplier orders).

Purpose: Implements the hybrid lens model -- bulk stock for common powers with per-piece tracking, plus custom orders per prescription for unusual parameters. Supports Essilor, Hoya, Viet Phap suppliers.
Output: 3 entity files in Optical.Domain/Entities/
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-CONTEXT.md
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Shared.Domain:
```csharp
public abstract class AggregateRoot : Entity { BranchId BranchId; void SetBranchId(BranchId); void SetUpdatedAt(); void AddDomainEvent(IDomainEvent); }
public abstract class Entity { Guid Id; DateTime CreatedAt; DateTime? UpdatedAt; }
public interface IAuditable { }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LensCatalogItem aggregate root</name>
  <files>backend/src/Modules/Optical/Optical.Domain/Entities/LensCatalogItem.cs</files>
  <action>
    Create LensCatalogItem : AggregateRoot, IAuditable in Optical.Domain.Entities:

    Properties (all private set):
    - string Brand (e.g., "Essilor", "Hoya", "Viet Phap")
    - string Name (e.g., "Essilor Crizal Single Vision")
    - string LensType (single_vision, bifocal, progressive, reading -- use string not enum for flexibility)
    - LensMaterial Material
    - LensCoating AvailableCoatings ([Flags] enum)
    - decimal SellingPrice, decimal CostPrice
    - bool IsActive
    - Guid? PreferredSupplierId (links to Pharmacy.Supplier via cross-module query)

    Child collection:
    - private readonly List<LensStockEntry> _stockEntries = new()
    - public IReadOnlyList<LensStockEntry> StockEntries => _stockEntries.AsReadOnly()

    Methods:
    - static LensCatalogItem Create(brand, name, lensType, material, availableCoatings, sellingPrice, costPrice, supplierId, BranchId)
    - void Update(brand, name, lensType, material, availableCoatings, sellingPrice, costPrice, supplierId)
    - LensStockEntry AddStockEntry(sph, cyl, add, quantity) -- creates and adds a stock entry for a specific power combo
    - void Deactivate() / void Activate()

    Private parameterless constructor for EF Core. Use backing field pattern for _stockEntries.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Domain/Optical.Domain.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>LensCatalogItem has full catalog properties and child stock entry collection</done>
</task>

<task type="auto">
  <name>Task 2: Create LensStockEntry and LensOrder entities</name>
  <files>
    backend/src/Modules/Optical/Optical.Domain/Entities/LensStockEntry.cs,
    backend/src/Modules/Optical/Optical.Domain/Entities/LensOrder.cs
  </files>
  <action>
    1. LensStockEntry : Entity in Optical.Domain.Entities:
       Properties:
       - Guid LensCatalogItemId (FK to parent)
       - decimal Sph (spherical power, e.g., -2.00)
       - decimal Cyl (cylinder power, e.g., -0.75, nullable=0 for sphere-only)
       - decimal? Add (addition for bifocal/progressive, nullable)
       - int Quantity (pieces in stock)
       - int MinStockLevel (default 2)

       Methods:
       - static LensStockEntry Create(lensCatalogItemId, sph, cyl, add, quantity)
       - void AdjustStock(int change) -- throws if goes negative. Raises LowStockAlertEvent on parent if below min.

    2. LensOrder : Entity, IAuditable in Optical.Domain.Entities:
       Properties:
       - Guid LensCatalogItemId (FK - which lens type)
       - Guid SupplierId (which supplier to order from)
       - Guid GlassesOrderId (FK - linked to the glasses order that triggered this)
       - Guid PatientId
       - decimal Sph, decimal Cyl, decimal? Add, decimal? Axis (prescription parameters)
       - LensCoating RequestedCoatings
       - string Status (Ordered, Received, Cancelled -- simple string, not complex state machine)
       - DateTime? ReceivedAt
       - string? Notes

       Methods:
       - static LensOrder Create(lensCatalogItemId, supplierId, glassesOrderId, patientId, sph, cyl, add, axis, coatings)
       - void MarkReceived() -- sets ReceivedAt = UtcNow, Status = "Received"
       - void Cancel(string reason) -- sets Status = "Cancelled", adds reason to Notes

    Private parameterless constructors for EF Core.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Domain/Optical.Domain.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>LensStockEntry tracks per-power stock with adjustment. LensOrder tracks custom supplier orders per prescription.</done>
</task>

</tasks>

<verification>
All 3 entity files exist and Optical.Domain builds successfully.
</verification>

<success_criteria>
- LensCatalogItem supports full catalog attributes with child stock entries
- LensStockEntry tracks per-piece quantity for specific power combinations
- LensOrder tracks custom orders to suppliers (Essilor, Hoya, Viet Phap)
- Optical.Domain compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-04-SUMMARY.md`
</output>
