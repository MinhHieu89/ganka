---
phase: 08-optical-center
plan: 09
type: execute
wave: 4
depends_on: [08-07]
files_modified:
  - backend/src/Modules/Optical/Optical.Infrastructure/Configurations/FrameConfiguration.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Configurations/LensCatalogItemConfiguration.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Configurations/LensOrderConfiguration.cs
autonomous: true
requirements: [OPT-01, OPT-02]

must_haves:
  truths:
    - "Frame has unique filtered index on Barcode for barcode uniqueness"
    - "LensCatalogItem has child collection mapped via backing field"
    - "LensOrder maps all prescription parameters"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Configurations/FrameConfiguration.cs"
      provides: "EF Core config with unique barcode index, enum conversions"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Configurations/LensCatalogItemConfiguration.cs"
      provides: "EF Core config with backing field for StockEntries, LensStockEntry owned config"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Configurations/LensOrderConfiguration.cs"
      provides: "EF Core config for custom lens orders to suppliers"
  key_links:
    - from: "FrameConfiguration.cs"
      to: "Frame.cs"
      via: "unique filtered index on Barcode"
      pattern: "HasIndex.*Barcode.*IsUnique"
---

<objective>
Create EF Core configurations for Frame, LensCatalogItem (with LensStockEntry), and LensOrder.

Purpose: Map domain entities to SQL Server with proper column types, indexes, constraints, and backing field configurations. Critical: Frame.Barcode must have a unique filtered index to prevent duplicate barcodes.
Output: 3 EF Core configuration files
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Pharmacy.Infrastructure.Configurations.SupplierConfiguration (pattern):
```csharp
public class SupplierConfiguration : IEntityTypeConfiguration<Supplier> {
    public void Configure(EntityTypeBuilder<Supplier> builder) {
        builder.ToTable("Suppliers");
        builder.HasKey(x => x.Id);
        builder.Property(x => x.Name).HasMaxLength(200).IsRequired();
        // ...
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Frame EF Core configuration</name>
  <files>backend/src/Modules/Optical/Optical.Infrastructure/Configurations/FrameConfiguration.cs</files>
  <action>
    Create FrameConfiguration : IEntityTypeConfiguration<Frame> in Optical.Infrastructure.Configurations:

    - ToTable("Frames")
    - HasKey(x => x.Id)
    - Property Brand: MaxLength(100), IsRequired
    - Property Model: MaxLength(100), IsRequired
    - Property Color: MaxLength(50), IsRequired
    - Property LensWidth, BridgeWidth, TempleLength: IsRequired
    - Property Material: enum stored as int
    - Property Type: enum stored as int (column name "FrameType" to avoid SQL keyword)
    - Property Gender: enum stored as int
    - Property SellingPrice, CostPrice: HasColumnType("decimal(18,2)")
    - Property Barcode: MaxLength(13), IsRequired(false)
    - HasIndex(x => x.Barcode).IsUnique().HasFilter("[Barcode] IS NOT NULL") -- CRITICAL: unique filtered index per RESEARCH.md pitfall 2
    - Property StockQuantity: default 0
    - Property MinStockLevel: default 2
    - Property IsActive: default true
    - HasQueryFilter(x => x.BranchId == ...) -- follow established BranchId query filter pattern

    Follow SupplierConfiguration pattern from Pharmacy module exactly.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Infrastructure/Optical.Infrastructure.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Frame configuration has unique filtered barcode index and all column constraints</done>
</task>

<task type="auto">
  <name>Task 2: Create LensCatalogItem and LensOrder EF Core configurations</name>
  <files>
    backend/src/Modules/Optical/Optical.Infrastructure/Configurations/LensCatalogItemConfiguration.cs,
    backend/src/Modules/Optical/Optical.Infrastructure/Configurations/LensOrderConfiguration.cs
  </files>
  <action>
    1. LensCatalogItemConfiguration : IEntityTypeConfiguration<LensCatalogItem>:
       - ToTable("LensCatalogItems")
       - HasKey(x => x.Id)
       - Property Brand: MaxLength(100), IsRequired
       - Property Name: MaxLength(200), IsRequired
       - Property LensType: MaxLength(50), IsRequired
       - Property Material: enum as int
       - Property AvailableCoatings: enum as int (flags are stored as combined int)
       - Property SellingPrice, CostPrice: decimal(18,2)
       - Property IsActive: default true
       - HasMany(x => x.StockEntries).WithOne().HasForeignKey(x => x.LensCatalogItemId)
       - Navigation StockEntries via backing field "_stockEntries": builder.Navigation(x => x.StockEntries).HasField("_stockEntries")
       - Configure LensStockEntry: ToTable("LensStockEntries"), properties Sph/Cyl/Add as decimal(5,2), unique index on (LensCatalogItemId, Sph, Cyl, Add) to prevent duplicate power entries

    2. LensOrderConfiguration : IEntityTypeConfiguration<LensOrder>:
       - ToTable("LensOrders")
       - HasKey(x => x.Id)
       - Properties Sph, Cyl, Axis: decimal(5,2), Add: decimal(5,2) nullable
       - Property RequestedCoatings: int (flags)
       - Property Status: MaxLength(20), IsRequired
       - Property Notes: MaxLength(500)
       - HasIndex on GlassesOrderId

    Follow established Pharmacy configuration patterns.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Infrastructure/Optical.Infrastructure.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>LensCatalogItem config uses backing field for StockEntries. LensStockEntry has unique power combo index.</done>
</task>

</tasks>

<verification>
All 3 configuration files exist and Optical.Infrastructure builds.
</verification>

<success_criteria>
- Frame.Barcode has unique filtered index (non-null)
- LensStockEntry has unique index on (CatalogItemId, Sph, Cyl, Add)
- All decimal columns use proper precision
- Optical.Infrastructure compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-09-SUMMARY.md`
</output>
