---
phase: 08-optical-center
plan: 10
type: execute
wave: 4
depends_on: [08-07, 08-08]
files_modified:
  - backend/src/Modules/Optical/Optical.Infrastructure/Configurations/GlassesOrderConfiguration.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Configurations/ComboPackageConfiguration.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Configurations/WarrantyClaimConfiguration.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Configurations/StocktakingSessionConfiguration.cs
autonomous: true
requirements: [OPT-03, OPT-06, OPT-07, OPT-09]

must_haves:
  truths:
    - "GlassesOrder configuration maps child items via backing field"
    - "StocktakingSession configuration has unique index on (SessionId, Barcode)"
    - "WarrantyClaim stores DocumentUrls as JSON column"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Configurations/GlassesOrderConfiguration.cs"
      provides: "EF config for order aggregate with items collection"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Configurations/ComboPackageConfiguration.cs"
      provides: "EF config for preset combo packages"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Configurations/WarrantyClaimConfiguration.cs"
      provides: "EF config for warranty claims with JSON document URLs"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Configurations/StocktakingSessionConfiguration.cs"
      provides: "EF config for stocktaking with items collection"
  key_links:
    - from: "StocktakingSessionConfiguration.cs"
      to: "StocktakingItem"
      via: "unique index on (SessionId, Barcode)"
      pattern: "HasIndex.*Barcode.*IsUnique"
---

<objective>
Create EF Core configurations for GlassesOrder, ComboPackage, WarrantyClaim, and StocktakingSession entities.

Purpose: Complete the infrastructure configurations for all remaining domain entities. GlassesOrder has child items, StocktakingSession has child items with unique barcode constraint, WarrantyClaim stores document URLs as JSON.
Output: 4 EF Core configuration files
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GlassesOrder and ComboPackage configurations</name>
  <files>
    backend/src/Modules/Optical/Optical.Infrastructure/Configurations/GlassesOrderConfiguration.cs,
    backend/src/Modules/Optical/Optical.Infrastructure/Configurations/ComboPackageConfiguration.cs
  </files>
  <action>
    1. GlassesOrderConfiguration : IEntityTypeConfiguration<GlassesOrder>:
       - ToTable("GlassesOrders")
       - HasKey(x => x.Id)
       - Property PatientName: MaxLength(200)
       - Property Status: enum as int
       - Property ProcessingType: enum as int
       - Property TotalPrice: decimal(18,2)
       - Property Notes: MaxLength(500)
       - HasMany(x => x.Items).WithOne().HasForeignKey(x => x.GlassesOrderId)
       - Navigation Items via backing field "_items"
       - Configure GlassesOrderItem as owned/child: ToTable("GlassesOrderItems"), Description MaxLength(300), UnitPrice decimal(18,2), Ignore LineTotal (computed)
       - HasIndex on PatientId, VisitId

    2. ComboPackageConfiguration : IEntityTypeConfiguration<ComboPackage>:
       - ToTable("ComboPackages")
       - HasKey(x => x.Id)
       - Property Name: MaxLength(200), IsRequired
       - Property Description: MaxLength(500)
       - Property ComboPrice, OriginalTotalPrice: decimal(18,2)
       - Property IsActive: default true

    Follow established Pharmacy patterns.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Infrastructure/Optical.Infrastructure.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>GlassesOrder maps Items child collection via backing field. ComboPackage has pricing columns.</done>
</task>

<task type="auto">
  <name>Task 2: Create WarrantyClaim and StocktakingSession configurations</name>
  <files>
    backend/src/Modules/Optical/Optical.Infrastructure/Configurations/WarrantyClaimConfiguration.cs,
    backend/src/Modules/Optical/Optical.Infrastructure/Configurations/StocktakingSessionConfiguration.cs
  </files>
  <action>
    1. WarrantyClaimConfiguration : IEntityTypeConfiguration<WarrantyClaim>:
       - ToTable("WarrantyClaims")
       - HasKey(x => x.Id)
       - Property Resolution: enum as int
       - Property ApprovalStatus: enum as int
       - Property AssessmentNotes: MaxLength(2000)
       - Property DiscountAmount: decimal(18,2), nullable
       - Property DocumentUrls: store as JSON using HasConversion with JSON serializer (EF Core 10 native JSON or manual converter). Use HasColumnType("nvarchar(max)"). Convert List<string> to/from JSON string.
       - HasIndex on GlassesOrderId
       - Ignore RequiresApproval (computed property)

    2. StocktakingSessionConfiguration : IEntityTypeConfiguration<StocktakingSession>:
       - ToTable("StocktakingSessions")
       - HasKey(x => x.Id)
       - Property Name: MaxLength(200), IsRequired
       - Property Status: enum as int
       - Property Notes: MaxLength(500)
       - HasMany(x => x.Items).WithOne().HasForeignKey(x => x.StocktakingSessionId)
       - Navigation Items via backing field "_items"
       - Configure StocktakingItem: ToTable("StocktakingItems"), Barcode MaxLength(13), FrameName MaxLength(300)
       - CRITICAL: HasIndex on StocktakingItem (StocktakingSessionId, Barcode).IsUnique() -- prevents duplicate barcode scans per session (RESEARCH.md pitfall 5)
       - Ignore Discrepancy on StocktakingItem (computed), Ignore TotalItemsScanned/DiscrepancyCount on Session (computed)

    Follow established patterns. Use System.Text.Json for DocumentUrls conversion.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Infrastructure/Optical.Infrastructure.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>WarrantyClaim stores DocumentUrls as JSON. StocktakingItem has unique (SessionId, Barcode) index.</done>
</task>

</tasks>

<verification>
All 4 configuration files exist and Optical.Infrastructure builds.
</verification>

<success_criteria>
- GlassesOrder items use backing field navigation
- WarrantyClaim DocumentUrls stored as JSON nvarchar(max)
- StocktakingItem has unique (SessionId, Barcode) constraint
- Optical.Infrastructure compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-10-SUMMARY.md`
</output>
