---
phase: 08-optical-center
plan: 14
type: execute
wave: 6
depends_on: [08-12]
files_modified:
  - backend/src/Modules/Optical/Optical.Infrastructure/Repositories/GlassesOrderRepository.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Repositories/ComboPackageRepository.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Repositories/WarrantyClaimRepository.cs
  - backend/src/Modules/Optical/Optical.Infrastructure/Repositories/StocktakingRepository.cs
autonomous: true
requirements: [OPT-03, OPT-06, OPT-07, OPT-09]

must_haves:
  truths:
    - "GlassesOrderRepository returns overdue orders"
    - "StocktakingRepository returns current in-progress session"
    - "All repositories eagerly load child collections"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Repositories/GlassesOrderRepository.cs"
      provides: "Order data access with status filter and overdue query"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Repositories/ComboPackageRepository.cs"
      provides: "Combo package data access"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Repositories/WarrantyClaimRepository.cs"
      provides: "Warranty claim data access with approval filter"
    - path: "backend/src/Modules/Optical/Optical.Infrastructure/Repositories/StocktakingRepository.cs"
      provides: "Stocktaking data access with current session query"
  key_links:
    - from: "GlassesOrderRepository.cs"
      to: "OpticalDbContext"
      via: "constructor injection"
      pattern: "OpticalDbContext"
---

<objective>
Implement remaining repositories for GlassesOrder, ComboPackage, WarrantyClaim, and Stocktaking.

Purpose: Complete data access layer for all optical entities. GlassesOrderRepository includes overdue order detection, StocktakingRepository includes current session lookup.
Output: 4 repository implementation files
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement GlassesOrder and ComboPackage repositories</name>
  <files>
    backend/src/Modules/Optical/Optical.Infrastructure/Repositories/GlassesOrderRepository.cs,
    backend/src/Modules/Optical/Optical.Infrastructure/Repositories/ComboPackageRepository.cs
  </files>
  <action>
    1. GlassesOrderRepository : IGlassesOrderRepository:
       - GetByIdAsync: Include(x => x.Items), FirstOrDefaultAsync
       - GetAllAsync: optional status filter, pagination (Skip/Take), order by CreatedAt desc, Include Items
       - GetByPatientIdAsync: filter by PatientId, Include Items, order by CreatedAt desc
       - GetByVisitIdAsync: filter by VisitId, Include Items
       - GetOverdueOrdersAsync: Where(x => x.EstimatedDeliveryDate != null && x.EstimatedDeliveryDate < DateTime.UtcNow && x.Status != GlassesOrderStatus.Delivered), Include Items
       - GetTotalCountAsync: same filters as GetAllAsync
       - Add: _context.GlassesOrders.Add(order)

    2. ComboPackageRepository : IComboPackageRepository:
       - GetByIdAsync: FirstOrDefaultAsync
       - GetAllAsync: filter by active if specified, order by Name
       - Add: _context.ComboPackages.Add(package)

    Follow Pharmacy repository patterns.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Infrastructure/Optical.Infrastructure.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>GlassesOrderRepository implements overdue detection. ComboPackageRepository has active filter.</done>
</task>

<task type="auto">
  <name>Task 2: Implement WarrantyClaim and Stocktaking repositories</name>
  <files>
    backend/src/Modules/Optical/Optical.Infrastructure/Repositories/WarrantyClaimRepository.cs,
    backend/src/Modules/Optical/Optical.Infrastructure/Repositories/StocktakingRepository.cs
  </files>
  <action>
    1. WarrantyClaimRepository : IWarrantyClaimRepository:
       - GetByIdAsync: FirstOrDefaultAsync
       - GetByOrderIdAsync: Where GlassesOrderId, order by ClaimDate desc
       - GetAllAsync: optional ApprovalStatus filter, pagination, order by CreatedAt desc
       - GetTotalCountAsync: same filters
       - Add: _context.WarrantyClaims.Add(claim)

    2. StocktakingRepository : IStocktakingRepository:
       - GetByIdAsync: Include(x => x.Items), FirstOrDefaultAsync
       - GetCurrentSessionAsync: Where Status == InProgress, Include Items, FirstOrDefaultAsync, order by CreatedAt desc
       - GetAllAsync: pagination, order by CreatedAt desc, Include Items
       - GetTotalCountAsync: count all
       - Add: _context.StocktakingSessions.Add(session)

    Follow Pharmacy repository patterns.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Infrastructure/Optical.Infrastructure.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>WarrantyClaimRepository has approval status filter. StocktakingRepository returns current in-progress session.</done>
</task>

</tasks>

<verification>
All 4 repository files exist and Optical.Infrastructure builds.
</verification>

<success_criteria>
- GlassesOrderRepository.GetOverdueOrdersAsync compares EstimatedDeliveryDate with UtcNow
- StocktakingRepository.GetCurrentSessionAsync returns active InProgress session
- All repositories eagerly load child collections where applicable
- Optical.Infrastructure compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-14-SUMMARY.md`
</output>
