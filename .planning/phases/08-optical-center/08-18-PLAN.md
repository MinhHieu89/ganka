---
phase: 08-optical-center
plan: 18
type: tdd
wave: 7
depends_on: [08-14, 08-15]
files_modified:
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/CreateGlassesOrder.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/UpdateOrderStatus.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/GetGlassesOrders.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/GetGlassesOrderById.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/GetOverdueOrders.cs
autonomous: true
requirements: [OPT-03, OPT-04]

must_haves:
  truths:
    - "Staff can create glasses orders linked to optical prescriptions"
    - "System blocks Processing status until payment is confirmed (OPT-04)"
    - "System tracks overdue orders past estimated delivery date"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Orders/CreateGlassesOrder.cs"
      provides: "Handler for creating glasses order from optical Rx"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Orders/UpdateOrderStatus.cs"
      provides: "Handler for status transitions with payment gate enforcement"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Orders/GetGlassesOrders.cs"
      provides: "Handler for paginated order list"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Orders/GetGlassesOrderById.cs"
      provides: "Handler for single order detail"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Orders/GetOverdueOrders.cs"
      provides: "Handler for overdue orders alert"
  key_links:
    - from: "UpdateOrderStatus.cs"
      to: "Billing.Contracts.Queries.GetVisitInvoiceQuery"
      via: "IMessageBus.InvokeAsync cross-module query"
      pattern: "GetVisitInvoiceQuery"
---

<objective>
Implement GlassesOrder handlers with TDD: CreateGlassesOrder, UpdateOrderStatus (with payment gate), GetGlassesOrders, GetGlassesOrderById, GetOverdueOrders.

Purpose: Enable glasses order lifecycle management with the critical OPT-04 payment enforcement -- system must check Billing module for full payment before allowing Processing status.
Output: 5 handler files + corresponding tests
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Billing.Contracts:
```csharp
public sealed record GetVisitInvoiceQuery(Guid VisitId);
public sealed record InvoiceDto(Guid Id, string InvoiceNumber, Guid? VisitId, Guid PatientId, string PatientName, int Status, decimal SubTotal, decimal DiscountTotal, decimal TotalAmount, decimal PaidAmount, decimal BalanceDue, ...);
```
</interfaces>
</context>

<feature>
  <name>GlassesOrder Lifecycle Handlers</name>
  <files>5 handler files</files>
  <behavior>
    CreateGlassesOrder:
    - Creates order with Ordered status
    - Links to patient, visit, optical prescription
    - Sets processing type and estimated delivery

    UpdateOrderStatus:
    - Transitions status via domain entity
    - CRITICAL: When transitioning to Processing, queries Billing via IMessageBus for payment confirmation
    - If invoice not found or BalanceDue > 0, returns validation error
    - Other transitions proceed without payment check

    GetGlassesOrders: Paginated list with status filter
    GetGlassesOrderById: Single order with items
    GetOverdueOrders: Orders past estimated delivery that aren't Delivered
  </behavior>
  <implementation>
    UpdateOrderStatus uses IMessageBus.InvokeAsync<InvoiceDto?>(new GetVisitInvoiceQuery(order.VisitId)) for payment check.
    This is a server-side check per RESEARCH.md pitfall 1 -- frontend check is UX only.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CreateGlassesOrder and UpdateOrderStatus with payment gate (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Orders/CreateGlassesOrder.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Orders/UpdateOrderStatus.cs
  </files>
  <behavior>
    - CreateGlassesOrder validates PatientId, VisitId, OpticalPrescriptionId, TotalPrice > 0
    - UpdateOrderStatus transitions status, blocks Ordered->Processing without payment
    - UpdateOrderStatus allows Processing->Received without payment check
  </behavior>
  <action>
    Write tests in Optical.Unit.Tests/Features/OrderHandlerTests.cs first (RED), then implement (GREEN).

    CreateGlassesOrder: Command(PatientId, PatientName, VisitId, OpticalPrescriptionId, ProcessingType (int), EstimatedDeliveryDate?, TotalPrice, ComboPackageId?, Notes, Items: List<OrderItemInput>). OrderItemInput record(FrameId?, LensCatalogItemId?, Description, UnitPrice, Quantity). Handler: validate, create GlassesOrder, add items, save, return id.

    UpdateOrderStatus: Command(OrderId, NewStatus (int)). Handler: get order by id, if transitioning to Processing -> query Billing via bus.InvokeAsync<InvoiceDto?>(new GetVisitInvoiceQuery(order.VisitId)). If invoice null or BalanceDue > 0, return Result.Failure("Payment must be completed before processing"). Otherwise call order.TransitionTo(newStatus). If transitioning to Processing, also call order.ConfirmPayment(). Save.

    IMPORTANT: Add project reference to Billing.Contracts in Optical.Application.csproj for GetVisitInvoiceQuery. Also need WolverineFx reference for IMessageBus (already in .csproj).

    Test the payment gate by mocking IMessageBus: when invoice has BalanceDue > 0, handler returns failure; when BalanceDue == 0, transition succeeds.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~OrderHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>UpdateOrderStatus blocks Processing without full payment, allows other transitions freely</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GetGlassesOrders, GetGlassesOrderById, GetOverdueOrders (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Orders/GetGlassesOrders.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Orders/GetGlassesOrderById.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Orders/GetOverdueOrders.cs
  </files>
  <behavior>
    - GetGlassesOrders returns paginated list with status filter
    - GetGlassesOrderById returns single order with items or NotFound
    - GetOverdueOrders returns orders past estimated delivery date
  </behavior>
  <action>
    Write tests first (RED), implement (GREEN).

    GetGlassesOrders: Query(int? StatusFilter, int Page, int PageSize). Maps to GlassesOrderSummaryDto list with TotalCount.
    GetGlassesOrderById: Query(Guid OrderId). Maps to full GlassesOrderDto with Items.
    GetOverdueOrders: Query(). Returns List<GlassesOrderSummaryDto> from repository.GetOverdueOrdersAsync.

    Map entities to DTOs inline. Compute IsOverdue and IsUnderWarranty from entity properties.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~OrderHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>All order query handlers pass tests: list, detail, overdue detection</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~OrderHandler" passes all tests.
</verification>

<success_criteria>
- Payment gate blocks Processing when BalanceDue > 0 (OPT-04)
- Payment gate uses server-side Billing query (not stale frontend data)
- Overdue detection works correctly
- All handler tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-18-SUMMARY.md`
</output>
