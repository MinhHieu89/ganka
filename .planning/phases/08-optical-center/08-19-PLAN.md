---
phase: 08-optical-center
plan: 19
type: tdd
wave: 7
depends_on: [08-14, 08-15]
files_modified:
  - backend/src/Modules/Optical/Optical.Application/Features/Combos/CreateComboPackage.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Combos/UpdateComboPackage.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Combos/GetComboPackages.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Warranty/CreateWarrantyClaim.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Warranty/ApproveWarrantyClaim.cs
autonomous: true
requirements: [OPT-06, OPT-07]

must_haves:
  truths:
    - "Staff can create preset combo packages with frame + lens combination pricing"
    - "Staff can file warranty claims with resolution type selection"
    - "Manager approval required for Replace resolution only"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Combos/CreateComboPackage.cs"
      provides: "Handler for creating preset combo packages"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Combos/UpdateComboPackage.cs"
      provides: "Handler for updating combo packages"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Combos/GetComboPackages.cs"
      provides: "Handler for listing combo packages"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Warranty/CreateWarrantyClaim.cs"
      provides: "Handler for filing warranty claims"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Warranty/ApproveWarrantyClaim.cs"
      provides: "Handler for manager approval/rejection of warranty claims"
  key_links:
    - from: "CreateWarrantyClaim.cs"
      to: "IGlassesOrderRepository"
      via: "checks IsUnderWarranty before allowing claim"
      pattern: "IsUnderWarranty"
---

<objective>
Implement Combo package CRUD and Warranty claim handlers with TDD.

Purpose: Enable preset combo pricing for frame+lens packages and warranty claim management with manager approval workflow for replacements.
Output: 5 handler files + corresponding tests
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<feature>
  <name>Combo and Warranty Handlers</name>
  <files>5 handler files</files>
  <behavior>
    CreateComboPackage: Creates preset combo with name, frame, lens, combo price
    UpdateComboPackage: Updates existing combo
    GetComboPackages: Lists active combo packages
    CreateWarrantyClaim: Files claim, validates order is under warranty (12 months from delivery)
    ApproveWarrantyClaim: Manager approves/rejects, only applicable for Replace resolution
  </behavior>
  <implementation>
    CreateWarrantyClaim checks GlassesOrder.IsUnderWarranty before allowing claim.
    For Replace resolution, claim starts as Pending and requires ApproveWarrantyClaim.
    For Repair/Discount, claim is auto-approved (no manager needed).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Combo package handlers (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Combos/CreateComboPackage.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Combos/UpdateComboPackage.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Combos/GetComboPackages.cs
  </files>
  <behavior>
    - Create validates Name required, ComboPrice > 0
    - Update finds package, validates, updates
    - Get returns active packages with frame/lens names
  </behavior>
  <action>
    Write tests in ComboHandlerTests.cs first (RED), then implement (GREEN).

    CreateComboPackage: Command(Name, Description?, FrameId?, LensCatalogItemId?, ComboPrice, OriginalTotalPrice?). Validator: Name NotEmpty MaxLength(200), ComboPrice > 0. Handler: validate, create via factory, save, return id.

    UpdateComboPackage: Command(Id, Name, Description?, FrameId?, LensCatalogItemId?, ComboPrice, OriginalTotalPrice?). Handler: get by id, validate, update, save.

    GetComboPackages: Query(bool IncludeInactive). Handler: get all, map to ComboPackageDto list. For frame/lens names, look up Frame/LensCatalogItem by id if FrameId/LensCatalogItemId is set.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~ComboHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>Combo package CRUD handlers pass all tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Warranty claim handlers (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Warranty/CreateWarrantyClaim.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Warranty/ApproveWarrantyClaim.cs
  </files>
  <behavior>
    - CreateWarrantyClaim validates order exists and IsUnderWarranty
    - Replace resolution sets ApprovalStatus=Pending, Repair/Discount auto-approves
    - ApproveWarrantyClaim accepts or rejects, only for Replace claims
  </behavior>
  <action>
    Write tests in WarrantyHandlerTests.cs first (RED), then implement (GREEN).

    CreateWarrantyClaim: Command(GlassesOrderId, Resolution (int), AssessmentNotes, DiscountAmount?). Handler: get order by id, check IsUnderWarranty (return error if not), create claim. If resolution is Replace, set ApprovalStatus=Pending. If Repair or Discount, auto-approve (ApprovalStatus=Approved). Save, return id.

    ApproveWarrantyClaim: Command(ClaimId, Approve (bool), Reason?). Handler: get claim by id. If not Replace resolution, return error (only replacements need approval). If Approve, call claim.Approve(currentUserId). If !Approve, call claim.Reject(currentUserId, reason). Save.

    Test: claim for expired warranty returns error. Replace claim starts Pending. Repair claim auto-approves. Approve transitions Pending to Approved.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~WarrantyHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>Warranty claims validated against 12-month warranty. Replace requires manager approval.</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~Combo|Warranty" passes all tests.
</verification>

<success_criteria>
- Combo package CRUD with pricing validation
- Warranty claim validates 12-month warranty window from DeliveredAt
- Replace resolution starts Pending, Repair/Discount auto-approved
- Manager approve/reject only for Replace claims
- All handler tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-19-SUMMARY.md`
</output>
