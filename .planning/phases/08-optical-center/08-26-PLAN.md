---
phase: 08-optical-center
plan: 26
type: execute
wave: 11
depends_on: [08-25]
files_modified:
  - frontend/src/features/optical/components/BarcodeScannerInput.tsx
  - frontend/src/features/optical/components/CameraScanner.tsx
  - frontend/src/features/optical/components/BarcodeDisplay.tsx
autonomous: true
requirements: [OPT-01, OPT-09]

must_haves:
  truths:
    - "USB barcode scanner input works as keyboard input to focused field"
    - "Camera scanner uses html5-qrcode for mobile barcode scanning"
    - "Barcode display renders EAN-13 barcodes using JsBarcode"
  artifacts:
    - path: "frontend/src/features/optical/components/BarcodeScannerInput.tsx"
      provides: "USB scanner keyboard input component with Enter key handling"
    - path: "frontend/src/features/optical/components/CameraScanner.tsx"
      provides: "html5-qrcode camera fallback for mobile stocktaking"
    - path: "frontend/src/features/optical/components/BarcodeDisplay.tsx"
      provides: "JsBarcode EAN-13 rendering to SVG"
  key_links:
    - from: "BarcodeScannerInput.tsx"
      to: "shadcn Input"
      via: "wrapper around Input component"
      pattern: "import.*Input"
---

<objective>
Create shared barcode components: USB scanner input, camera scanner, and barcode display.

Purpose: These three reusable components handle all barcode interactions -- USB scanner as keyboard input (primary), camera scanning via html5-qrcode (fallback for mobile), and EAN-13 rendering with JsBarcode. Used by frame catalog, stocktaking, and other pages.
Output: 3 barcode component files
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BarcodeScannerInput and BarcodeDisplay components</name>
  <files>
    frontend/src/features/optical/components/BarcodeScannerInput.tsx,
    frontend/src/features/optical/components/BarcodeDisplay.tsx
  </files>
  <action>
    1. BarcodeScannerInput.tsx:
       Props: { onScan: (barcode: string) => void, autoFocus?: boolean, className?: string }

       USB barcode scanners act like keyboards -- they type digits into the focused input field and press Enter. Component:
       - Uses shadcn/ui Input component as base
       - useRef for input element
       - onKeyDown handler: when Enter pressed, read input value, validate as 13-digit number (/^\d{13}$/), call onScan(value), clear input
       - Auto-focus on mount if autoFocus prop
       - Show scan icon (IconBarcode from @tabler/icons-react) as input prefix

       Style as a prominent input field with "Scan barcode..." instruction text (no placeholder per CLAUDE.md unless it makes sense -- this is a case where placeholder makes sense to indicate scanning).

    2. BarcodeDisplay.tsx:
       Props: { value: string, width?: number, height?: number, showText?: boolean }

       - Uses JsBarcode to render EAN-13 barcode to SVG
       - useRef for SVG element
       - useEffect: when value changes, call JsBarcode(svgRef, value, { format: 'EAN13', width: width ?? 2, height: height ?? 60, displayValue: showText ?? true, fontSize: 14, margin: 10 })
       - Gracefully handle invalid barcodes (try/catch around JsBarcode call)
       - Show fallback text if barcode is invalid

    Both components use shadcn/ui styling patterns and proper TypeScript types.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>BarcodeScannerInput handles USB scanner Enter key + validates EAN-13. BarcodeDisplay renders JsBarcode SVG.</done>
</task>

<task type="auto">
  <name>Task 2: Create CameraScanner component</name>
  <files>frontend/src/features/optical/components/CameraScanner.tsx</files>
  <action>
    CameraScanner.tsx:
    Props: { onScan: (barcode: string) => void, onError?: (error: string) => void, isActive?: boolean }

    - Uses html5-qrcode Html5QrcodeScanner
    - useEffect: when isActive, create scanner targeting a div element with id
    - Configure: fps: 10, qrbox: { width: 300, height: 100 }, formatsToSupport: [Html5QrcodeSupportedFormats.EAN_13]
    - On successful scan: call onScan(decodedText), then scanner.clear()
    - On error: call onError if provided
    - Cleanup: scanner.clear() in useEffect cleanup
    - Use useRef to track scanner instance for cleanup
    - Wrap in a Card component from shadcn/ui with a toggle button to activate/deactivate camera
    - Show instructions: "Point camera at barcode"

    Note: html5-qrcode injects its own UI into the target div. Style the container to fit within the page layout. Use a min-height to prevent layout shifts.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>CameraScanner uses html5-qrcode with EAN-13 format, cleanup on unmount, toggle activation</done>
</task>

</tasks>

<verification>
All 3 barcode components compile and export properly.
</verification>

<success_criteria>
- BarcodeScannerInput validates 13-digit EAN-13 on Enter key
- BarcodeDisplay renders barcode SVG with JsBarcode
- CameraScanner uses html5-qrcode with EAN-13 format filter
- All components follow shadcn/ui styling
- Frontend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-26-SUMMARY.md`
</output>
