---
phase: 08-optical-center
plan: 34
type: execute
wave: 11
depends_on: [08-25, 08-26]
files_modified:
  - frontend/src/features/optical/components/StocktakingPage.tsx
  - frontend/src/features/optical/components/StocktakingScanner.tsx
  - frontend/src/features/optical/components/DiscrepancyReport.tsx
  - frontend/src/app/routes/_authenticated/optical/stocktaking.tsx
autonomous: true
requirements: [OPT-09]

must_haves:
  truths:
    - "Staff can start stocktaking session, scan barcodes, and record physical counts"
    - "Discrepancy report shows over/under/missing inventory items"
    - "Both USB scanner and camera scanner supported"
  artifacts:
    - path: "frontend/src/features/optical/components/StocktakingPage.tsx"
      provides: "Stocktaking session management page"
    - path: "frontend/src/features/optical/components/StocktakingScanner.tsx"
      provides: "Scanning interface with USB and camera input"
    - path: "frontend/src/features/optical/components/DiscrepancyReport.tsx"
      provides: "Discrepancy report with over/under/missing categories"
    - path: "frontend/src/app/routes/_authenticated/optical/stocktaking.tsx"
      provides: "TanStack Router route file"
  key_links:
    - from: "StocktakingScanner.tsx"
      to: "BarcodeScannerInput.tsx"
      via: "import USB scanner component"
      pattern: "BarcodeScannerInput"
    - from: "StocktakingScanner.tsx"
      to: "CameraScanner.tsx"
      via: "import camera scanner component"
      pattern: "CameraScanner"
---

<objective>
Create the Stocktaking frontend: session page, scanning interface, discrepancy report, and route.

Purpose: Enable staff to perform barcode-based inventory counts. Supports both USB scanner (primary) and phone camera (fallback). Generates discrepancy reports comparing physical vs system counts.
Output: 3 component files + 1 route file
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StocktakingScanner and DiscrepancyReport</name>
  <files>
    frontend/src/features/optical/components/StocktakingScanner.tsx,
    frontend/src/features/optical/components/DiscrepancyReport.tsx
  </files>
  <action>
    1. StocktakingScanner.tsx:
       Props: { sessionId: string, onItemRecorded?: (item: StocktakingItemDto) => void }

       - Tab toggle: "USB Scanner" (default) | "Camera Scanner"
       - USB tab: BarcodeScannerInput component + physical count number input
       - Camera tab: CameraScanner component + physical count number input
       - Flow: scan barcode -> enter physical count -> submit
       - On scan: auto-focus physical count input, pre-fill barcode
       - Submit calls useRecordStocktakingItem mutation
       - Show last 10 scanned items as a running list below scanner
       - Each scanned item shows: Barcode, Frame Name (or "Unknown"), Physical Count, System Count, Discrepancy (color-coded)
       - Uses shadcn/ui Tabs, Input, Button, Card components

    2. DiscrepancyReport.tsx:
       Props: { sessionId: string }

       - Uses useDiscrepancyReport(sessionId) hook
       - Summary cards at top:
         - Total Items Scanned (number)
         - Matches (Physical == System, green)
         - Over Count (Physical > System, yellow)
         - Under Count (Physical < System, red)
         - Missing from System (barcode not in inventory, gray)
       - Detailed table: Barcode, Frame Name, Physical Count, System Count, Discrepancy (+/- number), Category (Match/Over/Under/Missing)
       - Sortable by discrepancy magnitude
       - Color-coded rows: green (match), yellow (over), red (under), gray (missing)
       - Print report button (opens print dialog)
       - Uses shadcn/ui Card, Table, Badge, Button components
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Scanner supports USB and camera with running scan list. Report categorizes discrepancies.</done>
</task>

<task type="auto">
  <name>Task 2: Create StocktakingPage and route</name>
  <files>
    frontend/src/features/optical/components/StocktakingPage.tsx,
    frontend/src/app/routes/_authenticated/optical/stocktaking.tsx
  </files>
  <action>
    1. StocktakingPage.tsx:
       - Two modes: "Session List" and "Active Session"
       - Session List mode:
         - Table of past stocktaking sessions: Name, Status, Started By, Date, Items Scanned, Discrepancies, Actions (View Report)
         - "Start New Stocktaking" button (opens dialog to enter session name)
         - Disabled if an InProgress session already exists
       - Active Session mode (when InProgress session exists):
         - Session header with name and "Complete" / "Cancel" buttons
         - StocktakingScanner component for scanning
         - Live count: "X items scanned, Y discrepancies found"
         - Complete button triggers confirmation dialog then calls useCompleteStocktaking
         - After completing, auto-navigates to DiscrepancyReport view
       - View Report mode: DiscrepancyReport for completed session
       - Uses useStocktakingSessions and useCurrentStocktakingSession hooks

    2. stocktaking.tsx route file:
       - createFileRoute('/_authenticated/optical/stocktaking')
       - component: StocktakingPage
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Stocktaking page supports full workflow: start, scan, complete, view report. Route at /optical/stocktaking.</done>
</task>

</tasks>

<verification>
All 4 files compile. Route accessible at /optical/stocktaking.
</verification>

<success_criteria>
- Both USB and camera scanner modes available
- Running scan list shows last 10 items with discrepancies
- Discrepancy report categorizes over/under/missing
- Session prevents concurrent stocktaking
- Complete triggers report generation
- Route accessible at /optical/stocktaking
- Frontend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-34-SUMMARY.md`
</output>
