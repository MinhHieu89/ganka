---
phase: 08-optical-center
plan: 29
type: execute
wave: 11
depends_on: [08-25]
files_modified:
  - frontend/src/features/optical/components/GlassesOrdersPage.tsx
  - frontend/src/features/optical/components/GlassesOrderTable.tsx
  - frontend/src/features/optical/components/CreateGlassesOrderForm.tsx
  - frontend/src/features/optical/components/OrderStatusBadge.tsx
  - frontend/src/app/routes/_authenticated/optical/orders.tsx
autonomous: true
requirements: [OPT-03, OPT-04]

must_haves:
  truths:
    - "Staff can view glasses orders in a filterable table with status badges"
    - "Staff can create new orders linked to optical prescriptions"
    - "Overdue orders are highlighted with alert"
  artifacts:
    - path: "frontend/src/features/optical/components/GlassesOrdersPage.tsx"
      provides: "Orders list page with overdue alert banner"
    - path: "frontend/src/features/optical/components/GlassesOrderTable.tsx"
      provides: "DataTable for orders with status filter"
    - path: "frontend/src/features/optical/components/CreateGlassesOrderForm.tsx"
      provides: "Form for creating glasses orders"
    - path: "frontend/src/features/optical/components/OrderStatusBadge.tsx"
      provides: "Color-coded status badge component"
    - path: "frontend/src/app/routes/_authenticated/optical/orders.tsx"
      provides: "TanStack Router route file"
  key_links:
    - from: "GlassesOrdersPage.tsx"
      to: "optical-queries.ts"
      via: "useGlassesOrders, useOverdueOrders hooks"
      pattern: "useGlassesOrders|useOverdueOrders"
---

<objective>
Create the Glasses Orders frontend: list page, order table, create form, status badge, and route.

Purpose: Enable staff to manage glasses orders with lifecycle tracking, overdue alerts, and payment status visibility. Orders are created from optical prescriptions.
Output: 4 component files + 1 route file
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OrderStatusBadge and GlassesOrderTable</name>
  <files>
    frontend/src/features/optical/components/OrderStatusBadge.tsx,
    frontend/src/features/optical/components/GlassesOrderTable.tsx
  </files>
  <action>
    1. OrderStatusBadge.tsx:
       Props: { status: number }
       - Maps status enum to label and color:
         0 (Ordered) -> blue badge "Ordered"
         1 (Processing) -> yellow badge "Processing"
         2 (Received) -> purple badge "Received"
         3 (Ready) -> green badge "Ready for Pickup"
         4 (Delivered) -> gray badge "Delivered"
       - Uses shadcn/ui Badge component with variant colors
       - Also accepts isOverdue and isPaymentConfirmed props for additional indicators

    2. GlassesOrderTable.tsx:
       - DataTable with columns: Order #, Patient Name, Status (OrderStatusBadge), Processing Type (InHouse/Outsourced), Total Price (VND), Payment (confirmed/pending badge), Est. Delivery, Created At, Actions
       - Status filter dropdown (Ordered/Processing/Received/Ready/Delivered/All)
       - Overdue orders highlighted with warning row style
       - Payment status shown as green check or red warning icon
       - Click row to navigate to order detail
       - Pagination
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>OrderStatusBadge shows color-coded status. GlassesOrderTable has status filter and overdue highlighting.</done>
</task>

<task type="auto">
  <name>Task 2: Create GlassesOrdersPage, CreateGlassesOrderForm, and route</name>
  <files>
    frontend/src/features/optical/components/GlassesOrdersPage.tsx,
    frontend/src/features/optical/components/CreateGlassesOrderForm.tsx,
    frontend/src/app/routes/_authenticated/optical/orders.tsx
  </files>
  <action>
    1. GlassesOrdersPage.tsx:
       - Page header: "Glasses Orders" with "New Order" button
       - Overdue orders alert banner at top (uses useOverdueOrders, show count)
       - GlassesOrderTable as main content
       - Uses useGlassesOrders hook with status filter state
       - Uses shadcn/ui Card, Alert components

    2. CreateGlassesOrderForm.tsx:
       - Dialog form for creating a new glasses order
       - Fields: Patient (search/select), Visit (select from patient visits), Optical Prescription (auto-populated from visit), Processing Type (InHouse/Outsourced radio), Estimated Delivery Date (date picker), Combo Package (optional select), Notes (textarea)
       - Items section: Frame (select from catalog with barcode scanner), Lens (select from catalog), custom line items
       - Total price auto-calculated from items
       - React Hook Form + Zod validation
       - Calls useCreateGlassesOrder mutation on submit

    3. orders.tsx route file:
       - createFileRoute('/_authenticated/optical/orders')
       - component: GlassesOrdersPage
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | tail -10</automated>
  </verify>
  <done>Orders page shows overdue alerts. Create form links to optical prescriptions.</done>
</task>

</tasks>

<verification>
All 5 files compile. Route accessible at /optical/orders.
</verification>

<success_criteria>
- Order table filterable by status with color-coded badges
- Overdue orders highlighted and counted in alert banner
- Payment status visible per order
- Create form links to optical prescription from visit
- Route accessible at /optical/orders
- Frontend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-29-SUMMARY.md`
</output>
