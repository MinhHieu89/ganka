---
phase: 08-optical-center
plan: 16
type: tdd
wave: 7
depends_on: [08-13]
files_modified:
  - backend/src/Modules/Optical/Optical.Application/Features/Frames/CreateFrame.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Frames/UpdateFrame.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Frames/GetFrames.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Frames/SearchFrames.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Frames/GenerateBarcode.cs
autonomous: true
requirements: [OPT-01]

must_haves:
  truths:
    - "Staff can create, update, and search frames with all catalog attributes"
    - "Staff can generate EAN-13 barcodes for untagged frames"
    - "Barcode uniqueness is validated before saving"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Frames/CreateFrame.cs"
      provides: "Command + Validator + Handler for frame creation with barcode uniqueness"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Frames/UpdateFrame.cs"
      provides: "Command + Validator + Handler for frame update"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Frames/GetFrames.cs"
      provides: "Query + Handler for paginated frame list"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Frames/SearchFrames.cs"
      provides: "Query + Handler for filtered frame search"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Frames/GenerateBarcode.cs"
      provides: "Command + Handler for auto-generating EAN-13 barcode"
  key_links:
    - from: "CreateFrame.cs"
      to: "IFrameRepository"
      via: "dependency injection in handler"
      pattern: "IFrameRepository"
---

<objective>
Implement Frame CRUD handlers with TDD: CreateFrame, UpdateFrame, GetFrames, SearchFrames, GenerateBarcode.

Purpose: Enable frame inventory management with full catalog attributes, search/filtering, and EAN-13 barcode generation for untagged frames.
Output: 5 handler files following vertical slice pattern
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Optical.Application.Interfaces:
```csharp
public interface IFrameRepository {
    Task<Frame?> GetByIdAsync(Guid id, CancellationToken ct);
    Task<Frame?> GetByBarcodeAsync(string barcode, CancellationToken ct);
    Task<List<Frame>> SearchAsync(string? searchTerm, int? material, int? frameType, int? gender, int page, int pageSize, CancellationToken ct);
    Task<int> GetTotalCountAsync(string? searchTerm, int? material, int? frameType, int? gender, CancellationToken ct);
    Task<long> GetNextSequenceNumberAsync(CancellationToken ct);
    Task<bool> IsBarcodeUniqueAsync(string barcode, Guid? excludeId, CancellationToken ct);
    void Add(Frame frame);
}
public interface IUnitOfWork { Task<int> SaveChangesAsync(CancellationToken ct); }
```
</interfaces>
</context>

<feature>
  <name>Frame CRUD Handlers</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Frames/CreateFrame.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Frames/UpdateFrame.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Frames/GetFrames.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Frames/SearchFrames.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Frames/GenerateBarcode.cs
  </files>
  <behavior>
    CreateFrame:
    - Valid command creates frame and returns Guid
    - Invalid barcode format returns validation error
    - Duplicate barcode returns error
    - Brand/Model required, LensWidth 40-65, BridgeWidth 12-24, TempleLength 120-155

    UpdateFrame:
    - Valid command updates all fields
    - Non-existent frame returns NotFound
    - Duplicate barcode (on different frame) returns error

    GetFrames:
    - Returns paginated list with total count
    - Supports includeInactive filter

    SearchFrames:
    - Filters by searchTerm, material, frameType, gender
    - Returns paginated results with total count

    GenerateBarcode:
    - Generates unique EAN-13 barcode using clinic prefix
    - Updates frame with generated barcode
  </behavior>
  <implementation>
    Follow Pharmacy.Application vertical slice pattern (Command record + Validator + static Handler).
    Each handler: validate -> execute -> save -> return Result<T>.
    Use FluentValidation with proper range validators for size fields.
    CreateFrame/UpdateFrame must check IsBarcodeUniqueAsync when barcode is provided.
    GenerateBarcode uses Ean13Generator.Generate(sequenceNumber) then saves to frame.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CreateFrame and UpdateFrame handlers (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Frames/CreateFrame.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Frames/UpdateFrame.cs
  </files>
  <behavior>
    - CreateFrame: validates all fields, checks barcode uniqueness, creates Frame via factory
    - UpdateFrame: finds frame by id, validates, checks barcode uniqueness (excluding self), updates
  </behavior>
  <action>
    Write tests in backend/tests/Optical.Unit.Tests/Features/FrameHandlerTests.cs first (RED), then implement handlers (GREEN).

    CreateFrame: Command record with all frame fields. Validator: Brand/Model NotEmpty MaxLength(100), Color NotEmpty MaxLength(50), LensWidth InclusiveBetween(40,65), BridgeWidth InclusiveBetween(12,24), TempleLength InclusiveBetween(120,155), SellingPrice/CostPrice > 0, Barcode optional Matches(@"^\d{13}$"). Handler: validate, check barcode uniqueness if provided, call Frame.Create, repository.Add, unitOfWork.SaveChanges, return frame.Id.

    UpdateFrame: Similar pattern. Handler: validate, get frame by id (return NotFound if null), check barcode uniqueness (exclude current id), call frame.Update, save.

    Use NSubstitute for IFrameRepository, IUnitOfWork, IValidator mocks.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~FrameHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>CreateFrame and UpdateFrame handlers pass all tests including validation and barcode uniqueness</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GetFrames, SearchFrames, and GenerateBarcode handlers (TDD)</name>
  <files>
    backend/src/Modules/Optical/Optical.Application/Features/Frames/GetFrames.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Frames/SearchFrames.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Frames/GenerateBarcode.cs
  </files>
  <behavior>
    - GetFrames: returns paginated FrameDto list with total
    - SearchFrames: returns filtered paginated FrameDto list
    - GenerateBarcode: generates and assigns EAN-13 to frame
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    GetFrames: Query record with (bool IncludeInactive, int Page = 1, int PageSize = 20). Handler: call repository.GetAllAsync, map to FrameDto list, return.

    SearchFrames: Query record with (string? SearchTerm, int? Material, int? FrameType, int? Gender, int Page = 1, int PageSize = 20). Handler: call repository.SearchAsync + GetTotalCountAsync, map to FrameDto list with total, return.

    GenerateBarcode: Command record with (Guid FrameId). Handler: get frame by id, get next sequence number, generate EAN-13 with Ean13Generator, check uniqueness, update frame barcode, save, return barcode string.

    Map entities to DTOs inline (no AutoMapper -- follow established project pattern).
  </action>
  <verify>
    <automated>dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~FrameHandler" --no-build -v q 2>&1 | tail -10</automated>
  </verify>
  <done>All frame handlers pass tests: CRUD, search, barcode generation</done>
</task>

</tasks>

<verification>
dotnet test backend/tests/Optical.Unit.Tests --filter "FullyQualifiedName~FrameHandler" passes all tests.
</verification>

<success_criteria>
- 5 handler files following vertical slice pattern
- FluentValidation with optical-specific range validators (size fields)
- Barcode uniqueness enforced on create and update
- GenerateBarcode uses Ean13Generator + uniqueness check
- All handler tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-16-SUMMARY.md`
</output>
