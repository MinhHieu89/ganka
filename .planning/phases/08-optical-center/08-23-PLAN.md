---
phase: 08-optical-center
plan: 23
type: execute
wave: 8
depends_on: [08-16, 08-17, 08-18, 08-19, 08-20, 08-21]
files_modified:
  - backend/src/Modules/Optical/Optical.Presentation/Optical.Presentation.csproj
  - backend/src/Modules/Optical/Optical.Presentation/IoC.cs
  - backend/src/Modules/Optical/Optical.Presentation/OpticalApiEndpoints.cs
  - backend/src/Modules/Optical/Optical.Presentation/WarrantyApiEndpoints.cs
  - backend/src/Modules/Optical/Optical.Presentation/StocktakingApiEndpoints.cs
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03, OPT-06, OPT-07, OPT-09]

must_haves:
  truths:
    - "All optical API endpoints are mapped with proper authentication and route groups"
    - "Optical.Presentation project exists with correct references"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Presentation/Optical.Presentation.csproj"
      provides: "Presentation project with references to Application and Shared.Presentation"
    - path: "backend/src/Modules/Optical/Optical.Presentation/IoC.cs"
      provides: "Placeholder IoC for presentation services"
    - path: "backend/src/Modules/Optical/Optical.Presentation/OpticalApiEndpoints.cs"
      provides: "Frame, Lens, Order, Combo API endpoints"
    - path: "backend/src/Modules/Optical/Optical.Presentation/WarrantyApiEndpoints.cs"
      provides: "Warranty claim API endpoints"
    - path: "backend/src/Modules/Optical/Optical.Presentation/StocktakingApiEndpoints.cs"
      provides: "Stocktaking API endpoints"
  key_links:
    - from: "OpticalApiEndpoints.cs"
      to: "Optical.Application handlers"
      via: "IMessageBus.InvokeAsync"
      pattern: "bus\\.InvokeAsync"
---

<objective>
Create Optical.Presentation project with all API endpoints.

Purpose: Expose all optical module functionality via Minimal API endpoints with proper authentication, route groups, and the established bus.InvokeAsync pattern.
Output: New Optical.Presentation project with 5 files
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-optical-center/08-RESEARCH.md

<interfaces>
From Pharmacy.Presentation.PharmacyApiEndpoints (pattern):
```csharp
public static class PharmacyApiEndpoints {
    public static void MapPharmacyApiEndpoints(this WebApplication app) {
        var group = app.MapGroup("/api/pharmacy").RequireAuthorization();
        group.MapGet("/drugs", async (IMessageBus bus, ...) => { ... });
        group.MapPost("/drugs", async (IMessageBus bus, CreateDrugCatalogItem.Command command) => { ... });
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Presentation project and IoC + OpticalApiEndpoints</name>
  <files>
    backend/src/Modules/Optical/Optical.Presentation/Optical.Presentation.csproj,
    backend/src/Modules/Optical/Optical.Presentation/IoC.cs,
    backend/src/Modules/Optical/Optical.Presentation/OpticalApiEndpoints.cs
  </files>
  <action>
    1. Create Optical.Presentation.csproj:
       - TargetFramework net10.0, ImplicitUsings enable, Nullable enable
       - PackageReference: WolverineFx.Http
       - ProjectReferences: Optical.Application, Optical.Contracts, Shared.Presentation
       - Add to solution: dotnet sln backend/Ganka28.sln add

    2. IoC.cs -- placeholder:
       namespace Optical.Presentation, public static class IoC
       - AddOpticalPresentation(this IServiceCollection services) { return services; }

    3. OpticalApiEndpoints.cs:
       namespace Optical.Presentation, public static class OpticalApiEndpoints
       - MapOpticalApiEndpoints(this WebApplication app):

       Frame routes (/api/optical/frames):
       - GET /frames -- GetFrames (paginated, query: includeInactive, page, pageSize)
       - GET /frames/search -- SearchFrames (query: searchTerm, material, frameType, gender, page, pageSize)
       - GET /frames/{id} -- get single frame by id
       - POST /frames -- CreateFrame
       - PUT /frames/{id} -- UpdateFrame
       - POST /frames/{id}/generate-barcode -- GenerateBarcode

       Lens routes (/api/optical/lenses):
       - GET /lenses -- GetLensCatalog (query: includeInactive)
       - POST /lenses -- CreateLensCatalogItem
       - PUT /lenses/{id} -- UpdateLensCatalogItem
       - POST /lenses/stock-adjust -- AdjustLensStock
       - GET /lenses/alerts -- GetLowLensStockAlerts

       Order routes (/api/optical/orders):
       - GET /orders -- GetGlassesOrders (query: statusFilter, page, pageSize)
       - GET /orders/{id} -- GetGlassesOrderById
       - GET /orders/overdue -- GetOverdueOrders
       - POST /orders -- CreateGlassesOrder
       - PUT /orders/{id}/status -- UpdateOrderStatus

       Combo routes (/api/optical/combos):
       - GET /combos -- GetComboPackages (query: includeInactive)
       - POST /combos -- CreateComboPackage
       - PUT /combos/{id} -- UpdateComboPackage

       Prescription routes (/api/optical/prescriptions):
       - GET /prescriptions/patient/{patientId} -- GetPatientPrescriptionHistory
       - GET /prescriptions/compare -- GetPrescriptionComparison (query: patientId, id1, id2)

       All routes use RequireAuthorization(). Use IMessageBus.InvokeAsync pattern. Map Result<T> to HTTP responses using ToHttpResult pattern from Shared.Presentation.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Presentation/Optical.Presentation.csproj -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Presentation project created with Frame, Lens, Order, Combo, Prescription endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Create Warranty and Stocktaking API endpoints</name>
  <files>
    backend/src/Modules/Optical/Optical.Presentation/WarrantyApiEndpoints.cs,
    backend/src/Modules/Optical/Optical.Presentation/StocktakingApiEndpoints.cs
  </files>
  <action>
    1. WarrantyApiEndpoints.cs:
       namespace Optical.Presentation, public static class WarrantyApiEndpoints
       - MapWarrantyApiEndpoints(this WebApplication app):

       Warranty routes (/api/optical/warranty):
       - GET /warranty -- GetWarrantyClaims (query: approvalStatusFilter, page, pageSize)
       - POST /warranty -- CreateWarrantyClaim
       - PUT /warranty/{id}/approve -- ApproveWarrantyClaim
       - POST /warranty/{id}/documents -- UploadWarrantyDocument (multipart file upload)

       For document upload, accept IFormFile and pass stream + filename to handler.

    2. StocktakingApiEndpoints.cs:
       namespace Optical.Presentation, public static class StocktakingApiEndpoints
       - MapStocktakingApiEndpoints(this WebApplication app):

       Stocktaking routes (/api/optical/stocktaking):
       - GET /stocktaking -- list sessions (paginated)
       - GET /stocktaking/{id} -- get session with items
       - GET /stocktaking/{id}/report -- GetDiscrepancyReport
       - POST /stocktaking -- StartStocktakingSession
       - POST /stocktaking/{id}/scan -- RecordStocktakingItem
       - PUT /stocktaking/{id}/complete -- CompleteStocktaking

       All routes use RequireAuthorization() and IMessageBus.InvokeAsync pattern.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Optical/Optical.Presentation/Optical.Presentation.csproj -v q 2>&1 | tail -5</automated>
  </verify>
  <done>Warranty and Stocktaking API endpoints created with proper route structure</done>
</task>

</tasks>

<verification>
All 5 files exist and Optical.Presentation project builds.
</verification>

<success_criteria>
- Optical.Presentation.csproj created and added to solution
- 25+ API endpoints covering all optical features
- All endpoints use RequireAuthorization()
- File upload endpoint for warranty documents
- Optical.Presentation compiles
</success_criteria>

<output>
After completion, create `.planning/phases/08-optical-center/08-23-SUMMARY.md`
</output>
