---
phase: 14-implement-receptionist-role-flow
plan: 04
type: execute
wave: 3
depends_on: [14-02, 14-03]
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/features/receptionist/types/receptionist.types.ts
  - frontend/src/features/receptionist/api/receptionist-api.ts
  - frontend/src/features/receptionist/hooks/useReceptionistPolling.ts
  - frontend/src/features/receptionist/schemas/intake-form.schema.ts
  - frontend/src/features/receptionist/schemas/booking.schema.ts
  - frontend/src/features/receptionist/components/StatusBadge.tsx
  - frontend/src/features/receptionist/components/SourceBadge.tsx
  - frontend/src/features/receptionist/components/KpiCards.tsx
  - frontend/src/features/receptionist/components/StatusFilterPills.tsx
  - frontend/src/features/receptionist/components/PatientQueueTable.tsx
  - frontend/src/features/receptionist/components/ReceptionistDashboard.tsx
  - frontend/src/app/routes/_authenticated/dashboard.tsx
autonomous: true
requirements: [RCP-01, RCP-02]

must_haves:
  truths:
    - "Receptionist sees a different dashboard than other roles at /dashboard (per D-01)"
    - "Dashboard shows 4 KPI cards with correct colors and 30s polling (per D-04)"
    - "Patient table polls every 15s with status filter pills (per D-03, D-04)"
    - "Table shows columns: STT, Ho ten, Nam sinh, Gio hen, Nguon, Ly do kham, Trang thai, Thao tac"
    - "Status badges use correct colors from UI-SPEC"
    - "Not-arrived rows have bg-secondary tint"
  artifacts:
    - path: "frontend/src/features/receptionist/api/receptionist-api.ts"
      provides: "API client with TanStack Query hooks for all receptionist endpoints"
    - path: "frontend/src/features/receptionist/components/ReceptionistDashboard.tsx"
      provides: "Main dashboard container with KPI + table + filters"
    - path: "frontend/src/features/receptionist/types/receptionist.types.ts"
      provides: "TypeScript types matching backend DTOs"
  key_links:
    - from: "dashboard.tsx"
      to: "ReceptionistDashboard.tsx"
      via: "Role check: user.roles.includes('Receptionist')"
      pattern: "Receptionist"
    - from: "receptionist-api.ts"
      to: "/api/scheduling/receptionist/dashboard"
      via: "TanStack Query with refetchInterval: 15000"
      pattern: "refetchInterval.*15"
---

<objective>
Build the frontend receptionist module foundation: CSS variables, types, API client, Zod schemas, and the complete dashboard (KPI cards, patient queue table with filters, polling, role-based rendering).

Purpose: Deliver the primary SCR-002a screen that receptionists interact with all day.
Output: Working receptionist dashboard at /dashboard for users with Receptionist role.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-implement-receptionist-role-flow/14-CONTEXT.md
@.planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
@.planning/phases/14-implement-receptionist-role-flow/14-RESEARCH.md
@.planning/phases/14-implement-receptionist-role-flow/14-01-SUMMARY.md
@.planning/phases/14-implement-receptionist-role-flow/14-02-SUMMARY.md

<interfaces>
From backend DTOs (plan 02):
```typescript
interface ReceptionistDashboardRow {
  id: string
  appointmentId: string | null
  visitId: string | null
  patientId: string | null
  patientName: string
  patientCode: string | null
  birthYear: number | null
  appointmentTime: string | null
  source: "appointment" | "walkin"
  reason: string | null
  status: "not_arrived" | "waiting" | "examining" | "completed"
  checkedInAt: string | null
  isGuestBooking: boolean
}

interface ReceptionistKpi {
  todayAppointments: number
  notArrived: number
  waiting: number
  examining: number
  completed: number
}
```

From authStore.ts (plan 01):
```typescript
export interface AuthUser {
  id: string; email: string; fullName: string;
  roles: string[]; permissions: string[]; preferredLanguage: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS variables + types + API client + schemas</name>
  <files>
    frontend/src/app/globals.css,
    frontend/src/features/receptionist/types/receptionist.types.ts,
    frontend/src/features/receptionist/api/receptionist-api.ts,
    frontend/src/features/receptionist/hooks/useReceptionistPolling.ts,
    frontend/src/features/receptionist/schemas/intake-form.schema.ts,
    frontend/src/features/receptionist/schemas/booking.schema.ts
  </files>
  <read_first>
    frontend/src/app/globals.css,
    frontend/src/features/scheduling/api/scheduling-api.ts,
    frontend/src/features/clinical/api/clinical-api.ts,
    frontend/src/features/patient/hooks/usePatientSearch.ts,
    .planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
  </read_first>
  <action>
    **CSS variables** -- append to `:root` block in globals.css (from UI-SPEC):
    ```css
    /* Receptionist status badges */
    --status-not-arrived-bg: #EEEDFE;
    --status-not-arrived-text: #3C3489;
    --status-waiting-bg: #FAEEDA;
    --status-waiting-text: #633806;
    --status-examining-bg: #E6F1FB;
    --status-examining-text: #0C447C;
    --status-completed-bg: #E1F5EE;
    --status-completed-text: #085041;
    --source-appointment-bg: #EEEDFE;
    --source-appointment-text: #3C3489;
    --source-walkin-bg: #FDE8E4;
    --source-walkin-text: #9C3420;
    --kpi-appointments: #534AB7;
    --kpi-waiting: #BA7517;
    --kpi-examining: #185FA5;
    --kpi-completed: #0F6E56;
    --action-reschedule: #534AB7;
    --action-noshow: #BA7517;
    --action-cancel: #A32D2D;
    --checkin-confirm: #534AB7;
    --avatar-complete-bg: #EEEDFE;
    --avatar-complete-text: #3C3489;
    --avatar-incomplete-bg: #FAEEDA;
    --avatar-incomplete-text: #854F0B;
    --avatar-cancel-bg: #FDE8E4;
    --avatar-cancel-text: #A32D2D;
    ```

    **Types** in `receptionist.types.ts`:
    - `ReceptionistStatus = "not_arrived" | "waiting" | "examining" | "completed"`
    - `ReceptionistSource = "appointment" | "walkin"`
    - `ReceptionistDashboardRow` interface matching backend DTO
    - `ReceptionistKpi` interface
    - `AvailableSlot` interface
    - `DashboardFilters = { status?: ReceptionistStatus; search?: string; page: number; pageSize: number }`

    **API client** in `receptionist-api.ts`:
    - Following established pattern from scheduling-api.ts using OpenAPI Fetch
    - `receptionistKeys` object for query key factory
    - `useReceptionistDashboard(filters: DashboardFilters)` -- queryKey with filters, refetchInterval: 15_000
    - `useReceptionistKpi()` -- refetchInterval: 30_000
    - `useAvailableSlots(date: string, doctorId?: string)` -- enabled when date is set
    - `useCheckInMutation()` -- POST /api/scheduling/appointments/check-in, invalidates dashboard+kpi
    - `useBookGuestMutation()` -- POST /api/scheduling/appointments/guest
    - `useMarkNoShowMutation()` -- POST, invalidates dashboard+kpi
    - `useCreateWalkInVisitMutation()` -- POST /api/clinical/visits/walk-in
    - `useCancelVisitMutation()` -- POST /api/clinical/visits/{id}/cancel-with-reason
    - `useRegisterFromIntakeMutation()` -- POST /api/patients/intake
    - `useUpdateFromIntakeMutation()` -- PUT /api/patients/{id}/intake

    **Polling hook** in `useReceptionistPolling.ts`:
    - Export `useManualRefresh()` that returns `{ refresh, isRefreshing }` using `queryClient.invalidateQueries({ queryKey: ['receptionist'] })`

    **Zod schemas**:
    - `intakeFormSchema` in intake-form.schema.ts: fullName (required, max 200), phone (required, regex ^0\d{9,10}$), dateOfBirth (required), gender (required), address (optional), cccd (optional), email (optional, email format), occupation (optional, max 200), reason (optional, max 500), ocularHistory (optional, max 2000), systemicHistory (optional, max 2000), currentMedications (optional, max 2000), screenTimeHours (optional, number >=0), workEnvironment (optional enum), contactLensUsage (optional enum), lifestyleNotes (optional, max 2000)
    - `bookingSchema` in booking.schema.ts: patientId (optional guid), guestName (optional, required if no patientId), guestPhone (optional, required if no patientId), reason (optional, max 500), doctorId (optional guid), date (required), startTime (required)
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains "--status-not-arrived-bg: #EEEDFE"
    - globals.css contains "--kpi-appointments: #534AB7"
    - receptionist.types.ts contains "ReceptionistDashboardRow" and "ReceptionistKpi"
    - receptionist-api.ts contains "useReceptionistDashboard" and "useReceptionistKpi"
    - receptionist-api.ts contains "refetchInterval: 15_000" (table) and "refetchInterval: 30_000" (kpi)
    - receptionist-api.ts contains "useCheckInMutation" and "useMarkNoShowMutation"
    - intake-form.schema.ts contains "intakeFormSchema" with "fullName" and "phone"
    - booking.schema.ts contains "bookingSchema"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CSS variables, TypeScript types, API client with polling hooks, and Zod schemas all in place.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard components (KPI, table, filters, badges) + role-based routing</name>
  <files>
    frontend/src/features/receptionist/components/StatusBadge.tsx,
    frontend/src/features/receptionist/components/SourceBadge.tsx,
    frontend/src/features/receptionist/components/KpiCards.tsx,
    frontend/src/features/receptionist/components/StatusFilterPills.tsx,
    frontend/src/features/receptionist/components/PatientQueueTable.tsx,
    frontend/src/features/receptionist/components/ReceptionistDashboard.tsx,
    frontend/src/app/routes/_authenticated/dashboard.tsx
  </files>
  <read_first>
    frontend/src/app/routes/_authenticated/dashboard.tsx,
    frontend/src/features/clinical/components/WorkflowDashboard.tsx,
    frontend/src/shared/stores/authStore.ts,
    frontend/src/features/receptionist/api/receptionist-api.ts,
    frontend/src/features/receptionist/types/receptionist.types.ts,
    docs/dev/receiptionist/receptionist-dashboard.md,
    .planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
  </read_first>
  <action>
    **StatusBadge.tsx:**
    - Props: `{ status: ReceptionistStatus }`
    - Map status to Vietnamese label and CSS variable colors:
      - not_arrived: "Chua den", bg `var(--status-not-arrived-bg)`, text `var(--status-not-arrived-text)`
      - waiting: "Cho kham", bg `var(--status-waiting-bg)`, text `var(--status-waiting-text)`
      - examining: "Dang kham", bg `var(--status-examining-bg)`, text `var(--status-examining-text)`
      - completed: "Hoan thanh", bg `var(--status-completed-bg)`, text `var(--status-completed-text)`
    - Use shadcn Badge with `className="border-transparent bg-[var(--status-X-bg)] text-[var(--status-X-text)]"`
    - Add `role="status"` per accessibility spec

    **SourceBadge.tsx:**
    - Props: `{ source: ReceptionistSource }`
    - appointment: "Hen", source-appointment colors
    - walkin: "Walk-in", source-walkin colors (coral)

    **KpiCards.tsx:**
    - Props: `{ kpi: ReceptionistKpi | undefined; isLoading: boolean }`
    - 4 cards in a row (grid gap-4 sm:grid-cols-2 lg:grid-cols-4)
    - Each card: shadcn Card, label text (14px semibold), number (28px semibold, custom color), icon
    - Card 1: "Lich hen hom nay" - number in `var(--kpi-appointments)` (#534AB7), sub text "X chua den" from kpi.notArrived
    - Card 2: "Cho kham" - `var(--kpi-waiting)` (#BA7517)
    - Card 3: "Dang kham" - `var(--kpi-examining)` (#185FA5)
    - Card 4: "Hoan thanh" - `var(--kpi-completed)` (#0F6E56)
    - Skeleton loading state per shadcn Skeleton
    - `aria-live="polite"` on number elements

    **StatusFilterPills.tsx:**
    - Props: `{ value: ReceptionistStatus | undefined; onChange: (status?) => void; counts: ReceptionistKpi }`
    - Use shadcn ToggleGroup (single mode)
    - Pills: "Tat ca" (no filter), "Chua den (N)", "Cho kham (N)", "Dang kham (N)", "Xong (N)"
    - Active pill: primary bg/text; inactive: secondary bg/text

    **PatientQueueTable.tsx:**
    - Props: `{ data, isLoading, onCheckIn, onActionMenu, filters, onFiltersChange }`
    - Use TanStack Table (following clinical dashboard pattern)
    - Columns per UI-SPEC:
      - STT: 48px, row index
      - Ho ten: flex-1 min-160px, font semibold 14px. Patient code below in mono text-muted-foreground
      - Nam sinh: 80px
      - Gio hen: 80px, format "HH:mm"
      - Nguon: 80px, SourceBadge component
      - Ly do kham: flex-1 min-120px, italic text-muted-foreground if empty ("Chua ro")
      - Trang thai: 100px, StatusBadge component
      - Thao tac: 100px, Check-in button (for not_arrived) or three-dot menu (for others)
    - Not-arrived rows: className `bg-secondary` (stone-50 tint)
    - Default sort: appointmentTime ascending, walk-ins sort to bottom
    - Sortable columns: Ho ten, Nam sinh, Gio hen, Nguon, Trang thai
    - Pagination: 10 rows default, options 10/20/50, footer "Hien thi X / Y benh nhan hom nay"
    - Check-in button in Thao tac column for not_arrived status: outline style, border/text #534AB7, hover bg #EEEDFE, label "Check-in"
    - Empty state: icon + "Chua co benh nhan nao hom nay" heading + "Bat dau tiep nhan benh nhan moi hoac cho benh nhan da hen den." body
    - Responsive: hide Nam sinh and Nguon columns below 1024px (per UI-SPEC)

    **ReceptionistDashboard.tsx:**
    - Main container component
    - Uses `useReceptionistDashboard(filters)` and `useReceptionistKpi()`
    - Layout:
      - Title "Dashboard" (h1, 20px semibold)
      - Action bar: search input (left, placeholder "Tim theo SBT hoac ten BN..."), buttons (right): "Dat lich hen" (outline, calendar icon, navigates to /appointment/new per D-09), "+ Tiep nhan BN moi" (filled primary, navigates to intake form)
      - Manual refresh button with tooltip "Cap nhat du lieu" using useManualRefresh()
      - KpiCards component
      - StatusFilterPills component
      - PatientQueueTable component
    - Connection lost banner: show amber warning "Mat ket noi. Du lieu co the chua cap nhat." when query error state

    **dashboard.tsx** (route file, per D-01):
    - Import useAuthStore, check `user.roles.includes("Receptionist")`
    - If receptionist: render `<ReceptionistDashboard />`
    - Otherwise: render existing dashboard content (keep current code as a separate component like `DefaultDashboard`)
    - No route change, no sidebar change (per D-01, D-02)
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - StatusBadge.tsx contains "Chua den" and "var(--status-not-arrived-bg)"
    - SourceBadge.tsx contains "Walk-in" and "var(--source-walkin-bg)"
    - KpiCards.tsx contains "Lich hen hom nay" and "var(--kpi-appointments)"
    - KpiCards.tsx contains 'aria-live="polite"'
    - StatusFilterPills.tsx contains "Tat ca" and "ToggleGroup"
    - PatientQueueTable.tsx contains "useReactTable" or "TanStack" table setup
    - PatientQueueTable.tsx contains "Chua co benh nhan nao hom nay" (empty state)
    - PatientQueueTable.tsx contains "Check-in" button
    - PatientQueueTable.tsx contains "bg-secondary" for not-arrived rows
    - ReceptionistDashboard.tsx contains "useReceptionistDashboard" and "useReceptionistKpi"
    - ReceptionistDashboard.tsx contains "Tim theo SBT hoac ten BN"
    - ReceptionistDashboard.tsx contains "Tiep nhan BN moi"
    - ReceptionistDashboard.tsx contains "Dat lich hen"
    - dashboard.tsx contains "Receptionist" role check
    - dashboard.tsx contains "ReceptionistDashboard"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Complete receptionist dashboard with KPI cards, patient table, filter pills, polling, and role-based rendering at /dashboard.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no type errors
- Dashboard renders correctly for Receptionist role
- Existing dashboard still works for other roles
</verification>

<success_criteria>
- Receptionist users see KPI cards + patient queue table at /dashboard
- Non-receptionist users see the existing default dashboard
- KPI cards poll every 30s, table polls every 15s
- Manual refresh button invalidates all queries
- Status badges show correct Vietnamese labels and colors
- Table has proper columns, sorting, pagination, empty state
- Responsive: columns hide on tablet/mobile per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/14-implement-receptionist-role-flow/14-04-SUMMARY.md`
</output>
