---
phase: 14-implement-receptionist-role-flow
plan: 07
type: execute
wave: 5
depends_on: [14-04, 14-05, 14-06]
files_modified:
  - frontend/src/features/receptionist/components/CheckInDialog.tsx
  - frontend/src/features/receptionist/components/CheckInIncompleteDialog.tsx
  - frontend/src/features/receptionist/components/WalkInVisitDialog.tsx
  - frontend/src/features/receptionist/components/RowActionMenu.tsx
  - frontend/src/features/receptionist/components/RescheduleDialog.tsx
  - frontend/src/features/receptionist/components/CancelAppointmentDialog.tsx
  - frontend/src/features/receptionist/components/NoShowDialog.tsx
  - frontend/src/features/receptionist/components/CancelVisitDialog.tsx
autonomous: true
requirements: [RCP-05, RCP-06]

must_haves:
  truths:
    - "Check-in dialog confirms patient info and creates visit (per D-05)"
    - "Incomplete patient check-in navigates to intake form with pre-filled data (per D-06)"
    - "Walk-in existing patient dialog creates visit without appointment (per D-05)"
    - "Row action menu shows correct actions per status (per D-13)"
    - "Cancel/No-Show dialogs require reason from dropdown (per D-14)"
    - "Dat hen lai checkbox navigates to /appointments/new with patient pre-filled (per D-15)"
  artifacts:
    - path: "frontend/src/features/receptionist/components/RowActionMenu.tsx"
      provides: "Context menu with status-dependent actions"
    - path: "frontend/src/features/receptionist/components/CheckInDialog.tsx"
      provides: "Check-in popup for complete patient"
    - path: "frontend/src/features/receptionist/components/CancelVisitDialog.tsx"
      provides: "Cancel visit popup with reason dropdown"
  key_links:
    - from: "RowActionMenu.tsx"
      to: "CheckInDialog.tsx / CancelAppointmentDialog.tsx / etc."
      via: "Dialog state management"
      pattern: "setOpen|onOpen"
    - from: "CheckInDialog.tsx"
      to: "receptionist-api.ts"
      via: "useCheckInMutation"
      pattern: "useCheckInMutation"
    - from: "RescheduleDialog.tsx"
      to: "TimeSlotGrid.tsx"
      via: "Reuses TimeSlotGrid from plan 14-06"
      pattern: "TimeSlotGrid"
---

<objective>
Build all check-in/walk-in dialogs (SCR-005) and action menu with dialogs (SCR-006): check-in complete, check-in incomplete, walk-in, reschedule, cancel appointment, no-show, cancel visit.

Purpose: Complete the dashboard interaction layer -- all actions a receptionist takes on patient rows.
Output: 8 dialog components + row action menu integrated with dashboard table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-implement-receptionist-role-flow/14-CONTEXT.md
@.planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
@.planning/phases/14-implement-receptionist-role-flow/14-RESEARCH.md
@.planning/phases/14-implement-receptionist-role-flow/14-04-SUMMARY.md
@.planning/phases/14-implement-receptionist-role-flow/14-05-SUMMARY.md
@.planning/phases/14-implement-receptionist-role-flow/14-06-SUMMARY.md

<interfaces>
From receptionist-api.ts (plan 04):
```typescript
useCheckInMutation()        // POST /api/scheduling/appointments/check-in
useMarkNoShowMutation()     // POST /api/scheduling/appointments/{id}/no-show
useCancelVisitMutation()    // POST /api/clinical/visits/{id}/cancel-with-reason
useCreateWalkInVisitMutation()  // POST /api/clinical/visits/walk-in
```

From receptionist.types.ts (plan 04):
```typescript
type ReceptionistStatus = "not_arrived" | "waiting" | "examining" | "completed"
interface ReceptionistDashboardRow {
  id: string; appointmentId: string | null; visitId: string | null;
  patientId: string | null; patientName: string; patientCode: string | null;
  status: ReceptionistStatus; isGuestBooking: boolean; ...
}
```

From TimeSlotGrid.tsx (plan 06 -- reused in RescheduleDialog):
```typescript
<TimeSlotGrid slots={slots} selectedSlot={selected} onSelectSlot={setSelected} isLoading={isLoading} />
```

Action matrix by status (from SCR-006 spec):
- not_arrived: Check-in, Xem ho so, Sua thong tin, Doi lich hen, Danh dau khong den, Huy hen
- waiting: Xem ho so, Sua thong tin, Huy luot kham
- examining: Xem ho so (only)
- completed: Xem ho so (only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check-in dialogs (complete, incomplete, walk-in)</name>
  <files>
    frontend/src/features/receptionist/components/CheckInDialog.tsx,
    frontend/src/features/receptionist/components/CheckInIncompleteDialog.tsx,
    frontend/src/features/receptionist/components/WalkInVisitDialog.tsx
  </files>
  <read_first>
    docs/dev/receiptionist/receptionist-checkin.md,
    frontend/src/shared/components/Dialog.tsx,
    frontend/src/features/receptionist/api/receptionist-api.ts,
    .planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
  </read_first>
  <action>
    **CheckInDialog.tsx** (complete patient, SCR-005 Flow A):
    - Props: `{ open: boolean; onOpenChange; row: ReceptionistDashboardRow }`
    - shadcn Dialog, max-width sm:max-w-md
    - Header: "Check-in benh nhan"
    - Patient avatar circle with initials, bg `var(--avatar-complete-bg)`, text `var(--avatar-complete-text)`
    - Patient info card (read-only): name, code, birth year, phone
    - Blue info note: "Xac nhan thong tin voi BN truoc khi check-in. Neu can sua, bam Sua thong tin."
    - Buttons:
      - "Sua thong tin" (outline) -- navigates to patient profile edit
      - "Quay lai" (ghost) -- closes dialog
      - "Xac nhan check-in" (filled, bg #534AB7, text white) -- calls useCheckInMutation with appointmentId
    - On success: toast, close dialog, queries auto-invalidated
    - On error (race condition): toast "BN da chuyen sang Dang kham, khong the huy."

    **CheckInIncompleteDialog.tsx** (incomplete/guest patient, SCR-005):
    - Props: `{ open: boolean; onOpenChange; row: ReceptionistDashboardRow }`
    - shadcn Dialog, max-width sm:max-w-md
    - Header: "Check-in benh nhan"
    - Avatar with incomplete styling: bg `var(--avatar-incomplete-bg)`, text `var(--avatar-incomplete-text)`
    - Amber warning banner: "Ho so chua day du -- BN dat hen qua dien thoai, chi co ten + SDT. Can bo sung ngay sinh, gioi tinh va cac thong tin khac truoc khi kham."
    - Patient info (what we have): name, phone only
    - Buttons:
      - "Quay lai" (ghost)
      - "Check-in & bo sung ho so" (filled, #534AB7) -- navigates to /patients/intake?patientId={id} (per D-06) OR if guest booking, navigates to intake form with GuestName/GuestPhone pre-filled
    - For guest bookings: the intake form will register the patient and then check-in flow completes

    **WalkInVisitDialog.tsx** (walk-in existing patient, SCR-005 Flow B):
    - Props: `{ open: boolean; onOpenChange }`
    - shadcn Dialog, max-width sm:max-w-md
    - Header: "Tao luot kham moi"
    - Patient search field using existing usePatientSearch + Command (cmdk)
    - Once patient selected: show patient info card
    - Doctor selector: Select using useDoctors()
    - Reason field: optional Textarea
    - Buttons:
      - "Huy" (ghost)
      - "Xac nhan tao luot kham" (filled primary) -- calls useCreateWalkInVisitMutation
    - On success: toast, close, queries invalidated
    - This dialog is opened from "+ Tiep nhan BN moi" button on dashboard for existing patients (different from new intake form path)
    - Actually per CONTEXT: "+ Tiep nhan BN moi" navigates to intake form for NEW patients. Walk-in dialog is for EXISTING patients who show up without appointment. Add a button or flow on dashboard to trigger this -- perhaps part of a dropdown on the primary CTA, or a separate action.
    - Per spec: The dashboard has "+ Tiep nhan BN moi" (goes to intake for new) and this walk-in dialog can be triggered from search results or from the action menu.
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - CheckInDialog.tsx contains "Check-in benh nhan" header
    - CheckInDialog.tsx contains "Xac nhan check-in" button
    - CheckInDialog.tsx contains "var(--avatar-complete-bg)" or "#EEEDFE"
    - CheckInDialog.tsx contains "useCheckInMutation"
    - CheckInIncompleteDialog.tsx contains "Ho so chua day du"
    - CheckInIncompleteDialog.tsx contains "Check-in & bo sung ho so"
    - CheckInIncompleteDialog.tsx navigates to "/patients/intake" with patientId
    - WalkInVisitDialog.tsx contains "Tao luot kham moi"
    - WalkInVisitDialog.tsx contains "useCreateWalkInVisitMutation"
    - All dialogs use shadcn Dialog component
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>3 check-in/walk-in dialog components built with correct UI spec styling and API integration.</done>
</task>

<task type="auto">
  <name>Task 2: RowActionMenu + action dialogs (reschedule, cancel, no-show, cancel visit)</name>
  <files>
    frontend/src/features/receptionist/components/RowActionMenu.tsx,
    frontend/src/features/receptionist/components/RescheduleDialog.tsx,
    frontend/src/features/receptionist/components/CancelAppointmentDialog.tsx,
    frontend/src/features/receptionist/components/NoShowDialog.tsx,
    frontend/src/features/receptionist/components/CancelVisitDialog.tsx
  </files>
  <read_first>
    docs/dev/receiptionist/receptionist-dashboard-actions.md,
    frontend/src/shared/components/DropdownMenu.tsx,
    frontend/src/features/scheduling/api/scheduling-api.ts,
    frontend/src/features/receptionist/api/receptionist-api.ts,
    frontend/src/features/receptionist/components/booking/TimeSlotGrid.tsx,
    .planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
  </read_first>
  <action>
    **RowActionMenu.tsx** (per D-13):
    - Props: `{ row: ReceptionistDashboardRow; onCheckIn: () => void }`
    - Uses shadcn DropdownMenu with IconDotsVertical trigger (Tabler)
    - Menu items vary by status:
      - **not_arrived**: "Xem ho so", "Sua thong tin", separator, "Doi lich hen" (text #534AB7), "Danh dau khong den" (text #BA7517), "Huy hen" (text #A32D2D)
      - **waiting**: "Xem ho so", "Sua thong tin", separator, "Huy luot kham" (text #A32D2D)
      - **examining**: "Xem ho so" only
      - **completed**: "Xem ho so" only
    - "Xem ho so" navigates to /patients/{patientId}
    - "Sua thong tin" navigates to /patients/{patientId}/edit
    - Other items open their respective dialogs
    - Min width 200px, separator before destructive items

    **RescheduleDialog.tsx:**
    - Props: `{ open; onOpenChange; row }`
    - Dialog max-width sm:max-w-lg (larger, has calendar)
    - Header: "Doi lich hen"
    - Show old schedule with strikethrough: "{old date} luc {old time}"
    - Calendar + TimeSlotGrid for new date/time (reuse TimeSlotGrid from plan 14-06 booking components)
    - Buttons: "Huy" (ghost), "Xac nhan doi lich" (filled, #534AB7)
    - Uses existing reschedule mutation from scheduling-api.ts

    **CancelAppointmentDialog.tsx** (per D-14):
    - Props: `{ open; onOpenChange; row }`
    - Dialog max-width sm:max-w-md
    - Header: "Huy hen"
    - Avatar with cancel styling: bg `var(--avatar-cancel-bg)`, text `var(--avatar-cancel-text)`
    - Red warning: "Hen se bi xoa hoan toan. BN se khong con xuat hien tren Dashboard ngay nay. Hanh dong khong the hoan tac."
    - Required dropdown "Ly do huy" with options:
      - "BN yeu cau huy"
      - "BN doi phong kham"
      - "Le tan dat nham"
      - "Khac"
    - Optional text note field when "Khac" selected
    - Buttons: "Huy" (ghost), "Xac nhan huy hen" (filled, #A32D2D)
    - Uses existing cancel appointment mutation
    - Success toast: "Da huy hen cho {name}"

    **NoShowDialog.tsx** (per D-14):
    - Props: `{ open; onOpenChange; row }`
    - Dialog max-width sm:max-w-md
    - Header: "Danh dau khong den"
    - Avatar with no-show styling (amber)
    - Warning: "BN se duoc ghi nhan la no-show. Hen van giu trong he thong de thong ke. Le tan co the dat hen lai cho BN sau."
    - Optional textarea: placeholder "VD: Da goi nhung khong lien lac duoc..."
    - Checkbox: "Dat hen lai cho BN nay" (per D-15)
    - Buttons: "Huy" (ghost), "Xac nhan no-show" (filled, #BA7517)
    - Uses useMarkNoShowMutation
    - If "Dat hen lai" checked: after mutation success, navigate to /appointments/new?patientId={patientId} (per D-15, using plural route)
    - Success toast

    **CancelVisitDialog.tsx:**
    - Props: `{ open; onOpenChange; row }`
    - Dialog max-width sm:max-w-md
    - Header: "Huy luot kham"
    - Avatar with cancel styling (red)
    - Warning: "BN se bi xoa khoi hang doi hom nay. Neu BN co hen, lich hen se chuyen thanh Da huy. Hanh dong khong the hoan tac."
    - Required dropdown "Ly do huy" with options:
      - "BN khong muon cho, bo ve"
      - "BN muon doi sang ngay khac"
      - "Le tan check-in nham nguoi"
      - "BN chuyen sang phong kham khac"
      - "Khac"
    - Checkbox: "Dat hen lai cho BN nay" (per D-15)
    - Buttons: "Huy" (ghost), "Xac nhan huy luot kham" (filled, #A32D2D)
    - Uses useCancelVisitMutation
    - If "Dat hen lai" checked: navigate to /appointments/new?patientId={patientId} (per D-15, using plural route)
    - Success toast: "Da huy luot kham cho {name}"
    - Race condition handling: if 409/conflict, toast "BN da chuyen sang Dang kham, khong the huy."

    **Integration with PatientQueueTable** (from plan 04):
    - Wire RowActionMenu into the Thao tac column of PatientQueueTable
    - For not_arrived rows: show Check-in button (outline, #534AB7) + three-dot menu
    - For other rows: show three-dot menu only
    - All dialog state managed locally in PatientQueueTable or ReceptionistDashboard
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - RowActionMenu.tsx contains "DropdownMenu" and "IconDotsVertical"
    - RowActionMenu.tsx contains "Xem ho so" and "Doi lich hen" and "Huy hen"
    - RowActionMenu.tsx has different menus per status (switch/if on row.status)
    - CancelAppointmentDialog.tsx contains "Ly do huy" dropdown with "BN yeu cau huy"
    - CancelAppointmentDialog.tsx contains "#A32D2D" or "var(--action-cancel)"
    - CancelAppointmentDialog.tsx contains "Da huy hen cho"
    - NoShowDialog.tsx contains "Dat hen lai cho BN nay" checkbox
    - NoShowDialog.tsx contains "#BA7517" or "var(--action-noshow)"
    - NoShowDialog.tsx contains "useMarkNoShowMutation"
    - NoShowDialog.tsx navigates to "/appointments/new" (plural) for rebook
    - CancelVisitDialog.tsx contains "BN khong muon cho, bo ve" reason option
    - CancelVisitDialog.tsx contains "Dat hen lai cho BN nay" checkbox
    - CancelVisitDialog.tsx navigates to "/appointments/new" (plural) for rebook
    - RescheduleDialog.tsx contains "TimeSlotGrid" and "Calendar"
    - RescheduleDialog.tsx contains "Xac nhan doi lich"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Complete action system: row menu with 6 actions, 4 action dialogs with correct Vietnamese copy, reason dropdowns, and rebook navigation to /appointments/new.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no type errors
- All dialogs match UI-SPEC copy and colors
- Action menu shows correct options per status
</verification>

<success_criteria>
- RowActionMenu renders status-appropriate actions per SCR-006 spec
- Check-in dialog for complete patients confirms and creates visit
- Incomplete patient check-in redirects to intake form with data pre-filled
- Walk-in dialog creates visit for existing patient without appointment
- Cancel/No-Show/Cancel-Visit dialogs require reasons, support "Dat hen lai"
- Reschedule dialog shows calendar + slot grid for new time selection (reusing TimeSlotGrid from plan 06)
- All colored actions match UI-SPEC color scheme
- All "Dat hen lai" navigations go to /appointments/new (plural, consistent)
</success_criteria>

<output>
After completion, create `.planning/phases/14-implement-receptionist-role-flow/14-07-SUMMARY.md`
</output>
