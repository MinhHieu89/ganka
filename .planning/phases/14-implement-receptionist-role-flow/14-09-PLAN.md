---
phase: 14-implement-receptionist-role-flow
plan: 09
type: execute
wave: 5
depends_on: [14-04, 14-05, 14-06, 14-07]
files_modified: []
autonomous: false
requirements: [RCP-01, RCP-02, RCP-03, RCP-04, RCP-05, RCP-06]

must_haves:
  truths:
    - "All backend tests pass"
    - "Frontend compiles without errors"
    - "Receptionist dashboard renders correctly for Receptionist role"
    - "Check-in, booking, intake, and action flows work end-to-end"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete receptionist workflow.

Purpose: Confirm all 5 screens work correctly before marking phase complete.
Output: Verified working receptionist flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-implement-receptionist-role-flow/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite + frontend type check</name>
  <files></files>
  <read_first></read_first>
  <action>
    1. Run full backend test suite: `dotnet test backend/tests/ -v q`
    2. Run frontend type check: `cd frontend && npx tsc --noEmit`
    3. Start backend at port 5255 and frontend at port 3000
    4. Verify API endpoints respond correctly:
       - GET /api/scheduling/receptionist/kpi returns 200
       - GET /api/scheduling/receptionist/dashboard returns 200
       - GET /api/scheduling/slots?date={today} returns 200
    5. Fix any issues found
  </action>
  <verify>
    <automated>dotnet test backend/tests/ -v q && cd D:/Project/ganka/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet test backend/tests/ -v q` exits 0
    - `npx tsc --noEmit` exits 0
    - Backend starts without errors on port 5255
    - Frontend starts without errors on port 3000
  </acceptance_criteria>
  <done>All automated checks pass, backend and frontend start successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete receptionist workflow across 5 screens:
    1. Role-based dashboard with KPI cards, patient queue table, status filters
    2. Patient intake form with 4 collapsible sections
    3. Appointment booking page with calendar and time slots
    4. Check-in dialogs (complete patient, incomplete patient, walk-in)
    5. Action menus with reschedule, cancel, no-show, cancel-visit dialogs
  </what-built>
  <how-to-verify>
    Backend: http://localhost:5255
    Frontend: http://localhost:3000

    **Pre-requisite:** Create a user with "Receptionist" role via Admin panel, or assign Receptionist role to the Admin user.

    **Test 1 - Dashboard (SCR-002a):**
    1. Log in as Receptionist user
    2. Verify /dashboard shows KPI cards (4 cards with colored numbers)
    3. Verify patient queue table renders (may be empty initially)
    4. Verify filter pills (Tat ca, Chua den, Cho kham, Dang kham, Xong)
    5. Verify search input with placeholder "Tim theo SBT hoac ten BN..."
    6. Verify "+ Tiep nhan BN moi" and "Dat lich hen" buttons visible

    **Test 2 - Patient Intake (SCR-003):**
    1. Click "+ Tiep nhan BN moi"
    2. Verify 4 collapsible sections all expanded
    3. Fill required fields: name, phone, DOB, gender
    4. Verify phone duplicate check (enter an existing patient's phone)
    5. Click "Luu ho so" -- verify success toast and redirect to dashboard

    **Test 3 - Appointment Booking (SCR-004):**
    1. Click "Dat lich hen" from dashboard
    2. Verify 2-column layout (patient info left, calendar right)
    3. Search for existing patient
    4. Select a date and time slot
    5. Click "Xac nhan dat hen" -- verify success toast

    **Test 4 - Check-in Flow (SCR-005):**
    1. On dashboard, find a "Chua den" patient
    2. Click "Check-in" button
    3. Verify check-in dialog shows patient info
    4. Click "Xac nhan check-in" -- patient moves to "Cho kham"

    **Test 5 - Action Menus (SCR-006):**
    1. Click three-dot menu on a "Chua den" patient
    2. Verify all 6 menu items visible
    3. Test "Danh dau khong den" -- verify no-show dialog with note field
    4. Test "Huy hen" -- verify cancel dialog with reason dropdown
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All backend tests pass
- All frontend types check
- All 5 screens render and function correctly
- Human verification confirms flows work end-to-end
</verification>

<success_criteria>
- Complete receptionist workflow verified by human
- No blocking bugs
- All UI matches spec (colors, copy, layout)
</success_criteria>

<output>
After completion, create `.planning/phases/14-implement-receptionist-role-flow/14-09-SUMMARY.md`
</output>
