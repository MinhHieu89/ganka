---
phase: 14-implement-receptionist-role-flow
plan: 06
type: execute
wave: 4
depends_on: [14-03, 14-04]
files_modified:
  - frontend/src/features/receptionist/components/booking/NewAppointmentPage.tsx
  - frontend/src/features/receptionist/components/booking/TimeSlotGrid.tsx
  - frontend/src/features/receptionist/components/booking/ConfirmationBar.tsx
  - frontend/src/app/routes/_authenticated/appointments/new.tsx
autonomous: true
requirements: [RCP-04]

must_haves:
  truths:
    - "Receptionist can book appointment for existing patient or new guest patient (per D-09, D-11)"
    - "Page is at /appointments/new as full page route (per D-09, using plural 'appointments' for REST convention)"
    - "30-min time slots shown in morning/afternoon groups (per D-10)"
    - "Guest bookings store GuestName, GuestPhone, GuestReason on appointment (per D-11)"
    - "Doctor-based slot capacity: 1 per doctor per slot, or free if no doctor (per D-12)"
    - "Confirmation bar shows booking summary at bottom"
  artifacts:
    - path: "frontend/src/features/receptionist/components/booking/NewAppointmentPage.tsx"
      provides: "2-column booking page with patient info + calendar/slots"
    - path: "frontend/src/features/receptionist/components/booking/TimeSlotGrid.tsx"
      provides: "Time slot grid component with morning/afternoon groups"
    - path: "frontend/src/app/routes/_authenticated/appointments/new.tsx"
      provides: "Route file at /appointments/new"
  key_links:
    - from: "NewAppointmentPage.tsx"
      to: "receptionist-api.ts"
      via: "useAvailableSlots + useBookGuestMutation"
      pattern: "useAvailableSlots|useBookGuestMutation"
---

<objective>
Build the new appointment booking page (SCR-004) with patient search/guest entry, calendar date picker, time slot grid, and confirmation bar.

Purpose: Enable receptionist to book appointments for existing patients or phone-in guests.
Output: Complete booking page at /appointments/new.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-implement-receptionist-role-flow/14-CONTEXT.md
@.planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
@.planning/phases/14-implement-receptionist-role-flow/14-RESEARCH.md
@.planning/phases/14-implement-receptionist-role-flow/14-01-SUMMARY.md
@.planning/phases/14-implement-receptionist-role-flow/14-04-SUMMARY.md

<interfaces>
From receptionist-api.ts (plan 04):
```typescript
useAvailableSlots(date: string, doctorId?: string): { data: AvailableSlot[], isLoading }
useBookGuestMutation(): UseMutationResult  // POST /api/scheduling/appointments/guest
// Also existing: useBookAppointmentMutation from scheduling-api.ts for patient-linked bookings
```

From existing hooks:
```typescript
usePatientSearch(query: string): { data: PatientSearchResult[] }
useDoctors(): { data: Doctor[] }
useClinicSchedule(): { data: ClinicSchedule[] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimeSlotGrid + ConfirmationBar components</name>
  <files>
    frontend/src/features/receptionist/components/booking/TimeSlotGrid.tsx,
    frontend/src/features/receptionist/components/booking/ConfirmationBar.tsx
  </files>
  <read_first>
    docs/dev/receiptionist/receptionist-new-appointment.md,
    frontend/src/features/scheduling/api/scheduling-api.ts,
    .planning/phases/14-implement-receptionist-role-flow/14-UI-SPEC.md
  </read_first>
  <action>
    **TimeSlotGrid.tsx:**
    - Props: `{ slots: AvailableSlot[]; selectedSlot: string | null; onSelectSlot: (startTime: string) => void; isLoading: boolean }`
    - Split slots into 2 groups: "Sang" (morning, before 12:00) and "Chieu" (afternoon, 12:00+)
    - Each group: header label ("Sang" / "Chieu") + slot availability count "{X} slot trong / {Y}"
    - Grid: flex-wrap with gap-2, each slot button min-w-[80px] h-9
    - Slot styles per UI-SPEC:
      - Empty/available: border `var(--border)`, bg white, text foreground. Clickable.
      - Selected: border `#534AB7`, bg `#EEEDFE`, text `#3C3489`, font-weight 600
      - Full/occupied: bg `var(--secondary)`, text `text-muted-foreground`, `line-through`, disabled
    - Display time in HH:mm format (Vietnam local time)
    - Loading: Skeleton placeholders

    **ConfirmationBar.tsx:**
    - Props: `{ patientName: string; date: string; time: string; reason?: string; doctorName?: string; onCancel: () => void; onConfirm: () => void; isSubmitting: boolean }`
    - Sticky bottom card (fixed at bottom of page, or sticky within scroll container)
    - Title: "Xac nhan lich hen" (14px semibold)
    - Summary line: patient name, formatted date, formatted time, reason (if any), doctor name (if any)
    - Buttons:
      - Left: "Huy bo" (outline/ghost) -- calls onCancel
      - Right: "Xac nhan dat hen" (filled primary) -- calls onConfirm
    - Show loading spinner on confirm button when isSubmitting
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TimeSlotGrid.tsx contains "Sang" and "Chieu" group labels
    - TimeSlotGrid.tsx contains "#534AB7" or "var(--checkin-confirm)" for selected slot
    - TimeSlotGrid.tsx contains "line-through" for full slots
    - ConfirmationBar.tsx contains "Xac nhan lich hen"
    - ConfirmationBar.tsx contains "Huy bo" and "Xac nhan dat hen"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>TimeSlotGrid and ConfirmationBar components built with correct styling per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: NewAppointmentPage + route file</name>
  <files>
    frontend/src/features/receptionist/components/booking/NewAppointmentPage.tsx,
    frontend/src/app/routes/_authenticated/appointments/new.tsx
  </files>
  <read_first>
    docs/dev/receiptionist/receptionist-new-appointment.md,
    frontend/src/features/receptionist/components/booking/TimeSlotGrid.tsx,
    frontend/src/features/receptionist/components/booking/ConfirmationBar.tsx,
    frontend/src/features/receptionist/api/receptionist-api.ts,
    frontend/src/features/receptionist/schemas/booking.schema.ts,
    frontend/src/features/patient/hooks/usePatientSearch.ts
  </read_first>
  <action>
    **NewAppointmentPage.tsx:**
    - Breadcrumb: "Dashboard / Dat lich hen"
    - 2-column layout: left 40%, right 60% (per UI-SPEC)
    - **Left column** ("Thong tin benh nhan"):
      - Patient search using existing usePatientSearch hook via Command (cmdk) autocomplete
      - Search placeholder: "Go SDT hoac ten de tim..."
      - When patient found: green info bar "Da tim thay: {name} ({year}) -- {code}"
      - When phone searched but not found: yellow bar "Khong tim thay BN voi SDT nay. Nhap thong tin ben duoi de tao hen cho BN moi."
      - For new patient (per D-11): show fields for GuestName, GuestPhone, GuestReason (reason as textarea)
      - Blue info note: "BN moi chi can ten + SDT + ly do kham de dat hen. Thong tin day du se bo sung khi BN den check-in."
      - Doctor selector: shadcn Select using useDoctors() hook. Include "BS nao trong" option (null doctorId)
      - Reason field: Textarea, optional
    - **Right column** ("Chon ngay & gio"):
      - shadcn Calendar component (month view)
      - Calendar selected date styling: bg #EEEDFE, text #3C3489
      - Past dates: disabled, text-muted-foreground
      - Today: underline accent, no background
      - Below calendar: TimeSlotGrid component loaded via useAvailableSlots(selectedDate, selectedDoctorId)
    - **Confirmation bar** (bottom): ConfirmationBar component, visible only when date + slot selected
    - **Submit flow**:
      - If existing patient: call existing booking mutation with patientId
      - If guest patient (per D-11): call useBookGuestMutation with GuestName, GuestPhone, GuestReason, DoctorId, StartTime, Source
      - Validation: "Vui long chon khung gio hen" if no slot selected
      - Success toast: "Da dat hen cho {name} vao {date} luc {time}"
      - Navigate back to /dashboard
    - **Pre-fill support**: Accept search params `{ patientId?: string }` for "Dat hen lai" flow from no-show/cancel dialogs (per D-15)

    **Route file** `frontend/src/app/routes/_authenticated/appointments/new.tsx`:
    ```typescript
    export const Route = createFileRoute("/_authenticated/appointments/new")({
      beforeLoad: () => requirePermission("Scheduling.Create"),
      component: NewAppointmentPage,
    })
    ```
    Note: Route is `/appointments/new` (plural) following REST conventions. D-09 specified `/appointment/new` (singular) but we standardize to plural for consistency with the `appointments` resource naming.
  </action>
  <verify>
    <automated>cd D:/Project/ganka/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - NewAppointmentPage.tsx contains "Thong tin benh nhan" and "Chon ngay & gio"
    - NewAppointmentPage.tsx contains "Da tim thay:" (found patient bar)
    - NewAppointmentPage.tsx contains "Khong tim thay BN" (not found bar)
    - NewAppointmentPage.tsx contains "BN moi chi can ten" (new patient info note)
    - NewAppointmentPage.tsx contains "BS nao trong" option for doctor selector
    - NewAppointmentPage.tsx contains "useAvailableSlots"
    - NewAppointmentPage.tsx contains "useBookGuestMutation" or booking mutation
    - NewAppointmentPage.tsx contains "Da dat hen cho"
    - new.tsx contains 'createFileRoute("/_authenticated/appointments/new")'
    - new.tsx contains "requirePermission"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Complete appointment booking page with existing/guest patient flows, calendar, slot grid, confirmation bar.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no type errors
- Booking page renders with 2-column layout
</verification>

<success_criteria>
- Booking page at /appointments/new with 2-column layout
- Existing patient search + guest patient entry flow
- Calendar with date selection + time slot grid
- Doctor selection with "BS nao trong" option
- Confirmation bar with booking summary
- Guest bookings store only name+phone+reason (no patient record per D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/14-implement-receptionist-role-flow/14-06-SUMMARY.md`
</output>
