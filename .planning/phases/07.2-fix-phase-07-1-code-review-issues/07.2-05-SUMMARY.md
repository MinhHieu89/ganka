---
phase: 07.2-fix-phase-07-1-code-review-issues
plan: 05
subsystem: ui
tags: [react, signalr, i18n, zod, tanstack-router, shadcn-ui]

requires:
  - phase: 07-billing
    provides: Billing frontend components, SignalR hub, service catalog

provides:
  - Stable SignalR connection using useRef pattern (no reconnection storms)
  - i18n-aware Zod validation schema factory for service catalog
  - Type-safe TanStack Router links with typed params
  - Disabled dead shift buttons with tooltip explanation
  - Trailing-slash-safe sidebar active detection
  - i18n enum display maps for all billing enums

affects: [billing, frontend]

tech-stack:
  added: []
  patterns:
    - "useRef pattern for stable SignalR connections in React"
    - "Zod schema factory accepting TFunction for i18n validation messages"
    - "i18n key maps for enum display values instead of hardcoded strings"

key-files:
  created: []
  modified:
    - frontend/src/features/billing/hooks/use-billing-hub.ts
    - frontend/src/features/billing/components/ServiceCatalogFormDialog.tsx
    - frontend/src/app/routes/_authenticated/billing/index.tsx
    - frontend/src/shared/components/AppSidebar.tsx
    - frontend/src/features/billing/api/billing-api.ts
    - frontend/src/features/billing/components/InvoiceView.tsx
    - frontend/src/features/billing/components/ShiftReportView.tsx
    - frontend/src/features/billing/components/PaymentMethodSelector.tsx
    - frontend/public/locales/en/billing.json
    - frontend/public/locales/vi/billing.json

key-decisions:
  - "Kept single Zod schema for create/edit with isActive default(true) rather than splitting into two schemas"
  - "Converted all billing enum maps to i18n key maps rather than inline t() calls for consistency"
  - "Used useRef pattern for queryClient to avoid SignalR reconnection storms"

patterns-established:
  - "Zod schema factory pattern: createXxxSchema(t: TFunction) with useMemo in component"
  - "Enum i18n key maps: export const XXX_I18N_KEY: Record<number, string> with billing namespace keys"

requirements-completed: [CR-13, CR-14, CR-15, CR-16, CR-17, CR-18, CR-19, CR-22]

duration: 7min
completed: 2026-03-13
---

# Phase 07.2 Plan 05: Frontend Quality Fixes Summary

**Stable SignalR via useRef, i18n Zod validation, typed router links, disabled dead buttons, sidebar trailing-slash fix, and full enum i18n conversion**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-13T16:40:56Z
- **Completed:** 2026-03-13T16:47:51Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments
- SignalR connection now created once with empty dependency array, preventing reconnection storms (CR-13)
- All mutateAsync calls wrapped in try/catch for proper error handling (CR-14)
- Zod validation messages converted to i18n factory pattern supporting Vietnamese (CR-15)
- TanStack Router Link uses typed params instead of `as string` cast (CR-16)
- Dead Open/Close Shift buttons disabled with tooltip explaining they are coming soon (CR-17)
- Sidebar active detection normalizes trailing slashes for correct highlighting (CR-18)
- All billing enum display maps converted from hardcoded strings to i18n key maps with proper Vietnamese diacritics (CR-19)
- isActive field in Zod schema documented with comment explaining create/edit behavior (CR-22)

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix SignalR stability + mutateAsync + Zod i18n + isActive schema** - `e168d3d` (fix)
2. **Task 2: Fix router type safety + dead buttons + sidebar active + enum i18n** - `02ec963` (fix)

## Files Created/Modified
- `frontend/src/features/billing/hooks/use-billing-hub.ts` - useRef pattern for stable SignalR, removed useCallback
- `frontend/src/features/billing/components/ServiceCatalogFormDialog.tsx` - i18n Zod schema factory, try/catch on mutations
- `frontend/src/app/routes/_authenticated/billing/index.tsx` - Typed router link, disabled shift buttons with tooltips
- `frontend/src/shared/components/AppSidebar.tsx` - Trailing-slash normalization for active detection
- `frontend/src/features/billing/api/billing-api.ts` - All enum maps converted to i18n key maps
- `frontend/src/features/billing/components/InvoiceView.tsx` - Uses i18n key maps with t() calls
- `frontend/src/features/billing/components/ShiftReportView.tsx` - Uses i18n key maps with t() parameter
- `frontend/src/features/billing/components/PaymentMethodSelector.tsx` - Labels use i18n keys
- `frontend/public/locales/en/billing.json` - Added validation, shiftManagement, paymentStatus, discountTypes, approvalStatus, refundStatus keys
- `frontend/public/locales/vi/billing.json` - Vietnamese translations with proper diacritics

## Decisions Made
- Kept single Zod schema for create/edit with `isActive: z.boolean().default(true)` rather than splitting into two schemas -- simpler code, no side effects
- Converted all billing enum maps to i18n key maps (PAYMENT_METHOD_I18N_KEY, etc.) rather than using inline t() calls -- ensures consistency and makes enum-to-i18n mapping explicit
- Used useRef pattern for queryClient to avoid SignalR reconnection storms -- the ref is updated each render but doesn't trigger useEffect

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Extended enum i18n conversion beyond billing-api maps**
- **Found during:** Task 2 (CR-19 enum i18n)
- **Issue:** PaymentMethodSelector.tsx also had hardcoded English labels, and InvoiceView/ShiftReportView consumed the old map names
- **Fix:** Updated all consumers to use new i18n key maps, added useTranslation to PaymentMethodSelector
- **Files modified:** PaymentMethodSelector.tsx, InvoiceView.tsx, ShiftReportView.tsx
- **Verification:** TypeScript compiles, no remaining hardcoded enum strings in billing features
- **Committed in:** 02ec963 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Auto-fix necessary for complete i18n coverage. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 8 code review issues (CR-13 through CR-19, CR-22) resolved
- Frontend billing module has full i18n support with proper Vietnamese diacritics
- SignalR connection is stable and won't cause reconnection storms

---
*Phase: 07.2-fix-phase-07-1-code-review-issues*
*Completed: 2026-03-13*
