---
phase: 07.2-fix-phase-07-1-code-review-issues
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Billing/Billing.Application/Features/HandleOtcSaleCompleted.cs
  - backend/src/Modules/Billing/Billing.Domain/Entities/Invoice.cs
  - backend/src/Modules/Billing/Billing.Application/Features/ServiceCatalog/GetServiceCatalogItemByCode.cs
  - backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleDrugDispensed.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleGlassesOrderCreated.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleTreatmentSessionCompleted.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCancelled.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCreated.cs
  - backend/src/Modules/Billing/Billing.Application/Interfaces/IBillingNotificationService.cs
  - backend/tests/Billing.Unit.Tests/Features/IntegrationEventHandlerTests.cs
  - backend/tests/Billing.Unit.Tests/Features/ServiceCatalogHandlerTests.cs
  - backend/tests/Ganka28.ArchitectureTests/ModuleBoundaryTests.cs
autonomous: true
requirements: [CR-07, CR-08, CR-09, CR-12, CR-20, CR-21]

must_haves:
  truths:
    - "Anonymous OTC sales use nullable PatientId instead of Guid.Empty"
    - "GetServiceCatalogItemByCode normalizes code to uppercase before lookup"
    - "Service catalog POST and PUT endpoints require Admin or Manager role"
    - "Zero-price off-catalog dispensing items are skipped or logged, not billed"
    - "All notification try-catch blocks in billing handlers are unconditionally removed (trust interface contract per RESEARCH.md)"
    - "Architecture test assertions use meaningful thresholds"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Application/Features/HandleOtcSaleCompleted.cs"
      provides: "Nullable PatientId for anonymous customers"
    - path: "backend/src/Modules/Billing/Billing.Application/Features/ServiceCatalog/GetServiceCatalogItemByCode.cs"
      provides: "Case-normalized code lookup"
      contains: "ToUpperInvariant"
    - path: "backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs"
      provides: "Role-based auth on service catalog write endpoints"
      contains: "RequireAuthorization"
  key_links:
    - from: "BillingApiEndpoints.MapServiceCatalogEndpoints"
      to: "Authorization policy"
      via: "RequireAuthorization on POST/PUT"
      pattern: "RequireAuthorization"
---

<objective>
Fix remaining backend issues: OTC anonymous patient handling (CR-07), service catalog code normalization (CR-08), endpoint authorization (CR-09), zero-price guard (CR-12), dead code cleanup (CR-20), and architecture test fix (CR-21).

Purpose: These are important-to-low-priority backend fixes that harden the billing module's correctness, security, and code quality.
Output: Fixed handlers, secured endpoints, cleaner code.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.2-fix-phase-07-1-code-review-issues/07.2-CONTEXT.md
@.planning/phases/07.2-fix-phase-07-1-code-review-issues/07.2-RESEARCH.md

<interfaces>
From backend/src/Modules/Billing/Billing.Domain/Entities/Invoice.cs:
```csharp
public Guid PatientId { get; private set; }  // NEEDS: change to Guid?
public static Invoice Create(string invoiceNumber, Guid patientId, ...)
// patientId parameter needs to become Guid?
```

From backend/src/Modules/Billing/Billing.Application/Features/HandleOtcSaleCompleted.cs:
```csharp
// CURRENT: @event.PatientId ?? Guid.Empty  -- WRONG
// FIX: Pass nullable PatientId directly
```

From backend/src/Modules/Billing/Billing.Application/Features/ServiceCatalog/GetServiceCatalogItemByCode.cs:
```csharp
// CURRENT: var item = await repository.GetActiveByCodeAsync(query.Code, ct);
// FIX: var item = await repository.GetActiveByCodeAsync(query.Code.ToUpperInvariant(), ct);
```

From backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs:
```csharp
// Service catalog POST/PUT need .RequireAuthorization("AdminOrManager") or equivalent
// Check existing auth pattern in the project for named policies
```

Handlers with notification try-catch blocks to remove (CR-20):
- HandleDrugDispensed.cs (line 52-60)
- HandleGlassesOrderCreated.cs (line 52-60)
- HandleOtcSaleCompleted.cs (line 50-55)
- HandleTreatmentSessionCompleted.cs (line 69-74)
- HandleVisitCancelled.cs (line 32-36)
- HandleVisitCreated.cs (line 55-60)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix OTC anonymous patient + service catalog code normalization + zero-price guard (CR-07, CR-08, CR-12)</name>
  <files>
    backend/src/Modules/Billing/Billing.Domain/Entities/Invoice.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleOtcSaleCompleted.cs,
    backend/src/Modules/Billing/Billing.Application/Features/ServiceCatalog/GetServiceCatalogItemByCode.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleDrugDispensed.cs,
    backend/tests/Billing.Unit.Tests/Features/IntegrationEventHandlerTests.cs,
    backend/tests/Billing.Unit.Tests/Features/ServiceCatalogHandlerTests.cs
  </files>
  <behavior>
    - Test: HandleOtcSaleCompleted with null PatientId creates invoice with null PatientId (not Guid.Empty)
    - Test: GetServiceCatalogItemByCode with lowercase code finds uppercase-stored item
    - Test: HandleDrugDispensed skips items with zero UnitPrice (no line item added for zero-price items)
  </behavior>
  <action>
1. **Invoice.cs (CR-07):** Change `PatientId` from `Guid` to `Guid?`:
   - `public Guid? PatientId { get; private set; }`
   - Update `Invoice.Create` parameter: `Guid? patientId`
   - Update EF Core config if needed for nullable FK

2. **HandleOtcSaleCompleted.cs (CR-07):** Remove `?? Guid.Empty` fallback:
   ```csharp
   var invoice = Invoice.Create(number, @event.PatientId, @event.CustomerName ?? "Anonymous", null, new BranchId(@event.BranchId));
   ```
   Since PatientId is now Guid?, pass `@event.PatientId` directly (it's already Guid?).

3. **Create EF migration** for nullable PatientId with data fixup:
   ```sql
   UPDATE billing.Invoices SET PatientId = NULL WHERE PatientId = '00000000-0000-0000-0000-000000000000';
   ```

4. **GetServiceCatalogItemByCode.cs (CR-08):** Normalize input:
   ```csharp
   var item = await repository.GetActiveByCodeAsync(query.Code.ToUpperInvariant(), ct);
   ```

5. **HandleDrugDispensed.cs (CR-12):** Add zero-price guard in the foreach loop:
   ```csharp
   foreach (var item in @event.Items)
   {
       if (item.UnitPrice <= 0)
       {
           logger.LogWarning("Skipping zero-price dispensing item {DrugName} on invoice {InvoiceId}", item.DrugName, invoice.Id);
           continue;
       }
       invoice.AddLineItem(...);
   }
   ```
   Apply same guard in HandleGlassesOrderCreated and HandleTreatmentSessionCompleted if applicable.

6. **Tests:** Add/update test cases for all three fixes. Update existing OTC tests to verify null PatientId. Add ServiceCatalog lowercase test. Add zero-price skip test for HandleDrugDispensed.

7. **Check all Invoice.Create callers** for compile errors after PatientId becomes nullable. Update HandleVisitCreated and other handlers if they pass non-nullable Guid (they should still work since Guid is implicitly convertible to Guid?).
  </action>
  <verify>
    <automated>dotnet test backend/tests/Billing.Unit.Tests --no-build -q</automated>
  </verify>
  <done>OTC anonymous uses null PatientId. Code lookup normalizes to uppercase. Zero-price items skipped. Migration applied. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add endpoint authorization + remove notification try-catch blocks + fix architecture test (CR-09, CR-20, CR-21)</name>
  <files>
    backend/src/Modules/Billing/Billing.Presentation/BillingApiEndpoints.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleDrugDispensed.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleGlassesOrderCreated.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleOtcSaleCompleted.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleTreatmentSessionCompleted.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCancelled.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCreated.cs,
    backend/tests/Ganka28.ArchitectureTests/ModuleBoundaryTests.cs
  </files>
  <action>
1. **BillingApiEndpoints.cs (CR-09):** Add role-based authorization to service catalog write endpoints. First check the existing auth pattern:
   - Look at how other modules restrict endpoints (likely `.RequireAuthorization()` with claims)
   - For service catalog POST and PUT, add authorization that requires Admin or Manager permission
   - Check if the project has a named policy like "AdminOrManager" or uses claim-based checks
   - If no named policy exists, use `.RequireAuthorization(policy => policy.RequireRole("Admin", "Manager"))` or the project's existing pattern for role restriction
   - GET endpoints remain accessible to all authenticated users (no change)

2. **Unconditionally remove notification try-catch in ALL billing event handlers (CR-20):**
   In HandleDrugDispensed, HandleGlassesOrderCreated, HandleOtcSaleCompleted, HandleTreatmentSessionCompleted, HandleVisitCancelled, and HandleVisitCreated:
   - Remove the outer `try { await notificationService.Notify*(...); } catch { logger.LogWarning(...); }` blocks
   - Call `await notificationService.Notify*(...)` directly without try-catch
   - Per RESEARCH.md: "remove outer try-catch, trust interface contract." The IBillingNotificationService contract is responsible for its own error resilience. Wolverine's retry policy handles transient failures at the handler level.
   - Do NOT conditionally keep them. The decision is final: remove all notification try-catch blocks.

3. **Architecture test fix (CR-21):** In ModuleBoundaryTests.cs, find the assertion that uses `>= 0` on a count (which is always true). Change to a meaningful threshold:
   - Find the specific test
   - Change assertion from `count >= 0` to either `count > 0` (if it should find types) or remove the assertion and use a different verification approach
   - The goal is to ensure the architecture test actually fails when boundaries are violated
  </action>
  <verify>
    <automated>dotnet test backend/tests/Ganka28.ArchitectureTests --no-build -q && dotnet build backend/src/Modules/Billing/Billing.Presentation/Billing.Presentation.csproj --no-restore -q</automated>
  </verify>
  <done>Service catalog POST/PUT require Admin/Manager role. All 6 handler notification calls have no try-catch wrapper (unconditionally removed). Architecture test assertion is meaningful. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build backend/Ganka28.sln --no-restore -q` compiles
- `dotnet test backend/tests --no-build -q` all tests pass
- Service catalog POST /api/billing/service-catalog returns 403 for non-admin users
- Invoice.PatientId is nullable in entity
- No `try.*notificationService` pattern in any billing handler (grep confirms)
</verification>

<success_criteria>
- CR-07: Anonymous OTC customers use null PatientId, migration applied
- CR-08: GetServiceCatalogItemByCode normalizes to uppercase
- CR-09: Service catalog write endpoints restricted to Admin/Manager
- CR-12: Zero-price items not added to invoices
- CR-20: No redundant try-catch in any billing handler notification calls (unconditionally removed)
- CR-21: Architecture test has meaningful assertion
</success_criteria>

<output>
After completion, create `.planning/phases/07.2-fix-phase-07-1-code-review-issues/07.2-03-SUMMARY.md`
</output>
