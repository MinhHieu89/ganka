---
phase: 06-pharmacy-consumables
plan: 07
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
  - 06-04
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/ISupplierRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IDrugBatchRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IStockImportRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IDispensingRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IOtcSaleRepository.cs
autonomous: true
requirements:
  - PHR-01
  - PHR-05
  - PHR-06
must_haves:
  truths:
    - "Repository interfaces exist for all aggregate roots and queryable entities"
    - "IDrugBatchRepository has methods for FEFO queries and stock level computation"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/ISupplierRepository.cs"
      provides: "Supplier CRUD + SupplierDrugPrice queries"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IDrugBatchRepository.cs"
      provides: "Batch queries for FEFO, expiry alerts, stock levels"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IStockImportRepository.cs"
      provides: "Stock import persistence"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IDispensingRepository.cs"
      provides: "Dispensing record persistence"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IOtcSaleRepository.cs"
      provides: "OTC sale persistence"
  key_links:
    - from: "IDrugBatchRepository"
      to: "DrugBatch"
      via: "FEFO query methods"
      pattern: "GetAvailableBatchesFEFO"
---

<objective>
Create repository interfaces for all pharmacy aggregate roots in the Application layer.

Purpose: Application-layer abstractions for data access. IDrugBatchRepository is critical for FEFO batch selection logic.
Output: 5 new repository interface files.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md

<interfaces>
From IDrugCatalogItemRepository.cs (existing pattern):
```csharp
public interface IDrugCatalogItemRepository
{
    Task<List<DrugCatalogItemDto>> SearchAsync(string searchTerm, CancellationToken ct);
    Task<DrugCatalogItem?> GetByIdAsync(Guid id, CancellationToken ct);
    Task<List<DrugCatalogItem>> GetAllActiveAsync(CancellationToken ct);
    void Add(DrugCatalogItem item);
    void Update(DrugCatalogItem item);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ISupplierRepository, IDrugBatchRepository, IStockImportRepository</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/ISupplierRepository.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IDrugBatchRepository.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IStockImportRepository.cs
  </files>
  <action>
    Follow IDrugCatalogItemRepository pattern:

    **ISupplierRepository.cs**:
    - GetByIdAsync(Guid id, CancellationToken ct) -> Supplier?
    - GetAllActiveAsync(CancellationToken ct) -> List<Supplier>
    - GetSupplierDrugPricesAsync(Guid supplierId, CancellationToken ct) -> List<SupplierDrugPriceDto>
    - GetDefaultPriceAsync(Guid supplierId, Guid drugCatalogItemId, CancellationToken ct) -> decimal?
    - Add(Supplier supplier)
    - void AddSupplierDrugPrice(SupplierDrugPrice price)
    - void UpdateSupplierDrugPrice(SupplierDrugPrice price)

    **IDrugBatchRepository.cs**:
    - GetByIdAsync(Guid id, CancellationToken ct) -> DrugBatch?
    - GetBatchesForDrugAsync(Guid drugCatalogItemId, CancellationToken ct) -> List<DrugBatch> (all batches including empty)
    - GetAvailableBatchesFEFOAsync(Guid drugCatalogItemId, CancellationToken ct) -> List<DrugBatch> (CurrentQuantity > 0, not expired, ordered by ExpiryDate ASC)
    - GetTotalStockAsync(Guid drugCatalogItemId, CancellationToken ct) -> int (sum of CurrentQuantity for available batches)
    - GetExpiryAlertsAsync(int daysThreshold, CancellationToken ct) -> List<ExpiryAlertDto>
    - GetLowStockAlertsAsync(CancellationToken ct) -> List<LowStockAlertDto>
    - Add(DrugBatch batch)
    - void AddStockAdjustment(StockAdjustment adjustment)

    **IStockImportRepository.cs**:
    - GetByIdAsync(Guid id, CancellationToken ct) -> StockImport?
    - GetAllAsync(int page, int pageSize, CancellationToken ct) -> (List<StockImportDto> Items, int TotalCount)
    - Add(StockImport stockImport)
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Application/Pharmacy.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>ISupplierRepository, IDrugBatchRepository, IStockImportRepository interfaces exist with FEFO and alert query methods.</done>
</task>

<task type="auto">
  <name>Task 2: Create IDispensingRepository, IOtcSaleRepository, IConsumableRepository</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IDispensingRepository.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Interfaces/IOtcSaleRepository.cs
  </files>
  <action>
    **IDispensingRepository.cs**:
    - GetByIdAsync(Guid id, CancellationToken ct) -> DispensingRecord?
    - GetByPrescriptionIdAsync(Guid prescriptionId, CancellationToken ct) -> DispensingRecord? (check if already dispensed)
    - GetHistoryAsync(int page, int pageSize, Guid? patientId, CancellationToken ct) -> (List<DispensingRecordDto> Items, int TotalCount)
    - Add(DispensingRecord record)

    **IOtcSaleRepository.cs**:
    - GetByIdAsync(Guid id, CancellationToken ct) -> OtcSale?
    - GetAllAsync(int page, int pageSize, CancellationToken ct) -> (List<OtcSaleDto> Items, int TotalCount)
    - Add(OtcSale sale)

    Also create IConsumableRepository in the same file or separate file. Since we already have 5 files in this plan, include IConsumableRepository in IOtcSaleRepository.cs file as a separate interface, OR create a 6th interface. Since the user asked for max 5 files per plan, keep IConsumableRepository in a separate plan.

    Actually, update Task 1 or Task 2: add IConsumableRepository.cs as a 3rd file in this task.
    Wait -- that would be 5 files total in this plan which is at the limit. Let me include it.

    Also create **IConsumableRepository.cs** (3rd file in this task):
    - GetByIdAsync(Guid id, CancellationToken ct) -> ConsumableItem?
    - GetAllActiveAsync(CancellationToken ct) -> List<ConsumableItem>
    - GetBatchesAsync(Guid consumableItemId, CancellationToken ct) -> List<ConsumableBatch>
    - GetBatchByIdAsync(Guid batchId, CancellationToken ct) -> ConsumableBatch?
    - GetAlertsAsync(CancellationToken ct) -> List<ConsumableItemDto> (items where IsLowStock)
    - Add(ConsumableItem item)
    - void Update(ConsumableItem item)
    - void AddBatch(ConsumableBatch batch)
    - void AddStockAdjustment(StockAdjustment adjustment)
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Application/Pharmacy.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>IDispensingRepository, IOtcSaleRepository, and IConsumableRepository interfaces exist.</done>
</task>

</tasks>

<verification>
All repository interfaces compile. FEFO query method exists on IDrugBatchRepository.
</verification>

<success_criteria>
- 6 repository interfaces defined in Application layer (5 new + 1 existing IDrugCatalogItemRepository)
- IDrugBatchRepository has FEFO and alert query methods
- `dotnet build Pharmacy.Application` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-07-SUMMARY.md`
</output>
