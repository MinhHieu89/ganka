---
phase: 06-pharmacy-consumables
plan: 09
type: execute
wave: 3
depends_on:
  - 06-05b
  - 06-06
  - 06-07
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/ConsumableRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Seeders/ConsumableCatalogSeeder.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/IoC.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugCatalogItemRepository.cs
autonomous: true
requirements:
  - CON-01
  - CON-02
  - PHR-01
must_haves:
  truths:
    - "ConsumableRepository implements IConsumableRepository with batch queries and alerts"
    - "ConsumableCatalogSeeder seeds 10-15 IPL/LLLT supplies with Vietnamese names"
    - "Pharmacy.Infrastructure IoC registers all repositories and unit of work"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/ConsumableRepository.cs"
      provides: "ConsumableItem/Batch CRUD and alert queries"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Seeders/ConsumableCatalogSeeder.cs"
      provides: "IHostedService seeding 10-15 consumable items"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/IoC.cs"
      provides: "DI registration for all pharmacy repositories"
  key_links:
    - from: "IoC.AddPharmacyInfrastructure"
      to: "All repository implementations"
      via: "services.AddScoped"
      pattern: "AddScoped<I.*Repository"
---

<objective>
Implement ConsumableRepository, ConsumableCatalogSeeder, Pharmacy.Infrastructure IoC, and update DrugCatalogItemRepository.

Purpose: Complete infrastructure layer with consumable data access, seed data, and DI registration.
Output: 3 new files + 1 updated file.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-CONTEXT.md
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md

<interfaces>
From Clinical.Infrastructure/IoC.cs (pattern):
```csharp
public static class IoC
{
    public static IServiceCollection AddClinicalInfrastructure(this IServiceCollection services)
    {
        services.AddScoped<IVisitRepository, VisitRepository>();
        services.AddScoped<IUnitOfWork, UnitOfWork>();
        return services;
    }
}
```

From Patient.Infrastructure/Seeders/AllergyCatalogSeeder.cs (pattern):
```csharp
public sealed class AllergyCatalogSeeder : IHostedService
{
    // Idempotent seeding: check if data exists, skip if so
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ConsumableRepository and ConsumableCatalogSeeder</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/ConsumableRepository.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Seeders/ConsumableCatalogSeeder.cs
  </files>
  <action>
    **ConsumableRepository.cs**: Implements IConsumableRepository.
    - GetAllActiveAsync: WHERE IsActive, ordered by Name
    - GetBatchesAsync: WHERE ConsumableItemId matches, ordered by ExpiryDate
    - GetAlertsAsync: For SimpleStock items: WHERE CurrentStock < MinStockLevel AND MinStockLevel > 0. For ExpiryTracked items: compute sum of batch CurrentQuantity and compare to MinStockLevel.

    **ConsumableCatalogSeeder.cs**: IHostedService following AllergyCatalogSeeder pattern.
    - Idempotent: check if ConsumableItems table has data, skip if so
    - Seed ~12 core IPL/LLLT/treatment supplies with proper Vietnamese diacritics:
      1. IPL gel / Gel IPL (Tube, SimpleStock)
      2. Eye shields / Kính chắn mắt (Piece, SimpleStock)
      3. LLLT disposable tips / Đầu LLLT dùng một lần (Piece, SimpleStock)
      4. Lid care pads / Miếng vệ sinh mi mắt (Piece, SimpleStock)
      5. Sterile wipes / Khăn lau vô trùng (Piece, SimpleStock)
      6. Anesthetic drops / Thuốc nhỏ tê (Bottle, ExpiryTracked)
      7. Fluorescein strips / Giấy thử fluorescein (Box, ExpiryTracked)
      8. Saline solution / Nước muối sinh lý (Bottle, ExpiryTracked)
      9. Cotton applicators / Que bông tăm (Bag, SimpleStock)
      10. Disposable gloves / Găng tay dùng một lần (Box, SimpleStock)
      11. MGD expression forceps tips / Đầu kẹp nặn tuyến Meibomian (Piece, SimpleStock)
      12. Thermal pulsation eyecups / Cốc mắt nhiệt xung (Piece, SimpleStock)
    - All items seeded with MinStockLevel = 10, CurrentStock = 0 (no initial stock)
    - Use BranchId.Default or a fixed test branch ID
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Pharmacy.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>ConsumableRepository implements all interface methods. Seeder seeds 12 consumable items with Vietnamese diacritics.</done>
</task>

<task type="auto">
  <name>Task 2: Create Pharmacy IoC and update DrugCatalogItemRepository</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/IoC.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugCatalogItemRepository.cs
  </files>
  <action>
    **IoC.cs**: Create AddPharmacyInfrastructure extension method registering all repositories:
    - services.AddScoped<IDrugCatalogItemRepository, DrugCatalogItemRepository>()
    - services.AddScoped<ISupplierRepository, SupplierRepository>()
    - services.AddScoped<IDrugBatchRepository, DrugBatchRepository>()
    - services.AddScoped<IStockImportRepository, StockImportRepository>()
    - services.AddScoped<IDispensingRepository, DispensingRepository>()
    - services.AddScoped<IOtcSaleRepository, OtcSaleRepository>()
    - services.AddScoped<IConsumableRepository, ConsumableRepository>()
    - services.AddScoped<IUnitOfWork, UnitOfWork>()
    NOTE: Check if UnitOfWork already exists. If not, create it in Repositories/ following Clinical.Infrastructure pattern.

    **Update DrugCatalogItemRepository.cs**:
    - Add GetAllWithInventoryAsync method returning DrugInventoryDto list (joins with DrugBatches to compute TotalStock, BatchCount, IsLowStock, HasExpiryAlert)
    - Add SearchAsync overload or update existing to include SellingPrice and MinStockLevel in the DTO projection
    - Add GetByIdWithPricingAsync if needed for inventory management views
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Pharmacy.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>IoC registers all pharmacy repositories. DrugCatalogItemRepository has inventory-aware queries.</done>
</task>

</tasks>

<verification>
Infrastructure layer complete. All repositories registered in IoC. ConsumableCatalogSeeder compiles.
</verification>

<success_criteria>
- ConsumableRepository, ConsumableCatalogSeeder, IoC created
- DrugCatalogItemRepository updated with inventory queries
- `dotnet build Pharmacy.Infrastructure` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-09-SUMMARY.md`
</output>
