---
phase: 06-pharmacy-consumables
plan: 22
type: execute
wave: 7
depends_on:
  - 06-19
files_modified:
  - frontend/src/features/pharmacy/components/PharmacyQueueTable.tsx
  - frontend/src/features/pharmacy/components/DispensingDialog.tsx
  - frontend/src/app/routes/_authenticated/pharmacy/queue.tsx
  - frontend/src/features/patient/components/PatientProfilePage.tsx
  - frontend/src/features/pharmacy/components/PatientPrescriptionsTab.tsx
autonomous: true
requirements:
  - PHR-05
  - PHR-07
must_haves:
  truths:
    - "Pharmacy queue page shows pending prescriptions sorted by time"
    - "Dispensing dialog shows prescription items with FEFO batch suggestions"
    - "7-day expiry warning banner appears for expired prescriptions with override option"
    - "Dispensing is accessible from patient profile page via Prescriptions tab"
  artifacts:
    - path: "frontend/src/features/pharmacy/components/PharmacyQueueTable.tsx"
      provides: "Pending prescriptions table with patient info and time"
    - path: "frontend/src/features/pharmacy/components/DispensingDialog.tsx"
      provides: "Dispensing confirmation dialog with FEFO batch display"
    - path: "frontend/src/app/routes/_authenticated/pharmacy/queue.tsx"
      provides: "Pharmacy queue page route"
    - path: "frontend/src/features/pharmacy/components/PatientPrescriptionsTab.tsx"
      provides: "Patient profile tab showing prescriptions with dispense shortcut"
    - path: "frontend/src/features/patient/components/PatientProfilePage.tsx"
      provides: "Updated patient profile with Prescriptions tab"
  key_links:
    - from: "PharmacyQueueTable"
      to: "usePendingPrescriptions hook"
      via: "TanStack Query"
      pattern: "usePendingPrescriptions"
    - from: "DispensingDialog"
      to: "useDispenseDrugs mutation"
      via: "form submit"
      pattern: "useDispenseDrugs"
    - from: "PatientPrescriptionsTab"
      to: "usePendingPrescriptions hook"
      via: "filtered by patientId"
      pattern: "usePendingPrescriptions.*patientId"
---

<objective>
Create the pharmacy dispensing queue page, dispensing confirmation dialog, and patient profile prescriptions tab.

Purpose: Core pharmacist workflow - view pending prescriptions, select one, confirm dispensing with FEFO batch preview. Per locked user decision, dispensing must also be accessible from the patient profile page.
Output: 4 new components + 1 route file + 1 updated component.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-CONTEXT.md
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md

<interfaces>
From frontend/src/features/patient/components/PatientProfilePage.tsx (existing tabs structure):
```typescript
// Patient profile uses Tabs with TabsList/TabsTrigger/TabsContent
// Currently has: overview, allergies, appointments, dry-eye tabs
<Tabs defaultValue="overview">
  <TabsList>
    <TabsTrigger value="overview">{t("overview")}</TabsTrigger>
    <TabsTrigger value="allergies">{t("allergies")}</TabsTrigger>
    <TabsTrigger value="appointments">{t("appointments")}</TabsTrigger>
    <TabsTrigger value="dry-eye">{tClinical("dryEye.tab")}</TabsTrigger>
  </TabsList>
  // ... TabsContent for each
</Tabs>
```

From pharmacy-queries.ts (Plan 19):
```typescript
usePendingPrescriptions() // can accept optional patientId filter
useDispenseDrugs() // mutation for dispensing
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pharmacy queue table and dispensing dialog</name>
  <files>
    frontend/src/features/pharmacy/components/PharmacyQueueTable.tsx,
    frontend/src/features/pharmacy/components/DispensingDialog.tsx
  </files>
  <action>
    **PharmacyQueueTable.tsx**:
    - DataTable showing pending prescriptions from usePendingPrescriptions()
    - Accept optional prop `patientId?: string` to filter prescriptions for a specific patient (used by PatientPrescriptionsTab)
    - Columns: Patient Name, Prescription Code, Prescribed At, Items Count, Days Remaining, Status
    - Sort by PrescribedAt ascending (oldest first = most urgent)
    - Row with IsExpired=true shows destructive badge "Het han" (Expired)
    - Row with DaysRemaining <= 2 shows warning badge
    - Click row opens DispensingDialog
    - Optional PatientId filter (search by patient) -- when patientId prop is provided, pre-filter to that patient

    **DispensingDialog.tsx**:
    - Full-width Dialog (or Sheet) showing prescription details
    - Patient info header: name, prescription code, prescribed date
    - **7-day expiry warning**: If prescription is expired (>7 days), show destructive Alert banner at top with text "Don thuoc da qua 7 ngay" and require Override Reason textarea to enable Dispense button (per PHR-07)
    - Items table: Drug Name, Quantity, Unit, Dosage, Status
    - For each catalog-linked item: show FEFO batch suggestion (batch number, qty available, expiry date)
    - Per-line actions: "Pha che" (Dispense) or "Bo qua" (Skip) toggle
    - Off-catalog items auto-marked as Skipped (no stock deduction)
    - Manual batch override: allow pharmacist to change suggested batch allocation
    - "Xac nhan pha che" (Confirm Dispense) button -> useDispenseDrugs mutation
    - Success: toast + close dialog + invalidate cache
    - Loading state while dispensing
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>PharmacyQueueTable (with optional patientId filter) and DispensingDialog with FEFO display and 7-day expiry override.</done>
</task>

<task type="auto">
  <name>Task 2: Create pharmacy queue route and patient profile prescriptions tab</name>
  <files>
    frontend/src/app/routes/_authenticated/pharmacy/queue.tsx,
    frontend/src/features/pharmacy/components/PatientPrescriptionsTab.tsx,
    frontend/src/features/patient/components/PatientProfilePage.tsx
  </files>
  <action>
    **queue.tsx** route:
    - Page title: "Hang doi pha che" / "Dispensing Queue"
    - PharmacyQueueTable as main content (no patientId prop -- shows all patients)
    - Count badge at top showing total pending prescriptions
    - Filter section: Patient search, date range
    - Auto-refresh note: "Tu dong cap nhat moi 30 giay" (Auto-refreshes every 30 seconds) since usePendingPrescriptions has refetchInterval
    - createFileRoute for TanStack Router

    **PatientPrescriptionsTab.tsx**:
    - Accepts prop `patientId: string`
    - Shows PharmacyQueueTable filtered by patientId (passes patientId prop to PharmacyQueueTable)
    - Additionally shows a "View full queue" link to /pharmacy/queue?patientId={patientId} for pharmacist to see full context
    - Shows dispensing history for this patient using useDispensingHistory with patientId filter
    - Two sections: "Pending Prescriptions" (top) and "Dispensing History" (bottom, collapsible)
    - Per locked user decision: "dispensing accessible from patient profile"

    **PatientProfilePage.tsx** (UPDATE existing file):
    - Import PatientPrescriptionsTab from pharmacy feature
    - Import useTranslation("pharmacy") for the tab label
    - Add new TabsTrigger: value="prescriptions", label from pharmacy i18n: t("prescriptions.tab") or "Don thuoc" / "Prescriptions"
    - Add new TabsContent: value="prescriptions" rendering PatientPrescriptionsTab with patientId={patient.id}
    - Place the prescriptions tab after "appointments" tab and before "dry-eye" tab
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Pharmacy queue route renders with pending prescriptions table. Patient profile page has Prescriptions tab with dispensing access per locked user decision.</done>
</task>

</tasks>

<verification>
TypeScript compiles. Queue page shows pending prescriptions. Dispensing dialog handles FEFO and expiry override. Patient profile has Prescriptions tab linking to dispensing.
</verification>

<success_criteria>
- Queue table sorted by prescription time (oldest first)
- Dispensing dialog with FEFO batch suggestions
- 7-day expiry warning with override reason requirement
- Skip/dispense per-line controls
- Patient profile page has "Prescriptions" tab showing pending prescriptions and dispensing history for that patient (per locked decision: "dispensing accessible from patient profile")
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-22-SUMMARY.md`
</output>
