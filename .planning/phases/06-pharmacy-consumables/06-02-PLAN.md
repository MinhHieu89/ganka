---
phase: 06-pharmacy-consumables
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockImport.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockImportLine.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockAdjustment.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/StockAdjustmentReason.cs
autonomous: true
requirements:
  - PHR-02
must_haves:
  truths:
    - "StockImport aggregate exists to track each stock import event from supplier invoice or Excel"
    - "StockImportLine child entity exists with drug, batch, quantity, and price details"
    - "StockAdjustment entity exists for manual corrections with reason tracking"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockImport.cs"
      provides: "StockImport AggregateRoot with ImportSource, supplier ref, lines collection"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockImportLine.cs"
      provides: "StockImportLine Entity child of StockImport"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockAdjustment.cs"
      provides: "StockAdjustment Entity for manual corrections with reason"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/StockAdjustmentReason.cs"
      provides: "StockAdjustmentReason enum"
  key_links:
    - from: "StockImportLine"
      to: "StockImport"
      via: "StockImportId FK"
      pattern: "StockImportId"
---

<objective>
Create stock import and stock adjustment domain entities for tracking inventory changes.

Purpose: Every stock change must have an auditable entity record. StockImport tracks supplier imports, StockAdjustment tracks manual corrections.
Output: 3 domain entity files + 1 enum file.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pharmacy-consumables/06-CONTEXT.md
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StockImport, StockImportLine entities</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockImport.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockImportLine.cs
  </files>
  <action>
    **StockImport.cs** (AggregateRoot, IAuditable):
    - Properties: SupplierId (Guid), SupplierName (string, denormalized), ImportSource (ImportSource enum), InvoiceNumber (string?), ImportedById (Guid), ImportedAt (DateTime), Notes (string?)
    - Backing field: _lines (List of StockImportLine), public IReadOnlyCollection Lines
    - Use PropertyAccessMode.Field for _lines (same pattern as Visit.Amendments)
    - Factory: static Create(supplierId, supplierName, importSource, invoiceNumber, importedById, notes, branchId)
    - Method: AddLine(drugCatalogItemId, drugName, batchNumber, expiryDate, quantity, purchasePrice) -- creates and adds StockImportLine to _lines

    **StockImportLine.cs** (Entity):
    - Properties: StockImportId (Guid), DrugCatalogItemId (Guid), DrugName (string, denormalized), BatchNumber (string), ExpiryDate (DateOnly), Quantity (int), PurchasePrice (decimal)
    - Factory: static Create(stockImportId, drugCatalogItemId, drugName, batchNumber, expiryDate, quantity, purchasePrice)
    - Domain guard: quantity must be positive, expiryDate must be future
    - Private constructor for EF Core
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Domain/Pharmacy.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>StockImport and StockImportLine entities compile with AddLine method and domain guards.</done>
</task>

<task type="auto">
  <name>Task 2: Create StockAdjustment entity and StockAdjustmentReason enum</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/StockAdjustment.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/StockAdjustmentReason.cs
  </files>
  <action>
    **StockAdjustmentReason.cs** enum: Correction = 0, WriteOff = 1, Damage = 2, Expired = 3, Other = 4

    **StockAdjustment.cs** (Entity, IAuditable):
    - Properties: DrugBatchId (Guid), QuantityChange (int -- positive for additions, negative for removals), Reason (StockAdjustmentReason), Notes (string?), AdjustedById (Guid), AdjustedAt (DateTime)
    - Factory: static Create(drugBatchId, quantityChange, reason, notes, adjustedById)
    - Domain guard: quantityChange must not be zero
    - Private constructor for EF Core
    - NOTE: StockAdjustment is generic -- also used for ConsumableBatch adjustments in Plan 03. Add ConsumableBatchId (Guid?) to support both. One of DrugBatchId or ConsumableBatchId will be set.
    - Actually, make DrugBatchId nullable too: DrugBatchId (Guid?), ConsumableBatchId (Guid?). Domain guard: exactly one must be non-null.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Domain/Pharmacy.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>StockAdjustment entity with dual FK support (drug batch or consumable batch) and StockAdjustmentReason enum exist.</done>
</task>

</tasks>

<verification>
All new entities compile. Domain guards enforce positive quantities and non-zero adjustments.
</verification>

<success_criteria>
- StockImport aggregate with StockImportLine children exists
- StockAdjustment with dual FK support exists
- StockAdjustmentReason enum exists
- `dotnet build Pharmacy.Domain` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-02-SUMMARY.md`
</output>
