---
phase: 06-pharmacy-consumables
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/DispensingRecord.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/DispensingLine.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/BatchDeduction.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/DispensingStatus.cs
autonomous: true
requirements:
  - PHR-05
  - PHR-07
must_haves:
  truths:
    - "DispensingRecord aggregate exists linking pharmacist to prescription with override reason for expired Rx"
    - "DispensingLine child entity exists for each drug line with dispensing status (Dispensed/Skipped)"
    - "BatchDeduction entity exists linking dispensing line to specific batch quantities"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/DispensingRecord.cs"
      provides: "DispensingRecord AggregateRoot with Lines collection"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/DispensingLine.cs"
      provides: "DispensingLine Entity with BatchDeductions collection"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/BatchDeduction.cs"
      provides: "BatchDeduction Entity linking line to batch with quantity"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/DispensingStatus.cs"
      provides: "DispensingStatus enum"
  key_links:
    - from: "DispensingLine"
      to: "DispensingRecord"
      via: "DispensingRecordId FK"
      pattern: "DispensingRecordId"
    - from: "BatchDeduction"
      to: "DispensingLine"
      via: "DispensingLineId FK"
      pattern: "DispensingLineId"
---

<objective>
Create dispensing domain entities: DispensingRecord (aggregate), DispensingLine, and BatchDeduction for tracking drug dispensing against HIS prescriptions.

Purpose: Models the core dispensing workflow where prescriptions from Clinical module are dispensed by pharmacist with FEFO batch selection.
Output: 3 domain entity files + 1 enum file.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DispensingStatus enum and BatchDeduction entity</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/DispensingStatus.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/BatchDeduction.cs
  </files>
  <action>
    **DispensingStatus.cs** enum: Dispensed = 0, Skipped = 1

    **BatchDeduction.cs** (Entity):
    - Properties: DispensingLineId (Guid?), OtcSaleLineId (Guid?), DrugBatchId (Guid), BatchNumber (string, denormalized), Quantity (int)
    - Make both DispensingLineId and OtcSaleLineId nullable so the same entity is reused for OTC sale batch deductions (Plan 04). Exactly one parent FK should be non-null.
    - Factory: static CreateForDispensing(dispensingLineId, drugBatchId, batchNumber, quantity)
    - Factory: static CreateForOtcSale(otcSaleLineId, drugBatchId, batchNumber, quantity)
    - Domain guard: quantity must be positive
    - Private constructor for EF Core
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Domain/Pharmacy.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>DispensingStatus enum and BatchDeduction entity with dual-parent FK support exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Create DispensingRecord and DispensingLine entities</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/DispensingRecord.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/DispensingLine.cs
  </files>
  <action>
    **DispensingRecord.cs** (AggregateRoot, IAuditable):
    - Properties: PrescriptionId (Guid, from Clinical DrugPrescription.Id), VisitId (Guid), PatientId (Guid), PatientName (string, denormalized), DispensedById (Guid), DispensedAt (DateTime), OverrideReason (string?, for expired Rx override per PHR-07)
    - Backing field: _lines (List of DispensingLine), public IReadOnlyCollection Lines
    - Factory: static Create(prescriptionId, visitId, patientId, patientName, dispensedById, overrideReason, branchId)
    - Method: AddLine(prescriptionItemId, drugCatalogItemId, drugName, quantity, status) -- creates DispensingLine and adds to _lines

    **DispensingLine.cs** (Entity):
    - Properties: DispensingRecordId (Guid), PrescriptionItemId (Guid, from Clinical PrescriptionItem.Id), DrugCatalogItemId (Guid), DrugName (string, denormalized), Quantity (int), Status (DispensingStatus)
    - Backing field: _batchDeductions (List of BatchDeduction), public IReadOnlyCollection BatchDeductions
    - Method: AddBatchDeduction(drugBatchId, batchNumber, quantity) -- creates BatchDeduction and adds to _batchDeductions
    - Private constructor for EF Core
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Domain/Pharmacy.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>DispensingRecord aggregate with DispensingLine children and BatchDeduction grandchildren compiles. All-or-nothing per line enforced by DispensingStatus.</done>
</task>

</tasks>

<verification>
All dispensing entities compile. Domain hierarchy: DispensingRecord -> DispensingLine -> BatchDeduction.
</verification>

<success_criteria>
- DispensingRecord, DispensingLine, BatchDeduction entities exist with proper hierarchy
- BatchDeduction supports both dispensing and OTC parent FKs
- DispensingStatus enum exists
- `dotnet build Pharmacy.Domain` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-03-SUMMARY.md`
</output>
