---
phase: 06-pharmacy-consumables
plan: 29
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/pharmacy/api/pharmacy-api.ts
  - frontend/src/features/pharmacy/components/DrugInventoryTable.tsx
  - frontend/src/app/routes/_authenticated/pharmacy/index.tsx
  - frontend/public/locales/en/pharmacy.json
  - frontend/public/locales/vi/pharmacy.json
autonomous: true
gap_closure: true
requirements: [PHR-01, PHR-02]
must_haves:
  truths:
    - "Clicking expand on a drug row triggers GET /inventory/{drugCatalogItemId}/batches and shows batch details in nested table"
    - "English mode shows all labels in English on the pharmacy inventory page, including subtitle and action buttons"
  artifacts:
    - path: "frontend/src/features/pharmacy/api/pharmacy-api.ts"
      provides: "DrugInventoryDto with drugCatalogItemId field matching backend"
      contains: "drugCatalogItemId"
    - path: "frontend/src/features/pharmacy/components/DrugInventoryTable.tsx"
      provides: "Expand row passing drugCatalogItemId to DrugBatchTable"
      contains: "drug.drugCatalogItemId"
    - path: "frontend/src/app/routes/_authenticated/pharmacy/index.tsx"
      provides: "Fully translated pharmacy page with no hardcoded Vietnamese"
      contains: "t(\"inventory."
    - path: "frontend/public/locales/en/pharmacy.json"
      provides: "English translations for subtitle and action buttons"
      contains: "manageSuppliers"
  key_links:
    - from: "frontend/src/features/pharmacy/components/DrugInventoryTable.tsx"
      to: "frontend/src/features/pharmacy/components/DrugBatchTable.tsx"
      via: "drugId prop from drug.drugCatalogItemId"
      pattern: "drugId=\\{drug\\.drugCatalogItemId\\}"
    - from: "frontend/src/features/pharmacy/components/DrugBatchTable.tsx"
      to: "/api/pharmacy/inventory/{drugId}/batches"
      via: "useDrugBatches(drugId) with enabled: !!drugId"
      pattern: "useDrugBatches\\(drugId\\)"
---

<objective>
Fix two UAT gaps from Phase 6 verification:
1. (Major) Drug batch expand shows "No batches" because frontend DrugInventoryDto.id does not match backend's drugCatalogItemId field, causing useDrugBatches to never fire
2. (Minor) Pharmacy inventory page has 3 hardcoded Vietnamese strings that stay Vietnamese in English mode

Purpose: Close the final 2 UAT issues (tests 17 and 24) to complete Phase 6
Output: Working batch expand + fully bilingual pharmacy page
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/features/pharmacy/api/pharmacy-api.ts
@frontend/src/features/pharmacy/api/pharmacy-queries.ts
@frontend/src/features/pharmacy/components/DrugInventoryTable.tsx
@frontend/src/features/pharmacy/components/DrugBatchTable.tsx
@frontend/src/app/routes/_authenticated/pharmacy/index.tsx
@frontend/public/locales/en/pharmacy.json
@frontend/public/locales/vi/pharmacy.json

<interfaces>
<!-- Backend DrugInventoryDto (from Pharmacy.Contracts) sends these fields: -->
<!-- DrugCatalogItemId (Guid), Name, NameVi, GenericName, Form, Strength, Unit, -->
<!-- SellingPrice, MinStockLevel, TotalStock, IsLowStock, HasExpiryAlert -->
<!-- NOTE: The ID field is "DrugCatalogItemId", NOT "Id" -->

<!-- Backend batch endpoint: GET /api/pharmacy/inventory/{drugId:guid}/batches -->
<!-- Backend pricing endpoint: PUT /api/pharmacy/inventory/{drugId:guid}/pricing -->

<!-- Frontend useDrugBatches hook (pharmacy-queries.ts line 101-107): -->
<!-- enabled: !!drugId — if drugId is undefined/falsy, query never fires -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix DrugInventoryDto field mismatch and update all references</name>
  <files>
    frontend/src/features/pharmacy/api/pharmacy-api.ts
    frontend/src/features/pharmacy/components/DrugInventoryTable.tsx
  </files>
  <action>
    **Root cause:** Backend `DrugInventoryDto` record has `DrugCatalogItemId` as the identifier field (camelCase in JSON: `drugCatalogItemId`). Frontend `DrugInventoryDto` interface declares `id: string` which never gets populated — the JSON key `drugCatalogItemId` goes unmapped.

    **Fix in `pharmacy-api.ts`:**
    1. In the `DrugInventoryDto` interface (line 50-63), rename `id: string` to `drugCatalogItemId: string`

    **Fix in `DrugInventoryTable.tsx`:**
    1. Line 82: Change `drugId: drug.id` to `drugId: drug.drugCatalogItemId` (inside `openPricingDialog` / `handleSubmit` for updateDrugPricing mutation)
    2. Line 336: Change `drugId={drug.id}` to `drugId={drug.drugCatalogItemId}` (the prop passed to `DrugBatchTable`)

    Do NOT touch `DrugCatalogItemDto.id` — that is a different DTO with a real `Id` field from the catalog entity. Only `DrugInventoryDto` has the mismatch.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - DrugInventoryDto uses `drugCatalogItemId` matching backend JSON
    - DrugInventoryTable passes `drug.drugCatalogItemId` to DrugBatchTable and to updateDrugPricing
    - TypeScript compiles without errors
    - Expanding a drug row will now pass a valid GUID to useDrugBatches, which triggers the API call
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded Vietnamese strings with i18n translation keys</name>
  <files>
    frontend/src/app/routes/_authenticated/pharmacy/index.tsx
    frontend/public/locales/en/pharmacy.json
    frontend/public/locales/vi/pharmacy.json
  </files>
  <action>
    **Root cause:** Three strings in `pharmacy/index.tsx` are hardcoded Vietnamese instead of using `t()` translation calls.

    **Fix in `pharmacy/index.tsx`:**
    1. Line 26: Replace hardcoded `Quản lý kho thuốc và theo dõi tồn kho` with `{t("inventory.subtitle")}`
    2. Line 33: Replace hardcoded `Quản lý nhà cung cấp` with `{t("supplier.manageLink")}`
    3. Line 39: Replace hardcoded `Nhập kho` with `{t("stockImport.importLink")}`

    **Fix in `frontend/public/locales/en/pharmacy.json`:**
    Add these new keys:
    - Inside the `inventory` object, add: `"subtitle": "Manage drug inventory and track stock levels"`
    - Inside the `supplier` object, add: `"manageLink": "Manage Suppliers"`
    - Inside the `stockImport` object, add: `"importLink": "Stock Import"`

    **Fix in `frontend/public/locales/vi/pharmacy.json`:**
    Add matching keys with proper Vietnamese diacritics:
    - Inside the `inventory` object, add: `"subtitle": "Quản lý kho thuốc và theo dõi tồn kho"`
    - Inside the `supplier` object, add: `"manageLink": "Quản lý nhà cung cấp"`
    - Inside the `stockImport` object, add: `"importLink": "Nhập kho"`
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - No hardcoded Vietnamese strings remain in pharmacy/index.tsx
    - English locale file has subtitle, manageLink, importLink keys
    - Vietnamese locale file has matching keys with proper diacritics
    - Switching to English shows all labels translated
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit` (zero errors)
2. Grep confirmation for gap 1: `grep -n "drugCatalogItemId" frontend/src/features/pharmacy/api/pharmacy-api.ts` shows the field in DrugInventoryDto
3. Grep confirmation for gap 1: `grep -n "drug.drugCatalogItemId" frontend/src/features/pharmacy/components/DrugInventoryTable.tsx` shows both usages
4. Grep confirmation for gap 2: `grep -c "Quản lý\|Nhập kho" frontend/src/app/routes/_authenticated/pharmacy/index.tsx` returns 0 (no hardcoded Vietnamese)
5. Grep confirmation for gap 2: `grep "manageLink\|subtitle\|importLink" frontend/public/locales/en/pharmacy.json` shows all 3 new keys
</verification>

<success_criteria>
- DrugInventoryDto.drugCatalogItemId field matches backend JSON serialization
- Expanding a drug row in the inventory table passes a valid GUID to DrugBatchTable, enabling the useDrugBatches query to fire
- All pharmacy inventory page text uses t() translation function with no hardcoded strings
- Both EN and VI locale files have complete translation coverage for the inventory page
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-29-SUMMARY.md`
</output>
