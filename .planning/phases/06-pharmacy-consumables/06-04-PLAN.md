---
phase: 06-pharmacy-consumables
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/OtcSale.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/OtcSaleLine.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/ConsumableItem.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/ConsumableBatch.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/ConsumableTrackingMode.cs
autonomous: true
requirements:
  - PHR-06
  - CON-01
  - CON-02
must_haves:
  truths:
    - "OtcSale aggregate exists for walk-in OTC sales with optional patient linkage"
    - "OtcSaleLine child entity exists with drug reference and selling price snapshot"
    - "ConsumableItem aggregate exists with configurable tracking mode (expiry-tracked or simple stock)"
    - "ConsumableBatch entity exists for expiry-tracked consumables"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/OtcSale.cs"
      provides: "OtcSale AggregateRoot with optional PatientId"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/OtcSaleLine.cs"
      provides: "OtcSaleLine Entity with drug reference and price"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/ConsumableItem.cs"
      provides: "ConsumableItem AggregateRoot with tracking mode"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/ConsumableBatch.cs"
      provides: "ConsumableBatch Entity for expiry-tracked items"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/ConsumableTrackingMode.cs"
      provides: "ConsumableTrackingMode enum"
  key_links:
    - from: "OtcSaleLine"
      to: "OtcSale"
      via: "OtcSaleId FK"
      pattern: "OtcSaleId"
    - from: "ConsumableBatch"
      to: "ConsumableItem"
      via: "ConsumableItemId FK"
      pattern: "ConsumableItemId"
---

<objective>
Create OTC sale entities and consumables warehouse domain entities.

Purpose: OtcSale models walk-in pharmacy sales without prescription. ConsumableItem/ConsumableBatch model the separate consumables warehouse for treatment supplies.
Output: 4 domain entity files + 1 enum file.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-pharmacy-consumables/06-CONTEXT.md
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OtcSale and OtcSaleLine entities</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/OtcSale.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/OtcSaleLine.cs
  </files>
  <action>
    Quick Sale form approach per Claude's discretion (not mini-prescription pipeline). OTC sales don't need prescription lifecycle.

    **OtcSale.cs** (AggregateRoot, IAuditable):
    - Properties: PatientId (Guid?, optional walk-in customer), CustomerName (string?, for anonymous sales), SoldById (Guid), SoldAt (DateTime), Notes (string?)
    - Backing field: _lines (List of OtcSaleLine), public IReadOnlyCollection Lines
    - Factory: static Create(patientId, customerName, soldById, notes, branchId)
    - Method: AddLine(drugCatalogItemId, drugName, quantity, unitPrice) -- creates OtcSaleLine, adds to _lines
    - Domain guard in Create: either PatientId or CustomerName must be provided (or both null for truly anonymous)

    **OtcSaleLine.cs** (Entity):
    - Properties: OtcSaleId (Guid), DrugCatalogItemId (Guid), DrugName (string, denormalized), Quantity (int), UnitPrice (decimal, selling price snapshot at time of sale)
    - Backing field: _batchDeductions (List of BatchDeduction), public IReadOnlyCollection BatchDeductions
    - Method: AddBatchDeduction(drugBatchId, batchNumber, quantity) -- creates BatchDeduction via CreateForOtcSale factory
    - Domain guard: quantity > 0, unitPrice >= 0
    - Private constructor for EF Core
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Domain/Pharmacy.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>OtcSale aggregate with OtcSaleLine children compiles. Quick Sale model with optional customer linkage.</done>
</task>

<task type="auto">
  <name>Task 2: Create ConsumableItem, ConsumableBatch entities and ConsumableTrackingMode enum</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/ConsumableItem.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Entities/ConsumableBatch.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Enums/ConsumableTrackingMode.cs
  </files>
  <action>
    **ConsumableTrackingMode.cs** enum: ExpiryTracked = 0, SimpleStock = 1

    **ConsumableItem.cs** (AggregateRoot, IAuditable):
    - Properties: Name (string), NameVi (string), Unit (string, e.g. "Tube", "Piece"), TrackingMode (ConsumableTrackingMode), CurrentStock (int, for SimpleStock mode), MinStockLevel (int, default 0), IsActive (bool, default true)
    - Factory: static Create(name, nameVi, unit, trackingMode, minStockLevel, branchId)
    - Methods: Update(name, nameVi, unit, trackingMode, minStockLevel), Deactivate(), Activate()
    - For SimpleStock mode: AddStock(int qty), RemoveStock(int qty) -- guards against negative stock
    - CurrentStock is only used for SimpleStock mode. For ExpiryTracked mode, stock is computed from ConsumableBatch.CurrentQuantity sum.

    **ConsumableBatch.cs** (Entity):
    - Properties: ConsumableItemId (Guid), BatchNumber (string), ExpiryDate (DateOnly), InitialQuantity (int), CurrentQuantity (int), RowVersion (byte[])
    - Factory: static Create(consumableItemId, batchNumber, expiryDate, quantity)
    - Methods: Deduct(int qty), IsExpired, IsNearExpiry(int daysThreshold)
    - Same pattern as DrugBatch
    - Private constructor for EF Core
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Domain/Pharmacy.Domain.csproj --no-restore -v q</automated>
  </verify>
  <done>ConsumableItem with dual tracking modes and ConsumableBatch with FEFO support compile. ConsumableTrackingMode enum exists.</done>
</task>

</tasks>

<verification>
All OTC and consumable entities compile. Domain guards enforce stock invariants.
</verification>

<success_criteria>
- OtcSale with lines and batch deductions exists
- ConsumableItem supports both ExpiryTracked and SimpleStock modes
- ConsumableBatch mirrors DrugBatch pattern
- `dotnet build Pharmacy.Domain` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-04-SUMMARY.md`
</output>
