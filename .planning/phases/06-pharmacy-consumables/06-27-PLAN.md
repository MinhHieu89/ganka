---
phase: 06-pharmacy-consumables
plan: 27
type: execute
wave: 9
depends_on:
  - 06-25
  - 06-26
files_modified: []
autonomous: false
requirements:
  - PHR-01
  - PHR-02
  - PHR-03
  - PHR-04
  - PHR-05
  - PHR-06
  - PHR-07
  - CON-01
  - CON-02
  - CON-03
must_haves:
  truths:
    - "All backend tests pass"
    - "Backend builds and runs without errors"
    - "Frontend compiles and renders pharmacy/consumables pages"
    - "Database migration applied successfully"
    - "Dispensing workflow operates end-to-end"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification checkpoint for the complete Phase 6 pharmacy and consumables implementation.

Purpose: Verify all features work together before marking phase complete.
Output: Verification results.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-pharmacy-consumables/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification</name>
  <files></files>
  <action>
    Run the following automated checks:

    1. Backend build: `dotnet build backend/backend.slnx -v q`
    2. All pharmacy tests: `dotnet test backend/tests/Pharmacy.Unit.Tests -v q`
    3. Full test suite: `dotnet test backend/tests/ -v q`
    4. Frontend TypeScript check: `cd frontend && npx tsc --noEmit`
    5. Database migration: Apply migration if not yet applied
    6. Start backend (port 5255) and verify:
       - GET /api/pharmacy/inventory returns 200
       - GET /api/pharmacy/suppliers returns 200
       - GET /api/pharmacy/dispensing/pending returns 200
       - GET /api/pharmacy/alerts/expiry?days=90 returns 200
       - GET /api/consumables returns 200
    7. Start frontend (port 3000) and verify build succeeds

    Record all results. If any fail, document the failure for debugging.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Pharmacy.Unit.Tests -v q && cd D:/projects/ganka/frontend && npx tsc --noEmit</automated>
  </verify>
  <done>All automated checks pass: build, tests, TypeScript, API endpoints respond.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete pharmacy and consumables module:
    - Drug inventory management with batch tracking and FEFO
    - Supplier management with default pricing
    - Stock import via supplier invoice and Excel bulk
    - Expiry and low stock alert banners
    - Dispensing queue with FEFO batch selection and 7-day prescription expiry
    - Walk-in OTC sales with optional customer linkage
    - Separate consumables warehouse with two tracking modes
    - Sidebar navigation with pending prescriptions badge
    - Bilingual (EN/VI) translations
  </what-built>
  <how-to-verify>
    Backend runs at http://localhost:5255, Frontend at http://localhost:3000
    Login: Admin@ganka28.com / Admin@123456

    1. **Sidebar**: Verify Pharmacy nav is active with sub-items (Inventory, Queue, Suppliers, Stock Import, OTC Sales). Verify Consumables nav item appears.

    2. **Suppliers** (/pharmacy/suppliers): Create a test supplier "Cong ty Duoc pham ABC". Verify it appears in the table.

    3. **Drug Inventory** (/pharmacy): Verify drugs from Phase 5 catalog appear. Check alert banners (may show no alerts if no batches yet).

    4. **Stock Import** (/pharmacy/stock-import): Import stock for a drug via supplier invoice: select supplier, add line with drug, batch number, expiry date, quantity. Verify batch appears in inventory.

    5. **Dispensing Queue** (/pharmacy/queue): If a prescription exists from Phase 5 testing, verify it appears in the queue. Click to open dispensing dialog and verify FEFO batch suggestions appear.

    6. **OTC Sales** (/pharmacy/otc-sales): Create a test OTC sale: add a drug line, verify stock is deducted.

    7. **Consumables** (/consumables): Verify seeded items appear (~12 IPL/LLLT supplies). Create a test item. Add stock. Verify stock level updates.

    8. **Alerts**: After importing stock with near-expiry dates, verify expiry alert banner appears.

    9. **Language**: Switch to Vietnamese and verify all pharmacy/consumables text is properly translated with diacritics.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All automated checks pass. Human verification confirms end-to-end functionality.
</verification>

<success_criteria>
- Backend builds and all tests pass
- Frontend compiles without errors
- All API endpoints return 200
- Human verification of full pharmacy workflow
- Human verification of consumables management
- Human verification of sidebar and i18n
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-27-SUMMARY.md`
</output>
