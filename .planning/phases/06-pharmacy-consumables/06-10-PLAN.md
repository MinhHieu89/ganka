---
phase: 06-pharmacy-consumables
plan: 10
type: tdd
wave: 3
depends_on:
  - 06-07
  - 06-06
files_modified:
  - backend/tests/Pharmacy.Unit.Tests/Pharmacy.Unit.Tests.csproj
  - backend/tests/Pharmacy.Unit.Tests/Domain/FEFOAllocatorTests.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Services/FEFOAllocator.cs
  - backend/tests/Pharmacy.Unit.Tests/Domain/DrugBatchTests.cs
autonomous: true
requirements:
  - PHR-05
must_haves:
  truths:
    - "FEFOAllocator domain service exists with tested batch allocation logic"
    - "FEFO selects earliest-expiry batches first, skips expired and zero-stock"
    - "Multi-batch allocation works when single batch has insufficient stock"
    - "Returns empty list when total available stock is insufficient"
  artifacts:
    - path: "backend/tests/Pharmacy.Unit.Tests/Pharmacy.Unit.Tests.csproj"
      provides: "Test project with xUnit, FluentAssertions, NSubstitute, Bogus"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Domain/Services/FEFOAllocator.cs"
      provides: "Static FEFO batch allocation service"
    - path: "backend/tests/Pharmacy.Unit.Tests/Domain/FEFOAllocatorTests.cs"
      provides: "Unit tests for FEFO allocation logic"
  key_links:
    - from: "FEFOAllocator.Allocate"
      to: "DrugBatch"
      via: "Batch ordering and selection"
      pattern: "OrderBy.*ExpiryDate"
---

<objective>
Create the Pharmacy.Unit.Tests project and implement the FEFO Allocator domain service using TDD.

Purpose: FEFO (First Expiry, First Out) is the core batch selection algorithm reused by both dispensing and OTC sales. Must be tested thoroughly.
Output: Test project + FEFO allocator with tests + DrugBatch domain tests.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md
</context>

<feature>
  <name>FEFO Batch Allocation</name>
  <files>backend/src/Modules/Pharmacy/Pharmacy.Domain/Services/FEFOAllocator.cs, backend/tests/Pharmacy.Unit.Tests/Domain/FEFOAllocatorTests.cs</files>
  <behavior>
    - Test: Single batch with sufficient stock -> allocates from that batch
    - Test: Multiple batches -> allocates from earliest expiry first
    - Test: Multi-batch allocation when first batch has insufficient stock -> spans multiple batches
    - Test: Expired batches are skipped (ExpiryDate <= today)
    - Test: Zero-quantity batches are skipped
    - Test: Insufficient total stock -> returns empty list
    - Test: Exact match (requested = available) -> allocates all
    - Test: DrugBatch.Deduct reduces CurrentQuantity correctly
    - Test: DrugBatch.Deduct throws when quantity > CurrentQuantity
    - Test: DrugBatch.IsExpired returns true when ExpiryDate <= today
  </behavior>
  <implementation>
    Static FEFOAllocator class with:
    ```csharp
    public sealed record BatchAllocation(Guid BatchId, string BatchNumber, int Quantity, DateOnly ExpiryDate);

    public static class FEFOAllocator
    {
        public static List<BatchAllocation> Allocate(IReadOnlyList<DrugBatch> availableBatches, int requiredQuantity)
        {
            // Filter: CurrentQuantity > 0, not expired
            // Order: ExpiryDate ASC
            // Allocate: take from each batch until requiredQuantity met
            // Return empty if insufficient stock
        }
    }
    ```
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test project and write FEFO allocator tests (RED)</name>
  <files>
    backend/tests/Pharmacy.Unit.Tests/Pharmacy.Unit.Tests.csproj,
    backend/tests/Pharmacy.Unit.Tests/Domain/FEFOAllocatorTests.cs,
    backend/tests/Pharmacy.Unit.Tests/Domain/DrugBatchTests.cs
  </files>
  <behavior>
    - FEFOAllocator_SingleBatch_AllocatesFromIt
    - FEFOAllocator_MultipleBatches_AllocatesFromEarliestExpiryFirst
    - FEFOAllocator_SpansMultipleBatches_WhenFirstInsufficient
    - FEFOAllocator_SkipsExpiredBatches
    - FEFOAllocator_SkipsZeroQuantityBatches
    - FEFOAllocator_ReturnsEmpty_WhenInsufficientStock
    - FEFOAllocator_ExactMatch_AllocatesAll
    - DrugBatch_Deduct_ReducesCurrentQuantity
    - DrugBatch_Deduct_ThrowsWhenInsufficient
    - DrugBatch_IsExpired_ReturnsTrueWhenPast
  </behavior>
  <action>
    1. Create Pharmacy.Unit.Tests.csproj with:
       - xUnit, FluentAssertions, NSubstitute, Bogus references (via CPM)
       - ProjectReference to Pharmacy.Domain, Pharmacy.Application, Pharmacy.Contracts

    2. Create FEFOAllocatorTests.cs with all test cases above. Use helper method to create DrugBatch instances via reflection (private constructor -- same TestHelpers pattern from Auth.Unit.Tests with reflection-based entity creation).

    3. Create DrugBatchTests.cs testing Deduct/IsExpired/IsNearExpiry domain methods.

    4. Run tests -- they should FAIL (RED) because FEFOAllocator.cs does not exist yet.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Pharmacy.Unit.Tests --filter "FEFOAllocator|DrugBatch" --no-build -v q 2>&1 | head -20</automated>
  </verify>
  <done>Test project created. FEFO and DrugBatch tests exist and fail (class not found or NotImplemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FEFOAllocator (GREEN)</name>
  <files>backend/src/Modules/Pharmacy/Pharmacy.Domain/Services/FEFOAllocator.cs</files>
  <action>
    Implement FEFOAllocator.Allocate:
    1. Filter batches: CurrentQuantity > 0 AND !IsExpired
    2. Order by ExpiryDate ascending
    3. Iterate: take Math.Min(remaining, batch.CurrentQuantity) from each batch
    4. If remaining > 0 after all batches: return empty list (insufficient stock)
    5. Return List<BatchAllocation>

    The BatchAllocation record should be defined in this file or in a separate file in Domain/Services/.

    Run all tests -- they should now PASS (GREEN).
  </action>
  <verify>
    <automated>dotnet test backend/tests/Pharmacy.Unit.Tests --filter "FEFOAllocator|DrugBatch" -v q</automated>
  </verify>
  <done>All FEFO and DrugBatch tests pass. FEFOAllocator correctly implements FEFO batch selection with multi-batch spanning.</done>
</task>

</tasks>

<verification>
`dotnet test backend/tests/Pharmacy.Unit.Tests -v q` passes all tests.
</verification>

<success_criteria>
- Pharmacy.Unit.Tests project exists with xUnit/FluentAssertions/NSubstitute
- FEFOAllocator has 7+ tests covering all scenarios
- DrugBatch domain methods have 3+ tests
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-10-SUMMARY.md`
</output>
