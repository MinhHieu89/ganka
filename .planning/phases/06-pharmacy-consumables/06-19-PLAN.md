---
phase: 06-pharmacy-consumables
plan: 19
type: execute
wave: 6
depends_on:
  - 06-17
  - 06-18
files_modified:
  - frontend/src/features/pharmacy/api/pharmacy-api.ts
  - frontend/src/features/pharmacy/api/pharmacy-queries.ts
  - frontend/src/features/consumables/api/consumables-api.ts
  - frontend/src/features/consumables/api/consumables-queries.ts
autonomous: true
requirements:
  - PHR-01
  - CON-01
must_haves:
  truths:
    - "Pharmacy API functions exist for all backend endpoints"
    - "TanStack Query hooks exist with proper key factories and cache invalidation"
    - "Consumables API functions and hooks are separate from pharmacy"
    - "TypeScript interfaces for all DTOs are defined inline matching backend Contracts"
  artifacts:
    - path: "frontend/src/features/pharmacy/api/pharmacy-api.ts"
      provides: "All pharmacy API functions (suppliers, inventory, dispensing, OTC, alerts, stock import) with TS types"
    - path: "frontend/src/features/pharmacy/api/pharmacy-queries.ts"
      provides: "TanStack Query key factories and hooks for pharmacy"
    - path: "frontend/src/features/consumables/api/consumables-api.ts"
      provides: "All consumables API functions with TS types"
    - path: "frontend/src/features/consumables/api/consumables-queries.ts"
      provides: "TanStack Query hooks for consumables"
  key_links:
    - from: "pharmacy-queries.ts"
      to: "pharmacy-api.ts"
      via: "queryFn calls"
      pattern: "queryFn.*pharmacy"
---

<objective>
Create frontend API layer for both pharmacy and consumables features, including TypeScript type definitions matching backend DTOs.

Purpose: API functions and TanStack Query hooks are the data access foundation for all frontend pages. Since this project uses openapi-fetch without generated types (all API calls use `as never` casts), each API file must define its own TypeScript interfaces matching the backend Contracts DTOs.
Output: 4 API files (2 per feature area).
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md

<interfaces>
NOTE: This project does NOT use generated OpenAPI TypeScript types. The openapi-fetch client is untyped
(`createClient({ baseUrl: API_URL })` without a type parameter). All API calls use `as never` casts.
Each feature's API file defines its own TypeScript interfaces manually, matching the backend Contracts DTOs.

From frontend/src/shared/lib/api-client.ts:
```typescript
export const api = createClient({ baseUrl: API_URL, credentials: "include" })
// No type parameter -- calls use `as never` pattern
```

From frontend/src/features/clinical/api/clinical-api.ts (established pattern):
```typescript
// Types defined at top of file matching backend DTOs
export interface ActiveVisitDto { id: string; patientId: string; ... }
// API functions use `as never` cast
const { data, error } = await api.GET("/api/clinical/active" as never)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pharmacy API functions and TanStack Query hooks</name>
  <files>
    frontend/src/features/pharmacy/api/pharmacy-api.ts,
    frontend/src/features/pharmacy/api/pharmacy-queries.ts
  </files>
  <action>
    Follow existing pattern from clinical/api/ or patient/api/.

    **pharmacy-api.ts**:

    First, define TypeScript interfaces at the top of the file matching backend Pharmacy.Contracts DTOs (Plan 06). This project does NOT use generated OpenAPI types -- types are manually defined per feature API file. Use `as never` casts on all api.GET/POST/PUT calls (same pattern as clinical-api.ts, patient-api.ts):

    Types to define (matching backend Contracts):
    - SupplierDto(id, name, contactInfo, isActive, supplierDrugPrices?)
    - SupplierDrugPriceDto(id, supplierId, drugCatalogItemId, drugName, defaultPurchasePrice)
    - DrugBatchDto(id, drugCatalogItemId, supplierId, supplierName, batchNumber, expiryDate, initialQuantity, currentQuantity, purchasePrice, isExpired, isNearExpiry)
    - DrugInventoryDto(id, name, nameVi, genericName, form, strength, unit, sellingPrice, minStockLevel, totalStock, isLowStock, hasExpiryAlert)
    - StockImportDto(id, supplierId, supplierName, invoiceNumber, importDate, totalAmount, lineCount, importSource)
    - StockImportLineDto(drugCatalogItemId, drugName, batchNumber, expiryDate, quantity, purchasePrice)
    - ExpiryAlertDto(drugCatalogItemId, drugName, batchNumber, expiryDate, currentQuantity, daysUntilExpiry)
    - LowStockAlertDto(drugCatalogItemId, drugName, currentStock, minStockLevel)
    - PendingPrescriptionDto(id, visitId, patientId, patientName, prescriptionCode, prescribedAt, daysRemaining, isExpired, itemCount, items)
    - DispensingRecordDto(id, prescriptionId, patientName, dispensedAt, dispensedByName, lineCount)
    - OtcSaleDto(id, patientId, customerName, saleDate, totalAmount, lineCount)
    - CreateStockImportInput, CreateOtcSaleInput, DispenseDrugsInput and related request types

    Functions using openapi-fetch client (api.GET, api.POST, api.PUT):
    - getSuppliers() -> GET /api/pharmacy/suppliers
    - createSupplier(data) -> POST /api/pharmacy/suppliers
    - updateSupplier(id, data) -> PUT /api/pharmacy/suppliers/{id}
    - getDrugInventory() -> GET /api/pharmacy/inventory
    - getDrugBatches(drugId) -> GET /api/pharmacy/inventory/{drugId}/batches
    - updateDrugPricing(drugId, data) -> PUT /api/pharmacy/inventory/{drugId}/pricing
    - adjustStock(data) -> POST /api/pharmacy/inventory/adjust
    - getStockImports(page, pageSize) -> GET /api/pharmacy/stock-imports
    - createStockImport(data) -> POST /api/pharmacy/stock-imports
    - importStockFromExcel(file, supplierId) -> POST /api/pharmacy/stock-imports/excel (use native fetch + FormData for file upload, following patient photo upload pattern)
    - getExpiryAlerts(days?) -> GET /api/pharmacy/alerts/expiry
    - getLowStockAlerts() -> GET /api/pharmacy/alerts/low-stock
    - getPendingPrescriptions() -> GET /api/pharmacy/dispensing/pending
    - getPendingCount() -> GET /api/pharmacy/dispensing/pending/count
    - dispenseDrugs(data) -> POST /api/pharmacy/dispensing
    - getDispensingHistory(page, pageSize) -> GET /api/pharmacy/dispensing/history
    - createOtcSale(data) -> POST /api/pharmacy/otc-sales
    - getOtcSales(page, pageSize) -> GET /api/pharmacy/otc-sales

    **pharmacy-queries.ts**: Key factories and hooks:
    - pharmacyKeys factory: suppliers.all, inventory.all, inventory.batches(drugId), stockImports.list(page), alerts.expiry(days), alerts.lowStock, dispensing.pending, dispensing.pendingCount, dispensing.history(page), otcSales.list(page)
    - useSuppliers() query
    - useDrugInventory() query
    - useDrugBatches(drugId) query
    - useStockImports(page) query
    - useExpiryAlerts(days) query
    - useLowStockAlerts() query
    - usePendingPrescriptions() query
    - usePendingCount() query with refetchInterval: 30000 (30s polling for sidebar badge)
    - useDispensingHistory(page) query
    - useOtcSales(page) query
    - useCreateSupplier() mutation with invalidate suppliers
    - useUpdateSupplier() mutation with invalidate suppliers
    - useUpdateDrugPricing() mutation with invalidate inventory
    - useAdjustStock() mutation with invalidate inventory + batches
    - useCreateStockImport() mutation with invalidate inventory + stockImports
    - useDispenseDrugs() mutation with invalidate pending + pendingCount + history
    - useCreateOtcSale() mutation with invalidate inventory + otcSales
    - All mutations have onError with toast.error per project convention
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Pharmacy API functions and TanStack Query hooks for all endpoints. 30s polling for pending count. All TypeScript types defined inline matching backend Contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Create consumables API functions and TanStack Query hooks</name>
  <files>
    frontend/src/features/consumables/api/consumables-api.ts,
    frontend/src/features/consumables/api/consumables-queries.ts
  </files>
  <action>
    **consumables-api.ts**:

    First, define TypeScript interfaces at top of file matching backend Contracts (same pattern as Task 1 -- manual types, `as never` casts):
    - ConsumableItemDto(id, name, nameVi, unit, trackingMode, currentStock, minStockLevel, isLowStock, isActive)
    - ConsumableBatchDto(id, consumableItemId, batchNumber, expiryDate, initialQuantity, currentQuantity, isExpired, isNearExpiry)
    - ConsumableAlertDto(consumableItemId, name, currentStock, minStockLevel)
    - CreateConsumableItemInput, AddConsumableStockInput, AdjustConsumableStockInput

    Functions:
    - getConsumableItems() -> GET /api/consumables
    - createConsumableItem(data) -> POST /api/consumables
    - updateConsumableItem(id, data) -> PUT /api/consumables/{id}
    - addConsumableStock(id, data) -> POST /api/consumables/{id}/stock
    - adjustConsumableStock(id, data) -> POST /api/consumables/{id}/adjust
    - getConsumableAlerts() -> GET /api/consumables/alerts

    **consumables-queries.ts**:
    - consumableKeys factory: items.all, alerts.all
    - useConsumableItems() query
    - useConsumableAlerts() query
    - useCreateConsumableItem() mutation with invalidate items
    - useUpdateConsumableItem() mutation with invalidate items
    - useAddConsumableStock() mutation with invalidate items
    - useAdjustConsumableStock() mutation with invalidate items
    - All mutations with onError toast.error
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Consumables API functions and TanStack Query hooks for all endpoints. TypeScript types defined inline.</done>
</task>

</tasks>

<verification>
TypeScript compiles without errors. All API functions match backend endpoints. Types defined inline.
</verification>

<success_criteria>
- 4 API files covering all backend endpoints
- TypeScript interfaces defined inline matching backend Contracts DTOs (no dependency on generated schema)
- TanStack Query hooks with key factories and cache invalidation
- 30s polling on pending prescription count
- All mutations have onError toast.error
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-19-SUMMARY.md`
</output>
