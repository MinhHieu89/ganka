---
phase: 06-pharmacy-consumables
plan: 16
type: tdd
wave: 4
depends_on:
  - 06-09
  - 06-10
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/GetConsumableItems.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/AddConsumableStock.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/AdjustConsumableStock.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/GetConsumableAlerts.cs
  - backend/tests/Pharmacy.Unit.Tests/Features/ConsumableStockHandlerTests.cs
autonomous: true
requirements:
  - CON-02
  - CON-03
must_haves:
  truths:
    - "AddConsumableStock supports both SimpleStock (direct increment) and ExpiryTracked (new batch)"
    - "AdjustConsumableStock creates audit record for manual corrections"
    - "GetConsumableAlerts returns low stock items (same pattern as drug alerts)"
    - "Entity design supports future Phase 9 auto-deduction (CON-03 preparedness)"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/AddConsumableStock.cs"
      provides: "Stock add handler supporting both tracking modes"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/AdjustConsumableStock.cs"
      provides: "Manual adjustment handler with audit"
    - path: "backend/tests/Pharmacy.Unit.Tests/Features/ConsumableStockHandlerTests.cs"
      provides: "TDD tests for consumable stock management"
  key_links:
    - from: "AddConsumableStock"
      to: "ConsumableItem.AddStock / ConsumableBatch.Create"
      via: "Tracking mode branching"
      pattern: "TrackingMode"
---

<objective>
Implement consumable stock management handlers using TDD.

Purpose: CON-02 requires stock management with alerts. Manual-only in Phase 6 (auto-deduction in Phase 9 per CON-03).
Output: 4 handler files + 1 test file.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-CONTEXT.md
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md
</context>

<feature>
  <name>Consumable Stock Management</name>
  <files>backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/*.cs</files>
  <behavior>
    - AddConsumableStock for SimpleStock: increments CurrentStock directly
    - AddConsumableStock for ExpiryTracked: creates new ConsumableBatch
    - AdjustConsumableStock: for SimpleStock modifies CurrentStock, for ExpiryTracked modifies batch
    - AdjustConsumableStock: creates StockAdjustment for audit
    - GetConsumableItems: returns all active items with stock info
    - GetConsumableAlerts: returns items below minimum stock level
  </behavior>
  <implementation>Branching logic based on ConsumableTrackingMode.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests (RED) and implement consumable stock handlers (GREEN)</name>
  <files>
    backend/tests/Pharmacy.Unit.Tests/Features/ConsumableStockHandlerTests.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/GetConsumableItems.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/AddConsumableStock.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/AdjustConsumableStock.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Consumables/GetConsumableAlerts.cs
  </files>
  <behavior>
    - AddConsumableStock_SimpleStock_IncrementsCurrentStock
    - AddConsumableStock_ExpiryTracked_CreatesBatch
    - AddConsumableStock_ExpiryTracked_RequiresBatchNumberAndExpiry
    - AdjustConsumableStock_SimpleStock_ModifiesCurrentStock
    - AdjustConsumableStock_ExpiryTracked_ModifiesBatch
    - AdjustConsumableStock_CreatesAuditRecord
    - AdjustConsumableStock_NegativeExceedsStock_ReturnsError
    - GetConsumableItems_ReturnsActiveWithStockInfo
    - GetConsumableAlerts_ReturnsBelowMinimum
  </behavior>
  <action>
    TDD cycle:

    **GetConsumableItems.cs**:
    - Query: GetConsumableItemsQuery()
    - Handler: Gets all active items. For ExpiryTracked items, computes stock from batch sums.
    - Returns List<ConsumableItemDto> with IsLowStock flag

    **AddConsumableStock.cs**:
    - Command: AddConsumableStockCommand(ConsumableItemId, Quantity, BatchNumber?, ExpiryDate?, Notes?)
    - Validator: ConsumableItemId required, Quantity > 0. If ExpiryTracked: BatchNumber and ExpiryDate required.
    - Handler: Get item by ID. If SimpleStock -> item.AddStock(qty). If ExpiryTracked -> ConsumableBatch.Create(itemId, batchNumber, expiryDate, qty), add via repository.

    **AdjustConsumableStock.cs**:
    - Command: AdjustConsumableStockCommand(ConsumableItemId, Guid? ConsumableBatchId, int QuantityChange, StockAdjustmentReason Reason, string? Notes)
    - If SimpleStock: ConsumableBatchId not needed, adjust item.CurrentStock. If positive -> AddStock, if negative -> RemoveStock.
    - If ExpiryTracked: ConsumableBatchId required, adjust batch.
    - Always create StockAdjustment record with ConsumableBatchId set.

    **GetConsumableAlerts.cs**:
    - Query: GetConsumableAlertsQuery()
    - Returns items where computed stock < MinStockLevel AND MinStockLevel > 0

    Run tests -- all should pass.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Pharmacy.Unit.Tests --filter "ConsumableStock" -v q</automated>
  </verify>
  <done>Consumable stock management handlers pass all tests. Both tracking modes work correctly.</done>
</task>

</tasks>

<verification>
`dotnet test backend/tests/Pharmacy.Unit.Tests --filter "ConsumableStock" -v q` passes.
</verification>

<success_criteria>
- AddConsumableStock handles both SimpleStock and ExpiryTracked modes
- AdjustConsumableStock with audit record
- GetConsumableAlerts returns low-stock items
- 9+ tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-16-SUMMARY.md`
</output>
