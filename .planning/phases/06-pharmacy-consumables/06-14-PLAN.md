---
phase: 06-pharmacy-consumables
plan: 14
type: tdd
wave: 4
depends_on:
  - 06-08
  - 06-10
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/CreateOtcSale.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/GetOtcSales.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/AdjustStock.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/GetDrugInventory.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/GetDrugBatches.cs
  - backend/tests/Pharmacy.Unit.Tests/Features/OtcSaleAndInventoryHandlerTests.cs
autonomous: true
requirements:
  - PHR-06
  - PHR-01
must_haves:
  truths:
    - "CreateOtcSale handler creates sale with FEFO batch deduction (same as dispensing)"
    - "Customer linkage is optional for OTC sales"
    - "AdjustStock handler creates StockAdjustment record and modifies batch quantity"
    - "GetDrugInventory returns drugs with computed stock levels and alert flags"
    - "GetDrugBatches returns batches for a specific drug with FEFO ordering"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/CreateOtcSale.cs"
      provides: "OTC sale handler with FEFO batch deduction"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/AdjustStock.cs"
      provides: "Manual stock adjustment handler"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/GetDrugBatches.cs"
      provides: "Query handler returning batches for a specific drug"
    - path: "backend/tests/Pharmacy.Unit.Tests/Features/OtcSaleAndInventoryHandlerTests.cs"
      provides: "TDD tests for OTC sales, inventory, and batch queries"
  key_links:
    - from: "CreateOtcSale"
      to: "FEFOAllocator"
      via: "Same FEFO mechanism as dispensing"
      pattern: "FEFOAllocator.Allocate"
    - from: "GetDrugBatches"
      to: "IDrugBatchRepository"
      via: "Repository query"
      pattern: "IDrugBatchRepository.GetBatchesForDrugAsync"
---

<objective>
Implement OTC sale and inventory management handlers using TDD, including the GetDrugBatches query handler.

Purpose: PHR-06 walk-in sales use same FEFO mechanism as dispensing. Inventory handlers provide drug stock views, batch detail queries, and manual adjustments. GetDrugBatches is the Application layer handler that bridges the GET /inventory/{drugId}/batches endpoint (Plan 17) to the IDrugBatchRepository (Plan 07).
Output: 5 handler files + 1 test file.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md
</context>

<feature>
  <name>OTC Sales and Inventory Management</name>
  <files>backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/*.cs, backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/*.cs</files>
  <behavior>
    - CreateOtcSale: valid sale with customer -> creates sale, deducts stock via FEFO
    - CreateOtcSale: anonymous sale (no customer) -> creates sale
    - CreateOtcSale: insufficient stock -> returns error
    - AdjustStock: positive adjustment -> increases batch quantity
    - AdjustStock: negative adjustment -> decreases batch quantity (guards against negative)
    - AdjustStock: creates StockAdjustment record for audit
    - GetDrugInventory: returns all drugs with current stock, alert flags
    - GetDrugBatches: returns batches for a specific drug ordered by expiry (FEFO)
    - GetDrugBatches: invalid drugCatalogItemId -> returns empty list
  </behavior>
  <implementation>OTC reuses FEFOAllocator. AdjustStock modifies batch directly with audit record. GetDrugBatches delegates to IDrugBatchRepository.GetBatchesForDrugAsync.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests (RED) and implement OTC sale + inventory handlers (GREEN)</name>
  <files>
    backend/tests/Pharmacy.Unit.Tests/Features/OtcSaleAndInventoryHandlerTests.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/CreateOtcSale.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/GetOtcSales.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/AdjustStock.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/GetDrugInventory.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Inventory/GetDrugBatches.cs
  </files>
  <behavior>
    - CreateOtcSale_ValidWithCustomer_CreatesSaleAndDeductsStock
    - CreateOtcSale_Anonymous_CreatesSale
    - CreateOtcSale_InsufficientStock_ReturnsError
    - AdjustStock_PositiveAdjustment_IncreasesQuantity
    - AdjustStock_NegativeAdjustment_DecreasesQuantity
    - AdjustStock_ExcessiveNegative_ReturnsError
    - AdjustStock_CreatesAuditRecord
    - GetDrugInventory_ReturnsComputedStockLevels
    - GetDrugBatches_ValidDrug_ReturnsBatchesFEFOOrdered
    - GetDrugBatches_NoMatchingDrug_ReturnsEmptyList
  </behavior>
  <action>
    TDD cycle:

    **CreateOtcSale.cs**:
    - Command: CreateOtcSaleCommand(Guid? PatientId, string? CustomerName, string? Notes, List<OtcSaleLineInput> Lines)
    - OtcSaleLineInput(DrugCatalogItemId, DrugName, Quantity, UnitPrice)
    - Handler: For each line, get FEFO batches, allocate, deduct. Create OtcSale aggregate with lines and batch deductions.
    - No payment collection (deferred to Phase 7 per user decision)

    **GetOtcSales.cs**: Paginated query.

    **AdjustStock.cs**:
    - Command: AdjustStockCommand(Guid DrugBatchId, int QuantityChange, StockAdjustmentReason Reason, string? Notes)
    - Validator: DrugBatchId required, QuantityChange != 0
    - Handler: Get batch, if positive -> batch.AddStock(qty), if negative -> batch.Deduct(abs(qty)). Create StockAdjustment record.

    **GetDrugInventory.cs**: Query handler using IDrugCatalogItemRepository.GetAllWithInventoryAsync returning DrugInventoryDto list.

    **GetDrugBatches.cs**:
    - Query: GetDrugBatchesQuery(Guid DrugCatalogItemId) record
    - Handler: Delegates to IDrugBatchRepository.GetBatchesForDrugAsync(drugCatalogItemId, ct)
    - Returns List<DrugBatchDto> (DrugBatchDto already defined in Plan 06 Contracts)
    - Batches returned include all batches (active, expired, empty) so pharmacist can see full history
    - This handler is invoked by GET /inventory/{drugId}/batches endpoint in Plan 17

    Run tests -- all should pass.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Pharmacy.Unit.Tests --filter "OtcSale|Inventory|DrugBatch" -v q</automated>
  </verify>
  <done>OTC sale and inventory handlers pass all tests. FEFO reused for OTC. Stock adjustments create audit records. GetDrugBatches returns batches for a specific drug via IDrugBatchRepository.</done>
</task>

</tasks>

<verification>
`dotnet test backend/tests/Pharmacy.Unit.Tests --filter "OtcSale|Inventory|DrugBatch" -v q` passes.
</verification>

<success_criteria>
- CreateOtcSale with FEFO batch deduction
- AdjustStock with audit trail
- GetDrugInventory with computed stock levels
- GetDrugBatches returns batches for a drug via IDrugBatchRepository.GetBatchesForDrugAsync
- 10+ tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-14-SUMMARY.md`
</output>
