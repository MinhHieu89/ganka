---
phase: 06-pharmacy-consumables
plan: 12
type: tdd
wave: 4
depends_on:
  - 06-08
  - 06-10
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/CreateStockImport.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/ImportStockFromExcel.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/GetStockImports.cs
  - backend/tests/Pharmacy.Unit.Tests/Features/StockImportHandlerTests.cs
  - backend/Directory.Packages.props
autonomous: true
requirements:
  - PHR-02
must_haves:
  truths:
    - "CreateStockImport handler creates batches from supplier invoice form"
    - "ImportStockFromExcel handler parses Excel file using MiniExcel and creates batches"
    - "Both import methods create StockImport record with lines for audit"
    - "MiniExcelLibs added to Central Package Management"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/CreateStockImport.cs"
      provides: "Supplier invoice import handler"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/ImportStockFromExcel.cs"
      provides: "Excel bulk import handler with MiniExcel"
    - path: "backend/tests/Pharmacy.Unit.Tests/Features/StockImportHandlerTests.cs"
      provides: "TDD tests for both import methods"
  key_links:
    - from: "CreateStockImport"
      to: "DrugBatch + StockImport"
      via: "Creates batch for each line item"
      pattern: "DrugBatch.Create"
---

<objective>
Implement stock import handlers (supplier invoice + Excel bulk) using TDD. Add MiniExcel to CPM.

Purpose: PHR-02 requires both import methods. Supplier invoice for daily use, Excel for bulk/initial load.
Output: 3 handler files + 1 test file + CPM update.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md
</context>

<feature>
  <name>Stock Import Handlers</name>
  <files>backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/*.cs, backend/tests/Pharmacy.Unit.Tests/Features/StockImportHandlerTests.cs</files>
  <behavior>
    - CreateStockImport: valid supplier invoice -> creates StockImport + DrugBatches
    - CreateStockImport: invalid data (missing batch number, negative qty) -> validation errors
    - ImportStockFromExcel: valid Excel stream -> parses rows, validates, returns preview
    - ImportStockFromExcel: invalid data rows -> returns row-level errors (not fail-fast)
    - GetStockImports: returns paginated list of stock imports
  </behavior>
  <implementation>MiniExcel for Excel parsing. FluentValidation for row validation.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add MiniExcel to CPM, write stock import tests (RED), implement handlers (GREEN)</name>
  <files>
    backend/Directory.Packages.props,
    backend/tests/Pharmacy.Unit.Tests/Features/StockImportHandlerTests.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/CreateStockImport.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/ImportStockFromExcel.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/StockImport/GetStockImports.cs
  </files>
  <behavior>
    - CreateStockImport_ValidInvoice_CreatesStockImportAndBatches
    - CreateStockImport_InvalidData_ReturnsValidationErrors
    - CreateStockImport_FutureExpiryRequired_RejectsExpiredDates
    - ImportStockFromExcel_ValidData_ReturnsPreviewLines
    - ImportStockFromExcel_InvalidRows_CollectsAllErrors
    - GetStockImports_ReturnsPaginated
  </behavior>
  <action>
    TDD cycle:

    1. Add to Directory.Packages.props: `<PackageVersion Include="MiniExcelLibs" Version="1.*" />`
    2. Add MiniExcelLibs PackageReference to Pharmacy.Application.csproj (or Pharmacy.Infrastructure.csproj depending on where Excel parsing lives -- prefer Application since it's business logic).

    3. Write tests in StockImportHandlerTests.cs.

    4. Implement handlers:

    **CreateStockImport.cs**:
    - Command: CreateStockImportCommand(SupplierId, InvoiceNumber?, Notes?, List<StockImportLineInput> Lines)
    - StockImportLineInput(DrugCatalogItemId, DrugName, BatchNumber, ExpiryDate, Quantity, PurchasePrice)
    - Validator: SupplierId required, Lines not empty, each line: BatchNumber required, Quantity > 0, ExpiryDate > today, PurchasePrice >= 0
    - Handler: Creates StockImport aggregate, AddLine for each, creates DrugBatch.Create for each line (linking to StockImport.Id), saves via repositories

    **ImportStockFromExcel.cs**:
    - Command: ImportStockFromExcelCommand(Stream FileStream, Guid SupplierId)
    - Handler: Use MiniExcel.Query<StockImportRow>(fileStream) to parse rows
    - Expected columns: DrugName, BatchNumber, ExpiryDate, Quantity, PurchasePrice
    - Validate ALL rows and collect errors (not fail-fast)
    - Match DrugName to DrugCatalogItem via repository search
    - Return Result<ExcelImportPreview> with valid lines + error lines for user confirmation
    - ExcelImportPreview record: ValidLines (List<StockImportLineDto>), Errors (List<ExcelImportError>)
    - ExcelImportError record: RowNumber, ColumnName, Message

    **GetStockImports.cs**: Simple paginated query.

    5. Run tests -- all should pass.
  </action>
  <verify>
    <automated>dotnet test backend/tests/Pharmacy.Unit.Tests --filter "StockImport" -v q</automated>
  </verify>
  <done>Stock import handlers pass all tests. MiniExcel in CPM. Excel import returns preview with row-level errors.</done>
</task>

</tasks>

<verification>
`dotnet test backend/tests/Pharmacy.Unit.Tests --filter "StockImport" -v q` passes.
</verification>

<success_criteria>
- MiniExcelLibs in Directory.Packages.props
- CreateStockImport creates batches from supplier invoice data
- ImportStockFromExcel parses Excel and returns preview with errors
- 6+ tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-12-SUMMARY.md`
</output>
