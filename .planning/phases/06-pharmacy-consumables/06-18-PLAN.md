---
phase: 06-pharmacy-consumables
plan: 18
type: execute
wave: 5
depends_on:
  - 06-09
  - 06-13
  - 06-14
  - 06-15
  - 06-16
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Presentation/DispensingApiEndpoints.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Presentation/ConsumablesApiEndpoints.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/GetPendingPrescriptions.cs
  - backend/src/Bootstrapper/Program.cs
autonomous: true
requirements:
  - PHR-05
  - PHR-06
  - PHR-07
  - CON-01
  - CON-02
must_haves:
  truths:
    - "Dispensing and OTC sale API endpoints exist"
    - "Consumables API endpoints exist under separate route group"
    - "GetPendingPrescriptions handler exists in Clinical.Application"
    - "Bootstrapper registers Pharmacy Presentation and ConsumableCatalogSeeder"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Presentation/DispensingApiEndpoints.cs"
      provides: "Dispensing queue, dispense, OTC sale endpoints"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Presentation/ConsumablesApiEndpoints.cs"
      provides: "Consumable CRUD, stock, alerts endpoints"
    - path: "backend/src/Modules/Clinical/Clinical.Application/Features/GetPendingPrescriptions.cs"
      provides: "Cross-module handler for pending prescriptions query"
    - path: "backend/src/Bootstrapper/Program.cs"
      provides: "Updated Bootstrapper with all Pharmacy registrations"
  key_links:
    - from: "Bootstrapper"
      to: "Pharmacy.Presentation/Infrastructure"
      via: "DI registration + endpoint mapping"
      pattern: "AddPharmacy.*MapPharmacy"
---

<objective>
Create dispensing and consumables API endpoints, implement cross-module pending prescriptions handler, and wire everything into the Bootstrapper.

Purpose: Completes the HTTP API surface and integrates the Pharmacy module into the application host.
Output: 2 endpoint files + 1 cross-module handler + Bootstrapper update.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md

<interfaces>
From Bootstrapper/Program.cs (pattern):
```csharp
// DI registration
builder.Services.AddClinicalInfrastructure();
builder.Services.AddClinicalPresentation();

// Endpoint mapping
app.MapClinicalApiEndpoints();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dispensing and consumables API endpoints</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/DispensingApiEndpoints.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/ConsumablesApiEndpoints.cs
  </files>
  <action>
    **DispensingApiEndpoints.cs**:
    - MapDispensingApiEndpoints extension method
    - Route group: /api/pharmacy (same base, different method group)

    Dispensing endpoints:
    - GET /dispensing/pending -> GetPendingPrescriptionsQuery (cross-module via bus)
    - GET /dispensing/pending/count -> count only (for sidebar badge)
    - POST /dispensing -> DispenseDrugsCommand
    - GET /dispensing/history -> GetDispensingHistoryQuery

    OTC sale endpoints:
    - POST /otc-sales -> CreateOtcSaleCommand
    - GET /otc-sales -> GetOtcSalesQuery

    **ConsumablesApiEndpoints.cs**:
    - MapConsumablesApiEndpoints extension method
    - Route group: /api/consumables with RequireAuthorization()

    Endpoints:
    - GET / -> GetConsumableItemsQuery
    - POST / -> CreateConsumableItemCommand
    - PUT /{id} -> UpdateConsumableItemCommand
    - POST /{id}/stock -> AddConsumableStockCommand
    - POST /{id}/adjust -> AdjustConsumableStockCommand
    - GET /alerts -> GetConsumableAlertsQuery

    Call these from PharmacyApiEndpoints or keep as separate extension methods both called from Bootstrapper.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Presentation/Pharmacy.Presentation.csproj --no-restore -v q</automated>
  </verify>
  <done>Dispensing, OTC, and consumables API endpoints exist. Separate route groups for pharmacy vs consumables.</done>
</task>

<task type="auto">
  <name>Task 2: Create cross-module handler and wire Bootstrapper</name>
  <files>
    backend/src/Modules/Clinical/Clinical.Application/Features/GetPendingPrescriptions.cs,
    backend/src/Bootstrapper/Program.cs
  </files>
  <action>
    **GetPendingPrescriptions.cs** in Clinical.Application:
    - Handler for GetPendingPrescriptionsQuery (from Clinical.Contracts)
    - Queries ClinicalDbContext for DrugPrescriptions that have NOT been dispensed
    - "Not dispensed" = prescriptionId NOT IN any DispensingRecord. Since Clinical module cannot query Pharmacy tables directly, two approaches:
      Option A: Pharmacy calls this handler via bus, then filters out dispensed ones locally
      Option B: Add a "DispensedAt" field to DrugPrescription (pollutes Clinical domain)
    - Use Option A: This handler returns ALL prescriptions from last 7 days. Pharmacy frontend filters out already-dispensed ones locally.
    - Returns List<PendingPrescriptionDto> with PrescribedAt for 7-day calculation
    - Include prescription items via Include/ThenInclude

    **Bootstrapper Program.cs** updates:
    1. Add using Pharmacy.Infrastructure; Pharmacy.Presentation;
    2. In service registration section:
       - builder.Services.AddPharmacyInfrastructure();
       - builder.Services.AddPharmacyPresentation();
       - builder.Services.AddHostedService<ConsumableCatalogSeeder>();
    3. In endpoint mapping section:
       - app.MapPharmacyApiEndpoints();
       - app.MapConsumablesApiEndpoints();
       - Also call app.MapDispensingApiEndpoints() or include in MapPharmacyApiEndpoints
    4. Add Pharmacy.Presentation project reference to Bootstrapper.csproj if not already present

    Run EF migration: `dotnet ef migrations add PharmacyInventory -p backend/src/Modules/Pharmacy/Pharmacy.Infrastructure -s backend/src/Bootstrapper`
  </action>
  <verify>
    <automated>dotnet build backend/src/Bootstrapper/Bootstrapper.csproj --no-restore -v q</automated>
  </verify>
  <done>Cross-module handler returns recent prescriptions. Bootstrapper registers all Pharmacy DI and endpoint mapping. Migration created.</done>
</task>

</tasks>

<verification>
`dotnet build backend/src/Bootstrapper` succeeds. All Pharmacy endpoints registered.
</verification>

<success_criteria>
- Dispensing + OTC + Consumables API endpoints defined
- Cross-module pending prescriptions handler in Clinical.Application
- Bootstrapper wires all Pharmacy services, repositories, endpoints, seeder
- EF migration created
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-18-SUMMARY.md`
</output>
