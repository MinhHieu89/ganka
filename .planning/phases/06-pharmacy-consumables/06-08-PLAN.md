---
phase: 06-pharmacy-consumables
plan: 08
type: execute
wave: 3
depends_on:
  - 06-05a
  - 06-05b
  - 06-06
  - 06-07
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/SupplierRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugBatchRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/StockImportRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DispensingRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/OtcSaleRepository.cs
autonomous: true
requirements:
  - PHR-01
  - PHR-03
  - PHR-04
  - PHR-05
must_haves:
  truths:
    - "Repository implementations exist for all Application-layer interfaces"
    - "DrugBatchRepository implements FEFO ordering and alert queries"
    - "DispensingRepository includes eager loading for Lines and BatchDeductions"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugBatchRepository.cs"
      provides: "FEFO batch queries, expiry alerts, stock level computation"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/SupplierRepository.cs"
      provides: "Supplier CRUD with SupplierDrugPrice queries"
  key_links:
    - from: "DrugBatchRepository.GetAvailableBatchesFEFOAsync"
      to: "PharmacyDbContext.DrugBatches"
      via: "EF Core LINQ query with OrderBy ExpiryDate"
      pattern: "OrderBy.*ExpiryDate"
---

<objective>
Implement repository classes for Supplier, DrugBatch, StockImport, Dispensing, and OtcSale.

Purpose: Infrastructure implementations of Application interfaces. DrugBatchRepository is critical for FEFO dispensing.
Output: 5 repository implementation files.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-pharmacy-consumables/06-RESEARCH.md

<interfaces>
From DrugCatalogItemRepository.cs (existing pattern):
```csharp
public class DrugCatalogItemRepository(PharmacyDbContext context) : IDrugCatalogItemRepository
{
    public async Task<List<DrugCatalogItemDto>> SearchAsync(string searchTerm, CancellationToken ct)
    {
        return await context.DrugCatalogItems
            .Where(d => d.IsActive && (d.Name.Contains(searchTerm) || ...))
            .Select(d => new DrugCatalogItemDto(...))
            .Take(20)
            .ToListAsync(ct);
    }
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SupplierRepository, DrugBatchRepository, StockImportRepository</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/SupplierRepository.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugBatchRepository.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/StockImportRepository.cs
  </files>
  <action>
    Use primary constructor injection pattern: `public class XxxRepository(PharmacyDbContext context) : IXxxRepository`

    **SupplierRepository.cs**: Standard CRUD. GetSupplierDrugPricesAsync joins with DrugCatalogItems for DrugName in DTO.

    **DrugBatchRepository.cs** (most important):
    - GetAvailableBatchesFEFOAsync: WHERE CurrentQuantity > 0 AND ExpiryDate > today (DateOnly) AND NOT IsDeleted, ORDER BY ExpiryDate ASC
    - GetTotalStockAsync: SUM(CurrentQuantity) WHERE CurrentQuantity > 0 AND ExpiryDate > today
    - GetExpiryAlertsAsync(daysThreshold): Batches WHERE CurrentQuantity > 0 AND ExpiryDate <= today + daysThreshold AND ExpiryDate > today. Join with DrugCatalogItems for drug name. Project to ExpiryAlertDto.
    - GetLowStockAlertsAsync: Group DrugBatches by DrugCatalogItemId, compute SUM(CurrentQuantity) for available batches, join with DrugCatalogItems WHERE totalStock < MinStockLevel AND MinStockLevel > 0. Project to LowStockAlertDto.
    - Use DateOnly.FromDateTime with Vietnam timezone for "today" comparison: use the cross-platform timezone pattern (SE Asia Standard Time on Windows, Asia/Ho_Chi_Minh on Linux).

    **StockImportRepository.cs**: Standard CRUD with pagination. GetAllAsync includes Lines via Include/ThenInclude.
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Pharmacy.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>SupplierRepository, DrugBatchRepository (with FEFO and alerts), StockImportRepository implement their interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DispensingRepository and OtcSaleRepository</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DispensingRepository.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/OtcSaleRepository.cs
  </files>
  <action>
    **DispensingRepository.cs**:
    - GetByIdAsync: Include Lines then ThenInclude BatchDeductions
    - GetByPrescriptionIdAsync: SingleOrDefaultAsync WHERE PrescriptionId matches
    - GetHistoryAsync: Paginated, optional PatientId filter, Include Lines, OrderByDescending DispensedAt

    **OtcSaleRepository.cs**:
    - GetByIdAsync: Include Lines
    - GetAllAsync: Paginated, Include Lines, OrderByDescending SoldAt
  </action>
  <verify>
    <automated>dotnet build backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Pharmacy.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>DispensingRepository and OtcSaleRepository implement their interfaces with eager loading.</done>
</task>

</tasks>

<verification>
All repositories compile. DrugBatchRepository has FEFO ordering and alert queries.
</verification>

<success_criteria>
- 5 repository implementations
- FEFO query returns batches ordered by ExpiryDate ascending
- Alert queries compute stock levels and expiry proximity correctly
- `dotnet build Pharmacy.Infrastructure` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-pharmacy-consumables/06-08-SUMMARY.md`
</output>
