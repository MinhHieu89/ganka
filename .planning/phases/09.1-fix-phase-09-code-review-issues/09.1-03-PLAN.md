---
phase: 09.1-fix-phase-09-code-review-issues
plan: 03
type: execute
wave: 2
depends_on: ["09.1-01"]
files_modified:
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/20260308073048_InitialTreatment.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/20260308073048_InitialTreatment.Designer.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/TreatmentDbContextModelSnapshot.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Configurations/TreatmentSessionConfiguration.cs
  - backend/src/Modules/Treatment/Treatment.Infrastructure/Configurations/SessionConsumableConfiguration.cs
autonomous: true
requirements: [TRT-02, TRT-11]

must_haves:
  truths:
    - "EF Core migration reflects Entity base class changes (UpdatedAt, IsDeleted columns on TreatmentSessions and SessionConsumables)"
    - "BranchId column exists on TreatmentProtocols table in migration"
    - "Database can be created from scratch with the migration"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/"
      provides: "Updated migration matching current domain model"
  key_links:
    - from: "TreatmentSession Entity inheritance"
      to: "Migration columns"
      via: "EF Core model snapshot"
      pattern: "UpdatedAt.*IsDeleted"
---

<objective>
Regenerate the Treatment EF Core migration to reflect Entity inheritance changes from Plan 01 (TreatmentSession and SessionConsumable now extend Entity, adding UpdatedAt/IsDeleted columns) and verify BranchId is present on TreatmentProtocols.

Purpose: Migration must match the domain model -- mismatches cause runtime SqlExceptions.
Output: Updated migration files that correctly map all entity columns.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-CONTEXT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-RESEARCH.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-01-SUMMARY.md
@backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/20260308073048_InitialTreatment.cs
@backend/src/Modules/Treatment/Treatment.Infrastructure/Configurations/TreatmentSessionConfiguration.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update EF Core configurations for Entity inheritance</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Infrastructure/Configurations/TreatmentSessionConfiguration.cs
    backend/src/Modules/Treatment/Treatment.Infrastructure/Configurations/SessionConsumableConfiguration.cs
  </files>
  <action>
    1. Check TreatmentSessionConfiguration.cs -- if it manually configures Id/CreatedAt, remove those manual configs since Entity base class provides them. The base entity configuration should handle Id, CreatedAt, UpdatedAt, IsDeleted via a shared convention or base config.

    2. Same for SessionConsumableConfiguration.cs -- remove any manual Id configuration now that SessionConsumable extends Entity.

    3. Verify that the global query filter for IsDeleted is applied (should be automatic from shared infrastructure pattern).
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>EF Core configurations updated to work with Entity base class. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate migration and verify schema correctness</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/20260308073048_InitialTreatment.cs
    backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/20260308073048_InitialTreatment.Designer.cs
    backend/src/Modules/Treatment/Treatment.Infrastructure/Migrations/TreatmentDbContextModelSnapshot.cs
  </files>
  <action>
    1. First, check if BranchId already exists in the current migration file (RESEARCH.md says it does at line 68). If it does, note this.

    2. Delete the existing migration files:
       - `20260308073048_InitialTreatment.cs`
       - `20260308073048_InitialTreatment.Designer.cs`
       - `TreatmentDbContextModelSnapshot.cs`

    3. Generate a new migration:
       ```bash
       cd backend && dotnet ef migrations add InitialTreatment --project src/Modules/Treatment/Treatment.Infrastructure --startup-project src/Bootstrapper --context TreatmentDbContext --output-dir Migrations
       ```

    4. Verify the generated migration contains:
       - `UpdatedAt` and `IsDeleted` columns on `TreatmentSessions` table
       - `UpdatedAt` and `IsDeleted` columns on `SessionConsumables` table
       - `BranchId` column on `TreatmentProtocols` table
       - All existing columns preserved

    5. Build to verify migration compiles.
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q</automated>
  </verify>
  <done>Migration regenerated with UpdatedAt/IsDeleted on TreatmentSessions and SessionConsumables, BranchId on TreatmentProtocols confirmed. No runtime SqlException risk.</done>
</task>

</tasks>

<verification>
```bash
cd backend && dotnet build src/Modules/Treatment/Treatment.Infrastructure/Treatment.Infrastructure.csproj --no-restore -v q
```
</verification>

<success_criteria>
- Migration includes UpdatedAt and IsDeleted columns for TreatmentSessions and SessionConsumables
- BranchId exists on TreatmentProtocols
- Migration compiles without errors
- Model snapshot matches entity definitions
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-03-SUMMARY.md`
</output>
