---
phase: 09.1-fix-phase-09-code-review-issues
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/routes/_authenticated/treatments/index.tsx
  - frontend/src/app/routes/_authenticated/treatments/$packageId.tsx
  - frontend/src/app/routes/_authenticated/treatments/templates.tsx
  - frontend/src/app/routes/_authenticated/treatments/approvals.tsx
  - frontend/src/features/treatment/components/CancellationApprovalQueue.tsx
  - frontend/src/features/treatment/components/CancellationRequestDialog.tsx
  - frontend/src/features/treatment/components/ProtocolTemplateList.tsx
  - frontend/public/locales/en/treatment.json
  - frontend/public/locales/vi/treatment.json
autonomous: true
requirements: [TRT-10, TRT-01, TRT-09]

must_haves:
  truths:
    - "Treatment route pages check for Treatment.View permission before rendering"
    - "Unauthorized users see an access denied message or are redirected"
    - "All hardcoded English strings in CancellationApprovalQueue and CancellationRequestDialog use i18n t() calls"
    - "ProtocolTemplateList column headers use i18n t() calls"
  artifacts:
    - path: "frontend/src/app/routes/_authenticated/treatments/index.tsx"
      provides: "Permission-guarded treatment list page"
      contains: "Treatment.View"
    - path: "frontend/public/locales/en/treatment.json"
      provides: "English translations for approval queue and template list"
    - path: "frontend/public/locales/vi/treatment.json"
      provides: "Vietnamese translations for approval queue and template list"
  key_links:
    - from: "Treatment route files"
      to: "authStore.permissions"
      via: "useAuthStore permission check"
      pattern: "Treatment\\.View"
---

<objective>
Add frontend permission guards on all treatment route pages and fix i18n gaps in CancellationApprovalQueue, CancellationRequestDialog, and ProtocolTemplateList components.

Purpose: Any authenticated user can currently access treatment pages (TRT-10 violation). Untranslated strings break Vietnamese user experience.
Output: Permission-protected routes and fully internationalized treatment components.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-CONTEXT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-RESEARCH.md
@frontend/src/app/routes/_authenticated/treatments/index.tsx
@frontend/src/app/routes/_authenticated/treatments/$packageId.tsx
@frontend/src/app/routes/_authenticated/treatments/templates.tsx
@frontend/src/app/routes/_authenticated/treatments/approvals.tsx
@frontend/src/features/treatment/components/CancellationApprovalQueue.tsx
@frontend/src/features/treatment/components/CancellationRequestDialog.tsx
@frontend/src/features/treatment/components/ProtocolTemplateList.tsx
@frontend/public/locales/en/treatment.json
@frontend/public/locales/vi/treatment.json

<interfaces>
Frontend permission guard pattern (from audit-logs route):
```typescript
import { useAuthStore } from "@/stores/auth-store";

const hasPermission = useAuthStore(s =>
    s.user?.permissions?.includes('Treatment.View')
);
if (!hasPermission) {
    return <UnauthorizedPage />;  // or navigate away
}
```

i18n pattern (from billing module):
```typescript
import { useTranslation } from "react-i18next";
const { t } = useTranslation("treatment");  // namespace = treatment
// Usage: {t("approvals.title")}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add permission guards to all treatment route pages</name>
  <files>
    frontend/src/app/routes/_authenticated/treatments/index.tsx
    frontend/src/app/routes/_authenticated/treatments/$packageId.tsx
    frontend/src/app/routes/_authenticated/treatments/templates.tsx
    frontend/src/app/routes/_authenticated/treatments/approvals.tsx
  </files>
  <action>
    1. In each of the 4 treatment route files, add a permission check at the start of the component:
       - Import `useAuthStore` from the auth store
       - Check `user?.permissions?.includes('Treatment.View')` for view pages
       - For `templates.tsx`: additionally check `Treatment.Create` or `Treatment.Manage` for create/manage operations (show view but disable actions)
       - For `approvals.tsx`: check `Treatment.Manage` since only managers should access the approval queue

    2. If the user lacks the required permission, show an unauthorized message or redirect:
       - Check what pattern other protected routes use (e.g., audit-logs route)
       - Use the same pattern for consistency

    3. Within pages, conditionally render action buttons based on write permissions:
       - "Create Package" button: check `Treatment.Create`
       - "Create Template" button: check `Treatment.Create`
       - Approve/Reject buttons: check `Treatment.Manage`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 4 treatment route pages check permissions before rendering. Unauthorized users see access denied. Write actions gated by specific permissions (TRT-10).</done>
</task>

<task type="auto">
  <name>Task 2: Fix i18n gaps in approval queue, cancellation dialog, and template list</name>
  <files>
    frontend/src/features/treatment/components/CancellationApprovalQueue.tsx
    frontend/src/features/treatment/components/CancellationRequestDialog.tsx
    frontend/src/features/treatment/components/ProtocolTemplateList.tsx
    frontend/public/locales/en/treatment.json
    frontend/public/locales/vi/treatment.json
  </files>
  <action>
    1. Scan `CancellationApprovalQueue.tsx` for all hardcoded English strings (column headers, labels, button text, status labels, empty state messages). Replace each with `t("treatment:approvals.{key}")` calls using `useTranslation("treatment")`.

    2. Scan `CancellationRequestDialog.tsx` for hardcoded strings (dialog title, form labels, validation messages, button text). Replace with `t("treatment:cancellation.{key}")` calls.

    3. Scan `ProtocolTemplateList.tsx` for hardcoded English column headers (Name, Type, Sessions, Status, etc.) and replace with `t("treatment:templates.{key}")` calls.

    4. Add the corresponding translation keys to `frontend/public/locales/en/treatment.json`:
       - `approvals.*` keys for CancellationApprovalQueue
       - `cancellation.*` keys for CancellationRequestDialog
       - `templates.*` keys for ProtocolTemplateList column headers

    5. Add Vietnamese translations to `frontend/public/locales/vi/treatment.json` for all new keys. Use proper Vietnamese diacritics:
       - "Approval Queue" -> "Danh sach duyet"
       - "Request Cancellation" -> "Yeu cau huy"
       - "Deduction Percent" -> "Phan tram khau tru"
       - "Template Name" -> "Ten mau"
       - "Sessions" -> "So buoi"
       - etc. (use proper diacritics in actual file)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All hardcoded English strings in CancellationApprovalQueue, CancellationRequestDialog, and ProtocolTemplateList replaced with i18n t() calls. English and Vietnamese translation JSON files updated. TypeScript clean.</done>
</task>

</tasks>

<verification>
```bash
cd frontend && npx tsc --noEmit
```
</verification>

<success_criteria>
- All 4 treatment route pages check Treatment.View permission
- approvals.tsx checks Treatment.Manage permission
- Write action buttons conditionally rendered based on permissions
- No hardcoded English in CancellationApprovalQueue, CancellationRequestDialog, ProtocolTemplateList
- treatment.json (en + vi) contains all new translation keys
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-05-SUMMARY.md`
</output>
