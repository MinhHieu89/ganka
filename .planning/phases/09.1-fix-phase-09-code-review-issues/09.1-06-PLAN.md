---
phase: 09.1-fix-phase-09-code-review-issues
plan: 06
type: execute
wave: 2
depends_on: ["09.1-02"]
files_modified:
  - backend/src/Modules/Treatment/Treatment.Application/Features/RegisterOsdiToken.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/GetTreatmentPackageVersions.cs
  - backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs
  - frontend/src/features/treatment/components/SessionOsdiCapture.tsx
  - frontend/src/features/treatment/components/VersionHistoryDialog.tsx
  - frontend/src/features/treatment/api/treatment-api.ts
  - frontend/src/features/treatment/api/treatment-queries.ts
autonomous: true
requirements: [TRT-03, TRT-07]

must_haves:
  truths:
    - "QR code in SessionOsdiCapture generates correct URL format (/osdi/:token)"
    - "QR token is registered with backend before QR code is displayed"
    - "VersionHistoryDialog fetches version data from a working backend endpoint"
    - "GET /api/treatments/packages/{id}/versions returns version history"
    - "POST /api/treatments/osdi-tokens registers a token linked to package/session"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Application/Features/RegisterOsdiToken.cs"
      provides: "OSDI token registration handler"
      contains: "RegisterOsdiTokenCommand"
    - path: "backend/src/Modules/Treatment/Treatment.Application/Features/GetTreatmentPackageVersions.cs"
      provides: "Version history query handler"
      contains: "GetTreatmentPackageVersionsQuery"
    - path: "frontend/src/features/treatment/components/SessionOsdiCapture.tsx"
      provides: "Correct QR URL and token registration"
      contains: "/osdi/"
  key_links:
    - from: "SessionOsdiCapture.tsx"
      to: "POST /api/treatments/osdi-tokens"
      via: "API call on component mount"
      pattern: "osdi-tokens"
    - from: "VersionHistoryDialog.tsx"
      to: "GET /api/treatments/packages/{id}/versions"
      via: "useQuery hook"
      pattern: "versions"
---

<objective>
Fix the broken QR OSDI self-fill flow by creating a backend token registration endpoint and fixing the frontend QR URL format. Add the missing version history API endpoint so the VersionHistoryDialog works.

Purpose: QR-based OSDI self-fill is completely broken (always 404s) and version history dialog shows nothing. Both are critical features.
Output: Working QR OSDI token flow and working version history display.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-CONTEXT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-RESEARCH.md
@frontend/src/features/treatment/components/SessionOsdiCapture.tsx
@frontend/src/features/treatment/components/VersionHistoryDialog.tsx
@frontend/src/features/treatment/api/treatment-api.ts
@frontend/src/features/treatment/api/treatment-queries.ts
@backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs
@backend/src/Modules/Treatment/Treatment.Domain/Entities/TreatmentPackage.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create backend endpoints for OSDI token registration and version history</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Application/Features/RegisterOsdiToken.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/GetTreatmentPackageVersions.cs
    backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs
  </files>
  <action>
    1. Create `RegisterOsdiToken.cs` handler:
       - Command: `RegisterOsdiTokenCommand(Guid PackageId, int? SessionNumber, string Token)`
       - Handler stores the token linked to the package/session context. Use an in-memory cache or a simple database table (check if there's already an OSDI-related entity). A lightweight approach: store in a `ConcurrentDictionary<string, OsdiTokenInfo>` in a singleton service, with TTL expiration (e.g., 24 hours). If the project already has a cache abstraction, use that.
       - Return the token in the response so frontend can confirm registration.
       - Add FluentValidation: Token required, PackageId required.

    2. Create `GetTreatmentPackageVersions.cs` query handler:
       - Query: `GetTreatmentPackageVersionsQuery(Guid PackageId)`
       - Handler loads the TreatmentPackage aggregate (which has `_versions` collection)
       - Maps `ProtocolVersion` entities to a `ProtocolVersionDto` (create this DTO if it doesn't exist in Contracts/Dtos)
       - ProtocolVersionDto should include: VersionNumber, TotalSessions, SessionsCompleted, TreatmentType, Notes, ModifiedAt, ModifiedBy
       - Return `List<ProtocolVersionDto>` ordered by VersionNumber descending

    3. In `TreatmentApiEndpoints.cs`, add both endpoints:
       - `POST /api/treatments/osdi-tokens` -> RegisterOsdiTokenCommand (RequireAuthorization only -- staff registers tokens)
       - `GET /api/treatments/packages/{id:guid}/versions` -> GetTreatmentPackageVersionsQuery (RequireAuthorization)
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj --no-restore -v q</automated>
  </verify>
  <done>POST /api/treatments/osdi-tokens endpoint registers QR tokens. GET /api/treatments/packages/{id}/versions returns version history. Both endpoints compile and are mapped.</done>
</task>

<task type="auto">
  <name>Task 2: Fix frontend QR URL and wire VersionHistoryDialog to API</name>
  <files>
    frontend/src/features/treatment/components/SessionOsdiCapture.tsx
    frontend/src/features/treatment/components/VersionHistoryDialog.tsx
    frontend/src/features/treatment/api/treatment-api.ts
    frontend/src/features/treatment/api/treatment-queries.ts
  </files>
  <action>
    1. In `treatment-api.ts`, add two new API functions:
       - `registerOsdiToken(data: { packageId: string; sessionNumber?: number; token: string })` -> POST /api/treatments/osdi-tokens
       - `getPackageVersions(packageId: string)` -> GET /api/treatments/packages/{packageId}/versions
       - Add `usePackageVersions(packageId: string)` query hook using TanStack Query

    2. In `treatment-queries.ts`, add query keys:
       - `versions: (packageId: string) => [...treatmentKeys.all, "versions", packageId] as const`
       - `osdiTokens: () => [...treatmentKeys.all, "osdi-tokens"] as const`

    3. In `SessionOsdiCapture.tsx`:
       - Fix QR URL format: change from `/osdi/self-fill?token={token}` to `/osdi/{token}`
       - On component mount (or when generating QR), call `registerOsdiToken` mutation to POST the token to backend BEFORE displaying the QR code
       - Use `useMutation` for token registration
       - Only show QR code after token registration succeeds
       - Show loading state while registering

    4. In `VersionHistoryDialog.tsx`:
       - Import and use the `usePackageVersions(packageId)` query hook
       - Replace any placeholder/empty data with the actual query results
       - Display version list with: version number, treatment type, total sessions, notes, modified date, modified by
       - Handle loading and empty states
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>SessionOsdiCapture generates correct QR URL and registers token with backend (TRT-03). VersionHistoryDialog fetches and displays version history from API (TRT-07). TypeScript clean.</done>
</task>

</tasks>

<verification>
```bash
cd frontend && npx tsc --noEmit && cd ../backend && dotnet build src/Modules/Treatment/ --no-restore -v q
```
</verification>

<success_criteria>
- QR code URL uses /osdi/:token format (not query param)
- Token is registered with backend before QR is shown
- Version history dialog loads data from GET /packages/{id}/versions
- Both backend endpoints compile and are mapped
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-06-SUMMARY.md`
</output>
