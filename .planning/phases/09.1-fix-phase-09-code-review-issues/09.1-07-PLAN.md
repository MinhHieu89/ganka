---
phase: 09.1-fix-phase-09-code-review-issues
plan: 07
type: execute
wave: 3
depends_on: ["09.1-01", "09.1-02", "09.1-03"]
files_modified:
  - backend/tests/Treatment.Unit.Tests/Treatment.Unit.Tests.csproj
  - backend/tests/Treatment.Unit.Tests/Features/TreatmentPackageHandlerTests.cs
  - backend/tests/Treatment.Unit.Tests/Features/SessionHandlerTests.cs
  - backend/tests/Treatment.Unit.Tests/Features/CancellationHandlerTests.cs
  - backend/tests/Treatment.Unit.Tests/Features/ModifyPackageHandlerTests.cs
autonomous: true
requirements: [TRT-02, TRT-04, TRT-05, TRT-09, TRT-10]

must_haves:
  truths:
    - "All test files compile and run without sentinel file exclusions"
    - "Tests verify interval enforcement with and without override"
    - "Tests verify session numbering excludes cancelled sessions"
    - "Tests verify Modify() auto-completion"
    - "Tests verify deduction range 10-20% validation"
    - "Test coverage for new domain behaviors is at least 80%"
  artifacts:
    - path: "backend/tests/Treatment.Unit.Tests/Treatment.Unit.Tests.csproj"
      provides: "Clean csproj without sentinel file exclusions"
    - path: "backend/tests/Treatment.Unit.Tests/Features/TreatmentPackageHandlerTests.cs"
      provides: "Tests for interval, numbering, deduction, auto-completion"
    - path: "backend/tests/Treatment.Unit.Tests/Features/SessionHandlerTests.cs"
      provides: "Tests for session recording with ICurrentUser"
  key_links:
    - from: "Test files"
      to: "Domain entity methods"
      via: "Direct method calls on TreatmentPackage"
      pattern: "RecordSession|Modify|RequestCancellation"
---

<objective>
Remove sentinel file exclusions from the test project, fix existing tests to work with domain changes from Plans 01-02, and add new tests covering interval enforcement, session numbering, auto-completion, deduction validation, and ICurrentUser usage.

Purpose: Tests must verify all domain and application fixes. Sentinel exclusions are a CI risk -- tests that never run can't catch regressions.
Output: Full test suite with 80%+ coverage of treatment business logic, no sentinel file exclusions.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-CONTEXT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-RESEARCH.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-01-SUMMARY.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-02-SUMMARY.md
@backend/tests/Treatment.Unit.Tests/Treatment.Unit.Tests.csproj
@backend/tests/Treatment.Unit.Tests/Features/TreatmentPackageHandlerTests.cs
@backend/tests/Treatment.Unit.Tests/Features/SessionHandlerTests.cs
@backend/tests/Treatment.Unit.Tests/Features/CancellationHandlerTests.cs
@backend/tests/Treatment.Unit.Tests/Features/ModifyPackageHandlerTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove sentinel exclusions and fix existing tests</name>
  <files>
    backend/tests/Treatment.Unit.Tests/Treatment.Unit.Tests.csproj
    backend/tests/Treatment.Unit.Tests/Features/TreatmentPackageHandlerTests.cs
    backend/tests/Treatment.Unit.Tests/Features/SessionHandlerTests.cs
  </files>
  <action>
    1. In `Treatment.Unit.Tests.csproj`, remove lines 34-38 (the sentinel file conditional compile removal):
       ```xml
       <!-- DELETE THIS BLOCK -->
       <ItemGroup>
         <Compile Remove="Features\TreatmentPackageHandlerTests.cs" Condition="!Exists('Features\.TreatmentPackageHandlerTests.ready')" />
         <Compile Remove="Features\SessionHandlerTests.cs" Condition="!Exists('Features\.SessionHandlerTests.ready')" />
       </ItemGroup>
       ```

    2. Delete any `.ready` sentinel files if they exist in the Features directory.

    3. Fix `TreatmentPackageHandlerTests.cs` to compile with the updated domain:
       - Update any calls to `RecordSession()` to include the new `intervalOverrideReason` parameter
       - Update any assertions that depend on session numbering to account for cancelled session filtering
       - Fix any mock setups that broke due to domain signature changes

    4. Fix `SessionHandlerTests.cs` to compile with the updated handler:
       - Add `ICurrentUser` mock setup where needed (RecordTreatmentSession handler now expects it)
       - Update test data to match new command structure

    5. Build and run existing tests to verify they pass.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Treatment.Unit.Tests --no-restore -v q 2>&1 | tail -20</automated>
  </verify>
  <done>Sentinel exclusions removed. All existing tests compile and pass with updated domain/handler signatures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add new tests for domain and handler fixes</name>
  <files>
    backend/tests/Treatment.Unit.Tests/Features/TreatmentPackageHandlerTests.cs
    backend/tests/Treatment.Unit.Tests/Features/SessionHandlerTests.cs
    backend/tests/Treatment.Unit.Tests/Features/CancellationHandlerTests.cs
    backend/tests/Treatment.Unit.Tests/Features/ModifyPackageHandlerTests.cs
  </files>
  <behavior>
    - RecordSession_WithIntervalViolation_NoOverride_ThrowsException
    - RecordSession_WithIntervalViolation_WithOverride_Succeeds
    - RecordSession_FirstSession_NoIntervalCheck_Succeeds
    - RecordSession_SessionNumber_ExcludesCancelledSessions
    - Modify_TotalSessionsEqualsCompleted_AutoCompletes
    - Modify_TotalSessionsAboveCompleted_StaysActive
    - Modify_NoChanges_DoesNotCreateVersion
    - RequestCancellation_DeductionBelow10_Throws
    - RequestCancellation_DeductionAbove20_Throws
    - RequestCancellation_Deduction15_Succeeds
    - ApproveCancellation_UsesManagerDeductionOverride
    - RequestCancellation_ProtocolNotFound_ReturnsError
  </behavior>
  <action>
    Write tests following RED-GREEN-REFACTOR:
    1. Since domain code is already fixed (Plan 01), write tests that should pass immediately (GREEN phase -- we're testing existing fixes).

    2. In `TreatmentPackageHandlerTests.cs` or a new domain test file, add:
       - Interval enforcement tests (3 tests: violation+no override, violation+override, first session)
       - Session numbering test (cancel a session, record new one, verify number)
       - Modify auto-completion tests (2 tests: equals completed, above completed)
       - Modify no-op detection test

    3. In `CancellationHandlerTests.cs`, add:
       - Deduction range tests (3 tests: below 10, above 20, valid 15)
       - ApproveCancellation override deduction test
       - RequestCancellation protocol-not-found test (verify error, not fallback)

    4. All tests should use xUnit + NSubstitute + FluentAssertions per project convention.

    5. For domain-level tests, create TreatmentPackage instances directly using the factory method/constructor and test domain methods.

    6. For handler-level tests, mock ITreatmentPackageRepository, IUnitOfWork, ICurrentUser, and invoke the static handler method directly.

    7. Run all tests and verify 80%+ pass rate.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Treatment.Unit.Tests --no-restore -v q 2>&1 | tail -20</automated>
  </verify>
  <done>12+ new tests covering interval enforcement, session numbering, auto-completion, deduction validation, and handler fixes. All tests pass. No sentinel exclusions remain.</done>
</task>

</tasks>

<verification>
```bash
cd backend && dotnet test tests/Treatment.Unit.Tests --no-restore -v q
```
</verification>

<success_criteria>
- No sentinel file exclusions in Treatment.Unit.Tests.csproj
- All existing tests pass after domain/handler changes
- 12+ new tests covering TRT-02, TRT-04, TRT-05, TRT-09, TRT-10 behaviors
- All tests pass (green)
- Test project builds and runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-07-SUMMARY.md`
</output>
