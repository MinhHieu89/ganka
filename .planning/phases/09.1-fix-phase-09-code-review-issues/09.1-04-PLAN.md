---
phase: 09.1-fix-phase-09-code-review-issues
plan: 04
type: execute
wave: 2
depends_on: ["09.1-02"]
files_modified:
  - frontend/src/features/treatment/components/TreatmentPackageDetail.tsx
  - frontend/src/features/treatment/components/TreatmentSessionCard.tsx
  - frontend/src/features/treatment/components/ModifyPackageDialog.tsx
  - frontend/src/features/treatment/components/PatientTreatmentsTab.tsx
  - frontend/src/features/treatment/api/treatment-queries.ts
  - frontend/src/features/treatment/api/treatment-api.ts
autonomous: true
requirements: [TRT-02, TRT-03, TRT-07, TRT-08, TRT-09]

must_haves:
  truths:
    - "All action buttons in TreatmentPackageDetail open their corresponding dialogs"
    - "TreatmentSessionCard displays correct IPL/LLLT parameter values from form data"
    - "ModifyPackageDialog prevents reducing totalSessions below sessionsCompleted"
    - "Back navigation from package detail goes to treatments list, not /dashboard"
    - "useProtocolTemplates query key includes type filter to avoid cache collisions"
    - "PatientTreatmentsTab passes patientId to TreatmentPackageForm"
  artifacts:
    - path: "frontend/src/features/treatment/components/TreatmentPackageDetail.tsx"
      provides: "Fully wired action buttons with dialog state management"
      contains: "useState"
    - path: "frontend/src/features/treatment/components/TreatmentSessionCard.tsx"
      provides: "Correct JSON key mapping for session parameters"
      contains: "pulseCount"
    - path: "frontend/src/features/treatment/api/treatment-queries.ts"
      provides: "Query key with type filter"
      contains: "templates"
  key_links:
    - from: "TreatmentPackageDetail buttons"
      to: "Dialog components"
      via: "useState + onClick handlers"
      pattern: "onClick.*set.*Open"
    - from: "TreatmentSessionCard"
      to: "TreatmentSessionForm serialization"
      via: "matching JSON keys"
      pattern: "pulseCount|treatmentZones|treatmentArea"
---

<objective>
Wire all non-functional frontend buttons to their dialog components, fix JSON key mismatches between session form and card, fix ModifyPackageDialog validation, fix navigation and query key issues, and pass patientId to TreatmentPackageForm.

Purpose: Frontend buttons and data display are broken -- users cannot interact with treatment packages despite backend handlers existing.
Output: Fully functional treatment package detail page with working dialogs, correct data display, and proper navigation.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-CONTEXT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-RESEARCH.md
@frontend/src/features/treatment/components/TreatmentPackageDetail.tsx
@frontend/src/features/treatment/components/TreatmentSessionCard.tsx
@frontend/src/features/treatment/components/ModifyPackageDialog.tsx
@frontend/src/features/treatment/components/PatientTreatmentsTab.tsx
@frontend/src/features/treatment/api/treatment-queries.ts
@frontend/src/features/treatment/api/treatment-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire action buttons to dialogs in TreatmentPackageDetail</name>
  <files>
    frontend/src/features/treatment/components/TreatmentPackageDetail.tsx
  </files>
  <action>
    1. Add useState hooks for each dialog:
       - `sessionFormOpen` for TreatmentSessionForm (Record Session button)
       - `modifyDialogOpen` for ModifyPackageDialog (Modify button)
       - `cancelDialogOpen` for CancellationRequestDialog (Request Cancellation button)
       - `switchDialogOpen` for SwitchTreatmentDialog (Switch Treatment button)
       - `historyDialogOpen` for VersionHistoryDialog (Version History button)

    2. Add onClick handlers to each existing action button:
       - Record Session -> `onClick={() => setSessionFormOpen(true)}`
       - Modify -> `onClick={() => setModifyDialogOpen(true)}`
       - Request Cancellation -> `onClick={() => setCancelDialogOpen(true)}`
       - Switch Treatment -> `onClick={() => setSwitchDialogOpen(true)}`
       - Version History -> `onClick={() => setHistoryDialogOpen(true)}`

    3. Import and render each dialog component at the bottom of the JSX, passing:
       - `open` and `onOpenChange` props for each dialog
       - Package data as needed (`package_`, `packageId`, etc.)
       - `onSuccess` callbacks that invalidate the package query

    4. Fix back navigation: replace any hardcoded `/dashboard` with `/treatments` (the treatments list route).

    5. Conditionally show action buttons based on package status (e.g., don't show "Record Session" if package is Completed/Cancelled).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All action buttons in TreatmentPackageDetail wire to their respective dialog components. Back navigation points to /treatments. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix JSON keys, form validation, query keys, and patientId passing</name>
  <files>
    frontend/src/features/treatment/components/TreatmentSessionCard.tsx
    frontend/src/features/treatment/components/ModifyPackageDialog.tsx
    frontend/src/features/treatment/components/PatientTreatmentsTab.tsx
    frontend/src/features/treatment/api/treatment-queries.ts
    frontend/src/features/treatment/api/treatment-api.ts
  </files>
  <action>
    1. In `TreatmentSessionCard.tsx`, fix JSON key reads to match form serialization:
       - IPL: read `pulseCount` instead of `pulses`, `treatmentZones` instead of `zones`
       - LLLT: read `treatmentArea` instead of `area`
       - Verify by checking `TreatmentSessionForm.tsx` form field names and align card display keys

    2. In `ModifyPackageDialog.tsx`, add validation to prevent reducing `totalSessions` below `sessionsCompleted`:
       - In the zod schema, add `.refine()` or `.superRefine()` that checks `totalSessions >= package.sessionsCompleted`
       - Show error message: "Cannot reduce below completed sessions ({N})"
       - Or use `min()` with dynamic minimum based on current sessionsCompleted

    3. In `PatientTreatmentsTab.tsx`, pass `patientId` prop to `TreatmentPackageForm`:
       - The tab component has access to the patient context
       - Pass `patientId={patientId}` to the form component so new packages are correctly linked

    4. In `treatment-queries.ts`, update the templates query key factory to include type filter:
       - Change: `templates: () => [...treatmentKeys.all, "templates"] as const`
       - To: `templates: (type?: string) => [...treatmentKeys.all, "templates", type ?? "all"] as const`

    5. In `treatment-api.ts`, update `useProtocolTemplates` hook to pass type to query key:
       - Pass the type parameter to `treatmentKeys.templates(type)` in queryKey
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TreatmentSessionCard reads correct JSON keys (TRT-03), ModifyPackageDialog validates session count floor, PatientTreatmentsTab passes patientId, query key includes type filter. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
```bash
cd frontend && npx tsc --noEmit
```
</verification>

<success_criteria>
- All 5 action buttons in TreatmentPackageDetail open corresponding dialogs
- TreatmentSessionCard shows correct IPL/LLLT parameter values
- ModifyPackageDialog blocks totalSessions below sessionsCompleted
- Back navigation goes to /treatments
- Template query key includes type for cache isolation
- PatientTreatmentsTab passes patientId to form
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-04-SUMMARY.md`
</output>
