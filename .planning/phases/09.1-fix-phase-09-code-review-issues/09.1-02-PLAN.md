---
phase: 09.1-fix-phase-09-code-review-issues
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/RecordTreatmentSession.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/ApproveCancellation.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/RequestCancellation.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/ModifyTreatmentPackage.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/PauseTreatmentPackage.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/SwitchTreatmentType.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/GetDueSoonSessions.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/GetPendingCancellations.cs
  - backend/src/Modules/Treatment/Treatment.Application/Features/GetProtocolTemplates.cs
autonomous: true
requirements: [TRT-01, TRT-07, TRT-08, TRT-09, TRT-10]

must_haves:
  truths:
    - "All write endpoints require specific Treatment.* permissions"
    - "RecordTreatmentSession uses ICurrentUser.UserId, not client-provided PerformedById"
    - "ApproveCancellation uses manager's DeductionPercent to recalculate refund"
    - "RequestCancellation returns error when protocol not found instead of hardcoded fallback"
    - "SwitchTreatmentType verifies new template exists before marking package as switched"
  artifacts:
    - path: "backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs"
      provides: "Permission-gated endpoints"
      contains: "RequirePermissions"
    - path: "backend/src/Modules/Treatment/Treatment.Application/Features/RecordTreatmentSession.cs"
      provides: "Secure session recording with ICurrentUser"
      contains: "ICurrentUser"
  key_links:
    - from: "TreatmentApiEndpoints.cs"
      to: "Permissions.Treatment.*"
      via: "RequirePermissions extension"
      pattern: "RequirePermissions.*Treatment"
    - from: "RecordTreatmentSession handler"
      to: "ICurrentUser"
      via: "Wolverine auto-injection"
      pattern: "ICurrentUser currentUser"
---

<objective>
Fix all critical and important application layer issues: add RequirePermissions authorization on all write endpoints (TRT-10), inject ICurrentUser in RecordSession, fix ApproveCancellation deduction override, remove hardcoded fallback in RequestCancellation, fix protocol name in DTOs, add OSDI validation, reorder SwitchTreatmentType, and fix GetProtocolTemplates type filter.

Purpose: Application layer security and correctness -- prevents unauthorized access and fixes logic errors in handlers.
Output: Permission-gated endpoints, secure session recording, correct cancellation workflow, accurate DTO population.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-CONTEXT.md
@.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-RESEARCH.md
@backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs
@backend/src/Modules/Treatment/Treatment.Application/Features/RecordTreatmentSession.cs
@backend/src/Modules/Treatment/Treatment.Application/Features/ApproveCancellation.cs
@backend/src/Modules/Treatment/Treatment.Application/Features/RequestCancellation.cs
@backend/src/Modules/Treatment/Treatment.Application/Features/SwitchTreatmentType.cs
@backend/src/Shared/Shared.Presentation/EndpointAuthorizationExtensions.cs
@backend/src/Shared/Shared.Domain/Permissions.cs

<interfaces>
From Shared.Domain/Permissions.cs:
```csharp
public static class Treatment
{
    public const string View = "Treatment.View";
    public const string Create = "Treatment.Create";
    public const string Update = "Treatment.Update";
    public const string Delete = "Treatment.Delete";
    public const string Export = "Treatment.Export";
    public const string Manage = "Treatment.Manage";
}
```

From Shared.Presentation/EndpointAuthorizationExtensions.cs:
```csharp
public static TBuilder RequirePermissions<TBuilder>(this TBuilder builder, params string[] permissions)
```

From Shared.Application/ICurrentUser.cs:
```csharp
public interface ICurrentUser
{
    Guid UserId { get; }
    string Email { get; }
    Guid BranchId { get; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RequirePermissions to all write endpoints and fix RecordSession/ApproveCancellation</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Presentation/TreatmentApiEndpoints.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/RecordTreatmentSession.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/ApproveCancellation.cs
  </files>
  <action>
    1. In `TreatmentApiEndpoints.cs`, chain `.RequirePermissions()` on ALL write endpoints:
       - POST /protocols -> Treatment.Create
       - PUT /protocols/{id} -> Treatment.Update
       - POST /packages -> Treatment.Create
       - POST /packages/{id}/sessions -> Treatment.Create
       - PUT /packages/{id}/modify -> Treatment.Update
       - POST /packages/{id}/switch -> Treatment.Update
       - POST /packages/{id}/pause -> Treatment.Update
       - POST /packages/{id}/cancel -> Treatment.Update
       - POST /packages/{id}/cancel/approve -> Treatment.Manage
       - POST /packages/{id}/cancel/reject -> Treatment.Manage
       Keep GET endpoints with only RequireAuthorization() (any authenticated user with Treatment.View).

    2. In `RecordTreatmentSession.cs` handler:
       - Add `ICurrentUser currentUser` to the handler method signature (Wolverine auto-injects)
       - Replace usage of `command.PerformedById` with `currentUser.UserId`
       - If PerformedById is in the command, keep it but ignore it (or remove from command if not used elsewhere)
       - Pass `command.IntervalOverrideReason` (add to command if not present) to domain's RecordSession method

    3. In `ApproveCancellation.cs` handler:
       - Use the command's `DeductionPercent` to recalculate the refund amount
       - Update the CancellationRequest entity's DeductionPercent with the manager's override value before approving
       - Ensure the domain's Approve method receives the potentially different deduction percentage
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Modules/Treatment/Treatment.Presentation/Treatment.Presentation.csproj --no-restore -v q</automated>
  </verify>
  <done>All write endpoints gated with RequirePermissions (TRT-10), RecordSession uses ICurrentUser (TRT-10), ApproveCancellation uses override deduction (TRT-09).</done>
</task>

<task type="auto">
  <name>Task 2: Fix remaining handler logic issues</name>
  <files>
    backend/src/Modules/Treatment/Treatment.Application/Features/RequestCancellation.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/ModifyTreatmentPackage.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/PauseTreatmentPackage.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/SwitchTreatmentType.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/GetDueSoonSessions.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/GetPendingCancellations.cs
    backend/src/Modules/Treatment/Treatment.Application/Features/GetProtocolTemplates.cs
  </files>
  <action>
    1. `RequestCancellation.cs`: Remove the hardcoded `?? 15m` fallback when protocol is not found. Instead, return `Result.NotFound("Protocol template not found")` or propagate the error. The handler should fail clearly rather than use a magic number.

    2. `ModifyTreatmentPackage.cs` and `PauseTreatmentPackage.cs`: Fix the response DTO to include `ProtocolTemplateName` by loading the protocol template and setting it on the DTO. Currently these return empty string for the protocol name.

    3. `SwitchTreatmentType.cs`: Reorder operations -- verify the new protocol template exists BEFORE calling `package.MarkAsSwitched()`. Currently the package is marked as switched even if the new template lookup fails.

    4. `GetDueSoonSessions.cs` and `GetPendingCancellations.cs`: Include `ProtocolTemplateName` in the response DTOs by joining with the protocol template data. Currently hard-coded as empty string.

    5. `GetProtocolTemplates.cs`: Fix the type filter to work correctly with the `IncludeInactive` parameter. Currently the type filter bypasses the inactive check. Ensure both filters are applied together.

    6. Add OSDI score validation (0-100 range) to the `RecordTreatmentSession` validator if not already present. Check the FluentValidation rule.
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Modules/Treatment/Treatment.Application/Treatment.Application.csproj --no-restore -v q</automated>
  </verify>
  <done>RequestCancellation returns error instead of fallback (TRT-09), ModifyPackage/PausePackage/GetDueSoon/GetPending include protocol name (TRT-07), SwitchTreatmentType validates before mutating (TRT-08), OSDI validation 0-100 (TRT-03), type filter respects IncludeInactive.</done>
</task>

</tasks>

<verification>
```bash
cd backend && dotnet build src/Modules/Treatment/ --no-restore -v q
```
</verification>

<success_criteria>
- All POST/PUT/DELETE treatment endpoints have RequirePermissions
- RecordTreatmentSession uses ICurrentUser.UserId
- ApproveCancellation applies manager's DeductionPercent override
- RequestCancellation fails clearly when protocol not found
- SwitchTreatmentType validates before mutating
- All DTOs include ProtocolTemplateName
- OSDI score validated 0-100
- Backend builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-fix-phase-09-code-review-issues/09.1-02-SUMMARY.md`
</output>
