---
phase: 10.1-fix-all-issues-in-issues-md
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugCatalogItemRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugBatchRepository.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Contracts/Dtos/DrugBatchDto.cs
  - frontend/src/features/pharmacy/components/DrugInventoryTable.tsx
  - frontend/src/features/pharmacy/components/OtcSaleForm.tsx
  - frontend/public/locales/vi/pharmacy.json
  - frontend/public/locales/en/pharmacy.json
autonomous: true
requirements: [TASK-16, TASK-23, TASK-21, TASK-25, TASK-18]

must_haves:
  truths:
    - "OTC retail sales screen loads and displays medication catalog when no search term is entered"
    - "User can search and select medications in OTC retail sales"
    - "Stock=0 displays 'Out of Stock' status, not 'Low Stock'"
    - "Medications with stock=0 appear in inventory alerts"
    - "OTC sale form columns are properly aligned with no overlapping delete icon"
  artifacts:
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs"
      provides: "Returns all active drugs when search term is empty"
    - path: "frontend/src/features/pharmacy/components/DrugInventoryTable.tsx"
      provides: "Distinguishes Out of Stock from Low Stock display"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugBatchRepository.cs"
      provides: "Includes stock=0 drugs in low stock alerts"
  key_links:
    - from: "OtcSaleForm.tsx"
      to: "SearchDrugCatalog.cs"
      via: "pharmacy-api.ts getDrugCatalogList"
      pattern: "getDrugCatalogList|searchDrugCatalog"
    - from: "DrugInventoryTable.tsx"
      to: "DrugCatalogItemRepository.cs"
      via: "IsOutOfStock or TotalStock===0 check"
      pattern: "OutOfStock|totalStock.*===.*0"
---

<objective>
Fix pharmacy/OTC catalog loading (TASK-16/23), stock status display (TASK-21), inventory alerts for zero-stock (TASK-25), and OTC UI alignment (TASK-18).

Purpose: OTC retail is completely broken (cannot add medications). Stock status is misleading. These fixes restore core pharmacy functionality.
Output: Working OTC catalog, correct stock statuses, proper alerts, aligned OTC UI.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix OTC catalog loading and stock status logic (TASK-16/23, TASK-21, TASK-25)</name>
  <files>backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs, backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugCatalogItemRepository.cs, backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugBatchRepository.cs, backend/src/Modules/Pharmacy/Pharmacy.Contracts/Dtos/DrugBatchDto.cs</files>
  <read_first>backend/src/Modules/Pharmacy/Pharmacy.Application/Features/SearchDrugCatalog.cs, backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugCatalogItemRepository.cs, backend/src/Modules/Pharmacy/Pharmacy.Infrastructure/Repositories/DrugBatchRepository.cs, backend/src/Modules/Pharmacy/Pharmacy.Contracts/Dtos/DrugBatchDto.cs</read_first>
  <action>
TASK-16/23 (OTC catalog not loading):
In SearchDrugCatalog.cs, find the handler (around line 19-20) that returns empty array when SearchTerm is empty/whitespace. Change this: when SearchTerm is empty or whitespace, return ALL active drug catalog items (paginated if applicable) instead of returning an empty array. The query should behave like: if search term is provided, filter by it; if not, return all active drugs.

TASK-21 (Stock=0 shows Low Stock):
In DrugCatalogItemRepository.cs (around line 158), the query computes `isLowStock = d.MinStockLevel > 0 && totalStock < d.MinStockLevel`. This is true when stock=0. Add a separate boolean: `isOutOfStock = totalStock == 0`. Update the DTO to include this field.
In DrugBatchDto.cs (or the relevant DTO used by the inventory table), add `bool IsOutOfStock` property alongside the existing `IsLowStock`.

TASK-25 (No alert at stock=0):
In DrugBatchRepository.cs `GetLowStockAlertsAsync()` (around line 94-131), the query only includes drugs where `d.MinStockLevel > 0 && totalStock < d.MinStockLevel`. Add an additional condition: also include drugs where `totalStock == 0` regardless of MinStockLevel setting. This ensures stock=0 always triggers an alert.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Bootstrapper/Bootstrapper.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SearchDrugCatalog.cs does NOT return empty array for empty/whitespace search term -- it returns all active drugs
    - DrugCatalogItemRepository.cs contains `isOutOfStock` or `IsOutOfStock` computation where `totalStock == 0`
    - DrugBatchRepository.cs GetLowStockAlertsAsync includes drugs where `totalStock == 0` regardless of MinStockLevel
    - DrugBatchDto.cs (or equivalent DTO) contains `IsOutOfStock` boolean property
    - Backend builds successfully
  </acceptance_criteria>
  <done>OTC catalog returns drugs on empty search; stock=0 distinguished as Out of Stock; stock=0 triggers inventory alert</done>
</task>

<task type="auto">
  <name>Task 2: Fix frontend stock status display and OTC UI alignment (TASK-21 frontend, TASK-18)</name>
  <files>frontend/src/features/pharmacy/components/DrugInventoryTable.tsx, frontend/src/features/pharmacy/components/OtcSaleForm.tsx, frontend/public/locales/vi/pharmacy.json, frontend/public/locales/en/pharmacy.json</files>
  <read_first>frontend/src/features/pharmacy/components/DrugInventoryTable.tsx, frontend/src/features/pharmacy/components/OtcSaleForm.tsx, frontend/public/locales/vi/pharmacy.json, frontend/public/locales/en/pharmacy.json</read_first>
  <action>
TASK-21 (Frontend stock status display):
In DrugInventoryTable.tsx, find where the stock status badge is rendered (likely checking `isLowStock` boolean). Add logic to check `IsOutOfStock` first:
- If `isOutOfStock` (or `totalStock === 0`): Show red badge "Out of Stock" / "Het hang"
- Else if `isLowStock`: Show yellow/orange badge "Low Stock" / "Sap het hang"
- Else: Show green badge "In Stock" / "Con hang"
Add i18n keys to pharmacy.json files:
- en: `"inventory.outOfStock": "Out of Stock"`, `"inventory.lowStock": "Low Stock"`, `"inventory.inStock": "In Stock"`
- vi: `"inventory.outOfStock": "Het hang"`, `"inventory.lowStock": "Sap het hang"`, `"inventory.inStock": "Con hang"` (with proper diacritics)

Also check if a supply inventory table has the same issue -- the research says supply inventory correctly shows alerts at stock=0 but the status text may also need fixing. Look for any SupplyInventoryTable or ConsumablesTable component and apply the same fix if needed.

TASK-18 (OTC UI alignment):
In OtcSaleForm.tsx, find the grid layout (uses `grid-cols-[1fr_90px_130px_40px]` for headers around line 520 and items around line 207). Adjust:
- Increase delete button column from 40px to 48px: `grid-cols-[1fr_90px_130px_48px]`
- On the delete button, ensure it has `flex items-center justify-center` to center within the cell
- Verify headers and data columns use the SAME grid template
- If the delete icon overlaps the border, add `ml-1` or small padding to create separation
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - DrugInventoryTable.tsx contains conditional logic checking `isOutOfStock` or `totalStock === 0` before `isLowStock`
    - DrugInventoryTable.tsx renders "Out of Stock" text/badge for zero-stock items
    - OtcSaleForm.tsx grid column template is consistent between headers and data rows
    - OtcSaleForm.tsx delete button column width is at least 48px
    - vi/pharmacy.json contains `outOfStock` or `het hang` translation key
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Stock=0 shows "Out of Stock" badge in inventory table; OTC sale form columns aligned with no overlapping delete icon</done>
</task>

</tasks>

<verification>
- Backend builds successfully
- Frontend TypeScript compiles
- SearchDrugCatalog returns results for empty search
- DrugInventoryTable shows Out of Stock for zero-stock items
- OTC form grid is aligned
- Low stock alerts include zero-stock medications
</verification>

<success_criteria>
OTC retail is functional (TASK-16/23), stock statuses are accurate (TASK-21), zero-stock alerts work (TASK-25), OTC UI is properly aligned (TASK-18).
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-03-SUMMARY.md`
</output>
