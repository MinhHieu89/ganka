---
phase: 10.1-fix-all-issues-in-issues-md
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/audit/components/AuditLogTable.tsx
  - frontend/src/features/audit/components/AuditLogDetail.tsx
  - frontend/src/shared/components/SiteHeader.tsx
  - frontend/src/app/routes/_authenticated/dashboard.tsx
  - backend/src/Modules/Patient/Patient.Application/Features/GetDashboardStats.cs
  - backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs
  - frontend/src/features/dashboard/api/dashboard-api.ts
autonomous: true
requirements: [TASK-11, TASK-26, TASK-17]

must_haves:
  truths:
    - "Activity Log hides ID/GUID columns from the table display"
    - "Clicking Examination title/breadcrumb navigates to /clinical workflow dashboard"
    - "Dashboard statistics cards show real data (patient count, today's appointments)"
  artifacts:
    - path: "frontend/src/features/audit/components/AuditLogTable.tsx"
      provides: "Hidden ID columns"
    - path: "frontend/src/shared/components/SiteHeader.tsx"
      provides: "Breadcrumb path override for /visits -> /clinical"
    - path: "frontend/src/app/routes/_authenticated/dashboard.tsx"
      provides: "Dashboard with real API-backed stats"
  key_links:
    - from: "dashboard.tsx"
      to: "dashboard-api.ts"
      via: "API calls for stats"
      pattern: "getDashboardStats|useQuery"
---

<objective>
Fix activity log ID visibility (TASK-11), examination navigation (TASK-26), and dashboard statistics (TASK-17).

Purpose: TASK-11 improves admin UX by hiding technical IDs. TASK-26 (HIGH PRIORITY) fixes broken navigation. TASK-17 makes the dashboard useful with real data.
Output: Clean audit log, correct examination navigation, data-driven dashboard.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide audit log IDs and fix examination breadcrumb navigation (TASK-11, TASK-26)</name>
  <files>frontend/src/features/audit/components/AuditLogTable.tsx, frontend/src/features/audit/components/AuditLogDetail.tsx, frontend/src/shared/components/SiteHeader.tsx</files>
  <read_first>frontend/src/features/audit/components/AuditLogTable.tsx, frontend/src/features/audit/components/AuditLogDetail.tsx, frontend/src/shared/components/SiteHeader.tsx</read_first>
  <action>
TASK-11 (Hide ID fields in Activity Log):
In AuditLogTable.tsx, find the column definitions array. Identify any columns that display entity IDs (GUIDs) -- these are typically columns named "Id", "EntityId", "UserId", or similar containing UUID patterns. Either:
- Remove these columns entirely from the columns array, OR
- Set them as hidden by default using the column visibility feature (if using TanStack Table, set `enableHiding: true` and default visibility to false)

In AuditLogDetail.tsx, find where entity details are rendered. Filter out any fields where the key ends with "Id" or the value matches a GUID pattern (`/^[0-9a-f]{8}-/`) from the detail display. Keep other meaningful fields.

TASK-26 (Examination breadcrumb navigation):
The issue is in `frontend/src/shared/components/SiteHeader.tsx`. When navigating to `/visits/$visitId`, the breadcrumb builds a clickable link to `/visits` (line 86: `path = "/" + segments.slice(0, index + 1).join("/")`). However `/visits` has no index route -- only `/visits/$visitId` exists.

Fix in SiteHeader.tsx:
1. Find the `noStandaloneRoute` array on line 88: `const noStandaloneRoute = ["approvals", "templates"]`
2. Add a path redirect map above it (similar pattern): create a `segmentPathOverride` map that redirects certain segments to different paths:
   ```
   const segmentPathOverride: Record<string, string> = {
     visits: "/clinical",
   }
   ```
3. After computing `path` on line 86, check if the current segment has an override:
   ```
   if (segmentPathOverride[segment]) {
     path = segmentPathOverride[segment]
   }
   ```
This ensures clicking the "Examination" breadcrumb when on `/visits/$visitId` navigates to `/clinical` (the workflow dashboard) instead of the non-existent `/visits` index.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AuditLogTable.tsx does not render columns containing entity IDs/GUIDs in visible state
    - AuditLogDetail.tsx filters or hides fields matching GUID patterns or ending with "Id"
    - SiteHeader.tsx contains a path override mapping "visits" to "/clinical"
    - No breadcrumb link points to bare `/visits` -- Examination links point to `/clinical`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Audit log hides technical ID columns; Examination breadcrumb navigates to /clinical dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Implement dashboard statistics with real API data (TASK-17)</name>
  <files>frontend/src/app/routes/_authenticated/dashboard.tsx, frontend/src/features/dashboard/api/dashboard-api.ts, backend/src/Modules/Patient/Patient.Application/Features/GetDashboardStats.cs, backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs</files>
  <read_first>frontend/src/app/routes/_authenticated/dashboard.tsx, backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs</read_first>
  <action>
TASK-17: Dashboard statistics not showing (currently hardcoded placeholders).

Follow TDD per CLAUDE.md: First write a failing test for the backend handler, then implement.

Backend -- create a dashboard stats endpoint:
1. First, write a failing test for `GetDashboardStatsHandler` in the Patient test project. Test that it returns patient count and today's appointment count.
2. Create `backend/src/Modules/Patient/Patient.Application/Features/GetDashboardStats.cs` with a Wolverine handler that queries:
   - Total active patient count: `SELECT COUNT(*) FROM Patients WHERE IsActive = true`
   - Today's appointment count: `SELECT COUNT(*) FROM Appointments WHERE Date = today`
3. Or add counts to existing repositories if simpler.
4. Register the endpoint in `PatientApiEndpoints.cs`: `GET /api/dashboard/stats` returning `{ totalPatients: number, todayAppointments: number, activeVisits: number, activeTreatments: number }`
5. For activeVisits and activeTreatments, query the Clinical and Treatment modules respectively. If cross-module querying is complex, start with just totalPatients and todayAppointments, and return 0 for the others with a TODO comment.
6. Verify the test passes (green).

Frontend:
7. Create `frontend/src/features/dashboard/api/dashboard-api.ts` (or add to existing) with:
   - `getDashboardStats()` function calling `GET /api/dashboard/stats`
   - TanStack Query hook `useDashboardStats()` wrapping the API call
8. In dashboard.tsx, replace the hardcoded em-dash and "Coming soon" placeholders:
   - Wire each stat card to the real data from `useDashboardStats()`
   - Show loading skeleton while fetching
   - Show actual numbers when loaded
   - Cards: "Total Patients" = totalPatients, "Today's Appointments" = todayAppointments, "Active Visits" = activeVisits, "Active Treatments" = activeTreatments
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Bootstrapper/Bootstrapper.csproj 2>&1 | tail -5 && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - A dashboard stats endpoint exists (GET /api/dashboard/stats or equivalent)
    - dashboard.tsx does NOT contain hardcoded "&mdash;" or "Coming soon" for stat values
    - dashboard.tsx uses a query hook (useDashboardStats or equivalent) to fetch real data
    - dashboard-api.ts (or equivalent) contains getDashboardStats function
    - Backend builds successfully, frontend TypeScript compiles
  </acceptance_criteria>
  <done>Dashboard shows real statistics for patients, appointments, visits, and treatments</done>
</task>

</tasks>

<verification>
- Backend builds, frontend TypeScript compiles
- Audit log table hides ID columns
- Examination breadcrumb navigates to /clinical
- Dashboard cards show real fetched data
</verification>

<success_criteria>
TASK-11: Activity log hides IDs. TASK-26: Examination navigates to /clinical. TASK-17: Dashboard shows real statistics.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-07-SUMMARY.md`
</output>
