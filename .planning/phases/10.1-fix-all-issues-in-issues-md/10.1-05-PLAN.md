---
phase: 10.1-fix-all-issues-in-issues-md
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/shared/stores/authStore.ts
  - frontend/src/features/auth/hooks/useAuth.ts
  - frontend/src/features/auth/api/auth-api.ts
  - frontend/src/app/routes/__root.tsx
  - frontend/public/locales/vi/billing.json
  - frontend/public/locales/en/billing.json
  - frontend/public/locales/vi/optical.json
  - frontend/public/locales/en/optical.json
  - frontend/public/locales/vi/treatment.json
  - frontend/public/locales/en/treatment.json
autonomous: true
requirements: [TASK-14, TASK-4, TASK-28]

must_haves:
  truths:
    - "User stays logged in after page refresh when Remember Login was checked"
    - "Cashier screens display in Vietnamese when Vietnamese language is selected"
    - "Optical Center screens display in Vietnamese when Vietnamese language is selected"
    - "Treatment protocol create/edit screens display in Vietnamese"
  artifacts:
    - path: "frontend/src/shared/stores/authStore.ts"
      provides: "Silent refresh on app bootstrap"
    - path: "frontend/public/locales/vi/billing.json"
      provides: "Vietnamese translations for Cashier"
    - path: "frontend/public/locales/vi/optical.json"
      provides: "Vietnamese translations for Optical"
    - path: "frontend/public/locales/vi/treatment.json"
      provides: "Vietnamese translations for Treatment templates"
  key_links:
    - from: "authStore.ts"
      to: "auth-api.ts"
      via: "silentRefresh() call on bootstrap"
      pattern: "silentRefresh|refreshToken"
---

<objective>
Fix Remember Login persistence (TASK-14) and add missing Vietnamese translations for Cashier/Optical (TASK-4) and Treatment templates (TASK-28).

Purpose: TASK-14 is a core usability issue (users must re-login every page refresh). TASK-4/28 are i18n gaps in recently added modules.
Output: Persistent auth sessions and complete Vietnamese translations for Cashier, Optical, and Treatment screens.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Remember Login with silent refresh on bootstrap (TASK-14)</name>
  <files>frontend/src/shared/stores/authStore.ts, frontend/src/features/auth/hooks/useAuth.ts, frontend/src/features/auth/api/auth-api.ts, frontend/src/app/routes/__root.tsx</files>
  <read_first>frontend/src/shared/stores/authStore.ts, frontend/src/features/auth/hooks/useAuth.ts, frontend/src/features/auth/api/auth-api.ts, frontend/src/app/routes/__root.tsx</read_first>
  <action>
TASK-14: Remember Login not working.

Root cause: authStore.ts uses plain zustand with NO persistence. On page reload, all auth state is lost. The backend properly stores refresh tokens with RememberMe flag in HTTP-only cookies, and `silentRefresh()` already exists in auth-api.ts (line 75).

Fix approach (DO NOT persist auth store to localStorage -- that would expose tokens):
1. In auth-api.ts, verify `silentRefresh()` function exists and calls `POST /api/auth/refresh` which uses the HTTP-only refresh token cookie.

2. In authStore.ts, add an `initialize()` action that:
   - Sets a `isInitializing: boolean` state to `true`
   - Calls `silentRefresh()` from auth-api.ts
   - If successful: updates `user`, `accessToken`, and sets `isAuthenticated = true`
   - If fails (401/expired): clears auth state (user was not remembered or token expired)
   - Sets `isInitializing = false` when done

3. In the app bootstrap (likely `__root.tsx` or the main App component), call `authStore.getState().initialize()` on mount (useEffect with empty deps, or in the route's beforeLoad). While `isInitializing` is true, show a loading spinner instead of redirecting to login.

4. Ensure the login form's "Remember Me" checkbox value is passed to the login API call (it should already be -- verify the `rememberMe` parameter flows through to the backend `POST /api/auth/login` request body).

IMPORTANT: Do NOT add zustand `persist` middleware to authStore. The correct pattern is silent refresh via HTTP-only cookie, not localStorage token storage.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - authStore.ts contains an `initialize` function/action
    - authStore.ts contains `isInitializing` boolean state
    - authStore.ts calls `silentRefresh` or equivalent refresh function
    - authStore.ts does NOT use zustand `persist` middleware
    - App bootstrap (__root.tsx or equivalent) calls initialize on mount
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Page refresh attempts silent token refresh; if successful, user stays logged in; if failed, redirects to login</done>
</task>

<task type="auto">
  <name>Task 2: Add Vietnamese translations for Cashier, Optical, Treatment (TASK-4, TASK-28)</name>
  <files>frontend/public/locales/vi/billing.json, frontend/public/locales/vi/optical.json, frontend/public/locales/vi/treatment.json, frontend/public/locales/en/billing.json, frontend/public/locales/en/optical.json, frontend/public/locales/en/treatment.json</files>
  <read_first>frontend/public/locales/vi/billing.json, frontend/public/locales/vi/optical.json, frontend/public/locales/vi/treatment.json, frontend/public/locales/en/billing.json, frontend/public/locales/en/optical.json, frontend/public/locales/en/treatment.json</read_first>
  <action>
TASK-4 (Cashier/Optical Vietnamese) and TASK-28 (Treatment Vietnamese):

1. Read the English locale files (en/billing.json, en/optical.json, en/treatment.json) to identify ALL keys.
2. Read the Vietnamese locale files (vi/billing.json, vi/optical.json, vi/treatment.json) to identify which keys are MISSING Vietnamese translations.
3. For each missing key in the Vietnamese files, add the Vietnamese translation with proper diacritics.

For billing.json (Cashier module): Translate all invoice, payment, shift management, receipt-related keys. Common translations:
- Invoice = Hoa don
- Payment = Thanh toan
- Shift = Ca lam viec
- Receipt = Phieu thu
- Total = Tong cong
- Discount = Giam gia
- Cash = Tien mat
- Bank Transfer = Chuyen khoan

For optical.json (Optical Center): Translate frame inventory, lens ordering, glasses order tracking, warranty keys. Common translations:
- Frame = Gong kinh
- Lens = Trong kinh
- Order = Don hang
- Warranty = Bao hanh
- Stocktaking = Kiem ke

For treatment.json (Treatment templates): Translate protocol creation/editing, session tracking, IPL/LLLT/Lid care keys. Common translations:
- Treatment = Dieu tri
- Protocol = Phac do
- Session = Buoi dieu tri
- IPL = IPL (keep as-is)
- LLLT = LLLT (keep as-is)

IMPORTANT: Use proper Vietnamese diacritics (matching existing patterns in the vi/*.json files). Do NOT leave any key untranslated. If a key exists in English but not Vietnamese, add it.

Also scan the relevant frontend component files to find any hardcoded English strings that should use i18n:
- `frontend/src/features/billing/components/` -- look for hardcoded English text
- `frontend/src/features/optical/components/` -- look for hardcoded English text
- `frontend/src/features/treatment/components/` -- look for hardcoded English text

Fix scope is LIMITED to the screens reported in TASK-4 (Cashier, Optical) and TASK-28 (Treatment templates) -- no full i18n audit.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && node -e "const en=require('./public/locales/en/billing.json'); const vi=require('./public/locales/vi/billing.json'); const missing=Object.keys(en).filter(k=>!vi[k]); console.log('Missing billing keys:', missing.length); if(missing.length>0) console.log(missing.slice(0,5))" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - vi/billing.json has Vietnamese translations for ALL keys present in en/billing.json
    - vi/optical.json has Vietnamese translations for ALL keys present in en/optical.json
    - vi/treatment.json has Vietnamese translations for ALL keys present in en/treatment.json
    - Vietnamese translations contain proper diacritics (check for characters like a, o, u with marks)
    - No empty string values in Vietnamese locale files
  </acceptance_criteria>
  <done>Cashier, Optical Center, and Treatment template screens fully translated to Vietnamese</done>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles
- Remember Login silent refresh is called on bootstrap
- All billing/optical/treatment English keys have Vietnamese counterparts
- No hardcoded English strings on Cashier, Optical, or Treatment screens
</verification>

<success_criteria>
TASK-14: Page refresh restores login session via silent refresh. TASK-4: Cashier and Optical screens in Vietnamese. TASK-28: Treatment template screens in Vietnamese.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-05-SUMMARY.md`
</output>
