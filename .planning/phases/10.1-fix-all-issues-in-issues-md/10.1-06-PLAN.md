---
phase: 10.1-fix-all-issues-in-issues-md
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/scheduling/components/AppointmentCalendar.tsx
  - frontend/src/features/scheduling/components/PendingBookingsPanel.tsx
  - frontend/src/features/scheduling/components/AppointmentDetailDialog.tsx
  - frontend/public/locales/vi/scheduling.json
  - frontend/public/locales/en/scheduling.json
autonomous: true
requirements: [TASK-12, TASK-13]

must_haves:
  truths:
    - "Calendar event cards show full patient info without text being cut off"
    - "Pending bookings show specific time slot, not just 'morning'"
    - "Approve popup defaults to customer's registered time, not 14:00"
    - "Appointment type displays in Vietnamese when Vietnamese language is selected"
  artifacts:
    - path: "frontend/src/features/scheduling/components/AppointmentCalendar.tsx"
      provides: "Improved calendar event display with full info"
    - path: "frontend/src/features/scheduling/components/PendingBookingsPanel.tsx"
      provides: "Time slot display alongside period label"
    - path: "frontend/src/features/scheduling/components/AppointmentDetailDialog.tsx"
      provides: "Default time from customer registration"
  key_links:
    - from: "PendingBookingsPanel.tsx"
      to: "AppointmentDetailDialog.tsx"
      via: "Selected booking data passed to approve dialog"
      pattern: "selectedBooking|onApprove"
---

<objective>
Fix appointment calendar info display (TASK-12) and time/type display improvements (TASK-13).

Purpose: Calendar info is cut off making it hard to identify patients. Time display shows generic "morning" instead of specific slots. Approve popup uses wrong default time.
Output: Readable calendar events, specific time displays, correct default times, Vietnamese appointment types.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix calendar event display (TASK-12)</name>
  <files>frontend/src/features/scheduling/components/AppointmentCalendar.tsx</files>
  <read_first>frontend/src/features/scheduling/components/AppointmentCalendar.tsx</read_first>
  <action>
TASK-12: Calendar event cards cut off patient info.

In AppointmentCalendar.tsx, find the event rendering configuration (FullCalendar eventContent or event render hook). The event card likely has:
- Fixed height or `overflow: hidden` that clips text
- Missing tooltip for full info on hover

Fix:
1. Remove any fixed `max-height` or `overflow: hidden` on the event card content that clips text
2. Add a `title` attribute or tooltip (using shadcn Tooltip component) on the event card that shows full patient name, appointment type, and time when hovered
3. Use CSS `text-overflow: ellipsis` with `overflow: hidden` and `white-space: nowrap` ONLY on the text, not the container -- so text truncates gracefully with "..." instead of being hard-clipped
4. If the calendar uses FullCalendar's event height settings, increase `eventMinHeight` or adjust `slotMinTime`/`slotMaxTime` for more vertical space
5. Ensure the event card shows at minimum: patient name (truncated with tooltip) and time
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AppointmentCalendar.tsx has tooltip or title attribute on event cards showing full patient info
    - Event card text has `text-overflow: ellipsis` or `truncate` Tailwind class for graceful truncation
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Calendar events show readable patient info with tooltip for full details</done>
</task>

<task type="auto">
  <name>Task 2: Fix appointment time display and type translation (TASK-13)</name>
  <files>frontend/src/features/scheduling/components/PendingBookingsPanel.tsx, frontend/src/features/scheduling/components/AppointmentDetailDialog.tsx, frontend/public/locales/vi/scheduling.json, frontend/public/locales/en/scheduling.json</files>
  <read_first>frontend/src/features/scheduling/components/PendingBookingsPanel.tsx, frontend/src/features/scheduling/components/AppointmentDetailDialog.tsx, frontend/public/locales/vi/scheduling.json, frontend/public/locales/en/scheduling.json</read_first>
  <action>
TASK-13: Three sub-issues in appointment time/type display.

Issue 1 (Time shown as "morning" instead of specific slot):
In PendingBookingsPanel.tsx, find where appointment time is displayed. It likely shows just the period label ("morning", "afternoon"). Change to show the time range alongside: e.g., "Sang (8:00 - 12:00)" or "Chieu (13:00 - 17:00)". If the customer selected a specific time slot, display that slot time. If only a period was selected, show the period with its time range.

Issue 2 (Approve popup defaults to 14:00):
In AppointmentDetailDialog.tsx (or wherever the approve popup form is), find the time field default value. It currently defaults to "14:00". Change the default to use the customer's originally registered time. The booking data passed to the dialog should contain the customer's preferred time -- use that as the initial value for the time picker. If the customer selected "morning", default to "08:00" (start of morning). If "afternoon", default to "13:00" (start of afternoon).

Issue 3 (Appointment type in English):
Find where appointment type is displayed (e.g., "New Patient", "Follow-up", "Treatment", "Ortho-K"). These should use i18n keys. Add/verify keys in scheduling.json:
- en: `"appointmentType.newPatient": "New Patient"`, `"appointmentType.followUp": "Follow-up"`, `"appointmentType.treatment": "Treatment"`, `"appointmentType.orthoK": "Ortho-K"`
- vi: `"appointmentType.newPatient": "Benh nhan moi"`, `"appointmentType.followUp": "Tai kham"`, `"appointmentType.treatment": "Dieu tri"`, `"appointmentType.orthoK": "Ortho-K"` (with proper diacritics)

Replace any hardcoded English appointment type strings with `t("appointmentType.{type}")` calls.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PendingBookingsPanel.tsx displays time range alongside period label (contains "8:00" or time formatting logic)
    - AppointmentDetailDialog.tsx time field default is NOT hardcoded "14:00" -- it uses the customer's registered time
    - scheduling.json (vi) contains Vietnamese appointment type translations
    - Appointment type display uses `t(` i18n calls, not hardcoded English strings
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Pending bookings show specific times, approve popup defaults to customer's time, appointment types display in Vietnamese</done>
</task>

</tasks>

<verification>
- Frontend TypeScript compiles
- Calendar events have tooltips and graceful text truncation
- Pending bookings show time ranges
- Approve dialog defaults to customer's time
- Appointment types translated to Vietnamese
</verification>

<success_criteria>
TASK-12: Calendar events readable with full info on hover. TASK-13: Specific time slots shown, approve defaults to customer time, appointment types in Vietnamese.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-06-SUMMARY.md`
</output>
