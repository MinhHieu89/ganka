---
phase: 10.1-fix-all-issues-in-issues-md
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/pharmacy/components/StockImportForm.tsx
  - frontend/src/features/billing/components/ShiftReportView.tsx
  - frontend/src/features/pharmacy/components/DispensingDialog.tsx
  - frontend/src/features/pharmacy/components/PatientPrescriptionsTab.tsx
  - frontend/src/features/billing/components/ServiceCatalogFormDialog.tsx
  - frontend/src/features/billing/api/service-catalog-api.ts
autonomous: false
requirements: [TASK-15, TASK-19, TASK-29, TASK-30]

must_haves:
  truths:
    - "Medication inventory import works when medications are linked to supplier"
    - "Cashier report timestamps match shift open/close times in Vietnam timezone"
    - "Medication dispensing confirmation from Patient Detail works"
    - "New services can be added from Service Catalog"
  artifacts:
    - path: "frontend/src/features/billing/components/ShiftReportView.tsx"
      provides: "Correct timezone-aware timestamp display"
    - path: "frontend/src/features/billing/components/ServiceCatalogFormDialog.tsx"
      provides: "Working service creation"
  key_links:
    - from: "ServiceCatalogFormDialog.tsx"
      to: "service-catalog-api.ts"
      via: "createServiceCatalogItem API call"
      pattern: "createServiceCatalogItem"
---

<objective>
Fix investigation-first issues: medication import (TASK-15), cashier report timestamps (TASK-19), dispensing confirmation (TASK-29), and service catalog creation (TASK-30).

Purpose: These four issues have lower confidence root causes and require investigation before fixing. Grouped together as they are all single-module fixes.
Output: Working medication import, correct timestamps, functional dispensing, and service creation.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix medication import and cashier report timestamps (TASK-15, TASK-19)</name>
  <files>frontend/src/features/pharmacy/components/StockImportForm.tsx, frontend/src/features/pharmacy/components/ExcelImportDialog.tsx, frontend/src/features/billing/components/ShiftReportView.tsx</files>
  <read_first>frontend/src/features/pharmacy/components/StockImportForm.tsx, frontend/src/features/pharmacy/components/ExcelImportDialog.tsx, frontend/src/features/billing/components/ShiftReportView.tsx, frontend/src/features/pharmacy/api/pharmacy-api.ts, frontend/src/features/billing/api/billing-api.ts</read_first>
  <action>
TASK-15 (Cannot import medication inventory):
Investigation-first approach:
1. Read StockImportForm.tsx and ExcelImportDialog.tsx to understand the import flow
2. Check if the import calls the correct backend endpoint
3. Check if Phase 10 plan 10-02 (Excel import with preview) was completed -- read `.planning/phases/10-address-all-pending-todos/` for relevant SUMMARY files
4. Start the backend and test the import endpoint directly with curl to see if it's a backend or frontend issue
5. Common issues to check:
   - Is the form submission sending data in the correct format?
   - Is the supplier ID being passed correctly?
   - Is there a validation error being swallowed?
6. Apply the fix based on diagnosis. If this was already addressed in Phase 10, verify and close.

TASK-19 (Cashier report timestamps mismatch):
In ShiftReportView.tsx, find where shift open/close timestamps are displayed. The issue is likely timezone-related:
1. Check if timestamps from the backend are in UTC
2. Check if the frontend applies timezone conversion
3. The backend stores times in UTC. The frontend uses `format()` from date-fns which formats in local browser timezone.
4. Fix: Ensure all timestamp displays use Vietnam timezone (UTC+7). Options:
   - Use `date-fns-tz` `formatInTimeZone(date, 'Asia/Ho_Chi_Minh', formatString)` for display
   - Or ensure the backend returns times already converted to Vietnam timezone
   - Check how other parts of the app handle timezone (appointment times, etc.) and follow the same pattern
5. If the app already has a timezone utility, use it. If not, add a helper in shared/lib that converts UTC to Vietnam time.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Bootstrapper/Bootstrapper.csproj 2>&1 | tail -5 && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - StockImportForm.tsx or related file has a working import flow (no swallowed errors, correct endpoint call)
    - ShiftReportView.tsx displays timestamps with timezone conversion (contains timezone utility usage or formatInTimeZone)
    - Backend builds, frontend TypeScript compiles
  </acceptance_criteria>
  <done>Medication import works; cashier report timestamps match shift times in Vietnam timezone</done>
</task>

<task type="auto">
  <name>Task 2: Fix dispensing confirmation and service catalog creation (TASK-29, TASK-30)</name>
  <files>frontend/src/features/pharmacy/components/DispensingDialog.tsx, frontend/src/features/pharmacy/components/PatientPrescriptionsTab.tsx, frontend/src/features/billing/components/ServiceCatalogFormDialog.tsx, frontend/src/features/billing/api/service-catalog-api.ts</files>
  <read_first>frontend/src/features/pharmacy/components/DispensingDialog.tsx, frontend/src/features/pharmacy/components/PatientPrescriptionsTab.tsx, frontend/src/features/billing/components/ServiceCatalogFormDialog.tsx, frontend/src/features/billing/api/service-catalog-api.ts</read_first>
  <action>
TASK-29 (Medication dispensing confirmation not working):
Investigation-first approach:
1. Read DispensingDialog.tsx and PatientPrescriptionsTab.tsx to understand the dispensing flow from Patient Detail
2. Start the backend and check backend logs for errors when dispensing is attempted
3. Check if:
   - The dispensing dialog opens correctly from Patient Detail
   - The confirmation button triggers the correct API call
   - The API call returns success or error
   - The prescriptions are correctly loaded on the Patient Detail tab
4. Common issues:
   - Missing or incorrect prescription ID being passed
   - Stock deduction failing due to insufficient stock
   - Dialog state management issue (dialog closes without confirming)
   - Endpoint requires different payload format
5. Apply fix based on diagnosis

TASK-30 (Cannot add new services):
Investigation-first approach:
1. Read ServiceCatalogFormDialog.tsx and service-catalog-api.ts
2. Check the backend endpoint: does `POST /api/billing/service-catalog` exist and accept the correct payload?
3. Start the backend and test with curl:
   `curl -X POST http://localhost:5255/api/billing/service-catalog -H "Content-Type: application/json" -H "Authorization: Bearer {token}" -d '{"name": "Test Service", "price": 100000}'`
4. Check backend logs for validation errors or 500 errors
5. Common issues:
   - Missing required fields in the request body
   - Backend validation rejecting the payload
   - Missing or incorrect endpoint registration
   - Permission issue (requires specific role)
6. Apply fix based on diagnosis. If it's a backend validation issue, fix the validator. If it's a frontend form submission issue, fix the API call.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Bootstrapper/Bootstrapper.csproj 2>&1 | tail -5 && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - DispensingDialog.tsx has working confirmation flow (correct API call, proper error handling)
    - ServiceCatalogFormDialog.tsx sends correct payload to the service catalog creation endpoint
    - Backend builds, frontend TypeScript compiles
  </acceptance_criteria>
  <done>Medication dispensing confirmation works from Patient Detail; new services can be added from Service Catalog</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify investigation-first fixes</name>
  <what-built>Fixed four investigation-first issues: medication import, cashier timestamps, dispensing confirmation, and service catalog creation.</what-built>
  <how-to-verify>
    1. Start backend (port 5255) and frontend (port 3000)
    2. Log in with Admin@ganka28.com / Admin@123456
    3. TASK-15: Go to Pharmacy > Inventory, try importing medications linked to a supplier
    4. TASK-19: Go to Cashier > Reports, verify timestamps match shift open/close times
    5. TASK-29: Go to a Patient Detail with a prescription, try confirming medication dispensing
    6. TASK-30: Go to Billing > Service Catalog, try adding a new service
  </how-to-verify>
  <resume-signal>Type "approved" if all four features work, or describe which issues remain</resume-signal>
</task>

</tasks>

<verification>
- Backend builds, frontend TypeScript compiles
- All four issues diagnosed and fixed
- Human verification confirms functionality
</verification>

<success_criteria>
TASK-15: Medication import works. TASK-19: Correct timestamps. TASK-29: Dispensing confirmation works. TASK-30: Service creation works. All human-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-08-SUMMARY.md`
</output>
