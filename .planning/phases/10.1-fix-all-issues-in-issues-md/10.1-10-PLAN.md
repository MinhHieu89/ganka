---
phase: 10.1-fix-all-issues-in-issues-md
plan: 10
type: execute
wave: 1
depends_on: ["10.1-08"]
files_modified:
  - frontend/src/features/pharmacy/components/DispensingDialog.tsx
  - frontend/src/features/pharmacy/components/PharmacyQueueTable.tsx
  - frontend/src/features/pharmacy/api/pharmacy-api.ts
  - frontend/src/features/pharmacy/api/pharmacy-queries.ts
autonomous: false
gap_closure: true
requirements: [TASK-29]

must_haves:
  truths:
    - "Pharmacist can open dispensing dialog from pharmacy queue for a pending prescription"
    - "Pharmacist can confirm dispensing and the prescription is marked as dispensed"
    - "After dispensing, the pharmacy queue updates to remove the dispensed prescription"
    - "Error messages are shown clearly if dispensing fails"
  artifacts:
    - path: "frontend/src/features/pharmacy/components/DispensingDialog.tsx"
      provides: "Working dispensing confirmation dialog"
    - path: "frontend/src/features/pharmacy/components/PharmacyQueueTable.tsx"
      provides: "Queue table that launches dispensing and refreshes after completion"
  key_links:
    - from: "DispensingDialog.tsx"
      to: "pharmacy-queries.ts"
      via: "useDispenseDrugs mutation"
      pattern: "dispenseMutation\\.mutateAsync"
    - from: "PharmacyQueueTable.tsx"
      to: "DispensingDialog.tsx"
      via: "prescription state passed to dialog"
      pattern: "DispensingDialog.*prescription"
---

<objective>
Investigate and fix the medication dispensing confirmation flow (TASK-29). The verification found that the only change in phase 10.1 was adding console.error logging -- the actual root cause of "dispensing not working" was never identified or fixed.

Purpose: Close TASK-29 gap -- ensure end-to-end dispensing confirmation works from the pharmacy queue.
Output: Working dispensing flow with human verification.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-08-SUMMARY.md

<interfaces>
<!-- Dispensing flow: PharmacyQueueTable -> DispensingDialog -> useDispenseDrugs mutation -> POST /api/pharmacy/dispensing -->

From pharmacy-queries.ts:
```typescript
export function useDispenseDrugs() {
  // calls dispenseDrugs() from pharmacy-api.ts
  // POST to /api/pharmacy/dispensing
}
```

From pharmacy-api.ts:
```typescript
export async function dispenseDrugs(input: DispenseDrugsInput): Promise<void>
// POST /api/pharmacy/dispensing with prescription details
```

From DispensingDialog.tsx handleDispense:
```typescript
const input = {
  prescriptionId: prescription.prescriptionId,
  visitId: prescription.visitId,
  patientId: prescription.patientId,
  patientName: prescription.patientName,
  prescribedAt: prescription.prescribedAt,
  overrideReason: prescription.isExpired ? overrideReason.trim() : null,
  lines: lineStates.map(l => ({ ... })),
}
await dispenseMutation.mutateAsync(input)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix dispensing confirmation flow</name>
  <files>
    frontend/src/features/pharmacy/components/DispensingDialog.tsx
    frontend/src/features/pharmacy/components/PharmacyQueueTable.tsx
    frontend/src/features/pharmacy/api/pharmacy-api.ts
    frontend/src/features/pharmacy/api/pharmacy-queries.ts
  </files>
  <read_first>
    frontend/src/features/pharmacy/components/DispensingDialog.tsx
    frontend/src/features/pharmacy/components/PharmacyQueueTable.tsx
    frontend/src/features/pharmacy/api/pharmacy-api.ts
    frontend/src/features/pharmacy/api/pharmacy-queries.ts
    backend/src/Modules/Pharmacy/Pharmacy.Presentation/PharmacyApiEndpoints.cs
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/DispenseDrugs.cs
    backend/src/Modules/Pharmacy/Pharmacy.Contracts/Dtos/DispenseDrugsCommand.cs
  </read_first>
  <action>
**Investigation steps (do each one, document findings):**

1. **Check the API endpoint exists and is correct:**
   - Read `PharmacyApiEndpoints.cs` and find the dispensing POST endpoint path
   - Compare with what `pharmacy-api.ts` calls (the URL path)
   - Check if the HTTP method matches (POST vs PUT etc.)

2. **Check payload shape alignment:**
   - Read `DispenseDrugsCommand.cs` (the backend command record) and note all property names and types
   - Compare with the `input` object constructed in `DispensingDialog.tsx` `handleDispense()`
   - Look for mismatches in property names (camelCase vs PascalCase), missing required fields, or type differences
   - Common issues: `prescriptionId` vs `PrescriptionId`, `lines` vs `Items`, `skip` vs `Skip`

3. **Check the mutation hook:**
   - Read `useDispenseDrugs` in `pharmacy-queries.ts` to verify it calls `dispenseDrugs()` from pharmacy-api
   - Check if the mutation has `onSuccess` that invalidates the pharmacy queue query cache (so the table refreshes)
   - Check if `onError` shows a toast with the actual error message from the backend

4. **Check PharmacyQueueTable opens DispensingDialog correctly:**
   - Verify `prescription` state is set before dialog opens
   - Verify `open` state toggles correctly
   - Verify prescription items (`items` array) are populated in the passed prescription object

5. **Start backend and test manually if code looks correct:**
   - Run backend on port 5255 and frontend on port 3000
   - Navigate to Pharmacy > Prescriptions Queue
   - Try to dispense a prescription
   - Check browser DevTools Network tab for the request/response
   - Check backend logs at `backend/src/Bootstrapper/logs/ganka-*.log`

**Fix based on findings:**

Apply the minimal fix to address the identified root cause. Common fixes include:
- Aligning payload property names between frontend and backend
- Adding missing required fields to the frontend payload
- Fixing the API endpoint URL
- Adding proper error message display from backend validation errors
- Ensuring query cache invalidation after successful dispense so the queue refreshes

If the dispensing flow works correctly after investigation (i.e., the original issue was already fixed by prior plan 08 changes or was environment-specific), document this finding and ensure:
- Error handling shows actual backend error messages (not just generic "Failed" toast)
- The `console.error` from plan 08 is kept for debugging
- The mutation's `onSuccess` invalidates relevant query caches
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --pretty 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - DispensingDialog.tsx `handleDispense` payload property names match backend `DispenseDrugsCommand` exactly
    - `useDispenseDrugs` mutation has `onSuccess` callback that invalidates pharmacy queue queries
    - `useDispenseDrugs` mutation has `onError` callback that shows specific error message from backend
    - No TypeScript errors in modified pharmacy files
  </acceptance_criteria>
  <done>Dispensing confirmation flow investigated, root cause identified and fixed (or confirmed working), error handling improved</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dispensing confirmation end-to-end</name>
  <what-built>Dispensing confirmation flow from pharmacy queue -- investigated and fixed any payload/API/cache issues</what-built>
  <how-to-verify>
    1. Start backend: `cd backend/src/Bootstrapper && dotnet run` (port 5255)
    2. Start frontend: `cd frontend && npm run dev` (port 3000)
    3. Log in with Admin@ganka28.com / Admin@123456
    4. Navigate to Pharmacy > Prescriptions Queue
    5. If no pending prescriptions exist: go to Clinical, create a visit for a patient, add a drug prescription, sign off the visit
    6. Back in Pharmacy Queue, find the pending prescription
    7. Click the dispense action button
    8. In the DispensingDialog: verify items are shown, FEFO batch suggestions appear for catalog drugs
    9. Click "Confirm Dispense"
    10. Expected: Toast shows success, dialog closes, prescription disappears from queue
    11. If error: check browser DevTools console and Network tab, check backend logs at `backend/src/Bootstrapper/logs/ganka-*.log`
  </how-to-verify>
  <resume-signal>Type "approved" if dispensing works end-to-end, or describe the error you see</resume-signal>
</task>

</tasks>

<verification>
1. Frontend compiles: `cd frontend && npx tsc --noEmit`
2. Human confirms dispensing works end-to-end from pharmacy queue
</verification>

<success_criteria>
- Dispensing confirmation completes successfully from pharmacy queue
- After dispensing, the prescription is removed from the queue (cache invalidated)
- Error messages from backend are displayed to the user (not just console.error)
- Human has verified the full flow works
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-10-SUMMARY.md`
</output>
