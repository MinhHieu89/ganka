---
phase: 10.1-fix-all-issues-in-issues-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/patient/components/PatientRegistrationForm.tsx
  - frontend/src/features/patient/components/PatientProfileHeader.tsx
  - frontend/src/features/patient/components/PatientProfilePage.tsx
  - frontend/public/locales/vi/patient.json
  - frontend/public/locales/en/patient.json
autonomous: true
requirements: [TASK-2, TASK-3, TASK-9]

must_haves:
  truths:
    - "Mouse scroll wheel works in allergy dropdown on Patient Registration"
    - "Edit button is hidden when patient profile is in edit mode"
    - "Full Name field validates min 3 and max 50 characters with Vietnamese error messages"
    - "DOB and Gender required errors display in Vietnamese"
    - "Duplicate phone number shows field-level error 'Phone number is already in use' below Phone Number field"
  artifacts:
    - path: "frontend/src/features/patient/components/PatientRegistrationForm.tsx"
      provides: "Allergy dropdown scroll fix, fullName validation, duplicate phone field error"
    - path: "frontend/src/features/patient/components/PatientProfileHeader.tsx"
      provides: "Conditional Edit button visibility"
    - path: "frontend/public/locales/vi/patient.json"
      provides: "Vietnamese validation message keys"
  key_links:
    - from: "PatientRegistrationForm.tsx"
      to: "vi/patient.json"
      via: "useTranslation('patient') for validation messages"
      pattern: "t\\("
---

<objective>
Fix three patient module UI bugs: mouse scroll in allergy dropdown (TASK-2), edit button visibility in edit mode (TASK-3), and validation message improvements (TASK-9).

Purpose: These are frontend-only patient module fixes that can be done independently without backend changes.
Output: Corrected patient registration form with scroll, proper edit mode UX, and i18n-compliant validation messages.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix allergy dropdown scroll and edit button visibility</name>
  <files>frontend/src/features/patient/components/PatientRegistrationForm.tsx, frontend/src/features/patient/components/PatientProfileHeader.tsx, frontend/src/features/patient/components/PatientProfilePage.tsx</files>
  <read_first>frontend/src/features/patient/components/PatientRegistrationForm.tsx, frontend/src/features/patient/components/PatientProfileHeader.tsx, frontend/src/features/patient/components/PatientProfilePage.tsx</read_first>
  <action>
TASK-2 (Allergy dropdown scroll):
In PatientRegistrationForm.tsx, find the allergy selector that uses Command/CommandGroup/CommandList inside a Popover. Ensure the CommandList wrapper has explicit `className="max-h-52 overflow-y-auto"` so mouse wheel scrolling works. The OTC drug combobox pattern (which works) uses `CommandGroup className="max-h-52 overflow-y-auto"` -- replicate this pattern. If CommandList is missing as a wrapper around CommandGroup items, add it.

TASK-3 (Edit button in edit mode):
In PatientProfilePage.tsx, find where `PatientProfileHeader` is rendered and identify the `isEditing` state variable. Pass `isEditing` as a prop to `PatientProfileHeader`.
In PatientProfileHeader.tsx, accept an `isEditing?: boolean` prop. Wrap the Edit button (the one with `onEdit` callback around line 197) in a conditional: only render when `isEditing` is falsy. Example: `{!isEditing && <Button onClick={onEdit}>...</Button>}`.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PatientRegistrationForm.tsx contains `max-h-52 overflow-y-auto` on the allergy CommandList or CommandGroup
    - PatientProfileHeader.tsx contains `isEditing` in its props interface/type
    - PatientProfileHeader.tsx contains conditional rendering like `!isEditing` around the Edit button
    - PatientProfilePage.tsx passes `isEditing` prop to PatientProfileHeader
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Mouse scroll works in allergy dropdown; Edit button hidden during edit mode</done>
</task>

<task type="auto">
  <name>Task 2: Fix patient validation messages (TASK-9)</name>
  <files>frontend/src/features/patient/components/PatientRegistrationForm.tsx, frontend/public/locales/vi/patient.json, frontend/public/locales/en/patient.json, frontend/src/features/patient/api/patient-api.ts</files>
  <read_first>frontend/src/features/patient/components/PatientRegistrationForm.tsx, frontend/public/locales/vi/patient.json, frontend/public/locales/en/patient.json, frontend/src/features/patient/api/patient-api.ts</read_first>
  <action>
TASK-9 Issue 1 (English error messages for DOB/Gender):
In PatientRegistrationForm.tsx, find the Zod schema validation for dateOfBirth and gender fields. Replace any hardcoded English message strings with i18n keys using the pattern from the form. Add corresponding keys to both locale files:
- en/patient.json: `"validation.dobRequired": "Date of birth is required for Medical patients"`, `"validation.genderRequired": "Gender is required for Medical patients"`
- vi/patient.json: `"validation.dobRequired": "Ngay sinh la bat buoc doi voi benh nhan kham benh"`, `"validation.genderRequired": "Gioi tinh la bat buoc doi voi benh nhan kham benh"` (with proper Vietnamese diacritics matching existing file patterns)

TASK-9 Issue 2 (FullName 3-50 character limit):
In the Zod schema, find the `fullName` field validation (currently has `.min(1)`). Change to `.min(3, { message: t("validation.fullNameMin") }).max(50, { message: t("validation.fullNameMax") })`. Add keys:
- en/patient.json: `"validation.fullNameMin": "Full name must be at least 3 characters"`, `"validation.fullNameMax": "Full name must not exceed 50 characters"`
- vi/patient.json: `"validation.fullNameMin": "Ho ten phai co it nhat 3 ky tu"`, `"validation.fullNameMax": "Ho ten khong duoc vuot qua 50 ky tu"` (with proper Vietnamese diacritics)

TASK-9 Issue 3 (Duplicate phone number):
In patient-api.ts, find the `registerPatient` function error handler (around line 186). Instead of showing generic "Failed to register patient" toast, parse the error response. If the error message/code indicates phone duplication (check for "phone" or "already exists" or specific backend error code), return a field-level error object that the form can use to set an error on the phone field. In PatientRegistrationForm.tsx, after the mutation call, if the response contains a phone duplicate error, use `form.setError("phoneNumber", { message: t("validation.phoneDuplicate") })`.
Add keys:
- en/patient.json: `"validation.phoneDuplicate": "Phone number is already in use"`
- vi/patient.json: `"validation.phoneDuplicate": "So dien thoai da duoc su dung"` (with proper diacritics)
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PatientRegistrationForm.tsx contains `.min(3` and `.max(50` for fullName validation
    - vi/patient.json contains keys for "validation.dobRequired", "validation.genderRequired", "validation.fullNameMin", "validation.fullNameMax", "validation.phoneDuplicate"
    - en/patient.json contains matching English keys for all five validation messages
    - patient-api.ts or PatientRegistrationForm.tsx contains phone duplicate error handling with `setError` or field-level error mapping
    - No hardcoded English strings "Date of birth is required" or "Gender is required" remain in PatientRegistrationForm.tsx
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>All three TASK-9 validation issues fixed: Vietnamese error messages for DOB/Gender, fullName 3-50 char validation, phone duplicate shows field-level error</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- Allergy dropdown in PatientRegistrationForm has overflow-y-auto class
- PatientProfileHeader conditionally hides Edit button based on isEditing prop
- All validation messages use i18n keys, no hardcoded English strings for DOB/Gender
- FullName validation enforces 3-50 character range
- Duplicate phone error displays below the phone field, not as generic toast
</verification>

<success_criteria>
All three patient UI bugs (TASK-2, TASK-3, TASK-9) are fixed with proper i18n support and TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-01-SUMMARY.md`
</output>
