---
phase: 10.1-fix-all-issues-in-issues-md
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Shared/Shared.Infrastructure/Services/AzureBlobService.cs
  - frontend/src/features/patient/api/patient-api.ts
  - frontend/src/features/admin/api/clinic-settings-api.ts
autonomous: false
requirements: [TASK-8, TASK-10]

must_haves:
  truths:
    - "Patient photo upload succeeds in development environment"
    - "Clinic logo upload succeeds in development environment"
    - "Upload errors show meaningful error messages to the user"
  artifacts:
    - path: "backend/src/Shared/Shared.Infrastructure/Services/AzureBlobService.cs"
      provides: "Working blob storage with dev fallback or Azurite setup"
  key_links:
    - from: "patient-api.ts"
      to: "AzureBlobService.cs"
      via: "POST /api/patients/{id}/photo upload endpoint"
      pattern: "uploadPatientPhoto|UploadPatientPhoto"
    - from: "clinic-settings-api.ts"
      to: "AzureBlobService.cs"
      via: "POST /api/clinic-settings/logo upload endpoint"
      pattern: "uploadClinicLogo|UploadClinicLogo"
---

<objective>
Diagnose and fix image upload failures for patient photos (TASK-8) and clinic logo (TASK-10). Both share the same Azure Blob Storage root cause.

Purpose: Upload functionality is broken -- users cannot add patient photos or clinic branding. Investigation-first approach per user decision.
Output: Working upload for both patient photos and clinic logos in dev environment.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix Azure Blob upload failures (TASK-8, TASK-10)</name>
  <files>backend/src/Shared/Shared.Infrastructure/Services/AzureBlobService.cs, backend/src/Modules/Patient/Patient.Application/Features/UploadPatientPhoto.cs, backend/src/Shared/Shared.Application/Features/UploadClinicLogo.cs, frontend/src/features/patient/api/patient-api.ts, frontend/src/features/admin/api/clinic-settings-api.ts</files>
  <read_first>backend/src/Shared/Shared.Infrastructure/Services/AzureBlobService.cs, backend/src/Modules/Patient/Patient.Application/Features/UploadPatientPhoto.cs, backend/src/Shared/Shared.Application/Features/UploadClinicLogo.cs, backend/src/Bootstrapper/appsettings.Development.json</read_first>
  <action>
Investigation-first approach:

1. Read AzureBlobService.cs to understand the current storage configuration. It likely falls back to `UseDevelopmentStorage=true` (Azurite) when no connection string is configured.

2. Check backend configuration: Read `appsettings.Development.json` and `appsettings.json` for `AzureStorage:ConnectionString` setting.

3. Check if Azurite is available: Run `azurite --version` or check if the npm package is installed. If not available, the upload will fail silently.

4. Implement fix based on findings:

   Option A (Preferred -- local file system fallback for dev):
   - Add a `LocalFileStorageService` that implements the same interface as `AzureBlobService`
   - In development environment (`IsDevelopment()`), use local file system storage under `wwwroot/uploads/` instead of Azure Blob
   - Register the correct implementation in DI based on environment
   - Ensure the upload endpoint returns the correct URL for local files (e.g., `/uploads/{container}/{filename}`)

   Option B (If Azurite is available):
   - Ensure `UseDevelopmentStorage=true` is properly configured
   - Start Azurite or add it to the dev setup script
   - Verify connection works

   Option C (If real Azure connection string exists):
   - Verify the connection string is correct
   - Check for CORS or firewall issues

5. After fixing the storage service, test both upload endpoints:
   - Start the backend
   - Test patient photo upload: `curl -X POST -F "file=@test.jpg" http://localhost:5255/api/patients/{id}/photo`
   - Test clinic logo upload: `curl -X POST -F "file=@test.jpg" http://localhost:5255/api/clinic-settings/logo`

6. Also check frontend error handling: In patient-api.ts and clinic-settings-api.ts, ensure upload errors are caught and displayed to the user (not swallowed silently). If the error is a generic "Failed to upload", add more specific error messages based on the HTTP status code.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Bootstrapper/Bootstrapper.csproj 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AzureBlobService.cs or a new LocalFileStorageService.cs provides working file upload in development
    - Backend builds successfully
    - Upload configuration is documented in appsettings.Development.json
    - Frontend upload API functions have error handling that shows meaningful messages
  </acceptance_criteria>
  <done>File upload infrastructure works in dev environment for both patient photos and clinic logos</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify upload functionality</name>
  <what-built>Fixed Azure Blob / local file storage for development environment. Both patient photo upload and clinic logo upload should now work.</what-built>
  <how-to-verify>
    1. Start backend (port 5255) and frontend (port 3000)
    2. Log in with Admin@ganka28.com / Admin@123456
    3. Navigate to any patient detail page
    4. Try uploading a patient photo -- should succeed
    5. Navigate to Admin > Clinic Settings
    6. Try uploading a clinic logo -- should succeed
    7. Refresh the page -- uploaded images should persist
  </how-to-verify>
  <resume-signal>Type "approved" if both uploads work, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Backend builds
- Upload endpoint returns 200 for valid file uploads
- Uploaded files are accessible via returned URL
- Frontend shows upload success/error feedback
</verification>

<success_criteria>
Both patient photo (TASK-8) and clinic logo (TASK-10) uploads work in development environment. Human verified.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-04-SUMMARY.md`
</output>
