---
phase: 10.1-fix-all-issues-in-issues-md
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/patient/components/PatientAppointmentTab.tsx
  - frontend/src/features/scheduling/components/AppointmentBookingDialog.tsx
  - frontend/src/features/patient/components/PatientListPage.tsx
  - frontend/src/features/patient/api/patient-api.ts
  - backend/src/Modules/Patient/Patient.Infrastructure/Repositories/PatientRepository.cs
  - backend/src/Modules/Patient/Patient.Application/Features/GetPatientList.cs
  - backend/src/Modules/Patient/Patient.Contracts/Queries/GetPatientListQuery.cs
autonomous: true
requirements: [TASK-5, TASK-7]

must_haves:
  truths:
    - "Book Appointment from Patient Detail auto-fills patient information"
    - "Patient list shows all patients when 'All' status filter is selected"
    - "Patient list defaults to showing Active patients only"
    - "Status filter dropdown exists on Patient List page with options: All, Active, Inactive"
  artifacts:
    - path: "frontend/src/features/scheduling/components/AppointmentBookingDialog.tsx"
      provides: "defaultPatientId prop for pre-filling patient"
    - path: "backend/src/Modules/Patient/Patient.Infrastructure/Repositories/PatientRepository.cs"
      provides: "Parameterized IsActive filter in GetPagedAsync"
    - path: "frontend/src/features/patient/components/PatientListPage.tsx"
      provides: "Status filter dropdown"
  key_links:
    - from: "PatientAppointmentTab.tsx"
      to: "AppointmentBookingDialog.tsx"
      via: "defaultPatientId prop or query param"
      pattern: "patientId|defaultPatient"
    - from: "PatientListPage.tsx"
      to: "patient-api.ts"
      via: "isActive query parameter"
      pattern: "isActive"
---

<objective>
Fix patient appointment auto-fill (TASK-5, HIGH PRIORITY) and inactive patient visibility (TASK-7).

Purpose: TASK-5 is a high-priority usability issue - doctors waste time re-selecting patients. TASK-7 prevents staff from finding inactive patients.
Output: Appointment booking pre-fills patient from detail page; Patient list has status filter defaulting to Active.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-CONTEXT.md
@.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-RESEARCH.md
@issues.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Book Appointment auto-fill patient (TASK-5)</name>
  <files>frontend/src/features/patient/components/PatientAppointmentTab.tsx, frontend/src/features/scheduling/components/AppointmentBookingDialog.tsx</files>
  <read_first>frontend/src/features/patient/components/PatientAppointmentTab.tsx, frontend/src/features/scheduling/components/AppointmentBookingDialog.tsx, frontend/src/features/scheduling/components/AppointmentCalendar.tsx</read_first>
  <action>
TASK-5: Book Appointment from Patient Detail should auto-fill patient.

1. In AppointmentBookingDialog.tsx:
   - Add optional props: `defaultPatientId?: string` and `defaultPatientName?: string`
   - When these props are provided, pre-populate the patient selection field with this patient (skip the patient search step)
   - If the dialog has a patient search/combobox, set its initial value to the provided patient

2. In PatientAppointmentTab.tsx:
   - Find the "Book Appointment" button (around line 112-114) that currently navigates to `/appointments`
   - Instead of navigating away, open the AppointmentBookingDialog directly from the patient detail page, passing the current patient's ID and name as `defaultPatientId` and `defaultPatientName` props
   - Alternative approach if dialog is not easily embeddable: navigate with query params `?patientId={id}&patientName={name}` and have the appointments page read these params to pre-fill the dialog

Choose the approach that best fits the existing code patterns. The key requirement is: clicking "Book Appointment" from patient detail MUST result in the patient being pre-selected in the booking form.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AppointmentBookingDialog.tsx contains `defaultPatientId` in its props type/interface
    - PatientAppointmentTab.tsx passes patient ID to the booking flow (either via props or query params)
    - No navigation to bare `/appointments` without patient context from PatientAppointmentTab
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Clicking "Book Appointment" from Patient Detail auto-fills the patient in the booking form</done>
</task>

<task type="auto">
  <name>Task 2: Add patient status filter with inactive visibility (TASK-7)</name>
  <files>backend/src/Modules/Patient/Patient.Infrastructure/Repositories/PatientRepository.cs, backend/src/Modules/Patient/Patient.Application/Features/GetPatientList.cs, frontend/src/features/patient/components/PatientListPage.tsx, frontend/src/features/patient/api/patient-api.ts</files>
  <read_first>backend/src/Modules/Patient/Patient.Infrastructure/Repositories/PatientRepository.cs, backend/src/Modules/Patient/Patient.Application/Features/GetPatientList.cs, frontend/src/features/patient/components/PatientListPage.tsx, frontend/src/features/patient/api/patient-api.ts</read_first>
  <action>
TASK-7: Show inactive patients when no filter or "All" filter is applied.

Backend changes:
1. In the GetPatientList query record (GetPatientListQuery or equivalent), add an optional parameter: `bool? IsActive = null`
2. In PatientRepository.cs `GetPagedAsync()`, find the hardcoded `.Where(p => p.IsActive)` at line 61. Replace with conditional: `if (query.IsActive.HasValue) query = query.Where(p => p.IsActive == request.IsActive.Value);` -- when IsActive is null, no filter is applied (shows all).
3. In the Minimal API endpoint for patient list, add `[FromQuery] bool? isActive = true` parameter and pass it to the query. Default is `true` so existing callers still see Active patients.

Frontend changes:
4. In patient-api.ts, add `isActive?: boolean | null` parameter to the patient list API function. Pass as query param `isActive` to the endpoint. When `null`, do not send the param (backend defaults to showing all).
5. In PatientListPage.tsx, add a status filter dropdown (Select component from shadcn) above or beside the search input with three options:
   - "Active" (value: `true`) -- DEFAULT selected
   - "Inactive" (value: `false`)
   - "All" (value: `null` or undefined)
   When the filter changes, re-fetch the patient list with the selected isActive value. Use i18n keys for labels.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet build backend/src/Bootstrapper/Bootstrapper.csproj 2>&1 | tail -5 && cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - PatientRepository.cs no longer has hardcoded `.Where(p => p.IsActive)` -- it uses conditional filtering based on the query parameter
    - GetPatientList query record contains `IsActive` optional parameter
    - PatientListPage.tsx contains a Select/dropdown for status filter with "Active", "Inactive", "All" options
    - patient-api.ts passes `isActive` query parameter to the backend
    - Backend builds successfully, frontend TypeScript compiles
  </acceptance_criteria>
  <done>Patient list has status filter defaulting to Active; selecting "All" shows active + inactive patients; selecting "Inactive" shows only inactive</done>
</task>

</tasks>

<verification>
- Backend builds successfully
- Frontend TypeScript compiles
- Patient appointment tab passes patient info to booking dialog
- Patient list has status filter dropdown
- PatientRepository uses parameterized IsActive filter
</verification>

<success_criteria>
TASK-5 (HIGH PRIORITY): Book Appointment from patient detail auto-fills patient. TASK-7: Patient list has working status filter with Active/Inactive/All options.
</success_criteria>

<output>
After completion, create `.planning/phases/10.1-fix-all-issues-in-issues-md/10.1-02-SUMMARY.md`
</output>
