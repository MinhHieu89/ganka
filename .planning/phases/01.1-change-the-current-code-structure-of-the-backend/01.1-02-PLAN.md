---
phase: 01.1-change-the-current-code-structure-of-the-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Audit/Audit.Application/Features/GetAuditLogs.cs
  - backend/src/Modules/Audit/Audit.Application/Features/ExportAuditLogs.cs
  - backend/src/Modules/Audit/Audit.Application/Features/GetAccessLogs.cs
  - backend/src/Modules/Audit/Audit.Application/Endpoints/AuditLogEndpoints.cs
  - backend/src/Modules/Audit/Audit.Application/Endpoints/AccessLogEndpoints.cs
  - backend/src/Modules/Audit/Audit.Application/IAuditReadContext.cs
autonomous: true
requirements: [ARCH-AUDIT-VERTICAL]

must_haves:
  truths:
    - "Audit endpoints are organized as vertical slice feature files under Features/ folder"
    - "Old Endpoints/ folder and files are deleted"
    - "IAuditReadContext is renamed to IAuditReadRepository for naming consistency"
    - "All audit API routes remain unchanged (/api/admin/audit-logs, /api/admin/access-logs, export)"
    - "Solution builds with no errors"
  artifacts:
    - path: "backend/src/Modules/Audit/Audit.Application/Features/GetAuditLogs.cs"
      provides: "Audit log query endpoint as vertical slice"
      contains: "class GetAuditLogsEndpoint"
    - path: "backend/src/Modules/Audit/Audit.Application/Features/ExportAuditLogs.cs"
      provides: "Audit log export endpoint as vertical slice"
      contains: "class ExportAuditLogsEndpoint"
    - path: "backend/src/Modules/Audit/Audit.Application/Features/GetAccessLogs.cs"
      provides: "Access log query endpoint as vertical slice"
      contains: "class GetAccessLogsEndpoint"
  key_links:
    - from: "backend/src/Modules/Audit/Audit.Application/Features/GetAuditLogs.cs"
      to: "IAuditReadRepository"
      via: "parameter injection"
      pattern: "IAuditReadRepository"
    - from: "backend/src/Bootstrapper/Program.cs"
      to: "IAuditReadRepository"
      via: "DI registration update"
      pattern: "IAuditReadRepository"
---

<objective>
Restructure the Audit module's Application layer from type-based Endpoints/ folder to vertical slice Features/ folder. The Audit module is simpler than Auth (3 endpoints, no business logic services to inline), so this is a straightforward file reorganization with namespace changes.

Purpose: Establishes the vertical slice pattern for the Audit module, consistent with the Auth module restructuring.

Output: 3 feature files in Audit.Application/Features/, old Endpoints/ folder deleted, IAuditReadContext renamed to IAuditReadRepository.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-change-the-current-code-structure-of-the-backend/01.1-RESEARCH.md

# Current source files:
@backend/src/Modules/Audit/Audit.Application/Endpoints/AuditLogEndpoints.cs
@backend/src/Modules/Audit/Audit.Application/Endpoints/AccessLogEndpoints.cs
@backend/src/Modules/Audit/Audit.Application/IAuditReadContext.cs
@backend/src/Modules/Audit/Audit.Infrastructure/AuditDbContext.cs
@backend/src/Bootstrapper/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Audit feature files and rename IAuditReadContext</name>
  <files>
    backend/src/Modules/Audit/Audit.Application/Features/GetAuditLogs.cs
    backend/src/Modules/Audit/Audit.Application/Features/ExportAuditLogs.cs
    backend/src/Modules/Audit/Audit.Application/Features/GetAccessLogs.cs
    backend/src/Modules/Audit/Audit.Application/IAuditReadContext.cs
  </files>
  <action>
    1. Read existing `Audit.Application/Endpoints/AuditLogEndpoints.cs` and `AccessLogEndpoints.cs` to understand current endpoint structure.

    2. Read `IAuditReadContext.cs` to understand the interface.

    3. Rename `IAuditReadContext` to `IAuditReadRepository`:
       - Update the interface name in `IAuditReadContext.cs` (or create new file `Repositories/IAuditReadRepository.cs` and delete old one)
       - Keep the `IQueryable<T>` pattern -- the Audit module has a legitimate need for dynamic query composition (multiple optional filters + cursor pagination). Do NOT switch to materialized methods.
       - Update namespace if moving to a Repositories/ subfolder

    4. Create `Audit.Application/Features/GetAuditLogs.cs`:
       - Move the audit log query endpoint from AuditLogEndpoints.cs
       - Namespace: `Audit.Application.Features`
       - Contains the static endpoint class (e.g., `GetAuditLogsEndpoint`) with `[WolverineGet]` attribute
       - Include any query parameter records if they exist in the endpoint
       - Inject `IAuditReadRepository` (renamed from IAuditReadContext)
       - Preserve exact same API route, authorization, and behavior

    5. Create `Audit.Application/Features/ExportAuditLogs.cs`:
       - Move the export endpoint from AuditLogEndpoints.cs (if it's a separate endpoint method)
       - If AuditLogEndpoints.cs has both query and export in one class, split them into separate feature files
       - Same pattern: static endpoint class, same route, same behavior

    6. Create `Audit.Application/Features/GetAccessLogs.cs`:
       - Move the access log endpoint from AccessLogEndpoints.cs
       - Same pattern as above

    7. Update ALL references to `IAuditReadContext` across the codebase:
       - `Audit.Infrastructure/AuditDbContext.cs` (implements the interface)
       - `Bootstrapper/Program.cs` (DI registration: `builder.Services.AddScoped<IAuditReadContext>(...)`)
       - Any other files that reference it (grep for `IAuditReadContext`)
       - Change all to `IAuditReadRepository`
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet build --no-restore 2>&1 | tail -5</automated>
    <manual>Verify all three feature files exist in Features/ folder with correct endpoint routes</manual>
  </verify>
  <done>Three feature files exist in Audit.Application/Features/. IAuditReadContext renamed to IAuditReadRepository everywhere. Solution compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Delete old Audit endpoint files and verify</name>
  <files>
    backend/src/Modules/Audit/Audit.Application/Endpoints/AuditLogEndpoints.cs
    backend/src/Modules/Audit/Audit.Application/Endpoints/AccessLogEndpoints.cs
  </files>
  <action>
    1. Delete `Audit.Application/Endpoints/AuditLogEndpoints.cs`
    2. Delete `Audit.Application/Endpoints/AccessLogEndpoints.cs`
    3. Delete the `Audit.Application/Endpoints/` directory if empty
    4. If `IAuditReadContext.cs` was replaced by a new file (e.g., moved to Repositories/ subfolder), delete the old file
    5. Verify no remaining references to the old `Audit.Application.Endpoints` namespace anywhere in the solution (grep)
    6. Run `dotnet build` to confirm everything compiles
    7. Run architecture tests to confirm no violations: `dotnet test --filter "FullyQualifiedName~ArchitectureTests"`
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet build --no-restore && dotnet test tests/Ganka28.ArchitectureTests/ --no-restore --verbosity quiet 2>&1 | tail -10</automated>
    <manual>Verify Endpoints/ folder no longer exists in Audit.Application</manual>
  </verify>
  <done>Old Endpoints/ folder deleted. No references to old namespaces remain. Solution builds. Architecture tests pass.</done>
</task>

</tasks>

<verification>
1. `dotnet build` passes with zero errors
2. `Audit.Application/Features/` contains GetAuditLogs.cs, ExportAuditLogs.cs, GetAccessLogs.cs
3. `Audit.Application/Endpoints/` directory no longer exists
4. `IAuditReadRepository` is used everywhere (no remaining `IAuditReadContext` references)
5. Architecture tests pass: `dotnet test --filter "FullyQualifiedName~ArchitectureTests"`
6. API routes for audit endpoints are unchanged
</verification>

<success_criteria>
- Audit module Application layer uses Features/ vertical slice organization
- Old Endpoints/ folder is removed
- IAuditReadContext renamed to IAuditReadRepository consistently
- Solution builds and architecture tests pass
- API routes unchanged (no breaking changes)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-change-the-current-code-structure-of-the-backend/01.1-02-SUMMARY.md`
</output>
