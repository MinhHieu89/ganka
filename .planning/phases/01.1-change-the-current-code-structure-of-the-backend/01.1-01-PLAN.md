---
phase: 01.1-change-the-current-code-structure-of-the-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Ganka28.slnx
  - backend/src/Modules/Auth/Auth.Application/Auth.Application.csproj
  - backend/src/Modules/Auth/Auth.Application/Interfaces/IUserRepository.cs
  - backend/src/Modules/Auth/Auth.Application/Interfaces/IRoleRepository.cs
  - backend/src/Modules/Auth/Auth.Application/Interfaces/IPermissionRepository.cs
  - backend/src/Modules/Auth/Auth.Application/Interfaces/IRefreshTokenRepository.cs
  - backend/src/Modules/Auth/Auth.Application/Interfaces/ISystemSettingRepository.cs
  - backend/src/Modules/Auth/Auth.Application/Interfaces/IPasswordHasher.cs
  - backend/src/Modules/Auth/Auth.Application/Interfaces/IUnitOfWork.cs
  - backend/src/Modules/Auth/Auth.Presentation/Auth.Presentation.csproj
  - backend/src/Modules/Auth/Auth.Presentation/AuthApiEndpoints.cs
  - backend/src/Modules/Auth/Auth.Infrastructure/Repositories/UserRepository.cs
  - backend/src/Modules/Auth/Auth.Infrastructure/Repositories/RoleRepository.cs
  - backend/src/Modules/Auth/Auth.Infrastructure/Repositories/PermissionRepository.cs
  - backend/src/Modules/Auth/Auth.Infrastructure/Repositories/RefreshTokenRepository.cs
  - backend/src/Modules/Auth/Auth.Infrastructure/Repositories/SystemSettingRepository.cs
  - backend/src/Modules/Auth/Auth.Infrastructure/UnitOfWork.cs
  - backend/src/Modules/Auth/Auth.Infrastructure/Auth.Infrastructure.csproj
  - backend/src/Modules/Audit/Audit.Application/Audit.Application.csproj
  - backend/src/Modules/Audit/Audit.Application/Interfaces/IAuditReadRepository.cs
  - backend/src/Modules/Audit/Audit.Presentation/Audit.Presentation.csproj
  - backend/src/Modules/Audit/Audit.Presentation/AuditApiEndpoints.cs
  - backend/src/Bootstrapper/Bootstrapper.csproj
  - backend/src/Bootstrapper/Program.cs
  - backend/tests/Ganka28.ArchitectureTests/DependencyDirectionTests.cs
autonomous: true
requirements:
  - INTERNAL-RESTRUCTURE

must_haves:
  truths:
    - "Auth.Application has repository interfaces per aggregate root (IUserRepository, IRoleRepository) plus IRefreshTokenRepository, IPermissionRepository, ISystemSettingRepository"
    - "Auth.Application has IPasswordHasher and IUnitOfWork interfaces in Interfaces/ folder"
    - "Auth.Infrastructure has repository implementations and UnitOfWork class"
    - "Auth.Presentation and Audit.Presentation projects exist with placeholder endpoint extension methods"
    - "Bootstrapper DI registers all new repository/UoW services and maps both module endpoint extensions"
    - "Solution builds and architecture tests pass with Presentation layer properly isolated"
  artifacts:
    - path: "backend/src/Modules/Auth/Auth.Application/Interfaces/IUserRepository.cs"
      provides: "User aggregate repository interface"
    - path: "backend/src/Modules/Auth/Auth.Application/Interfaces/IUnitOfWork.cs"
      provides: "Unit of Work interface"
    - path: "backend/src/Modules/Auth/Auth.Presentation/Auth.Presentation.csproj"
      provides: "Auth Presentation layer project"
    - path: "backend/src/Modules/Audit/Audit.Presentation/Audit.Presentation.csproj"
      provides: "Audit Presentation layer project"
    - path: "backend/src/Modules/Auth/Auth.Infrastructure/UnitOfWork.cs"
      provides: "UnitOfWork wrapping AuthDbContext"
    - path: "backend/src/Modules/Auth/Auth.Infrastructure/Repositories/UserRepository.cs"
      provides: "UserRepository implementation"
  key_links:
    - from: "Auth.Application/Interfaces/IUserRepository.cs"
      to: "Auth.Infrastructure/Repositories/UserRepository.cs"
      via: "DI registration in Bootstrapper Program.cs"
      pattern: "AddScoped<IUserRepository, UserRepository>"
    - from: "Auth.Presentation/AuthApiEndpoints.cs"
      to: "Program.cs"
      via: "MapAuthApiEndpoints() call in middleware pipeline"
      pattern: "app\\.MapAuthApiEndpoints"
    - from: "Auth.Application/Interfaces/IUnitOfWork.cs"
      to: "Auth.Infrastructure/UnitOfWork.cs"
      via: "DI registration in Bootstrapper"
      pattern: "AddScoped<IUnitOfWork, UnitOfWork>"
---

<objective>
Create the foundational infrastructure for the vertical slice restructuring: new Presentation layer projects (Auth.Presentation, Audit.Presentation), all repository interfaces in Auth.Application/Interfaces/, repository implementations in Auth.Infrastructure/Repositories/, IUnitOfWork + UnitOfWork, IPasswordHasher interface, updated Bootstrapper DI wiring, and solution file updates. Audit's IAuditReadContext renamed to IAuditReadRepository.

Purpose: Establish the project structure, interfaces, and DI wiring that all subsequent plans depend on. Without this foundation, feature handlers cannot be written (they inject repository interfaces) and endpoints cannot be created (they live in Presentation projects).

Output: Compiling solution with new projects, interfaces, implementations, and DI wiring. Architecture tests pass. Placeholder endpoints registered.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-change-the-current-code-structure-of-the-backend/01.1-RESEARCH.md

@backend/src/Bootstrapper/Program.cs
@backend/src/Bootstrapper/Bootstrapper.csproj
@backend/Ganka28.slnx
@backend/src/Modules/Auth/Auth.Application/Auth.Application.csproj
@backend/src/Modules/Auth/Auth.Application/Services/IJwtService.cs
@backend/src/Modules/Auth/Auth.Application/Services/IAuthService.cs
@backend/src/Modules/Auth/Auth.Application/Services/IUserService.cs
@backend/src/Modules/Auth/Auth.Application/Services/IRoleService.cs
@backend/src/Modules/Auth/Auth.Application/Services/IPermissionService.cs
@backend/src/Modules/Auth/Auth.Infrastructure/AuthDbContext.cs
@backend/src/Modules/Auth/Auth.Infrastructure/Services/PasswordHasher.cs
@backend/src/Modules/Auth/Auth.Infrastructure/Services/JwtService.cs
@backend/src/Modules/Auth/Auth.Infrastructure/Auth.Infrastructure.csproj
@backend/src/Modules/Auth/Auth.Domain/Entities/
@backend/src/Modules/Audit/Audit.Application/IAuditReadContext.cs
@backend/src/Modules/Audit/Audit.Application/Audit.Application.csproj
@backend/src/Modules/Audit/Audit.Infrastructure/AuditDbContext.cs
@backend/tests/Ganka28.ArchitectureTests/DependencyDirectionTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Presentation projects, Application interfaces, and Infrastructure implementations</name>
  <files>
    backend/Ganka28.slnx
    backend/src/Modules/Auth/Auth.Presentation/Auth.Presentation.csproj
    backend/src/Modules/Auth/Auth.Presentation/AuthApiEndpoints.cs
    backend/src/Modules/Audit/Audit.Presentation/Audit.Presentation.csproj
    backend/src/Modules/Audit/Audit.Presentation/AuditApiEndpoints.cs
    backend/src/Modules/Auth/Auth.Application/Auth.Application.csproj
    backend/src/Modules/Auth/Auth.Application/Interfaces/IUserRepository.cs
    backend/src/Modules/Auth/Auth.Application/Interfaces/IRoleRepository.cs
    backend/src/Modules/Auth/Auth.Application/Interfaces/IPermissionRepository.cs
    backend/src/Modules/Auth/Auth.Application/Interfaces/IRefreshTokenRepository.cs
    backend/src/Modules/Auth/Auth.Application/Interfaces/ISystemSettingRepository.cs
    backend/src/Modules/Auth/Auth.Application/Interfaces/IPasswordHasher.cs
    backend/src/Modules/Auth/Auth.Application/Interfaces/IUnitOfWork.cs
    backend/src/Modules/Auth/Auth.Infrastructure/Repositories/UserRepository.cs
    backend/src/Modules/Auth/Auth.Infrastructure/Repositories/RoleRepository.cs
    backend/src/Modules/Auth/Auth.Infrastructure/Repositories/PermissionRepository.cs
    backend/src/Modules/Auth/Auth.Infrastructure/Repositories/RefreshTokenRepository.cs
    backend/src/Modules/Auth/Auth.Infrastructure/Repositories/SystemSettingRepository.cs
    backend/src/Modules/Auth/Auth.Infrastructure/UnitOfWork.cs
    backend/src/Modules/Audit/Audit.Application/Audit.Application.csproj
    backend/src/Modules/Audit/Audit.Application/Interfaces/IAuditReadRepository.cs
    backend/src/Modules/Audit/Audit.Application/IAuditReadContext.cs
  </files>
  <action>
**1. Create Auth.Presentation project:**
- Create `Auth.Presentation/Auth.Presentation.csproj` as a class library targeting `net10.0` with:
  - `PackageReference` to `WolverineFx` (NOT WolverineFx.Http) version `5.*`
  - `FrameworkReference` to `Microsoft.AspNetCore.App`
  - `ProjectReference` to `Auth.Application.csproj`
- Create `Auth.Presentation/AuthApiEndpoints.cs` with a static class `AuthApiEndpoints` containing a public static extension method `MapAuthApiEndpoints(this IEndpointRouteBuilder app)` that returns `IEndpointRouteBuilder`. For now, add a `// TODO: endpoints will be added by Plan 03 and Plan 04` comment and just return `app`. This is a placeholder -- the actual endpoint definitions are created in Plans 03 and 04.

**2. Create Audit.Presentation project:**
- Create `Audit.Presentation/Audit.Presentation.csproj` as a class library targeting `net10.0` with:
  - `PackageReference` to `WolverineFx` version `5.*`
  - `FrameworkReference` to `Microsoft.AspNetCore.App`
  - `ProjectReference` to `Audit.Application.csproj`
- Create `Audit.Presentation/AuditApiEndpoints.cs` with static class `AuditApiEndpoints` containing `MapAuditApiEndpoints(this IEndpointRouteBuilder app)` returning `IEndpointRouteBuilder`. Placeholder -- Plan 02 adds actual endpoints.

**3. Create Auth.Application/Interfaces/ with all repository interfaces:**

Read the existing domain entities in `Auth.Domain/Entities/` and the existing service interfaces (`IAuthService`, `IUserService`, `IRoleService`, `IPermissionService`, `IJwtService`) plus the Infrastructure implementations (`AuthService.cs`, `UserService.cs`, `RoleService.cs`, `PermissionService.cs`, `JwtService.cs`, `PasswordHasher.cs`) to understand all data access patterns. Then create:

- `IUserRepository.cs` -- Methods: `GetByIdAsync(Guid)`, `GetByIdWithRolesAsync(Guid)`, `GetByEmailWithRolesAndPermissionsAsync(string)`, `GetPagedAsync(int page, int pageSize)` returning `(List<User>, int TotalCount)`, `EmailExistsAsync(string)`, `Add(User)`. Derive exact method signatures from what existing AuthService/UserService actually queries.
- `IRoleRepository.cs` -- Methods: `GetByIdAsync(Guid)`, `GetByIdWithPermissionsAsync(Guid)`, `GetAllWithPermissionsAsync()`, `NameExistsAsync(string, Guid? excludeId)`, `Add(Role)`.
- `IPermissionRepository.cs` -- Read-only: `GetAllAsync()`, `GetByIdsAsync(IEnumerable<Guid>)`.
- `IRefreshTokenRepository.cs` -- Methods: `GetByTokenAsync(string)`, `GetActiveByFamilyIdAsync(Guid)`, `Add(RefreshToken)`, `RevokeAllByFamilyIdAsync(Guid, string reason)`, `RevokeAllByUserIdAsync(Guid, string reason)`.
- `ISystemSettingRepository.cs` -- Read-only: `GetValueAsync(string key)`.
- `IPasswordHasher.cs` -- Methods: `HashPassword(string)`, `VerifyPassword(string password, string storedHash)`. Extracted from existing `PasswordHasher` concrete class in Infrastructure.
- `IUnitOfWork.cs` -- Method: `Task<int> SaveChangesAsync(CancellationToken cancellationToken = default)`.

Move the existing `IJwtService.cs` interface from `Auth.Application/Services/` to `Auth.Application/Interfaces/`. Add `GetRefreshTokenLifetimeDays(bool rememberMe)` to the interface if it's not already there (research says it's on the concrete class but not the interface). Update the namespace from `Auth.Application.Services` to `Auth.Application.Interfaces`.

All interfaces use namespace `Auth.Application.Interfaces`.

**4. Modify Auth.Application.csproj:**
- Remove `WolverineFx.Http` package reference (HTTP concerns move to Presentation)
- Remove `FrameworkReference Microsoft.AspNetCore.App` (no longer needed in Application)
- Keep `FluentValidation` and project references to `Auth.Domain` and `Auth.Contracts`

**5. Create Auth.Infrastructure/Repositories/ implementations:**

Read existing service implementations (`AuthService.cs`, `UserService.cs`, `RoleService.cs`, `PermissionService.cs`) to extract data access logic. Create thin repository classes:

- `UserRepository.cs` -- Implements `IUserRepository`. Constructor injects `AuthDbContext`. Pure EF Core queries. No business logic. No SaveChanges.
- `RoleRepository.cs` -- Implements `IRoleRepository`. Same pattern.
- `PermissionRepository.cs` -- Implements `IPermissionRepository`. Read-only queries.
- `RefreshTokenRepository.cs` -- Implements `IRefreshTokenRepository`. Token lookup and revocation queries.
- `SystemSettingRepository.cs` -- Implements `ISystemSettingRepository`. Simple key-value lookup.

**6. Create Auth.Infrastructure/UnitOfWork.cs:**
- `UnitOfWork` class implementing `IUnitOfWork` from `Auth.Application.Interfaces`
- Constructor injects `AuthDbContext`
- `SaveChangesAsync` delegates to `_dbContext.SaveChangesAsync()`

**7. Update Auth.Infrastructure/Services/PasswordHasher.cs:**
- Add `: IPasswordHasher` interface implementation to the existing `PasswordHasher` class
- Add `using Auth.Application.Interfaces;`
- No logic changes -- just implement the new interface

**8. Rename IAuditReadContext to IAuditReadRepository:**
- Create `Audit.Application/Interfaces/IAuditReadRepository.cs` with same content as `IAuditReadContext.cs` but renamed interface and updated namespace to `Audit.Application.Interfaces`
- Delete `Audit.Application/IAuditReadContext.cs`
- Update `AuditDbContext.cs` to implement `IAuditReadRepository` instead of `IAuditReadContext`

**9. Modify Audit.Application.csproj:**
- Remove `WolverineFx.Http` package reference
- Remove `FrameworkReference Microsoft.AspNetCore.App`
- Keep `FluentValidation`, `Microsoft.EntityFrameworkCore`

**10. Update Ganka28.slnx:**
- Add `Auth.Presentation` and `Audit.Presentation` projects under their respective module folders
- Add no new test projects yet (that's Plan 05)

**IMPORTANT NOTES:**
- Do NOT delete existing `Auth.Application/Endpoints/`, `Auth.Application/Validators/`, or `Auth.Application/Services/` folders yet. Those are consumed by existing Wolverine.Http endpoints that still work. Plans 02-04 will delete them when migrating to features.
- Do NOT delete existing `Auth.Infrastructure/Services/AuthService.cs`, `UserService.cs`, etc. yet. Those are still registered in DI and used by existing endpoints. Plans 03-04 will remove them.
- The old DI registrations (`IAuthService`, `IUserService`, etc.) stay in Program.cs for now. Plans 03-04 will remove them when features replace the old endpoints.
- Keep `app.MapWolverineEndpoints()` in Program.cs -- scaffold modules still use it.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet build Ganka28.slnx --no-restore 2>&1 | tail -5</automated>
    <manual>Verify Auth.Presentation and Audit.Presentation projects exist in solution, all interfaces exist in Auth.Application/Interfaces/, all repository implementations exist in Auth.Infrastructure/Repositories/</manual>
  </verify>
  <done>
- Auth.Presentation and Audit.Presentation projects created with correct dependencies
- 7 interfaces in Auth.Application/Interfaces/ (IUserRepository, IRoleRepository, IPermissionRepository, IRefreshTokenRepository, ISystemSettingRepository, IPasswordHasher, IUnitOfWork) plus IJwtService (moved from Services/)
- 5 repository implementations in Auth.Infrastructure/Repositories/ + UnitOfWork.cs
- IAuditReadContext renamed to IAuditReadRepository in Audit.Application/Interfaces/
- Auth.Application.csproj no longer depends on WolverineFx.Http or ASP.NET Core
- Solution builds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Bootstrapper DI wiring and architecture tests for Presentation layer</name>
  <files>
    backend/src/Bootstrapper/Bootstrapper.csproj
    backend/src/Bootstrapper/Program.cs
    backend/tests/Ganka28.ArchitectureTests/DependencyDirectionTests.cs
  </files>
  <action>
**1. Update Bootstrapper.csproj:**
- Add project references to `Auth.Presentation` and `Audit.Presentation`

**2. Update Program.cs DI registrations:**
- Add new repository/UoW registrations ALONGSIDE existing service registrations (both must coexist until Plans 03-04 migrate features):
  ```
  builder.Services.AddScoped<IUserRepository, UserRepository>();
  builder.Services.AddScoped<IRoleRepository, RoleRepository>();
  builder.Services.AddScoped<IPermissionRepository, PermissionRepository>();
  builder.Services.AddScoped<IRefreshTokenRepository, RefreshTokenRepository>();
  builder.Services.AddScoped<ISystemSettingRepository, SystemSettingRepository>();
  builder.Services.AddScoped<IUnitOfWork, UnitOfWork>();
  builder.Services.AddSingleton<IPasswordHasher, PasswordHasher>();
  ```
- Add using statements for `Auth.Application.Interfaces`, `Auth.Infrastructure.Repositories`
- Replace `IAuditReadContext` registration with `IAuditReadRepository`:
  `builder.Services.AddScoped<IAuditReadRepository>(sp => sp.GetRequiredService<AuditDbContext>());`
  Use `Audit.Application.Interfaces` namespace.
- Add Minimal API endpoint mapping calls AFTER `app.MapWolverineEndpoints(...)`:
  ```
  app.MapAuthApiEndpoints();
  app.MapAuditApiEndpoints();
  ```
- Add using statements for `Auth.Presentation` and `Audit.Presentation`
- Keep ALL existing service registrations (`IAuthService`, `IUserService`, etc.) -- Plans 03-04 remove them

**3. Update DependencyDirectionTests.cs:**
- Add `"Presentation"` as a recognized layer in the architecture tests
- Add a test `Presentation_Should_Not_Depend_On_Infrastructure` verifying that Presentation projects only depend on Application (not Infrastructure or Domain directly, though transitive Domain is OK)
- Add Presentation to the test that ensures Application does not depend on Infrastructure (Presentation is a separate layer, not Application)
- The existing `Application_Should_Not_Depend_On_Infrastructure` test must still pass -- Auth.Application no longer has WolverineFx.Http dependency

**CRITICAL:** Ensure the existing `IAuditReadContext` reference in `Program.cs` is updated to `IAuditReadRepository` (the rename from Task 1). Grep for all `IAuditReadContext` references across the codebase and update them.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet build Ganka28.slnx 2>&1 | tail -3 && dotnet test tests/Ganka28.ArchitectureTests/ --no-build --verbosity normal 2>&1 | tail -10</automated>
    <manual>Check Program.cs has both old service registrations and new repository registrations coexisting</manual>
  </verify>
  <done>
- Bootstrapper references Auth.Presentation and Audit.Presentation
- Program.cs registers all new repositories, IUnitOfWork, IPasswordHasher alongside existing service registrations
- Program.cs calls MapAuthApiEndpoints() and MapAuditApiEndpoints()
- IAuditReadContext fully renamed to IAuditReadRepository everywhere
- Architecture tests pass including new Presentation layer isolation test
- Solution builds and all existing architecture tests pass
  </done>
</task>

</tasks>

<verification>
1. `dotnet build Ganka28.slnx` succeeds with 0 errors
2. `dotnet test tests/Ganka28.ArchitectureTests/` -- all tests pass (including new Presentation layer test)
3. Auth.Application.csproj does NOT reference WolverineFx.Http or Microsoft.AspNetCore.App
4. Audit.Application.csproj does NOT reference WolverineFx.Http or Microsoft.AspNetCore.App
5. No references to `IAuditReadContext` remain in codebase (fully renamed)
6. Existing Auth endpoints still work via Wolverine.Http (not yet migrated)
</verification>

<success_criteria>
- Solution compiles cleanly with new Presentation projects and all interfaces/implementations
- Architecture tests pass, proving dependency direction is correct
- Both old (Wolverine.Http) and new (placeholder Minimal API) endpoint paths coexist without conflict
- DI container resolves all new interfaces without runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-change-the-current-code-structure-of-the-backend/01.1-01-SUMMARY.md`
</output>
