---
phase: 03-clinical-workflow-examination
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified: []
autonomous: false
requirements:
  - CLN-01
  - CLN-02
  - CLN-03
  - CLN-04
  - REF-01
  - REF-02
  - REF-03
  - DX-01
  - DX-02

must_haves:
  truths:
    - "Doctor can create a visit, record examination data, and sign off -- complete workflow"
    - "Signed visits are immutable; amendments preserve original and log changes"
    - "Kanban dashboard shows active patients in real-time"
    - "Refraction data entry works for all 3 types with OD/OS layout"
    - "ICD-10 search works in both languages with per-doctor favorites and laterality enforcement"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete clinical workflow: Kanban dashboard, visit lifecycle, refraction recording, ICD-10 diagnosis, sign-off immutability, and amendment workflow.

Purpose: Verify all Phase 3 requirements (CLN-01 through DX-02) work together before marking the phase complete.

Output: Verification results confirming all success criteria are met.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-clinical-workflow-examination/03-CONTEXT.md
@.planning/phases/03-clinical-workflow-examination/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification -- backend build, tests, and API endpoint checks</name>
  <files></files>
  <action>
Run the following verifications:

1. **Full solution build:**
   ```bash
   dotnet build backend/Ganka28.slnx
   ```
   Expected: 0 errors in Clinical module projects

2. **Clinical unit tests:**
   ```bash
   dotnet test backend/tests/Clinical.Unit.Tests -x
   ```
   Expected: All tests pass, 80%+ coverage on handler logic

3. **Full test suite:**
   ```bash
   dotnet test backend/Ganka28.slnx
   ```
   Expected: All tests across all modules pass (no regressions)

4. **Frontend build:**
   ```bash
   cd frontend && npm run build
   ```
   Expected: 0 errors

5. **Frontend TypeScript check:**
   ```bash
   cd frontend && npx tsc --noEmit
   ```
   Expected: 0 TypeScript errors

6. **Start backend and verify API endpoints are accessible:**
   Start backend on port 5255 and verify:
   - GET /swagger -- Swagger UI loads with /api/clinical/* endpoints listed
   - All clinical endpoints are visible in the swagger doc

7. **Start frontend and verify pages render:**
   Start frontend on port 3000 and verify:
   - /clinical route loads Kanban dashboard
   - No console errors on initial load

Record any failures for the checkpoint task.
  </action>
  <verify>
    <automated>cd D:/projects/ganka && dotnet test backend/tests/Clinical.Unit.Tests -x && cd frontend && npm run build</automated>
  </verify>
  <done>Full solution builds. All Clinical unit tests pass. Frontend builds with 0 errors. API endpoints visible in Swagger. /clinical route renders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete clinical workflow</name>
  <files></files>
  <action>
    Human verifies all Clinical Workflow and Examination features end-to-end.
    Backend running at http://localhost:5255, Frontend at http://localhost:3000.

    Complete system built:
    - Kanban workflow dashboard with 5 grouped columns and drag-and-drop
    - Visit detail page with collapsible card sections
    - Refraction recording with OD/OS side-by-side and 3 types
    - ICD-10 diagnosis with bilingual search, per-doctor favorites, laterality enforcement
    - Visit sign-off with immutability
    - Amendment workflow with mandatory reason and field-level diff

    Verification steps:

    1. **Kanban Dashboard (CLN-03, CLN-04):**
       - Navigate to /clinical
       - Verify 5 Kanban columns are visible (Reception, Testing, Doctor, Processing, Done)
       - Create a new visit via "New Visit" button (select a patient, assign doctor)
       - Verify the new visit appears as a card in the "Reception" column
       - Drag the card from Reception to Testing -- verify stage updates
       - Click the "Advance" button on the card -- verify stage advances
       - Verify wait time shows correctly on card
       - Verify allergy warning icon appears for patients with allergies

    2. **Visit Detail Page (CLN-01):**
       - Click on a patient card to open visit details at /visits/{visitId}
       - Verify single scrollable page with collapsible card sections
       - Verify Patient Info section shows patient name, doctor, visit date
       - Collapse and expand sections -- verify animation works

    3. **Refraction Recording (REF-01, REF-02, REF-03):**
       - Open Refraction section
       - Verify Manifest | Autorefraction | Cycloplegic tabs
       - Enter refraction data in Manifest tab: SPH, CYL, AXIS for OD and OS
       - Verify OD/OS side-by-side layout
       - Enter VA (UCVA, BCVA), IOP with method, and Axial Length
       - Switch to Autorefraction tab and enter different values
       - Verify (*) indicator appears on tabs with data
       - Verify data auto-saves on field blur

    4. **ICD-10 Diagnosis (DX-01, DX-02):**
       - Open Diagnosis section
       - Click "Add Diagnosis"
       - Type "dry" in search -- verify results appear in Vietnamese and English
       - Star a favorite code -- verify it appears at top on next search
       - Select a code that requires laterality -- verify OD/OS/OU selector appears
       - Select OD -- verify diagnosis added with "OD" laterality badge
       - Select another code with OU -- verify two diagnosis entries created
       - Verify first diagnosis shows "Primary" badge, second shows "Secondary"
       - Remove a diagnosis with X button

    5. **Examination Notes:**
       - Type notes in the Examination Notes textarea
       - Verify auto-save (check for success indicator)

    6. **Sign-Off (CLN-01):**
       - Click "Sign Off Visit" at the bottom
       - Verify confirmation dialog with warning message
       - Confirm sign-off
       - Verify ALL fields become read-only (refraction inputs disabled, diagnosis add/remove buttons gone, notes textarea disabled)
       - Verify signed badge with timestamp appears

    7. **Amendment (CLN-02):**
       - Click "Amend" button on the signed visit
       - Verify amendment dialog with mandatory reason field
       - Enter reason and confirm
       - Verify fields become editable again
       - Make a change (e.g., edit a refraction value)
       - Sign off again
       - Verify amendment history section shows the amendment with reason and field changes

    8. **Vietnamese Language:**
       - Switch to Vietnamese
       - Verify all clinical labels, column headers, and messages are in Vietnamese with proper diacritics
  </action>
  <verify>
    <automated>echo "Human verification -- no automated check"</automated>
  </verify>
  <done>Human confirms all 9 requirements (CLN-01 through DX-02) work correctly. Type "approved" or describe issues found.</done>
</task>

</tasks>

<verification>
All Phase 3 success criteria verified:
1. Doctor can create a visit record, record examination findings, and sign off -- immutable
2. Corrections to signed records create amendments with reason and field-level changes
3. Dashboard shows active patients and workflow stages in real-time
4. Refraction data recording with SPH, CYL, AXIS, ADD, PD, VA, IOP, Axial Length per eye, 3 types
5. ICD-10 search in Vietnamese and English with per-doctor favorites and laterality enforcement
</verification>

<success_criteria>
Human verification confirms all 9 requirements (CLN-01 through DX-02) work correctly in the running application. No blocking issues found.
</success_criteria>

<output>
After completion, create `.planning/phases/03-clinical-workflow-examination/03-05-SUMMARY.md`
</output>
