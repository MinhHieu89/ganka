---
phase: 02-patient-management-scheduling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/shared/components/Command.tsx
  - frontend/src/shared/components/Popover.tsx
  - frontend/src/shared/components/Tabs.tsx
  - frontend/src/shared/components/DatePicker.tsx
  - frontend/src/shared/components/GlobalSearch.tsx
  - frontend/src/shared/stores/recentPatientsStore.ts
  - frontend/src/shared/components/SiteHeader.tsx
  - frontend/src/shared/components/AppSidebar.tsx
  - frontend/src/app/routes/_authenticated/dashboard.tsx
  - frontend/public/locales/en/patient.json
  - frontend/public/locales/vi/patient.json
  - frontend/public/locales/en/scheduling.json
  - frontend/public/locales/vi/scheduling.json
  - frontend/public/locales/en/common.json
  - frontend/public/locales/vi/common.json
autonomous: true
requirements:
  - PAT-04

must_haves:
  truths:
    - "FullCalendar and cmdk packages installed and importable"
    - "Global search bar in SiteHeader shows recent patients when focused (before typing)"
    - "Global search returns patient results when typing 2+ characters"
    - "Dashboard shows recent patients widget"
    - "Sidebar Patients and Appointments nav items are now active (not disabled)"
  artifacts:
    - path: "frontend/src/shared/components/Command.tsx"
      provides: "shadcn Command wrapper (re-exports cmdk)"
      contains: "export"
    - path: "frontend/src/shared/components/GlobalSearch.tsx"
      provides: "Global search combobox in SiteHeader"
      contains: "GlobalSearch"
    - path: "frontend/src/shared/stores/recentPatientsStore.ts"
      provides: "Zustand store for recent patients"
      contains: "useRecentPatientsStore"
    - path: "frontend/public/locales/en/patient.json"
      provides: "English translations for patient module"
    - path: "frontend/public/locales/vi/patient.json"
      provides: "Vietnamese translations for patient module"
  key_links:
    - from: "frontend/src/shared/components/GlobalSearch.tsx"
      to: "frontend/src/shared/stores/recentPatientsStore.ts"
      via: "Zustand store import for recent patients"
      pattern: "useRecentPatientsStore"
    - from: "frontend/src/shared/components/SiteHeader.tsx"
      to: "frontend/src/shared/components/GlobalSearch.tsx"
      via: "Component composition in header"
      pattern: "GlobalSearch"
---

<objective>
Install new frontend dependencies (FullCalendar, cmdk, radix primitives), create shadcn wrapper components following the established wrapper pattern, build the global search component with recent patients, and prepare i18n translations for Patient and Scheduling modules.

Purpose: Provides the shared frontend infrastructure that Plan 04 (Patient Frontend) and Plan 05 (Scheduling Frontend) both depend on -- search, wrappers, calendar library, and translations.
Output: All new npm packages installed, 5 new shadcn wrappers, GlobalSearch in SiteHeader, recent patients Zustand store, dashboard widget, and i18n files for patient/scheduling namespaces.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-management-scheduling/02-CONTEXT.md
@.planning/phases/02-patient-management-scheduling/02-RESEARCH.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-01b-SUMMARY.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-02-SUMMARY.md

# Key existing files for wrapper pattern:
@frontend/src/shared/components/Button.tsx
@frontend/src/shared/components/Dialog.tsx
@frontend/src/shared/components/SiteHeader.tsx
@frontend/src/shared/components/AppSidebar.tsx
@frontend/src/shared/stores/authStore.ts
@frontend/src/app/routes/_authenticated/dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create shadcn wrapper components</name>
  <files>
    frontend/package.json
    frontend/src/shared/components/ui/command.tsx
    frontend/src/shared/components/ui/popover.tsx
    frontend/src/shared/components/ui/tabs.tsx
    frontend/src/shared/components/ui/calendar.tsx
    frontend/src/shared/components/Command.tsx
    frontend/src/shared/components/Popover.tsx
    frontend/src/shared/components/Tabs.tsx
    frontend/src/shared/components/DatePicker.tsx
  </files>
  <action>
    1. Install new npm packages:
       ```bash
       cd frontend
       npm install @fullcalendar/core @fullcalendar/react @fullcalendar/timegrid @fullcalendar/daygrid @fullcalendar/interaction
       npm install cmdk @radix-ui/react-popover @radix-ui/react-tabs
       ```
       Note: react-day-picker may already be installed (check package.json). If not, install it.

    2. Generate shadcn/ui primitives using the shadcn CLI:
       ```bash
       cd frontend
       npx shadcn@latest add command popover tabs calendar
       ```
       This creates files in `src/shared/components/ui/` (command.tsx, popover.tsx, tabs.tsx, calendar.tsx)

    3. Create wrapper components following the established re-export pattern from Phase 01.2-01b:

       **Command.tsx:** Re-export all exports from `./ui/command`:
       ```typescript
       export {
         Command,
         CommandDialog,
         CommandInput,
         CommandList,
         CommandEmpty,
         CommandGroup,
         CommandItem,
         CommandShortcut,
         CommandSeparator,
       } from './ui/command'
       ```

       **Popover.tsx:** Re-export from `./ui/popover`:
       ```typescript
       export { Popover, PopoverTrigger, PopoverContent, PopoverAnchor } from './ui/popover'
       ```

       **Tabs.tsx:** Re-export from `./ui/tabs`:
       ```typescript
       export { Tabs, TabsList, TabsTrigger, TabsContent } from './ui/tabs'
       ```

       **DatePicker.tsx:** Create a reusable DatePicker form component (NOT just a re-export):
       ```typescript
       // A form-friendly date picker that combines Popover + Calendar + Button
       // Uses react-day-picker via shadcn Calendar primitive
       // Accepts: value (Date | undefined), onChange, placeholder, locale
       // Renders: Button showing formatted date -> Popover with Calendar
       // Format dates in Vietnamese locale: dd/MM/yyyy
       ```

    4. Verify all imports resolve:
       ```bash
       cd frontend && npx tsc --noEmit 2>&1 | head -20
       ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
    <manual>Verify Command, Popover, Tabs, DatePicker wrapper files exist following the pattern from Button.tsx / Dialog.tsx</manual>
  </verify>
  <done>FullCalendar (5 packages), cmdk, radix-popover, radix-tabs installed. 4 shadcn primitives generated (command, popover, tabs, calendar). 4 wrapper components created (Command, Popover, Tabs, DatePicker). All TypeScript imports resolve.</done>
</task>

<task type="auto">
  <name>Task 2: GlobalSearch component, recent patients store, dashboard widget, sidebar activation, and i18n translations</name>
  <files>
    frontend/src/shared/components/GlobalSearch.tsx
    frontend/src/shared/stores/recentPatientsStore.ts
    frontend/src/shared/components/SiteHeader.tsx
    frontend/src/shared/components/AppSidebar.tsx
    frontend/src/app/routes/_authenticated/dashboard.tsx
    frontend/public/locales/en/patient.json
    frontend/public/locales/vi/patient.json
    frontend/public/locales/en/scheduling.json
    frontend/public/locales/vi/scheduling.json
    frontend/public/locales/en/common.json
    frontend/public/locales/vi/common.json
  </files>
  <action>
    1. Create `recentPatientsStore.ts` as Zustand store (follow authStore.ts pattern):
       ```typescript
       // Store last 10 recently viewed/registered patients in localStorage
       // Interface: { id: string, fullName: string, patientCode: string, phone: string }
       // Actions: addRecent(patient), clearRecent()
       // Uses zustand persist middleware with localStorage
       // Deduplicates by id, moves existing to front, caps at 10
       ```

    2. Create `GlobalSearch.tsx` component:
       - Uses Command (cmdk) + Popover pattern from research
       - Opens on click or keyboard shortcut (Ctrl+K)
       - When focused and query empty: show recent patients from recentPatientsStore
       - When typing (2+ chars): debounce search via API call to `/api/patients/search?term=X`
       - Use `useDeferredValue` (React 19) for the search term
       - Results show patient icon (IconUser from @tabler/icons-react), name, patient code, phone
       - Clicking a result navigates to `/patients/{id}` and adds to recent patients store
       - Prepared for future entity types (show entity type icon)
       - Use `useTranslation` for all visible text
       - Follow the code example from research exactly (with adjustments for actual API hooks)
       - Create a temporary `usePatientSearch` hook that calls the API. Place it in `frontend/src/features/patient/hooks/usePatientSearch.ts`. It should use React Query + openapi-fetch to call GET /api/patients/search.

    3. Update `SiteHeader.tsx`:
       - Add `<GlobalSearch />` component in the header bar, positioned between breadcrumbs and language toggle
       - Import from `@/shared/components/GlobalSearch`

    4. Update `AppSidebar.tsx`:
       - Change "Patients" nav item from disabled (with "Coming soon" tooltip) to active with href="/patients"
       - Change "Appointments" nav item from disabled to active with href="/appointments"
       - Keep all other placeholder items disabled

    5. Update `dashboard.tsx`:
       - Add a "Recent Patients" widget card showing last 5 patients from recentPatientsStore
       - Each entry shows patient name, code, and a link to profile
       - If no recent patients, show empty state message
       - Use Card component from shadcn wrappers

    6. Create i18n translation files:

       **patient.json (en):**
       ```json
       {
         "title": "Patients",
         "register": "Register Patient",
         "search": "Search patients...",
         "recent": "Recent Patients",
         "noRecent": "No recent patients",
         "list": "Patient List",
         "profile": "Patient Profile",
         "medicalPatient": "Medical Patient",
         "walkInCustomer": "Walk-in Customer",
         "fullName": "Full Name",
         "phone": "Phone Number",
         "dateOfBirth": "Date of Birth",
         "gender": "Gender",
         "male": "Male",
         "female": "Female",
         "other": "Other",
         "address": "Address",
         "cccd": "CCCD/ID Number",
         "patientCode": "Patient Code",
         "patientType": "Patient Type",
         "active": "Active",
         "inactive": "Inactive",
         "deactivate": "Deactivate",
         "reactivate": "Reactivate",
         "allergies": "Allergies",
         "addAllergy": "Add Allergy",
         "allergyName": "Allergy Name",
         "severity": "Severity",
         "mild": "Mild",
         "moderate": "Moderate",
         "severe": "Severe",
         "noAllergies": "No known allergies",
         "allergyAlert": "Allergy Alert",
         "overview": "Overview",
         "appointments": "Appointments",
         "photo": "Photo",
         "uploadPhoto": "Upload Photo",
         "age": "age",
         "edit": "Edit",
         "save": "Save",
         "cancel": "Cancel",
         "filters": "Filters",
         "hasAllergies": "Has Allergies",
         "dateRange": "Date Range",
         "yes": "Yes",
         "no": "No"
       }
       ```

       **patient.json (vi):** Same keys with proper Vietnamese diacritics:
       ```json
       {
         "title": "Bệnh nhân",
         "register": "Đăng ký bệnh nhân",
         "search": "Tìm kiếm bệnh nhân...",
         "recent": "Bệnh nhân gần đây",
         "noRecent": "Chưa có bệnh nhân gần đây",
         "list": "Danh sách bệnh nhân",
         "profile": "Hồ sơ bệnh nhân",
         "medicalPatient": "Bệnh nhân khám bệnh",
         "walkInCustomer": "Khách mua thuốc",
         "fullName": "Họ và tên",
         "phone": "Số điện thoại",
         "dateOfBirth": "Ngày sinh",
         "gender": "Giới tính",
         "male": "Nam",
         "female": "Nữ",
         "other": "Khác",
         "address": "Địa chỉ",
         "cccd": "Số CCCD",
         "patientCode": "Mã bệnh nhân",
         "patientType": "Loại bệnh nhân",
         "active": "Hoạt động",
         "inactive": "Ngưng hoạt động",
         "deactivate": "Ngưng hoạt động",
         "reactivate": "Kích hoạt lại",
         "allergies": "Dị ứng",
         "addAllergy": "Thêm dị ứng",
         "allergyName": "Tên dị ứng",
         "severity": "Mức độ",
         "mild": "Nhẹ",
         "moderate": "Trung bình",
         "severe": "Nặng",
         "noAllergies": "Không có dị ứng",
         "allergyAlert": "Cảnh báo dị ứng",
         "overview": "Tổng quan",
         "appointments": "Lịch hẹn",
         "photo": "Ảnh",
         "uploadPhoto": "Tải ảnh lên",
         "age": "tuổi",
         "edit": "Sửa",
         "save": "Lưu",
         "cancel": "Hủy",
         "filters": "Bộ lọc",
         "hasAllergies": "Có dị ứng",
         "dateRange": "Khoảng thời gian",
         "yes": "Có",
         "no": "Không"
       }
       ```

       **scheduling.json (en):** Include keys for appointment types, statuses, booking, calendar labels, self-booking page
       **scheduling.json (vi):** Vietnamese translations with proper diacritics

    7. Update `common.json` (en/vi) to add any shared keys needed (e.g., "searchPlaceholder", "noResults", "loading" if not already present)

    8. Register patient and scheduling i18n namespaces in the i18next configuration (check how auth/admin/audit namespaces are loaded and follow the same pattern)
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
    <manual>Verify GlobalSearch appears in SiteHeader, Patients/Appointments sidebar items are active, dashboard has recent patients widget</manual>
  </verify>
  <done>GlobalSearch component renders in SiteHeader with Command+Popover pattern. Recent patients Zustand store persists to localStorage. Dashboard shows recent patients widget. Patients and Appointments sidebar nav items are active (no longer disabled). Patient and scheduling i18n files created for both en and vi with proper Vietnamese diacritics.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` succeeds
2. FullCalendar packages importable: `import FullCalendar from '@fullcalendar/react'` compiles
3. Command, Popover, Tabs, DatePicker wrappers follow established re-export pattern
4. GlobalSearch integrated in SiteHeader
5. Recent patients store uses Zustand persist middleware
6. Dashboard has recent patients widget
7. Sidebar "Patients" and "Appointments" items link to correct routes
8. patient.json and scheduling.json exist in both en/ and vi/ with proper diacritics
</verification>

<success_criteria>
- All 5 FullCalendar packages + cmdk + radix primitives installed
- 4 new shadcn wrapper components match existing wrapper pattern
- GlobalSearch shows recent patients on focus and search results on typing
- Dashboard widget displays recent patients from localStorage
- i18n translations complete for patient and scheduling domains in both languages
</success_criteria>

<output>
After completion, create `.planning/phases/02-patient-management-scheduling/02-03-SUMMARY.md`
</output>
