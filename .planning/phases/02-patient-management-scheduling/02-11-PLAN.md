---
phase: 02-patient-management-scheduling
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Patient/Patient.Infrastructure/Repositories/PatientRepository.cs
  - backend/src/Modules/Scheduling/Scheduling.Application/Features/ApproveSelfBooking.cs
  - backend/src/Modules/Scheduling/Scheduling.Application/Features/BookAppointment.cs
  - backend/src/Modules/Scheduling/Scheduling.Domain/Entities/ClinicSchedule.cs
  - backend/src/Shared/Shared.Presentation/ResultExtensions.cs
  - backend/src/Modules/Patient/Patient.Application/Features/RegisterPatient.cs
  - backend/tests/Patient.Unit.Tests/**
  - backend/tests/Scheduling.Unit.Tests/**
autonomous: true
requirements: [PAT-02, SCH-01, SCH-05, PAT-01]
gap_closure: true

must_haves:
  truths:
    - "Searching '0001' matches patient code 'GK-2026-0001' via substring"
    - "Searching '6543' matches phone '0987654321' via substring"
    - "Approving a self-booking with UTC time for a Vietnam afternoon slot succeeds"
    - "Booking an appointment with UTC time for a Vietnam afternoon slot succeeds"
    - "Backend validation errors return structured field-level error dictionary"
  artifacts:
    - path: "backend/src/Modules/Patient/Patient.Infrastructure/Repositories/PatientRepository.cs"
      provides: "Substring search for PatientCode and Phone"
      contains: "PatientCode.Contains"
    - path: "backend/src/Modules/Scheduling/Scheduling.Application/Features/ApproveSelfBooking.cs"
      provides: "UTC to Asia/Ho_Chi_Minh timezone conversion before schedule validation"
      contains: "Asia/Ho_Chi_Minh"
    - path: "backend/src/Modules/Scheduling/Scheduling.Application/Features/BookAppointment.cs"
      provides: "UTC to Asia/Ho_Chi_Minh timezone conversion before schedule validation"
      contains: "Asia/Ho_Chi_Minh"
    - path: "backend/src/Shared/Shared.Presentation/ResultExtensions.cs"
      provides: "Structured validation errors with field names in RFC 7807 errors dictionary"
      contains: "errors"
  key_links:
    - from: "PatientRepository.cs"
      to: "SQL Server EF Core LINQ"
      via: "Contains() for substring matching"
      pattern: "p\\.PatientCode\\.Contains"
    - from: "ApproveSelfBooking.cs"
      to: "ClinicSchedule.IsWithinHours"
      via: "Vietnam local TimeOfDay passed to schedule check"
      pattern: "TimeZoneInfo.ConvertTimeFromUtc"
---

<objective>
Fix three critical backend bugs: substring search for patient code/phone, UTC-to-Vietnam timezone conversion for scheduling, and structured validation error responses.

Purpose: Tests 3/4 (search), 9/13 (timezone 400 error), and 2 (validation errors) all fail due to backend logic issues. These are independent backend fixes that unblock multiple frontend gaps.
Output: Passing unit tests for all three fixes, backend API returning correct results.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/uat-t3-t4-search-substring-match.md
@.planning/debug/uat-tests-9-10-13.md
@.planning/debug/uat-test2-patient-registration-medical.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD - Fix substring search and timezone conversion</name>
  <files>
    backend/tests/Patient.Unit.Tests/Features/SearchPatientsTests.cs
    backend/tests/Patient.Unit.Tests/Features/GetPatientListTests.cs
    backend/tests/Scheduling.Unit.Tests/Features/BookAppointmentTests.cs
    backend/tests/Scheduling.Unit.Tests/Features/ApproveSelfBookingTests.cs
    backend/src/Modules/Patient/Patient.Infrastructure/Repositories/PatientRepository.cs
    backend/src/Modules/Scheduling/Scheduling.Application/Features/ApproveSelfBooking.cs
    backend/src/Modules/Scheduling/Scheduling.Application/Features/BookAppointment.cs
    backend/src/Modules/Scheduling/Scheduling.Domain/Entities/ClinicSchedule.cs
  </files>
  <action>
    Apply TDD (red-green-refactor) per CLAUDE.md requirement.

    **RED phase - Write failing tests first:**

    1. In Patient.Unit.Tests, add/update tests for SearchAsync and GetPagedAsync:
       - Test: searching "0001" matches patient with code "GK-2026-0001" (substring match)
       - Test: searching "6543" matches patient with phone "0987654321" (substring match)
       - Test: searching "GK-2026-0001" still matches (full match)
       - Test: searching "098" still matches phone "0987654321" (prefix still works)
       Note: If repository tests require EF Core InMemory or mock, follow the established test patterns in the project (check existing Patient.Unit.Tests structure). If the existing tests mock IPatientRepository at the handler level, add handler-level tests that verify the search term is passed through correctly, and additionally create integration-style assertions about the expected LINQ behavior.

    2. In Scheduling.Unit.Tests, add/update tests for BookAppointment and ApproveSelfBooking:
       - Test: booking at UTC 07:00 (= 14:00 Vietnam) with schedule 13:00-20:00 succeeds (currently fails with 400)
       - Test: booking at UTC 23:00 (= 06:00 Vietnam next day) fails (outside hours)
       - Test: approving self-booking at UTC 07:00 (= 14:00 Vietnam) with schedule 13:00-20:00 succeeds
       Follow existing test patterns (check how handlers are tested - likely via direct static Handle() calls with mocked repositories).

    Run tests to confirm they fail (RED).

    **GREEN phase - Implement fixes:**

    1. **PatientRepository.cs** - Fix SearchAsync (line 41-42) and GetPagedAsync (line 69-70):
       - Change `p.PatientCode == term` to `p.PatientCode.Contains(term)` in BOTH methods
       - Change `p.Phone.StartsWith(term)` to `p.Phone.Contains(term)` in BOTH methods

    2. **Timezone fix** - In both ApproveSelfBooking.cs and BookAppointment.cs:
       - Before extracting TimeOfDay and DayOfWeek from command.StartTime, convert from UTC to Asia/Ho_Chi_Minh:
         ```csharp
         var vietnamTz = TimeZoneInfo.FindSystemTimeZoneById("SE Asia Standard Time");
         // Note: On Windows, use "SE Asia Standard Time"; on Linux, use "Asia/Ho_Chi_Minh"
         // Use the cross-platform approach:
         var vietnamTz = TimeZoneInfo.FindSystemTimeZoneById(
             OperatingSystem.IsWindows() ? "SE Asia Standard Time" : "Asia/Ho_Chi_Minh");
         var localStart = TimeZoneInfo.ConvertTimeFromUtc(command.StartTime, vietnamTz);
         var localEnd = TimeZoneInfo.ConvertTimeFromUtc(endTime, vietnamTz);
         ```
       - Use localStart.DayOfWeek for GetForDayAsync
       - Use localStart.TimeOfDay and localEnd.TimeOfDay for IsWithinHours
       - Keep command.StartTime (UTC) for the actual appointment storage and overlap check (DB stores UTC)

    Run tests to confirm they pass (GREEN).
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet test --filter "SearchPatient|GetPatientList|BookAppointment|ApproveSelfBooking" --no-restore -v q</automated>
    <manual>All new tests pass, existing tests still pass</manual>
  </verify>
  <done>
    - Substring search returns results for partial patient code and phone matches
    - UTC times are correctly converted to Vietnam local time before schedule validation
    - All new and existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: TDD - Structured validation error responses</name>
  <files>
    backend/src/Shared/Shared.Presentation/ResultExtensions.cs
    backend/src/Shared/Shared.Domain/Error.cs
    backend/src/Modules/Patient/Patient.Application/Features/RegisterPatient.cs
    backend/src/Modules/Scheduling/Scheduling.Application/Features/BookAppointment.cs
    backend/src/Modules/Scheduling/Scheduling.Application/Features/ApproveSelfBooking.cs
    backend/tests/Shared.Unit.Tests/** (or appropriate test project)
  </files>
  <action>
    Apply TDD (red-green-refactor) per CLAUDE.md requirement. This creates a reusable structured validation error pattern for the entire backend.

    **RED phase - Write failing tests:**

    Write tests that verify:
    - When FluentValidation returns errors with PropertyName, the HTTP response includes an "errors" dictionary mapping field names to error messages
    - The Problem Details response for validation errors has structure: `{ title: "Validation Error", status: 400, errors: { "FullName": ["Full name is required."], "Phone": ["Phone must be a valid..."] } }`

    **GREEN phase - Implement:**

    1. **Error.cs** - Add a new factory method or extend the existing Error type to carry structured validation errors:
       - Add a dictionary property or create a new ValidationError subclass/record:
         ```csharp
         public Dictionary<string, string[]>? ValidationErrors { get; init; }

         public static Error ValidationWithDetails(Dictionary<string, string[]> errors)
             => new("Error.Validation", "One or more validation errors occurred.", ValidationErrors: errors);
         ```
       - Keep the existing Error.Validation(string) for backward compat

    2. **ResultExtensions.cs** - Update the validation error mapping in MapError():
       - When error.ValidationErrors is not null, return Results.ValidationProblem(error.ValidationErrors) which produces standard RFC 7807 format with "errors" dictionary
       - When error.ValidationErrors is null, fall through to existing Results.Problem behavior
       - This ensures existing non-field-level errors still work

    3. **Handler updates** - In RegisterPatient.cs, BookAppointment.cs, ApproveSelfBooking.cs (and all handlers that do FluentValidation):
       - Replace the flat string join with structured errors:
         ```csharp
         if (!validationResult.IsValid)
         {
             var errors = validationResult.Errors
                 .GroupBy(e => e.PropertyName)
                 .ToDictionary(
                     g => g.Key,
                     g => g.Select(e => e.ErrorMessage).ToArray());
             return Result<Guid>.Failure(Error.ValidationWithDetails(errors));
         }
         ```
       - Search for ALL handlers in the codebase that use the `string.Join("; ", validationResult.Errors.Select(e => e.ErrorMessage))` pattern and update them all to use the structured approach
       - Use grep to find: `string.Join.*validationResult.Errors` across the backend

    Run tests to confirm they pass (GREEN).
  </action>
  <verify>
    <automated>cd D:/projects/ganka/backend && dotnet test --no-restore -v q</automated>
    <manual>All tests pass including new validation error structure tests</manual>
  </verify>
  <done>
    - All FluentValidation failures return RFC 7807 response with "errors" dictionary containing field-level messages
    - Existing error handling for non-validation errors unchanged
    - Pattern is reusable across all modules
  </done>
</task>

</tasks>

<verification>
- `dotnet test` passes for all backend test projects
- Manual API test: POST /api/patients with invalid phone returns `{ errors: { "Phone": ["Phone must be..."] } }`
- Manual API test: GET /api/patients?search=0001 returns patient with code GK-2026-0001
</verification>

<success_criteria>
- Patient search returns substring matches for patient code and phone
- Scheduling endpoints accept UTC times and validate against Vietnam local hours
- All validation errors return structured field-level error dictionaries
- 80%+ code coverage maintained per CLAUDE.md requirement
</success_criteria>

<output>
After completion, create `.planning/phases/02-patient-management-scheduling/02-11-SUMMARY.md`
</output>
