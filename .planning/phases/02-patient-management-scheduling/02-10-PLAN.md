---
phase: 02-patient-management-scheduling
plan: 10
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Patient/Patient.Domain/Enums/FieldRequirementContext.cs
  - backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs
  - backend/src/Modules/Patient/Patient.Application/Features/ValidatePatientFields.cs
  - backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs
  - backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs
  - backend/tests/Patient.Unit.Tests/PatientFieldValidatorTests.cs
  - backend/tests/Patient.Unit.Tests/Patient.Unit.Tests.csproj
  - frontend/src/features/patient/components/PatientOverviewTab.tsx
  - frontend/src/features/patient/components/PatientFieldWarning.tsx
  - frontend/src/features/booking/components/BookingForm.tsx
  - frontend/public/locales/en/patient.json
  - frontend/public/locales/vi/patient.json
autonomous: true
gap_closure: true
requirements:
  - PAT-03
  - PAT-01
  - PAT-02
  - PAT-04
  - PAT-05
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-04
  - SCH-05
  - SCH-06

must_haves:
  truths:
    - "PatientFieldValidator.Validate returns errors when Address/CCCD are null for Referral context"
    - "PatientFieldValidator.Validate returns errors when Address/CCCD are null for LegalExport context"
    - "PatientFieldValidator.Validate returns NO errors when Address/CCCD are null for Registration context"
    - "PatientFieldValidator.Validate returns NO errors when Address/CCCD are present for any context"
    - "GET /api/patients/{id}/field-validation returns missing field warnings for patients without Address/CCCD"
    - "Patient profile Overview tab shows a warning indicator when Address or CCCD is empty"
    - "BookingForm.tsx Input fields have no redundant placeholder props that duplicate their labels"
  artifacts:
    - path: "backend/src/Modules/Patient/Patient.Domain/Enums/FieldRequirementContext.cs"
      provides: "Enum defining contexts where field requirements differ"
      contains: "FieldRequirementContext"
    - path: "backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs"
      provides: "Domain service that validates patient fields by context"
      contains: "PatientFieldValidator"
    - path: "backend/tests/Patient.Unit.Tests/PatientFieldValidatorTests.cs"
      provides: "TDD tests for PatientFieldValidator"
      contains: "PatientFieldValidatorTests"
    - path: "frontend/src/features/patient/components/PatientFieldWarning.tsx"
      provides: "Warning component for missing mandatory fields"
      contains: "PatientFieldWarning"
  key_links:
    - from: "backend/src/Modules/Patient/Patient.Application/Features/ValidatePatientFields.cs"
      to: "backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs"
      via: "Handler calls domain service"
      pattern: "PatientFieldValidator\\.Validate"
    - from: "frontend/src/features/patient/components/PatientOverviewTab.tsx"
      to: "frontend/src/features/patient/components/PatientFieldWarning.tsx"
      via: "Component composition"
      pattern: "PatientFieldWarning"
---

<objective>
Implement PAT-03 hardcoded conditional field enforcement infrastructure and clean up BookingForm placeholder redundancy.

Purpose: PAT-03 requires Address/CCCD to be optional at registration but required for downstream workflows (referrals, legal export). Since those workflows don't exist yet, this plan builds the validation infrastructure (domain service, endpoint, UI warnings) so it's ready when Phase 3+ features consume it. Also resolves the minor BookingForm placeholder cleanup gap.

Output: PatientFieldValidator domain service with TDD tests, a validation endpoint, frontend warning indicators on patient profile, and clean BookingForm without redundant placeholders.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-management-scheduling/02-01-SUMMARY.md
@.planning/phases/02-patient-management-scheduling/02-07-SUMMARY.md
@backend/src/Modules/Patient/Patient.Domain/Entities/Patient.cs
@backend/src/Modules/Patient/Patient.Application/Features/RegisterPatient.cs
@backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientDto.cs
@frontend/src/features/patient/components/PatientOverviewTab.tsx
@frontend/src/features/booking/components/BookingForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — PatientFieldValidator domain service + validation endpoint</name>
  <files>
    backend/src/Modules/Patient/Patient.Domain/Enums/FieldRequirementContext.cs
    backend/src/Modules/Patient/Patient.Domain/Services/PatientFieldValidator.cs
    backend/src/Modules/Patient/Patient.Application/Features/ValidatePatientFields.cs
    backend/src/Modules/Patient/Patient.Contracts/Dtos/PatientFieldValidationResult.cs
    backend/src/Modules/Patient/Patient.Presentation/PatientApiEndpoints.cs
    backend/tests/Patient.Unit.Tests/PatientFieldValidatorTests.cs
    backend/tests/Patient.Unit.Tests/Patient.Unit.Tests.csproj
  </files>
  <action>
    Follow TDD red-green-refactor cycle.

    **RED phase — Write failing tests first:**

    1. Create `backend/tests/Patient.Unit.Tests/Patient.Unit.Tests.csproj` test project:
       - Reference `Patient.Domain`, `Patient.Contracts`
       - Use xUnit, FluentAssertions, NSubstitute (same pattern as Auth.Unit.Tests)

    2. Create `PatientFieldValidatorTests.cs` with these test cases:
       - `Validate_Registration_NullAddressAndCccd_ReturnsNoErrors` — Registration context allows nulls
       - `Validate_Referral_NullAddress_ReturnsAddressRequired` — Referral context requires Address
       - `Validate_Referral_NullCccd_ReturnsCccdRequired` — Referral context requires CCCD
       - `Validate_Referral_BothPresent_ReturnsNoErrors` — Referral context passes when both present
       - `Validate_LegalExport_NullAddress_ReturnsAddressRequired` — LegalExport context requires Address
       - `Validate_LegalExport_NullCccd_ReturnsCccdRequired` — LegalExport context requires CCCD
       - `Validate_LegalExport_BothPresent_ReturnsNoErrors` — LegalExport passes when both present
       - `Validate_Registration_BothPresent_ReturnsNoErrors` — Registration always passes

    3. Run tests — they MUST fail (classes don't exist yet). Commit: `test(02-10): add failing tests for PatientFieldValidator`

    **GREEN phase — Implement to pass:**

    4. Create `FieldRequirementContext.cs` enum in `Patient.Domain/Enums/`:
       ```csharp
       public enum FieldRequirementContext
       {
           Registration,    // Fields optional
           Referral,        // Address + CCCD required
           LegalExport,     // Address + CCCD required
           SoYTeReporting   // Address + CCCD required
       }
       ```

    5. Create `PatientFieldValidator.cs` in `Patient.Domain/Services/`:
       - Static class with `Validate(string? address, string? cccd, FieldRequirementContext context)` method
       - Returns `PatientFieldValidationResult` (list of field names that are missing/required)
       - Logic: For `Registration` context, always return empty (no errors). For `Referral`, `LegalExport`, `SoYTeReporting`, return errors for null/empty Address and/or CCCD.
       - This is a pure domain service with no dependencies — easy to test.

    6. Create `PatientFieldValidationResult.cs` in `Patient.Contracts/Dtos/`:
       ```csharp
       public record PatientFieldValidationResult(
           bool IsValid,
           List<MissingFieldInfo> MissingFields);

       public record MissingFieldInfo(
           string FieldName,
           string RequiredForContext);
       ```

    7. Run tests — they MUST pass. Commit: `feat(02-10): implement PatientFieldValidator domain service`

    **Application + Presentation wiring:**

    8. Create `ValidatePatientFields.cs` in `Patient.Application/Features/`:
       - `ValidatePatientFieldsQuery` record with `Guid PatientId`
       - `ValidatePatientFieldsHandler` that:
         a. Loads patient by ID via `IPatientRepository`
         b. Calls `PatientFieldValidator.Validate(patient.Address, patient.Cccd, FieldRequirementContext.Referral)` (use Referral as the "strictest common" context — the UI just needs to know if fields will be needed)
         c. Returns `PatientFieldValidationResult`

    9. Add endpoint to `PatientApiEndpoints.cs`:
       - `GET /api/patients/{id:guid}/field-validation` → returns `PatientFieldValidationResult`
       - Place it in the existing patient route group, requires authorization

    10. Commit: `feat(02-10): add field validation endpoint`
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Patient.Unit.Tests/ --verbosity normal</automated>
    <manual>Verify PatientFieldValidator has 8 passing tests covering all context combinations</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - PatientFieldValidator domain service exists with pure validation logic
    - 8 unit tests pass covering Registration (no enforcement), Referral (requires both), LegalExport (requires both), and all-present scenarios
    - GET /api/patients/{id}/field-validation endpoint wired and returns missing field info
    - FieldRequirementContext enum ready for downstream Phase 3+ features to consume
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend warning indicators + BookingForm placeholder cleanup</name>
  <files>
    frontend/src/features/patient/components/PatientFieldWarning.tsx
    frontend/src/features/patient/components/PatientOverviewTab.tsx
    frontend/src/features/patient/api/patient-api.ts
    frontend/src/features/booking/components/BookingForm.tsx
    frontend/public/locales/en/patient.json
    frontend/public/locales/vi/patient.json
  </files>
  <action>
    **Part A: Patient field warning on profile**

    1. Add React Query hook in `patient-api.ts`:
       - `usePatientFieldValidation(patientId: string)` that calls `GET /api/patients/{patientId}/field-validation`
       - Returns `PatientFieldValidationResult` type (define matching TS interface)

    2. Create `PatientFieldWarning.tsx`:
       - A small warning banner component that displays when `isValid === false`
       - Uses shadcn Alert component (variant: "warning" or custom amber styling)
       - Shows message like: "This patient is missing Address and/or CCCD. These fields are required for referrals and legal exports."
       - Lists each missing field by name
       - Include an "Update Profile" button/link that scrolls to or activates the edit mode on the Overview tab
       - Use i18n for all text (add keys to both locale files)

    3. Add i18n keys to `en/patient.json` and `vi/patient.json`:
       - `"fieldWarning.title"`: "Missing Required Information" / "Thiếu thông tin bắt buộc"
       - `"fieldWarning.description"`: "The following fields are needed for referrals and legal exports:" / "Các trường sau cần thiết cho giấy chuyển viện và xuất dữ liệu pháp lý:"
       - `"fieldWarning.address"`: "Address" / "Địa chỉ"
       - `"fieldWarning.cccd"`: "CCCD (Citizen ID)" / "CCCD (Căn cước công dân)"
       - `"fieldWarning.updateProfile"`: "Update Profile" / "Cập nhật hồ sơ"

    4. Integrate `PatientFieldWarning` into `PatientOverviewTab.tsx`:
       - Import `usePatientFieldValidation` and `PatientFieldWarning`
       - Call the hook with the patient ID
       - Render `<PatientFieldWarning>` above the overview content (between tabs and the info section) when the validation result shows missing fields
       - The warning should only show in read mode (not while editing)
       - When "Update Profile" is clicked, switch to edit mode

    **Part B: BookingForm placeholder cleanup**

    5. In `BookingForm.tsx`:
       - Verify Input fields for patientName, phone, email, notes do NOT have placeholder props (they should already be clean from plan 02-08). If any still have placeholder props, remove them.
       - The DatePicker `placeholder` prop on line 197 is acceptable per VERIFICATION.md (standard shadcn DatePicker pattern) — leave it. However, per CLAUDE.md "Only add placeholder where it makes sense", since the field already has a FieldLabel with the same text, remove the `placeholder={t("selfBooking.preferredDate")}` from the DatePicker to be consistent.
       - Select component `<SelectValue placeholder=...>` is standard shadcn usage for showing hint text in an empty dropdown — leave these as they serve a functional purpose (showing what to select).

    6. Commit: `feat(02-10): add patient field warning indicators and clean BookingForm placeholders`
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
    <manual>Verify PatientFieldWarning renders on patient profile when Address/CCCD are empty. Verify BookingForm has no redundant placeholder props on Input fields.</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
    - PatientFieldWarning component shows amber warning banner on patient profile when Address or CCCD is missing
    - Warning lists specific missing fields with i18n support (Vietnamese + English)
    - "Update Profile" button switches to edit mode
    - Warning hidden during edit mode and when all fields are present
    - BookingForm Input fields have no redundant placeholders; DatePicker placeholder removed (label suffices)
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `cd backend && dotnet test tests/Patient.Unit.Tests/ --verbosity normal` — all 8 PatientFieldValidator tests pass
2. `cd backend && dotnet build` — solution compiles with no errors
3. `cd frontend && npx tsc --noEmit` — TypeScript compiles with no errors
4. Grep BookingForm.tsx Input elements for `placeholder` props — none found on Input components
5. PatientFieldWarning component exists and is imported in PatientOverviewTab
6. FieldRequirementContext enum has Registration, Referral, LegalExport, SoYTeReporting values
7. GET /api/patients/{id}/field-validation endpoint exists in PatientApiEndpoints.cs
</verification>

<success_criteria>
- PAT-03 infrastructure is complete: domain service validates fields by context, endpoint exposes validation, UI warns about missing fields
- Downstream Phase 3+ features (referrals, legal export) can import FieldRequirementContext and call PatientFieldValidator.Validate() without any additional infrastructure work
- BookingForm has no redundant placeholder text on labeled Input fields
- All unit tests pass with >= 80% coverage on PatientFieldValidator
</success_criteria>

<output>
After completion, create `.planning/phases/02-patient-management-scheduling/02-10-SUMMARY.md`
</output>
