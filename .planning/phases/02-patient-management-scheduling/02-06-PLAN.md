---
phase: 02-patient-management-scheduling
plan: 06
type: execute
wave: 3
depends_on:
  - "02-04"
  - "02-05"
files_modified: []
autonomous: false
requirements:
  - PAT-01
  - PAT-02
  - PAT-03
  - PAT-04
  - PAT-05
  - SCH-01
  - SCH-02
  - SCH-03
  - SCH-04
  - SCH-05
  - SCH-06

must_haves:
  truths:
    - "Staff can register a medical patient and see GK-YYYY-NNNN code"
    - "Staff can register a walk-in pharmacy customer with name+phone only"
    - "Staff can search patients by name (diacritics-insensitive), phone, or code"
    - "Staff can view patient profile with allergies displayed as alert"
    - "Staff can book an appointment on the calendar and system prevents double-booking"
    - "Public booking page works without login"
    - "Pending self-bookings appear for staff approval"
  artifacts: []
  key_links: []
---

<objective>
End-to-end integration verification of Patient Management and Scheduling features. Start both backend and frontend, run through all critical user flows, and verify the complete Phase 02 functionality.

Purpose: Ensures all backend APIs, frontend components, and cross-module integrations work together correctly before marking Phase 02 as complete.
Output: Verified working system with all PAT and SCH requirements functional.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-management-scheduling/02-CONTEXT.md
@.planning/phases/02-patient-management-scheduling/02-01-SUMMARY.md
@.planning/phases/02-patient-management-scheduling/02-02-SUMMARY.md
@.planning/phases/02-patient-management-scheduling/02-03-SUMMARY.md
@.planning/phases/02-patient-management-scheduling/02-04-SUMMARY.md
@.planning/phases/02-patient-management-scheduling/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start backend and frontend, run automated API verification</name>
  <files></files>
  <action>
    1. Kill any processes on ports 5255 and 3000:
       ```bash
       # Kill processes if ports are in use
       ```

    2. Start backend on port 5255:
       ```bash
       cd backend/src/Bootstrapper && dotnet run --urls "https://localhost:5255" &
       ```
       Wait for "Application started" in logs.

    3. Run EF Core migrations for Patient and Scheduling schemas:
       - Verify PatientDbContext and SchedulingDbContext migrations applied
       - Verify allergy catalog seeded
       - Verify appointment types seeded (NewPatient, FollowUp, Treatment, OrthoK)
       - Verify clinic schedule seeded (Mon closed, Tue-Fri 13-20, Sat-Sun 8-12)

    4. Start frontend on port 3000:
       ```bash
       cd frontend && npm run dev -- --port 3000 &
       ```

    5. Test Patient API endpoints via curl:
       - POST /api/patients (Medical patient) -> verify 201 with patient ID
       - POST /api/patients (Walk-in customer) -> verify 201
       - GET /api/patients/search?term=test -> verify results
       - GET /api/patients/{id} -> verify patient with allergies
       - POST /api/patients/{id}/allergies -> verify allergy added
       - POST /api/patients/{id}/deactivate -> verify soft delete

    6. Test Scheduling API endpoints via curl:
       - GET /api/appointments/types -> verify 4 types returned
       - GET /api/appointments/schedule -> verify 7 days returned
       - POST /api/appointments (valid slot) -> verify 201
       - POST /api/appointments (overlapping slot) -> verify 409
       - POST /api/appointments (outside hours) -> verify 400
       - PUT /api/appointments/{id}/cancel -> verify cancellation
       - POST /api/public/booking -> verify 201 (no auth token)
       - GET /api/public/booking/status/{ref} -> verify Pending status

    7. If any API test fails, fix the issue immediately.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://localhost:5255/api/appointments/types -k</automated>
    <manual>All API endpoints return expected status codes</manual>
  </verify>
  <done>Backend starts, migrations applied, seed data present. All Patient API endpoints return correct responses. All Scheduling API endpoints enforce double-booking and operating hours. Public booking endpoint works without auth. Frontend dev server starts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Patient Management and Scheduling UI</name>
  <files></files>
  <action>
    Human verifies all Patient Management and Scheduling UI flows end-to-end.
    Backend running at https://localhost:5255, Frontend at http://localhost:3000.

    Complete system built:
    - Patient registration (medical + walk-in) with GK-YYYY-NNNN auto-ID
    - Patient search (global search bar + patient list page)
    - Patient profile with Overview, Allergies, Appointments tabs
    - Allergy management with autocomplete and severity
    - Appointment calendar per doctor with FullCalendar
    - Booking via calendar slot click and full form
    - Drag-drop rescheduling, appointment cancellation with reason
    - Public self-booking page at /book, booking status check at /book/status
    - Pending bookings panel with approve/reject

    Verification steps:
    1. Patient Registration (PAT-01, PAT-02): Register Medical patient with all fields -> verify GK-YYYY-NNNN code. Register Walk-in with name+phone only.
    2. Patient Search (PAT-04): Global search bar (Ctrl+K) -> type 2+ chars -> verify diacritics-insensitive results. Verify recent patients on focus.
    3. Patient Profile (PAT-01, PAT-03, PAT-05): Open profile -> verify tabs (Overview, Allergies, Appointments) -> Edit mode -> DOB+age display.
    4. Allergy Management (PAT-05): Add allergy with autocomplete + free-text + severity -> verify alert banner -> remove allergy.
    5. Appointment Calendar (SCH-01, SCH-04, SCH-05, SCH-06): Select doctor -> verify business hours (Tue-Fri 13-20, Sat-Sun 8-12, Mon closed) -> click slot -> book -> verify color.
    6. Double-Booking (SCH-03): Book same slot again -> verify error.
    7. Drag-Drop Rescheduling: Drag appointment to new slot.
    8. Cancellation: Cancel with required reason.
    9. Public Self-Booking (SCH-02): /book in incognito -> branded form -> submit -> reference number -> /book/status shows Pending.
    10. Pending Approval: Approve self-booking on /appointments -> verify on calendar -> /book/status shows Approved.
    11. Language Toggle: English/Vietnamese for all patient/scheduling text.
  </action>
  <verify>Human confirms all 11 verification steps pass</verify>
  <done>All PAT-01 through PAT-05 and SCH-01 through SCH-06 requirements verified working end-to-end by human inspection</done>
</task>

</tasks>

<verification>
All PAT-01 through PAT-05 and SCH-01 through SCH-06 requirements verified via both automated API tests and human UI verification.
</verification>

<success_criteria>
- Human verifies all 11 requirement flows work end-to-end
- No critical bugs in patient registration, search, or profile management
- Calendar renders correctly with business hours and color coding
- Double-booking prevention works both in UI and at API level
- Public booking page accessible without authentication
- Self-booking approval workflow complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-patient-management-scheduling/02-06-SUMMARY.md`
</output>
