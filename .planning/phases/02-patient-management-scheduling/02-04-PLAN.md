---
phase: 02-patient-management-scheduling
plan: 04
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-03"
files_modified:
  - frontend/src/features/patient/api/patient-api.ts
  - frontend/src/features/patient/hooks/usePatients.ts
  - frontend/src/features/patient/components/PatientRegistrationForm.tsx
  - frontend/src/features/patient/components/PatientListPage.tsx
  - frontend/src/features/patient/components/PatientTable.tsx
  - frontend/src/features/patient/components/PatientProfilePage.tsx
  - frontend/src/features/patient/components/PatientProfileHeader.tsx
  - frontend/src/features/patient/components/PatientOverviewTab.tsx
  - frontend/src/features/patient/components/PatientAllergyTab.tsx
  - frontend/src/features/patient/components/PatientAppointmentTab.tsx
  - frontend/src/features/patient/components/AllergyForm.tsx
  - frontend/src/features/patient/components/AllergyAlert.tsx
  - frontend/src/app/routes/_authenticated/patients/index.tsx
  - frontend/src/app/routes/_authenticated/patients/$patientId.tsx
autonomous: true
requirements:
  - PAT-01
  - PAT-02
  - PAT-03
  - PAT-04
  - PAT-05

must_haves:
  truths:
    - "Staff can register a medical patient with name, phone, DOB, gender and see the auto-generated GK-YYYY-NNNN code on the profile page"
    - "Staff can register a walk-in pharmacy customer with just name and phone"
    - "Staff can search for patients by name (diacritics-insensitive), phone, or patient code"
    - "Staff can view a paginated patient list with gender, allergy, and date filters"
    - "Staff can view a patient profile with Overview, Allergies, and Appointments tabs"
    - "Staff can add allergies with name (autocomplete + free-text) and severity"
    - "Allergy alert banner shows on patient profile when patient has allergies"
    - "Staff can deactivate/reactivate a patient (soft delete)"
  artifacts:
    - path: "frontend/src/features/patient/components/PatientRegistrationForm.tsx"
      provides: "Patient registration with medical vs walk-in toggle"
      contains: "PatientRegistrationForm"
    - path: "frontend/src/features/patient/components/PatientListPage.tsx"
      provides: "Paginated patient list with filters"
      contains: "PatientListPage"
    - path: "frontend/src/features/patient/components/PatientProfilePage.tsx"
      provides: "Tabbed patient profile"
      contains: "PatientProfilePage"
    - path: "frontend/src/features/patient/components/AllergyAlert.tsx"
      provides: "Reusable allergy alert banner for downstream workflows"
      contains: "AllergyAlert"
    - path: "frontend/src/app/routes/_authenticated/patients/index.tsx"
      provides: "Patient list route"
    - path: "frontend/src/app/routes/_authenticated/patients/$patientId.tsx"
      provides: "Patient profile route"
  key_links:
    - from: "frontend/src/features/patient/components/PatientRegistrationForm.tsx"
      to: "frontend/src/features/patient/api/patient-api.ts"
      via: "React Query mutation for POST /api/patients"
      pattern: "useRegisterPatient"
    - from: "frontend/src/features/patient/components/PatientListPage.tsx"
      to: "frontend/src/features/patient/api/patient-api.ts"
      via: "React Query query for GET /api/patients"
      pattern: "usePatientList"
    - from: "frontend/src/app/routes/_authenticated/patients/$patientId.tsx"
      to: "frontend/src/features/patient/components/PatientProfilePage.tsx"
      via: "Route renders profile component"
      pattern: "PatientProfilePage"
---

<objective>
Build the complete Patient frontend: registration form (medical + walk-in), paginated patient list with DataTable and filters, patient profile page with tabbed layout (Overview, Allergies, Appointments), allergy management with autocomplete, and allergy alert banner for downstream reuse.

Purpose: Provides the user-facing patient management interface allowing staff to register, search, view, edit, and manage patient profiles with structured allergy tracking.
Output: Working patient registration, list, profile, and allergy management pages connected to the Patient module API.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-management-scheduling/02-CONTEXT.md
@.planning/phases/02-patient-management-scheduling/02-RESEARCH.md
@.planning/phases/02-patient-management-scheduling/02-01-SUMMARY.md
@.planning/phases/02-patient-management-scheduling/02-03-SUMMARY.md
@.planning/phases/01.2-refactor-frontend-to-shadcn-ui-with-tanstack-start-file-based-routing/01.2-04-SUMMARY.md

# Key existing files for UI patterns:
@frontend/src/features/admin/components/UserFormDialog.tsx
@frontend/src/features/admin/components/UserTable.tsx
@frontend/src/features/admin/api/admin-api.ts
@frontend/src/shared/components/DataTable.tsx
@frontend/src/shared/components/Field.tsx
@frontend/src/shared/components/Button.tsx
@frontend/src/shared/components/Dialog.tsx
@frontend/src/shared/components/Tabs.tsx
@frontend/src/shared/components/Command.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patient API hooks, registration form, and patient list page with DataTable</name>
  <files>
    frontend/src/features/patient/api/patient-api.ts
    frontend/src/features/patient/hooks/usePatients.ts
    frontend/src/features/patient/components/PatientRegistrationForm.tsx
    frontend/src/features/patient/components/PatientListPage.tsx
    frontend/src/features/patient/components/PatientTable.tsx
    frontend/src/app/routes/_authenticated/patients/index.tsx
  </files>
  <action>
    1. Create `patient-api.ts` with React Query hooks (follow admin-api.ts pattern):
       - `useRegisterPatient()` - mutation for POST /api/patients
       - `useUpdatePatient()` - mutation for PUT /api/patients/{id}
       - `useDeactivatePatient()` - mutation for POST /api/patients/{id}/deactivate
       - `useReactivatePatient()` - mutation for POST /api/patients/{id}/reactivate
       - `usePatientById(patientId)` - query for GET /api/patients/{id}
       - `usePatientList(params)` - query for GET /api/patients with pagination + filters (page, pageSize, gender, hasAllergies, dateFrom, dateTo)
       - `useAddAllergy()` - mutation for POST /api/patients/{id}/allergies
       - `useRemoveAllergy()` - mutation for DELETE /api/patients/{id}/allergies/{allergyId}
       - `useUploadPatientPhoto()` - mutation for POST /api/patients/{id}/photo
       - All hooks use openapi-fetch client from existing api-client.ts with proper typing
       - Mutations invalidate relevant queries on success

    2. Create `usePatients.ts` with additional derived hooks:
       - `usePatientSearch` hook (if not already created in Plan 03): debounced search using GET /api/patients/search
       - `useRecentPatients` for GET /api/patients/recent

    3. Create `PatientRegistrationForm.tsx`:
       - **Claude decides: single form with toggle** between Medical and Walk-in patient types
       - Toggle at top of form: two radio-style buttons/segments for "Medical Patient" / "Walk-in Customer"
       - **Medical mode:** Full Name (required), Phone (required, Vietnamese format validation), Date of Birth (required, uses DatePicker), Gender (required, select dropdown), Address (optional), CCCD (optional)
       - **Walk-in mode:** Full Name (required), Phone (required) only
       - **Allergy section (optional, both modes):** Expandable accordion/collapsible section. For each allergy: Name input (Command autocomplete from catalog + free-text entry) + Severity select (Mild/Moderate/Severe). Add/remove allergy rows.
       - Use react-hook-form + zod (schema defined inside component for i18n access, per established pattern)
       - Use Field+Controller pattern for all form fields (per 01.2-04 established pattern)
       - On submit: call useRegisterPatient mutation
       - On success: navigate to `/patients/{newPatientId}` (redirect to patient profile per user decision -- no special toast for ID)
       - Use Dialog component if registration is triggered from a dialog context, otherwise full-page form at `/patients?action=register`
       - Actually: render as a Dialog triggered by "Register Patient" button on the patient list page

    4. Create `PatientTable.tsx`:
       - Reuse generic DataTable component from Phase 01.2-04
       - Columns: Patient Code, Full Name, Phone, Type (badge: Medical/Walk-in), Gender, Allergies count (badge if > 0), Status (Active/Inactive), Actions (view profile)
       - Configure useReactTable with manual pagination (server-side)
       - Row click navigates to patient profile
       - Use @tabler/icons-react for icons

    5. Create `PatientListPage.tsx`:
       - Page header: "Patients" title + "Register Patient" button (opens registration dialog)
       - Filter bar using Field+FieldLabel pattern (per 01.2-04):
         - Gender filter (Select: All/Male/Female/Other)
         - Has Allergies filter (Select: All/Yes/No)
         - Date range filter (two DatePicker components for from/to)
       - PatientTable below filters
       - Pagination controls below table
       - Use usePatientList hook with filter state

    6. Create route `patients/index.tsx`:
       - File-based route under `_authenticated/patients/`
       - Renders PatientListPage component
       - Uses createFileRoute from @tanstack/react-router
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
    <manual>Check that PatientListPage renders DataTable with filters and registration dialog</manual>
  </verify>
  <done>Patient API hooks created for all CRUD operations. Registration form supports Medical/Walk-in toggle with inline allergy entry. Patient list page uses DataTable with server-side pagination and gender/allergy/date filters. Route `/patients` renders the patient list.</done>
</task>

<task type="auto">
  <name>Task 2: Patient profile page with tabs, allergy management, and allergy alert component</name>
  <files>
    frontend/src/features/patient/components/PatientProfilePage.tsx
    frontend/src/features/patient/components/PatientProfileHeader.tsx
    frontend/src/features/patient/components/PatientOverviewTab.tsx
    frontend/src/features/patient/components/PatientAllergyTab.tsx
    frontend/src/features/patient/components/PatientAppointmentTab.tsx
    frontend/src/features/patient/components/AllergyForm.tsx
    frontend/src/features/patient/components/AllergyAlert.tsx
    frontend/src/app/routes/_authenticated/patients/$patientId.tsx
  </files>
  <action>
    1. Create `PatientProfileHeader.tsx`:
       - Left: Patient photo (uploaded image or initials avatar fallback -- use first letters of name, colored circle)
       - Center: Patient name (large), Patient code (GK-YYYY-NNNN), Patient type badge (Medical/Walk-in)
       - Right: DOB with calculated age (e.g., "15/03/1985 (40 tuổi)"), Gender, Phone
       - Below: AllergyAlert component (if patient has allergies)
       - Actions: "Edit" button (toggles form mode), "Deactivate"/"Reactivate" button with confirmation dialog
       - Photo upload: click on photo placeholder opens file picker, calls useUploadPatientPhoto

    2. Create `AllergyAlert.tsx` (reusable for downstream workflows):
       - **Claude decides: persistent banner** at top of patient-related pages (prioritizes patient safety)
       - If patient has allergies: show a yellow/orange banner with warning icon (IconAlertTriangle)
       - Lists allergy names with severity badges (red for Severe, orange for Moderate, gray for Mild)
       - Compact mode: just shows "X allergies" with a tooltip listing them (for use in compact contexts like prescribing)
       - Props: `allergies: AllergyDto[]`, `compact?: boolean`
       - This component is designed to be importable by future phases (Phase 5: prescribing)

    3. Create `PatientOverviewTab.tsx`:
       - Displays patient details in a read-only card layout
       - Two columns: Personal Info (name, phone, DOB, gender, address, CCCD) and System Info (patient code, type, created date, status)
       - "Edit" mode: switches to inline form with Field+Controller pattern, Save/Cancel buttons
       - Uses useUpdatePatient mutation for saves

    4. Create `PatientAllergyTab.tsx`:
       - Lists existing allergies in a table/card view: Name, Severity (color-coded badge), Remove button (with confirmation)
       - "Add Allergy" button opens AllergyForm dialog
       - Uses useAddAllergy and useRemoveAllergy mutations
       - Shows "No known allergies" empty state

    5. Create `AllergyForm.tsx`:
       - Dialog with: Allergy Name input (uses Command/Popover for autocomplete from catalog + allows free-text), Severity select (Mild/Moderate/Severe)
       - Autocomplete fetches from allergy catalog API (or uses a static predefined list initially)
       - react-hook-form + zod validation
       - On submit: calls useAddAllergy mutation

    6. Create `PatientAppointmentTab.tsx`:
       - **Claude decides: single tab showing all appointments** (upcoming + past, separated by section headers)
       - Upcoming appointments at top, past appointments below
       - Each appointment shows: date, time, doctor name, type (color badge), status
       - "Book Appointment" button navigates to /appointments
       - Uses API from scheduling module (GET /api/appointments/by-patient/{patientId})
       - Note: This tab depends on Scheduling API (Plan 02). For now, create the component structure with the API call. If the API isn't available yet at runtime, show an empty state gracefully.

    7. Create `PatientProfilePage.tsx`:
       - Composes: PatientProfileHeader + Tabs (Overview, Allergies, Appointments)
       - Uses usePatientById to load patient data
       - Loading state with skeleton
       - Error state (patient not found)
       - Updates recentPatientsStore when patient is loaded

    8. Create route `patients/$patientId.tsx`:
       - Dynamic route under `_authenticated/patients/`
       - Extracts patientId from URL params
       - Renders PatientProfilePage with patientId prop
       - Uses createFileRoute from @tanstack/react-router
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
    <manual>Check that patient profile renders with tabs, allergy alert banner, and edit mode</manual>
  </verify>
  <done>Patient profile page renders with tabbed layout (Overview, Allergies, Appointments). Profile header shows patient info with photo/avatar, DOB+age, and action buttons. AllergyAlert banner shows when patient has allergies (persistent yellow/orange warning). Allergy tab supports add/remove with autocomplete. Overview tab supports inline editing. Appointments tab shows appointment history.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` succeeds
2. `/patients` route renders patient list with DataTable, filters, and registration dialog
3. `/patients/{id}` route renders profile with Overview, Allergies, Appointments tabs
4. Registration form toggles between Medical (full fields) and Walk-in (name+phone)
5. AllergyAlert renders as persistent banner with severity-coded badges
6. Allergy entry uses Command autocomplete + free-text
7. Profile edit mode uses Field+Controller pattern
8. Recent patients store updated when viewing a profile
9. All text uses i18n translations (patient namespace)
</verification>

<success_criteria>
- Complete patient management UI: register, list, search, profile, edit, allergies
- Medical vs Walk-in patient distinction in registration form
- Allergy alert component designed for downstream reuse (Phase 5 prescribing)
- DataTable reused from Phase 01.2-04 for patient list
- Field+Controller pattern used for all form fields
- All UI text bilingual (Vietnamese + English)
</success_criteria>

<output>
After completion, create `.planning/phases/02-patient-management-scheduling/02-04-SUMMARY.md`
</output>
