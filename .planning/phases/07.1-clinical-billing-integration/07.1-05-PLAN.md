---
phase: 07.1-clinical-billing-integration
plan: 05
type: execute
wave: 3
depends_on: ["07.1-03", "07.1-04"]
files_modified:
  - backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCreated.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCancelled.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleDrugDispensed.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleOtcSaleCompleted.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleGlassesOrderCreated.cs
  - backend/src/Modules/Billing/Billing.Application/Features/HandleTreatmentSessionCompleted.cs
  - backend/src/Modules/Billing/Billing.Application/IoC.cs
  - backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs
  - backend/src/Bootstrapper/Program.cs
  - frontend/package.json
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05
  - INT-06
  - INT-08
must_haves:
  truths:
    - "All 6 event handlers send SignalR notifications after processing"
    - "CancelVisit API endpoint exists in Clinical presentation layer"
    - "Billing module references all source module Contracts projects for integration events"
    - "Frontend has @microsoft/signalr dependency installed"
    - "Full backend compiles and all tests pass"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCreated.cs"
      provides: "Event handler with SignalR notification"
      contains: "NotifyInvoiceCreatedAsync"
    - path: "backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs"
      provides: "CancelVisit API endpoint"
      contains: "cancel"
  key_links:
    - from: "HandleVisitCreatedHandler"
      to: "IBillingNotificationService"
      via: "DI injection"
      pattern: "IBillingNotificationService.*NotifyInvoiceCreatedAsync"
    - from: "HandleDrugDispensedHandler"
      to: "IBillingNotificationService"
      via: "DI injection"
      pattern: "NotifyLineItemAddedAsync"
---

<objective>
Wire SignalR notifications into all 6 billing event handlers, add CancelVisit API endpoint, ensure cross-module project references compile, install @microsoft/signalr on frontend, and verify full integration builds.

Purpose: Connects the event handlers (Plan 03) to the SignalR infrastructure (Plan 04) so cashier dashboard receives real-time updates. Also wires remaining API endpoints and ensures all module references are correct.
Output: Complete backend integration that compiles and passes all tests, frontend SignalR dependency ready.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-clinical-billing-integration/07.1-CONTEXT.md
@.planning/phases/07.1-clinical-billing-integration/07.1-03-SUMMARY.md
@.planning/phases/07.1-clinical-billing-integration/07.1-04-SUMMARY.md

<interfaces>
From Plan 04 (IBillingNotificationService):
```csharp
public interface IBillingNotificationService
{
    Task NotifyInvoiceCreatedAsync(Guid invoiceId, string invoiceNumber, Guid? visitId, string patientName, decimal totalAmount, CancellationToken ct);
    Task NotifyLineItemAddedAsync(Guid invoiceId, string invoiceNumber, string description, decimal amount, string department, CancellationToken ct);
    Task NotifyInvoiceVoidedAsync(Guid invoiceId, string invoiceNumber, CancellationToken ct);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire SignalR into event handlers + cross-module references</name>
  <files>
    backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCreated.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleVisitCancelled.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleDrugDispensed.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleOtcSaleCompleted.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleGlassesOrderCreated.cs,
    backend/src/Modules/Billing/Billing.Application/Features/HandleTreatmentSessionCompleted.cs,
    backend/src/Modules/Billing/Billing.Application/Billing.Application.csproj
  </files>
  <action>
    1. Add `IBillingNotificationService` parameter to all 6 event handler Handle methods.
    2. In HandleVisitCreated: after SaveChanges, call `NotifyInvoiceCreatedAsync(invoice.Id, invoice.InvoiceNumber, @event.VisitId, @event.PatientName, invoice.TotalAmount, ct)`.
    3. In HandleVisitCancelled: after Void + SaveChanges, call `NotifyInvoiceVoidedAsync(invoice.Id, invoice.InvoiceNumber, ct)`.
    4. In HandleDrugDispensed: after adding all line items + SaveChanges, call `NotifyLineItemAddedAsync` for each drug item (or a single notification with total). Use department "Pharmacy".
    5. In HandleOtcSaleCompleted: after creating invoice + SaveChanges, call `NotifyInvoiceCreatedAsync`.
    6. In HandleGlassesOrderCreated: after adding line items + SaveChanges, call `NotifyLineItemAddedAsync` with department "Optical".
    7. In HandleTreatmentSessionCompleted: after adding line item + SaveChanges, call `NotifyLineItemAddedAsync` with department "Treatment".
    8. Wrap all notification calls in try/catch -- log warning on failure, never throw.
    9. Ensure Billing.Application.csproj has ProjectReferences to:
       - Clinical.Contracts (for VisitCreatedIntegrationEvent, VisitCancelledIntegrationEvent)
       - Pharmacy.Contracts (for DrugDispensedIntegrationEvent, OtcSaleCompletedIntegrationEvent)
       - Optical.Contracts (for GlassesOrderCreatedIntegrationEvent)
       - Treatment.Contracts (for TreatmentSessionCompletedIntegrationEvent -- may already exist)
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Modules/Billing/Billing.Application --no-restore</automated>
  </verify>
  <done>
    All 6 event handlers inject and call IBillingNotificationService, all Contracts references resolve, Application project builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: CancelVisit endpoint + frontend SignalR install + full build verify</name>
  <files>
    backend/src/Modules/Clinical/Clinical.Presentation/ClinicalApiEndpoints.cs,
    backend/src/Bootstrapper/Program.cs,
    frontend/package.json
  </files>
  <action>
    1. Add CancelVisit API endpoint to ClinicalApiEndpoints.cs:
       - POST /api/clinical/visits/{visitId}/cancel -> dispatches CancelVisitCommand
       - Follow existing endpoint pattern (WolverineFx Minimal API or manual dispatch)
    2. Verify Bootstrapper Program.cs correctly wires:
       - Billing.Application assembly registered in Wolverine discovery (so integration event handlers are found)
       - Clinical.Contracts, Pharmacy.Contracts, Optical.Contracts assemblies discoverable by Wolverine for integration event routing
    3. Install @microsoft/signalr on frontend:
       - `cd frontend && npm install @microsoft/signalr`
    4. Run full backend build and test suite:
       - `dotnet build backend/src/Bootstrapper`
       - `dotnet test backend/tests --no-restore -v q`
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Bootstrapper --no-restore && dotnet test tests --no-restore -v q</automated>
  </verify>
  <done>
    CancelVisit endpoint registered, all Wolverine handler assemblies discoverable, @microsoft/signalr installed, full backend builds and all tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd backend && dotnet build src/Bootstrapper --no-restore && dotnet test tests --no-restore -v q
```
</verification>

<success_criteria>
- All 6 event handlers push SignalR notifications (fire-and-forget with error logging)
- CancelVisit endpoint at POST /api/clinical/visits/{visitId}/cancel
- Billing.Application has project references to all 4 source module Contracts
- Wolverine discovers all integration event handlers
- @microsoft/signalr installed in frontend
- Full backend compiles, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-clinical-billing-integration/07.1-05-SUMMARY.md`
</output>
