---
phase: 07.1-clinical-billing-integration
plan: 07
type: execute
wave: 5
depends_on: ["07.1-05", "07.1-06"]
files_modified: []
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05
  - INT-06
  - INT-07
  - INT-08
must_haves:
  truths:
    - "Creating a visit auto-generates a Draft invoice with consultation fee"
    - "Cancelling a Draft visit voids its invoice"
    - "Dispensing drugs adds per-drug line items to the visit invoice"
    - "Creating an OTC sale generates a standalone invoice"
    - "Creating a glasses order adds frame/lens line items to the visit invoice"
    - "Recording a treatment session adds a session fee line item"
    - "Cashier dashboard shows charges appearing in real-time via SignalR"
    - "Service catalog admin page allows CRUD of service items"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the clinical-billing integration. Run all backend tests, start the application, and verify the complete event flow from source modules through to the cashier dashboard.

Purpose: Confirm that all integration events fire correctly, invoices auto-populate, and the cashier sees real-time updates.
Output: Verified working integration, ready for UAT.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-clinical-billing-integration/07.1-CONTEXT.md
@.planning/phases/07.1-clinical-billing-integration/07.1-05-SUMMARY.md
@.planning/phases/07.1-clinical-billing-integration/07.1-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated test suite verification + migration apply</name>
  <files>
    backend/tests/Billing.Unit.Tests/Features/IntegrationEventHandlerTests.cs,
    backend/tests/Billing.Unit.Tests/Features/ServiceCatalogHandlerTests.cs,
    backend/tests/Clinical.Unit.Tests/Features/CancelVisitTests.cs,
    backend/tests/Clinical.Unit.Tests/Features/CreateVisitEventTests.cs,
    backend/tests/Pharmacy.Unit.Tests/Features/DispensingEventTests.cs,
    backend/tests/Pharmacy.Unit.Tests/Features/OtcSaleEventTests.cs,
    backend/tests/Optical.Unit.Tests/Features/GlassesOrderEventTests.cs
  </files>
  <action>
    1. Run full backend test suite: `cd backend && dotnet test tests --no-restore -v q`
    2. Verify all billing integration event handler tests pass
    3. Verify all source module event tests pass (Clinical, Pharmacy, Optical)
    4. Verify service catalog CRUD tests pass
    5. Run frontend TypeScript check: `cd frontend && npx tsc --noEmit`
    6. Run frontend build: `cd frontend && npm run build`
    7. Apply any pending EF Core migrations: run the backend briefly to apply migrations
    8. Fix any issues found
  </action>
  <verify>
    <automated>cd backend && dotnet test tests --no-restore -v q && cd ../frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    All backend tests pass, frontend compiles, migrations applied successfully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete clinical-billing integration:
    1. Visit creation auto-creates Draft invoice with consultation fee from service catalog
    2. Drug dispensing adds per-drug line items to visit invoice
    3. OTC sales create standalone invoices
    4. Glasses orders add frame/lens line items to visit invoice
    5. Treatment sessions add session fee line items
    6. Visit cancellation voids invoice
    7. Cashier dashboard receives real-time SignalR updates
    8. Service catalog admin page with CRUD
  </what-built>
  <how-to-verify>
    Backend runs on port 5255, frontend on port 3000.
    Login: Admin@ganka28.com / Admin@123456

    **Test 1: Service Catalog**
    1. Navigate to /billing/service-catalog
    2. Verify "Consultation" (150,000 VND) and "Follow-up" (100,000 VND) are seeded
    3. Click "Add Service", create a test service
    4. Edit the test service price, verify it saves

    **Test 2: Visit -> Invoice Auto-Creation**
    1. Open cashier dashboard (/billing) in one browser tab
    2. In another tab, create a new visit for a patient
    3. Switch to cashier tab -- verify a Draft invoice appeared with consultation fee
    4. Verify the invoice shows the correct patient name and consultation amount

    **Test 3: Drug Dispensing -> Invoice Line Items**
    1. With the visit from Test 2, dispense drugs from the pharmacy
    2. Check cashier dashboard -- verify drug line items appeared on the invoice

    **Test 4: Visit Cancellation -> Invoice Voided**
    1. Create another test visit (to avoid affecting Test 2 data)
    2. Cancel the visit via API or UI
    3. Verify the invoice status changes to "Voided" on cashier dashboard

    **Test 5: SignalR Real-Time Updates**
    1. Verify the cashier dashboard shows a green connection indicator
    2. Verify charges appear without page refresh (real-time push)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Full backend test suite green + human verification of end-to-end flows.
</verification>

<success_criteria>
- All backend tests pass (dotnet test)
- Frontend compiles and builds
- Visit creation auto-generates invoice with consultation fee
- Drug dispensing adds line items to invoice
- Visit cancellation voids invoice
- Cashier dashboard shows real-time SignalR updates
- Service catalog admin page works
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-clinical-billing-integration/07.1-07-SUMMARY.md`
</output>
