---
phase: 07.1-clinical-billing-integration
plan: 01c
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Optical/Optical.Domain/Events/GlassesOrderCreatedEvent.cs
  - backend/src/Modules/Optical/Optical.Contracts/IntegrationEvents/GlassesOrderCreatedIntegrationEvent.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/PublishGlassesOrderCreatedIntegrationEvent.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/CreateGlassesOrder.cs
  - backend/tests/Optical.Unit.Tests/Features/GlassesOrderEventTests.cs
autonomous: true
requirements:
  - INT-05
must_haves:
  truths:
    - "Glasses order creation raises GlassesOrderCreatedEvent with frame/lens line items and prices"
    - "Domain event produces GlassesOrderCreatedIntegrationEvent via cascading handler"
  artifacts:
    - path: "backend/src/Modules/Optical/Optical.Contracts/IntegrationEvents/GlassesOrderCreatedIntegrationEvent.cs"
      provides: "Cross-module integration event for glasses orders"
      contains: "GlassesOrderCreatedIntegrationEvent"
    - path: "backend/src/Modules/Optical/Optical.Application/Features/Orders/PublishGlassesOrderCreatedIntegrationEvent.cs"
      provides: "Cascading handler for glasses order event"
      contains: "PublishGlassesOrderCreatedIntegrationEventHandler"
  key_links:
    - from: "CreateGlassesOrderHandler"
      to: "GlassesOrderCreatedEvent"
      via: "Domain event raised on order creation"
      pattern: "GlassesOrderCreatedEvent"
    - from: "GlassesOrderCreatedEvent"
      to: "GlassesOrderCreatedIntegrationEvent"
      via: "Wolverine cascading handler"
      pattern: "PublishGlassesOrderCreatedIntegrationEventHandler"
---

<objective>
Add domain event and integration event contract to the Optical module for glasses order creation.

Purpose: Optical events trigger frame/lens line items on the visit invoice in Billing.
Output: Domain event on GlassesOrder aggregate, integration event in Optical.Contracts, cascading publish handler, and unit tests.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-clinical-billing-integration/07.1-CONTEXT.md
@.planning/phases/07.1-clinical-billing-integration/07.1-RESEARCH.md

<interfaces>
<!-- Established cascading handler pattern to replicate -->
From backend/src/Modules/Treatment/Treatment.Application/Features/PublishSessionCompletedIntegrationEvent.cs:
```csharp
public static class PublishSessionCompletedIntegrationEventHandler
{
    public static TreatmentSessionCompletedIntegrationEvent Handle(
        TreatmentSessionCompletedEvent domainEvent)
    {
        return new TreatmentSessionCompletedIntegrationEvent(...);
    }
}
```

From Optical.Domain/Events/ (GlassesOrderStatusChangedEvent already exists -- pattern reference):
```csharp
// Optical.Domain/Events/ directory already exists
```

From Shared.Domain/IDomainEvent.cs:
```csharp
public interface IDomainEvent
{
    Guid EventId { get; }
    DateTime OccurredAt { get; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Optical domain event + integration event + publish handler</name>
  <files>
    backend/src/Modules/Optical/Optical.Domain/Events/GlassesOrderCreatedEvent.cs,
    backend/src/Modules/Optical/Optical.Contracts/IntegrationEvents/GlassesOrderCreatedIntegrationEvent.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Orders/PublishGlassesOrderCreatedIntegrationEvent.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Orders/CreateGlassesOrder.cs,
    backend/tests/Optical.Unit.Tests/Features/GlassesOrderEventTests.cs
  </files>
  <behavior>
    - Test: CreateGlassesOrder handler raises GlassesOrderCreatedEvent after successful order creation (contains OrderId, VisitId, PatientId, PatientName, list of OrderLineDto with description, unitPrice)
    - Test: PublishGlassesOrderCreatedIntegrationEventHandler converts domain event to integration event correctly
  </behavior>
  <action>
    1. Create `GlassesOrderCreatedEvent` record implementing IDomainEvent with: OrderId (Guid), VisitId (Guid?), PatientId (Guid), PatientName (string), Items (List of OrderLineDto with Description, DescriptionVi, UnitPrice, Quantity). Note: Optical.Domain/Events/ directory already exists with GlassesOrderStatusChangedEvent.cs.
    2. Create integration event in Optical.Contracts/IntegrationEvents/:
       - `GlassesOrderCreatedIntegrationEvent(Guid OrderId, Guid? VisitId, Guid PatientId, string PatientName, List<OrderLineDto> Items)`
       - Include nested `OrderLineDto(string Description, string DescriptionVi, decimal UnitPrice, int Quantity)` record.
    3. Modify `CreateGlassesOrderHandler`: after successful order creation, raise GlassesOrderCreatedEvent on the GlassesOrder aggregate. Read existing CreateGlassesOrder.cs to understand current flow. Add AddDomainEvent call in GlassesOrder.Create() or in handler.
    4. Create cascading publish handler following Treatment pattern exactly.
    5. Write TDD tests first.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Optical.Unit.Tests --filter "GlassesOrderEvent" --no-restore -v q</automated>
  </verify>
  <done>
    CreateGlassesOrder raises GlassesOrderCreatedEvent with frame+lens line items, cascading handler produces GlassesOrderCreatedIntegrationEvent, all tests pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd backend && dotnet test tests/Optical.Unit.Tests --filter "GlassesOrderEvent" --no-restore -v q
```
</verification>

<success_criteria>
- CreateGlassesOrder raises GlassesOrderCreatedEvent with frame+lens line items
- GlassesOrderCreatedIntegrationEvent exists in Optical.Contracts
- Cascading publish handler follows Treatment module pattern
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-clinical-billing-integration/07.1-01c-SUMMARY.md`
</output>
