---
phase: 07.1-clinical-billing-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/Modules/Clinical/Clinical.Domain/Events/VisitCreatedEvent.cs
  - backend/src/Modules/Clinical/Clinical.Domain/Events/VisitCancelledEvent.cs
  - backend/src/Modules/Clinical/Clinical.Domain/Enums/VisitStatus.cs
  - backend/src/Modules/Clinical/Clinical.Domain/Entities/Visit.cs
  - backend/src/Modules/Clinical/Clinical.Contracts/IntegrationEvents/VisitCreatedIntegrationEvent.cs
  - backend/src/Modules/Clinical/Clinical.Contracts/IntegrationEvents/VisitCancelledIntegrationEvent.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/PublishVisitCreatedIntegrationEvent.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/PublishVisitCancelledIntegrationEvent.cs
  - backend/src/Modules/Clinical/Clinical.Application/Features/CancelVisit.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Events/DrugDispensedEvent.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Domain/Events/OtcSaleCompletedEvent.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Contracts/IntegrationEvents/DrugDispensedIntegrationEvent.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Contracts/IntegrationEvents/OtcSaleCompletedIntegrationEvent.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Dispensing/PublishDrugDispensedIntegrationEvent.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/PublishOtcSaleCompletedIntegrationEvent.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Dispensing/DispenseDrugs.cs
  - backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/CreateOtcSale.cs
  - backend/src/Modules/Optical/Optical.Domain/Events/GlassesOrderCreatedEvent.cs
  - backend/src/Modules/Optical/Optical.Contracts/IntegrationEvents/GlassesOrderCreatedIntegrationEvent.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/PublishGlassesOrderCreatedIntegrationEvent.cs
  - backend/src/Modules/Optical/Optical.Application/Features/Orders/CreateGlassesOrder.cs
  - backend/tests/Clinical.Unit.Tests/Features/CancelVisitTests.cs
  - backend/tests/Clinical.Unit.Tests/Features/CreateVisitEventTests.cs
  - backend/tests/Pharmacy.Unit.Tests/Features/DispensingEventTests.cs
  - backend/tests/Pharmacy.Unit.Tests/Features/OtcSaleEventTests.cs
  - backend/tests/Optical.Unit.Tests/Features/GlassesOrderEventTests.cs
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05
  - INT-06
must_haves:
  truths:
    - "Visit creation raises VisitCreatedEvent domain event with patient/doctor/visit IDs"
    - "Visit cancellation raises VisitCancelledEvent and sets status to Cancelled"
    - "Drug dispensing raises DrugDispensedEvent with drug details and prices"
    - "OTC sale raises OtcSaleCompletedEvent with sale items and customer info"
    - "Glasses order creation raises GlassesOrderCreatedEvent with frame/lens line items"
    - "All domain events produce corresponding integration events via cascading handlers"
  artifacts:
    - path: "backend/src/Modules/Clinical/Clinical.Domain/Events/VisitCreatedEvent.cs"
      provides: "Domain event for visit creation"
      contains: "VisitCreatedEvent"
    - path: "backend/src/Modules/Clinical/Clinical.Application/Features/CancelVisit.cs"
      provides: "CancelVisit command handler"
      contains: "CancelVisitHandler"
    - path: "backend/src/Modules/Pharmacy/Pharmacy.Contracts/IntegrationEvents/DrugDispensedIntegrationEvent.cs"
      provides: "Cross-module integration event for drug dispensing"
      contains: "DrugDispensedIntegrationEvent"
    - path: "backend/src/Modules/Optical/Optical.Contracts/IntegrationEvents/GlassesOrderCreatedIntegrationEvent.cs"
      provides: "Cross-module integration event for glasses orders"
      contains: "GlassesOrderCreatedIntegrationEvent"
  key_links:
    - from: "Visit.Create()"
      to: "VisitCreatedEvent"
      via: "AddDomainEvent in factory method"
      pattern: "AddDomainEvent.*VisitCreatedEvent"
    - from: "Visit.Cancel()"
      to: "VisitCancelledEvent"
      via: "AddDomainEvent in Cancel method"
      pattern: "AddDomainEvent.*VisitCancelledEvent"
    - from: "VisitCreatedEvent"
      to: "VisitCreatedIntegrationEvent"
      via: "Wolverine cascading handler"
      pattern: "PublishVisitCreatedIntegrationEventHandler"
---

<objective>
Add domain events and integration event contracts to Clinical, Pharmacy, and Optical source modules so that chargeable actions (visit creation, drug dispensing, OTC sale, glasses order) publish cross-module integration events for Billing consumption. Also add CancelVisit feature to Clinical module.

Purpose: This is the "source side" of the event-driven billing integration. Without these events, the Billing module has nothing to consume. Follows the established Treatment module cascading handler pattern.
Output: Domain events on aggregates, integration event records in Contracts projects, cascading publish handlers, CancelVisit feature, and unit tests for all.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-clinical-billing-integration/07.1-CONTEXT.md
@.planning/phases/07.1-clinical-billing-integration/07.1-RESEARCH.md

<interfaces>
<!-- Established cascading handler pattern to replicate -->
From backend/src/Modules/Treatment/Treatment.Application/Features/PublishSessionCompletedIntegrationEvent.cs:
```csharp
public static class PublishSessionCompletedIntegrationEventHandler
{
    public static TreatmentSessionCompletedIntegrationEvent Handle(
        TreatmentSessionCompletedEvent domainEvent)
    {
        return new TreatmentSessionCompletedIntegrationEvent(
            PackageId: domainEvent.PackageId,
            SessionId: domainEvent.SessionId,
            PatientId: domainEvent.PatientId,
            TreatmentType: (int)domainEvent.TreatmentType,
            Consumables: domainEvent.Consumables
                .Select(c => new TreatmentSessionCompletedIntegrationEvent.ConsumableUsageDto(
                    c.ConsumableItemId, c.Quantity))
                .ToList());
    }
}
```

From backend/src/Modules/Clinical/Clinical.Domain/Entities/Visit.cs:
```csharp
public class Visit : AggregateRoot, IAuditable
{
    public Guid PatientId { get; private set; }
    public string PatientName { get; private set; }
    public Guid DoctorId { get; private set; }
    public string DoctorName { get; private set; }
    public VisitStatus Status { get; private set; }
    // ... Visit.Create() factory does NOT currently raise any domain event
}
```

From backend/src/Modules/Clinical/Clinical.Domain/Enums/VisitStatus.cs:
```csharp
public enum VisitStatus { Draft = 0, Signed = 1, Amended = 2 }
// Need to add: Cancelled = 3
```

From backend/src/Modules/Shared/Shared.Domain/IDomainEvent.cs (pattern):
```csharp
public interface IDomainEvent
{
    Guid EventId { get; }
    DateTime OccurredAt { get; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clinical domain events + CancelVisit feature + integration events</name>
  <files>
    backend/src/Modules/Clinical/Clinical.Domain/Events/VisitCreatedEvent.cs,
    backend/src/Modules/Clinical/Clinical.Domain/Events/VisitCancelledEvent.cs,
    backend/src/Modules/Clinical/Clinical.Domain/Enums/VisitStatus.cs,
    backend/src/Modules/Clinical/Clinical.Domain/Entities/Visit.cs,
    backend/src/Modules/Clinical/Clinical.Contracts/IntegrationEvents/VisitCreatedIntegrationEvent.cs,
    backend/src/Modules/Clinical/Clinical.Contracts/IntegrationEvents/VisitCancelledIntegrationEvent.cs,
    backend/src/Modules/Clinical/Clinical.Application/Features/PublishVisitCreatedIntegrationEvent.cs,
    backend/src/Modules/Clinical/Clinical.Application/Features/PublishVisitCancelledIntegrationEvent.cs,
    backend/src/Modules/Clinical/Clinical.Application/Features/CancelVisit.cs,
    backend/tests/Clinical.Unit.Tests/Features/CancelVisitTests.cs,
    backend/tests/Clinical.Unit.Tests/Features/CreateVisitEventTests.cs
  </files>
  <behavior>
    - Test: Visit.Create() raises VisitCreatedEvent with correct PatientId, PatientName, DoctorId, VisitId
    - Test: Visit.Cancel() on Draft visit sets Status=Cancelled and raises VisitCancelledEvent
    - Test: Visit.Cancel() on Signed visit throws InvalidOperationException (only Draft visits can be cancelled)
    - Test: Visit.Cancel() on already Cancelled visit throws InvalidOperationException
    - Test: CancelVisitHandler calls Cancel() on visit and saves
    - Test: CancelVisitHandler returns failure when visit not found
    - Test: PublishVisitCreatedIntegrationEventHandler converts domain event to integration event with correct fields
    - Test: PublishVisitCancelledIntegrationEventHandler converts domain event to integration event with correct fields
  </behavior>
  <action>
    1. Add `Cancelled = 3` to VisitStatus enum.
    2. Create `VisitCreatedEvent` record implementing IDomainEvent with fields: VisitId, PatientId, PatientName, DoctorId, DoctorName. Create Events/ directory under Clinical.Domain.
    3. Create `VisitCancelledEvent` record implementing IDomainEvent with field: VisitId.
    4. Modify `Visit.Create()` to call `AddDomainEvent(new VisitCreatedEvent(...))` before returning.
    5. Add `Visit.Cancel()` method: guard that Status is Draft (throw InvalidOperationException otherwise), set Status = Cancelled, call SetUpdatedAt(), AddDomainEvent(new VisitCancelledEvent(Id)).
    6. Create integration event records in Clinical.Contracts/IntegrationEvents/:
       - `VisitCreatedIntegrationEvent(Guid VisitId, Guid PatientId, string PatientName)`
       - `VisitCancelledIntegrationEvent(Guid VisitId)`
    7. Create cascading handlers in Clinical.Application/Features/ following Treatment pattern exactly:
       - `PublishVisitCreatedIntegrationEventHandler.Handle(VisitCreatedEvent) -> VisitCreatedIntegrationEvent`
       - `PublishVisitCancelledIntegrationEventHandler.Handle(VisitCancelledEvent) -> VisitCancelledIntegrationEvent`
    8. Create `CancelVisitCommand(Guid VisitId)` + `CancelVisitHandler` in Clinical.Application/Features/CancelVisit.cs. Handler loads visit from IVisitRepository, calls Cancel(), saves via IUnitOfWork.
    9. Write TDD tests first (red), then implement (green).
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Clinical.Unit.Tests --filter "CancelVisit|VisitCreated" --no-restore -v q</automated>
  </verify>
  <done>
    Visit.Create() raises VisitCreatedEvent, Visit.Cancel() works for Draft visits only, both events produce integration events via cascading handlers, CancelVisit command handler works, all tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pharmacy domain events + integration events + publish handlers</name>
  <files>
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Events/DrugDispensedEvent.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Domain/Events/OtcSaleCompletedEvent.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Contracts/IntegrationEvents/DrugDispensedIntegrationEvent.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Contracts/IntegrationEvents/OtcSaleCompletedIntegrationEvent.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Dispensing/PublishDrugDispensedIntegrationEvent.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/PublishOtcSaleCompletedIntegrationEvent.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/Dispensing/DispenseDrugs.cs,
    backend/src/Modules/Pharmacy/Pharmacy.Application/Features/OtcSales/CreateOtcSale.cs,
    backend/tests/Pharmacy.Unit.Tests/Features/DispensingEventTests.cs,
    backend/tests/Pharmacy.Unit.Tests/Features/OtcSaleEventTests.cs
  </files>
  <behavior>
    - Test: DispenseDrugs handler raises DrugDispensedEvent after successful dispensing (contains VisitId, PatientId, list of DrugLineDto with drugName, quantity, unitPrice)
    - Test: CreateOtcSale handler raises OtcSaleCompletedEvent after successful OTC sale (contains OtcSaleId, PatientId nullable, CustomerName, list of items)
    - Test: PublishDrugDispensedIntegrationEventHandler converts domain event to integration event correctly
    - Test: PublishOtcSaleCompletedIntegrationEventHandler converts domain event to integration event correctly
  </behavior>
  <action>
    1. Create `Pharmacy.Domain/Events/` directory. Create `DrugDispensedEvent` record implementing IDomainEvent with: VisitId (Guid), PatientId (Guid), PatientName (string), Items (List of DrugLineDto where DrugLineDto has DrugName, DrugNameVi, Quantity, UnitPrice).
    2. Create `OtcSaleCompletedEvent` record implementing IDomainEvent with: OtcSaleId (Guid), PatientId (Guid?), CustomerName (string), Items (same DrugLineDto list).
    3. Create integration events in Pharmacy.Contracts/IntegrationEvents/:
       - `DrugDispensedIntegrationEvent(Guid VisitId, Guid PatientId, string PatientName, List<DrugLineDto> Items)`
       - `OtcSaleCompletedIntegrationEvent(Guid OtcSaleId, Guid? PatientId, string CustomerName, List<DrugLineDto> Items)`
       - Include nested `DrugLineDto(string DrugName, string DrugNameVi, int Quantity, decimal UnitPrice)` record.
    4. Modify `DispenseDrugsHandler`: after successful dispensing and SaveChanges, the dispensing aggregate (or handler directly) should raise DrugDispensedEvent. Read existing DispenseDrugs.cs to understand the current flow. Add domain event to the DispensingRecord aggregate or use IMessageBus.PublishAsync in handler if aggregate pattern doesn't fit.
    5. Modify `CreateOtcSaleHandler`: after successful OTC sale creation and SaveChanges, raise OtcSaleCompletedEvent similarly.
    6. Create cascading publish handlers following Treatment pattern exactly.
    7. Write TDD tests first for event publishing and cascading handlers.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Pharmacy.Unit.Tests --filter "DispensingEvent|OtcSaleEvent" --no-restore -v q</automated>
  </verify>
  <done>
    DispenseDrugs raises DrugDispensedEvent with per-drug line items, CreateOtcSale raises OtcSaleCompletedEvent, both events produce integration events via cascading handlers, all tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Optical domain event + integration event + publish handler</name>
  <files>
    backend/src/Modules/Optical/Optical.Domain/Events/GlassesOrderCreatedEvent.cs,
    backend/src/Modules/Optical/Optical.Contracts/IntegrationEvents/GlassesOrderCreatedIntegrationEvent.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Orders/PublishGlassesOrderCreatedIntegrationEvent.cs,
    backend/src/Modules/Optical/Optical.Application/Features/Orders/CreateGlassesOrder.cs,
    backend/tests/Optical.Unit.Tests/Features/GlassesOrderEventTests.cs
  </files>
  <behavior>
    - Test: CreateGlassesOrder handler raises GlassesOrderCreatedEvent after successful order creation (contains OrderId, VisitId, PatientId, PatientName, list of OrderLineDto with description, unitPrice)
    - Test: PublishGlassesOrderCreatedIntegrationEventHandler converts domain event to integration event correctly
  </behavior>
  <action>
    1. Create `GlassesOrderCreatedEvent` record implementing IDomainEvent with: OrderId (Guid), VisitId (Guid?), PatientId (Guid), PatientName (string), Items (List of OrderLineDto with Description, DescriptionVi, UnitPrice, Quantity). Note: Optical.Domain/Events/ directory already exists with GlassesOrderStatusChangedEvent.cs.
    2. Create integration event in Optical.Contracts/IntegrationEvents/:
       - `GlassesOrderCreatedIntegrationEvent(Guid OrderId, Guid? VisitId, Guid PatientId, string PatientName, List<OrderLineDto> Items)`
       - Include nested `OrderLineDto(string Description, string DescriptionVi, decimal UnitPrice, int Quantity)` record.
    3. Modify `CreateGlassesOrderHandler`: after successful order creation, raise GlassesOrderCreatedEvent on the GlassesOrder aggregate. Read existing CreateGlassesOrder.cs to understand current flow. Add AddDomainEvent call in GlassesOrder.Create() or in handler.
    4. Create cascading publish handler following Treatment pattern exactly.
    5. Write TDD tests first.
  </action>
  <verify>
    <automated>cd backend && dotnet test tests/Optical.Unit.Tests --filter "GlassesOrderEvent" --no-restore -v q</automated>
  </verify>
  <done>
    CreateGlassesOrder raises GlassesOrderCreatedEvent with frame+lens line items, cascading handler produces GlassesOrderCreatedIntegrationEvent, all tests pass.
  </done>
</task>

</tasks>

<verification>
All source module domain events fire correctly and produce integration events:
```bash
cd backend && dotnet test tests/Clinical.Unit.Tests tests/Pharmacy.Unit.Tests tests/Optical.Unit.Tests --no-restore -v q
```
</verification>

<success_criteria>
- Visit.Create() raises VisitCreatedEvent, Visit.Cancel() raises VisitCancelledEvent (Draft only)
- DispenseDrugs raises DrugDispensedEvent, CreateOtcSale raises OtcSaleCompletedEvent
- CreateGlassesOrder raises GlassesOrderCreatedEvent
- All 6 integration event contracts exist in respective .Contracts projects
- All 5 cascading publish handlers follow Treatment module pattern
- CancelVisit command handler works for Draft visits, rejects Signed visits
- All unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-clinical-billing-integration/07.1-01-SUMMARY.md`
</output>
