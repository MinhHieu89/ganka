---
phase: 07.1-clinical-billing-integration
plan: 04
type: execute
wave: 2
depends_on: ["07.1-02"]
files_modified:
  - backend/src/Modules/Billing/Billing.Infrastructure/Hubs/BillingHub.cs
  - backend/src/Modules/Billing/Billing.Application/Interfaces/IBillingNotificationService.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/Services/BillingNotificationService.cs
  - backend/src/Modules/Billing/Billing.Infrastructure/IoC.cs
  - backend/src/Bootstrapper/Program.cs
autonomous: true
requirements:
  - INT-08
must_haves:
  truths:
    - "BillingHub accepts authenticated WebSocket connections"
    - "Cashiers can join/leave 'cashier-dashboard' group"
    - "BillingNotificationService can push InvoiceCreated, LineItemAdded, InvoiceVoided messages to the group"
    - "SignalR hub is mapped at /api/hubs/billing in Bootstrapper"
  artifacts:
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/Hubs/BillingHub.cs"
      provides: "SignalR hub for cashier dashboard"
      contains: "class BillingHub"
    - path: "backend/src/Modules/Billing/Billing.Application/Interfaces/IBillingNotificationService.cs"
      provides: "Notification service interface"
      exports: ["NotifyInvoiceCreatedAsync", "NotifyLineItemAddedAsync", "NotifyInvoiceVoidedAsync"]
    - path: "backend/src/Modules/Billing/Billing.Infrastructure/Services/BillingNotificationService.cs"
      provides: "SignalR-backed notification service"
      contains: "class BillingNotificationService"
  key_links:
    - from: "BillingNotificationService"
      to: "BillingHub"
      via: "IHubContext<BillingHub>"
      pattern: "IHubContext<BillingHub>"
    - from: "Program.cs"
      to: "BillingHub"
      via: "app.MapHub<BillingHub>"
      pattern: "MapHub.*billing"
---

<objective>
Set up SignalR infrastructure for real-time cashier dashboard updates. Create BillingHub with JWT authentication, notification service interface, and Bootstrapper wiring. The event handlers (Plan 03) will call this service after processing events.

Purpose: Cashiers need to see charges accumulating in real-time as patients progress through clinical workflows. SignalR pushes updates instead of polling.
Output: BillingHub, IBillingNotificationService, BillingNotificationService implementation, Bootstrapper wiring.
</objective>

<execution_context>
@C:/Users/minhh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/minhh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-clinical-billing-integration/07.1-CONTEXT.md
@.planning/phases/07.1-clinical-billing-integration/07.1-RESEARCH.md

<interfaces>
From backend/src/Bootstrapper/Program.cs (pattern for service registration):
```csharp
// Existing: app.MapWolverineEndpoints(); and module endpoint mapping
// Need to add: app.MapHub<BillingHub>("/api/hubs/billing");
// Need to add: builder.Services.AddSignalR(); (if not already present)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BillingHub + notification service interface</name>
  <files>
    backend/src/Modules/Billing/Billing.Infrastructure/Hubs/BillingHub.cs,
    backend/src/Modules/Billing/Billing.Application/Interfaces/IBillingNotificationService.cs
  </files>
  <action>
    1. Create `IBillingNotificationService` interface in Billing.Application/Interfaces/:
       - `Task NotifyInvoiceCreatedAsync(Guid invoiceId, string invoiceNumber, Guid? visitId, string patientName, decimal totalAmount, CancellationToken ct)`
       - `Task NotifyLineItemAddedAsync(Guid invoiceId, string invoiceNumber, string description, decimal amount, string department, CancellationToken ct)`
       - `Task NotifyInvoiceVoidedAsync(Guid invoiceId, string invoiceNumber, CancellationToken ct)`
    2. Create `BillingHub` in Billing.Infrastructure/Hubs/:
       - Decorate with `[Authorize]`
       - `JoinCashierDashboard()` method: adds connection to "cashier-dashboard" group
       - `LeaveCashierDashboard()` method: removes connection from group
       - Override `OnDisconnectedAsync` to clean up group membership
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Modules/Billing/Billing.Application && dotnet build src/Modules/Billing/Billing.Infrastructure</automated>
  </verify>
  <done>
    BillingHub compiles with Authorize attribute, IBillingNotificationService interface defined with 3 notification methods.
  </done>
</task>

<task type="auto">
  <name>Task 2: BillingNotificationService + Bootstrapper wiring</name>
  <files>
    backend/src/Modules/Billing/Billing.Infrastructure/Services/BillingNotificationService.cs,
    backend/src/Modules/Billing/Billing.Infrastructure/IoC.cs,
    backend/src/Bootstrapper/Program.cs
  </files>
  <action>
    1. Create `BillingNotificationService` implementing `IBillingNotificationService`:
       - Inject `IHubContext<BillingHub>` (from Microsoft.AspNetCore.SignalR)
       - `NotifyInvoiceCreatedAsync`: sends "InvoiceCreated" to "cashier-dashboard" group with { InvoiceId, InvoiceNumber, VisitId, PatientName, TotalAmount }
       - `NotifyLineItemAddedAsync`: sends "LineItemAdded" to "cashier-dashboard" group with { InvoiceId, InvoiceNumber, Description, Amount, Department }
       - `NotifyInvoiceVoidedAsync`: sends "InvoiceVoided" to "cashier-dashboard" group with { InvoiceId, InvoiceNumber }
       - All methods fire-and-forget approach -- wrap in try/catch, log warning on failure, never throw (SignalR failure should not break invoice processing).
    2. Register in Billing Infrastructure IoC.cs: `services.AddScoped<IBillingNotificationService, BillingNotificationService>()`
    3. Update Bootstrapper Program.cs:
       - Add `builder.Services.AddSignalR()` (check if already present first)
       - Add `app.MapHub<BillingHub>("/api/hubs/billing")` after existing endpoint mappings
       - Ensure CORS policy allows SignalR WebSocket connections from frontend origin
    4. Verify the Billing.Infrastructure.csproj has reference to Microsoft.AspNetCore.SignalR (it's part of the shared framework, but if using IHubContext, may need `<FrameworkReference Include="Microsoft.AspNetCore.App" />` if not already present).
  </action>
  <verify>
    <automated>cd backend && dotnet build src/Bootstrapper && dotnet test tests/Billing.Unit.Tests --no-restore -v q</automated>
  </verify>
  <done>
    BillingNotificationService sends SignalR messages to cashier-dashboard group, registered in DI, BillingHub mapped at /api/hubs/billing, Bootstrapper builds successfully, existing Billing unit tests still pass.
  </done>
</task>

</tasks>

<verification>
```bash
cd backend && dotnet build src/Bootstrapper --no-restore && dotnet test tests/Billing.Unit.Tests --no-restore -v q
```
</verification>

<success_criteria>
- BillingHub with JWT [Authorize], JoinCashierDashboard/LeaveCashierDashboard methods
- IBillingNotificationService with 3 notification methods
- BillingNotificationService uses IHubContext to push to cashier-dashboard group
- SignalR failures logged but don't break event processing (fire-and-forget)
- Hub mapped at /api/hubs/billing in Bootstrapper
- Bootstrapper compiles and runs
- Existing Billing unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-clinical-billing-integration/07.1-04-SUMMARY.md`
</output>
