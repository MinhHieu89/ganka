---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/shared/components/ui/input.tsx
  - frontend/src/shared/components/ui/select.tsx
  - frontend/src/shared/components/ui/textarea.tsx
  - frontend/src/shared/components/NumberInput.tsx
  - frontend/src/shared/components/ui/field.tsx
autonomous: true
must_haves:
  truths:
    - "Input, Select, NumberInput, and Textarea borders turn red when aria-invalid is set"
    - "FieldLabel shows a red asterisk (*) when required prop is true"
    - "All required form fields across the app display the asterisk indicator"
    - "Label text turns red on validation error (already works via Field data-invalid)"
    - "Error message text is red (already works via FieldError)"
  artifacts:
    - path: "frontend/src/shared/components/ui/input.tsx"
      provides: "Input with aria-invalid:border-destructive"
    - path: "frontend/src/shared/components/ui/select.tsx"
      provides: "SelectTrigger with aria-invalid:border-destructive"
    - path: "frontend/src/shared/components/ui/textarea.tsx"
      provides: "Textarea with aria-invalid:border-destructive"
    - path: "frontend/src/shared/components/NumberInput.tsx"
      provides: "NumberInput with aria-invalid:border-destructive"
    - path: "frontend/src/shared/components/ui/field.tsx"
      provides: "FieldLabel with required prop showing asterisk"
  key_links:
    - from: "Field (data-invalid)"
      to: "FieldLabel, FieldError"
      via: "group/field text-destructive inheritance"
    - from: "Controller (aria-invalid)"
      to: "Input/Select/NumberInput/Textarea"
      via: "aria-invalid attribute triggers border-destructive"
---

<objective>
Standardize validation error highlighting and required field indicators across all forms.

Purpose: Consistent UX where validation errors show red label + red input border + red error message, and required fields show an asterisk (*) on the label.
Output: Updated core UI components and all form files with required indicators.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/shared/components/ui/field.tsx
@frontend/src/shared/components/ui/input.tsx
@frontend/src/shared/components/ui/select.tsx
@frontend/src/shared/components/ui/textarea.tsx
@frontend/src/shared/components/NumberInput.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error border styling to all input components and required indicator to FieldLabel</name>
  <files>
    frontend/src/shared/components/ui/input.tsx
    frontend/src/shared/components/ui/select.tsx
    frontend/src/shared/components/ui/textarea.tsx
    frontend/src/shared/components/NumberInput.tsx
    frontend/src/shared/components/ui/field.tsx
  </files>
  <action>
1. **Input** (`input.tsx`): Add `aria-[invalid=true]:border-destructive` to the className string, right after the existing `border-input` class.

2. **SelectTrigger** (`select.tsx`): Add `aria-[invalid=true]:border-destructive` to the SelectTrigger className string. Note: Radix Select uses `data-` attributes not `aria-invalid`, so ALSO add `data-[invalid=true]:border-destructive`. Check how forms pass invalid state to Select — if they use `aria-invalid` on the trigger, use that. If they set a data attribute, use that. Support both to be safe.

3. **Textarea** (`textarea.tsx`): Add `aria-[invalid=true]:border-destructive` to the className string.

4. **NumberInput** (`NumberInput.tsx`): Add `aria-[invalid=true]:border-destructive` to the className string.

5. **FieldLabel** (`field.tsx`): Add an optional `required?: boolean` prop. When true, render a `<span className="text-destructive">*</span>` after the children. Update the component signature:

```tsx
function FieldLabel({
  className,
  required,
  children,
  ...props
}: React.ComponentProps<typeof Label> & { required?: boolean }) {
  return (
    <Label
      data-slot="field-label"
      className={cn(
        "group/field-label peer/field-label flex w-fit gap-2 leading-snug group-data-[disabled=true]/field:opacity-50",
        "has-[>[data-slot=field]]:w-full has-[>[data-slot=field]]:flex-col has-[>[data-slot=field]]:rounded-md has-[>[data-slot=field]]:border [&>[data-slot=field]]:p-4",
        "has-data-[state=checked]:bg-primary/5 has-data-[state=checked]:border-primary dark:has-data-[state=checked]:bg-primary/10",
        className
      )}
      {...props}
    >
      {children}
      {required && <span className="text-destructive">*</span>}
    </Label>
  )
}
```

This ensures the asterisk is always `text-destructive` (red) regardless of field state.
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Input, SelectTrigger, Textarea, NumberInput all have `aria-[invalid=true]:border-destructive` in their className
    - FieldLabel accepts `required` prop and renders red asterisk when true
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add required prop to FieldLabel across all form files</name>
  <files>
    frontend/src/features/optical/components/FrameFormDialog.tsx
    frontend/src/features/optical/components/LensFormDialog.tsx
    frontend/src/features/optical/components/ComboPackageForm.tsx
    frontend/src/features/optical/components/CreateGlassesOrderForm.tsx
    frontend/src/features/optical/components/WarrantyClaimForm.tsx
    frontend/src/features/pharmacy/components/DrugFormDialog.tsx
    frontend/src/features/pharmacy/components/OtcSaleForm.tsx
    frontend/src/features/pharmacy/components/StockImportForm.tsx
    frontend/src/features/pharmacy/components/SupplierForm.tsx
    frontend/src/features/pharmacy/components/StockAdjustmentDialog.tsx
    frontend/src/features/pharmacy/components/DrugInventoryTable.tsx
    frontend/src/features/admin/components/UserFormDialog.tsx
    frontend/src/features/admin/components/RoleManagementPage.tsx
    frontend/src/features/admin/components/ClinicSettingsPage.tsx
    frontend/src/features/billing/components/DiscountDialog.tsx
    frontend/src/features/billing/components/RefundDialog.tsx
    frontend/src/features/billing/components/PaymentForm.tsx
    frontend/src/features/billing/components/ServiceCatalogFormDialog.tsx
    frontend/src/features/billing/components/ShiftCloseDialog.tsx
    frontend/src/features/billing/components/ShiftOpenDialog.tsx
    frontend/src/features/consumables/components/ConsumableItemForm.tsx
    frontend/src/features/consumables/components/AddStockDialog.tsx
    frontend/src/features/consumables/components/ConsumableAdjustDialog.tsx
    frontend/src/features/clinical/components/DrugPrescriptionForm.tsx
    frontend/src/features/clinical/components/DryEyeForm.tsx
    frontend/src/features/clinical/components/OpticalPrescriptionForm.tsx
    frontend/src/features/clinical/components/RefractionForm.tsx
    frontend/src/features/clinical/components/AmendmentDialog.tsx
    frontend/src/features/treatment/components/TreatmentPackageForm.tsx
    frontend/src/features/treatment/components/TreatmentSessionForm.tsx
    frontend/src/features/treatment/components/ProtocolTemplateForm.tsx
    frontend/src/features/treatment/components/CancellationRequestDialog.tsx
    frontend/src/features/treatment/components/CancellationApprovalQueue.tsx
    frontend/src/features/treatment/components/ModifyPackageDialog.tsx
    frontend/src/features/treatment/components/SwitchTreatmentDialog.tsx
    frontend/src/features/scheduling/components/AppointmentBookingDialog.tsx
    frontend/src/features/patient/components/PatientRegistrationForm.tsx
    frontend/src/features/patient/components/PatientOverviewTab.tsx
    frontend/src/features/patient/components/AllergyForm.tsx
    frontend/src/features/auth/components/LoginForm.tsx
  </files>
  <action>
For EACH form file listed above:

1. Open the file and find its Zod schema (or validation schema). Identify which fields are required (not `.optional()`, not `.nullable()` without default).

2. For every `<FieldLabel>` that corresponds to a required field, add `required` prop: `<FieldLabel required htmlFor={...}>`.

3. For **ClinicSettingsPage** specifically: it currently uses plain `<Label>` and `<div>` instead of the Field system. Migrate it to use `<Field>`, `<FieldLabel>`, and `<FieldError>` components with proper `data-invalid` and `aria-invalid` attributes, matching the pattern used in other forms.

4. Do NOT add `required` to labels for optional fields.

5. Do NOT change any other behavior — only add the `required` prop to existing `<FieldLabel>` components and migrate ClinicSettingsPage.

Pattern to follow — change FROM:
```tsx
<FieldLabel htmlFor={field.name}>{t("some.label")}</FieldLabel>
```
TO:
```tsx
<FieldLabel required htmlFor={field.name}>{t("some.label")}</FieldLabel>
```
  </action>
  <verify>
    <automated>cd D:/projects/ganka/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - All FieldLabel elements for required fields have `required` prop
    - ClinicSettingsPage migrated to Field system with proper error styling
    - TypeScript compiles without errors
    - No optional fields have the required indicator
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Standardized validation error highlighting (red label, red input border, red error message) and required field asterisks (*) across all forms</what-built>
  <how-to-verify>
    1. Start the app (backend port 5255, frontend port 3000)
    2. Log in with Admin@ganka28.com / Admin@123456
    3. Open any form (e.g., Optical > Frames > Add Frame)
    4. Verify required fields show red asterisk (*) next to their labels
    5. Submit the form empty (or clear required fields) to trigger validation
    6. Verify: labels turn red, input borders turn red, error messages are red
    7. Spot-check 2-3 other forms (e.g., Pharmacy > Drug Form, Admin > User Form, Patient Registration)
    8. Check Admin > Clinic Settings page uses the same field system now
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` passes
- Visual: Submitting any form with empty required fields shows red label + red border + red error message
- Visual: Required fields show red asterisk (*) next to label
- ClinicSettingsPage uses Field/FieldLabel/FieldError components
</verification>

<success_criteria>
- All input components (Input, SelectTrigger, Textarea, NumberInput) show red border on validation error
- FieldLabel supports `required` prop with red asterisk
- All required fields across all forms display the asterisk
- ClinicSettingsPage migrated to use Field system
- Consistent error highlighting pattern: red label + red border + red error message
</success_criteria>

<output>
After completion, create `.planning/quick/260324-u4a-standardize-validation-error-highlightin/260324-u4a-SUMMARY.md`
</output>
